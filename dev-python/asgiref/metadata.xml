<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>asgiref.. image httpsapi.travis-ci.orgdjangoasgiref.svg    target httpstravis-ci.orgdjangoasgiref.. image httpsimg.shields.iopypivasgiref.svg    target httpspypi.python.orgpypiasgirefASGI is a standard for Python asynchronous web apps and servers to communicatewith each other, and positioned as an asynchronous successor to WSGI. You canread more at httpsasgi.readthedocs.ioenlatestThis package includes ASGI base libraries, such as Sync-to-async and async-to-sync function wrappers, asgiref.sync Server base classes, asgiref.server A WSGI-to-ASGI adapter, in asgiref.wsgiFunction wrappers-----------------These allow you to wrap or decorate async or sync functions to call them fromthe other style (so you can call async functions from a synchronous thread,or vice-versa).In particular AsyncToSync lets a synchronous subthread stop and wait while the async  function is called on the main threads event loop, and then control is  returned to the thread when the async function is finished. SyncToAsync lets async code call a synchronous function, which is run in  a threadpool and control returned to the async coroutine when the synchronous  function completes.The idea is to make it easier to call synchronous APIs from async code andasynchronous APIs from synchronous code so its easier to transition code fromone style to the other. In the case of Channels, we wrap the (synchronous)Django view system with SyncToAsync to allow it to run inside the (asynchronous)ASGI server.Note that exactly what threads things run in is very specific, and aimed tokeep maximum compatibility with old synchronous code. SeeSynchronous code  Threads below for a full explanation. By default,sync_to_async will run all synchronous code in the program in the samethread for safety reasons you can disable this for more performance withsync_to_async(thread_sensitiveFalse), but make sure that your code doesnot rely on anything bound to threads (like database connections) when you do.Threadlocal replacement-----------------------This is a drop-in replacement for threading.local that works with boththreads and asyncio Tasks. Even better, it will proxy values through from atask-local context to a thread-local context when you use sync_to_asyncto run things in a threadpool, and vice-versa for async_to_sync.If you instead want true thread- and task-safety, you can setthread_critical on the Local object to ensure this instead.Server base classes-------------------Includes a StatelessServer class which provides all the hard work ofwriting a stateless server (as in, does not handle direct incoming socketsbut instead consumes external streams or sockets to work out what is happening).An example of such a server would be a chatbot server that connects out toa central chat server and provides a connection scope per user chatting toit. Theres only one actual connection, but the server has to separate thingsinto several scopes for easier writing of the code.You can see an example of this being used in frequensgi httpsgithub.comandrewgodwinfrequensgi_.WSGI-to-ASGI adapter--------------------Allows you to wrap a WSGI application so it appears as a valid ASGI application.Simply wrap it around your WSGI application like so    asgi_application  WsgiToAsgi(wsgi_application)The WSGI application will be run in a synchronous threadpool, and the wrappedASGI application will be one that accepts http class messages.Please note that not all extended features of WSGI may be supported (such asfile handles for incoming POST bodies).Dependencies------------asgiref requires Python 3.7 or higher.Contributing------------Please refer to themain Channels contributing docs httpsgithub.comdjangochannelsblobmasterCONTRIBUTING.rst_.TestingTo run tests, make sure you have installed the tests extra with the package    cd asgiref    pip install -e .[tests]    pytestBuilding the documentationThe documentation uses Sphinx httpwww.sphinx-doc.org_    cd asgirefdocs    pip install sphinxTo build the docs, you can use the default tools    sphinx-build -b html . _buildhtml   or make html, if youve got make set up    cd _buildhtml    python -m http.server...or you can use sphinx-autobuild to run a server and rebuildreloadyour documentation changes automatically    pip install sphinx-autobuild    sphinx-autobuild . _buildhtmlReleasingTo release, first add details to CHANGELOG.txt and update the version number in asgiref__init__.py.Then, build and push the packages    python -m build    twine upload dist    rm -r build distImplementation Details----------------------Synchronous code  threadsThe asgiref.sync module provides two wrappers that let you go betweenasynchronous and synchronous code at will, while taking care of the rough edgesfor you.Unfortunately, the rough edges are numerous, and the code has to work especiallyhard to keep things in the same thread as much as possible. Notably, therestrictions we are working with are All synchronous code called through SyncToAsync and marked with  thread_sensitive should run in the same thread as each other (and if the  outer layer of the program is synchronous, the main thread) If a thread already has a running async loop, AsyncToSync cant run things  on that loop if its blocked on synchronous code that is above you in the  call stack.The first compromise you get to might be that thread_sensitive code shouldjust run in the same thread and not spawn in a sub-thread, fulfilling the firstrestriction, but that immediately runs you into the second restriction.The only real solution is to essentially have a variant of ThreadPoolExecutorthat executes any thread_sensitive code on the outermost synchronousthread - either the main thread, or a single spawned subthread.This means you now have two basic states If the outermost layer of your program is synchronous, then all async code  run through AsyncToSync will run in a per-call event loop in arbitrary  sub-threads, while all thread_sensitive code will run in the main thread. If the outermost layer of your program is asynchronous, then all async code  runs on the main threads event loop, and all thread_sensitive synchronous  code will run in a single shared sub-thread.Crucially, this means that in both cases there is a thread which is a sharedresource that all thread_sensitive code must run on, and there is a chancethat this thread is currently blocked on its own AsyncToSync call. Thus,AsyncToSync needs to act as an executor for thread code while its blocking.The CurrentThreadExecutor class provides this functionality rather thansimply waiting on a Future, you can call its run_until_future method andit will run submitted code until that Future is done. This means that codeinside the call can then run code on your thread.Maintenance and Security------------------------To report security issues, please contact securitydjangoproject.com. For GPGsignatures and more security process information, seehttpsdocs.djangoproject.comendevinternalssecurity.To report bugs or request new features, please open a new GitHub issue.This repository is part of the Channels project. For the shepherd and maintenance team, please see themain Channels readme httpsgithub.comdjangochannelsblobmasterREADME.rst_.</longdescription>
</pkgmetadata>