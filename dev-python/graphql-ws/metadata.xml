<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>GraphQL WSWebsocket backend for GraphQL subscriptions.Supports the following application serversPython 3 application servers, using asyncio     aiohttp_     websockets compatible servers_ such as Sanic      (via websockets httpsgithub.comaaugustinwebsockets__ library)     Django v2+_Python 2 application servers     Gevent compatible servers_ such as Flask     Django v1.x_      (via channels v1.x httpschannels.readthedocs.ioen1.xinshort.html__)Installation instructionsFor instaling graphql-ws, just run this command in your shell.. code bash    pip install graphql-wsExamplesPython 3 servers----------------Create a subscribable schema like this.. code python    import asyncio    import graphene    class Query(graphene.ObjectType)        hello  graphene.String()        staticmethod        def resolve_hello(obj, info, kwargs)            return world    class Subscription(graphene.ObjectType)        count_seconds  graphene.Float(up_tographene.Int())        async def resolve_count_seconds(root, info, up_to)            for i in range(up_to)                yield i                await asyncio.sleep(1.)            yield up_to    schema  graphene.Schema(queryQuery, subscriptionSubscription)aiohttpThen just plug into your aiohttp server... code python    from graphql_ws.aiohttp import AiohttpSubscriptionServer    from .schema import schema    subscription_server  AiohttpSubscriptionServer(schema)    async def subscriptions(request)        ws  web.WebSocketResponse(protocols(graphql-ws,))        await ws.prepare(request)        await subscription_server.handle(ws)        return ws    app  web.Application()    app.router.add_get(subscriptions, subscriptions)    web.run_app(app, port8000)You can see a full example herehttpsgithub.comgraphql-pythongraphql-wstreemasterexamplesaiohttpwebsockets compatible serversWorks with any framework that uses the websockets library for its websocketimplementation. For this example, plug in your Sanic server... code python    from graphql_ws.websockets_lib import WsLibSubscriptionServer    from . import schema    app  Sanic(__name__)    subscription_server  WsLibSubscriptionServer(schema)    app.websocket(subscriptions, subprotocols[graphql-ws])    async def subscriptions(request, ws)        await subscription_server.handle(ws)        return ws    app.run(host0.0.0.0, port8000)Django v2+Django Channels 2Set up with Django Channels just takes three steps1. Install the apps2. Set up your schema3. Configure the channels router applicationFirst pip install channels and it to your INSTALLED_APPS. If youwant graphiQL, install the graphql_ws.django app beforegraphene_django to serve a graphiQL template that will work withwebsockets.. code python    INSTALLED_APPS  [        channels,        graphql_ws.django,        graphene_django,         ...    ]Point to your schema in Django settings.. code python    GRAPHENE  {        SCHEMA yourproject.schema.schema    }Finally, you can set up channels routing yourself (maybe usinggraphql_ws.django.routing.websocket_urlpatterns in yourURLRouter), or you can just use one of the preset channelsapplications.. code python    ASGI_APPLICATION  graphql_ws.django.routing.application     or    ASGI_APPLICATION  graphql_ws.django.routing.auth_applicationRun .manage.py runserver and go tohttplocalhost8000graphql httplocalhost8000graphql__ to testPython 2  servers-----------------Create a subscribable schema like this.. code python    import graphene    from rx import Observable    class Query(graphene.ObjectType)        hello  graphene.String()        staticmethod        def resolve_hello(obj, info, kwargs)            return world    class Subscription(graphene.ObjectType)        count_seconds  graphene.Float(up_tographene.Int())        async def resolve_count_seconds(root, info, up_to5)            return Observable.interval(1000)                             .map(lambda i {0}.format(i))                             .take_while(lambda i int(i)  up_to)    schema  graphene.Schema(queryQuery, subscriptionSubscription)Gevent compatible serversThen just plug into your Gevent server, for example, Flask.. code python    from flask_sockets import Sockets    from graphql_ws.gevent import GeventSubscriptionServer    from schema import schema    subscription_server  GeventSubscriptionServer(schema)    app.app_protocol  lambda environ_path_info graphql-ws    sockets.route(subscriptions)    def echo_socket(ws)        subscription_server.handle(ws)        return []You can see a full example herehttpsgithub.comgraphql-pythongraphql-wstreemasterexamplesflask_geventDjango v1.xFor Django v1.x and Django Channels v1.x, setup your schema in settings.py.. code python    GRAPHENE  {        SCHEMA yourproject.schema.schema    }Then pip install channels1 and it to your django apps, adding thefollowing to your settings.py.. code python    CHANNELS_WS_PROTOCOLS  [graphql-ws, ]    CHANNEL_LAYERS  {        default {            BACKEND asgiref.inmemory.ChannelLayer,            ROUTING django_subscriptions.urls.channel_routing,        },    }And finally add the channel routes.. code python    from channels.routing import route_class    from graphql_ws.django_channels import GraphQLSubscriptionConsumer    channel_routing  [        route_class(GraphQLSubscriptionConsumer, pathrsubscriptions),    ]You can see a full example herehttpsgithub.comgraphql-pythongraphql-wstreemasterexamplesdjango_subscriptions</longdescription>
</pkgmetadata>