<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Plugin for mypy to support zope.interface[[Build Status](httpstravis-ci.orgShoobxmypy-zope.svgbranchmaster)](httpstravis-ci.orgShoobxmypy-zope)[[Coverage Status](httpscoveralls.ioreposgithubShoobxmypy-zopebadge.svg)](httpscoveralls.iogithubShoobxmypy-zope)[[Checked with mypy](httpwww.mypy-lang.orgstaticmypy_badge.svg)](httpmypy-lang.org)The goal is to be able to make zope interfaces to be treated as types in mypysense. UsageInstall both mypy and mypy-zopeshpip install mypy-zopeEdit mypy.ini file in your project to enable the pluginini[mypy]namespace_packagesTruepluginsmypy_zopepluginYoure done You can now check your project with mypyshmypy your-project-dir What is supportedYou can browse[sample files](httpsgithub.comShoobxmypy-zopetreemastertestssamples)to get some sense on what features are supported and how they are handled. Interface declarationsYou can define the interface and provide implementationpythonclass IAnimal(zope.interface.Interface)    def say() - None        passzope.interface.implementer(IAnimal)class Cow(object)    def say(self) - None        print(Moooo)animal IAnimal  Cow()animal.say()The interface IAnimal will be treated as superclass of the implementationCow you will be able to pass an implementation to functions accepting aninterface and all the usual polymorphism tricks.It is also possible to declare the implementation using classImplementsfunction  with the same effect as imlementer decorator. This is useful ifyou do not control the code that defines the implementation class.pythonclassImplements(Cow, IAnimal)animal IAnimal  Cow() Schema field type inferenceA limited support for defining attributes as zope.schema.Fields is supported toopythonclass IAnimal(zope.interface.Interface)    number_of_legs  zope.schema.Int(titleNumber of legs)zope.interface.implementer(IAnimal)class Cow(object)    number_of_legs  4In context of an interface, some known zope.schema field types areautomatically translated to python types, so the number_of_legs attributes isgetting the type int in the example above. That means mypy will report anerror if you try to assign string to that attribute on an instance of IAnimaltype. Custom fields or fields not recognized by plugin are given type Any. Field propertiesSupport for zope.schema.FieldProperty is limited, because type information isnot transferred from an interface to implementation attribute, but mypy doesntreport errors on sources like thispythonclass IAnimal(zope.interface.Interface)    number_of_legs  zope.schema.Int(titleNumber of legs)zope.interface.implementer(IAnimal)class Cow(object)    number_of_legs  zope.schema.FieldProperty(IAnimal[number_of_legs])The type of Cow.number_of_legs will become Any in this case, even thoughIAnimal.number_of_legs would be inferred as int. Adaptation patternZope interfaces can be called to lookup an adapter, like thispythonclass IEUPowerSocket(zope.interface.Interface)    def fit()        passadapter  IEUPowerSocket(us_plug)adapter.fit()Type of the adapter variable will be set to IEUPowerSocket. Conditional type inferenceWhen using zope.interfaces implementedBy() and providedBy() methodsin an if statement, mypy will know which type it is inside those statements.pythonif IAnimal.providedBy(ob)    ob.number_of_legs + 2 Declaration of overloaded methods in interfacesSimilarly to regular [overloadedfunctions](httpsdocs.python.org3librarytyping.htmltyping.overload),overload declarations are supported in interfaces as wellpythonclass IAnimal(zope.interface.Interface)    overload    def say() - str        ...    overload    def say(count int) - List[str]        ...    def say(count int  None) - Union[str, List[str]]        passzope.interface.implementer(IAnimal)class Cow(object)    overload    def say(self) - str        ...    overload    def say(self, count int) - List[str]        ...    def say(self, count int  None) - Union[str, List[str]]        if count is None            return Mooo        return [Mooo]  count Type stubs for zope.interface and zope.schemamypy-zope ships with type stubs (.pyi files) for zope.interface andzope.schema packages. They are enabled automatically as soon as plugin isenabled. What is not supportedThese zope.interface features are not supported Declaring modules as interface implementers. Type inference for zope.schema.List and zope.schema.Dict fields. Stub files are largely incomplete Interface compatibility checker will not type-check non-method attributes Ready to useCurrently, the project is used in production in various substantially largeprojects and considered production-grade, however there still might be subtlebugs around. Suggestions and pull requests are welcomed</longdescription>
</pkgmetadata>