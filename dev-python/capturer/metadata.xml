<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>capturer Easily capture stdoutstderr of the current process and subprocesses.. image httpstravis-ci.orgxoloxpython-capturer.svgbranchmaster   target httpstravis-ci.orgxoloxpython-capturer.. image httpscoveralls.ioreposxoloxpython-capturerbadge.svgbranchmaster  target httpscoveralls.iorxoloxpython-capturerbranchmasterThe capturer package makes it easy to capture the stdout_ and stderr_ streamsof the current process and subprocesses. Output can be relayed to theterminal in real time but is also available to the Python program foradditional processing. Its currently tested on cPython 2.7, 3.5+ and PyPy(2.7). Its tested on Linux and Mac OS X and may work on other unixes butdefinitely wont work on Windows (due to the use of the platform dependent pty_module). For usage instructions please refer to the documentation_... contents   localStatus------The capturer package was developed as a proof of concept over the course of aweekend, because I was curious to see if it could be done (reliably). After aweekend of extensive testing it seems to work fairly well so Im publishing theinitial release as version 1.0, however I still consider this a proof ofconcept because I dont have extensive production experience using it yet.Heres hoping it works as well in practice as it did during my testing -).Installation------------The capturer package is available on PyPI_ which means installation should beas simple as.. code-block console    pip install capturerTheres actually a multitude of ways to install Python packages (e.g. the peruser site-packages directory_, virtual environments_ or just installingsystem wide) and I have no intention of getting into that discussion here, soif this intimidates you then read up on your options before returning to theseinstructions -).Getting started---------------The easiest way to capture output is to use a context manager.. code-block python   import subprocess   from capturer import CaptureOutput   with CaptureOutput() as capturer        Generate some output from Python.       print Output from Python        Generate output from a subprocess.       subprocess.call([echo, Output from a subprocess])        Get the output in each of the supported formats.       assert capturer.get_bytes()  bOutput from PythonrnOutput from a subprocessrn       assert capturer.get_lines()  [uOutput from Python, uOutput from a subprocess]       assert capturer.get_text()  uOutput from PythonnOutput from a subprocessThe use of a context manager (the with statement_) ensures that outputcapturing is enabled and disabled at the appropriate time, regardless ofwhether exceptions interrupt the normal flow of processing.Note that the first call to get_bytes()_, get_lines()_ or get_text()_will stop the capturing of output by default. This is intended as a sanedefault to prevent partial reads (which can be confusing as hell when you donthave experience with them). So we could have simply used print to showthe results without causing a recursive captured output is printed and thencaptured again loop. Theres an optional partialTrue keyword argumentthat can be used to disable this behavior (please refer to the documentation_for details).Design choices--------------There are existing solutions out there to capture the stdout_ and stderr_streams of (Python) processes. The capturer package was created for a veryspecific use case that wasnt catered for by existing solutions (that I couldfind). This section documents the design choices that guided the development ofthe capturer package.. contents  localIntercepts writes to low level file descriptorsLibraries like capture_ and iocapture_ change Pythons sys.stdout_ andsys.stderr_ file objects to fake file objects (using StringIO_). This enablescapturing of (most) output written to the stdout_ and stderr_ streams from thesame Python process, however any output from subprocesses is unaffected by theredirection and not captured.The capturer package instead intercepts writes to low level file descriptors(similar to and inspired by how pytest does it_). This enables capturing ofoutput written to the standard output and error streams from the same Pythonprocess as well as any subprocesses.Uses a pseudo terminal to emulate a real terminalThe capturer package uses a pseudo terminal created using pty.openpty()_ tocapture output. This means subprocesses will use ANSI escape sequences becausethey think theyre connected to a terminal. In the current implementation youcant opt out of this, but feel free to submit a feature request to change this-). This does have some drawbacks- The use of pty.openpty()_ means you need to be running in a UNIX like  environment for capturer to work (Windows definitely isnt supported).- All output captured is relayed on the stderr_ stream by default, so capturing  changes the semantics of your programs. How much this matters obviously  depends on your use case. For the use cases that triggered me to create  capturer it doesnt matter, which explains why this is the default mode.  There is experimental support for capturing stdout_ and stderr_ separately  and relaying captured output to the appropriate original stream. Basically  you call CaptureOutput(mergedFalse) and then you use the stdout and  stderr attributes of the CaptureOutput object to get at the output  captured on each stream.  I say experimental because this method of capturing can unintentionally  change the order in which captured output is emitted, in order to avoid  interleaving output emitted on the stdout_ and stderr_ streams (which would  most likely result in incomprehensible output). Basically output is relayed  on each stream separately after each line break. This means interactive  prompts that block on reading from standard input without emitting a line  break wont show up (until its too late -).Relays output to the terminal in real timeThe main use case of capturer is to capture all output of a snippet of Pythoncode (including any output by subprocesses) but also relay the output to theterminal in real time. This has a couple of useful properties- Long running operations can provide the operator with real time feedback by  emitting output on the terminal. This sounds obvious (and it is) but it is  non-trivial to implement (an understatement -) when you also want to  capture the output.- Programs like gpg_ and ssh_ that use interactive password prompts will render  their password prompt on the terminal in real time. This avoids the awkward  interaction where a password prompt is silenced but the program still hangs,  waiting for input on stdin_.Contact-------The latest version of capturer is available on PyPI_ and GitHub_. Thedocumentation is hosted on Read the Docs_ and includes a changelog_. For bugreports please create an issue on GitHub_. If you have questions, suggestions,etc. feel free to send me an e-mail at peterpeterodding.com_.License-------This software is licensed under the MIT license_. 2020 Peter Odding.A big thanks goes out to the pytest_ developers because pytests mechanism forcapturing the output of subprocesses provided inspiration for the capturerpackage. No code was copied, but both projects are MIT licensed anyway, so itsnot like its very relevant -)... External references.. _capture httpspypi.orgprojectcapture.. _changelog httpscapturer.readthedocs.ioenlatestchangelog.html.. _documentation httpscapturer.readthedocs.io.. _get_bytes() httpscapturer.readthedocs.ioenlatestapi.htmlcapturer.CaptureOutput.get_bytes.. _get_lines() httpscapturer.readthedocs.ioenlatestapi.htmlcapturer.CaptureOutput.get_lines.. _get_text() httpscapturer.readthedocs.ioenlatestapi.htmlcapturer.CaptureOutput.get_text.. _GitHub httpsgithub.comxoloxpython-capturer.. _gpg httpsen.wikipedia.orgwikiGNU_Privacy_Guard.. _how pytest does it httpspytest.orglatestcapture.html.. _iocapture httpspypi.orgprojectiocapture.. _MIT license httpen.wikipedia.orgwikiMIT_License.. _per user site-packages directory httpswww.python.orgdevpepspep-0370.. _peterpeterodding.com peterpeterodding.com.. _pty.openpty() httpsdocs.python.org2librarypty.htmlpty.openpty.. _pty httpsdocs.python.org2librarypty.html.. _PyPI httpspypi.orgprojectcapturer.. _pytest httpspypi.orgprojectpytest.. _Read the Docs httpscapturer.readthedocs.io.. _ssh httpsen.wikipedia.orgwikiSecure_Shell.. _stderr httpsen.wikipedia.orgwikiStandard_streamsStandard_error_.28stderr.29.. _stdin httpsen.wikipedia.orgwikiStandard_streamsStandard_input_.28stdin.29.. _stdout httpsen.wikipedia.orgwikiStandard_streamsStandard_output_.28stdout.29.. _StringIO httpsdocs.python.org2librarystringio.html.. _sys.stderr httpsdocs.python.org2librarysys.htmlsys.stderr.. _sys.stdout httpsdocs.python.org2librarysys.htmlsys.stdout.. _the with statement httpsdocs.python.org2referencecompound_stmts.htmlthe-with-statement.. _virtual environments httpdocs.python-guide.orgenlatestdevvirtualenvs</longdescription>
</pkgmetadata>