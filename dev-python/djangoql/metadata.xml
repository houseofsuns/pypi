<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>DjangoQL.. image httpsgithub.comivelumdjangoqlworkflowsTestsbadge.svg        target httpsgithub.comivelumdjangoqlactionsqueryworkflow3ATestsAdvanced search language for Django, with auto-completion. Supports logicaloperators, parenthesis, table joins, and works with any Django model. Tested onPython 2.7, 3.5 - 3.10, Django 1.8 - 4.0. The auto-completion feature has beentested in Chrome, Firefox, Safari, IE9+.See a video DjangoQL demo httpsyoutu.beoKVff4dHZB8_.. image httpsraw.githubusercontent.comivelumdjangoqlmasterdjangoqlstaticdjangoqlimgcompletion_example_scaled.pngDjangoQL is used bylogo1 logo2 logo3 logo4.. logo1 image httpsraw.githubusercontent.comivelumdjangoqlmasterassetsredhat.svg   width 22   target httpswww.redhat.com.. logo2 image httpsraw.githubusercontent.comivelumdjangoqlmasterassetsteamplify.svg   width 22   target httpsteamplify.com.. logo3 image httpsraw.githubusercontent.comivelumdjangoqlmasterassetspolice1.svg   width 22   target httpswww.police1.com.. logo4 image httpsraw.githubusercontent.comivelumdjangoqlmasterassets15-five.svg   width 22   target httpswww.15five.comIs your project using DjangoQL Please submit a PR and let us knowContents-------- Installation_ Add it to your Django admin_ Using DjangoQL with the standard Django admin search_ Language reference_ DjangoQL Schema_ Custom search fields_ Can I use it outside of Django admin_ Using completion widget outside of Django admin_Installation------------.. code shell     pip install djangoqlAdd djangoql to INSTALLED_APPS in your settings.py.. code python    INSTALLED_APPS  [        ...        djangoql,        ...    ]Add it to your Django admin---------------------------Adding DjangoQLSearchMixin your model admin will replace the standardDjango search functionality with DjangoQL search. Example.. code python    from django.contrib import admin    from djangoql.admin import DjangoQLSearchMixin    from .models import Book    admin.register(Book)    class BookAdmin(DjangoQLSearchMixin, admin.ModelAdmin)        passUsing DjangoQL with the standard Django admin search----------------------------------------------------DjangoQL will recognize if you have defined search_fields in your ModelAdminclass, and doing so will allow you to choose between an advanced search withDjangoQL and a standard Django search (as specified by search fields). Example.. code python    admin.register(Book)    class BookAdmin(DjangoQLSearchMixin, admin.ModelAdmin)        search_fields  (title, author__name)For the example above, a checkbox that controls search mode will appear nearthe search input. If the checkbox is on, then DjanqoQL search is used. There isalso an option that controls if that checkbox is enabled by default -djangoql_completion_enabled_by_default (set to True by default).. code python    admin.register(Book)    class BookAdmin(DjangoQLSearchMixin, admin.ModelAdmin)        search_fields  (title, author__name)        djangoql_completion_enabled_by_default  FalseIf you dont want two search modes, simply remove search_fields from yourModelAdmin class.Language reference------------------DjangoQL is shipped with comprehensive Syntax Help, which can be found in Djangoadmin (see the Syntax Help link in auto-completion popup). Heres a quicksummaryDjangoQLs syntax resembles Pythons, with some minordifferences. Basically you just reference model fields as you wouldin Python code, then apply comparison and logical operators andparenthesis. DjangoQL is case-sensitive.- model fields exactly as they are defined in Python code. Access  nested properties via ., for example author.last_name- strings must be double-quoted. Single quotes are not supported.  To escape a double quote use - boolean and null values True, False, None. Please note  that they can be combined only with equality operators, so you can  write published  False or date_published  None, but  published  False will cause an error- logical operators and, or- comparison operators , , , , ,   - work as you expect- string-specific comparison operators startswith, not startswith,  endswith, not endswith - work as you expect. Test whether or not a  string contains a substring  and  (translated into  __icontains under the hood).  Example name endswith peace or author.last_name  tolstoy- date-specific comparison operators, compare by date part  and .  Example date_published  2021-11 - find books published in Nov, 2021- test a value vs. list in, not in. Example  pk in (2, 3).DjangoQL Schema---------------Schema defines limitations - what you can do with a DjangoQL query.If you dont specify any schema, DjangoQL will provide a defaultschema for you. This will walk recursively through all model fields andrelations and include everything it finds in the schema, sousers would be able to search through everything. Sometimesthis is not what you want, either due to DB performance or securityconcerns. If youd like to limit search models or fields, you shoulddefine a schema. Heres an example.. code python    class UserQLSchema(DjangoQLSchema)        exclude  (Book,)        suggest_options  {            Group [name],        }        def get_fields(self, model)            if model  Group                return [name]            return super(UserQLSchema, self).get_fields(model)    admin.register(User)    class CustomUserAdmin(DjangoQLSearchMixin, UserAdmin)        djangoql_schema  UserQLSchemaIn the example above we created a schema that does 3 things- excludes the Book model from search via exclude option. Instead of  exclude you may also use include, which limits a search to  listed models only- limits available search fields for Group model to only the name field  , in the .get_fields() method- enables completion options for Group names via suggest_options.An important note about suggest_options it looks for the choices modelfield parameter first, and if its not specified - it will synchronously pullall values for given model fields, so you should avoid large querysets there.If youd like to define custom suggestion options, see below.Custom search fields--------------------Deeper search customization can be achieved with custom search fields. Customsearch fields can be used to search by annotations, define custom suggestionoptions, or define fully custom search logic. In djangoql.schema, DjangoQLdefines the following base field classes that you maysubclass to define your own behavior IntField FloatField StrField BoolField DateField DateTimeField RelationFieldHere are examples for common use casesSearch by queryset annotations.. code python    from djangoql.schema import DjangoQLSchema, IntField    class UserQLSchema(DjangoQLSchema)        def get_fields(self, model)            fields  super(UserQLSchema, self).get_fields(model)            if model  User                fields + [IntField(namegroups_count)]            return fields    admin.register(User)    class CustomUserAdmin(DjangoQLSearchMixin, UserAdmin)        djangoql_schema  UserQLSchema        def get_queryset(self, request)            qs  super(CustomUserAdmin, self).get_queryset(request)            return qs.annotate(groups_countCount(groups))Lets take a closer look at whats happening in the example above. First, weadd groups_count annotation to the queryset that is used by Django adminin the CustomUserAdmin.get_queryset() method. It would contain the numberof groups a user belongs to. As our queryset now pulls this column, we canfilter by it. It just needs to be included in the schema. InUserQLSchema.get_fields() we define a custom integer search field for theUser model. Its name should match the name of the column in our queryset.Custom suggestion options.. code python    from djangoql.schema import DjangoQLSchema, StrField    class GroupNameField(StrField)        model  Group        name  name        suggest_options  True        def get_options(self, search)            return super(GroupNameField, self)                .get_options(search)                .annotate(users_countCount(user))                .order_by(-users_count)    class UserQLSchema(DjangoQLSchema)        def get_fields(self, model)            if model  Group                return [id, GroupNameField()]            return super(UserQLSchema, self).get_fields(model)    admin.register(User)    class CustomUserAdmin(DjangoQLSearchMixin, UserAdmin)        djangoql_schema  UserQLSchemaIn this example weve defined a custom GroupNameField that sorts suggestionsfor group names by popularity (no. of users in a group) instead of defaultalphabetical sorting.Custom search lookupDjangoQL base fields provide two basic methods that you can override tosubstitute either search column, search value, or both -.get_lookup_name() and .get_lookup_value(value).. code python    class UserDateJoinedYear(IntField)        name  date_joined_year        def get_lookup_name(self)            return date_joined__year    class UserQLSchema(DjangoQLSchema)        def get_fields(self, model)            fields  super(UserQLSchema, self).get_fields(model)            if model  User                fields + [UserDateJoinedYear()]            return fields    admin.register(User)    class CustomUserAdmin(DjangoQLSearchMixin, UserAdmin)        djangoql_schema  UserQLSchemaIn this example weve defined the custom date_joined_year search field forusers, and used the built-in Django __year filter option in.get_lookup_name() to filter by date year only. Similarly you can use.get_lookup_value(value) hook to modify a search value before its used inthe filter.Fully custom search lookup.get_lookup_name() and .get_lookup_value(value) hooks cover manysimple use cases, but sometimes theyre not enough and you want a fully customsearch logic. In such cases you can override main .get_lookup() method ofa field. Example below demonstrates User age search.. code python    from djangoql.schema import DjangoQLSchema, IntField    class UserAgeField(IntField)                Search by given number of full years                model  User        name  age        def get_lookup_name(self)                        Well be doing comparisons vs. this model field                        return date_joined        def get_lookup(self, path, operator, value)                        The lookup should support with all operators compatible with IntField                        if operator  in                result  None                for year in value                    condition  self.get_lookup(path, , year)                    result  condition if result is None else result  condition                return result            elif operator  not in                result  None                for year in value                    condition  self.get_lookup(path, , year)                    result  condition if result is None else result  condition                return result            value  self.get_lookup_value(value)            search_field  __.join(path + [self.get_lookup_name()])            year_start  self.years_ago(value + 1)            year_end  self.years_ago(value)            if operator                  return (                    Q({s__gt  search_field year_start})                     Q({s__lte  search_field year_end})                )            elif operator                  return (                    Q({s__lte  search_field year_start})                     Q({s__gt  search_field year_end})                )            elif operator                  return Q({s__lt  search_field year_start})            elif operator                  return Q({s__lte  search_field year_end})            elif operator                  return Q({s__gt  search_field year_end})            elif operator                  return Q({s__gte  search_field year_start})        def years_ago(self, n)            timestamp  now()            try                return timestamp.replace(yeartimestamp.year - n)            except ValueError                 February 29                return timestamp.replace(month2, day28, yeartimestamp.year - n)    class UserQLSchema(DjangoQLSchema)        def get_fields(self, model)            fields  super(UserQLSchema, self).get_fields(model)            if model  User                fields + [UserAgeField()]            return fields    admin.register(User)    class CustomUserAdmin(DjangoQLSearchMixin, UserAdmin)        djangoql_schema  UserQLSchemaCan I use it outside of Django admin-------------------------------------Sure. You can add DjangoQL search functionality to any Django model usingDjangoQLQuerySet.. code python    from django.db import models    from djangoql.queryset import DjangoQLQuerySet    class Book(models.Model)        name  models.CharField(max_length255)        author  models.ForeignKey(auth.User)        objects  DjangoQLQuerySet.as_manager()With the example above you can perform a search like this.. code python    qs  Book.objects.djangoql(        name  war and author.last_name  Tolstoy    )It returns a normal queryset, so you can extend it and reuse ifnecessary. The following code works fine.. code python    print(qs.count())Alternatively you can add DjangoQL search to any existing queryset,even if its not an instance of DjangoQLQuerySet.. code python    from django.contrib.auth.models import User    from djangoql.queryset import apply_search    qs  User.objects.all()    qs  apply_search(qs, groups  None)    print(qs.exists())Schemas can be specified either as a queryset option, or passedto .djangoql() queryset method directly.. code python    class BookQuerySet(DjangoQLQuerySet)        djangoql_schema  BookSchema    class Book(models.Model)        ...        objects  BookQuerySet.as_manager()     Now, Book.objects.djangoql() will use BookSchema by default    Book.objects.djangoql(name  Peace)   uses BookSchema     Overriding default queryset schema with AnotherSchema    Book.objects.djangoql(name  Peace, schemaAnotherSchema)You can also provide schema as an option for apply_search().. code python    qs  User.objects.all()    qs  apply_search(qs, groups  None, schemaCustomSchema)Using completion widget outside of Django admin-----------------------------------------------The completion widget is not tightly coupled to Django admin, so you can easilyuse it outside of the admin if you want. The widget isavailable on npm httpswww.npmjs.compackagedjangoql-completion_ as astandalone package.See the source code and the docs in thedjangoql-completion httpsgithub.comivelumdjangoql-completion_repo on GitHub.The completion widget is also bundled with thedjangoql httpspypi.orgprojectdjangoql_ Python package on PyPI. Ifyoure not using Webpack or another JavaScript bundler, you can use thepre-built version that ships with the Python package. Here is an exampleTemplate code, completion_demo.html.. code html    { load static }    DOCTYPE html    html langen    head      meta charsetUTF-8      titleDjangoQL completion demotitle      link relstylesheet typetextcss href{ static djangoqlcsscompletion.css }       script src{ static djangoqljscompletion.js }script    head    body      form action methodget        p stylecolor red{{ error }}p        textarea nameq cols40 rows1 autofocus{{ q }}textarea      form      ul      { for item in search_results }        li{{ item }}li      { endfor }      ul      script        DjangoQL.DOMReady(function () {          new DjangoQL({             either JS object with a result of DjangoQLSchema(MyModel).as_dict(),             or an URL from which this information could be loaded asynchronously            introspections {{ introspectionssafe }},             css selector for query input or HTMLElement object.             It should be a textarea            selector textarea[nameq],             optional, you can provide URL for Syntax Help link here.             If not specified, Syntax Help link will be hidden.            syntaxHelp null,             optional, enable textarea auto-resize feature. If enabled,             textarea will automatically grow its height when entered text             doesnt fit, and shrink back when text is removed. The purpose             of this is to see full search query without scrolling, could be             helpful for really long queries.            autoResize true          })        })      script    body    htmlAnd in your views.py.. code python    import json    from django.contrib.auth.models import Group, User    from django.shortcuts import render_to_response    from django.views.decorators.http import require_GET    from djangoql.exceptions import DjangoQLError    from djangoql.queryset import apply_search    from djangoql.schema import DjangoQLSchema    from djangoql.serializers import DjangoQLSchemaSerializer    class UserQLSchema(DjangoQLSchema)        include  (User, Group)        suggest_options  {            Group [name],        }    require_GET    def completion_demo(request)        q  request.GET.get(q, )        error          query  User.objects.all().order_by(username)        if q            try                query  apply_search(query, q, schemaUserQLSchema)            except DjangoQLError as e                query  query.none()                error  str(e)         You may want to use SuggestionsAPISerializer and an additional API         endpoint (see in djangoql.views) for asynchronous suggestions loading        introspections  DjangoQLSchemaSerializer().serialize(          UserQLSchema(query.model),        )        return render_to_response(completion_demo.html, {            q q,            error error,            search_results query,            introspections json.dumps(introspections),        })License-------MIT</longdescription>
</pkgmetadata>