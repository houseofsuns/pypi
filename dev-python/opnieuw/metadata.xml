<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>OpnieuwOpnieuw is a general-purpose retrying library, written in Python, inorder to simplify the task of adding retry behavior to both synchronous as wellas asynchronous tasks. Opnieuw is easy and straightforward to use.Opnieuw makes it easy to add robust retries  There is a single retry strategy, exponential backoff with jitter, which   makes it impossible to forget to add jitter.  It has just two parameters, to eliminate the possibility of invalid and   nonsensical combinations.  Parameters are named clearly, to avoid confusing e.g. number of calls   (including the initial one) with number of retries (excluding the initial   call).  The parameters are intuitive instead of configuring the base delay for   exponential backoff, Opnieuw accepts a maximum number of calls to make, and   maximum time after the first call to still retry.  Time units are clear from parameter names to make the decorated code obvious,   and readable without needing to refer to documentation.See our [announcement post][post] for a bit more background on why we wroteOpnieuw.Example-------Suppose we want to parse httpstech.channable.comatom.xml and we want toadd a retry to handle a specific network Exception. We can add Opnieuw to ournetwork handler as followspythonimport requestsfrom requests.exceptions import ConnectionError, HTTPErrorfrom opnieuw import retryretry(    retry_on_exceptions(ConnectionError, HTTPError),    max_calls_total4,    retry_window_after_first_call_in_seconds60,)def get_page() - str    response  requests.get(httpstech.channable.comatom.xml)    return response.text  retry_on_exceptions specifies which exceptions we want to retry on.  max_calls_total is the maximum number of times that the decorated function   gets called, including the initial call.  retry_window_after_first_call_in_seconds is the maximum number of seconds   after the first call was initiated, where we would still do a new attempt.Features--------  Generic decorator API  Specify retry exception (i.e. type of exception that we want retry)  [Exponential backoff with jitter][exponential-backoff]  Pre-shipped list of popular exceptions, which can easily be expandedInstallation------------To install Opnieuw, simply     pip install opnieuwMore examples--------The short example above provides a concise demonstration of how Opnieuw couldbe used. Lets dig deeper into Opnieuw and add another exception toretry_on_exceptions to do a retry onpythonfrom urllib.error import URLErrorimport requestsfrom opnieuw import RetryException, retryretry(    retry_on_exceptions(ConnectionError, RetryException, URLError),    max_calls_total4,    retry_window_after_first_call_in_seconds60,)def get_page() - str    response  requests.get(httpstech.channable.comatom.xml)    if response.status_code  200        raise RetryException    return response.textWe can pass the name of exception we want do retry for or a tuple of exceptionsto the retry_on_exceptions. As mentioned earlier Opnieuw was developed tomake it more convenient to add retry behavior to any task.Lets make it a little bit more generic and define a list of retry exceptionsthat should trigger a retry of the functionpythonSTANDARD_HTTP_EXCEPTIONS  (    ConnectionError,    ProtocolError,    RetryException,    ...)retry(    retry_on_exceptionsSTANDARD_HTTP_EXCEPTIONS,    max_calls_total4,    retry_window_after_first_call_in_seconds60,)def get_page() - str    response  requests.post(httpstech.channable.comatom.xml)    return response.textNow our retry is more generic, as exceptions raised which are inSTANDARD_HTTP_EXCEPTIONS will be retried.If you want retry behavior for async tasks, then there is also an async retrywhich basically work the same way, but for async tasks.Here is the example above but in async modepythonfrom opnieuw import retry_asyncSTANDARD_HTTP_EXCEPTIONS  (    ConnectionError,    EOFError,    RetryException,    ...)retry_async(    retry_on_exceptionsSTANDARD_HTTP_EXCEPTIONS,    max_calls_total4,    retry_window_after_first_call_in_seconds60,)async def get_page() - str    response  requests.get(httpstech.channable.comatom.xml)    return response.text[post] httpstech.channable.composts2020-02-05-opnieuw.html[exponential-backoff] httpsaws.amazon.comblogsarchitectureexponential-backoff-and-jitter</longdescription>
</pkgmetadata>