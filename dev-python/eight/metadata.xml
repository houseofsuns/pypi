<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>eight Python 2 to the power of 3Eight is a Python module that provides a minimalist compatibility layer between Python 3 and 2. Eight lets you writecode for Python 3.3+ while providing limited compatibility with Python 2.7 with no code changes.  Eight is inspired bysix httpspythonhosted.orgsix_, nine httpsgithub.comnandoflorestannine_, and python-futurehttpsgithub.comPythonCharmerspython-future_, but provides better internationalization (i18n) support, is morelightweight, easier to use, and unambiguously biased toward Python 3 code if you remove eight from your code, it willcontinue to function exactly as it did with eight on Python 3.To write code for Python 3 that is portable to Python 2, you may also want to read Armin Ronachers excellent Python 3porting guide httplucumr.pocoo.org2013521porting-to-python-3-redux_, as well as the officialporting guide httpdocs.python.org3howtopyporting.html_.Writing from eight import  in your code is a no-op in Python 3. In Python 2, it binds a bunch of Python 3 names totheir Python 2 equivalents. Also, if you need to import a module or module member that was renamed in Python 3, writingfrom eight import module will do the right thing (equivalent to import module on Python 3 and importold_name as module on Python 2). Finally, eight can optionally wrap your standard streams and environment variableIO to use text, not bytes (see below).Installation------------    pip install eightSynopsis--------.. code-block python    from eight import     from eight import queue    from eight.collections import UserList, dequeIf you use print, division, non-ASCII literals, or relative imports, you should also add this future importhttpdocs.python.org3library__future__.html_ at the top of each source file.. code-block python    from __future__ import (print_function, division, unicode_literals, absolute_import)Wrapping stdio--------------Eight provides wrappers for sys.stdin, sys.stdout, and sys.stderr to make them (and methods that use them)behave like they do on Python 3. Specifically, in Python 3 these streams accept text data, and their .buffer attributesrefer to the underlying streams that accept bytes. Eight uses the io httpdocs.python.org2libraryio.html_ moduleto do the same for you, but subclasses the TextIOWrapper class for sys.stdout and sys.stderr to coerce non-unicodeinput to unicode on Python 2 (otherwise, because of the Python 2 semantics, things like exception printing cease to work).To enable stdio wrapping, use the following.. code-block python    import eight    eight.wrap_stdio()To revert the effects of this on any of the streams, use the detach method, e.g. sys.stdin  sys.stdin.detach() (butremember to condition this on eight.USING_PYTHON2). See the io module documentationhttpdocs.python.org2libraryio.html_ for more information.Decoding command-line arguments-------------------------------Eight provides a utility function to decode the contents of sys.argv on Python 2 (as Python 3 does). It usessys.stdin.encoding as the encoding to do so.. code-block python    import eight    eight.decode_command_line_args()The call to decode_command_line_args() replaces sys.argv with its decoded contents and returns the new contents.On Python 3, the call is a no-op (it returns sys.argv and leaves it intact).Wrapping environment variable getters and setters-------------------------------------------------Eight provides utility wrappers to help bring Python 2 environment variable access and assignment in line with Python3 encode the input to os.putenv (which is used for statements like os.environ[x]  y) and decode the output ofos.getenv (used for x  os.environ[y]). Use wrap_os_environ_io() to monkey-patch these wrappers into theos module.. code-block python    import eight    eight.wrap_os_environ_io()On Python 3, the call is a no-op.Selecting from the buffet-------------------------You can see what from eight import  will do by running IPython httpsgithub.comipythonipython_ and typingimport eight, then eight.TAB. Here is a full list of whats available ascii bytes chr filter hex input int map oct open range round str super zipYou can import these symbols by listing them explicitly. If for any reason you see an issue with importing them all (whichis recommended), you can of course import a subset.In addition to names imported by from eight import , the following modules are available and should be imported byname using from eight import name when needed queue (old name Queue) builtins (old name __builtin__) copyreg (old name copy_reg) configparser (old name ConfigParser) reprlib (old name repr) winreg (old name _winreg) _thread (old name thread) _dummy_thread (old name dummy_thread)The following modules have attributes which resided elsewhere in Python 2 TODOAcknowledgments---------------Python-future httpsgithub.comPythonCharmerspython-future_ for doing a bunch of heavy lifting on backports ofPython 3 features.Links----- Project home page (GitHub) httpsgithub.comkislyukeight_ Documentation (Read the Docs) httpseight.readthedocs.ioenlatest_ Package distribution (PyPI) httpspypi.python.orgpypieight_BugsPlease report bugs, issues, feature requests, etc. on GitHub httpsgithub.comkislyukeightissues_.License-------Licensed under the terms of the Apache License, Version 2.0 httpwww.apache.orglicensesLICENSE-2.0_... image httpsimg.shields.iotraviskislyukeight.svg        target httpstravis-ci.orgkislyukeight.. image httpscodecov.iogithubkislyukeightcoverage.svgbranchmaster        target httpscodecov.iogithubkislyukeightbranchmaster.. image httpsimg.shields.iopypiveight.svg        target httpspypi.python.orgpypieight.. image httpsimg.shields.iopypileight.svg        target httpspypi.python.orgpypieight.. image httpsreadthedocs.orgprojectseightbadgeversionlatest        target httpseight.readthedocs.io</longdescription>
</pkgmetadata>