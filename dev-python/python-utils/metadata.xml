<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Useful Python Utils.. image httpsgithub.comWoLpHpython-utilsactionsworkflowsmain.ymlbadge.svgbranchmaster  target httpsgithub.comWoLpHpython-utilsactionsworkflowsmain.yml.. image httpscoveralls.ioreposWoLpHpython-utilsbadge.svgbranchmaster  target httpscoveralls.iorWoLpHpython-utilsbranchmasterPython Utils is a collection of small Python functions andclasses which make common patterns shorter and easier. It is by no means acomplete collection but it has served me quite a bit in the past and I willkeep extending it.One of the libraries using Python Utils is Django Utils.Documentation is available at httpspython-utils.readthedocs.orgenlatestLinks----- - The source httpsgithub.comWoLpHpython-utils - Project page httpspypi.python.orgpypipython-utils - Reporting bugs httpsgithub.comWoLpHpython-utilsissues - Documentation httpspython-utils.readthedocs.ioenlatest - My blog httpswol.phRequirements for installing------------------------------------------------------------------------------For the Python 3+ release (i.e. v3.0.0 or higher) there are no requirements.For the Python 2 compatible version (v2.x.x) the six package is needed.Installation------------------------------------------------------------------------------The package can be installed through pip (this is the recommended method).. code-block bash    pip install python-utils    Or if pip is not available, easy_install should work as well.. code-block bash    easy_install python-utils    Or download the latest release from Pypi (httpspypi.python.orgpypipython-utils) or Github.Note that the releases on Pypi are signed with my GPG key (httpspgp.mit.edupkslookupopvindexsearch0xE81444E9CE1F695D) and can be checked using GPG.. code-block bash     gpg --verify python-utils-version.tar.gz.asc python-utils-version.tar.gzQuickstart------------------------------------------------------------------------------This module makes it easy to execute common tasks in Python scripts such asconverting text to numbers and making sure a string is in unicode or bytesformat.Examples------------------------------------------------------------------------------Automatically converting a generator to a list, dict or other collectionsusing a decorator.. code-block pycon     decorators.listify()    ... def generate_list()    ...     yield 1    ...     yield 2    ...     yield 3    ...     generate_list()    [1, 2, 3]     listify(collectiondict)    ... def dict_generator()    ...     yield a, 1    ...     yield b, 2     dict_generator()    {a 1, b 2}Retrying until timeoutTo easily retry a block of code with a configurable timeout, you can use thetime.timeout_generator.. code-block pycon     for i in time.timeout_generator(10)    ...     try    ...          Run your code here    ...     except Exception as e    ...          Handle the exceptionFormatting of timestamps, dates and timesEasy formatting of timestamps and calculating the time since.. code-block pycon     time.format_time(1)    00001     time.format_time(1.234)    00001     time.format_time(1)    00001     time.format_time(datetime.datetime(2000, 1, 2, 3, 4, 5, 6))    2000-01-02 030405     time.format_time(datetime.date(2000, 1, 2))    2000-01-02     time.format_time(datetime.timedelta(seconds3661))    10101     time.format_time(None)    ------     formatters.timesince(now)    just now     formatters.timesince(now - datetime.timedelta(seconds1))    1 second ago     formatters.timesince(now - datetime.timedelta(seconds2))    2 seconds ago     formatters.timesince(now - datetime.timedelta(seconds60))    1 minute agoConverting your test from camel-case to underscores.. code-block pycon     camel_to_underscore(SpamEggsAndBacon)    spam_eggs_and_baconAttribute setting decorator. Very useful for the Django adminA convenient decorator to set function attributes using a decorator.. code-block pycon    You can use     decorators.set_attributes(short_descriptionName)    ... def upper_case_name(self, obj)    ...     return (s s  (obj.first_name, obj.last_name)).upper()    Instead of     def upper_case_name(obj)    ...     return (s s  (obj.first_name, obj.last_name)).upper()     upper_case_name.short_description  NameThis can be very useful for the Django admin as it allows you to have allmetadata in one place.Scaling numbers between ranges.. code-block pycon     converters.remap(500, old_min0, old_max1000, new_min0, new_max100)    50     Or with decimals     remap(decimal.Decimal(250.0), 0.0, 1000.0, 0.0, 100.0)    Decimal(25.0)Get the screenwindowterminal size in characters.. code-block pycon     terminal.get_terminal_size()    (80, 24)That method supports IPython and Jupyter as well as regular shells, usingblessings and other modules depending on what is available.Extracting numbers from nearly every string.. code-block pycon     converters.to_int(spam15eggs)    15     converters.to_int(spam)    0     number  converters.to_int(spam, default1)    1Doing a global import of all the modules in a package programmaticallyTo do a global import programmatically you can use the import_globalfunction. This effectively emulates a from ... import .. code-block python    from python_utils.import_ import import_global     The following is  the equivalent of from some_module import     import_global(some_module)Automatically named logger for classesOr add a correclty named logger to your classes which can be easily accessed.. code-block python    class MyClass(Logged)        def __init__(self)            Logged.__init__(self)    my_class  MyClass()     Accessing the logging method    my_class.error(error)     With formatting    my_class.error(The logger supports (formatting)s,                   formattingnamed parameters)     Or to access the actual log function (overwriting the log formatting can     be done n the log method)    import logging    my_class.log(logging.ERROR, log)Alternatively loguru is also supported. It is largely a drop-in replacement for the logging module which is a bit more convenient to configureFirst install the extra loguru package.. code-block bash    pip install python-utils[loguru].. code-block python    class MyClass(Logurud)        ...Now you can use the Logurud class to make functions such as self.info()available. The benefit of this approach is that you can add extra context oroptions to you specific loguru instance (i.e. self.logger)Convenient type aliases and some commonly used types.. code-block python     For type hinting scopes such as localsglobalsvars    Scope  Dict[str, Any]    OptionalScope  O[Scope]     Note that Number is only useful for extra clarity since float     will work for both int and float in practice.    Number  U[int, float]    DecimalNumber  U[Number, decimal.Decimal]     To accept an exception or list of exceptions    ExceptionType  Type[Exception]    ExceptionsType  U[Tuple[ExceptionType, ...], ExceptionType]     Matching stringbytes types    StringTypes  U[str, bytes]</longdescription>
</pkgmetadata>