<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-appconf.. image httpcodecov.iogithubdjango-compressordjango-appconfcoverage.svgbranchdevelop    alt Code Coverage    target httpcodecov.iogithubdjango-compressordjango-appconfbranchdevelop.. image httpssecure.travis-ci.orgdjango-compressordjango-appconf.svgbranchdevelop    alt Build Status    target httptravis-ci.orgdjango-compressordjango-appconfA helper class for handling configuration defaults of packaged Djangoapps gracefully... note    This app precedes Djangos own AppConfig_ classes that act as    objects [to] store metadata for an application inside Djangos    app loading mechanism. In other words, they solve a related but    different use case than django-appconf and cant easily be used    as a replacement. The similarity in name is purely coincidental... _AppConfig httpsdocs.djangoproject.comenstablerefapplicationsdjango.apps.AppConfigOverview--------Say you have an app called myapp with a few defaults, which you wantto refer to in the apps code without repeating yourself all the time.appconf provides a simple class to implement those defaults. Simply addsomething like the following code somewhere in your app files.. code-block python    from appconf import AppConf    class MyAppConf(AppConf)        SETTING_1  one        SETTING_2  (            two,        ).. note    AppConf classes depend on being imported during startup of the Django    process. Even though there are multiple modules loaded automatically,    only the models modules (usually the models.py file of your    app) are guaranteed to be loaded at startup. Therefore its recommended    to put your AppConf subclass(es) there, too.The settings are initialized with the capitalized app label of where thesetting is located at. E.g. if your models.py with the AppConf classis in the myapp package, the prefix of the settings will be MYAPP.You can override the default prefix by specifying a prefix attribute ofan inner Meta class.. code-block python    from appconf import AppConf    class AcmeAppConf(AppConf)        SETTING_1  one        SETTING_2  (            two,        )        class Meta            prefix  acmeThe MyAppConf class will automatically look at Djangos global settingsto determine if youve overridden it. For example, adding this to your sitessettings.py would override SETTING_1 of the above MyAppConf.. code-block python    ACME_SETTING_1  uno    Since django-appconf completes Djangos global settings with its default values (like one above), the standard python manage.py diffsettings will show these defaults automatically.In case you want to use a different settings object instead of the defaultdjango.conf.settings, set the holder attribute of the innerMeta class to a dotted import path.. code-block python    from appconf import AppConf    class MyAppConf(AppConf)        SETTING_1  one        SETTING_2  (            two,        )        class Meta            prefix  acme            holder  acme.conf.settingsIf you ship an AppConf class with your reusable Django app, itsrecommended to put it in a conf.py file of your app package andimport django.conf.settings in it, too.. code-block python    from django.conf import settings    from appconf import AppConf    class MyAppConf(AppConf)        SETTING_1  one        SETTING_2  (            two,        )In the other files of your app you can easily make sure the settingsare correctly loaded if you import Djangos settings object from thatmodule, e.g. in your apps views.py.. code-block python    from django.http import HttpResponse    from myapp.conf import settings    def index(request)        text  Setting 1 is s  settings.MYAPP_SETTING_1        return HttpResponse(text)</longdescription>
</pkgmetadata>