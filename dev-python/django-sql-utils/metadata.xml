<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpstravis-ci.orgmartsbergerdjango-sql-utils.svgbranchmaster    target httpstravis-ci.orgmartsbergerdjango-sql-utilsDjango SQL UtilsThis package provides utilities for working with Django querysets so thatyou can generate the SQL that you want, with an API you enjoy.Subquery Aggregates-------------------The Count aggregation in Django    Parent.objects.annotate(child_countCount(child))generates SQL like the following    SELECT parent., Count(child.id) as child_count    FROM parent    JOIN child on child.parent_id  parent.id    GROUP BY parent.idIn many cases, this is not as performant as doing the count in a SUBQUERYinstead of with a JOIN    SELECT parent.,           (SELECT Count(id)            FROM child            WHERE parent_id  parent.id) as child_count    FROM parentDjango allows us to generate this SQL using The Subquery and OuterRef classes    subquery  Subquery(Child.objects.filter(parent_idOuterRef(id)).order_by()                        .values(parent).annotate(countCount(pk))                        .values(count), output_fieldIntegerField())    Parent.objects.annotate(child_countCoalesce(subquery, 0))Holy cow Its not trivial to figure what everything is doing in the abovecode and its not particularly good for maintenance. SubqueryAggregates allowyou to forget all that complexity and generate the subquery count like this    Parent.objects.annotate(child_countSubqueryCount(child))Phew Much easier to read and understand. Its the same API as the original Countjust specifying the Subquery version.Easier API for Exists---------------------If you have a ParentChild relationship (Child has a ForeignKey to Parent), you can annotate a querysetof Parent objects with a boolean indicating whether or not the parent has children    from django.db.models import Exists    parents  Parent.objects.annotate(        has_childrenExists(Child.objects.filter(parentOuterRef(pk))    )Thats a bit more boilerplate than should be necessary, so we provide a simpler API for Exists    from sql_util.utils import Exists    parents  Parent.objects.annotate(        has_childrenExists(child)    )The child queryset can be filtered with the keyword argument filter. E.g.,    parents  Parent.objects.annotate(        has_child_named_John  Exists(child, filterQ(nameJohn))    )The sql_util version of Exists can also take a queryset as the first parameter and behave just likethe Django Exists class, so you are able to use it everywhere without worrying about name confusion.Installation and Usage----------------------Install from PyPI    pip install django-sql-utilsThen you can    from sql_util.utils import SubqueryCountAnd use that as shown above.In addition to SubqueryCount, this package provides SubqueryMin SubqueryMax SubquerySum SubqueryAvgIf you want to use other aggregates, you can use thegeneric SubqueryAggregate class. For example, if you want to use Postgres ArrayAggto get an array of Child.name for each Parent    from django.contrib.postgres.aggregates import ArrayAgg    aggregate  SubqueryAggregate(child__name, aggregateArrayAgg)    Parent.objects.annotate(child_namesaggregate)Or subclass SubqueryAggregate    from django.contrib.postgres.aggregates import ArrayAgg    class SubqueryArrayAgg(SubqueryAggregate)        aggregate  ArrayAgg        unordered  True    Parent.objects.annotate(avg_child_ageSubqueryArrayAgg(child__age))</longdescription>
</pkgmetadata>