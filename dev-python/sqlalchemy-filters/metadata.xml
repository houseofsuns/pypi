<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>SQLAlchemy filters.. pull-quote    Filter, sort and paginate SQLAlchemy query objects. Ideal for    exposing these actions over a REST API... image httpsimg.shields.iopypivsqlalchemy-filters.svg    target httpspypi.orgprojectsqlalchemy-filters.. image httpsimg.shields.iopypipyversionssqlalchemy-filters.svg    target httpspypi.orgprojectsqlalchemy-filters.. image httpsimg.shields.iopypiformatsqlalchemy-filters.svg    target httpspypi.orgprojectsqlalchemy-filters.. image httpstravis-ci.orgjuliotrigosqlalchemy-filters.svgbranchmaster    target httpstravis-ci.orgjuliotrigosqlalchemy-filtersFiltering---------Assuming that we have a SQLAlchemy_ query object.. code-block python    from sqlalchemy import Column, Integer, String    from sqlalchemy.ext.declarative import declarative_base    class Base(object)        id  Column(Integer, primary_keyTrue)        name  Column(String(50), nullableFalse)        count  Column(Integer, nullableTrue)        hybrid_property        def count_square(self)            return self.count  self.count        hybrid_method        def three_times_count(self)            return self.count  3    Base  declarative_base(clsBase)    class Foo(Base)        __tablename__  foo     ...    query  session.query(Foo)Then we can apply filters to that query object (multiple times).. code-block python    from sqlalchemy_filters import apply_filters     query should be a SQLAlchemy query object    filter_spec  [{field name, op , value name_1}]    filtered_query  apply_filters(query, filter_spec)    more_filters  [{field foo_id, op is_not_null}]    filtered_query  apply_filters(filtered_query, more_filters)    result  filtered_query.all()It is also possible to filter queries that contain multiple models,including joins.. code-block python    class Bar(Base)        __tablename__  bar        foo_id  Column(Integer, ForeignKey(foo.id)).. code-block python    query  session.query(Foo).join(Bar)    filter_spec  [        {model Foo, field name, op , value name_1},        {model Bar, field count, op , value 5},    ]    filtered_query  apply_filters(query, filter_spec)    result  filtered_query.all()apply_filters will attempt to automatically join models to queryif theyre not already present and a model-specific filter is supplied.For example, the value of filtered_query in the following two codeblocks is identical.. code-block python    query  session.query(Foo).join(Bar)   join pre-applied to query    filter_spec  [        {model Foo, field name, op , value name_1},        {model Bar, field count, op , value 5},    ]    filtered_query  apply_filters(query, filter_spec).. code-block python    query  session.query(Foo)   join to Bar will be automatically applied    filter_spec  [        {field name, op , value name_1},        {model Bar, field count, op , value 5},    ]    filtered_query  apply_filters(query, filter_spec)The automatic join is only possible if SQLAlchemy_ can implictlydetermine the condition for the join, for example because of a foreignkey relationship.Automatic joins allow flexibility for clients to filter and sort by relatedobjects without specifying all possible joins on the server beforehand. Featurecan be explicitly disabled by passing do_auto_joinFalse argument to theapply_filters call.Note that first filter of the second block does not specify a model.It is implictly applied to the Foo model because that is the onlymodel in the original query passed to apply_filters.It is also possible to apply filters to queries defined by fields, functions orselect_from clause.. code-block python    query_alt_1  session.query(Foo.id, Foo.name)    query_alt_2  session.query(func.count(Foo.id))    query_alt_3  session.query().select_from(Foo).add_column(Foo.id)Hybrid attributesYou can filter by a hybrid attribute_ a hybrid property_ or a hybrid method_... code-block python    query  session.query(Foo)    filter_spec  [{field count_square, op , value 25}]    filter_spec  [{field three_times_count, op , value 15}]    filtered_query  apply_filters(query, filter_spec)    result  filtered_query.all()Restricted Loads----------------You can restrict the fields that SQLAlchemy_ loads from the database byusing the apply_loads function.. code-block python    query  session.query(Foo, Bar).join(Bar)    load_spec  [        {model Foo, fields [name]},        {model Bar, fields [count]}    ]    query  apply_loads(query, load_spec)   will load only Foo.name and Bar.countThe effect of the apply_loads function is to _defer_ the loadof any other fields to whenif theyre accessed, rather than loadingthem when the query is executed. It only applies to fields that would beloaded during normal query execution.Effect on joined queriesThe default SQLAlchemy_ join is lazy, meaning that columns from thejoined table are loaded only when required. Therefore apply_loadshas limited effect in the following scenario.. code-block python    query  session.query(Foo).join(Bar)    load_spec  [        {model Foo, fields [name]}        {model Bar, fields [count]}   ignored    ]    query  apply_loads(query, load_spec)   will load only Foo.nameapply_loads cannot be applied to columns that are loaded asjoined eager loads httpdocs.sqlalchemy.orgenlatestormloading_relationships.htmljoined-eager-loading_.This is because a joined eager load does not add the joined model to theoriginal query, as explainedhere httpdocs.sqlalchemy.orgenlatestormloading_relationships.htmlthe-zen-of-joined-eager-loading_The following would not prevent all columns from Bar being eagerlyloaded.. code-block python    query  session.query(Foo).options(joinedload(Foo.bar))    load_spec  [        {model Foo, fields [name]}        {model Bar, fields [count]}    ]    query  apply_loads(query, load_spec).. sidebar Automatic Join    In fact, what happens here is that Bar is automatically joined    to query, because it is determined that Bar is not part of    the original query. The load_spec therefore has no effect    because the automatic join results in lazy evaluation.If you wish to perform a joined load with restricted columns, you mustspecify the columns as part of the joined load, rather than withapply_loads.. code-block python    query  session.query(Foo).options(joinedload(Bar).load_only(count))    load_spec  [        {model Foo, fields [name]}    ]    query  apply_loads(query. load_spec)   will load ony Foo.name and Bar.countSort----.. code-block python    from sqlalchemy_filters import apply_sort     query should be a SQLAlchemy query object    sort_spec  [        {model Foo, field name, direction asc},        {model Bar, field id, direction desc},    ]    sorted_query  apply_sort(query, sort_spec)    result  sorted_query.all()apply_sort will attempt to automatically join models to query iftheyre not already present and a model-specific sort is supplied.The behaviour is the same as in apply_filters.This allows flexibility for clients to sort by fields on related objectswithout specifying all possible joins on the server beforehand.Hybrid attributesYou can sort by a hybrid attribute_ a hybrid property_ or a hybrid method_.Pagination----------.. code-block python    from sqlalchemy_filters import apply_pagination     query should be a SQLAlchemy query object    query, pagination  apply_pagination(query, page_number1, page_size10)    page_size, page_number, num_pages, total_results  pagination    assert 10  len(query)    assert 10  page_size  pagination.page_size    assert 1  page_number  pagination.page_number    assert 3  num_pages  pagination.num_pages    assert 22  total_results  pagination.total_resultsFilters format--------------Filters must be provided in a list and will be applied sequentially.Each filter will be a dictionary element in that list, using thefollowing format.. code-block python    filter_spec  [        {model model_name, field field_name, op , value field_value},        {model model_name, field field_2_name, op , value field_2_value},         ...    ]The model key is optional if the original query being filtered onlyapplies to one model.If there is only one filter, the containing list may be omitted.. code-block python    filter_spec  {field field_name, op , value field_value}Where field is the name of the field that will be filtered using theoperator provided in op (optional, defaults to ) and theprovided value (optional, depending on the operator).This is the list of operators that can be used- is_null- is_not_null- , eq- , ne- , gt- , lt- , ge- , le- like- ilike- not_ilike- in- not_in- any- not_anyany  not_anyPostgreSQL specific operators allow to filter queries on columns of type ARRAY.Use any to filter if a value is present in an array and not_any if its not.Boolean Functionsand, or, and not functions can be used and nested within thefilter specification.. code-block python    filter_spec  [        {            or [                {                    and [                        {field field_name, op , value field_value},                        {field field_2_name, op , value field_2_value},                    ]                },                {                    not [                        {field field_3_name, op , value field_3_value}                    ]                },            ],        }    ]Note or and and must reference a list of at least one element.not must reference a list of exactly one element.Sort format-----------Sort elements must be provided as dictionaries in a list and will beapplied sequentially.. code-block python    sort_spec  [        {model Foo, field name, direction asc},        {model Bar, field id, direction desc},         ...    ]Where field is the name of the field that will be sorted using theprovided direction.The model key is optional if the original query being sorted onlyapplies to one model.nullsfirst  nullslast.. code-block python    sort_spec  [        {model Baz, field count, direction asc, nullsfirst True},        {model Qux, field city, direction desc, nullslast True},         ...    ]nullsfirst is an optional attribute that will place NULL values firstif set to True, according to the SQLAlchemy documentation httpsdocs.sqlalchemy.orgenlatestcoresqlelement.htmlsqlalchemy.sql.expression.nullsfirst__.nullslast is an optional attribute that will place NULL values lastif set to True, according to the SQLAlchemy documentation httpsdocs.sqlalchemy.orgenlatestcoresqlelement.htmlsqlalchemy.sql.expression.nullslast__.If none of them are provided, then NULL values will be sorted accordingto the RDBMS being used. SQL defines that NULL values should be placedtogether when sorting, but it does not specify whether they should be placedfirst or last.Even though both nullsfirst and nullslast are part of SQLAlchemy_,they will raise an unexpected exception if the RDBMS that is being used doesnot support them.At the moment they aresupported by PostgreSQL httpswww.postgresql.orgdocscurrentqueries-order.html_,but they are not supported by SQLite and MySQL.Running tests-------------The default configuration uses SQLite, MySQL (if the driver isinstalled, which is the case when tox is used) and PostgreSQL(if the driver is installed, which is the case when tox is used) torun the tests, with the following URIs.. code-block shell    sqlite+pysqlitetest_sqlalchemy_filters.db    mysql+mysqlconnectorrootlocalhost3306test_sqlalchemy_filters    postgresql+psycopg2postgreslocalhost5432test_sqlalchemy_filtersclient_encodingutf8A test database will be created, used during the tests and destroyedafterwards for each RDBMS configured.There are Makefile targets to run docker containers locally for bothMySQL and PostgreSQL, using the default ports and configuration.. code-block shell     make mysql-container     make postgres-containerTo run the tests locally.. code-block shell      Createactivate a virtual environment     pip install tox     toxThere are some other Makefile targets that can be used to run the testsThere are other Makefile targets to run the tests, but extradependencies will have to be installed.. code-block shell     pip install -U --editable .[dev,mysql,postgresql]      using default settings     make test     make coverage      overriding DB parameters     ARGS--mysql-test-db-uri mysql+mysqlconnectorroot192.168.99.1003340test_sqlalchemy_filters make test     ARGS--sqlite-test-db-uri sqlite+pysqlitetest_sqlalchemy_filters.db make test     ARGS--mysql-test-db-uri mysql+mysqlconnectorroot192.168.99.1003340test_sqlalchemy_filters make coverage     ARGS--sqlite-test-db-uri sqlite+pysqlitetest_sqlalchemy_filters.db make coverageDatabase management systems---------------------------The following RDBMS are supported (tested)- SQLite- MySQL- PostgreSQLPython 2--------There is no active support for Python 2. However, it is compatible as ofFebruary 2019, if you install funcsigs, included in the python2extra requirements.SQLAlchemy support------------------The following SQLAlchemy_ versions are supported 1.0, 1.1,1.2, 1.3.Changelog---------Consult the CHANGELOG httpsgithub.comjuliotrigosqlalchemy-filtersblobmasterCHANGELOG.rst_document for fixes and enhancements of each version.License-------Apache 2.0. See LICENSE httpsgithub.comjuliotrigosqlalchemy-filtersblobmasterLICENSE_for details... _SQLAlchemy httpswww.sqlalchemy.org.. _hybrid attribute httpsdocs.sqlalchemy.orgen13ormextensionshybrid.html.. _hybrid property httpsdocs.sqlalchemy.orgen13ormextensionshybrid.htmlsqlalchemy.ext.hybrid.hybrid_property.. _hybrid method httpsdocs.sqlalchemy.orgen13ormextensionshybrid.htmlsqlalchemy.ext.hybrid.hybrid_method</longdescription>
</pkgmetadata>