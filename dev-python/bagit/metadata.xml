<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>bagit-pythonBuild Status Coverage Statusbagit is a Python library and command line utility for working withBagIt httppurl.orgnetbagit__ style packages.Installation------------bagit.py is a single-file python module that you can drop into yourproject as needed or you can install globally with    pip install bagitPython v2.7+ is required.Command Line Usage------------------When you install bagit you should get a command-line program calledbagit.py which you can use to turn an existing directory into a bag    bagit.py --contact-name John Kunze directorytobagFinding Bagit on your systemThe bagit.py program should be available in your normal command-linewindow (Terminal on OS X, Command Prompt or Powershell on Windows,etc.). If you are unsure where it was installed you can also requestthat Python search for bagit as a Python module simply replacebagit.py with python -m bagit    python -m bagit --helpOn some systems Python may have been installed as python3, py,etc.  simply use the same name you use to start an interactive Pythonshell    py -m bagit --help    python3 -m bagit --helpConfiguring BagItYou can pass in keyvalue metadata for the bag using options like--contact-name above, which get persisted to the bag-info.txt. For acomplete list of bag-info.txt properties you can use as commmand linearguments see --help.Since calculating checksums can take a while when creating a bag, youmay want to calculate them in parallel if you are on a multicoremachine. You can do that with the --processes option    bagit.py --processes 4 directorytobagTo specify which checksum algorithm(s) to use when generating themanifest, use the --md5, --sha1, --sha256 andor --sha512 flags (MD5 isgenerated by default).    bagit.py --sha1 pathtobag    bagit.py --sha256 pathtobag    bagit.py --sha512 pathtobagIf you would like to validate a bag you can use the --validate flag.    bagit.py --validate pathtobagIf you would like to take a quick look at the bag to see if it seemsvalid by just examining the structure of the bag, and comparing itspayload-oxum (byte count and number of files) then use the --fastflag.    bagit.py --validate --fast pathtobagAnd finally, if youd like to parallelize validation to take advantageof multiple CPUs you can    bagit.py --validate --processes 4 pathtobagUsing BagIt in your programs----------------------------You can also use BagIt programatically in your own Python programs byimporting the bagit module.CreateTo create a bag you would do this.. code python    bag  bagit.make_bag(mydir, {Contact-Name John Kunze})make_bag returns a Bag instance. If you have a bag already on diskand would like to create a Bag instance for it, simply call theconstructor directly.. code python    bag  bagit.Bag(pathtobag)Update Bag MetadataYou can change the metadata persisted to the bag-info.txt by using theinfo property on a Bag... code python     load the bag    bag  bagit.Bag(pathtobag)     update bag info metadata    bag.info[Internal-Sender-Description]  Updated on 2014-06-28.    bag.info[Authors]  [John Kunze, Andy Boyko]    bag.save()Update Bag ManifestsBy default save will not update manifests. This guards against asituation where a call to save to persist bag metadata accidentallyregenerates manifests for an invalid bag. If you have modified thepayload of a bag by adding, modifying or deleting files in the datadirectory, and wish to regenerate the manifests set the manifestsparameter to True when calling save... code python    import shutil, os     add a file    shutil.copyfile(newfile, pathtobagdatanewfile)     remove a file    os.remove(pathtobagdatafile)     persist changes    bag.save(manifestsTrue)The save method takes an optional processes parameter which willdetermine how many processes are used to regenerate the checksums. Thiscan be handy on multicore machines.ValidationIf you would like to see if a bag is valid, use its is_valid method.. code python    bag  bagit.Bag(pathtobag)    if bag.is_valid()        print(yay ))    else        print(boo ()If youd like to get a detailed list of validation errors, execute thevalidate method and catch the BagValidationError exception. Ifthe bags manifest was invalid (and it wasnt caught by the payloadoxum) the exceptions details property will contain a list ofManifestError s that you can introspect on. Each ManifestError,will be of type ChecksumMismatch, FileMissing,UnexpectedFile.So for example if you want to print out checksums that failed tovalidate you can do this.. code python    bag  bagit.Bag(pathtobag)    try      bag.validate()    except bagit.BagValidationError as e        for d in e.details            if isinstance(d, bagit.ChecksumMismatch)                print(expected s to have s checksum of s but found s                       (d.path, d.algorithm, d.expected, d.found))To iterate through a bags manifest and retrieve checksums for thepayload files use the bags entries dictionary.. code python    bag  bagit.Bag(pathtobag)    for path, fixity in bag.entries.items()      print(paths md5s  (path, fixity[md5]))Contributing to bagit-python development----------------------------------------     git clone gitgithub.comLibraryOfCongressbagit-python.git     cd bagit-python     MAKE CHANGES     python test.pyRunning the testsYou can quickly run the tests by having setuptools install dependencies    python setup.py testOnce your code is working, you can useTox httpstox.readthedocs.io__ to run the tests with everysupported version of Python which you have installed on the localsystem    toxIf you have Docker installed, you can run the tests under Linux inside acontainer     docker build -t bagitlatest .  docker run -it bagitlatestBenchmarks----------If youd like to see how increasing parallelization of bag creation onyour system effects the time to create a bag try using the includedbench utility     .bench.pyLicense-------cc0Note By contributing to this project, you agree to license your workunder the same terms as those that govern this projects distribution... Build Status image httpstravis-ci.orgLibraryOfCongressbagit-python.svgbranchmaster   target httptravis-ci.orgLibraryOfCongressbagit-python.. Coverage Status image httpscoveralls.ioreposgithubLibraryOfCongressbagit-pythonbadge.svgbranchmaster   target httpscoveralls.iogithubLibraryOfCongressbagit-pythonbranchmaster.. cc0 image httpi.creativecommons.orgpzero1.088x31.png   target httpcreativecommons.orgpublicdomainzero1.0</longdescription>
</pkgmetadata>