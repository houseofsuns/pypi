<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpsgithub.comPhynixyamlloaderactionsworkflowsci.ymlbadge.svg    target httpsgithub.comPhynixyamlloaderactions.. image httpsimg.shields.iopypipyversionsyamlloader.svg    target httpspypi.orgprojectyamlloader.. image httpsbadge.fury.iopyyamlloader.svg    target httpsbadge.fury.iopyyamlloader.. image httpscoveralls.ioreposgithubPhynixyamlloaderbadge.svg    target httpscoveralls.iogithubPhynixyamlloaderyamlloaderThis module provides loaders and dumpers for PyYAML. Currently, an OrderedDict loaderdumper isimplemented, allowing to keep items orderwhen loading resp. dumping a file fromto an OrderedDict (Python 3.7+ Also regular dicts are supported and are the default items to be loaded to. As of Python 3.7 preservation of insertion order is a language feature of regular dicts.)This project was originally mirrored fromyamlordereddict httpsgithub.comfmenabepython-yamlordereddictloader_Many thanks to the original author Franois MnabThe library contains several improvements including automated testing andthe much faster C-versions of the LoadersDumpers.API Documentation httpsyamlloader.readthedocs.io_Install-------There is a pip and a conda version available.. code-block bash     pip install yamlloaderor.. code-block bash     conda install yamlloader -c conda-forgeBut does [your special case here] also work--------------------------------------------Tests are run continuously using randomly generated yaml files.Also, there are no fails to be expected.Still, if you are concerned that your special case may breaks in the future, pleaseadd your own tests as test_ext_anyname.py under tests or let us know about your needs.This guarantees that no code will be added that breaks your case.C vs non-C version------------------A significant speedup can be reached by replacing the Loader and Dumper classes by CLoaderand CDumper. The package hereby relies on the implementations from PyYAML. If they have notbeen compiled, yamlloader automatically falls back to the non-C versions.Therefore using the C-version is safe if it is not available, the pure Python version isautomatically used.Usage examplesLoader usage------------.. code-block python    import yaml    import yamlloader    with open(myfile.yml) as yaml_file        data  yaml.load(yaml_file,                         Loaderyamlloader.ordereddict.CLoader)                          CLoader is faster than LoaderNote For using the safe loader (which takes standard YAML tags and doesnot construct arbitrary Python objects), replace yamlloader.ordereddict.CLoader byyamlloader.ordereddict.CSafeLoader.Dumper usage------------.. code-block python    import yaml    import yamlloader    from collections import OrderedDict    data  OrderedDict([(key1, val1),                        (key2, OrderedDict([(key21, val21),                                              (key22, val22)]))])    with open(myfile.yaml, w) as yaml_file        yaml.dump(data, yaml_file,                  Dumperyamlloader.ordereddict.CDumper)Note For using the safe dumper (which produce standard YAML tags and doesnot represent arbitrary Python objects), replace yamlloader.ordereddict.CDumper byyamlloader.ordereddict.CSafeDumper.FAQC version not working---------------------------If the C version is not working (it falls back by default to a non-C version),check if yaml.cyaml exists. If not, the cyaml module was not compiled during the installation ofyaml (pyyaml). Make sure that cython is installed (pip install Cython) and the yaml.h file isthere (apt libyaml-dev).</longdescription>
</pkgmetadata>