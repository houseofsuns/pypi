<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>p aligncenter   img srcimageslogo.png width40 alignmiddlep Typed Argument Parser (Tap)[[PyPI - Python Version](httpsimg.shields.iopypipyversionstyped-argument-parser)](httpsbadge.fury.iopytyped-argument-parser)[[PyPI version](httpsbadge.fury.iopytyped-argument-parser.svg)](httpsbadge.fury.iopytyped-argument-parser)[[Downloads](httpspepy.techbadgetyped-argument-parser)](httpspepy.techprojecttyped-argument-parser)[[Build Status](httpsgithub.comswansonk14typed-argument-parserworkflowstestsbadge.svg)](httpsgithub.comswansonk14typed-argument-parser)[[codecov](httpscodecov.ioghswansonk14typed-argument-parserbranchmaingraphbadge.svg)](httpscodecov.ioghswansonk14typed-argument-parser)[[license](httpsimg.shields.iogithublicenseswansonk14typed-argument-parser.svg)](httpsgithub.comswansonk14typed-argument-parserblobmainLICENSE.txt)Tap is a typed modernization of Pythons [argparse](httpsdocs.python.org3libraryargparse.html) library.Tap provides the following benefits- Static type checking- Code completion- Source code navigation (e.g. go to definition and go to implementation)[Tap](httpsgithub.comswansonk14typed-argument-parserrawmainimagestap.png)See [this poster](httpsdocs.google.compresentationd1AirN6gpiq4P1L8K003EsXmobVxP3A4AVEIR2KOEQN7Yedituspsharing), which we presented at [PyCon 2020](httpsus.pycon.org2020), for a presentation of some of the relevant concepts we used to guide the development of Tap. As of version 1.8.0, Tap includes tapify, which runs functions or initializes classes with arguments parsed from the command line. We show an example below.python square.pyfrom tap import tapifydef square(num float) - float    return num  2if __name__  __main__    print(fThe square of your number is {tapify(square)}.)Running python square.py --num 2 will print The square of your number is 4.0.. Please see [tapify](tapify) for more details. InstallationTap requires Python 3.7+To install Tap from PyPI run pip install typed-argument-parserTo install Tap from source, run the following commandsgit clone httpsgithub.comswansonk14typed-argument-parser.gitcd typed-argument-parserpip install -e . Table of Contents [Installation](installation) [Table of Contents](table-of-contents) [Tap is Python-native](tap-is-python-native) [Tap features](tap-features)  + [Arguments](arguments)  + [Tap help](tap-help)  + [Configuring arguments](configuring-arguments)    - [Adding special argument behavior](adding-special-argument-behavior)    - [Adding subparsers](adding-subparsers)  + [Types](types)  + [Argument processing](argument-processing)  + [Processing known args](processing-known-args)  + [Subclassing](subclassing)  + [Printing](printing)  + [Reproducibility](reproducibility)  + [Saving and loading arguments](saving-and-loading-arguments)  + [Loading from configuration files](loading-from-configuration-files) [tapify](tapify)  + [Examples](examples)    - [Function](function)    - [Class](class)    - [Dataclass](dataclass)  + [tapify help](tapify-help)  + [Command line vs explicit arguments](command-line-vs-explicit-arguments)  + [Known args](known-args) Tap is Python-nativeTo see this, lets look at an examplepythonmain.pyfrom tap import Tapclass SimpleArgumentParser(Tap)    name str   Your name    language str  Python   Programming language    package str  Tap   Package name    stars int   Number of stars    max_stars int  5   Maximum starsargs  SimpleArgumentParser().parse_args()print(fMy name is {args.name} and I give the {args.language} package       f{args.package} {args.stars}{args.max_stars} stars)You use Tap the same way you use standard argparse. python main.py --name Jesse --stars 5My name is Jesse and I give the Python package Tap 55 starsThe equivalent argparse code ispythonmain.pyfrom argparse import ArgumentParserparser  ArgumentParser()parser.add_argument(--name, typestr, requiredTrue,                    helpYour name)parser.add_argument(--language, typestr, defaultPython,                    helpProgramming language)parser.add_argument(--package, typestr, defaultTap,                    helpPackage name)parser.add_argument(--stars, typeint, requiredTrue,                    helpNumber of stars)parser.add_argument(--max_stars, typeint, default5,                    helpMaximum stars)args  parser.parse_args()print(fMy name is {args.name} and I give the {args.language} package       f{args.package} {args.stars}{args.max_stars} stars)The advantages of being Python-native include being able to- Overwrite convenient built-in methods (e.g. process_args ensures consistency among arguments)- Add custom methods- Inherit from your own template classes Tap featuresNow we are going to highlight some of our favorite features and give examples of how they work in practice. ArgumentsArguments are specified as class variables defined in a subclass of Tap. Variables defined as name type are required arguments while variables defined as name type  value are not required and default to the provided value.pythonclass MyTap(Tap)    required_arg str    default_arg str  default value Tap helpSingle line andor multiline comments which appear after the argument are automatically parsed into the help string provided when running python main.py -h. The type and default values of arguments are also provided in the help string.pythonmain.pyfrom tap import Tapclass MyTap(Tap)    x float   What am I    pi float  3.14   Im pi    Pi is my favorite numberargs  MyTap().parse_args()Running python main.py -h results in the following python main.py -husage demo.py --x X [--pi PI] [-h]optional arguments  --x X       (float, required) What am I  --pi PI     (float, default3.14) Im pi Pi is my favorite number.  -h, --help  show this help message and exit Configuring argumentsTo specify behavior beyond what can be specified using arguments as class variables, override the configure method.configure provides access to advanced argument parsing features such as add_argument and add_subparser.Since Tap is a wrapper around argparse, Tap provides all of the same functionality.We detail these two functions below. Adding special argument behaviorIn the configure method, call self.add_argument just as you would use argparses add_argument. For example,pythonfrom tap import Tapclass MyTap(Tap)    positional_argument str    list_of_three_things List[str]    argument_with_really_long_name int    def configure(self)        self.add_argument(positional_argument)        self.add_argument(--list_of_three_things, nargs3)        self.add_argument(-arg, --argument_with_really_long_name) Adding subparsersTo add a subparser, override the configure method and call self.add_subparser. Optionally, to specify keyword arguments (e.g., help) to the subparser collection, call self.add_subparsers. For example,pythonclass SubparserA(Tap)    bar int   bar helpclass SubparserB(Tap)    baz Literal[X, Y, Z]   baz helpclass Args(Tap)    foo bool  False   foo help    def configure(self)        self.add_subparsers(helpsub-command help)        self.add_subparser(a, SubparserA, helpa help)        self.add_subparser(b, SubparserB, helpb help) TypesTap automatically handles all the following typespythonstr, int, float, boolOptional, Optional[str], Optional[int], Optional[float], Optional[bool]List, List[str], List[int], List[float], List[bool]Set, Set[str], Set[int], Set[float], Set[bool]Tuple, Tuple[Type1, Type2, etc.], Tuple[Type, ...]  LiteralIf youre using Python 3.9+, then you can replace List with list, Set with set, and Tuple with tuple.Tap also supports Union, but this requires additional specification (see [Union](-union-) section below).Additionally, any type that can be instantiated with a string argument can be used. For example, inpythonfrom pathlib import Pathfrom tap import Tapclass Args(Tap)   path Pathargs  Args().parse_args()args.path is a Path instance containing the string passed in through the command line. str, int, and floatEach is automatically parsed to their respective types, just like argparse. boolIf an argument arg is specified as arg bool or arg bool  False, then adding the --arg flag to the command line will set arg to True. If arg is specified as arg bool  True, then adding --arg sets arg to False.Note that if the Tap instance is created with explicit_boolTrue, then booleans can be specified on the command line as --arg True or --arg False rather than --arg. Additionally, booleans can be specified by prefixes of True and False with any capitalization as well as 1 or 0 (e.g. for True, --arg tRu, --arg T, --arg 1 all suffice).  OptionalThese arguments are parsed in exactly the same way as str, int, float, and bool. Note bools can be specified using the same rules as above and that Optional is equivalent to Optional[str]. ListIf an argument arg is a List, simply specify the values separated by spaces just as you would with regular argparse. For example, --arg 1 2 3 parses to arg  [1, 2, 3]. SetIdentical to List but parsed into a set rather than a list. TupleTuples can be used to specify a fixed number of arguments with specified types using the syntax Tuple[Type1, Type2, etc.] (e.g. Tuple[str, int, bool, str]). Tuples with a variable number of arguments are specified by Tuple[Type, ...] (e.g. Tuple[int, ...]). Note Tuple defaults to Tuple[str, ...]. LiteralLiteral is analagous to argparses [choices](httpsdocs.python.org3libraryargparse.htmlchoices), which specifies the values that an argument can take. For example, if arg can only be one of H, 1, False, or 1.0078 then you would specify that arg Literal[H, 1, False, 1.0078]. For instance, --arg False assigns arg to False and --arg True throws error. The Literal type was introduced in Python 3.8 ([PEP 586](httpswww.python.orgdevpepspep-0586)) and can be imported with from typing_extensions import Literal. UnionUnion types must include the type keyword argument in add_argument in order to specify which type to use, as in the example below.pythondef to_number(string str) - Union[float, int]    return float(string) if . in string else int(string)class MyTap(Tap)    number Union[float, int]    def configure(self)        self.add_argument(--number, typeto_number)In Python 3.10+, Union[Type1, Type2, etc.] can be replaced with Type1  Type2  etc., but the type keyword argument must still be provided in add_argument. Complex TypesTap can also support more complex types than the ones specified above. If the desired type is constructed with a single string as input, then the type can be specified directly without additional modifications. For example,pythonclass Person    def __init__(self, name str) - None        self.name  nameclass Args(Tap)    person Personargs  Args().parse_args(--person Tapper.split())print(args.person.name)   TapperIf the desired type has a more complex constructor, then the type keyword argument must be provided in add_argument. For example,pythonclass AgedPerson    def __init__(self, name str, age int) - None        self.name  name        self.age  agedef to_aged_person(string str) - AgedPerson    name, age  string.split(,)    return AgedPerson(namename, ageint(age))class Args(Tap)    aged_person AgedPerson    def configure(self) - None        self.add_argument(--aged_person, typeto_aged_person)args  Args().parse_args(--aged_person Tapper,27.split())print(f{args.aged_person.name} is {args.aged_person.age})   Tapper is 27 Argument processingWith complex argument parsing, arguments often end up having interdependencies. This means that it may be necessary to disallow certain combinations of arguments or to modify some arguments based on other arguments.To handle such cases, simply override process_args and add the required logic. process_args is automatically called when parse_args is called.pythonclass MyTap(Tap)    package str    is_cool bool    stars int    def process_args(self)         Validate arguments        if self.is_cool and self.stars  4            raise ValueError(Cool packages cannot have fewer than 4 stars)         Modify arguments        if self.package  Tap            self.is_cool  True            self.stars  5 Processing known argsSimilar to argparses parse_known_args, Tap is capable of parsing only arguments that it is aware of without raising an error due to additional arguments. This can be done by calling parse_args with known_onlyTrue. The remaining un-parsed arguments are then available by accessing the extra_args field of the Tap object.pythonclass MyTap(Tap)    package strargs  MyTap().parse_args([--package, Tap, --other_arg, value], known_onlyTrue)print(args.extra_args)   [--other_arg, value] SubclassingIt is sometimes useful to define a template Tap and then subclass it for different use cases. Since Tap is a native Python class, inheritance is built-in, making it easy to customize from a template Tap.In the example below, StarsTap and AwardsTap inherit the arguments (package and is_cool) and the methods (process_args) from BaseTap.pythonclass BaseTap(Tap)    package str    is_cool bool    def process_args(self)        if self.package  Tap            self.is_cool  Trueclass StarsTap(BaseTap)    stars intclass AwardsTap(BaseTap)    awards List[str] PrintingTap uses Pythons [pretty printer](httpsdocs.python.org3librarypprint.html) to print out arguments in an easy-to-read format.pythonmain.pyfrom tap import Tapfrom typing import Listclass MyTap(Tap)    package str    is_cool bool  True    awards List[str]  [amazing, wow, incredible, awesome]args  MyTap().parse_args()print(args)Running python main.py --package Tap results in python main.py{awards [amazing, wow, incredible, awesome], is_cool True, package Tap} ReproducibilityTap makes reproducibility easy, especially when running code in a git repo. Reproducibility infoSpecifically, Tap has a method called get_reproducibility_info that returns a dictionary containing all the information necessary to replicate the settings under which the code was run. This dictionary includes- Python command    - The Python command that was used to run the program    - Ex. python main.py --package Tap- Time    - The time when the command was run    - Ex. Thu Aug 15 000913 2019- Git root    - The root of the git repo containing the code that was run    - Ex. Usersswansonk14typed-argument-parser- Git url    - The url to the git repo, specifically pointing to the current git hash (i.e. the hash of HEAD in the local repo)    - Ex. [httpsgithub.comswansonk14typed-argument-parsertree446cf046631d6bdf7cab6daec93bf7a02ac00998](httpsgithub.comswansonk14typed-argument-parsertree446cf046631d6bdf7cab6daec93bf7a02ac00998)- Uncommitted changes    - Whether there are any uncommitted changes in the git repo (i.e. whether the code is different from the code at the above git hash)    - Ex. True or False Saving and loading arguments SaveTap has a method called save which saves all arguments, along with the reproducibility info, to a JSON file.pythonmain.pyfrom tap import Tapclass MyTap(Tap)    package str    is_cool bool  True    stars int  5args  MyTap().parse_args()args.save(args.json)After running python main.py --package Tap, the file args.json will contain{    is_cool true,    package Tap,    reproducibility {        command_line python main.py --package Tap,        git_has_uncommitted_changes false,        git_root Usersswansonk14typed-argument-parser,        git_url httpsgithub.comswansonk14typed-argument-parsertree446cf046631d6bdf7cab6daec93bf7a02ac00998,        time Thu Aug 15 001831 2019    },    stars 5}Note More complex types will be encoded in JSON as a pickle string. Load exclamation warningbr Never call args.load(args.json) on untrusted files. Argument loading uses the pickle module to decode complex types automatically. Unpickling of untrusted data is a security risk and can lead to arbitrary code execution. See [the warning in the pickle docs](httpsdocs.python.org3librarypickle.html).br exclamation warningArguments can be loaded from a JSON file rather than parsed from the command line.pythonmain.pyfrom tap import Tapclass MyTap(Tap)    package str    is_cool bool  True    stars int  5args  MyTap()args.load(args.json)Note All required arguments (in this case package) must be present in the JSON file if not already set in the Tap object. Load from dictArguments can be loaded from a Python dictionary rather than parsed from the command line.pythonmain.pyfrom tap import Tapclass MyTap(Tap)    package str    is_cool bool  True    stars int  5args  MyTap()args.from_dict({    package Tap,    stars 20})Note As with load, all required arguments must be present in the dictionary if not already set in the Tap object. All values in the provided dictionary will overwrite values currently in the Tap object. Loading from configuration filesConfiguration files can be loaded along with arguments with the optional flag config_files List[str]. Arguments passed in from the command line overwrite arguments from the configuration files. Arguments in configuration files that appear later in the list overwrite the arguments in previous configuration files.For example, if you have the config file my_config.txt--arg1 1--arg2 twothen you can writepythonfrom tap import Tapclass Args(Tap)    arg1 int    arg2 strargs  Args(config_files[my_config.txt]).parse_args()Config files are parsed using shlex.split from the python standard library, which supports shell-style string quoting, as well as line-end comments starting with .For example, if you have the config file my_config_shlex.txt--arg1 21  Important arg value Multi-word quoted string--arg2 two three fourthen you can writepythonfrom tap import Tapclass Args(Tap)    arg1 int    arg2 strargs  Args(config_files[my_config_shlex.txt]).parse_args()to get the resulting args  {arg1 21, arg2 two three four}The legacy parsing behavior of using standard string split can be re-enabled by passing legacy_config_parsingTrue to parse_args. tapifytapify makes it possible to run functions or initialize objects via command line arguments. This is inspired by Googles [Python Fire](httpsgithub.comgooglepython-fire), but tapify also automatically casts command line arguments to the appropriate types based on the type hints. Under the hood, tapify implicitly creates a Tap object and uses it to parse the command line arguments, which it then uses to run the function or initialize the class. We show a few examples below. Examples Functionpython square_function.pyfrom tap import tapifydef square(num float) - float    Square a number.    param num The number to square.        return num  2if __name__  __main__    squared  tapify(square)    print(fThe square of your number is {squared}.)Running python square_function.py --num 5 prints The square of your number is 25.0.. Classpython square_class.pyfrom tap import tapifyclass Squarer    def __init__(self, num float) - None        Initialize the Squarer with a number to square.        param  num The number to square.                self.num  num    def get_square(self) - float        Get the square of the number.        return self.num  2if __name__  __main__    squarer  tapify(Squarer)    print(fThe square of your number is {squarer.get_square()}.)Running python square_class.py --num 2 prints The square of your number is 4.0.. Dataclasspython square_dataclass.pyfrom dataclasses import dataclassfrom tap import tapifydataclassclass Squarer    Squarer with a number to square.     param  num The number to square.        num float    def get_square(self) - float        Get the square of the number.        return self.num  2if __name__  __main__    squarer  tapify(Squarer)    print(fThe square of your number is {squarer.get_square()}.)Running python square_dataclass.py --num -1 prints The square of your number is 1.0.. tapify helpThe help string on the command line is set based on the docstring for the function or class. For example, running python square_function.py -h will printusage square_function.py [-h] --num NUMSquare a number.options  -h, --help  show this help message and exit  --num NUM   (float, required) The number to square.Note that for classes, if there is a docstring in the __init__ method, then tapify sets the help string description to that docstring. Otherwise, it uses the docstring from the top of the class. Command line vs explicit argumentstapify can simultaneously use both arguments passed from the command line and arguments passed in explicitly in the tapify call. Arguments provided in the tapify call override function defaults, and arguments provided via the command line override both arguments provided in the tapify call and function defaults. We show an example below.python add.pyfrom tap import tapifydef add(num_1 float, num_2 float  0.0, num_3 float  0.0) - float    Add numbers.    param num_1 The first number.    param num_2 The second number.    param num_3 The third number.        return num_1 + num_2 + num_3if __name__  __main__    added  tapify(add, num_22.2, num_34.1)    print(fThe sum of your numbers is {added}.)Running python add.py --num_1 1.0 --num_2 0.9 prints The sum of your numbers is 6.0.. (Note that add took num_1  1.0 and num_2  0.9 from the command line and num_34.1 from the tapify call due to the order of precedence.) Known argsCalling tapify with known_onlyTrue allows tapify to ignore additional arguments from the command line that are not needed for the function or class. If known_onlyFalse (the default), then tapify will raise an error when additional arguments are provided. We show an example below where known_onlyTrue might be useful for running multiple tapify calls.python person.pyfrom tap import tapifydef print_name(name str) - None    Print a persons name.    param name A persons name.        print(fMy name is {name}.)def print_age(age int) - None    Print a persons age.    param name A persons age.        print(fMy age is {age}.)if __name__  __main__    tapify(print_name, known_onlyTrue)    tapify(print_age, known_onlyTrue)Running python person.py --name Jesse --age 1 prints My name is Jesse. followed by My age is 1.. Without known_onlyTrue, the tapify calls would raise an error due to the extra argument.</longdescription>
</pkgmetadata>