<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpstravis-ci.orgParselypykafka.svgbranchmaster    target httpstravis-ci.orgParselypykafka.. image httpscodecov.iogithubParselypykafkacoverage.svgbranchmaster    target httpscodecov.iogithubParselypykafkabranchmasterPyKafka.. image httpi.imgur.comztYl4lG.jpgPyKafka is a programmer-friendly Kafka client for Python. It includes Pythonimplementations of Kafka producers and consumers, which are optionally backedby a C extension built on librdkafka_. It runs under Python 2.7+, Python 3.4+,and PyPy, and supports versions of Kafka 0.8.2 and newer... _librdkafka httpsgithub.comedenhilllibrdkafkaPyKafkas primary goal is to provide a similar level of abstraction to theJVM Kafka client_ using idioms familiar to Python programmers and exposingthe most Pythonic API possible.You can install PyKafka from PyPI with     pip install pykafkaor from conda-forge with     conda install -c conda-forge pykafkaFull documentation and usage examples for PyKafka can be found on readthedocs_.You can install PyKafka for local development and testing by cloning this repository andrunning     python setup.py develop.. _JVM Kafka client httpsgithub.comapachekafkatree0.8.2clientssrcmainjavaorgapachekafka.. _readthedocs httppykafka.readthedocs.orgenlatestGetting Started---------------Assuming you have at least one Kafka instance running on localhost, you can use PyKafkato connect to it... sourcecode python     from pykafka import KafkaClient     client  KafkaClient(hosts127.0.0.19092,127.0.0.19093,...)Or, for a TLS connection, you might write (and also see SslConfig docsfor further details).. sourcecode python     from pykafka import KafkaClient, SslConfig     config  SslConfig(cafileyourca.cert,    ...                    certfileyourclient.cert,   optional    ...                    keyfileyourclient.key,   optional    ...                    passwordunlock my client key please)   optional     client  KafkaClient(hosts127.0.0.1ssl-port,...,    ...                      ssl_configconfig)If the cluster youve connected to has any topics defined on it, you can listthem with.. sourcecode python     client.topics     topic  client.topics[my.test]Once youve got a Topic, you can create a Producer for it and startproducing messages... sourcecode python     with topic.get_sync_producer() as producer    ...     for i in range(4)    ...         producer.produce(test message  + str(i  2))The example above would produce to kafka synchronously - the call onlyreturns after we have confirmation that the message made it to the cluster.To achieve higher throughput, we recommend using the Producer inasynchronous mode, so that produce() calls will return immediately and theproducer may opt to send messages in larger batches. The Producer collectsproduced messages in an internal queue for linger_ms before sending each batch.This delay can be removed or changed at the expense of efficiency with linger_ms,min_queued_messages, and other keyword arguments (see readthedocs_). You can still obtaindelivery confirmation for messages, through a queue interface which can beenabled by setting delivery_reportsTrue.  Heres a rough usage example.. sourcecode python     with topic.get_producer(delivery_reportsTrue) as producer    ...     count  0    ...     while True    ...         count + 1    ...         producer.produce(test msg, partition_key{}.format(count))    ...         if count  10  5  0   adjust this or bring lots of RAM )    ...             while True    ...                 try    ...                     msg, exc  producer.get_delivery_report(blockFalse)    ...                     if exc is not None    ...                         print Failed to deliver msg {} {}.format(    ...                             msg.partition_key, repr(exc))    ...                     else    ...                         print Successfully delivered msg {}.format(    ...                         msg.partition_key)    ...                 except Queue.Empty    ...                     breakNote that the delivery report queue is thread-local it will only serve reportsfor messages which were produced from the current thread. Also, if youre usingdelivery_reportsTrue, failing to consume the delivery report queue will causePyKafkas memory usage to grow unbounded.You can also consume messages from this topic using a Consumer instance... sourcecode python     consumer  topic.get_simple_consumer()     for message in consumer    ...     if message is not None    ...         print message.offset, message.value    0 test message 0    1 test message 1    2 test message 4    3 test message 9This SimpleConsumer doesnt scale - if you have two SimpleConsumersconsuming the same topic, they will receive duplicate messages. To get aroundthis, you can use the BalancedConsumer... sourcecode python     balanced_consumer  topic.get_balanced_consumer(    ...     consumer_grouptestgroup,    ...     auto_commit_enableTrue,    ...     zookeeper_connectmyZkClusterNode1.com2181,myZkClusterNode2.com2181myZkChroot    ... )You can have as many BalancedConsumer instances consuming a topic as thattopic has partitions. If they are all connected to the same zookeeper instance,they will communicate with it to automatically balance the partitions betweenthemselves. The partition assignment strategy used by the BalancedConsumer isthe range strategy by default. The strategy is switchable via the membership_protocolkeyword argument, and can be either an object exposed by pykafka.membershipprotocol ora custom instance of pykafka.membershipprotocol.GroupMembershipProtocol.You can also use the Kafka 0.9 Group Membership API with the managedkeyword argument on get_balanced_consumer.Using the librdkafka extension------------------------------PyKafka includes a C extension that makes use of librdkafka to speed up producerand consumer operation. To use the librdkafka extension, you need to make sure the headerfiles and shared library are somewhere where python can find them, both when you buildthe extension (which is taken care of by setup.py develop) and at run time.Typically, this means that you need to either install librdkafka in a placeconventional for your system, or declare C_INCLUDE_PATH, LIBRARY_PATH,and LD_LIBRARY_PATH in your shell environment to point to the installation locationof the librdkafka shared objects. You can find this location with locate librdkafka.so.After that, all thats needed is that you pass an extra parameteruse_rdkafkaTrue to topic.get_producer(),topic.get_simple_consumer(), or topic.get_balanced_consumer().  Notethat some configuration options may have different optimal values it may beworthwhile to consult librdkafkas configuration notes_ for this... _0.9.1 httpsgithub.comedenhilllibrdkafkareleasestag0.9.1.. _configuration notes httpsgithub.comedenhilllibrdkafkablob0.9.1CONFIGURATION.mdOperational Tools-----------------PyKafka includes a small collection of CLI tools_ that can help with common tasksrelated to the administration of a Kafka cluster, including offset and lag monitoring andtopic inspection. The full, up-to-date interface for these tools can be fould by running.. sourcecode sh     python clikafka_tools.py --helpor after installing PyKafka via setuptools or pip.. sourcecode sh     kafka-tools --help.. _CLI tools httpsgithub.comParselypykafkablobmasterpykafkaclikafka_tools.pyPyKafka or kafka-python------------------------These are two different projects.See the discussion here httpsgithub.comParselypykafkaissues334_ for comparisonsbetween the two projects.Contributing------------If youre interested in contributing code to PyKafka, a good place to start is thehelp wanted_ issue tag. We also recommend taking a look at the contribution guide_... _help wanted httpsgithub.comParselypykafkaissuesqis3Aopen+is3Aissue+label3A22help+wanted22Support-------If you need help using PyKafka, there are a bunch of resources available.For usage questions or common recipes, check out the StackOverflow tag_.The Google Group_ can be useful for more in-depth questions or inquriesyoud like to send directly to the PyKafka maintainers. If you believe youvefound a bug in PyKafka, please open a github issue_ after reading thecontribution guide_... _StackOverflow tag httpsstackoverflow.comquestionstaggedpykafka.. _github issue httpsgithub.comParselypykafkaissues.. _Google Group httpsgroups.google.comforumforumpykafka-user.. _contribution guide httpsgithub.comParselypykafkablobmasterCONTRIBUTING.rst</longdescription>
</pkgmetadata>