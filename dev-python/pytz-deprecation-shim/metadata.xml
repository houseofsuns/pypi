<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pytz_deprecation_shim Shims to help you safely remove pytzpytz has served the Python community well for many years, but it is nolonger the best option for providing time zones. pytz has a non-standardinterface that is very easy to misusehttpsblog.ganssle.ioarticles201803pytz-fastest-footgun.html_ thisinterface was necessary when pytz was created, because datetime had noway to represent ambiguous datetimes, but this was solved in in Python 3.6,which added a fold attribute to datetimes in PEP 495httpswww.python.orgdevpepspep-0495_. With the addition of thezoneinfo module in Python 3.9 (PEP 615httpswww.python.orgdevpepspep-0615_), there has never been a bettertime to migrate away from pytz.However, since pytz time zones are used very differently from a standardtzinfo, and many libraries have built pytz zones into their standardtime zone interface (and thus may have users relying on the existence of thelocalize and normalize methods) this library provides shim classesthat are compatible with both PEP 495 and pytzs interface, to make iteasier for libraries to deprecate pytz.UsageThis library is intended for temporary usage only, and should allow you todrop your dependency on pytz while also giving your users notice thateventually you will remove support for the pytz-specific interface.Within your own code, use pytz_deprecation_shim.timezone shims as if theywere zoneinfo or dateutil.tz zones  do not use localize ornormalize.. code-block pycon     import pytz_deprecation_shim as pds     from datetime import datetime, timedelta     LA  pds.timezone(AmericaLos_Angeles)     dt  datetime(2020, 10, 31, 12, tzinfoLA)     print(dt)    2020-10-31 120000-0700     dt.tzname()    PDTDatetime addition will work like normal Python datetime arithmetichttpsblog.ganssle.ioarticles201802aware-datetime-arithmetic.html_,even across a daylight saving time transition.. code-block pycon     dt_add  dt + timedelta(days1)     print(dt_add)    2020-11-01 120000-0800     dt_add.tzname()    PSTHowever, if you have exposed a time zone to end users who are using localizeandor normalize or any other pytz-specific features (or if youvefailed to convert some of your own code all the way), those users will seea warning (rather than an exception) when they use those features.. code-block pycon     dt  LA.localize(datetime(2020, 10, 31, 12))    stdin1 PytzUsageWarning The localize method is no longer necessary, as    this time zone supports the fold attribute (PEP 495). For more details on    migrating to a PEP 495-compliant implementation, see    httpspytz-deprecation-shim.readthedocs.ioenlatestmigration.html      print(dt)    2020-10-31 120000-0700     dt.tzname()    PDT     dt_add  LA.normalize(dt + timedelta(days1))    stdin1 PytzUsageWarning The normalize method is no longer necessary,    as this time zone supports the fold attribute (PEP 495). For more details    on migrating to a PEP 495-compliant implementation, see    httpspytz-deprecation-shim.readthedocs.ioenlatestmigration.html     print(dt_add)    2020-11-01 120000-0800     dt_add.tzname()    PSTFor IANA time zones, calling str() on the shim zones (and indeed on pytzand zoneinfo zones as well) returns the IANA key, so end users who wouldlike to actively migrate to a zoneinfo (or backports.zoneinfo) can doso.. code-block pycon     from zoneinfo import ZoneInfo     LA  pds.timezone(AmericaLos_Angeles)     LA_zi  ZoneInfo(str(LA))     print(LA_zi)    zoneinfo.ZoneInfo(keyAmericaLos_Angeles)</longdescription>
</pkgmetadata>