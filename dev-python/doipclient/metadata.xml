<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>python-doipclient.. image httpstravis-ci.comjacobschaerpython-doipclient.svgbranchmain    target httpstravis-ci.comjacobschaerpython-doipclientdoipclient is a pure Python 3 Diagnostic over IP (DoIP) client which can be usedfor communicating with modern ECUs over automotive ethernet. It implements themajority of ISO-13400 (2019) from the perspective of a short-lived synchronousclient. The primary use case is to serve as a transport layer implementation forthe udsoncan httpsgithub.compylessardpython-udsoncan_ library. The codeis published under MIT license on GitHub (jacobschaerpython-doipclient).Documentation-------------The documentation is available here  httpspython-doipclient.readthedocs.ioRequirements------------ - Python 3.6+Installation------------using pip    pip install doipclientRunning Tests from source-------------------------using pytest    pip install pytest pytest-mock    pytestExample-------Updated version of udsoncans example using python_doip instead of IsoTPSocketConnection.. code-block python   import SomeLib.SomeCar.SomeModel as MyCar   import udsoncan   from doipclient import DoIPClient   from doipclient.connectors import DoIPClientUDSConnector   from udsoncan.client import Client   from udsoncan.exceptions import    from udsoncan.services import    udsoncan.setup_logging()   ecu_ip  127.0.0.1   ecu_logical_address  0x00E0   doip_client  DoIPClient(ecu_ip, ecu_logical_address)   conn  DoIPClientUDSConnector(doip_client)   with Client(conn, request_timeout2, configMyCar.config) as client      try         client.change_session(DiagnosticSessionControl.Session.extendedDiagnosticSession)   integer with value of 3         client.unlock_security_access(MyCar.debug_level)                                    Fictive security level. Integer coming from fictive lib, lets say its value is 5         client.write_data_by_identifier(udsoncan.DataIdentifier.VIN, ABC123456789)        Standard ID for VIN is 0xF190. Codec is set in the client configuration         print(Vehicle Identification Number successfully changed.)         client.ecu_reset(ECUReset.ResetType.hardReset)                                      HardReset  0x01      except NegativeResponseException as e         print(Server refused our request for service s with code s (0x02x)  (e.response.service.get_name(), e.response.code_name, e.response.code))      except (InvalidResponseException, UnexpectedResponseException) as e         print(Server sent an invalid payload  s  e.response.original_payload)       Because we reset our UDS server, we must also reconnectreactivate the DoIP socket       Alternatively, we could have used the auto_reconnect_tcp flag on the DoIPClient       Note ECUs do not restart instantly, so you may need a sleep() before moving on      doip_client.reconnect()      client.tester_present()    Cleanup the DoIP Socket when were done. Alternatively, we could have used the    close_connection flag on conn so that the udsoncan client would clean it up   doip_client.close()python-uds Support------------------The python-uds httpsgithub.comrichClubbpython-uds_ can also be usedbut requires a fork until the owner merges this PRDoip 63 httpsgithub.comrichClubbpython-udspull63_. For now, to usethe portusing pip    git clone httpsgithub.comjacobschaerpython-uds    git checkout doip    cd python-uds    pip install .Example.. code-block python   from uds import Uds   ecu  Uds(transportProtocolDoIP, ecu_ip192.168.1.1, ecu_logical_address1)   try       response  ecu.send([0x3E, 0x00])       print(response)   This should be [0x7E, 0x00]   except       print(Send did not complete)</longdescription>
</pkgmetadata>