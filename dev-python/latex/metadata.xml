<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>latexDocumentation can be found at httpspythonhosted.orglatex .Allows calling LaTeX from Python without leaving a mess. Similar to the(officially obsolete) tex httpspypi.python.orgpypitex_ package, whosesuccessor httpwww.profv.detexcaller_ is not PyPi-installable.. code-block python     min_latex  (rdocumentclass{article}                  rbegin{document}                  rHello, world                  rend{document})     from latex import build_pdf      this builds a pdf-file inside a temporary directory     pdf  build_pdf(min_latex)      look at the first few bytes of the header     print bytes(pdf)[10]Also comes with support for using Jinja2 httpjinja.pocoo.org_ templatesto generate LaTeX files.make_env can be used to create an Environment that plays well withLaTex   Variables can be used in a LaTeX friendly way Hello, VAR{namee}.   Note that autoescaping is off. Blocks are creating using the block macro   BLOCK{if weather is good}   Hooray.   BLOCK{endif}   {comments are supported as well}    and so are line comments   To keep things short, line statements can be used   - if weather is good   Yay.   - endifExample use-----------.. code-block python    from jinja2.loaders import FileSystemLoader    from latex.jinja2 import make_env    env  make_env(loaderFileSystemLoader(.))    tpl  env.get_template(doc.latex)    print(tpl.render(nameAlice))The base.latex demonstrates how BLOCK{...} is substituted for{ ... }.. code-block latex    documentclass{article}    begin{document}    BLOCK{block body}BLOCK{endblock}    end{document}Finally, doc.latex shows why the - syntax is usually preferable.. code-block latex    - extends base.latex    - block body    Hello, VAR{namee}.    - endblock</longdescription>
</pkgmetadata>