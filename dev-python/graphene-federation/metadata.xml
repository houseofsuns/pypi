<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> graphene-federationFederation support for [Graphene Logo](httpgraphene-python.orgfavicon.png) [Graphene](httpgraphene-python.org) following the [Apollo Federation specifications](httpswww.apollographql.comdocsfederationsubgraph-spec).[[PyPI version][pypi-image]][pypi-url][[Unit Tests Status][unit-tests-image]][unit-tests-url][[Coverage Status][coveralls-image]][coveralls-url][[Integration Tests Status][integration-tests-image]][integration-tests-url][pypi-image] httpsbadge.fury.iopygraphene-federation.svg[pypi-url] httpspypi.orgprojectgraphene-federation[unit-tests-image] httpsgithub.comgraphql-pythongraphene-federationworkflowsUnit20Testsbadge.svgbranchmain[unit-tests-url] httpsgithub.comgraphql-pythongraphene-federationactionsqueryworkflow3AUnit+Tests[coveralls-image] httpscoveralls.ioreposgithubgraphql-pythongraphene-federationbadge.svgbranchmain[coveralls-url] httpscoveralls.iogithubgraphql-pythongraphene-federationbranchmain[integration-tests-image] httpsgithub.comgraphql-pythongraphene-federationworkflowsIntegration20Testsbadge.svgbranchmain[integration-tests-url] httpsgithub.comgraphql-pythongraphene-federationactionsqueryworkflow3AIntegration+TestsThis repository is heavily based on the repo it was forked from... Huge thanks to [Preply for setting up the foundations](httpsmedium.compreply-engineeringapollo-federation-support-in-graphene-761a0512456d).WARNING This version is not compatible with graphene version below v3.If you need to use a version compatible with graphene v2 I recommend using the version 1.0.0 of graphene_federation.------------------------ Supported FeaturesAt the moment it supports sdl (_service on field) enable to add schema in federation (as is) key decorator (entity support) enable to perform queries across service boundaries (you can have more than one key per type) extend extend remote types external() mark a field as external requires() mark that field resolver requires other fields to be pre-fetched provides()provides annotate the expected returned fieldset from a field on a base type that is guaranteed to be selectable by the gateway.Each type which is decorated with key or extend is added to the _Entity union.The [__resolve_reference method](httpswww.apollographql.comdocsfederationapiapollo-federation__resolvereference) can be defined for each type that is an entity.Note that since the notation with double underscores can be problematic in Python for model inheritance this resolver method can also be named _resolve_reference (the __resolve_reference method will take precedence if both are declared).This method is called whenever an entity is requested as part of the fulfilling a query plan.If not explicitly defined, the default resolver is used.The default resolver just creates instance of type with passed fieldset as kwargs, see [entity.get_entity_query](graphene_federationentity.py) for more details You should define __resolve_reference, if you need to extract object before passing it to fields resolvers (example [FileNode](integration_testsservice_bsrcschema.py)) You should not define __resolve_reference, if fields resolvers need only data passed in fieldset (example [FunnyText](integration_testsservice_asrcschema.py))Read more in [official documentation](httpswww.apollographql.comdocsapollo-serverapiapollo-federation__resolvereference).------------------------ ExampleHere is an example of implementation based on the [Apollo Federation introduction example](httpswww.apollographql.comdocsfederation).It implements a federation schema for a basic e-commerce application over three services accounts, products, reviews. AccountsFirst add an account service that expose a User type that can then be referenced in other services by its id fieldpythonfrom graphene import Field, ID, ObjectType, Stringfrom graphene_federation import build_schema, keykey(id)class User(ObjectType)    id  Int(requiredTrue)    username  String(requiredTrue)    def __resolve_reference(self, info, kwargs)                Here we resolve the reference of the user entity referenced by its id field.                return User(idself.id, emailfuser_{self.id}mail.com)class Query(ObjectType)    me  Field(User)schema  build_schema(queryQuery) ProductThe product service exposes a Product type that can be used by other services via the upc fieldpythonfrom graphene import Argument, ID, Int, List, ObjectType, Stringfrom graphene_federation import build_schema, keykey(upc)class Product(ObjectType)    upc  String(requiredTrue)    name  String(requiredTrue)    price  Int()    def __resolve_reference(self, info, kwargs)                Here we resolve the reference of the product entity referenced by its upc field.                return Product(upcself.upc, namefproduct {self.upc})class Query(ObjectType)    topProducts  List(Product, firstArgument(Int, default_value5))schema  build_schema(queryQuery) ReviewsThe reviews service exposes a Review type which has a link to both the User and Product types.It also has the ability to provide the username of the User.On top of that it adds to the UserProduct types (that are both defined in other services) the ability to get their reviews.pythonfrom graphene import Field, ID, Int, List, ObjectType, Stringfrom graphene_federation import build_schema, extend, external, providesextend(id)class User(ObjectType)    id  external(Int(requiredTrue))    reviews  List(lambda Review)    def resolve_reviews(self, info, args, kwargs)                Get all the reviews of a given user. (not implemented here)                return []extend(upc)class Product(ObjectType)    upc  external(String(requiredTrue))    reviews  List(lambda Review) Note that both the base type and the field need to be decorated with provides (on the field itself you need to specify which fields get provided).providesclass Review(ObjectType)    body  String()    author  provides(Field(User), fieldsusername)    product  Field(Product)class Query(ObjectType)    review  Field(Review)schema  build_schema(queryQuery) FederationNote that each schema declaration for the services is a valid graphql schema (it only adds the _Entity and _Service types).The best way to check that the decorator are set correctly is to request the service sdlpythonfrom graphql import graphqlquery  query {    _service {        sdl    }}result  graphql(schema, query)print(result.data[_service][sdl])Those can then be used in a federated schema.You can find more examples in the unit  integration tests and [examples folder](examples).There is also a cool [example](httpsgithub.compreplygraphene-federationissues1) of integration with Mongoengine.------------------------ Known issues1. decorators will not work properly on fields with custom names for example some_field  String(nameanother_name)1. key decorator will not work on [compound primary key](httpswww.apollographql.comdocsfederationentities-advancedcompound-keys)------------------------ Contributing You can run the unit tests by doing make tests. You can run the integration tests by doing make integration-build  make integration-test. You can get a development environment (on a Docker container) with make dev-setup. You should use black to format your code.The tests are automatically run on Travis CI on push to GitHub.---------------------------</longdescription>
</pkgmetadata>