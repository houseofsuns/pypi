<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Django Reverse AdminModule that makes django admin handle OneToOneFields in a better way. A common use case for one-to-one relationships is to embed a model inside another one. For example, a Person may have multiple foreign keys pointing to an Address entity, one home address, one business address and so on. Django admin displays those relations using select boxes, letting the user choose which address entity to connect to a person. A more natural way to handle the relationship is using inlines. However, since the foreign key is placed on the owning entity, django admins standard inline classes cant be used. Which is why I created this module that implements reverse inlines for this use case.Fixextension of [adminreverse](httpsgithub.comrpkilbydjango-reverse-admin) [reverseadmin](httpdjangosnippets.orgsnippets2032)[[CircleCI](httpscircleci.comghdaniyalzadedjango_reverse_admin.svgstylesvg)](httpscircleci.comghdaniyalzadedjango_reverse_admin) Requirements Python 3.4, 3.5, 3.6, 3.7 Django 2.0+From version 2.0 onwards Django Reverse Admin is Python 3 only. If you need to support Python 2.7 use the version 1.0 release. InstallationInstall using pipshpip install django_reverse_admin TestingUse tox for testing.shtox Testing Manually Using Django AdminRun the following commands to make sure that db.sqlite3 has all the data that you needshcd testspython manage.py makemigrationspython manage.py makemigrations pollspython manage.py migratepython manage.py loaddata fixturesinit_data.jsonpython manage.py runserverAdmin user username is reverse and password is reverseadmin Usagemodels.py filepyfrom django.db import modelsclass Address(models.Model)    street  models.CharField(max_length255)    zipcode  models.CharField(max_length10)    city  models.CharField(max_length255)    state  models.CharField(max_length2)class Person(models.Model)    name  models.CharField(max_length255)    business_addr  models.ForeignKey(Address,                                      related_namebusiness_addr)    home_addr  models.OneToOneField(Address, related_namehome_addr)    other_addr  models.OneToOneField(Address, related_nameother_addr)admin.py filepyfrom django.contrib import adminfrom django.db import modelsfrom models import Personfrom django_reverse_admin import ReverseModelAdminclass PersonAdmin(ReverseModelAdmin)    inline_type  tabular    inline_reverse  [business_addr,                      (home_addr, {fields [street, city, state, zipcode]}),                      ]admin.site.register(Person, PersonAdmin)inline_type can be either tabular or stacked for tabular and stacked inlines respectively.The module is designed to work with Django 2+ Since it hooks into the internals of the admin package, it may not work with later Django versions. DemoBelow is what an admin change_view with reverse inline looks like[reverse inline](imagesadmin_with_reverse_inline_1.png)Below is what the same change_view would look like WITHOUT reverse inline[no reverse inline](imagesadmin_without_reverse_inline.png) Contribtion Make sure that the tests are passing before opening up the PR Create a PR for feature enhancements Once a PR is merged, update version with the following commandsbumpversion patchgit push origin master --tags Packagingtox creates a package in .toxdist. Use twine to upload it to pypishtwine upload .toxdistdjango_reverse_admin-.zip</longdescription>
</pkgmetadata>