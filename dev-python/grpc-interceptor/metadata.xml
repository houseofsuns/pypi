<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>[[Tests](httpsgithub.comd5h-fossgrpc-interceptorworkflowsTestsbadge.svg)](httpsgithub.comd5h-fossgrpc-interceptoractionsworkflowTests)[[Codecov](httpscodecov.ioghd5h-fossgrpc-interceptorbranchmastergraphbadge.svg)](httpscodecov.ioghd5h-fossgrpc-interceptor)[[Read the Docs](httpsreadthedocs.orgprojectsgrpc-interceptorbadge)](httpsgrpc-interceptor.readthedocs.io)[[PyPI](httpsimg.shields.iopypivgrpc-interceptor.svg)](httpspypi.orgprojectgrpc-interceptor) SummarySimplified Python gRPC interceptors.The Python grpc package provides service interceptors, but theyre a bit hard touse because of their flexibility. The grpc interceptors dont have direct accessto the request and response objects, or the service context. Access to these are oftendesired, to be able to log data in the request or response, or set status codes on thecontext. InstallationTo just get the interceptors (and probably not write your own)console pip install grpc-interceptorTo also get the testing framework, which is good if youre writing your own interceptorsconsole pip install grpc-interceptor[testing] Usage Server InterceptorTo define your own interceptor (we can use ExceptionToStatusInterceptor as an example)pythonfrom grpc_interceptor import ServerInterceptorfrom grpc_interceptor.exceptions import GrpcExceptionclass ExceptionToStatusInterceptor(ServerInterceptor)    def intercept(        self,        method Callable,        request Any,        context grpc.ServicerContext,        method_name str,    ) - Any        Override this method to implement a custom interceptor.         You should call method(request, context) to invoke the         next handler (either the RPC method implementation, or the         next interceptor in the list).         Args             method The next interceptor, or method implementation.             request The RPC request, as a protobuf message.             context The ServicerContext pass by gRPC to the service.             method_name A string of the form                 protobuf.package.ServiceMethod         Returns             This should generally return the result of             method(request, context), which is typically the RPC             method response, as a protobuf message. The interceptor             is free to modify this in some way, however.                 try            return method(request, context)        except GrpcException as e            context.set_code(e.status_code)            context.set_details(e.details)            raiseThen inject your interceptor when you create the grpc serverpythoninterceptors  [ExceptionToStatusInterceptor()]server  grpc.server(    futures.ThreadPoolExecutor(max_workers10),    interceptorsinterceptors)To use ExceptionToStatusInterceptorpythonfrom grpc_interceptor.exceptions import NotFoundclass MyService(my_pb2_grpc.MyServiceServicer)    def MyRpcMethod(        self, request MyRequest, context grpc.ServicerContext    ) - MyResponse        thing  lookup_thing()        if not thing            raise NotFound(Sorry, your thing is missing)        ...This results in the gRPC status status code being set to NOT_FOUND,and the details Sorry, your thing is missing. This saves you the hassle ofcatching exceptions in your service handler, or passing the context down intohelper functions so they can call context.abort or context.set_code. It allowsthe more Pythonic approach of just raising an exception from anywhere in the code,and having it be handled automatically. Client InterceptorWe will use an invocation metadata injecting interceptor as an example of defininga client interceptorpythonfrom grpc_interceptor import ClientCallDetails, ClientInterceptorclass MetadataClientInterceptor(ClientInterceptor)    def intercept(        self,        method Callable,        request_or_iterator Any,        call_details grpc.ClientCallDetails,    )        Override this method to implement a custom interceptor.        This method is called for all unary and streaming RPCs. The interceptor        implementation should call method using a grpc.ClientCallDetails and the        request_or_iterator object as parameters. The request_or_iterator        parameter may be type checked to determine if this is a singluar request        for unary RPCs or an iterator for client-streaming or client-server streaming        RPCs.        Args            method A function that proceeds with the invocation by executing the next                interceptor in the chain or invoking the actual RPC on the underlying                channel.            request_or_iterator RPC request message or iterator of request messages                for streaming requests.            call_details Describes an RPC to be invoked.        Returns            The type of the return should match the type of the return value received            by calling method. This is an object that is both a            Call httpsgrpc.github.iogrpcpythongrpc.htmlgrpc.Call_ for the            RPC and a Future httpsgrpc.github.iogrpcpythongrpc.htmlgrpc.Future_.            The actual result from the RPC can be got by calling .result() on the            value returned from method.                new_details  ClientCallDetails(            call_details.method,            call_details.timeout,            [(authorization, Bearer mysecrettoken)],            call_details.credentials,            call_details.wait_for_ready,            call_details.compression,        )        return method(request_or_iterator, new_details)Now inject your interceptor when you create the grpc channelpythoninterceptors  [MetadataClientInterceptor()]with grpc.insecure_channel(grpc-server50051) as channel    channel  grpc.intercept_channel(channel, interceptors)    ...Client interceptors can also be used to retry RPCs that fail due to specific errors, ora host of other use cases. There are some basic approaches in the tests to get youstarted. DocumentationThe examples above showed usage for simple unary-unary RPC calls. For examples ofstreaming and asyncio RPCs, read the[complete documentation here](httpsgrpc-interceptor.readthedocs.io).Note that there is no asyncio client interceptors at the moment, though contributionsare welcome.</longdescription>
</pkgmetadata>