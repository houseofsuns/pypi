<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>datadata is a small Python module that allows you to treat input in a singularway and leave it up to the caller to supply a byte-string, a unicode object, afile-like or a filename... code-block python     open(helloworld.txt, w).write(hello, world from a file)     from data import Data as I     a  I(uhello, world)     b  I(filehelloworld.txt)     c  I(open(helloworld.txt))     print unicode(a)    hello, world     print unicode(b)    hello, world from a file     print unicode(c)    hello, world from a fileThis can be made even more convenient using the data decorator.. code-block python     from data.decorators import data     data(buf)    ... def parse_buffer(buf, magic_modeFalse)    ...   return buf passed in as  + repr(buf)    ...     parse_buffer(hello)    buf passed in as Data(datahello, encodingutf8)     rv  parse_buffer(open(helloworld.txt))     assert file in rvFitting in----------All instances support methods like read or __str__ that make it easy tofit it into existing APIs.. code-block python     d  I(some data)     d.read(4)    usome     d.read(4)    u dat     d.read(4)    ua     e  I(umore data)     str(e)    more dataNote how read returns unicode. Additionally, readb is available.. code-block python     f  I(uI am xdcnicode.)     f.readb()    I am xc3x9cnicode.Every data object has an encoding attribute which is used for convertingfrom and to unicode... code-block python     g  I(uI am xdcnicode., encodinglatin1)     g.readb()    I am xdcnicode.Iteration and line reading are also supported.. code-block python     h  I(I amnof manynlines)     h.readline()    uI amn     h.readlines()    [uof manyn, ulines]     i  I(line onenline twon)     list(iter(i))    [uline onen, uline twon]Extras------save_toSome useful convenience methods are available.. code-block python     j  I(example)     j.save_to(example.txt)The save_to method will use the most efficient way possible to save thedata to a file (copyfileobj or write()). It can also be passed afile-like object.. code-block python     k  I(example2)     with open(example2.txt, wb) as out    ...     k.save_to(out)    ...temp_savedIf you need the output inside a secure temporary file, temp_saved isavailable.. code-block python     l  I(goes into tmp)     with l.temp_saved() as tmp    ...     print tmp.name.startswith(tmptmp)    ...     print l.read()    ...    True    goes into tmptemp_saved functions almost identically to tempfile.NamedTemporaryFile,with one difference There is no delete argument. The file is removed onlywhen the context manager exits.Where it is useful------------------data can be used on both sides of an API, either while passing values in.. code-block python     import json     from data import Data as I     m  I({this json})     json.load(m)    {uthis ujson}or when getting values passed (see the data decorator example above). Ifnecessary, you can also support APIs that allow users to pass in filenames.. code-block python     class Parser(object)    ...   data(input)    ...   def parse(self, input, parser_optFalse)    ...     return input    ...   def parse_file(self, input_file, args, kwargs)    ...     return self.parse(I(fileinput_file), args, kwargs)    ...     p  Parser()     p.parse_file(devurandom)    Data(filedevurandom, encodingutf8)See the documentation at httppythonhosted.orgdata for an API reference.Python 2 and 3--------------data works the same on Python 2 and 3 thanks to sixhttpspypi.python.orgpypisix_, a few compatibility functions and atestsuite.Python 3 is supported from 3.3 onwards, Python 2 from 2.6.</longdescription>
</pkgmetadata>