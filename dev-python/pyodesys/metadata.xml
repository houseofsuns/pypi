<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pyodesys========.. image:: http://hera.physchem.kth.se:8090/api/badges/bjodah/pyodesys/status.svg   :target: http://hera.physchem.kth.se:8090/bjodah/pyodesys   :alt: Build status on Drone.. image:: https://circleci.com/gh/bjodah/pyodesys.svg?style=svg   :target: https://circleci.com/gh/bjodah/pyodesys   :alt: Build status on CircleCI.. image:: https://img.shields.io/pypi/v/pyodesys.svg   :target: https://pypi.python.org/pypi/pyodesys   :alt: PyPI version.. image:: https://img.shields.io/badge/python-3.7,3.8-blue.svg   :target: https://www.python.org/   :alt: Python version.. image:: https://img.shields.io/pypi/l/pyodesys.svg   :target: https://github.com/bjodah/pyodesys/blob/master/LICENSE   :alt: License.. image:: http://img.shields.io/badge/benchmarked%20by-asv-green.svg?style=flat   :target: http://hera.physchem.kth.se/~pyodesys/benchmarks   :alt: airspeedvelocity.. image:: http://hera.physchem.kth.se/~pyodesys/branches/master/htmlcov/coverage.svg   :target: http://hera.physchem.kth.se/~pyodesys/branches/master/htmlcov   :alt: coverage.. image:: http://joss.theoj.org/papers/10.21105/joss.00490/status.svg   :target: https://doi.org/10.21105/joss.00490   :alt: Journal of Open Source Software DOI``pyodesys`` provides a straightforward wayof numerically integrating systems of ordinary differential equations (initial value problems).It unifies the interface of several libraries for performing the numerical integration as well asseveral libraries for symbolic representation. It also provides a convenience class for representing and integrating ODE systems defined by symbolic expressions, e.g. `SymPy &lt;http://www.sympy.org&gt;`_expressions. This allows the user to write concise code and rely on ``pyodesys`` to handle the subtle differencesbetween libraries.The numerical integration is performed using either:- `scipy.integrate.ode &lt;http://docs.scipy.org/doc/scipy/reference/generated/scipy.integrate.ode.html&gt;`_- pygslodeiv2_- pyodeint_- pycvodes_.. _pygslodeiv2: https://github.com/bjodah/pygslodeiv2 .. _pyodeint: https://github.com/bjodah/pyodeint.. _pycvodes: https://github.com/bjodah/pycvodesNote that implicit steppers require a user supplied callback for calculating the Jacobian.``pyodesys.SymbolicSys`` derives the Jacobian automatically.The symbolic representation is usually in the form of SymPy expressions, but the user maychoose another symbolic back-end (see `sym &lt;https://github.com/bjodah/sym&gt;`_).When performance is of utmost importance, e.g. in model fitting where results are neededfor a large set of initial conditions and parameters, the user may transparentlyrely on compiled native code (classes in ``pyodesys.native.native_sys`` can generate optimal C++ code).The major benefit is that there is no need to manually rewrite the corresponding expressions in anotherprogramming language.Documentation-------------Auto-generated API documentation for latest stable release is found here:`&lt;https://bjodah.github.io/pyodesys/latest&gt;`_(and the development version for the current master branch is found here:`&lt;http://hera.physchem.kth.se/~pyodesys/branches/master/html&gt;`_).Installation------------Simplest way to install pyodesys and its (optional) dependencies is to use the`conda package manager &lt;http://conda.pydata.org/docs/&gt;`_:::   $ conda install -c bjodah pyodesys pytest   $ python -m pytest --pyargs pyodesysOptional dependencies~~~~~~~~~~~~~~~~~~~~~If you used ``conda`` to install pyodesys_ you can skip this section.But if you use ``pip`` you may want to know that the default installationof ``pyodesys`` only requires SciPy::   $ pip install pyodesys   $ pytest --pyargs pyodesys -rsThe above command should finish without errors but with some skipped tests.The reason for why some tests are skipped should be because missing optional solvers.To install the optional solvers you will first need to install third party libraries forthe solvers and then their python bindings. The 3rd party requirements are as follows:- pygslodeiv2_ (requires GSL_ &gt;=1.16)- pyodeint_ (requires boost_ &gt;=1.72.0)- pycvodes_ (requires SUNDIALS_ &gt;=5.3.0).. _GSL: https://www.gnu.org/software/gsl/.. _boost: http://www.boost.org/.. _SUNDIALS: https://computation.llnl.gov/projects/sundialsif you want to see what packages need to be installed on a Debian based system you may look at this`Dockerfile &lt;scripts/environment/Dockerfile&gt;`_.If you manage to install all three external libraries you may install pyodesys with the option &quot;all&quot;::  $ pip install pyodesys[all]  $ pytest --pyargs pyodesys -rsnow there should be no skipped tests. If you try to install pyodesys on a machine where you do not haveroot permissions you may find the flag ``--user`` helpful when using pip. Also if there are multipleversions of python installed you may want to invoke python for an explicit version of python, e.g.::  $ python3.6 -m pip install --user pyodesys[all]see `setup.py &lt;setup.py&gt;`_ for the exact list of requirements.Using Docker~~~~~~~~~~~~If you have `Docker &lt;https://www.docker.com&gt;`_ installed, you may use it to host a jupyternotebook server::  $ ./scripts/host-jupyter-using-docker.sh . 8888the first time you run the command some dependencies will be downloaded. When the installationis complete there will be a link visible which you can open in your browser. You can also runthe test suite using the same docker-image::  $ ./scripts/host-jupyter-using-docker.sh . 0there will be one skipped test (due to symengine missing in this pip installed environment) andquite a few instances of RuntimeWarning.Examples--------The classic van der Pol oscillator (see `examples/van_der_pol.py &lt;examples/van_der_pol.py&gt;`_).. code:: python   &gt;&gt;&gt; from pyodesys.symbolic import SymbolicSys   &gt;&gt;&gt; def f(t, y, p):   ...     return [y[1], -y[0] + p[0]*y[1]*(1 - y[0]**2)]   ...    &gt;&gt;&gt; odesys = SymbolicSys.from_callback(f, 2, 1)   &gt;&gt;&gt; xout, yout, info = odesys.integrate(10, [1, 0], [1], integrator='odeint', nsteps=1000)   &gt;&gt;&gt; _ = odesys.plot_result()   &gt;&gt;&gt; import matplotlib.pyplot as plt; plt.show()  # doctest: +SKIP.. image:: https://raw.githubusercontent.com/bjodah/pyodesys/master/examples/van_der_pol.pngIf the expression contains transcendental functions you will need to provide a ``backend`` keyword argument:.. code:: python   &gt;&gt;&gt; import math   &gt;&gt;&gt; def f(x, y, p, backend=math):   ...     return [backend.exp(-p[0]*y[0])]  # analytic: y(x) := ln(kx + kc)/k   ...    &gt;&gt;&gt; odesys = SymbolicSys.from_callback(f, 1, 1)   &gt;&gt;&gt; y0, k = -1, 3   &gt;&gt;&gt; xout, yout, info = odesys.integrate(5, [y0], [k], integrator='cvode', method='bdf')   &gt;&gt;&gt; _ = odesys.plot_result()   &gt;&gt;&gt; import matplotlib.pyplot as plt   &gt;&gt;&gt; import numpy as np   &gt;&gt;&gt; c = 1./k*math.exp(k*y0)  # integration constant   &gt;&gt;&gt; _ = plt.plot(xout, np.log(k*(xout+c))/k, '--', linewidth=2, alpha=.5, label='analytic')   &gt;&gt;&gt; _ = plt.legend(loc='best'); plt.show()  # doctest: +SKIP.. image:: https://raw.githubusercontent.com/bjodah/pyodesys/master/examples/lnx.pngIf you already have symbolic expressions created using e.g. SymPy you can create your system from those:.. code:: python   &gt;&gt;&gt; import sympy as sp   &gt;&gt;&gt; t, u, v, k  = sp.symbols('t u v k')   &gt;&gt;&gt; dudt = v   &gt;&gt;&gt; dvdt = -k*u  # differential equations for a harmonic oscillator   &gt;&gt;&gt; odesys = SymbolicSys([(u, dudt), (v, dvdt)], t, [k])   &gt;&gt;&gt; result = odesys.integrate(7, {u: 2, v: 0}, {k: 3}, integrator='gsl', method='rk8pd', atol=1e-11, rtol=1e-12)   &gt;&gt;&gt; _ = plt.subplot(1, 2, 1)   &gt;&gt;&gt; _ = result.plot()   &gt;&gt;&gt; _ = plt.subplot(1, 2, 2)   &gt;&gt;&gt; _ = plt.plot(result.xout, 2*np.cos(result.xout*3**0.5) - result.yout[:, 0])   &gt;&gt;&gt; plt.show()  # doctest: +SKIP.. image:: https://raw.githubusercontent.com/bjodah/pyodesys/master/examples/harmonic.pngYou can also refer to the dependent variables by name instead of index:.. code:: python   &gt;&gt;&gt; odesys = SymbolicSys.from_callback(   ...     lambda t, y, p: {   ...         'x': -p['a']*y['x'],   ...         'y': -p['b']*y['y'] + p['a']*y['x'],   ...         'z': p['b']*y['y']   ...     }, names='xyz', param_names='ab', dep_by_name=True, par_by_name=True)   ...    &gt;&gt;&gt; t, ic, pars = [42, 43, 44], {'x': 7, 'y': 5, 'z': 3}, {'a': [11, 17, 19], 'b': 13}   &gt;&gt;&gt; for r, a in zip(odesys.integrate(t, ic, pars, integrator='cvode'), pars['a']):   ...     assert np.allclose(r.named_dep('x'), 7*np.exp(-a*(r.xout - r.xout[0])))   ...     print('%.2f ms ' % (r.info['time_cpu']*1e3))  # doctest: +SKIP   ...    10.54 ms   11.55 ms   11.06 msNote how we generated a list of results for each value of the parameter ``a``. When using a classfrom ``pyodesys.native.native_sys`` those integrations are run in separate threads (bag of tasksparallelism):.. code:: python   &gt;&gt;&gt; from pyodesys.native import native_sys   &gt;&gt;&gt; native = native_sys['cvode'].from_other(odesys)   &gt;&gt;&gt; for r, a in zip(native.integrate(t, ic, pars), pars['a']):   ...     assert np.allclose(r.named_dep('x'), 7*np.exp(-a*(r.xout - r.xout[0])))   ...     print('%.2f ms ' % (r.info['time_cpu']*1e3))  # doctest: +SKIP   ...    0.42 ms   0.43 ms   0.42 msFor this small example we see a 20x (serial) speedup by using native code. Bigger systems often see 100x speedup.Since the latter is run in parallel the (wall clock) time spent waiting for the results is in practicefurther reduced by a factor equal to the number of cores of your CPU (number of threads used is set bythe environment variable ``ANYODE_NUM_THREADS``).For further examples, see `examples/ &lt;https://github.com/bjodah/pyodesys/tree/master/examples&gt;`_, and renderedjupyter notebooks here: `&lt;http://hera.physchem.kth.se/~pyodesys/branches/master/examples&gt;`_Run notebooks using binder~~~~~~~~~~~~~~~~~~~~~~~~~~Using only a web-browser (and an internet connection) it is possible to explore thenotebooks here: (by the courtesy of the people behind mybinder).. image:: http://mybinder.org/badge.svg   :target: https://mybinder.org/v2/gh/bjodah/pyodesys/v0.11.6?filepath=index.ipynb   :alt: BinderCiting------If you make use of pyodesys in e.g. academic work you may cite the following peer-reviewed publication:.. image:: http://joss.theoj.org/papers/10.21105/joss.00490/status.svg   :target: https://doi.org/10.21105/joss.00490   :alt: Journal of Open Source Software DOIDepending on what underlying solver you are using you should also cite the appropriate paper(you can look at the list of references in the JOSS article). If you need to reference,in addition to the paper, a specific point version of pyodesys (for e.g. reproducibility)you can get per-version DOIs from the zenodo archive:.. image:: https://zenodo.org/badge/43131469.svg   :target: https://zenodo.org/badge/latestdoi/43131469   :alt: Zenodo DOILicenseing----------The source code is Open Source and is released under the simplified 2-clause BSD license. See `LICENSE &lt;LICENSE&gt;`_ for further details.Contributing------------Contributors are welcome to suggest improvements at https://github.com/bjodah/pyodesys (see further details `here &lt;CONTRIBUTORS.rst&gt;`_).Author------Original author: Björn I. Dahlgren (gmail address: bjodah).See file `AUTHORS &lt;AUTHORS&gt;`_ for a list of all authors.</longdescription>
</pkgmetadata>