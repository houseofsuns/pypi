<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Python Client for Cloud SpannerGA pypi versions Cloud Spanner_ is the worlds first fully managed relational database serviceto offer both strong consistency and horizontal scalability formission-critical online transaction processing (OLTP) applications. With CloudSpanner you enjoy all the traditional benefits of a relational database butunlike any other relational database service, Cloud Spanner scales horizontallyto hundreds or thousands of servers to handle the biggest transactionalworkloads.- Client Library Documentation_- Product Documentation_.. GA image httpsimg.shields.iobadgesupport-GA-gold.svg   target httpsgithub.comgoogleapisgoogle-cloud-pythonblobmainREADME.rstgeneral-availability.. pypi image httpsimg.shields.iopypivgoogle-cloud-spanner.svg   target httpspypi.orgprojectgoogle-cloud-spanner.. versions image httpsimg.shields.iopypipyversionsgoogle-cloud-spanner.svg   target httpspypi.orgprojectgoogle-cloud-spanner.. _Cloud Spanner httpscloud.google.comspanner.. _Client Library Documentation httpscloud.google.compythondocsreferencespannerlatest.. _Product Documentation  httpscloud.google.comspannerdocsQuick Start-----------In order to use this library, you first need to go through the following steps1. Select or create a Cloud Platform project._2. Enable billing for your project._3. Enable the Google Cloud Spanner API._4. Setup Authentication._.. _Select or create a Cloud Platform project. httpsconsole.cloud.google.comproject.. _Enable billing for your project. httpscloud.google.combillingdocshow-tomodify-projectenable_billing_for_a_project.. _Enable the Google Cloud Spanner API.  httpscloud.google.comspanner.. _Setup Authentication. httpsgoogleapis.devpythongoogle-api-corelatestauth.htmlInstallationInstall this library in a virtualenv_ using pip. virtualenv_ is a tool tocreate isolated Python environments. The basic problem it addresses is one ofdependencies and versions, and indirectly permissions.With virtualenv_, its possible to install this library without needing systeminstall permissions, and without clashing with the installed systemdependencies... _virtualenv httpsvirtualenv.pypa.ioenlatestSupported Python VersionsPython  3.7Deprecated Python VersionsPython  2.7.Python  3.5.Python  3.6.MacLinux.. code-block console    pip install virtualenv    virtualenv your-env    source your-envbinactivate    your-envbinpip install google-cloud-spannerWindows.. code-block console    pip install virtualenv    virtualenv your-env    your-envScriptsactivate    your-envScriptspip.exe install google-cloud-spannerExample Usage-------------Executing Arbitrary SQL in a TransactionGenerally, to work with Cloud Spanner, you will want a transaction. Thepreferred mechanism for this is to create a single function, which executesas a callback to database.run_in_transaction.. code python     First, define the function that represents a single unit of work     that should be run within the transaction.    def update_anniversary(transaction, person_id, unix_timestamp)         The query itself is just a string.                 The use of parameters is recommended rather than doing your         own string interpolation this provides protections against         SQL injection attacks.        query  SELECT anniversary FROM people            WHERE id  person_id         When executing the SQL statement, the query and parameters are sent         as separate arguments. When using parameters, you must specify         both the parameters themselves and their types.        row  transaction.execute_sql(            queryquery,            params{person_id person_id},            param_types{                person_id types.INT64_PARAM_TYPE,            },        ).one()         Now perform an update on the data.        old_anniversary  row[0]        new_anniversary  _compute_anniversary(old_anniversary, years)        transaction.update(            people,            [person_id, anniversary],            [person_id, new_anniversary],        )     Actually run the update_anniversary function in a transaction.    database.run_in_transaction(update_anniversary,        person_id42,        unix_timestamp1335020400,    )Select records using a TransactionOnce you have a transaction object (such as the first argument sent torun_in_transaction), reading data is easy.. code python     Define a SELECT query.    query  SELECT e.first_name, e.last_name, p.telephone        FROM employees as e, phones as p        WHERE p.employee_id  e.employee_id     Execute the query and return results.    result  transaction.execute_sql(query)    for row in result.rows        print(row)Insert records using Data Manipulation Language (DML) with a TransactionUse the execute_update() method to execute a DML statement.. code python    spanner_client  spanner.Client()    instance  spanner_client.instance(instance_id)    database  instance.database(database_id)    def insert_singers(transaction)        row_ct  transaction.execute_update(            INSERT Singers (SingerId, FirstName, LastName)              VALUES (10, Virginia, Watson)        )        print({} record(s) inserted..format(row_ct))    database.run_in_transaction(insert_singers)Insert records using Mutations with a TransactionTo add one or more records to a table, use insert.. code python    transaction.insert(        citizens,        columns[email, first_name, last_name, age],        values[            [phredexammple.com, Phred, Phlyntstone, 32],            [bharneyexample.com, Bharney, Rhubble, 31],        ],    )Update records using Data Manipulation Language (DML) with a Transaction.. code python    spanner_client  spanner.Client()    instance  spanner_client.instance(instance_id)    database  instance.database(database_id)    def update_albums(transaction)        row_ct  transaction.execute_update(            UPDATE Albums             SET MarketingBudget  MarketingBudget  2             WHERE SingerId  1 and AlbumId  1        )        print({} record(s) updated..format(row_ct))    database.run_in_transaction(update_albums)Update records using Mutations with a TransactionTransaction.update updates one or more existing records in a table.  Failsif any of the records does not already exist... code python    transaction.update(        citizens,        columns[email, age],        values[            [phredexammple.com, 33],            [bharneyexample.com, 32],        ],    )Connection API--------------Connection API represents a wrap-around for Python Spanner API, written in accordance with PEP-249, and provides a simple way of communication with a Spanner database through connection objects.. code python   from google.cloud.spanner_dbapi.connection import connect   connection  connect(instance-id, database-id)   connection.autocommit  True   cursor  connection.cursor()      cursor.execute(SELECT  FROM table_name)   result  cursor.fetchall()Aborted Transactions Retry MechanismIn autocommit mode, transactions can be aborted due to transient errors. In most cases retry of an aborted transaction solves the problem. To simplify it, connection tracks SQL statements, executed in the current transaction. In case the transaction aborted, the connection initiates a new one and re-executes all the statements. In the process, the connection checks that retried statements are returning the same results that the original statements did. If results are different, the transaction is dropped, as the underlying data changed, and auto retry is impossible.Auto-retry of aborted transactions is enabled only for autocommit mode, as in autocommit mode transactions are never aborted.Next Steps- See the Client Library Documentation_ to learn how to connect to Cloud  Spanner using this Client Library.- Read the Product documentation_ to learn  more about the product and see How-to Guides.</longdescription>
</pkgmetadata>