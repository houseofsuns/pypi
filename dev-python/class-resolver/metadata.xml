<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>--p aligncenter  img srcdocssourcelogo.png height150p--h1 aligncenter  Class Resolverh1p aligncenter    a hrefhttpsgithub.comcthoytclass-resolveractionsqueryworkflow3ATests        img altTests srchttpsgithub.comcthoytclass-resolverworkflowsTestsbadge.svg     a    a hrefhttpsgithub.comcthoytcookiecutter-python-package        img altCookiecutter template from cthoyt srchttpsimg.shields.iobadgeCookiecutter-snekpack-blue      a    a hrefhttpspypi.orgprojectclass_resolver        img altPyPI srchttpsimg.shields.iopypivclass_resolver     a    a hrefhttpspypi.orgprojectclass_resolver        img altPyPI - Python Version srchttpsimg.shields.iopypipyversionsclass_resolver     a    a hrefhttpsgithub.comcthoytclass-resolverblobmainLICENSE        img altPyPI - License srchttpsimg.shields.iopypilclass-resolver     a    a hrefhttpsclass_resolver.readthedocs.ioenlatestbadgelatest        img srchttpsreadthedocs.orgprojectsclass_resolverbadgeversionlatest altDocumentation Status     a    a hrefhttpscodecov.ioghcthoytclass-resolverbranchmain        img srchttpscodecov.ioghcthoytclass-resolverbranchmaingraphbadge.svg altCodecov status     a      a hrefhttpszenodo.orgbadgelatestdoi343741010        img srchttpszenodo.orgbadge343741010.svg altDOI    a    a hrefhttpsgithub.compsfblack        img srchttpsimg.shields.iobadgecode20style-black-000000.svg altCode style black     apLookup and instantiate classes with style.  Getting Startedpythonfrom class_resolver import ClassResolverfrom dataclasses import dataclassclass Base passdataclassclass A(Base)   name strdataclassclass B(Base)   name str Indexresolver  ClassResolver([A, B], baseBase) Lookupassert A  resolver.lookup(A) Instantiate with a dictionaryassert A(namehi)  resolver.make(A, {name hi}) Instantiate with kwargsassert A(namehi)  resolver.make(A, namehi) A pre-instantiated class will simply be passed throughassert A(namehi)  resolver.make(A(namehi))  Writing Extensible Machine Learning Models with class-resolverAssume youve implemented a simple multi-layer perceptron in PyTorchpythonfrom itertools import chainfrom more_itertools import pairwisefrom torch import nnclass MLP(nn.Sequential)    def __init__(self, dims list[int])        super().__init__(chain.from_iterable(            (                nn.Linear(in_features, out_features),                nn.ReLU(),            )            for in_features, out_features in pairwise(dims)        ))This MLP uses a hard-coded rectified linear unit as the non-linear activationfunction between layers. We can generalize this MLP to use a variety ofnon-linear activation functions by adding an argument to its__init__() function like inpythonfrom itertools import chainfrom more_itertools import pairwisefrom torch import nnclass MLP(nn.Sequential)    def __init__(self, dims list[int], activation str  relu)        if activation  relu            activation  nn.ReLU()        elif activation  tanh            activation  nn.Tanh()        elif activation  hardtanh            activation  nn.Hardtanh()        else            raise KeyError(fUnsupported activation {activation})        super().__init__(chain.from_iterable(            (                nn.Linear(in_features, out_features),                activation,            )            for in_features, out_features in pairwise(dims)        ))The first issue with this implementation is it relies on a hard-coded set ofconditional statements and is therefore hard to extend. It can be improvedby using a dictionary lookuppythonfrom itertools import chainfrom more_itertools import pairwisefrom torch import nnactivation_lookup dict[str, nn.Module]  {   relu nn.ReLU(),   tanh nn.Tanh(),   hardtanh nn.Hardtanh(),}class MLP(nn.Sequential)    def __init__(self, dims list[int], activation str  relu)        activation  activation_lookup[activation]        super().__init__(chain.from_iterable(            (                nn.Linear(in_features, out_features),                activation,            )            for in_features, out_features in pairwise(dims)        ))This approach is rigid because it requires pre-instantiation of the activations.If we needed to vary the arguments to the nn.HardTanh class, the previousapproach wouldnt work. We can change the implementation to lookup on the class before instantiation then optionally pass some argumentspythonfrom itertools import chainfrom more_itertools import pairwisefrom torch import nnactivation_lookup dict[str, type[nn.Module]]  {   relu nn.ReLU,   tanh nn.Tanh,   hardtanh nn.Hardtanh,}class MLP(nn.Sequential)    def __init__(        self,         dims list[int],         activation str  relu,         activation_kwargs None  dict[str, any]  None,    )        activation_cls  activation_lookup[activation]        activation  activation_cls((activation_kwargs or {}))        super().__init__(chain.from_iterable(            (                nn.Linear(in_features, out_features),                activation,            )            for in_features, out_features in pairwise(dims)        ))This is pretty good, but it still has a few issues1. you have to manually maintain the activation_lookup dictionary,2. you cant pass an instance or class through the activation keyword3. you have to get the casing just right4. the default is hard-coded as a string, which means this has to get copied   (error-prone) in any place that creates an MLP5. you have to re-write this logic for all of your classesEnter the class_resolver package, which takes care of all of thesethings using the followingpythonfrom itertools import chainfrom class_resolver import ClassResolver, Hintfrom more_itertools import pairwisefrom torch import nnactivation_resolver  ClassResolver(    [nn.ReLU, nn.Tanh, nn.Hardtanh],    basenn.Module,    defaultnn.ReLU,)class MLP(nn.Sequential)    def __init__(        self,         dims list[int],         activation Hint[nn.Module]  None,   Hint  Union[None, str, nn.Module, type[nn.Module]]        activation_kwargs None  dict[str, any]  None,    )        super().__init__(chain.from_iterable(            (                nn.Linear(in_features, out_features),                activation_resolver.make(activation, activation_kwargs),            )            for in_features, out_features in pairwise(dims)        ))Because this is such a common pattern, weve made it available through contribmodule in class_resolver.contrib.torchpythonfrom itertools import chainfrom class_resolver import Hintfrom class_resolver.contrib.torch import activation_resolverfrom more_itertools import pairwisefrom torch import nnclass MLP(nn.Sequential)    def __init__(        self,         dims list[int],         activation Hint[nn.Module]  None,        activation_kwargs None  dict[str, any]  None,    )        super().__init__(chain.from_iterable(            (                nn.Linear(in_features, out_features),                activation_resolver.make(activation, activation_kwargs),            )            for in_features, out_features in pairwise(dims)        ))Now, you can instantiate the MLP with any of the followingpythonMLP(dims[10, 200, 40])   uses default, which is ReLUMLP(dims[10, 200, 40], activationrelu)   uses lowercaseMLP(dims[10, 200, 40], activationReLU)   uses stylizedMLP(dims[10, 200, 40], activationnn.ReLU)   uses classMLP(dims[10, 200, 40], activationnn.ReLU())   uses instanceMLP(dims[10, 200, 40], activationhardtanh, activation_kwargs{min_val 0.0, max_value 6.0})   uses kwargsMLP(dims[10, 200, 40], activationnn.HardTanh, activation_kwargs{min_val 0.0, max_value 6.0})   uses kwargsMLP(dims[10, 200, 40], activationnn.HardTanh(0.0, 6.0))   uses instanceIn practice, it makes sense to stick to using the strings in combination withhyper-parameter optimization libraries like [Optuna](httpsoptuna.org).  InstallationThe most recent release can be installed from[PyPI](httpspypi.orgprojectclass_resolver) withbash pip install class_resolverThe most recent code and data can be installed directly from GitHub withbash pip install git+httpsgithub.comcthoytclass-resolver.gitTo install in development mode, use the followingbash git clone git+httpsgithub.comcthoytclass-resolver.git cd class-resolver pip install -e .  ContributingContributions, whether filing an issue, making a pull request, or forking, are appreciated. See[CONTRIBUTING.rst](httpsgithub.comcthoytclass-resolverblobmasterCONTRIBUTING.rst) for moreinformation on getting involved.  Attribution  LicenseThe code in this package is licensed under the MIT License.  CookiecutterThis package was created with [audreyfeldroy](httpsgithub.comaudreyfeldroy)s[cookiecutter](httpsgithub.comcookiecuttercookiecutter) package using [cthoyt](httpsgithub.comcthoyt)s[cookiecutter-snekpack](httpsgithub.comcthoytcookiecutter-snekpack) template.  For Developersdetails  summarySee developer instructionssummaryThe final section of the README is for if you want to get involved by making a code contribution.  TestingAfter cloning the repository and installing tox with pip install tox, the unit tests in the tests folder can berun reproducibly withshell toxAdditionally, these tests are automatically re-run with each commit in a [GitHub Action](httpsgithub.com{{cookiecutter.github_organization_name}}{{cookiecutter.github_repository_name}}actionsqueryworkflow3ATests).  Making a ReleaseAfter installing the package in development mode and installingtox with pip install tox, the commands for making a new release are contained within the finish environmentin tox.ini. Run the following from the shellshell tox -e finishThis script does the following1. Uses BumpVersion to switch the version number in the setup.cfg and   src{{cookiecutter.package_name}}version.py to not have the -dev suffix2. Packages the code in both a tar archive and a wheel3. Uploads to PyPI using twine. Be sure to have a .pypirc file configured to avoid the need for manual input at this   step4. Push to GitHub. Youll need to make a release going with the commit where the version was bumped.5. Bump the version to the next patch. If you made big changes and want to bump the version by minor, you can   use tox -e bumpversion minor after.details</longdescription>
</pkgmetadata>