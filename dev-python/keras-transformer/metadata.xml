<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Keras Transformer[[Version](httpsimg.shields.iopypivkeras-transformer.svg)](httpspypi.orgprojectkeras-transformer)[License](httpsimg.shields.iopypilkeras-transformer.svg) [[](httpsgithub.comCyberZHGkeras-transformerblobmasterREADME.zh-CN.md)[English](httpsgithub.comCyberZHGkeras-transformerblobmasterREADME.md)]Implementation of [transformer](httpsarxiv.orgpdf1706.03762.pdf) for seq2seq tasks. Installbashpip install keras-transformer Usage Trainpythonimport numpy as npfrom keras_transformer import get_model Build a small toy token dictionarytokens  all work and no play makes jack a dull boy.split( )token_dict  {    PAD 0,    START 1,    END 2,}for token in tokens    if token not in token_dict        token_dict[token]  len(token_dict) Generate toy dataencoder_inputs_no_padding  []encoder_inputs, decoder_inputs, decoder_outputs  [], [], []for i in range(1, len(tokens) - 1)    encode_tokens, decode_tokens  tokens[i], tokens[i]    encode_tokens  [START] + encode_tokens + [END] + [PAD]  (len(tokens) - len(encode_tokens))    output_tokens  decode_tokens + [END, PAD] + [PAD]  (len(tokens) - len(decode_tokens))    decode_tokens  [START] + decode_tokens + [END] + [PAD]  (len(tokens) - len(decode_tokens))    encode_tokens  list(map(lambda x token_dict[x], encode_tokens))    decode_tokens  list(map(lambda x token_dict[x], decode_tokens))    output_tokens  list(map(lambda x [token_dict[x]], output_tokens))    encoder_inputs_no_padding.append(encode_tokens[i + 2])    encoder_inputs.append(encode_tokens)    decoder_inputs.append(decode_tokens)    decoder_outputs.append(output_tokens) Build the modelmodel  get_model(    token_numlen(token_dict),    embed_dim30,    encoder_num3,    decoder_num2,    head_num3,    hidden_dim120,    attention_activationrelu,    feed_forward_activationrelu,    dropout_rate0.05,    embed_weightsnp.random.random((13, 30)),)model.compile(    optimizeradam,    losssparse_categorical_crossentropy,)model.summary() Train the modelmodel.fit(    x[np.asarray(encoder_inputs  1000), np.asarray(decoder_inputs  1000)],    ynp.asarray(decoder_outputs  1000),    epochs5,) Predictpythonfrom keras_transformer import decodedecoded  decode(    model,    encoder_inputs_no_padding,    start_tokentoken_dict[START],    end_tokentoken_dict[END],    pad_tokentoken_dict[PAD],    max_len100,)token_dict_rev  {v k for k, v in token_dict.items()}for i in range(len(decoded))    print( .join(map(lambda x token_dict_rev[x], decoded[i][1-1]))) Translationpythonimport numpy as npfrom keras_transformer import get_model, decodesource_tokens  [    i need more power.split( ),    eat jujube and pill.split( ),]target_tokens  [    list(),    list(),] Generate dictionariesdef build_token_dict(token_list)    token_dict  {        PAD 0,        START 1,        END 2,    }    for tokens in token_list        for token in tokens            if token not in token_dict                token_dict[token]  len(token_dict)    return token_dictsource_token_dict  build_token_dict(source_tokens)target_token_dict  build_token_dict(target_tokens)target_token_dict_inv  {v k for k, v in target_token_dict.items()} Add special tokensencode_tokens  [[START] + tokens + [END] for tokens in source_tokens]decode_tokens  [[START] + tokens + [END] for tokens in target_tokens]output_tokens  [tokens + [END, PAD] for tokens in target_tokens] Paddingsource_max_len  max(map(len, encode_tokens))target_max_len  max(map(len, decode_tokens))encode_tokens  [tokens + [PAD]  (source_max_len - len(tokens)) for tokens in encode_tokens]decode_tokens  [tokens + [PAD]  (target_max_len - len(tokens)) for tokens in decode_tokens]output_tokens  [tokens + [PAD]  (target_max_len - len(tokens)) for tokens in output_tokens]encode_input  [list(map(lambda x source_token_dict[x], tokens)) for tokens in encode_tokens]decode_input  [list(map(lambda x target_token_dict[x], tokens)) for tokens in decode_tokens]decode_output  [list(map(lambda x [target_token_dict[x]], tokens)) for tokens in output_tokens] Build  fit modelmodel  get_model(    token_nummax(len(source_token_dict), len(target_token_dict)),    embed_dim32,    encoder_num2,    decoder_num2,    head_num4,    hidden_dim128,    dropout_rate0.05,    use_same_embedFalse,   Use different embeddings for different languages)model.compile(adam, sparse_categorical_crossentropy)model.summary()model.fit(    x[np.array(encode_input  1024), np.array(decode_input  1024)],    ynp.array(decode_output  1024),    epochs10,    batch_size32,) Predictdecoded  decode(    model,    encode_input,    start_tokentarget_token_dict[START],    end_tokentarget_token_dict[END],    pad_tokentarget_token_dict[PAD],)print(.join(map(lambda x target_token_dict_inv[x], decoded[0][1-1])))print(.join(map(lambda x target_token_dict_inv[x], decoded[1][1-1]))) DecodeIn decode, the word with top probability is selected as the predicted token by default. You can add randomness by setting top_k and temperaturepythondecoded  decode(    model,    encode_input,    start_tokentarget_token_dict[START],    end_tokentarget_token_dict[END],    pad_tokentarget_token_dict[PAD],    top_k10,    temperature1.0,)print(.join(map(lambda x target_token_dict_inv[x], decoded[0][1-1])))print(.join(map(lambda x target_token_dict_inv[x], decoded[1][1-1])))</longdescription>
</pkgmetadata>