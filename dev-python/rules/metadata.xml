<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>rulesrules is a tiny but powerful app providing object-level permissions toDjango, without requiring a database. At its core, it is a generic frameworkfor building rule-based systems, similar to decision trees_. It can also beused as a standalone library in other contexts and frameworks... image httpsimg.shields.iogithubworkflowstatusdfuncktdjango-rulesCImaster    target httpsgithub.comdfuncktdjango-rulesactions.. image httpscoveralls.ioreposdfuncktdjango-rulesbadge.svg    target httpscoveralls.iordfuncktdjango-rules.. image httpsimg.shields.iopypivrules.svg    target httpspypi.orgprojectrules.. image httpsimg.shields.iopypipyversionsrules.svg    target httpspypi.orgprojectrules.. image httpsimg.shields.iobadgecode20style-black-000000.svg    target httpsgithub.compsfblack.. image httpsimg.shields.iobadgepre--commit-enabled-brightgreenlogopre-commitlogoColorwhite    target httpsgithub.compre-commitpre-commit.. _decision trees httpwikipedia.orgwikiDecision_treeFeaturesrules has got you covered. rules is-   Documented, tested, reliable and easy to use.-   Versatile. Decorate callables to build complex graphs of predicates.    Predicates can be any type of callable -- simple functions, lambdas,    methods, callable class objects, partial functions, decorated functions,    anything really.-   A good Django citizen. Seamless integration with Django views,    templates and the Admin for testing for object-level permissions.-   Efficient and smart. No need to mess around with a database to figure    out whether John really wrote that book.-   Simple. Dive in the code. Youll need 10 minutes to figure out how it    works.-   Powerful. rules comes complete with advanced features, such as    invocation context and storage for arbitrary data, skipping evaluation of    predicates under specific conditions, logging of evaluated predicates and moreTable of Contents- Requirements_- Upgrading from 2.x_- Upgrading from 1.x_- How to install_  - Configuring Django_- Using Rules_  - Creating predicates_  - Setting up rules_  - Combining predicates_- Using Rules with Django_  - Permissions_  - Permissions in models_  - Permissions in views_  - Permissions and rules in templates_  - Permissions in the Admin_  - Permissions in Django Rest Framework_- Advanced features_  - Custom rule sets_  - Invocation context_  - Binding self_  - Skipping predicates_  - Logging predicate evaluation_- Best practices_- API Reference_- Licence_Requirementsrules requires Python 3.7 or newer. The last version to support Python 2.7is rules 2.2. It can optionally integrate with Django, in which caserequires Django 2.2 or newer.Note At any given moment in time, rules will maintain support for allcurrently supported Django versions, while dropping support for those versionsthat reached end-of-life in minor releases. See the Supported Versions_section on Django Project website for the current state and timeline... _Supported Versions httpswww.djangoproject.comdownloadsupported-versionsUpgrading from 2.xThe are no significant changes between rules 2.x and 3.x except droppingsupport for Python 2, so before upgrading to 3.x you just need to make sureyoure running a supported Python 3 version.Upgrading from 1.x   Support for Python 2.6 and 3.3, and Django versions before 1.11 has been    dropped.   The SkipPredicate exception and skip() method of Predicate,    that were used to signify that a predicate should be skipped, have been    removed. You may return None from your predicate to achieve this.   The APIs to replace a rules predicate have been renamed and their    behaviour changed. replace_rule and replace_perm functions and    replace_rule method of RuleSet have been renamed to set_rule,    set_perm and RuleSet.set_perm respectively. The old behaviour was    to raise a KeyError if a rule by the given name did not exist. Since    version 2.0 this has changed and you can safely use set_ to set a    rules predicate without having to ensure the rule exists first.How to installUsing pip.. code bash     pip install rulesManually.. code bash     git clone httpsgithub.comdfuncktdjango-rules.git     cd django-rules     python setup.py installRun tests with.. code bash     .runtests.shYou may also want to read Best practices_ for general advice on how touse rules.Configuring Django------------------Add rules to INSTALLED_APPS.. code python    INSTALLED_APPS  (         ...        rules,    )Add the authentication backend.. code python    AUTHENTICATION_BACKENDS  (        rules.permissions.ObjectPermissionBackend,        django.contrib.auth.backends.ModelBackend,    )Using Rulesrules is based on the idea that you maintain a dict-like object that mapsstring keys used as identifiers of some kind, to callables, calledpredicates. This dict-like object is actually an instance of RuleSet andthe predicates are instances of Predicate.Creating predicates-------------------Lets ignore rule sets for a moment and go ahead and define a predicate. Theeasiest way is with the predicate decorator.. code python     rules.predicate     def is_book_author(user, book)    ...     return book.author  user    ...     is_book_author    Predicateis_book_author object at 0x10eeaa490This predicate will return True if the books author is the given user,False otherwise.Predicates can be created from any callable that accepts anything from zero totwo positional arguments   fn(obj, target)   fn(obj)   fn()This is their generic form. If seen from the perspective of authorization inDjango, the equivalent signatures are   fn(user, obj)   fn(user)   fn()Predicates can do pretty much anything with the given arguments, but mustalways return True if the condition they check is true, Falseotherwise. rules comes with several predefined predicates that you mayread about later on in API Reference_, that are mostly useful when dealingwith authorization in Django_.Setting up rules----------------Lets pretend that we want to let authors edit or delete their books, but notbooks written by other authors. So, essentially, what determines whether anauthor can edit or can delete a given book is whether they are itsauthor.In rules, such requirements are modelled as rules. A rule is a map ofa unique identifier (eg. can edit) to a predicate. Rules are groupedtogether into a rule set. rules has two predefined rule sets   A default rule set storing shared rules.   Another rule set storing rules that serve as permissions in a Django    context.So, lets define our first couple of rules, adding them to the shared ruleset. We can use the is_book_author predicate we defined earlier.. code python     rules.add_rule(can_edit_book, is_book_author)     rules.add_rule(can_delete_book, is_book_author)Assuming weve got some data, we can now test our rules.. code python     from django.contrib.auth.models import User     from books.models import Book     guidetodjango  Book.objects.get(isbn978-1-4302-1936-1)     guidetodjango.author    User adrian     adrian  User.objects.get(usernameadrian)     rules.test_rule(can_edit_book, adrian, guidetodjango)    True     rules.test_rule(can_delete_book, adrian, guidetodjango)    TrueNice... but not awesome.Combining predicates--------------------Predicates by themselves are not so useful -- not more useful than any otherfunction would be. Predicates, however, can be combined using binary operatorsto create more complex ones. Predicates support the following operators   P1  P2 Returns a new predicate that returns True if both    predicates return True, otherwise False. If P1 returns False,    P2 will not be evaluated.   P1  P2 Returns a new predicate that returns True if any of the    predicates returns True, otherwise False. If P1 returns True,    P2 will not be evaluated.   P1  P2 Returns a new predicate that returns True if one of the    predicates returns True and the other returns False, otherwise    False.   P Returns a new predicate that returns the negated result of the    original predicate.Suppose the requirement for allowing a user to edit a given book was for themto be either the books author, or a member of the editors group. Allowingusers to delete a book should still be determined by whether the user is thebooks author.With rules thats easy to implement. Wed have to define anotherpredicate, that would return True if the given user is a member of theeditors group, False otherwise. The built-in is_group_member factorywill come in handy.. code python     is_editor  rules.is_group_member(editors)     is_editor    Predicateis_group_membereditors object at 0x10eee1350We could combine it with the is_book_author predicate to create a new onethat checks for either condition.. code python     is_book_author_or_editor  is_book_author  is_editor     is_book_author_or_editor    Predicate(is_book_author  is_group_membereditors) object at 0x10eee1390We can now update our can_edit_book rule.. code python     rules.set_rule(can_edit_book, is_book_author_or_editor)     rules.test_rule(can_edit_book, adrian, guidetodjango)    True     rules.test_rule(can_delete_book, adrian, guidetodjango)    TrueLets see what happens with another user.. code python     martin  User.objects.get(usernamemartin)     list(martin.groups.values_list(name, flatTrue))    [editors]     rules.test_rule(can_edit_book, martin, guidetodjango)    True     rules.test_rule(can_delete_book, martin, guidetodjango)    FalseAwesome.So far, weve only used the underlying, generic framework for defining andtesting rules. This layer is not at all specific to Django it may be used inany context. Theres actually no import of anything Django-related in thewhole app (except in the rules.templatetags module). rules however canintegrate tightly with Django to provide authorization... _authorization in DjangoUsing Rules with Djangorules is able to provide object-level permissions in Django. It comeswith an authorization backend and a couple template tags for use in yourtemplates.Permissions-----------In rules, permissions are a specialised type of rules. You still definerules by creating and combining predicates. These rules however, must be addedto a permissions-specific rule set that comes with rules so that they canbe picked up by the rules authorization backend.Creating permissions++++++++++++++++++++The convention for naming permissions in Django is app_label.action_object,and we like to adhere to that. Lets add rules for the books.change_bookand books.delete_book permissions.. code python     rules.add_perm(books.change_book, is_book_author  is_editor)     rules.add_perm(books.delete_book, is_book_author)See the difference in the API add_perm adds to a permissions-specificrule set, whereas add_rule adds to a default shared rule set. Itsimportant to know however, that these two rule sets are separate, meaning thatadding a rule in one does not make it available to the other.Checking for permission+++++++++++++++++++++++Lets go ahead and check whether adrian has change permission to theguidetodjango book.. code python     adrian.has_perm(books.change_book, guidetodjango)    FalseWhen you call the User.has_perm method, Django asks each backend insettings.AUTHENTICATION_BACKENDS whether a user has the given permissionfor the object. When queried for object permissions, Djangos defaultauthentication backend always returns False. rules comes with anauthorization backend, that is able to provide object-level permissions bylooking into the permissions-specific rule set.Lets add the rules authorization backend in settings.. code python    AUTHENTICATION_BACKENDS  (        rules.permissions.ObjectPermissionBackend,        django.contrib.auth.backends.ModelBackend,    )Now, checking again gives adrian the required permissions.. code python     adrian.has_perm(books.change_book, guidetodjango)    True     adrian.has_perm(books.delete_book, guidetodjango)    True     martin.has_perm(books.change_book, guidetodjango)    True     martin.has_perm(books.delete_book, guidetodjango)    FalseNOTE Calling has_perm on a superuser will ALWAYS return True.Permissions in models---------------------NOTE The features described in this section work on Python 3+ only.It is common to have a set of permissions for a model, like what Django offers withits default model permissions (such as add, change etc.). When using rulesas the permission checking backend, you can declare object-level permissions forany model in a similar way, using a new Meta option.First, you need to switch your models base and metaclass to the slightly extendedversions provided in rules.contrib.models. There are several classes and mixinsyou can use, depending on whether youre already using a custom base andor metaclassfor your models or not. The extensions are very slim and dont affect the modelsbehavior in any way other than making it register permissions. If youre using the stock django.db.models.Model as base for your models,  simply switch over to RulesModel and youre good to go. If you already have a custom base class adding common functionality to your models,  add RulesModelMixin to the classes it inherits from and set RulesModelBase  as its metaclass, like so      from django.db.models import Model      from rules.contrib.models import RulesModelBase, RulesModelMixin      class MyModel(RulesModelMixin, Model, metaclassRulesModelBase)          ... If youre using a custom metaclass for your models, youll already know how to  make it inherit from RulesModelBaseMixin yourself.Then, create your models like so, assuming youre using RulesModel as basedirectly    import rules    from rules.contrib.models import RulesModel    class Book(RulesModel)        class Meta            rules_permissions  {                add rules.is_staff,                read rules.is_authenticated,            }This would be equivalent to the following calls    rules.add_perm(app_label.add_book, rules.is_staff)    rules.add_perm(app_label.read_book, rules.is_authenticated)There are methods in RulesModelMixin that you can overwrite in order to customizehow a models permissions are registered. See the documented source code for detailsif you need this.Of special interest is the get_perm classmethod of RulesModelMixin, which canbe used to convert a permission type to the corresponding full permission name. Ifyou need to query for some type of permission on a given model programmatically,this is handy    if user.has_perm(Book.get_perm(read))        ...Permissions in views--------------------rules comes with a set of view decorators to help you enforceauthorization in your views.Using the function-based view decorator+++++++++++++++++++++++++++++++++++++++For function-based views you can use the permission_required decorator.. code python    from django.shortcuts import get_object_or_404    from rules.contrib.views import permission_required    from posts.models import Post    def get_post_by_pk(request, post_id)        return get_object_or_404(Post, pkpost_id)    permission_required(posts.change_post, fnget_post_by_pk)    def post_update(request, post_id)         ...Usage is straight-forward, but theres one thing in the example above thatstands out and this is the get_post_by_pk function. This function, giventhe current request and all arguments passed to the view, is responsible forfetching and returning the object to check permissions against -- i.e. thePost instance with PK equal to the given post_id in the example.This specific use-case is quite common so, to save you some typing, rulescomes with a generic helper function that you can use to do this declaratively.The example below is equivalent to the one above.. code python    from rules.contrib.views import permission_required, objectgetter    from posts.models import Post    permission_required(posts.change_post, fnobjectgetter(Post, post_id))    def post_update(request, post_id)         ...For more information on the decorator and helper function, refer to therules.contrib.views module.Using the class-based view mixin++++++++++++++++++++++++++++++++Django includes a set of access mixins that you can use in your class-basedviews to enforce authorization. rules extends this framework to provideobject-level permissions via a mixin, PermissionRequiredMixin.The following example will automatically test for permission against theinstance returned by the views get_object method.. code python    from django.views.generic.edit import UpdateView    from rules.contrib.views import PermissionRequiredMixin    from posts.models import Post    class PostUpdate(PermissionRequiredMixin, UpdateView)        model  Post        permission_required  posts.change_postYou can customise the object either by overriding get_object orget_permission_object.For more information refer to the Django documentation_ and therules.contrib.views module... _Django documentation httpsdocs.djangoproject.comen1.9topicsauthdefaultlimiting-access-to-logged-in-usersChecking permission automatically based on view type++++++++++++++++++++++++++++++++++++++++++++++++++++If you use the mechanisms provided by rules.contrib.models to register permissionsfor your models as described in Permissions in models_, theres another convenientmixin for class-based views available for you.rules.contrib.views.AutoPermissionRequiredMixin can recognize the type of viewits used with and check for the corresponding permission automatically.This example view would, without any further configuration, automatically check forthe posts.change_post permission, given that the app label is posts    from django.views.generic import UpdateView    from rules.contrib.views import AutoPermissionRequiredMixin    from posts.models import Post    class UpdatePostView(AutoPermissionRequiredMixin, UpdateView)        model  PostBy default, the generic CRUD views from django.views.generic are mapped to thenative Django permission types (add, change, delete and view). However,the pre-defined mappings can be extended, changed or replaced altogether whensubclassing AutoPermissionRequiredMixin. See the fully documented source codefor details on how to do that properly.Permissions and rules in templates----------------------------------rules comes with two template tags to allow you to test for rules andpermissions in templates.Add rules to your INSTALLED_APPS.. code python    INSTALLED_APPS  (         ...        rules,    )Then, in your template    { load rules }    { has_perm books.change_book author book as can_edit_book }    { if can_edit_book }        ...    { endif }    { test_rule has_super_feature user as has_super_feature }    { if has_super_feature }        ...    { endif }Permissions in the Admin------------------------If youve setup rules to be used with permissions in Django, youre almostset to also use rules to authorize any addchangedelete actions in theAdmin. The Admin asks for four different permissions, depending on action- app_label.add_modelname- app_label.view_modelname- app_label.change_modelname- app_label.delete_modelname- app_labelNote view permission is new in Django v2.1 and should not be added in versions before that.The first four are obvious. The fifth is the required permission for an appto be displayed in the Admins dashboard. Overriding it does not restrict access to the add,change or delete views. Heres some rules for our imaginary books app as an example.. code python     rules.add_perm(books, rules.always_allow)     rules.add_perm(books.add_book, is_staff)     rules.add_perm(books.view_book, is_staff  has_secret_access_code)     rules.add_perm(books.change_book, is_staff)     rules.add_perm(books.delete_book, is_staff)Django Admin does not support object-permissions, in the sense that it willnever ask for permission to perform an action on an object, only whether auser is allowed to act on (any) instances of a model.If youd like to tell Django whether a user has permissions on a specificobject, youd have to override the following methods of a modelsModelAdmin- has_view_permission(user, objNone)- has_change_permission(user, objNone)- has_delete_permission(user, objNone)rules comes with a custom ModelAdmin subclass,rules.contrib.admin.ObjectPermissionsModelAdmin, that overrides thesemethods to pass on the edited model instance to the authorization backends,thus enabling permissions per object in the Admin.. code python     booksadmin.py    from django.contrib import admin    from rules.contrib.admin import ObjectPermissionsModelAdmin    from .models import Book    class BookAdmin(ObjectPermissionsModelAdmin)        pass    admin.site.register(Book, BookAdmin)Now this allows you to specify permissions like this.. code python     rules.add_perm(books, rules.always_allow)     rules.add_perm(books.add_book, has_author_profile)     rules.add_perm(books.change_book, is_book_author_or_editor)     rules.add_perm(books.delete_book, is_book_author)To preserve backwards compatibility, Django will ask for either view orchange permission. For maximum flexibility, rules behaves subtlydifferent rules will ask for the change permission if and only if no ruleexists for the view permission.Permissions in Django Rest Framework------------------------------------Similar to rules.contrib.views.AutoPermissionRequiredMixin, there is arules.contrib.rest_framework.AutoPermissionViewSetMixin for viewsets in DjangoRest Framework. The difference is that it doesnt derive permission from the typeof view but from the API action (create, retrieve etc.) thats tried to beperformed. Of course, it also requires you to declare your models as described inPermissions in models_.Here is a possible ModelViewSet for the Post model with fully automated CRUDpermission checking    from rest_framework.serializers import ModelSerializer    from rest_framework.viewsets import ModelViewSet    from rules.contrib.rest_framework import AutoPermissionViewSetMixin    from posts.models import Post    class PostSerializer(ModelSerializer)        class Meta            model  Post            fields  __all__    class PostViewSet(AutoPermissionViewSetMixin, ModelViewSet)        queryset  Post.objects.all()        serializer_class  PostSerializerBy default, the CRUD actions of ModelViewSet are mapped to the nativeDjango permission types (add, change, delete and view). The listaction has no permission checking enabled. However, the pre-defined mappingscan be extended, changed or replaced altogether when using (or subclassing)AutoPermissionViewSetMixin. Custom API actions defined via the actiondecorator may then be mapped as well. See the fully documented source code fordetails on how to properly customize the default behavior.Advanced featuresCustom rule sets----------------You may create as many rule sets as you need.. code python     features  rules.RuleSet()And manipulate them by adding, removing, querying and testing rules.. code python     features.rule_exists(has_super_feature)    False     is_special_user  rules.is_group_member(special)     features.add_rule(has_super_feature, is_special_user)     has_super_feature in features    True     features[has_super_feature]    Predicateis_group_memberspecial object at 0x10eeaa500     features.test_rule(has_super_feature, adrian)    True     features.remove_rule(has_super_feature)Note however that custom rule sets are not available in Django templates --you need to provide integration yourself.Invocation context------------------A new context is created as a result of invoking Predicate.test() and isonly valid for the duration of the invocation. A context is a simple dictthat you can use to store arbitrary data, (eg. caching computed values,setting flags, etc.), that can be used by predicates later on in the chain.Inside a predicate function it can be used like so.. code python     predicate    ... def mypred(a, b)    ...     value  compute_expensive_value(a)    ...     mypred.context[value]  value    ...     return TrueOther predicates can later use stored values.. code python     predicate    ... def myotherpred(a, b)    ...     value  myotherpred.context.get(value)    ...     if value is not None    ...         return do_something_with_value(value)    ...     else    ...         return do_something_without_value()Predicate.context provides a single args attribute that contains thearguments as given to test() at the beginning of the invocation.Binding self--------------In a predicates function body, you can refer to the predicate instance itselfby its name, eg. is_book_author. Passing bindTrue as a keywordargument to the predicate decorator will let you refer to the predicatewith self, which is more convenient. Binding self is just syntacticsugar. As a matter of fact, the following two are equivalent.. code python     predicate    ... def is_book_author(user, book)    ...     if is_book_author.context.args    ...         return user  book.author    ...     return False     predicate(bindTrue)    ... def is_book_author(self, user, book)    ...     if self.context.args    ...         return user  book.author    ...     return FalseSkipping predicates-------------------You may skip evaluation by returning None from your predicate.. code python     predicate(bindTrue)    ... def is_book_author(self, user, book)    ...     if len(self.context.args)  1    ...         return user  book.author    ...     else    ...         return NoneReturning None signifies that the predicate need not be evaluated, thusleaving the predicate result up to that point unchanged.Logging predicate evaluation----------------------------rules can optionally be configured to log debug information as rules areevaluated to help with debugging your predicates. Messages are sent at theDEBUG level to the rules logger. The following dictConfig_ configuresa console logger (place this in your projects settings.py if youre usingrules with Django).. code python    LOGGING  {        version 1,        disable_existing_loggers False,        handlers {            console {                level DEBUG,                class logging.StreamHandler,            },        },        loggers {            rules {                handlers [console],                level DEBUG,                propagate True,            },        },    }When this logger is active each individual predicate will have a log messageprinted when it is evaluated... _dictConfig httpsdocs.python.org3.6librarylogging.config.htmllogging-config-dictschemaBest practicesBefore you can test for rules, these rules must be registered with a rule set,and for this to happen the modules containing your rule definitions must beimported.For complex projects with several predicates and rules, it may not bepractical to define all your predicates and rules inside one module. It mightbe best to split them among any sub-components of your project. In a Djangocontext, these sub-components could be the apps for your project.On the other hand, because importing predicates from all over the place inorder to define rules can lead to circular imports and broken hearts, itsbest to further split predicates and rules in different modules.rules may optionally be configured to autodiscover rules.py modules inyour apps and import them at startup. To have rules do so, just edit yourINSTALLED_APPS setting.. code python    INSTALLED_APPS  (         replace rules with        rules.apps.AutodiscoverRulesConfig,    )Note On Python 2, you must also add the following to the top of yourrules.py file, or youll get import errors trying to import rulesitself.. code python    from __future__ import absolute_importAPI ReferenceThe core APIs are accessible from the root rules module. Django-specificfunctionality for the Admin and views is available from rules.contrib.Class rules.Predicate-------------------------You create Predicate instances by passing in a callable.. code python     def is_book_author(user, book)    ...     return book.author  user    ...     pred  Predicate(is_book_author)     pred    Predicateis_book_author object at 0x10eeaa490You may optionally provide a different name for the predicate that is usedwhen inspecting it.. code python     pred  Predicate(is_book_author, nameanother_name)     pred    Predicateanother_name object at 0x10eeaa490Also, you may optionally provide bindTrue in order to be able to accessthe predicate instance with self.. code python     def is_book_author(self, user, book)    ...     if self.context.args    ...         return user  book.author    ...     return False    ...     pred  Predicate(is_book_author, bindTrue)     pred    Predicateis_book_author object at 0x10eeaa490Instance methods++++++++++++++++test(objNone, targetNone)    Returns the result of calling the passed in callable with zero, one or two    positional arguments, depending on how many it accepts.Class rules.RuleSet-----------------------RuleSet extends Pythons built-in dict_ type. Therefore, you may createand use a rule set any way youd use a dict... _dict httpdocs.python.orglibrarystdtypes.htmlmapping-types-dictInstance methods++++++++++++++++add_rule(name, predicate)    Adds a predicate to the rule set, assigning it to the given rule name.    Raises KeyError if another rule with that name already exists.set_rule(name, predicate)    Set the rule with the given name, regardless if one already exists.remove_rule(name)    Remove the rule with the given name. Raises KeyError if a rule with    that name does not exist.rule_exists(name)    Returns True if a rule with the given name exists, False otherwise.test_rule(name, objNone, targetNone)    Returns the result of calling predicate.test(obj, target) where    predicate is the predicate for the rule with the given name. Returns    False if a rule with the given name does not exist.Decorators----------predicate    Decorator that creates a predicate out of any callable    .. code python         predicate        ... def is_book_author(user, book)        ...     return book.author  user        ...         is_book_author        Predicateis_book_author object at 0x10eeaa490    Customising the predicate name    .. code python         predicate(nameanother_name)        ... def is_book_author(user, book)        ...     return book.author  user        ...         is_book_author        Predicateanother_name object at 0x10eeaa490    Binding self    .. code python         predicate(bindTrue)        ... def is_book_author(self, user, book)        ...     if user_has_special_flag in self.context        ...         return self.context[user_has_special_flag]        ...     return book.author  userPredefined predicates---------------------always_allow(), always_true()    Always returns True.always_deny(), always_false()    Always returns False.is_authenticated(user)    Returns the result of calling user.is_authenticated(). Returns    False if the given user does not have an is_authenticated method.is_superuser(user)    Returns the result of calling user.is_superuser. Returns False    if the given user does not have an is_superuser property.is_staff(user)    Returns the result of calling user.is_staff. Returns False if the    given user does not have an is_staff property.is_active(user)    Returns the result of calling user.is_active. Returns False if the    given user does not have an is_active property.is_group_member(groups)    Factory that creates a new predicate that returns True if the given    user is a member of all the given groups, False otherwise.Shortcuts---------Managing the shared rule set++++++++++++++++++++++++++++add_rule(name, predicate)    Adds a rule to the shared rule set. See RuleSet.add_rule.set_rule(name, predicate)    Set the rule with the given name from the shared rule set. See    RuleSet.set_rule.remove_rule(name)    Remove a rule from the shared rule set. See RuleSet.remove_rule.rule_exists(name)    Returns whether a rule exists in the shared rule set. See    RuleSet.rule_exists.test_rule(name, objNone, targetNone)    Tests the rule with the given name. See RuleSet.test_rule.Managing the permissions rule set+++++++++++++++++++++++++++++++++add_perm(name, predicate)    Adds a rule to the permissions rule set. See RuleSet.add_rule.set_perm(name, predicate)    Replace a rule from the permissions rule set. See RuleSet.set_rule.remove_perm(name)    Remove a rule from the permissions rule set. See RuleSet.remove_rule.perm_exists(name)    Returns whether a rule exists in the permissions rule set. See    RuleSet.rule_exists.has_perm(name, userNone, objNone)    Tests the rule with the given name. See RuleSet.test_rule.Licencedjango-rules is distributed under the MIT licence.Copyright (c) 2014 Akis KesoglouPermission is hereby granted, free of charge, to any personobtaining a copy of this software and associated documentationfiles (the Software), to deal in the Software withoutrestriction, including without limitation the rights to use,copy, modify, merge, publish, distribute, sublicense, andor sellcopies of the Software, and to permit persons to whom theSoftware is furnished to do so, subject to the followingconditionsThe above copyright notice and this permission notice shall beincluded in all copies or substantial portions of the Software.THE SOFTWARE IS PROVIDED AS IS, WITHOUT WARRANTY OF ANY KIND,EXPRESS OR IMPLIED, INCLUDING BUT NOT LIMITED TO THE WARRANTIESOF MERCHANTABILITY, FITNESS FOR A PARTICULAR PURPOSE ANDNONINFRINGEMENT. IN NO EVENT SHALL THE AUTHORS OR COPYRIGHTHOLDERS BE LIABLE FOR ANY CLAIM, DAMAGES OR OTHER LIABILITY,WHETHER IN AN ACTION OF CONTRACT, TORT OR OTHERWISE, ARISINGFROM, OUT OF OR IN CONNECTION WITH THE SOFTWARE OR THE USE OROTHER DEALINGS IN THE SOFTWARE.</longdescription>
</pkgmetadata>