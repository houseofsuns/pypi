<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>LogotqdmPy-Versions Versions Conda-Forge-Status Docker SnapcraftBuild-Status Coverage-Status Branch-Coverage-Status Codacy-Grade Libraries-Rank PyPI-DownloadsLICENCE OpenHub-Status binder-demo awesome-pythontqdm derives from the Arabic word taqaddum () which can mean progress,and is an abbreviation for I love you so much in Spanish (te quiero demasiado).Instantly make your loops show a smart progress meter - just wrap anyiterable with tqdm(iterable), and youre done.. code python    from tqdm import tqdm    for i in tqdm(range(10000))        ...76   756810000 [00330010, 229.00its]trange(N) can be also used as a convenient shortcut fortqdm(range(N)).Screenshot    Video Slides MerchIt can also be executed as a module with pipes.. code sh     seq 9999999  tqdm --bytes  wc -l    75.2MB [0000, 217MBs]    9999999     tar -zcf - docs  tqdm --bytes --total du -sb docs  cut -f1          backup.tgz     32                       8.89G27.9G [00420131, 223MBs]Overhead is low -- about 60ns per iteration (80ns with tqdm.gui), and isunit tested against performance regression.By comparison, the well-establishedProgressBar httpsgithub.comniltonvolpatopython-progressbar__ hasan 800nsiter overhead.In addition to its low overhead, tqdm uses smart algorithms to predictthe remaining time and to skip unnecessary iteration displays, which allowsfor a negligible overhead in most cases.tqdm works on any platform(Linux, Windows, Mac, FreeBSD, NetBSD, SolarisSunOS),in any console or in a GUI, and is also friendly with IPythonJupyter notebooks.tqdm does not require any dependencies (not even curses), justPython and an environment supporting carriage return r andline feed n control characters.------------------------------------------.. contents Table of contents   backlinks top   localInstallation------------Latest PyPI stable releaseVersions PyPI-Downloads Libraries-Dependents.. code sh    pip install tqdmLatest development release on GitHubGitHub-Status GitHub-Stars GitHub-Commits GitHub-Forks GitHub-UpdatedPull and install pre-release devel branch.. code sh    pip install git+httpsgithub.comtqdmtqdm.gitdeveleggtqdmLatest Conda releaseConda-Forge-Status.. code sh    conda install -c conda-forge tqdmLatest Snapcraft releaseSnapcraftThere are 3 channels to choose from.. code sh    snap install tqdm   implies --stable, i.e. latest tagged release    snap install tqdm  --candidate   master branch    snap install tqdm  --edge   devel branchNote that snap binaries are purely for CLI use (not import-able), andautomatically set up bash tab-completion.Latest Docker releaseDocker.. code sh    docker pull tqdmtqdm    docker run -i --rm tqdmtqdm --helpOtherThere are other (unofficial) places where tqdm may be downloaded, particularly for CLI useRepology.. Repology image httpsrepology.orgbadgetiny-repospythontqdm.svg   target httpsrepology.orgprojectpythontqdmversionsChangelog---------The list of all changes is available either on GitHubs ReleasesGitHub-Status, on thewiki httpsgithub.comtqdmtqdmwikiReleases__, or on thewebsite httpstqdm.github.ioreleases__.Usage-----tqdm is very versatile and can be used in a number of ways.The three main ones are given below.Iterable-basedWrap tqdm() around any iterable.. code python    from tqdm import tqdm    from time import sleep    text      for char in tqdm([a, b, c, d])        sleep(0.25)        text  text + chartrange(i) is a special optimised instance of tqdm(range(i)).. code python    from tqdm import trange    for i in trange(100)        sleep(0.01)Instantiation outside of the loop allows for manual control over tqdm().. code python    pbar  tqdm([a, b, c, d])    for char in pbar        sleep(0.25)        pbar.set_description(Processing s  char)ManualManual control of tqdm() updates using a with statement.. code python    with tqdm(total100) as pbar        for i in range(10)            sleep(0.1)            pbar.update(10)If the optional variable total (or an iterable with len()) isprovided, predictive stats are displayed.with is also optional (you can just assign tqdm() to a variable,but in this case dont forget to del or close() at the end.. code python    pbar  tqdm(total100)    for i in range(10)        sleep(0.1)        pbar.update(10)    pbar.close()ModulePerhaps the most wonderful use of tqdm is in a script or on the commandline. Simply inserting tqdm (or python -m tqdm) between pipes will passthrough all stdin to stdout while printing progress to stderr.The example below demonstrate counting the number of lines in all Python filesin the current directory, with timing information included... code sh     time find . -name .py -type f -exec cat {}   wc -l    857365    real    0m3.458s    user    0m0.274s    sys     0m3.325s     time find . -name .py -type f -exec cat {}   tqdm  wc -l    857366it [0003, 246471.31its]    857365    real    0m3.585s    user    0m0.862s    sys     0m3.358sNote that the usual arguments for tqdm can also be specified... code sh     find . -name .py -type f -exec cat {}          tqdm --unit loc --unit_scale --total 857366  devnull    100 857K857K [00040000, 246Klocs]Backing up a large directory.. code sh     tar -zcf - docs  tqdm --bytes --total du -sb docs  cut -f1        backup.tgz     44                    153M352M [00140018, 11.0MBs]This can be beautified further.. code sh     BYTES(du -sb docs  cut -f1)     tar -cf - docs        tqdm --bytes --total BYTES --desc Processing  gzip        tqdm --bytes --total BYTES --desc Compressed --position 1        backup.tgz    Processing 100 352M352M [00140000, 30.2MBs]    Compressed  42             148M352M [00140019, 10.9MBs]Or done on a file level using 7-zip.. code sh     7z a -bd -r backup.7z docs  grep Compressing        tqdm --total (find docs -type f  wc -l) --unit files        grep -v Compressing    100 1532715327 [01000000, 712.96filess]Pre-existing CLI programs already outputting basic progress information willbenefit from tqdms --update and --update_to flags.. code sh     seq 3 0.1 5  tqdm --total 5 --update_to --null    100 5.05 [00000000, 9673.21its]     seq 10  tqdm --update --null   1 + 2 + ... + 10  55 iterations    55it [0000, 90006.52its]FAQ and Known Issues--------------------GitHub-IssuesThe most common issues relate to excessive output on multiple lines, insteadof a neat one-line progress bar.- Consoles in general require support for carriage return (CR, r).- Nested progress bars   Consoles in general require support for moving cursors up to the    previous line. For example,    IDLE httpsgithub.comtqdmtqdmissues191issuecomment-230168030__,    ConEmu httpsgithub.comtqdmtqdmissues254__ and    PyCharm httpsgithub.comtqdmtqdmissues203__ (also    here httpsgithub.comtqdmtqdmissues208__,    here httpsgithub.comtqdmtqdmissues307__, and    here httpsgithub.comtqdmtqdmissues454issuecomment-335416815__)    lack full support.   Windows additionally may require the Python module colorama    to ensure nested bars stay within their respective lines.- Unicode   Environments which report that they support unicode will have solid smooth    progressbars. The fallback is an ascii-only bar.   Windows consoles often only partially support unicode and thus    often require explicit asciiTrue httpsgithub.comtqdmtqdmissues454issuecomment-335416815__    (also here httpsgithub.comtqdmtqdmissues499__). This is due to    either normal-width unicode characters being incorrectly displayed as    wide, or some unicode characters not rendering.- Wrapping generators   Generator wrapper functions tend to hide the length of iterables.    tqdm does not.   Replace tqdm(enumerate(...)) with enumerate(tqdm(...)) or    tqdm(enumerate(x), totallen(x), ...).    The same applies to numpy.ndenumerate.   Replace tqdm(zip(a, b)) with zip(tqdm(a), b) or even    zip(tqdm(a), tqdm(b)).   The same applies to itertools.   Some useful convenience functions can be found under tqdm.contrib.- Hanging pipes in python2 httpsgithub.comtqdmtqdmissues359__  when using tqdm on the CLI, you may need to use Python 3.5+ for correct  buffering.- No intermediate output in docker-compose httpsgithub.comtqdmtqdmissues771__  use docker-compose run instead of docker-compose up and tty true.If you come across any other difficulties, browse and file GitHub-Issues.Documentation-------------Py-Versions README-Hits (Since 19 May 2016).. code python    class tqdm()            Decorate an iterable object, returning an iterator which acts exactly      like the original iterable, but prints a dynamically updating      progressbar every time a value is requested.            def __init__(self, iterableNone, descNone, totalNone, leaveTrue,                   fileNone, ncolsNone, mininterval0.1,                   maxinterval10.0, minitersNone, asciiNone, disableFalse,                   unitit, unit_scaleFalse, dynamic_ncolsFalse,                   smoothing0.3, bar_formatNone, initial0, positionNone,                   postfixNone, unit_divisor1000)Parameters iterable   iterable, optional      Iterable to decorate with a progressbar.    Leave blank to manually manage the updates. desc   str, optional      Prefix for the progressbar. total   int or float, optional      The number of expected iterations. If unspecified,    len(iterable) is used if possible. If float(inf) or as a last    resort, only basic progress statistics are displayed    (no ETA, no progressbar).    If gui is True and this parameter needs subsequent updating,    specify an initial arbitrary large positive number,    e.g. 9e9. leave   bool, optional      If [default True], keeps all traces of the progressbar    upon termination of iteration.    If None, will leave only if position is 0. file   io.TextIOWrapper or io.StringIO, optional      Specifies where to output the progress messages    (default sys.stderr). Uses file.write(str) and file.flush()    methods.  For encoding, see write_bytes. ncols   int, optional      The width of the entire output message. If specified,    dynamically resizes the progressbar to stay within this bound.    If unspecified, attempts to use environment width. The    fallback is a meter width of 10 and no limit for the counter and    statistics. If 0, will not print any meter (only stats). mininterval   float, optional      Minimum progress display update interval [default 0.1] seconds. maxinterval   float, optional      Maximum progress display update interval [default 10] seconds.    Automatically adjusts miniters to correspond to mininterval    after long display update lag. Only works if dynamic_miniters    or monitor thread is enabled. miniters   int or float, optional      Minimum progress display update interval, in iterations.    If 0 and dynamic_miniters, will automatically adjust to equal    mininterval (more CPU efficient, good for tight loops).    If  0, will skip display of specified number of iterations.    Tweak this and mininterval to get very efficient loops.    If your progress is erratic with both fast and slow iterations    (network, skipping items, etc) you should set miniters1. ascii   bool or str, optional      If unspecified or False, use unicode (smooth blocks) to fill    the meter. The fallback is to use ASCII characters  123456789. disable   bool, optional      Whether to disable the entire progressbar wrapper    [default False]. If set to None, disable on non-TTY. unit   str, optional      String that will be used to define the unit of each iteration    [default it]. unit_scale   bool or int or float, optional      If 1 or True, the number of iterations will be reducedscaled    automatically and a metric prefix following the    International System of Units standard will be added    (kilo, mega, etc.) [default False]. If any other non-zero    number, will scale total and n. dynamic_ncols   bool, optional      If set, constantly alters ncols and nrows to the    environment (allowing for window resizes) [default False]. smoothing   float, optional      Exponential moving average smoothing factor for speed estimates    (ignored in GUI mode). Ranges from 0 (average speed) to 1    (currentinstantaneous speed) [default 0.3]. bar_format   str, optional      Specify a custom bar string formatting. May impact performance.    [default {l_bar}{bar}{r_bar}], where    l_bar{desc} {percentage3.0f} and    r_bar {n_fmt}{total_fmt} [{elapsed}{remaining},     {rate_fmt}{postfix}]    Possible vars l_bar, bar, r_bar, n, n_fmt, total, total_fmt,    percentage, elapsed, elapsed_s, ncols, nrows, desc, unit,    rate, rate_fmt, rate_noinv, rate_noinv_fmt,    rate_inv, rate_inv_fmt, postfix, unit_divisor,    remaining, remaining_s, eta.    Note that a trailing   is automatically removed after {desc}    if the latter is empty. initial   int or float, optional      The initial counter value. Useful when restarting a progress    bar [default 0]. If using float, consider specifying {n.3f}    or similar in bar_format, or specifying unit_scale. position   int, optional      Specify the line offset to print this bar (starting from 0)    Automatic if unspecified.    Useful to manage multiple bars at once (eg, from threads). postfix   dict or , optional      Specify additional stats to display at the end of the bar.    Calls set_postfix(postfix) if possible (dict). unit_divisor   float, optional      [default 1000], ignored unless unit_scale is True. write_bytes   bool, optional      Whether to write bytes. If (default False) will write unicode. lock_args   tuple, optional      Passed to refresh for intermediate output    (initialisation, iterating, and updating). nrows   int, optional      The screen height. If specified, hides nested bars outside this    bound. If unspecified, attempts to use environment height.    The fallback is 20. colour   str, optional      Bar colour (e.g. green, 00ff00). delay   float, optional      Dont display until [default 0] seconds have elapsed.Extra CLI Options delim   chr, optional      Delimiting character [default n]. Use 0 for null.    N.B. on Windows systems, Python converts n to rn. buf_size   int, optional      String buffer size in bytes [default 256]    used when delim is specified. bytes   bool, optional      If true, will count bytes, ignore delim, and default    unit_scale to True, unit_divisor to 1024, and unit to B. tee   bool, optional      If true, passes stdin to both stderr and stdout. update   bool, optional      If true, will treat input as newly elapsed iterations,    i.e. numbers to pass to update(). Note that this is slow    (2e5 its) since every input must be decoded as a number. update_to   bool, optional      If true, will treat input as total elapsed iterations,    i.e. numbers to assign to self.n. Note that this is slow    (2e5 its) since every input must be decoded as a number. null   bool, optional      If true, will discard input (no stdout). manpath   str, optional      Directory in which to install tqdm man pages. comppath   str, optional      Directory in which to place tqdm completion. log   str, optional      CRITICALFATALERRORWARN(ING)[default INFO]DEBUGNOTSET.Returns out   decorated iterator.  .. code python    class tqdm()      def update(self, n1)                    Manually update the progress bar, useful for streams          such as reading files.          E.g.           t  tqdm(totalfilesize)  Initialise           for current_buffer in stream          ...    ...          ...    t.update(len(current_buffer))           t.close()          The last line is highly recommended, but possibly not necessary if          t.update() will be called in such a way that filesize will be          exactly reached and printed.          Parameters          ----------          n   int or float, optional              Increment to add to the internal counter of iterations              [default 1]. If using float, consider specifying {n.3f}              or similar in bar_format, or specifying unit_scale.          Returns          -------          out   bool or None              True if a display() was triggered.                def close(self)          Cleanup and (if leaveFalse) close the progressbar.      def clear(self, nomoveFalse)          Clear current bar display.      def refresh(self)                    Force refresh the display of this bar.          Parameters          ----------          nolock   bool, optional              If True, does not lock.              If [default False] calls acquire() on internal lock.          lock_args   tuple, optional              Passed to internal locks acquire().              If specified, will only display() if acquire() returns True.                def unpause(self)          Restart tqdm timer from last print time.      def reset(self, totalNone)                    Resets to 0 iterations for repeated use.          Consider combining with leaveTrue.          Parameters          ----------          total   int or float, optional. Total to use for the new bar.                def set_description(self, descNone, refreshTrue)                    Setmodify description of the progress bar.          Parameters          ----------          desc   str, optional          refresh   bool, optional              Forces refresh [default True].                def set_postfix(self, ordered_dictNone, refreshTrue, tqdm_kwargs)                    Setmodify postfix (additional stats)          with automatic formatting based on datatype.          Parameters          ----------          ordered_dict   dict or OrderedDict, optional          refresh   bool, optional              Forces refresh [default True].          kwargs   dict, optional                classmethod      def write(cls, s, filesys.stdout, endn)          Print a message via tqdm (without overlap with bars).      property      def format_dict(self)          Public API for read-only member access.      def display(self, msgNone, posNone)                    Use self.sp to display msg in the specified pos.          Consider overloading this function when inheriting to use e.g.          self.some_frontend(self.format_dict) instead of self.sp.          Parameters          ----------          msg   str, optional. What to display (default repr(self)).          pos   int, optional. Position to moveto            (default abs(self.pos)).                classmethod      contextmanager      def wrapattr(cls, stream, method, totalNone, bytesTrue, tqdm_kwargs)                    stream   file-like object.          method   str, read or write. The result of read() and              the first argument of write() should have a len().           with tqdm.wrapattr(file_obj, read, totalfile_obj.size) as fobj          ...     while True          ...         chunk  fobj.read(chunk_size)          ...         if not chunk          ...             break                classmethod      def pandas(cls, targs, tqdm_kwargs)          Registers the current tqdm class with pandas.    def trange(args, tqdm_kwargs)        Shortcut for tqdm(range(args), tqdm_kwargs).Convenience Functions.. code python    def tqdm.contrib.tenumerate(iterable, start0, totalNone,                                tqdm_classtqdm.auto.tqdm, tqdm_kwargs)        Equivalent of numpy.ndenumerate or builtin enumerate.    def tqdm.contrib.tzip(iter1, iter2plus, tqdm_kwargs)        Equivalent of builtin zip.    def tqdm.contrib.tmap(function, sequences, tqdm_kwargs)        Equivalent of builtin map.Submodules.. code python    class tqdm.notebook.tqdm(tqdm.tqdm)        IPythonJupyter Notebook widget.    class tqdm.auto.tqdm(tqdm.tqdm)        Automatically chooses beween tqdm.notebook and tqdm.tqdm.    class tqdm.asyncio.tqdm(tqdm.tqdm)      Asynchronous version.      classmethod      def as_completed(cls, fs, , loopNone, timeoutNone, totalNone,                       tqdm_kwargs)          Wrapper for asyncio.as_completed.    class tqdm.gui.tqdm(tqdm.tqdm)        Matplotlib GUI version.    class tqdm.tk.tqdm(tqdm.tqdm)        Tkinter GUI version.    class tqdm.rich.tqdm(tqdm.tqdm)        rich.progress version.    class tqdm.keras.TqdmCallback(keras.callbacks.Callback)        Keras callback for epoch and batch progress.    class tqdm.dask.TqdmCallback(dask.callbacks.Callback)        Dask callback for task progress.contrib+++++++++++The tqdm.contrib package also contains experimental modules- tqdm.contrib.itertools Thin wrappers around itertools- tqdm.contrib.concurrent Thin wrappers around concurrent.futures- tqdm.contrib.slack Posts to Slack httpsslack.com__ bots- tqdm.contrib.discord Posts to Discord httpsdiscord.com__ bots- tqdm.contrib.telegram Posts to Telegram httpstelegram.org__ bots- tqdm.contrib.bells Automagically enables all optional features   auto, pandas, slack, discord, telegramExamples and Advanced Usage---------------------------- See the examples httpsgithub.comtqdmtqdmtreemasterexamples__  folder- import the module and run help()- consult the wiki httpsgithub.comtqdmtqdmwiki__   this has an    excellent article httpsgithub.comtqdmtqdmwikiHow-to-make-a-great-Progress-Bar__    on how to make a great progressbar- check out the slides from PyData London httpstqdm.github.ioPyData2019slides.html__, or- run the binder-demo.Description and additional statsCustom information can be displayed and updated dynamically on tqdm barswith the desc and postfix arguments.. code python    from tqdm import tqdm, trange    from random import random, randint    from time import sleep    with trange(10) as t        for i in t             Description will be displayed on the left            t.set_description(GEN i  i)             Postfix will be displayed on the right,             formatted automatically based on arguments datatype            t.set_postfix(lossrandom(), genrandint(1,999), strh,                          lst[1, 2])            sleep(0.1)    with tqdm(total10, bar_format{postfix[0]} {postfix[1][value]8.2g},              postfix[Batch, {value 0}]) as t        for i in range(10)            sleep(0.1)            t.postfix[1][value]  i  2            t.update()Points to remember when using {postfix[...]} in the bar_format string- postfix also needs to be passed as an initial argument in a compatible  format, and- postfix will be auto-converted to a string if it is a dict-like  object. To prevent this behaviour, insert an extra item into the dictionary  where the key is not a string.Additional bar_format parameters may also be defined by overridingformat_dict, and the bar itself may be modified using ascii.. code python    from tqdm import tqdm    class TqdmExtraFormat(tqdm)        Provides a total_time format parameter        property        def format_dict(self)            d  super(TqdmExtraFormat, self).format_dict            total_time  d[elapsed]  (d[total] or 0)  max(d[n], 1)            d.update(total_timeself.format_interval(total_time) +  in total)            return d    for i in TqdmExtraFormat(          range(9), ascii .oO0,          bar_format{total_time} {percentage.0f}{bar}{r_bar})        if i  4            break.. code    0000 in total 440000.      49 [00000000, 962.93its]Note that {bar} also supports a format specifier [width][type].- width   unspecified (default) automatic to fill ncols   int  0 fixed width overriding ncols logic   int  0 subtract from the automatic default- type   a ascii (asciiTrue override)   u unicode (asciiFalse override)   b blank (ascii   override)This means a fixed bar with right-justified text may be created by usingbar_format{l_bar}{bar10}{bar-10b}right-justifiedNested progress barstqdm supports nested progress bars. Heres an example.. code python    from tqdm.auto import trange    from time import sleep    for i in trange(4, desc1st loop)        for j in trange(5, desc2nd loop)            for k in trange(50, desc3rd loop, leaveFalse)                sleep(0.01)For manual control over positioning (e.g. for multi-processing use),you may specify positionn where n0 for the outermost bar,n1 for the next, and so on.However, its best to check if tqdm can work without manual positionfirst... code python    from time import sleep    from tqdm import trange, tqdm    from multiprocessing import Pool, RLock, freeze_support    L  list(range(9))    def progresser(n)        interval  0.001  (n + 2)        total  5000        text  {}, est. {04.2}s.format(n, interval  total)        for _ in trange(total, desctext, positionn)            sleep(interval)    if __name__  __main__        freeze_support()   for Windows support        tqdm.set_lock(RLock())   for managing output contention        p  Pool(initializertqdm.set_lock, initargs(tqdm.get_lock(),))        p.map(progresser, L)Note that in Python 3, tqdm.write is thread-safe.. code python    from time import sleep    from tqdm import tqdm, trange    from concurrent.futures import ThreadPoolExecutor    L  list(range(9))    def progresser(n)        interval  0.001  (n + 2)        total  5000        text  {}, est. {04.2}s.format(n, interval  total)        for _ in trange(total, desctext)            sleep(interval)        if n  6            tqdm.write(n  6 completed.)            tqdm.write(tqdm.write() is thread-safe in py3)    if __name__  __main__        with ThreadPoolExecutor() as p            p.map(progresser, L)Hooks and callbackstqdm can easily support callbackshooks and manual updates.Heres an example with urlliburllib.urlretrieve documentation     [...]     If present, the hook function will be called once     on establishment of the network connection and once after each block read     thereafter. The hook will be passed three arguments a count of blocks     transferred so far, a block size in bytes, and the total size of the file.     [...].. code python    import urllib, os    from tqdm import tqdm    urllib  getattr(urllib, request, urllib)    class TqdmUpTo(tqdm)        Provides update_to(n) which uses tqdm.update(delta_n).        def update_to(self, b1, bsize1, tsizeNone)                        b   int, optional                Number of blocks transferred so far [default 1].            bsize   int, optional                Size of each block (in tqdm units) [default 1].            tsize   int, optional                Total size (in tqdm units). If [default None] remains unchanged.                        if tsize is not None                self.total  tsize            return self.update(b  bsize - self.n)   also sets self.n  b  bsize    eg_link  httpscaspersci.uk.tomatryoshka.zip    with TqdmUpTo(unitB, unit_scaleTrue, unit_divisor1024, miniters1,                  desceg_link.split()[-1]) as t   all optional kwargs        urllib.urlretrieve(eg_link, filenameos.devnull,                           reporthookt.update_to, dataNone)        t.total  t.nInspired by twine242 httpsgithub.compypatwinepull242__.Functional alternative inexamplestqdm_wget.py httpsgithub.comtqdmtqdmblobmasterexamplestqdm_wget.py__.It is recommend to use miniters1 whenever there is potentiallylarge differences in iteration speed (e.g. downloading a file overa patchy connection).Wrapping readwrite methodsTo measure throughput through a file-like objects read or writemethods, use CallbackIOWrapper.. code python    from tqdm.auto import tqdm    from tqdm.utils import CallbackIOWrapper    with tqdm(totalfile_obj.size,              unitB, unit_scaleTrue, unit_divisor1024) as t        fobj  CallbackIOWrapper(t.update, file_obj, read)        while True            chunk  fobj.read(chunk_size)            if not chunk                break        t.reset()         ... continue to use t for something elseAlternatively, use the even simpler wrapattr convenience function,which would condense both the urllib and CallbackIOWrapper examplesdown to.. code python    import urllib, os    from tqdm import tqdm    eg_link  httpscaspersci.uk.tomatryoshka.zip    response  getattr(urllib, request, urllib).urlopen(eg_link)    with tqdm.wrapattr(open(os.devnull, wb), write,                       miniters1, desceg_link.split()[-1],                       totalgetattr(response, length, None)) as fout        for chunk in response            fout.write(chunk)The requests equivalent is nearly identical.. code python    import requests, os    from tqdm import tqdm    eg_link  httpscaspersci.uk.tomatryoshka.zip    response  requests.get(eg_link, streamTrue)    with tqdm.wrapattr(open(os.devnull, wb), write,                       miniters1, desceg_link.split()[-1],                       totalint(response.headers.get(content-length, 0))) as fout        for chunk in response.iter_content(chunk_size4096)            fout.write(chunk)Custom callbacktqdm is known for intelligently skipping unnecessary displays. To make acustom callback take advantage of this, simply use the return value ofupdate(). This is set to True if a display() was triggered... code python    from tqdm.auto import tqdm as std_tqdm    def external_callback(args, kwargs)        ...    class TqdmExt(std_tqdm)        def update(self, n1)            displayed  super(TqdmExt, self).update(n)            if displayed                external_callback(self.format_dict)            return displayedasyncioNote that break isnt currently caught by asynchronous iterators.This means that tqdm cannot clean up after itself in this case.. code python    from tqdm.asyncio import tqdm    async for i in tqdm(range(9))        if i  2            breakInstead, either call pbar.close() manually or use the context manager syntax.. code python    from tqdm.asyncio import tqdm    with tqdm(range(9)) as pbar        async for i in pbar            if i  2                breakPandas IntegrationDue to popular demand weve added support for pandas -- heres an examplefor DataFrame.progress_apply and DataFrameGroupBy.progress_apply.. code python    import pandas as pd    import numpy as np    from tqdm import tqdm    df  pd.DataFrame(np.random.randint(0, 100, (100000, 6)))     Register pandas.progress_apply and pandas.Series.map_apply with tqdm     (can use tqdm.gui.tqdm, tqdm.notebook.tqdm, optional kwargs, etc.)    tqdm.pandas(descmy bar)     Now you can use progress_apply instead of apply     and progress_map instead of map    df.progress_apply(lambda x x2)     can also groupby     df.groupby(0).progress_apply(lambda x x2)In case youre interested in how this works (and how to modify it for yourown callbacks), see theexamples httpsgithub.comtqdmtqdmtreemasterexamples__folder or import the module and run help().Keras IntegrationA keras callback is also available.. code python    from tqdm.keras import TqdmCallback    ...    model.fit(..., verbose0, callbacks[TqdmCallback()])Dask IntegrationA dask callback is also available.. code python    from tqdm.dask import TqdmCallback    with TqdmCallback(desccompute)        ...        arr.compute()     or use callback globally    cb  TqdmCallback(descglobal)    cb.register()    arr.compute()IPythonJupyter IntegrationIPythonJupyter is supported via the tqdm.notebook submodule.. code python    from tqdm.notebook import trange, tqdm    from time import sleep    for i in trange(3, desc1st loop)        for j in tqdm(range(100), desc2nd loop)            sleep(0.01)In addition to tqdm features, the submodule provides a native Jupyterwidget (compatible with IPython v1-v4 and Jupyter), fully working nested barsand colour hints (blue normal, green completed, red errorinterrupt,light blue no ETA) as demonstrated below.Screenshot-Jupyter1Screenshot-Jupyter2Screenshot-Jupyter3The notebook version supports percentage or pixels for overall width(e.g. ncols100 or ncols480px).It is also possible to let tqdm automatically choose betweenconsole or notebook versions by using the autonotebook submodule.. code python    from tqdm.autonotebook import tqdm    tqdm.pandas()Note that this will issue a TqdmExperimentalWarning if run in a notebooksince it is not meant to be possible to distinguish between jupyter notebookand jupyter console. Use auto instead of autonotebook to suppressthis warning.Note that notebooks will display the bar in the cell where it was created.This may be a different cell from the one where it is used.If this is not desired, either- delay the creation of the bar to the cell where it must be displayed, or- create the bar with displayFalse, and in a later cell call  display(bar.container).. code python    from tqdm.notebook import tqdm    pbar  tqdm(..., displayFalse).. code python     different cell    display(pbar.container)The keras callback has a display() method which can be used likewise.. code python    from tqdm.keras import TqdmCallback    cbk  TqdmCallback(displayFalse).. code python     different cell    cbk.display()    model.fit(..., verbose0, callbacks[cbk])Another possibility is to have a single bar (near the top of the notebook)which is constantly re-used (using reset() rather than close()).For this reason, the notebook version (unlike the CLI version) does notautomatically call close() upon Exception... code python    from tqdm.notebook import tqdm    pbar  tqdm().. code python     different cell    iterable  range(100)    pbar.reset(totallen(iterable))   initialise with new total    for i in iterable        pbar.update()    pbar.refresh()   force print final status but dont close()Custom IntegrationTo change the default arguments (such as making dynamic_ncolsTrue),simply use built-in Python magic.. code python    from functools import partial    from tqdm import tqdm as std_tqdm    tqdm  partial(std_tqdm, dynamic_ncolsTrue)For further customisation,tqdm may be inherited from to create custom callbacks (as with theTqdmUpTo example above hooks-and-callbacks__) or for custom frontends(e.g. GUIs such as notebook or plotting packages). In the latter case1. def __init__() to call super().__init__(..., guiTrue) to disable   terminal status_printer creation.2. Redefine close(), clear(), display().Consider overloading display() to use e.g.self.frontend(self.format_dict) instead of self.sp(repr(self)).Some submodule examples of inheritance- tqdmnotebook.py httpsgithub.comtqdmtqdmblobmastertqdmnotebook.py__- tqdmgui.py httpsgithub.comtqdmtqdmblobmastertqdmgui.py__- tqdmtk.py httpsgithub.comtqdmtqdmblobmastertqdmtk.py__- tqdmcontribslack.py httpsgithub.comtqdmtqdmblobmastertqdmcontribslack.py__- tqdmcontribdiscord.py httpsgithub.comtqdmtqdmblobmastertqdmcontribdiscord.py__- tqdmcontribtelegram.py httpsgithub.comtqdmtqdmblobmastertqdmcontribtelegram.py__Dynamic MonitorMeterYou can use a tqdm as a meter which is not monotonically increasing.This could be because n decreases (e.g. a CPU usage monitor) or totalchanges.One example would be recursively searching for files. The total is thenumber of objects found so far, while n is the number of those objects whichare files (rather than folders).. code python    from tqdm import tqdm    import os.path    def find_files_recursively(path, show_progressTrue)        files  []         total1 assumes path is a file        t  tqdm(total1, unitfile, disablenot show_progress)        if not os.path.exists(path)            raise IOError(Cannot find + path)        def append_found_file(f)            files.append(f)            t.update()        def list_found_dir(path)            returns os.listdir(path) assuming os.path.isdir(path)            listing  os.listdir(path)             subtract 1 since a file we found was actually this directory            t.total + len(listing) - 1             fancy way to give info without forcing a refresh            t.set_postfix(dirpath[-10], refreshFalse)            t.update(0)   may trigger a refresh            return listing        def recursively_search(path)            if os.path.isdir(path)                for f in list_found_dir(path)                    recursively_search(os.path.join(path, f))            else                append_found_file(path)        recursively_search(path)        t.set_postfix(dirpath)        t.close()        return filesUsing update(0) is a handy way to let tqdm decide when to trigger adisplay refresh to avoid console spamming.Writing messagesThis is a work in progress (see737 httpsgithub.comtqdmtqdmissues737__).Since tqdm uses a simple printing mechanism to display progress bars,you should not write any message in the terminal using print() whilea progressbar is open.To write messages in the terminal without any collision with tqdm bardisplay, a .write() method is provided.. code python    from tqdm.auto import tqdm, trange    from time import sleep    bar  trange(10)    for i in bar         Print using tqdm class method .write()        sleep(0.1)        if not (i  3)            tqdm.write(Done task i  i)         Can also use bar.write()By default, this will print to standard output sys.stdout. but you canspecify any file-like object using the file argument. For example, thiscan be used to redirect the messages writing to a log file or class.Redirecting writingIf using a library that can print messages to the console, editing the libraryby  replacing print() with tqdm.write() may not be desirable.In that case, redirecting sys.stdout to tqdm.write() is an option.To redirect sys.stdout, create a file-like class that will writeany input string to tqdm.write(), and supply the argumentsfilesys.stdout, dynamic_ncolsTrue.A reusable canonical example is given below.. code python    from time import sleep    import contextlib    import sys    from tqdm import tqdm    from tqdm.contrib import DummyTqdmFile    contextlib.contextmanager    def std_out_err_redirect_tqdm()        orig_out_err  sys.stdout, sys.stderr        try            sys.stdout, sys.stderr  map(DummyTqdmFile, orig_out_err)            yield orig_out_err[0]         Relay exceptions        except Exception as exc            raise exc         Always restore sys.stdouterr if necessary        finally            sys.stdout, sys.stderr  orig_out_err    def some_fun(i)        print(Fee, fi, fo,.split()[i])     Redirect stdout to tqdm.write() (dont forget the as save_stdout)    with std_out_err_redirect_tqdm() as orig_stdout         tqdm needs the original stdout         and dynamic_ncolsTrue to autodetect console width        for i in tqdm(range(3), fileorig_stdout, dynamic_ncolsTrue)            sleep(.5)            some_fun(i)     After the with, printing is restored    print(Done)Redirecting loggingSimilar to sys.stdoutsys.stderr as detailed above, console loggingmay also be redirected to tqdm.write().Warning if also redirecting sys.stdoutsys.stderr, make sure toredirect logging first if needed.Helper methods are available in tqdm.contrib.logging. For example.. code python    import logging    from tqdm import trange    from tqdm.contrib.logging import logging_redirect_tqdm    LOG  logging.getLogger(__name__)    if __name__  __main__        logging.basicConfig(levellogging.INFO)        with logging_redirect_tqdm()            for i in trange(9)                if i  4                    LOG.info(console logging redirected to tqdm.write())         logging restoredMonitoring thread, intervals and miniterstqdm implements a few tricks to increase efficiency and reduce overhead.- Avoid unnecessary frequent bar refreshing mininterval defines how long  to wait between each refresh. tqdm always gets updated in the background,  but it will display only every mininterval.- Reduce number of calls to check system clocktime.- mininterval is more intuitive to configure than miniters.  A clever adjustment system dynamic_miniters will automatically adjust  miniters to the amount of iterations that fit into time mininterval.  Essentially, tqdm will check if its time to print without actually  checking time. This behaviour can be still be bypassed by manually setting  miniters.However, consider a case with a combination of fast and slow iterations.After a few fast iterations, dynamic_miniters will set miniters to alarge number. When iteration rate subsequently slows, miniters willremain large and thus reduce display update frequency. To address this- maxinterval defines the maximum time between display refreshes.  A concurrent monitoring thread checks for overdue updates and forces one  where necessary.The monitoring thread should not have a noticeable overhead, and guaranteesupdates at least every 10 seconds by default.This value can be directly changed by setting the monitor_interval ofany tqdm instance (i.e. t  tqdm.tqdm(...) t.monitor_interval  2).The monitor thread may be disabled application-wide by settingtqdm.tqdm.monitor_interval  0 before instantiation of any tqdm bar.Merch-----You can buy tqdm branded merch httpstqdm.github.iomerch__ nowContributions-------------GitHub-Commits GitHub-Issues GitHub-PRs OpenHub-Status GitHub-Contributions CII Best PracticesAll source code is hosted on GitHub httpsgithub.comtqdmtqdm__.Contributions are welcome.See theCONTRIBUTING httpsgithub.comtqdmtqdmblobmasterCONTRIBUTING.md__file for more information.Developers who have made significant contributions, ranked by SLoC(surviving lines of code,git fame httpsgithub.comcasperdclgit-fame__ -wMC --excl .(pnggifjpg)),are   Name                 ID                                                       SLoC Notes   Casper da Costa-Luis casperdcl httpsgithub.comcasperdcl__             78 primary maintainer Gift-CasperStephen Larroque     lrq3000 httpsgithub.comlrq3000__                 10 team memberMartin Zugnoni       martinzugnoni httpsgithub.commartinzugnoni__     4Daniel Ecer          de-code httpsgithub.comde-code__                 2Richard Sheridan     richardsheridan httpsgithub.comrichardsheridan__ 1Guangshuo Chen       chengs httpsgithub.comchengs__                   1Kyle Altendorf       altendky httpsgithub.comaltendky__               1Matthew Stevens      mjstevens777 httpsgithub.commjstevens777__       1Hadrien Mary         hadim httpsgithub.comhadim__                     1  team memberNoam Yorav-Raphael   noamraph httpsgithub.comnoamraph__               1  original authorMikhail Korobov      kmike httpsgithub.comkmike__                     1  team member   Ports to Other LanguagesA list is available onthis wiki page httpsgithub.comtqdmtqdmwikitqdm-ports__.LICENCE-------Open Source (OSI approved) LICENCECitation information DOIREADME-Hits (Since 19 May 2016).. Logo image httpsimg.tqdm.mllogo.gif.. Screenshot image httpsimg.tqdm.mltqdm.gif.. Video image httpsimg.tqdm.mlvideo.jpg   target httpstqdm.github.iovideo.. Slides image httpsimg.tqdm.mlslides.jpg   target httpstqdm.github.ioPyData2019slides.html.. Merch image httpsimg.tqdm.mlmerch.jpg   target httpstqdm.github.iomerch.. Build-Status image httpsimg.shields.iogithubactionsworkflowstatustqdmtqdmtest.ymlbranchmasterlabeltqdmlogoGitHub   target httpsgithub.comtqdmtqdmactionsworkflowstest.yml.. Coverage-Status image httpsimg.shields.iocoverallsgithubtqdmtqdmmasterlogocoveralls   target httpscoveralls.iogithubtqdmtqdm.. Branch-Coverage-Status image httpscodecov.ioghtqdmtqdmbranchmastergraphbadge.svg   target httpscodecov.ioghtqdmtqdm.. Codacy-Grade image httpsapp.codacy.comprojectbadgeGrade3f965571598f44549c7818f29cdcf177   target httpswww.codacy.comghtqdmtqdmdashboard.. CII Best Practices image httpsbestpractices.coreinfrastructure.orgprojects3264badge   target httpsbestpractices.coreinfrastructure.orgprojects3264.. GitHub-Status image httpsimg.shields.iogithubtagtqdmtqdm.svgmaxAge86400logogithublogoColorwhite   target httpsgithub.comtqdmtqdmreleases.. GitHub-Forks image httpsimg.shields.iogithubforkstqdmtqdm.svglogogithublogoColorwhite   target httpsgithub.comtqdmtqdmnetwork.. GitHub-Stars image httpsimg.shields.iogithubstarstqdmtqdm.svglogogithublogoColorwhite   target httpsgithub.comtqdmtqdmstargazers.. GitHub-Commits image httpsimg.shields.iogithubcommit-activityytqdmtqdm.svglogogitlogoColorwhite   target httpsgithub.comtqdmtqdmgraphscommit-activity.. GitHub-Issues image httpsimg.shields.iogithubissues-closedtqdmtqdm.svglogogithublogoColorwhite   target httpsgithub.comtqdmtqdmissuesq.. GitHub-PRs image httpsimg.shields.iogithubissues-pr-closedtqdmtqdm.svglogogithublogoColorwhite   target httpsgithub.comtqdmtqdmpulls.. GitHub-Contributions image httpsimg.shields.iogithubcontributorstqdmtqdm.svglogogithublogoColorwhite   target httpsgithub.comtqdmtqdmgraphscontributors.. GitHub-Updated image httpsimg.shields.iogithublast-committqdmtqdmmaster.svglogogithublogoColorwhitelabelpushed   target httpsgithub.comtqdmtqdmpulse.. Gift-Casper image httpsimg.shields.iobadgedynamicjson.svgcolorff69b4labelgifts20receivedprefixC2A3query24..sumurlhttps3A2F2Fcaspersci.uk.to2Fgifts.json   target httpscdcl.mlsponsor.. Versions image httpsimg.shields.iopypivtqdm.svg   target httpstqdm.github.ioreleases.. PyPI-Downloads image httpsimg.shields.iopypidmtqdm.svglabelpypi20downloadslogoPyPIlogoColorwhite   target httpspepy.techprojecttqdm.. Py-Versions image httpsimg.shields.iopypipyversionstqdm.svglogopythonlogoColorwhite   target httpspypi.orgprojecttqdm.. Conda-Forge-Status image httpsimg.shields.iocondavconda-forgetqdm.svglabelconda-forgelogoconda-forge   target httpsanaconda.orgconda-forgetqdm.. Snapcraft image httpsimg.shields.iobadgesnap-install-82BEA0.svglogosnapcraft   target httpssnapcraft.iotqdm.. Docker image httpsimg.shields.iobadgedocker-pull-blue.svglogodockerlogoColorwhite   target httpshub.docker.comrtqdmtqdm.. Libraries-Rank image httpsimg.shields.iolibrariesiosourcerankpypitqdm.svglogokodinglogoColorwhite   target httpslibraries.iopypitqdm.. Libraries-Dependents image httpsimg.shields.iolibrariesiodependent-repospypitqdm.svglogokodinglogoColorwhite    target httpsgithub.comtqdmtqdmnetworkdependents.. OpenHub-Status image httpswww.openhub.netptqdmwidgetsproject_thin_badgeformatgif   target httpswww.openhub.netptqdmrefThin+badge.. awesome-python image httpsawesome.rementioned-badge.svg   target httpsgithub.comvintaawesome-python.. LICENCE image httpsimg.shields.iopypiltqdm.svg   target httpsraw.githubusercontent.comtqdmtqdmmasterLICENCE.. DOI image httpsimg.shields.iobadgeDOI-10.5281zenodo.595120-blue.svg   target httpsdoi.org10.5281zenodo.595120.. binder-demo image httpsmybinder.orgbadge_logo.svg   target httpsmybinder.orgv2ghtqdmtqdmmasterfilepathDEMO.ipynb.. Screenshot-Jupyter1 image httpsimg.tqdm.mljupyter-1.gif.. Screenshot-Jupyter2 image httpsimg.tqdm.mljupyter-2.gif.. Screenshot-Jupyter3 image httpsimg.tqdm.mljupyter-3.gif.. README-Hits image httpscaspersci.uk.tocgi-binhits.cgiqtqdmstylesocialrhttpsgithub.comtqdmtqdmlhttpsimg.tqdm.mlfavicon.pngfhttpsimg.tqdm.mllogo.gif   target httpscaspersci.uk.tocgi-binhits.cgiqtqdmaplotrhttpsgithub.comtqdmtqdmlhttpsimg.tqdm.mlfavicon.pngfhttpsimg.tqdm.mllogo.gifstylesocial</longdescription>
</pkgmetadata>