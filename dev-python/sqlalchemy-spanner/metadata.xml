<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Spanner dialect for SQLAlchemySpanner dialect for SQLAlchemy represents an interface API designed tomake it possible to control Cloud Spanner databases with SQLAlchemy API.The dialect is built on top of the Spanner DBAPI httpsgithub.comgoogleapispython-spannertreemastergooglecloudspanner_dbapi__,which is designed in accordance withPEP-249 httpswww.python.orgdevpepspep-0249__.Known limitations are listed here features-and-limitations__. Allsupported features have been tested and verified to work with the testconfigurations. There may be configurations andor data model variationsthat have not yet been covered by the tests and that show unexpectedbehavior. Please report any problems that you might encounter bycreating a newissue httpsgithub.comgoogleapispython-spanner-sqlalchemyissuesnew__.-  Cloud Spanner product   documentation httpscloud.google.comspannerdocs__-  SQLAlchemy product documentation httpswww.sqlalchemy.org__Quick Start-----------In order to use this package, you first need to go through the followingsteps1. Select or create a Cloud Platform   project. httpsconsole.cloud.google.comproject__2. Enable billing for your   project. httpscloud.google.combillingdocshow-tomodify-projectenable_billing_for_a_project__3. Enable the Google Cloud Spanner   API. httpscloud.google.comspanner__4. Setup   Authentication. httpsgoogleapis.devpythongoogle-api-corelatestauth.html__Installation------------Stable released version of the package is available on PyPi   pip install sqlalchemy-spannerTo install an in-development version of the package, clone itsGit-repository   git clone httpsgithub.comgoogleapispython-spanner-sqlalchemy.gitNext install the package from the package setup.py file   python setup.py installDuring setup the dialect will be registered with entry points.A Minimal App-------------Database URLIn order to connect to a database one have to use its URL on connectioncreation step. SQLAlchemy 1.3 and 1.4 versions have a bit of differenceon this step in a dialect prefix part.. code python    for SQLAlchemy 1.3   spannerprojectsproject-idinstancesinstance-iddatabasesdatabase-id    for SQLAlchemy 1.4   spanner+spannerprojectsproject-idinstancesinstance-iddatabasesdatabase-idCreate a table.. code python   from sqlalchemy import (       Column,       Integer,       MetaData,       String,       Table,       create_engine,   )   engine  create_engine(       spannerprojectsproject-idinstancesinstance-iddatabasesdatabase-id   )   metadata  MetaData(bindengine)   user  Table(       users,       metadata,       Column(user_id, Integer, primary_keyTrue),       Column(user_name, String(16), nullableFalse),   )   metadata.create_all(engine)Insert a row.. code python   import uuid   from sqlalchemy import (       MetaData,       Table,       create_engine,   )   engine  create_engine(       spannerprojectsproject-idinstancesinstance-iddatabasesdatabase-id   )   user  Table(users, MetaData(bindengine), autoloadTrue)   user_id  uuid.uuid4().hex[6].lower()   with engine.begin() as connection       connection.execute(user.insert(), {user_id user_id, user_name Full Name})Read.. code python   from sqlalchemy import MetaData, Table, create_engine, select   engine  create_engine(       spannerprojectsproject-idinstancesinstance-iddatabasesdatabase-id   )   table  Table(users, MetaData(bindengine), autoloadTrue)   with engine.begin() as connection       for row in connection.execute(select([], from_objtable)).fetchall()           print(row)Migration---------SQLAlchemy uses Alembic httpsalembic.sqlalchemy.orgenlatest__tool to organize database migrations.Spanner dialect doesnt provide a default migration environment, its upto user to write it. One thing to be noted here - one should explicitlyset alembic_version table not to use migration revision id as aprimary key.. code python   with connectable.connect() as connection       context.configure(           connectionconnection,           target_metadatatarget_metadata,           version_table_pkFalse,   dont use primary key in the versions table       )As Spanner restricts changing a primary key value, not setting the version_table_pk flagto False can cause migration problems. If alembic_versions table was already created with a primary key, setting the flag to False will not work, because the flag is only applied on table creation.    Notice that DDL statements in Spanner are not transactional. They will not be automatically reverted in case of a migration fail. Also Spanner encourage use of the autocommit_block() httpsalembic.sqlalchemy.orgenlatestapiruntime.htmlalembic.runtime.migration.MigrationContext.autocommit_block__ for migrations in order to prevent DDLs from aborting migration transactions with schema modifications. Warning A migration script can produce a lot of DDL statements. If each of the  statements is executed separately, performance issues can occur. To  avoid it, its highly recommended to use the Alembic batch  context httpsalembic.sqlalchemy.orgenlatestbatch.html__  feature to pack DDL statements into groups of statements.Features and limitations------------------------Interleaved tables Cloud Spanner dialect includes two dialect-specific arguments for  Table constructor, which help to define interleave relations  spanner_interleave_in - a parent table name  spanner_inverleave_on_delete_cascade - a flag specifying if  ON DELETE CASCADE statement must be used for the interleave  relation An example of interleave relations definition.. code python   team  Table(       team,       metadata,       Column(team_id, Integer, primary_keyTrue),       Column(team_name, String(16), nullableFalse),   )   team.create(engine)   client  Table(       client,       metadata,       Column(team_id, Integer, primary_keyTrue),       Column(client_id, Integer, primary_keyTrue),       Column(client_name, String(16), nullableFalse),       spanner_interleave_inteam,       spanner_interleave_on_delete_cascadeTrue,   )   client.add_is_dependent_on(team)   client.create(engine)Note Interleaved tables have a dependency between them, so theparent table must be created before the child table. When creatingtables with this feature, make sure to call add_is_dependent_on() onthe child table to request SQLAlchemy to create the parent table beforethe child table.Unique constraintsCloud Spanner doesnt support direct UNIQUE constraints creation. Inorder to achieve column values uniqueness UNIQUE indexes should be used.Instead of direct UNIQUE constraint creation.. code python   Table(       table,       metadata,       Column(col1, Integer),       UniqueConstraint(col1, nameuix_1)   )Create a UNIQUE index.. code python   Table(       table,       metadata,       Column(col1, Integer),       Index(uix_1, col1, uniqueTrue),   )Autocommit modeSpanner dialect supports both SERIALIZABLE and AUTOCOMMITisolation levels. SERIALIZABLE is the default one, wheretransactions need to be committed manually. AUTOCOMMIT modecorresponds to automatically committing of a query right in itsexecution time.Isolation level change example.. code python   from sqlalchemy import create_engine   eng  create_engine(spannerprojectsproject-idinstancesinstance-iddatabasesdatabase-id)   autocommit_engine  eng.execution_options(isolation_levelAUTOCOMMIT)Automatic transactions retryIn the default SERIALIZABLE mode transactions may fail with Aborted exception. This is a transient kind of errors, which mostly happen to prevent data corruption by concurrent modifications. Though the original transaction becomes non operational, a simple retry of the queries solves the issue.This, however, may require to manually repeat a long list of operations, executed in the failed transaction. To simplify it, Spanner Connection API tracks all the operations, executed inside current transaction, and their result checksums. If the transaction failed with Aborted exception, the Connection API will automatically start a new transaction and will re-run all the tracked operations, checking if their results are the same as they were in the original transaction. In case data changed, and results differ, the transaction is dropped, as there is no way to automatically retry it.In AUTOCOMMIT mode automatic transactions retry mechanism is disabled, as every operation is committed just in time, and there is no way an Aborted exception can happen.Autoincremented IDsCloud Spanner doesnt support autoincremented IDs mechanism due toperformance reasons (see for moredetails httpscloud.google.comspannerdocsschema-designprimary-key-prevent-hotspots__).We recommend that you use the Pythonuuid httpsdocs.python.org3libraryuuid.html__ module togenerate primary key fields to avoid creating monotonically increasingkeys.Though its not encouraged to do so, in case you need the feature, youcan simulate it manually as follows.. code python   with engine.begin() as connection       top_id  connection.execute(           select([user.c.user_id]).order_by(user.c.user_id.desc()).limit(1)       ).fetchone()       next_id  top_id[0] + 1 if top_id else 1       connection.execute(user.insert(), {user_id next_id})Query hintsSpanner dialect supports queryhints httpscloud.google.comspannerdocsquery-syntaxtable_hints__,which give the ability to set additional query execution parameters.Usage example.. code python   session  Session(engine)   Base  declarative_base()   class User(Base)       Data model.       __tablename__  users       id  Column(Integer, primary_keyTrue)       name  Column(String(50))   query  session.query(User)   query  query.with_hint(       selectableUser, text{FORCE_INDEXindex_name}   )   query  query.filter(User.name.in_([val1, val2]))   query.statement.compile(session.bind)ReadOnly transactionsBy default, transactions produced by a Spanner connection are inReadWrite mode. However, some applications require an ability to grantReadOnly access to usersmethods for these cases Spanner dialectsupports the read_only execution option, which switches a connectioninto ReadOnly mode.. code python   with engine.connect().execution_options(read_onlyTrue) as connection       connection.execute(select([], from_objtable)).fetchall()Note that execution options are applied lazily - on the execute()method call, right before it.ReadOnlyReadWrite mode of a connection cant be changed while atransaction is in progress - first you must commit or rollback it.Stale readsTo use the Spanner StaleReads httpscloud.google.comspannerdocsreadsperform-stale-read__with SQLAlchemy you can tweak the connection execution options with awanted staleness value. For example.. code python    maximum staleness   with engine.connect().execution_options(       read_onlyTrue,       staleness{max_staleness datetime.timedelta(seconds5)}   ) as connection       connection.execute(select([], from_objtable)).fetchall().. code python    exact staleness   with engine.connect().execution_options(       read_onlyTrue,       staleness{exact_staleness datetime.timedelta(seconds5)}   ) as connection       connection.execute(select([], from_objtable)).fetchall().. code python    min read timestamp   with engine.connect().execution_options(       read_onlyTrue,       staleness{min_read_timestamp datetime.datetime(2021, 11, 17, 12, 55, 30)}   ) as connection       connection.execute(select([], from_objtable)).fetchall().. code python    read timestamp   with engine.connect().execution_options(       read_onlyTrue,       staleness{read_timestamp datetime.datetime(2021, 11, 17, 12, 55, 30)}   ) as connection       connection.execute(select([], from_objtable)).fetchall()Note that the set option will be dropped when the connection is returnedback to the pool.DDL and transactionsDDL statements are executed outside the regular transactions mechanism,which means DDL statements will not be rolled back on normal transactionrollback.Dropping a tableCloud Spanner, by default, doesnt drop tables, which have secondaryindexes andor foreign key constraints. In Spanner dialect forSQLAlchemy, however, this restriction is omitted - if a table you aretrying to delete has indexesforeign keys, they will be droppedautomatically right before dropping the table.Data typesData types table mapping SQLAlchemy types to Cloud Spanner types SQLAlchemy Spanner INTEGER    INT64BIGINT     INT64DECIMAL    NUMERICFLOAT      FLOAT64TEXT       STRINGARRAY      ARRAYBINARY     BYTESVARCHAR    STRINGCHAR       STRINGBOOLEAN    BOOLDATETIME   TIMESTAMPNUMERIC    NUMERIC Other limitations-  WITH RECURSIVE statement is not supported.-  Named schemas are not supported.-  Temporary tables are not supported.-  Numeric type dimensions (scale and precision) are constant. See the   docs httpscloud.google.comspannerdocsdata-typesnumeric_types__.Best practices--------------When a SQLAlchemy function is called, a new connection to a database isestablished and a Spanner session object is fetched. In case ofconnectionless execution these fetches are done for every execute()call, which can cause a significant latency. To avoid initiating aSpanner session on every execute() call its recommended to writecode in connection-bounded fashion. Once a Connection() object isexplicitly initiated, it fetches a Spanner session object and uses itfor all the following calls made on this Connection() object.Non-optimal connectionless use.. code python    execute() is called on object, which is not a Connection() object   insert(user).values(user_id1, user_nameFull Name).execute()Optimal connection-bounded use.. code python   with engine.begin() as connection        execute() is called on a Connection() object       connection.execute(user.insert(), {user_id 1, user_name Full Name})Connectionless way of use is also deprecated since SQLAlchemy 2.0 andsoon will be removed (see in SQLAlchemydocs httpsdocs.sqlalchemy.orgen14coreconnections.htmlconnectionless-execution-implicit-execution__).Running tests-------------Spanner dialect includes a compliance, migration and unit test suite. Torun the tests the nox package commands can be used    Run the whole suite    nox    Run a particular test session    nox -s migration_testRunning tests on Spanner emulatorThe dialect test suite can be runned on Spanneremulator httpscloud.google.comspannerdocsemulator__. Severaltests, relating to NULL values of data types, are skipped whenexecuted on emulator.Contributing------------Contributions to this library are welcome and encouraged. Please reportissues, file feature requests, and send pull requests. SeeCONTRIBUTING httpsgithub.comgoogleapispython-spanner-sqlalchemyblobmaincontributing.md__for more information on how to get started.Note that this project is not officially supported by Google as partof the Cloud Spanner product.Please note that this project is released with a Contributor Code ofConduct. By participating in this project you agree to abide by itsterms. See the Code ofConduct httpsgithub.comgoogleapispython-spanner-sqlalchemyblobmaincode-of-conduct.md__for more information.</longdescription>
</pkgmetadata>