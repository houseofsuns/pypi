<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>============flake8-raise============A `flake8 &lt;https://flake8.readthedocs.io/&gt;`_ plugin that finds improvements forraise statements.Installation------------Install using ``pip``:.. code-block:: sh    $ pip install flake8-raiseWhen installed, the plugin will automatically be used by ``flake8``. To checkit is installed correctly, run ``flake8 --version`` and look in the list ofinstalled plugins:.. code-block:: sh    $ flake8 --version    3.7.9 (flake8-raise: 0.0.5, mccabe: 0.6.1, pycodestyle: 2.5.0, pyflakes: 2.1.1) CPython 3.8.1 on LinuxRules-----==== ====Code Rule==== ====R100 ``raise`` in ``except`` handler without ``from``R101 use bare ``raise`` in ``except`` handlerR102 unnecessary parentheses on raised exception==== ====Examples--------R100 ``raise`` in ``except`` handler without ``from``~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~.. code-block:: py    try:        foo[&quot;bar&quot;]    except KeyError:        raise MyExceptionWill result in the error:.. code-block:: text    R100 raise in except handler without from.To fix, change to:.. code-block:: py    try:        foo['bar']    except KeyError as e:        raise MyException from eR101 use bare ``raise`` in ``except`` handler~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~.. code-block:: py    try:        foo[&quot;bar&quot;]    except KeyError as e:        raise eWill result in the error:.. code-block:: text    R101 Use bare raise in except handler.To fix, change to:.. code-block:: py    try:        foo['bar']    except KeyError:        raiseR102 unnecessary parentheses on raised exception~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~.. code-block:: py    raise TypeError()Will result in the error:.. code-block:: text    R102 unnecessary parentheses on raised exceptionTo fix, change to:.. code-block:: py    raise TypeError</longdescription>
</pkgmetadata>