<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>View Manipulation for SQLAlchemy================================Adds ``CreateView`` and ``DropView`` constructs to SQLAlchemy.Status------Current version is 0.3.2; releases are infrequent as the code here is small and based on stable pieces of the SQLAlchemy API, so there has been minimal maintenance required so far to keep compatibility with new SQLAlchemy or python versions.This package is working with latest Python and SQLAlchemy (1.0+ and 2.0) as of early 2023.Usage-----Examples:    &gt;&gt;&gt; from sqlalchemy import Table, MetaData    &gt;&gt;&gt; from sqlalchemy.sql import text    &gt;&gt;&gt; from sqlalchemy_views import CreateView, DropView    &gt;&gt;&gt; view = Table('my_view', MetaData())    &gt;&gt;&gt; definition = text(&quot;SELECT * FROM my_table&quot;)    &gt;&gt;&gt; create_view = CreateView(view, definition)    &gt;&gt;&gt; print(str(create_view.compile()).strip())    CREATE VIEW my_view AS SELECT * FROM my_table    &gt;&gt;&gt; create_view = CreateView(view, definition, or_replace=True)    &gt;&gt;&gt; print(str(create_view.compile()).strip())    CREATE OR REPLACE VIEW my_view AS SELECT * FROM my_table    &gt;&gt;&gt; create_view = CreateView(view, definition, options={'check_option': 'local'})    &gt;&gt;&gt; print(str(create_view.compile()).strip())    CREATE VIEW my_view WITH (check_option=local) AS SELECT * FROM my_table    &gt;&gt;&gt; drop_view = DropView(view)    &gt;&gt;&gt; print(str(drop_view.compile()).strip())    DROP VIEW my_view    &gt;&gt;&gt; drop_view = DropView(view, if_exists=True, cascade=True)    &gt;&gt;&gt; print(str(drop_view.compile()).strip())    DROP VIEW IF EXISTS my_view CASCADENote that the SQLAlchemy ``Table`` object is used to representboth tables and views. To introspect a view, create a ``Table``with ``autoload=True``, and then use SQLAlchemy's``get_view_definition`` method to generate the secondargument to ``CreateView``.Installation------------``sqlalchemy-views`` is available on PyPI and can be installed via ``pip`` ::    pip install sqlalchemy-viewsLimitations-----------Various SQL dialects have developed custom``CREATE VIEW`` and ``DROP VIEW`` syntax.This project aims to provide the core set of functionalityshared by most database engines.Acknowledgements----------------Some design ideas taken from the`SQLAlchemy Usage Recipe for views &lt;https://bitbucket.org/zzzeek/sqlalchemy/wiki/UsageRecipes/Views&gt;`_.Package structure is based on`python-project-template &lt;https://github.com/seanfisk/python-project-template&gt;`_.And thank you to the various`contributors &lt;https://github.com/jklukas/sqlalchemy-views/graphs/contributors&gt;`_!</longdescription>
</pkgmetadata>