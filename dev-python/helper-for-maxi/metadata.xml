<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># Helper for MaxiAll the Functions I could need multiple times## Instructions1. Install:```pip install helper-for-maxi```2. Import package:```pythonfrom helper import *ORimport helperhelper.*# You can replace * with what you need!``` ## Currently Implemented Classes / FunctionsAppendClipboard()```pythonimport helperhelper.AppendClipboard(text: str)ORfrom helper import AppendClipboardAppendClipboard(text: str)# copies text## Copies / Appends the given text to the windows clipboard using the utf-8 encoding## Parameters# ----------# text: str#     The text that is copied / appended to the clipboard```BetterColorInput()```pythonimport helperhelper.BetterColorInput(text: str, delay: float = None)ORfrom helper import BetterColorInputBetterInput(text: str, end: str = None, delay: float = None)# Gets an input# # Prints the given text letter by letter using the specified delay and gets an input() in cyan after# # Parameters# ----------# text: Optional[str]#     The text that is printed letter by letter#     DEFAULT: None# # delay: Optional[float]#     Changes the time between each printed letter#     DEFAULT: .01# # beforeColor: Optional[Terminal.color.foreground]#     The color to change back to after getting the input#     DEFAULT: None```BetterInput()```pythonimport helperhelper.BetterInput(text: str, end: str = None, delay: float = None)ORfrom helper import BetterInputBetterInput(text: str, end: str = None, delay: float = None)# Gets an input# # Prints the given text letter by letter using the specified delay and gets an input() after# # Parameters# ----------# text: Optional[str]#     The text that is printed letter by letter#     DEFAULT: None# # end: Optional[str]#     The text that is passed into the input() statement. #     Be aware that this part is not printed letter by letter.#     DEFAULT: None# # delay: Optional[float]#     Changes the time between each printed letter#     DEFAULT: .01```BetterPrint()```pythonimport helperhelper.BetterPrint(text: str, delay: float = .01)ORfrom helper import BetterPrintBetterPrint(text: str, delay: float = .01)# Prints text# # Prints the given text letter by letter to the command line using the specified delay# # Parameters# ----------# text: str#     The text that is printed letter by letter# # delay: Optional[float]#     Changes the time between each printed letter#     DEFAULT: .01# # newLine: Optional[bool]#     whether to add a new line at the end or not#     DEFAULT: True```ColorInput()```pythonimport helperhelper.ColorInput(text: str = None, beforeColor: Terminal.color.foreground = None)ORfrom helper import ColorInputColorInput(text: str = None, beforeColor: Terminal.color.foreground = None)# Gets an input# # executes the input() function in cyan and changes the color back to beforeColor if given# # Parameters# ----------# text: Optional[str]#     The text that is printed before getting the input#     DEFAULT: None# beforeColor: Optional[Terminal.color.foreground]#     The color that was used before (if u want it to change back)#     DEFAULT: None```Output()```pythonimport helperhelper.Output(outputValue)ORfrom helper import OutputOutput(outputValue)# Outputs to stdout# # Outputs the given outputValue to stdout and flushes the stream after# # Parameters# ----------# outputValue: Any#     The value that's written to stdout```Terminal```pythonimport helperhelper.Terminal._  # _ = subclassORfrom helper import TerminalTerminal._         # _ = subclass# Functions &amp; Stuff for the Windows Terminal# # Contains classes and Functions to use for/in the Windows Terminal# # Subclasses# ----------# color:#     returns escape sequences to manipulate the color of the Terminal when printed```</longdescription>
</pkgmetadata>