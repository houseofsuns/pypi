<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Django-RQBuild StatusDjango integration with RQ httpsgithub.comnvierq__, a Redis httpredis.io__based Python queuing library. Django-RQ httpsgithub.comrqdjango-rq__ is asimple app that allows you to configure your queues in djangos settings.pyand easily use them in your project.Support Django-RQIf you find django-rq useful, please consider supporting its development via Tidelift httpstidelift.comsubscriptionpkgpypi-django_rqutm_sourcepypi-django-rqutm_mediumreferralutm_campaignreadme_.Requirements Django httpswww.djangoproject.com__ (2.0+) RQ httpsgithub.comnvierq__Installation Install django-rq (or download from PyPI httppypi.python.orgpypidjango-rq__).. code-block python    pip install django-rq Add django_rq to INSTALLED_APPS in settings.py.. code-block python    INSTALLED_APPS  (         other apps        django_rq,    ) Configure your queues in djangos settings.py.. code-block python    RQ_QUEUES  {        default {            HOST localhost,            PORT 6379,            DB 0,            PASSWORD some-password,            DEFAULT_TIMEOUT 360,        },        with-sentinel {            SENTINELS [(localhost, 26736), (localhost, 26737)],            MASTER_NAME redismaster,            DB 0,            PASSWORD secret,            SOCKET_TIMEOUT None,            CONNECTION_KWARGS {                socket_connect_timeout 0.3            },        },        high {            URL os.getenv(REDISTOGO_URL, redislocalhost63790),  If youre on Heroku            DEFAULT_TIMEOUT 500,        },        low {            HOST localhost,            PORT 6379,            DB 0,        }    }    RQ_EXCEPTION_HANDLERS  [path.to.my.handler]  If you need custom exception handlers Include django_rq.urls in your urls.py.. code-block python    urlpatterns + [        path(django-rq, include(django_rq.urls))    ]UsagePutting jobs in the queue-------------------------Django-RQ allows you to easily put jobs into any of the queues defined insettings.py. It comes with a few utility functions enqueue - push a job to the default queue.. code-block python    import django_rq    django_rq.enqueue(func, foo, barbaz) get_queue - returns an Queue instance... code-block python    import django_rq    queue  django_rq.get_queue(high)    queue.enqueue(func, foo, barbaz)In addition to name argument, get_queue also accepts default_timeout,is_async, autocommit, connection and queue_class arguments. For example.. code-block python    queue  django_rq.get_queue(default, autocommitTrue, is_asyncTrue, default_timeout360)    queue.enqueue(func, foo, barbaz)You can provide your own singleton Redis connection object to this function so that it will notcreate a new connection object for each queue definition. This will help you limitnumber of connections to Redis server. For example.. code-block python    import django_rq    import redis    redis_cursor  redis.StrictRedis(host, port, db, password)    high_queue  django_rq.get_queue(high, connectionredis_cursor)    low_queue  django_rq.get_queue(low, connectionredis_cursor) get_connection - accepts a single queue name argument (defaults to default)  and returns a connection to the queues Redis server.. code-block python    import django_rq    redis_conn  django_rq.get_connection(high) get_worker - accepts optional queue names and returns a new RQ  Worker instance for specified queues (or default queue).. code-block python    import django_rq    worker  django_rq.get_worker()  Returns a worker for default queue    worker.work()    worker  django_rq.get_worker(low, high)  Returns a worker for low and highjob decorator--------------To easily turn a callable into an RQ task, you can also use the jobdecorator that comes with django_rq.. code-block python    from django_rq import job    job    def long_running_func()        pass    long_running_func.delay()  Enqueue function in default queue    job(high)    def long_running_func()        pass    long_running_func.delay()  Enqueue function in high queueYou can pass in any arguments that RQs job decorator accepts.. code-block python    job(default, timeout3600)    def long_running_func()        pass    long_running_func.delay()  Enqueue function with a timeout of 3600 seconds.Its possible to specify default for result_ttl decorator keyword argumentvia DEFAULT_RESULT_TTL setting.. code-block python    RQ  {        DEFAULT_RESULT_TTL 5000,    }With this setting, job decorator will set result_ttl to 5000 unless itsspecified explicitly.Running workers---------------django_rq provides a management command that starts a worker for every queuespecified as arguments    python manage.py rqworker high default lowIf you want to run rqworker in burst mode, you can pass in the --burst flag    python manage.py rqworker high default low --burstIf you need to use custom worker, job or queue classes, it is best to use global settings(see Custom queue classes_ and Custom job and worker classes_). However, it is also possibleto override such settings with command line options as follows.To use a custom worker class, you can pass in the --worker-class flagwith the path to your worker    python manage.py rqworker high default low --worker-class path.to.GeventWorkerTo use a custom queue class, you can pass in the --queue-class flagwith the path to your queue class    python manage.py rqworker high default low --queue-class path.to.CustomQueueTo use a custom job class, provide --job-class flag.Support for scheduled jobs--------------------------With RQ 1.2.0. you can use built-in scheduler httpspython-rq.orgdocsscheduling__for your jobs. For example.. code-block python    from django_rq.queues import get_queue    queue  get_queue(default)    job  queue.enqueue_at(datetime(2020, 10, 10), func)If you are using built-in scheduler you have to start workers with scheduler support    python manage.py rqworker --with-schedulerAlternatively you can use RQ Scheduler httpsgithub.comuirq-scheduler__.After install you can also use the get_scheduler function to return aScheduler instance for queues defined in settings.pys RQ_QUEUES.For example.. code-block python    import django_rq    scheduler  django_rq.get_scheduler(default)    job  scheduler.enqueue_at(datetime(2020, 10, 10), func)You can also use the management command rqscheduler to start the scheduler    python manage.py rqschedulerSupport for django-redis and django-redis-cache-----------------------------------------------If you have django-redis httpsdjango-redis.readthedocs.org__ ordjango-redis-cache httpsgithub.comsebleierdjango-redis-cache__installed, you can instruct django_rq to use the same connection informationfrom your Redis cache. This has two advantages its DRY and it takes advantageof any optimization that may be going on in your cache setup (like usingconnection pooling or Hiredis httpsgithub.comredishiredis__.)To use configure it, use a dict with the key USE_REDIS_CACHE pointing to thename of the desired cache in your RQ_QUEUES dict. It goes without sayingthat the chosen cache must exist and use the Redis backend. See your respectiveRedis cache package docs for configuration instructions. Its also important topoint out that since the django-redis-cache ShardedClient splits the cacheover multiple Redis connections, it does not work.Here is an example settings fragment for django-redis.. code-block python    CACHES  {        redis-cache {            BACKEND redis_cache.cache.RedisCache,            LOCATION localhost63791,            OPTIONS {                CLIENT_CLASS django_redis.client.DefaultClient,                MAX_ENTRIES 5000,            },        },    }    RQ_QUEUES  {        high {            USE_REDIS_CACHE redis-cache,        },        low {            USE_REDIS_CACHE redis-cache,        },    }Queue Statistics----------------django_rq also provides a dashboard to monitor the status of your queues atdjango-rq (or whatever URL you set in your urls.py during installation.You can also add a link to this dashboard link in admin by addingRQ_SHOW_ADMIN_LINK  True in settings.py. Be careful though, this willoverride the default admin template so it may interfere with other apps thatmodifies the default admin template.These statistics are also available in JSON format viadjango-rqstats.json, which is accessible to staff members.If you need to access this view via otherHTTP clients (for monitoring purposes), you can define RQ_API_TOKEN and access it viadjango-rqstats.jsonAPI_TOKEN... image  demo-django-rq-json-dashboard.pngNote Statistics of scheduled jobs display jobs from RQ built-in scheduler httpspython-rq.orgdocsscheduling__,not optional RQ scheduler httpsgithub.comrqrq-scheduler__.Additionally, these statistics are also accessible from  the command line... code-block bash    python manage.py rqstats    python manage.py rqstats --interval1   Refreshes every second    python manage.py rqstats --json   Output as JSON    python manage.py rqstats --yaml   Output as YAML.. image demo-django-rq-cli-dashboard.gifConfiguring Sentry-------------------Django-RQ  2.0 uses sentry-sdk instead of the deprecated raven library. Sentryshould be configured within the Django settings.py as described in the Sentry docs httpsdocs.sentry.ioplatformspythondjango__.You can override the default Django Sentry configuration when running the rqworker commandby passing the sentry-dsn option.manage.py rqworker --sentry-dsnhttpssentry.io222222This will override any existing Django configuration and reinitialise Sentry,setting the following Sentry options.. code-block python    {        debug options.get(sentry_debug),        ca_certs options.get(sentry_ca_certs),        integrations [RedisIntegration(), RqIntegration(), DjangoIntegration()]    }Configuring Logging-------------------Starting from version 0.3.3, RQ uses Pythons logging, this meansyou can easily configure rqworkers logging mechanism in djangossettings.py. For example.. code-block python    LOGGING  {        version 1,        disable_existing_loggers False,        formatters {            rq_console {                format (asctime)s (message)s,                datefmt HMS,            },        },        handlers {            rq_console {                level DEBUG,                class rq.utils.ColorizingStreamHandler,                formatter rq_console,                exclude [(asctime)s],            },             If you use sentry for logging            sentry {                level ERROR,                class raven.contrib.django.handlers.SentryHandler,            },        },        loggers {            rq.worker {                handlers [rq_console, sentry],                level DEBUG            },        }    }Note error logging to Sentry is known to be unreliable with RQ when using asynctransports (the default transport). Please configure Raven to usesync+https or requests+https transport in settings.py.. code-block python    RAVEN_CONFIG  {        dsn sync+httpspublicsecretexample.com1,    }For more info, refer to Ravens documentation httpraven.readthedocs.org__.Custom Queue Classes--------------------By default, every queue will use DjangoRQ class. If you want to use a custom queue class, you can do soby adding a QUEUE_CLASS option on a per queue basis in RQ_QUEUES.. code-block python    RQ_QUEUES  {        default {            HOST localhost,            PORT 6379,            DB 0,            QUEUE_CLASS module.path.CustomClass,        }    }or you can specify DjangoRQ to use a custom class for all your queues in RQ settings.. code-block python    RQ  {        QUEUE_CLASS module.path.CustomClass,    }Custom queue classes should inherit from django_rq.queues.DjangoRQ.If you are using more than one queue class (not recommended), be sure to only run workerson queues with same queue class. For example if you have two queues defined in RQ_QUEUES andone has custom class specified, you would have to run at least two separate workers for eachqueue.Custom Job and Worker Classes-----------------------------Similarly to custom queue classes, global custom job and worker classes can be configured usingJOB_CLASS and WORKER_CLASS settings.. code-block python    RQ  {        JOB_CLASS module.path.CustomJobClass,        WORKER_CLASS module.path.CustomWorkerClass,    }Custom job class should inherit from rq.job.Job. It will be used for all jobsif configured.Custom worker class should inherit from rq.worker.Worker. It will be used for runningall workers unless overridden by rqworker management command worker-class option.Testing Tip-----------For an easier testing process, you can run a worker synchronously this way.. code-block python    from django.test import TestCase    from django_rq import get_worker    class MyTest(TestCase)        def test_something_that_creates_jobs(self)            ...                       Stuff that init jobs.            get_worker().work(burstTrue)   Processes all jobs then stop.            ...                       Asserts that the job stuff is done.Synchronous Mode----------------You can set the option ASYNC to False to make synchronous operation thedefault for a given queue. This will cause jobs to execute immediately and onthe same thread as they are dispatched, which is useful for testing anddebugging. For example, you might add the following after you queueconfiguration in your settings file.. code-block python     ... Logic to set DEBUG and TESTING settings to True or False ...     ... Regular RQ_QUEUES setup code ...    if DEBUG or TESTING        for queueConfig in RQ_QUEUES.values()            queueConfig[ASYNC]  FalseNote that setting the is_async parameter explicitly when calling get_queuewill override this setting.Running TestsTo run django_rqs test suite    which django-admin.py test django_rq --settingsdjango_rq.tests.settings --pythonpath.Deploying on UbuntuCreate an rqworker service that runs the high, default, and low queues.sudo vi etcsystemdsystemrqworker.service.. code-block bash    [Unit]    DescriptionDjango-RQ Worker    Afternetwork.target    [Service]    WorkingDirectorypath_to_your_project_folder    ExecStarthomeubuntu.virtualenvyour_virtualenvbinpython         path_to_your_project_foldermanage.py         rqworker high default low    [Install]    WantedBymulti-user.targetEnable and start the service.. code-block bash    sudo systemctl enable rqworker    sudo systemctl start rqworkerDeploying on HerokuAdd django-rq to your requirements.txt file with.. code-block bash    pip freeze  requirements.txtUpdate your Procfile to.. code-block bash    web gunicorn --pythonpathPWDyour_app_name config.wsgiapplication    worker python your_app_namemanage.py rqworker high default lowCommit and re-deploy. Then add your new worker with.. code-block bash    heroku scale worker1ChangelogSee CHANGELOG.md httpsgithub.comrqdjango-rqblobmasterCHANGELOG.md__... Build Status image httpssecure.travis-ci.orgrqdjango-rq.svgbranchmaster   target httpstravis-ci.orgrqdjango-rq</longdescription>
</pkgmetadata>