<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Pyroute2Pyroute2 is a pure Python netlink library. The core requires only Pythonstdlib, no 3rd party libraries. The library was started as an RTNL protocolimplementation, so the name is pyroute2, but now it supports many netlinkprotocols. Some supported netlink families and protocols rtnl, network settings --- addresses, routes, traffic controls nfnetlink --- netfilter API ipq --- simplest userspace packet filtering, iptables QUEUE target devlink --- manage and monitor devlink-enabled hardware generic --- generic netlink families uevent --- same uevent messages as in udevNetfilter API ipset --- IP sets nftables --- packet filtering nfct --- connection trackingGeneric netlink ethtool --- low-level network interface setup wireguard --- VPN setup nl80211 --- wireless functions API (basic support) taskstats --- extended process statistics acpi_events --- ACPI events monitoring thermal_events --- thermal events monitoring VFS_DQUOT --- disk quota events monitoringOn the low level the library provides socket objects with anextended API. The additional functionality aims to Help to openbind netlink sockets Discover generic netlink protocols and multicast groups Construct, encode and decode netlink and PF_ROUTE messagesSupported systems-----------------Pyroute2 runs natively on Linux and emulates some limited subsetof RTNL netlink API on BSD systems on top of PF_ROUTE notificationsand standard system tools.Other platforms are not supported.NDB -- high level RTNL API--------------------------Key features Data integrity Transactions with commitrollback changes State synchronization Multiple sources, including netns and remote systemsA Hello world example.. code-block python    from pyroute2 import NDB    with NDB() as ndb        with ndb.interfaces[eth0] as eth0             set one parameter            eth0.set(statedown)            eth0.commit()   make sure that the interface is down             or multiple parameters at once            eth0.set(ifnamehello_world, stateup)            eth0.commit()   rename, bring up and wait for success         -- -- here you can be sure that the interface is up  renamedMore examples.. code-block python    from pyroute2 import NDB    ndb  NDB(logdebug)    for record in ndb.interfaces.summary()        print(record.ifname, record.address, record.state)    if_dump  ndb.interfaces.dump()    if_dump.select_records(stateup)    if_dump.select_fields(index, ifname, kind)    for line in if_dump.format(json)        print(line)    addr_summary  nsb.addresses.summary()    addr_summary.select_records(ifnameeth0)    for line in addr_summary.format(csv)        print(line)    with ndb.interfaces.create(ifnamebr0, kindbridge) as br0        br0.add_port(eth0)        br0.add_port(eth1)        br0.add_ip(10.0.0.124)        br0.add_ip(192.168.0.124)        br0.set(            br_stp_state1,   set STP on            br_group_fwd_mask0x4000,   set LLDP forwarding            stateup,   bring the interface up        )     -- -- commit() will be run by the context manager     operate on netns    ndb.sources.add(netnstestns)   connect to a namespace    with (        ndb.interfaces.create(            ifnameveth0,   create veth            kindveth,            peer{                ifname eth0,   setup peer                net_ns_fd testns,   in a namespace            },            stateup,        )    ) as veth0        veth0.add_ip(address172.16.230.1, prefixlen24)    with ndb.interfaces.wait(        targettestns, ifnameeth0    ) as peer   wait for the peer        peer.set(stateup)   bring it up        peer.add_ip(172.16.230.224)   add addressIPRoute -- Low level RTNL API-----------------------------Low-level IPRoute utility --- Linux network configuration.The IPRoute class is a 1-to-1 RTNL mapping. There are no implicitinterface lookups and so on.Get notifications about network settings changes with IPRoute.. code-block python    from pyroute2 import IPRoute    with IPRoute() as ipr         With IPRoute objects you have to call bind() manually        ipr.bind()        for message in ipr.get()            print(message)More examples.. code-block python    from socket import AF_INET    from pyroute2 import IPRoute     get access to the netlink socket    ipr  IPRoute()     no monitoring here -- thus no bind()     print interfaces    for link in ipr.get_links()        print(link)     create VETH pair and move v0p1 to netns test    ipr.link(add, ifnamev0p0, peerv0p1, kindveth)     wait for the devices    peer, veth  ipr.poll(        ipr.link, dump, timeout5, ifnamelambda x x in (v0p0, v0p1)    )    ipr.link(set, indexpeer[index], net_ns_fdtest)     bring v0p0 up and add an address    ipr.link(set, indexveth[index], stateup)    ipr.addr(add, indexveth[index], address10.0.0.1, prefixlen24)     release Netlink socket    ip.close()Network namespace examples--------------------------Network namespace manipulation.. code-block python    from pyroute2 import netns     create netns    netns.create(test)     list    print(netns.listnetns())     remove netns    netns.remove(test)Create veth interfaces pair and move to netns.. code-block python    from pyroute2 import IPRoute    with IPRoute() as ipr         create interface pair        ipr.link(add, ifnamev0p0, kindveth,  peerv0p1)         wait for the peer        (peer,)  ipr.poll(ipr.link, dump, timeout5, ifnamev0p1)         move the peer to the test netns        ipr.link(set, indexpeer[index], net_ns_fdtest)List interfaces in some netns.. code-block python    from pyroute2 import NetNS    from pprint import pprint    ns  NetNS(test)    pprint(ns.get_links())    ns.close()More details and samples see in the documentation.Installation------------Using pypi.. code-block bash    pip install pyroute2Using git.. code-block bash    pip install git+httpsgithub.comsvinotapyroute2.gitUsing source, requires make and nox.. code-block bash    git clone httpsgithub.comsvinotapyroute2.git    cd pyroute2    make installRequirements------------Python  3.6Links----- home httpspyroute2.org source httpsgithub.comsvinotapyroute2 bugs httpsgithub.comsvinotapyroute2issues pypi httpspypi.python.orgpypipyroute2 docs httpdocs.pyroute2.org</longdescription>
</pkgmetadata>