<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> inotify_simple 1.3inotify_simple is a simple Python wrapper around[inotify](httpman7.orglinuxman-pagesman7inotify.7.html).No fancy bells and whistles, just a literal wrapper with ctypes. Only  100lines of codeinotify_init1() is wrapped as a file-like object, INotify(), holding the inotifyfile descriptor. INotify().read() reads available data from the file descriptor andreturns events as namedtuple objects after unpacking them with the struct module.inotify_add_watch() and inotify_rm_watch() are wrapped with no changes at all,taking and returning watch descriptor integers that calling code is expected to keeptrack of itself, just as one would use inotify from C. Works with Python 2.7 andPython  3.2.[View on PyPI](httppypi.python.orgpypiinotify_simple) [Fork me on github](httpsgithub.comchrisjbillingtoninotify_simple) [Read the docs](httpinotify_simple.readthedocs.org) Installationto install inotify_simple, run pip3 install inotify_simpleor to install from source python3 setup.py installNote  If on Python  3.4, youll need the backported [enum34module](httpspypi.python.orgpypienum34). IntroductionThere are many inotify python wrappers out there. [I found them allunsatisfactory](httpsxkcd.com927). Most are far too high-level for mytastes, and the supposed convenience they provide actually limits one fromusing inotify in ways other than those the author imagined. Others are Cextensions, requiring compilation for different platforms and Python versions,rather than a pure python module using ctypes. This one is pretty low-leveland really just does what inotify itself does and nothing more. So hopefullyif Ive written it right, it will remain functional well into the future withno changes, recompilation or attention on my part. Example usagepythonimport osfrom inotify_simple import INotify, flagsos.mkdir(tmpinotify_test)inotify  INotify()watch_flags  flags.CREATE  flags.DELETE  flags.MODIFY  flags.DELETE_SELFwd  inotify.add_watch(tmpinotify_test, watch_flags) Now create, delete and modify some files in the directory being monitoredos.chdir(tmpinotify_test) CREATE event for a directoryos.system(mkdir foo) CREATE event for a fileos.system(echo hello  test.txt) MODIFY event for the fileos.system(echo world  test.txt) DELETE event for the fileos.system(rm test.txt) DELETE event for the directoryos.system(rmdir foo)os.chdir(tmp) DELETE_SELF on the original directory.  Also generates an IGNORED event indicating the watch was removed.os.system(rmdir inotify_test) And see the corresponding eventsfor event in inotify.read()    print(event)    for flag in flags.from_mask(event.mask)        print(     + str(flag))This outputs the followingEvent(wd1, mask1073742080, cookie0, nameufoo)    flags.CREATE    flags.ISDIREvent(wd1, mask256, cookie0, nameutest.txt)    flags.CREATEEvent(wd1, mask2, cookie0, nameutest.txt)    flags.MODIFYEvent(wd1, mask512, cookie0, nameutest.txt)    flags.DELETEEvent(wd1, mask1073742336, cookie0, nameufoo)    flags.DELETE    flags.ISDIREvent(wd1, mask1024, cookie0, nameu)    flags.DELETE_SELFEvent(wd1, mask32768, cookie0, nameu)    flags.IGNOREDNote that the flags, since they are defined with an enum.IntEnum, print aswhat they are called rather than their integer values. However they are stilljust integers and so can be bitwise-ANDed and ORed etc with masks etc. Theflags.from_mask() method bitwise-ANDs a mask with all possible flags andreturns a list of matches. This is for convenience and useful for debuggingwhich events are coming through, but performance critical code shouldgenerally bitwise-AND masks with flags of interest itself so as to not dounnecessary checks.[See here](httpinotify_simple.readthedocs.org) for more.</longdescription>
</pkgmetadata>