<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>glog for Python.. image httpstravis-ci.orgbenleypython-glog.svgbranchmaster    target httpstravis-ci.orgbenleypython-glogA simple Google-style logging wrapper for Python.This library attempts to greatly simplify logging in Python applications.Nobody wants to spend hours pouring over the PEP 282 logger documentation, andalmost nobody actually needs things like loggers that can be reconfigured overthe network.  We just want to get on with writing our apps.Styled somewhat after the twitter.common.log_ interface, which in turn wasmodeled after Googles internal python logger, which was never actuallyreleased_ to the wild, and which in turn was based on the C++ glog library_.Core benefits-------------- You and your code dont need to care about how logging works. Unless you  want to, of course.- No more complicated setup boilerplate- Your apps and scripts will all have a consistent log format, and the same  predictable behaviours.This library configures the root logger, so nearly everything you import thatuses the standard Python logging module will play along nicely.Behaviours-----------  Messages are always written to stderr.-  Lines are prefixed with a google-style log prefix, of the formE0924 221915.123456 19552 filename.py87] Log message blah blahSplitting on spaces, the fields are1. The first character is the log level, followed by MMDD (month, day)2. HHMMSS.microseconds3. Process ID4. basename_of_sourcefile.pylinenumber]5. The body of the log message.Example use-----------.. code python    import glog as log    log.setLevel(INFO)   Integer levels are also allowed.    log.info(It works.)    log.warn(Something not ideal)    log.error(Something went wrong)    log.fatal(AAAAAAAAAAAAAAA)If your app uses gflags_, it will automatically gain a --verbosity flag,and you can skip calling log.setLevel.  Just import glog and start logging.Check macros  assert helpers-----------------------------Like the C++ version of glog_, python-glog provides a set of check macros[1]_ that help document and enforce invariants.  These provide a detailedmessage indicating what values caused the assertion to fail, along with a stacktrace identifying the code-path that caused the failure, hopefully making iteasier to reproduce the error.  Failed checks raise the FailedCheckException.You may find these more convenient andor more familiar than standard Pythonasserts, particularly if you are working in a mixed C++ and Python codebase... code python    import glog as log    import math    def compute_something(a)        log.check_eq(type(a), float)  require floating point types        log.check_ge(a, 0)  require non-negative values        value  math.sqrt(a)        return value    if __name__  __main__        compute_something(10)Provided check functions.. code python    check(condition)    check_eq(obj1, obj2)    check_ne(obj1, obj2)    check_le(obj1, obj2)    check_ge(obj1, obj2)    check_lt(obj1, obj2)    check_gt(obj1, obj2)    check_notnone(obj1, obj2)Happy logging.. _twitter.common.log httpsgithub.comtwittercommonstreemastersrcpythontwittercommonlog.. _never actually released httpsgroups.google.comdmsggoogle-gloga_JcyJ4p8MQXu-vDPiuCCYJ.. _C++ glog library httpsgithub.comgoogleglog.. _gflags httpsgithub.comgooglepython-gflags.. _Like the C++ version of glog httpshtmlpreview.github.iohttpsgithub.comgoogleglogmasterdocglog.htmlcheck.. [1] Technically these are functions, not macros.  Python does not have   syntactic macros in the sense that C++ and most lisp-like languages do.</longdescription>
</pkgmetadata>