<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>xenon.. image httpsimg.shields.iotravisrubikxenonmaster.svg    alt Travis-CI badge    target httpstravis-ci.orgrubikxenon.. image httpsimg.shields.iocoverallsrubikxenonmaster.svg    alt Coveralls badge    target httpscoveralls.iorrubikxenonbranchmaster.. image httpsimg.shields.iopypivxenon.svg    alt PyPI latest version badge    target httpspypi.python.orgpypixenon.. image httpsimg.shields.iopypiformatxenon.svg    alt Download format    target httppythonwheels.com.. image httpsimg.shields.iopypilxenon.svg    alt Xenon license    target httpspypi.python.orgpypixenon----Xenon is a monitoring tool based on Radon httpsgithub.comrubikradon_.It monitors your codes complexity.  Ideally, Xenon is run every time youcommit code. Through command line options, you can set various thresholds forthe complexity of your code. It will fail (i.e. it will exit with anon-zero exit code) when any of these requirements is not met.Installation------------With Pip.. code-block sh    pip install xenonOr download the source and run the setup file (requires setuptools).. code-block sh    python setup.py installXenon is tested with all versions of Python from 2.7 to 3.6 as well asPyPy.Usage-----Typically you would use Xenon in two scenarios1. As a git commit hook to make sure that your code never exceeds some   complexity values.2. On a continuous integration server as a part of your build, to keep   under control, as above, your codes complexity. See Xenons   .travis.yml file_ for an example usage.The command line++++++++++++++++Everything boils down to Xenons command line usage.To control which files are analyzed, you use the options -e, --exclude and-i, --ignore. Both accept a comma-separated list of glob patterns. Thevalue usually needs quoting at the command line, to prevent the shell fromexpanding the pattern (in case there is only one). Every filename is matchedagainst the exclude patterns. Every directory name is matched against theignore patterns.  If any of the patterns matches, Xenon wont even descendinto them.The actual threshold values are defined through these options -a, --max-average Threshold for the average complexity (across all the  codebase). -m, --max-modules Threshold for modules complexity. -b, --max-absolute Absolute threshold for block complexity.All of these options are inclusive.An actual example+++++++++++++++++.. code-block sh    xenon --max-absolute B --max-modules A --max-average Aor, more succinctly.. code-block sh    xenon -b B -m A -a AWith these options Xenon will exit with a non-zero exit code if any of thefollowing conditions is met At least one block has a rank higher than B (i.e. C, D, E or  F). At least one module has a rank higher than A. The average complexity (among all of the analyzed blocks) is ranked with  B or higher.Other resources---------------For more information regarding cyclomatic complexity and static analysis inPython, please refer to Radons documentation, the project on which Xenon isbased on More on cyclomatic complexity  httpradon.readthedocs.orgenlatestintro.html More on Radons ranking  httpradon.readthedocs.orgenlatestcommandline.htmlthe-cc-command.. _.travis.yml file httpsgithub.comrubikxenonblobmaster.travis.yml</longdescription>
</pkgmetadata>