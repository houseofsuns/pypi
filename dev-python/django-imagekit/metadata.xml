<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Build Status_.. Build Status image httpsgithub.commatthewwithanmdjango-imagekitactionsworkflowspython.ymlbadge.svgbranchdevelop.. _Build Status httpsgithub.commatthewwithanmdjango-imagekitactionsworkflowspython.ymlImageKit is a Django app for processing images. Need a thumbnail Ablack-and-white version of a user-uploaded image ImageKit will make them foryou. If you need to programmatically generate one image from another, you needImageKit.ImageKit comes with a bunch of image processors for common tasks like resizingand cropping, but you can also create your own. For an idea of whats possible,check out the Instakit__ project.For the complete documentation on the latest stable version of ImageKit, seeImageKit on RTD_... _ImageKit on RTD httpdjango-imagekit.readthedocs.org__ httpsgithub.comfish2000instakitInstallation1. Install Pillow_. (If youre using an ImageField in Django,   you should have already done this.)2. pip install django-imagekit3. Add imagekit to your INSTALLED_APPS list in your projects settings.py.. _Pillow httppypi.python.orgpypiPillowUsage Overview.. _specsSpecs-----You have one image and you want to do something to it to create another image.But how do you tell ImageKit what to do By defining an image spec.An image spec is a type of image generator that generates a new imagefrom a source image.Defining Specs In ModelsThe easiest way to use define an image spec is by using an ImageSpecField onyour model class.. code-block python    from django.db import models    from imagekit.models import ImageSpecField    from imagekit.processors import ResizeToFill    class Profile(models.Model)        avatar  models.ImageField(upload_toavatars)        avatar_thumbnail  ImageSpecField(sourceavatar,                                          processors[ResizeToFill(100, 50)],                                          formatJPEG,                                          options{quality 60})    profile  Profile.objects.all()[0]    print(profile.avatar_thumbnail.url)      mediaCACHEimages982d5af84cddddfd0fbf70892b4431e4.jpg    print(profile.avatar_thumbnail.width)    100As you can probably tell, ImageSpecFields work a lot like DjangosImageFields. The difference is that theyre automatically generated byImageKit based on the instructions you give. In the example above, the avatarthumbnail is a resized version of the avatar image, saved as a JPEG with aquality of 60.Sometimes, however, you dont need to keep the original image (the avatar inthe above example) when the user uploads an image, you just want to process itand save the result. In those cases, you can use the ProcessedImageFieldclass.. code-block python    from django.db import models    from imagekit.models import ProcessedImageField    from imagekit.processors import ResizeToFill    class Profile(models.Model)        avatar_thumbnail  ProcessedImageField(upload_toavatars,                                               processors[ResizeToFill(100, 50)],                                               formatJPEG,                                               options{quality 60})    profile  Profile.objects.all()[0]    print(profile.avatar_thumbnail.url)      mediaavatarsMY-avatar.jpg    print(profile.avatar_thumbnail.width)    100This is pretty similar to our previous example. We dont need to specify asource any more since were not processing another image field, but we do needto pass an upload_to argument. This behaves exactly as it does for DjangoImageFields... note    You might be wondering why we didnt need an upload_to argument for our    ImageSpecField. The reason is that ProcessedImageFields really are just like    ImageFieldsthey save the file path in the database and you need to run    syncdb (or create a migration) when you add one to your model.    ImageSpecFields, on the other hand, are virtualthey add no fields to your    database and dont require a database. This is handy for a lot of reasons,    but it means that the path to the image file needs to be programmatically    constructed based on the source image and the spec.Defining Specs Outside of ModelsDefining specs as models fields is one very convenient way to process images,but it isnt the only way. Sometimes you cant (or dont want to) add fields toyour models, and thats okay. You can define image spec classes and use themdirectly. This can be especially useful for doing image processing in viewsparticularly when the processing being done depends on user input... code-block python    from imagekit import ImageSpec    from imagekit.processors import ResizeToFill    class Thumbnail(ImageSpec)        processors  [ResizeToFill(100, 50)]        format  JPEG        options  {quality 60}Its probably not surprising that this class is capable of processing an imagein the exact same way as our ImageSpecField above. However, unlike with theimage spec model field, this class doesnt define what source the spec is actingon, or what should be done with the result thats up to you.. code-block python    source_file  open(pathtomyimage.jpg, rb)    image_generator  Thumbnail(sourcesource_file)    result  image_generator.generate().. note    You dont have to use open You can use whatever File-like object you    wantincluding a models ImageField.The result of calling generate() on an image spec is a file-like objectcontaining our resized image, with which you can do whatever you want. Forexample, if you wanted to save it to disk.. code-block python    dest  open(pathtodest.jpg, wb)    dest.write(result.read())    dest.close()Using Specs In TemplatesIf you have a model with an ImageSpecField or ProcessedImageField, you caneasily use those processed image just as you would a normal image field.. code-block html    img src{{ profile.avatar_thumbnail.url }} (This is assuming you have a view thats setting a context variable namedprofile to an instance of our Profile model.)But you can also generate processed image files directly in your templatefromany imagewithout adding anything to your model. In order to do this, youllfirst have to define an image generator class (remember, specs are a type ofgenerator) in your app somewhere, just as we did in the last section. Youllalso need a way of referring to the generator in your template, so youll needto register it... code-block python    from imagekit import ImageSpec, register    from imagekit.processors import ResizeToFill    class Thumbnail(ImageSpec)        processors  [ResizeToFill(100, 50)]        format  JPEG        options  {quality 60}    register.generator(myappthumbnail, Thumbnail).. note    You can register your generator with any id you want, but choose wisely    If you pick something too generic, you could have a conflict with another    third-party app youre using. For this reason, its a good idea to prefix    your generator ids with the name of your app. Also, ImageKit recognizes    colons as separators when doing pattern matching (e.g. in the generateimages    management command), so its a good idea to use those too.. warning    This code can go in any file you wantbut you need to make sure its loaded    In order to keep things simple, ImageKit will automatically try to load an    module named imagegenerators in each of your installed apps. So why dont    you just save yourself the headache and put your image specs in thereNow that weve created an image generator class and registered it with ImageKit,we can use it in our templatesgenerateimageThe most generic template tag that ImageKit gives you is called generateimage.It requires at least one argument the id of a registered image generator.Additional keyword-style arguments are passed to the registered generator class.As we saw above, image spec constructors expect a source keyword argument, sothats what we need to pass to use our thumbnail spec.. code-block html    { load imagekit }    { generateimage myappthumbnail sourcesource_file }This will output the following HTML.. code-block html    img srcmediaCACHEimages982d5af84cddddfd0fbf70892b4431e4.jpg width100 height50 You can also add additional HTML attributes just separate them from yourkeyword args using two dashes.. code-block html    { load imagekit }    { generateimage myappthumbnail sourcesource_file -- altA picture of Me idmypicture }Not generating HTML image tags No problem. The tag also functions as anassignment tag, providing access to the underlying file object.. code-block html    { load imagekit }    { generateimage myappthumbnail sourcesource_file as th }    a href{{ th.url }}Click to download a cool {{ th.width }} x {{ th.height }} imageathumbnailBecause its such a common use case, ImageKit also provides a thumbnailtemplate tag.. code-block html    { load imagekit }    { thumbnail 100x50 source_file }Like the generateimage tag, the thumbnail tag outputs an img tag.. code-block html    img srcmediaCACHEimages982d5af84cddddfd0fbf70892b4431e4.jpg width100 height50 Comparing this syntax to the generateimage tag above, youll notice a fewdifferences.First, we didnt have to specify an image generator id unless we tell itotherwise, thumbnail tag uses the generator registered with the idimagekitthumbnail. Its important to note that this tag is not using theThumbnail spec class we defined earlier its using the generator registeredwith the id imagekitthumbnail which, by default, isimagekit.generatorlibrary.Thumbnail.Second, were passing two positional arguments (the dimensions and the sourceimage) as opposed to the keyword arguments we used with the generateimage tag.Like with the generateimage tag, you can also specify additional HTML attributesfor the thumbnail tag, or use it as an assignment tag.. code-block html    { load imagekit }    { thumbnail 100x50 source_file -- altA picture of Me idmypicture }    { thumbnail 100x50 source_file as th }Using Specs in FormsIn addition to the model field above, theres also a form field version of theProcessedImageField class. The functionality is basically the same (itprocesses an image once and saves the result), but its used in a form class.. code-block python    from django import forms    from imagekit.forms import ProcessedImageField    from imagekit.processors import ResizeToFill    class ProfileForm(forms.Form)        avatar_thumbnail  ProcessedImageField(spec_idmyappprofileavatar_thumbnail,                                               processors[ResizeToFill(100, 50)],                                               formatJPEG,                                               options{quality 60})The benefit of using imagekit.forms.ProcessedImageField (as opposed toimagekit.models.ProcessedImageField above) is that it keeps the logic forcreating the image outside of your model (in which you would use a normal DjangoImageField). You can even create multiple forms, each with their ownProcessedImageField, that all store their results in the same image field.Processors----------So far, weve only seen one processor imagekit.processors.ResizeToFill. ButImageKit is capable of far more than just resizing images, and that power comesfrom its processors.Processors take a PIL image object, do something to it, and return a new one.A spec can make use of as many processors as youd like, which will all be runin order... code-block python    from imagekit import ImageSpec    from imagekit.processors import TrimBorderColor, Adjust    class MySpec(ImageSpec)        processors  [            TrimBorderColor(),            Adjust(contrast1.2, sharpness1.1),        ]        format  JPEG        options  {quality 60}The imagekit.processors module contains processors for many commonimage manipulations, like resizing, rotating, and color adjustments. However,if they arent up to the task, you can create your own. All you have to do isdefine a class that implements a process() method.. code-block python    class Watermark(object)        def process(self, image)             Code for adding the watermark goes here.            return imageThats all there is to it To use your fancy new custom processor, just includeit in your specs processors list.. code-block python    from imagekit import ImageSpec    from imagekit.processors import TrimBorderColor, Adjust    from myapp.processors import Watermark    class MySpec(ImageSpec)        processors  [            TrimBorderColor(),            Adjust(contrast1.2, sharpness1.1),            Watermark(),        ]        format  JPEG        options  {quality 60}Note that when you import a processor from imagekit.processors, imagekitin turn imports the processor from PILKit_. So if you are looking foravailable processors, look at PILKit... _PILKit httpsgithub.commatthewwithanmpilkitAdmin-----ImageKit also contains a class named imagekit.admin.AdminThumbnailfor displaying specs (or even regular ImageFields) in theDjango admin change list_. AdminThumbnail is used as a property onDjango admin classes.. code-block python    from django.contrib import admin    from imagekit.admin import AdminThumbnail    from .models import Photo    class PhotoAdmin(admin.ModelAdmin)        list_display  (__str__, admin_thumbnail)        admin_thumbnail  AdminThumbnail(image_fieldthumbnail)    admin.site.register(Photo, PhotoAdmin)To use specs defined outside of models.. code-block python        from django.contrib import admin    from imagekit.admin import AdminThumbnail    from imagekit import ImageSpec    from imagekit.processors import ResizeToFill    from imagekit.cachefiles import ImageCacheFile    from .models import Photo    class AdminThumbnailSpec(ImageSpec)        processors  [ResizeToFill(100, 30)]        format  JPEG        options  {quality 60 }    def cached_admin_thumb(instance)         image is the name of the image field on the model        cached  ImageCacheFile(AdminThumbnailSpec(instance.image))         only generates the first time, subsequent calls use cache        cached.generate()        return cached    class PhotoAdmin(admin.ModelAdmin)        list_display  (__str__, admin_thumbnail)        admin_thumbnail  AdminThumbnail(image_fieldcached_admin_thumb)    admin.site.register(Photo, PhotoAdmin) AdminThumbnail can even use a custom template. For more information, seeimagekit.admin.AdminThumbnail... _Django admin change list httpsdocs.djangoproject.comendevintrotutorial02customize-the-admin-change-listManagement Commands-------------------ImageKit has one management commandgenerateimageswhich will generate cachefiles for all of your registered image generators. You can also pass it a listof generator ids in order to generate images selectively.CommunityPlease use the GitHub issue tracker httpsgithub.commatthewwithanmdjango-imagekitissues_to report bugs with django-imagekit. A mailing list httpsgroups.google.comforumforumdjango-imagekit_also exists to discuss the project and ask questions, as well as the officialimagekit ircirc.freenode.netimagekit_ channel on Freenode.ContributingWe love contributions And you dont have to be an expert with the libraryoreven Djangoto contribute either ImageKits processors are standalone classesthat are completely separate from the more intimidating internals of DjangosORM. If youve written a processor that you think might be useful to otherpeople, open a pull request so we can take a lookYou can also check out our list of open, contributor-friendly issues__ forideas.Check out our contributing guidelines_ for more information about pitching inwith ImageKit.__ httpsgithub.commatthewwithanmdjango-imagekitissueslabelscontributor-friendlystateopen.. _contributing guidelines httpsgithub.commatthewwithanmdjango-imagekitblobdevelopCONTRIBUTING.rst</longdescription>
</pkgmetadata>