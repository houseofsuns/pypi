<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> python-zipstreamzipstream.py is a zip archive generator based on python 3.3s zipfile.py. It was created togenerate a zip file generator for streaming (ie web apps). This is beneficial for when youwant to provide a downloadable archive of a large collection of regular files, which would be infeasible togenerate the archive prior to downloading or of a very large file that you do not want to store entirely on disk or on memory.The archive is generated as an iterator of strings, which, when joined, formthe zip archive. For example, the following code snippet would write a ziparchive containing files from path to a normal filepythonimport zipstreamz  zipstream.ZipFile()z.write(pathtofiles)with open(zipfile.zip, wb) as f    for data in z        f.write(data)zipstream also allows to take as input a byte string iterable and to generatethe archive as an iterator.This avoids storing large files on disk or in memory.To do so you could use something like this snippetpythondef iterable()    for _ in xrange(10)        yield bthis is a byte stringx01nz  zipstream.ZipFile()z.write_iter(my_archive_iter, iterable())with open(zipfile.zip, wb) as f    for data in z        f.write(data)Of course both approach can be combinedpythondef iterable()    for _ in xrange(10)        yield bthis is a byte stringx01nz  zipstream.ZipFile()z.write(pathtofiles, my_archive_files)z.write_iter(my_archive_iter, iterable())with open(zipfile.zip, wb) as f    for data in z        f.write(data)Since recent versions of web.py support returning iterators of strings to besent to the browser, to download a dynamically generated archive, you coulduse something like this snippetpythondef GET(self)    path  pathtodiroffiles    zip_filename  files.zip    web.header(Content-type , applicationzip)    web.header(Content-Disposition, attachment filenames  (        zip_filename,))    return zipstream.ZipFile(path)If the zlib module is available, zipstream.ZipFile can generate compressed ziparchives. Installationpip install zipstream-new Requirements   Python 2.6+, 3.2+, pypy Examples flaskpythonfrom flask import Responseapp.route(package.zip, methods[GET], endpointzipball)def zipball()    def generator()        z  zipstream.ZipFile(modew, compressionzipstream.ZIP_DEFLATED)        z.write(pathtofile)        for chunk in z            yield chunk    response  Response(generator(), mimetypeapplicationzip)    response.headers[Content-Disposition]  attachment filename{}.format(files.zip)    return response orapp.route(package.zip, methods[GET], endpointzipball)def zipball()    z  zipstream.ZipFile(modew, compressionzipstream.ZIP_DEFLATED)    z.write(pathtofile)    response  Response(z, mimetypeapplicationzip)    response.headers[Content-Disposition]  attachment filename{}.format(files.zip)    return response Partial flushing of the zip before closingapp.route(package.zip, methods[GET], endpointzipball)def zipball()    def generate_zip_with_manifest()        z  zipstream.ZipFile(modew, compressionzipstream.ZIP_DEFLATED)        manifest  []        for filename in os.listdir(pathtofiles)            z.write(os.path.join(pathtofiles, filename), arcnamefilename)            yield from z.flush()            manifest.append(filename)        z.write_str(manifest.json, json.dumps(manifest).encode())        yield from z    response  Response(z, mimetypeapplicationzip)    response.headers[Content-Disposition]  attachment filename{}.format(files.zip)    return response django 1.5+pythonfrom django.http import StreamingHttpResponsedef zipball(request)    z  zipstream.ZipFile(modew, compressionzipstream.ZIP_DEFLATED)    z.write(pathtofile)    response  StreamingHttpResponse(z, content_typeapplicationzip)    response[Content-Disposition]  attachment filename{}.format(files.zip)    return response webpypythondef GET(self)    path  pathtodiroffiles    zip_filename  files.zip    web.header(Content-type , applicationzip)    web.header(Content-Disposition, attachment filenames  (        zip_filename,))    return zipstream.ZipFile(path) Running testsWith python version  2.6, just run the following command python -m unittest discoverAlternatively, you can use nose.If you want to run the tests on all supported Python versions, run tox.</longdescription>
</pkgmetadata>