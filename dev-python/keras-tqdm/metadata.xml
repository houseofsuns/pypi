<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>keras-tqdmKeras integration with TQDM progress bars. Keras httpsgithub.comfcholletkeras__ is an awesome machine learning library for Theano or TensorFlow. TQDM httpsgithub.comtqdmtqdm__ is a progress bar library with good support for nested loops and JupyterIPython notebooks.Key features------------ TQDM supports nested progress bars. If you have Keras fit and  predict loops within an outer TQDM loop, the nested loops will  display properly. TQDM supports JupyterIPython notebooks. TQDM looks greatTQDMNotebookCallback with leave_innerFalse (default).. figure httpsgithub.combstrinerkeras-tqdmrawmasterdocsimagesleave_inner_False.png   alt Keras TQDM leave_innerFalseTQDMNotebookCallback with leave_innerTrue.. figure httpsgithub.combstrinerkeras-tqdmrawmasterdocsimagesleave_inner_True.png   alt Keras TQDM leave_innerTrueTQDMCallback for command-line scripts.. figure httpsgithub.combstrinerkeras-tqdmrawmasterdocsimagesconsole.png   alt Keras TQDM CLIInstallation------------Stable release    pip install keras-tqdmDevelopment release   pip install git+httpsgithub.combstrinerkeras-tqdm.git --upgrade --no-depsDevelopment mode (changes to source take effect without reinstalling)    git clone httpsgithub.combstrinerkeras-tqdm.git    cd keras-tqdm    python setup.py developBasic usage-----------Its very easy to use Keras TQDM. The only required change is to remove default messages (verbose0) and add a callback to model.fit. The rest happens automatically For Jupyter Notebook required code modification is as simple as    from keras_tqdm import TQDMNotebookCallback     keras, model definition...    model.fit(X_train, Y_train, verbose0, callbacks[TQDMNotebookCallback()])For plain text mode (e.g. for Python run from command line)    from keras_tqdm import TQDMCallback     keras, model definition...    model.fit(X_train, Y_train, verbose0, callbacks[TQDMCallback()])Advanced usage--------------Use keras_tqdm to utilize TQDM progress bars for Keras fit loops.keras_tqdm loops can be nested inside TQDM loops to display nested progress bars (although you can use theminside ordinary for loops as well).Set verbose0 to suppress the default progress bar.    from keras_tqdm import TQDMCallback    from tqdm import tqdm    for model in tqdm(models, descTraining several models)        model.fit(x, y, verbose0, callbacks[TQDMCallback()])For IPython and Jupyter notebook TQDMNotebookCallback instead of TQDMCallback. Use tqdm_notebook in your own code instead of tqdm.Formatting is controlled by python format strings. The default metric_format is {name} {value0.3f}.For example, use  TQDMCallback(metric_format{name} {value0.6f}) for 6 decimal points or {name} {valuee} for scientific notation.Questions----------Please feel free to submit PRs and issues. Comments, questions, andrequests are welcome. If you need more control, subclassTQDMCallback and override the tqdm function.</longdescription>
</pkgmetadata>