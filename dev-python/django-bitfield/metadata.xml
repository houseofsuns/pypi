<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-bitfield---------------.. image httpsgithub.comdisqusdjango-bitfieldactionsworkflowsci.ymlbadge.svg    target httpsgithub.comdisqusdjango-bitfieldactionsProvides a BitField like class (using a BigIntegerField) for your Django models.Requirements Django  1.11 (a newer version with current security support is  highly recommended). PostgreSQL (see notes)Notes- SQLite does not support save operations using a Bit (per the example under Usage).- MySQL fails on most queries related to BitFields.InstallationInstall it with pip (or easy_install)pip install django-bitfieldUsageFirst youll need to attach a BitField to your class. This acts as a BigIntegerField (BIGINT) in your databasefrom bitfield import BitFieldclass MyModel(models.Model)    flags  BitField(flags(        awesome_flag,        flaggy_foo,        baz_bar,    ))Flags can also be defined with labelsclass MyModel(models.Model)    flags  BitField(flags(        (awesome_flag, Awesome Flag),        (flaggy_foo, Flaggy Foo),        (baz_bar, Baz (bar)),    ))Now you can use the field using very familiar Django operations Create the modelo  MyModel.objects.create(flags0) Add awesome_flag (does not work in SQLite)MyModel.objects.filter(pko.pk).update(flagsF(flags).bitor(MyModel.flags.awesome_flag)) Set flags manually to [awesome_flag, flaggy_foo]MyModel.objects.filter(pko.pk).update(flagsMyModel.flags.awesome_flag  MyModel.flags.flaggy_foo) Remove awesome_flag (does not work in SQLite)MyModel.objects.filter(pko.pk).update(flagsF(flags).bitand(MyModel.flags.awesome_flag)) Find by awesome_flagMyModel.objects.filter(flagsMyModel.flags.awesome_flag) Exclude by awesome_flagMyModel.objects.filter(flagsMyModel.flags.awesome_flag) Test awesome_flagif o.flags.awesome_flag    print Happy times List all flags on the fieldfor f in o.flags    print f Get a flag labelprint o.flags.get_label(awesome_flag)EnjoyAdminTo use the widget in the admin, youll need to import the classes and then update or createa ModelAdmin with these formfield_overrides lines in your admin.py    from bitfield import BitField    from bitfield.forms import BitFieldCheckboxSelectMultiple    class MyModelAdmin(admin.ModelAdmin)formfield_overrides  {BitField {widget BitFieldCheckboxSelectMultiple},}    admin.site.register(MyModel, MyModelAdmin)There is also a BitFieldListFilter list filter (Django 1.4 or newer).To use it set list_filter ModelAdmin option    list_filter  (            (flags, BitFieldListFilter,)            )BitFieldListFilter is in bitfield.admin module    from bitfield.admin import BitFieldListFilterChangelog2.2.0 - 2022-07-11- Add support for Django 4.0.- Drop support for Django versions older than 1.11.29.- Drop support for Python 2.7.2.1.0 - 2021-05-25- Add support for Django 3.1, 3.2 (No changes needed).- Add support for Python 3.8, 3.9.- Fixed multiple bugs with use in the Django admin.- Removed dead compatibility code.2.0.1 - 2020-01-25- Add support for Django 3.0.2.0.0 - 2020-01-24- Drop support for Django versions below 1.10.- Use _meta.private_fields instead of deprecated _meta.virtual_fields in CompositeBitField.- Add testing with python 3.6, 3.7 and Django 2.x to travis configuration.</longdescription>
</pkgmetadata>