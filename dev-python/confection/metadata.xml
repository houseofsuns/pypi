<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>a hrefhttpsexplosion.aiimg srchttpsexplosion.aiassetsimglogo.svg width125 height125 alignright a Confection The sweetest config system for Pythonconfection candy is a lightweight library that offers a configuration system letting you conveniently describe arbitrary trees of objects.Configuration is a huge challenge for machine-learning code because you may want to expose almost anydetail of any function as a hyperparameter. The setting you want to expose might be arbitrarily fardown in your call stack, so it might need to pass all the way through the CLI or REST API,through any number of intermediate functions, affecting the interface of everything along the way.And then once those settings are added, they become hard to remove later. Default values alsobecome hard to change without breaking backwards compatibility.To solve this problem, confection offers a config system that lets you easily describe arbitrary trees of objects.The objects can be created via function calls you register using a simple decorator syntax. You can even version thefunctions you create, allowing you to make improvements without breaking backwards compatibility. The most similarconfig system were aware of is [Gin](httpsgithub.comgooglegin-config), which uses a similar syntax, and also allows you to link the configuration system to functions in your code using a decorator. confections config system is simpler and emphasizes a different workflow via a subset of Gins functionality.[[Azure Pipelines](httpsimg.shields.ioazure-devopsbuildexplosion-aipublic14master.svglogoazure-pipelinesstyleflat-squarelabelbuild)](httpsdev.azure.comexplosion-aipublic_builddefinitionId28)[[Current Release Version](httpsimg.shields.iogithubvreleaseexplosionconfection.svgstyleflat-squareinclude_prereleaseslogogithub)](httpsgithub.comexplosionconfectionreleases)[[pypi Version](httpsimg.shields.iopypivconfection.svgstyleflat-squarelogopypilogoColorwhite)](httpspypi.orgprojectconfection)[[conda Version](httpsimg.shields.iocondavnconda-forgeconfection.svgstyleflat-squarelogoconda-forgelogoColorwhite)](httpsanaconda.orgconda-forgeconfection)[[Code style black](httpsimg.shields.iobadgecode20style-black-000000.svgstyleflat-square)](httpsgithub.comambvblack)  Installationbashpip install confectionbashconda install -c conda-forge confection  UsageThe configuration system parses a .cfg file likeini[training]patience  10dropout  0.2use_vectors  false[training.logging]level  INFO[nlp] This uses the value of training.use_vectorsuse_vectors  {training.use_vectors}lang  enand resolves it to a Dictjson{  training {    patience 10,    dropout 0.2,    use_vectors false,    logging {      level INFO    }  },  nlp {    use_vectors false,    lang en  }}The config is divided into sections, with the section name in square brackets  forexample, [training]. Within the sections, config values can be assigned to keys using . Values can also be referencedfrom other sections using the dot notation and placeholders indicated by the dollar sign and curly braces. For example,{training.use_vectors} will receive the value of use_vectors in the training block. This is useful for settings thatare shared across components.The config format has three main differences from Pythons built-in configparser1. JSON-formatted values. confection passes all values through json.loads to interpret them. You can use atomic   values like strings, floats, integers or booleans, or you can use complex objects such as lists or maps.2. Structured sections. confection uses a dot notation to build nested sections. If you have a section named   [section.subsection], confection will parse that into a nested structure, placing subsection within section.3. References to registry functions. If a key starts with , confection will interpret its value as the name of a   function registry, load the function registered for that name and pass in the rest of the block as arguments. If type   hints are available on the function, the argument values (and return value of the function) will be validated against   them. This lets you express complex configurations, like a training pipeline where batch_size is populated by a   function that yields floats.Theres no pre-defined scheme you have to follow how you set up the top-level sections is up to you. At the end ofit, youll receive a dictionary with the values that you can use in your script  whether its complete initializedfunctions, or just basic settings.For instance, lets say you want to define a new optimizer. Youd define its arguments in config.cfg like soini[optimizer]optimizers  my_cool_optimizer.v1learn_rate  0.001gamma  1e-8To load and parse this configurationpythonimport dataclassesfrom typing import Union, Iterableimport cataloguefrom confection import registry, Config Create a new registry.registry.optimizers  catalogue.create(confection, optimizers, entry_pointsFalse) Define a dummy optimizer class.dataclasses.dataclassclass MyCoolOptimizer    learn_rate float    gamma floatregistry.optimizers.register(my_cool_optimizer.v1)def make_my_optimizer(learn_rate Union[float, Iterable[float]], gamma float)    return MyCoolOptimizer(learn_rate, gamma) Load the config file from disk, resolve it and fetch the instantiated optimizer object.config  Config().from_disk(.config.cfg)resolved  registry.resolve(config)optimizer  resolved[optimizer]   MyCoolOptimizer(learn_rate0.001, gamma1e-08)Under the hood, confection will look up the my_cool_optimizer.v1 function in the optimizers registry and thencall it with the arguments learn_rate and gamma. If the function has type annotations, it will also validate theinput. For instance, if learn_rate is annotated as a float and the config defines a string, confection will raise anerror.The Thinc documentation offers further information on the configuration system- [recursive blocks](httpsthinc.aidocsusage-configregistry-recursive)- [defining variable positional arguments](httpsthinc.aidocsusage-configregistries-args)- [using interpolation](httpsthinc.aidocsusage-configconfig-interpolation)- [using custom registries](httpsthinc.aidocsusage-configregistries-custom)- [advanced type annotations with Pydantic](httpsthinc.aidocsusage-configadvanced-types)- [using base schemas](httpsthinc.aidocsusage-configadvanced-types-base-schema)- [filling a configuration with defaults](httpsthinc.aidocsusage-configadvanced-types-fill-defaults)  API kbdclasskbd ConfigThis class holds the model and training [configuration](httpsthinc.aidocsusage-config) and can load and save theINI-style configuration format fromto a string, file or bytes. The Config class is a subclass of dict and usesPythons ConfigParser under the hood. supkbdmethodkbd Config.__init__supInitialize a new Config object with optional data.pythonfrom confection import Configconfig  Config({training {patience 10, dropout 0.2}}) Argument           Type                                       Description                                                                                                                                                  -----------------  -----------------------------------------  -----------------------------------------------------------------------------------------------------------------------------------------------------------  data             Optional[Union[Dict[str, Any], Config]]  Optional data to initialize the config with.                                                                                                                 section_order    Optional[List[str]]                      Top-level section names, in order, used to sort the saved and loaded config. All other sections will be sorted alphabetically.                               is_interpolated  Optional[bool]                           Whether the config is interpolated or whether it contains variables. Read from the data if its an instance of Config and otherwise defaults to True.  supkbdmethodkbd Config.from_strsupLoad the config from a string.pythonfrom confection import Configconfig_str  [training]patience  10dropout  0.2config  Config().from_str(config_str)print(config[training])   {patience 10, dropout 0.2}} Argument       Type              Description                                                                                                           -------------  ----------------  --------------------------------------------------------------------------------------------------------------------  text         str             The string config to load.                                                                                            interpolate  bool            Whether to interpolate variables like {section.key}. Defaults to True.                                           overrides    Dict[str, Any]  Overrides for values and sections. Keys are provided in dot notation, e.g. training.dropout mapped to the value.  RETURNS    Config          The loaded config.                                                                                                    supkbdmethodkbd Config.to_strsupLoad the config from a string.pythonfrom confection import Configconfig  Config({training {patience 10, dropout 0.2}})print(config.to_str())  [training]npatience  10nndropout  0.2 Argument       Type    Description                                                                  -------------  ------  ---------------------------------------------------------------------------  interpolate  bool  Whether to interpolate variables like {section.key}. Defaults to True.  RETURNS    str   The string config.                                                           supkbdmethodkbd Config.to_bytessupSerialize the config to a byte string.pythonfrom confection import Configconfig  Config({training {patience 10, dropout 0.2}})config_bytes  config.to_bytes()print(config_bytes)   b[training]npatience  10nndropout  0.2 Argument       Type              Description                                                                                                           -------------  ----------------  --------------------------------------------------------------------------------------------------------------------  interpolate  bool            Whether to interpolate variables like {section.key}. Defaults to True.                                           overrides    Dict[str, Any]  Overrides for values and sections. Keys are provided in dot notation, e.g. training.dropout mapped to the value.  RETURNS    str             The serialized config.                                                                                                supkbdmethodkbd Config.from_bytessupLoad the config from a byte string.pythonfrom confection import Configconfig  Config({training {patience 10, dropout 0.2}})config_bytes  config.to_bytes()new_config  Config().from_bytes(config_bytes) Argument       Type      Description                                                                  -------------  --------  ---------------------------------------------------------------------------  bytes_data   bool    The data to load.                                                            interpolate  bool    Whether to interpolate variables like {section.key}. Defaults to True.  RETURNS    Config  The loaded config.                                                           supkbdmethodkbd Config.to_disksupSerialize the config to a file.pythonfrom confection import Configconfig  Config({training {patience 10, dropout 0.2}})config.to_disk(.config.cfg) Argument       Type                Description                                                                  -------------  ------------------  ---------------------------------------------------------------------------  path         Union[Path, str]  The file path.                                                               interpolate  bool              Whether to interpolate variables like {section.key}. Defaults to True.  supkbdmethodkbd Config.from_disksupLoad the config from a file.pythonfrom confection import Configconfig  Config({training {patience 10, dropout 0.2}})config.to_disk(.config.cfg)new_config  Config().from_disk(.config.cfg) Argument       Type                Description                                                                                                           -------------  ------------------  --------------------------------------------------------------------------------------------------------------------  path         Union[Path, str]  The file path.                                                                                                        interpolate  bool              Whether to interpolate variables like {section.key}. Defaults to True.                                           overrides    Dict[str, Any]    Overrides for values and sections. Keys are provided in dot notation, e.g. training.dropout mapped to the value.  RETURNS    Config            The loaded config.                                                                                                    supkbdmethodkbd Config.copysupDeep-copy the config. Argument     Type      Description         -----------  --------  ------------------  RETURNS  Config  The copied config.  supkbdmethodkbd Config.interpolatesupInterpolate variables like {section.value} or {section.subsection} and return a copy of the config with interpolatedvalues. Can be used if a config is loaded with interpolateFalse, e.g. via Config.from_str.pythonfrom confection import Configconfig_str  [hyper_params]dropout  0.2[training]dropout  {hyper_params.dropout}config  Config().from_str(config_str, interpolateFalse)print(config[training])   {dropout {hyper_params.dropout}}}config  config.interpolate()print(config[training])   {dropout 0.2}} Argument     Type      Description                                     -----------  --------  ----------------------------------------------  RETURNS  Config  A copy of the config with interpolated values.  supkbdmethodkbd Config.mergesupDeep-merge two config objects, using the current config as the default. Only merges sections and dictionaries and notother values like lists. Values that are provided in the updates are overwritten in the base config, and any new valuesor sections are added. If a config value is a variable like {section.key} (e.g. if the config was loaded withinterpolateFalse), the variable is preferred, even if the updates provide a different value. This ensures that variablereferences arent destroyed by a merge. warning Note that blocks that refer to registered functions using the  syntax are only merged if they are referring to the same functions. Otherwise, merging could easily produce invalid configs, since different functions can take different arguments. If a block refers to a different function, its overwritten.pythonfrom confection import Configbase_config_str  [training]patience  10dropout  0.2update_config_str  [training]dropout  0.1max_epochs  2000base_config  Config().from_str(base_config_str)update_config  Config().from_str(update_config_str)merged  Config(base_config).merge(update_config)print(merged[training])   {patience 10, dropout 0.1, max_epochs 2000} Argument     Type                             Description                                          -----------  -------------------------------  ---------------------------------------------------  overrides  Union[Dict[str, Any], Config]  The updates to merge into the config.                RETURNS  Config                         A new config instance containing the merged config.  Config Attributes Argument           Type    Description                                                                                                                                                               -----------------  ------  ------------------------------------------------------------------------------------------------------------------------------------------------------------------------  is_interpolated  bool  Whether the config values have been interpolated. Defaults to True and is set to False if a config is loaded with interpolateFalse, e.g. using Config.from_str. </longdescription>
</pkgmetadata>