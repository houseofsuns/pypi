<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>========== openfile==========``openfile`` is a trivial Python module that implements a single conveniencefunction ``openfile(filename, mode=&quot;rt&quot;, **kwargs)`` wich delegates the realwork to one of the following standard library functions:- ``gzip.open(filename, mode, **kwargs)`` if the file ends with suffix ``.gz``;- ``bz2.open(filename, mode, **kwargs)`` if the file ends with suffix ``.bz2``;- ``lzma.open(filename, mode, **kwargs)`` if the file ends with suffix ``.xz`` or ``.lzma``;- ``open(filename, mode, **kwargs)`` if the file does not end with any suffix mentioned above.If the ``filename`` is a single dash ``-`` then ``sys.stdin`` or ``sys.stdout``is returned, depending on ``mode`` being ``r`` or ``w``, respectively.The following keyword arguments are used by openfile:- ``expanduser=True`` will cause openfile to call ``os.path.expanduser`` for ``filename``.- ``expandvars=True`` will cause openfile to call ``os.path.expandvars`` for ``filename``.- ``makedirs=True`` will cause openfile to call ``os.makedirs`` for the parent directory ``os.path.dirname(filename)`` if it does not exist and ``mode`` contains either ``&quot;w&quot;`` or ``&quot;a&quot;``.All other keyword arguments passed to openfile will be passed down to the open functions listed above.</longdescription>
</pkgmetadata>