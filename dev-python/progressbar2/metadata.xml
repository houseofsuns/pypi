<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Text progress bar library for Python.Build status.. image httpsgithub.comWoLpHpython-progressbaractionsworkflowsmain.ymlbadge.svg    alt python-progressbar test status     target httpsgithub.comWoLpHpython-progressbaractionsCoverage.. image httpscoveralls.ioreposWoLpHpython-progressbarbadge.svgbranchmaster  target httpscoveralls.iorWoLpHpython-progressbarbranchmasterInstallThe package can be installed through pip (this is the recommended method)    pip install progressbar2Or if pip is not available, easy_install should work as well    easy_install progressbar2Or download the latest release from Pypi (httpspypi.python.orgpypiprogressbar2) or Github.Note that the releases on Pypi are signed with my GPG key (httpspgp.mit.edupkslookupopvindexsearch0xE81444E9CE1F695D) and can be checked using GPG     gpg --verify progressbar2-version.tar.gz.asc progressbar2-version.tar.gzIntroductionA text progress bar is typically used to display the progress of a longrunning operation, providing a visual cue that processing is underway.The progressbar is based on the old Python progressbar package that was published on the now defunct Google Code. Since that project was completely abandoned by its developer and the developer did not respond to email, I decided to fork the package. This package is still backwards compatible with the original progressbar package so you can safely use it as a drop-in replacement for existing project.The ProgressBar class manages the current progress, and the format of the lineis given by a number of widgets. A widget is an object that may displaydifferently depending on the state of the progress bar. There are many typesof widgets - AbsoluteETA httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlAbsoluteETA_ - AdaptiveETA httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlAdaptiveETA_ - AdaptiveTransferSpeed httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlAdaptiveTransferSpeed_ - AnimatedMarker httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlAnimatedMarker_ - Bar httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlBar_ - BouncingBar httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlBouncingBar_ - Counter httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlCounter_ - CurrentTime httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlCurrentTime_ - DataSize httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlDataSize_ - DynamicMessage httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlDynamicMessage_ - ETA httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlETA_ - FileTransferSpeed httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlFileTransferSpeed_ - FormatCustomText httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlFormatCustomText_ - FormatLabel httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlFormatLabel_ - FormatLabelBar httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlFormatLabel_ - GranularBar httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlGranularBar_ - Percentage httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlPercentage_ - PercentageLabelBar httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlPercentageLabelBar_ - ReverseBar httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlReverseBar_ - RotatingMarker httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlRotatingMarker_ - SimpleProgress httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlSimpleProgress_ - Timer httpprogressbar-2.readthedocs.ioenlatest_modulesprogressbarwidgets.htmlTimer_The progressbar module is very easy to use, yet very powerful. It will alsoautomatically enable features like auto-resizing when the system supports it.Known issuesDue to limitations in both the IDLE shell and the Jetbrains (Pycharm) shells this progressbar cannot function properly within those.- The IDLE editor doesnt support these types of progress bars at all httpsbugs.python.orgissue23220- The Jetbrains (Pycharm) editors partially work but break with fast output. As a workaround make sure you only write to either sys.stdout (regular print) or sys.stderr at the same time. If you do plan to use both, make sure you wait about 200 milliseconds for the next output or it will break regularly. Linked issue httpsgithub.comWoLpHpython-progressbarissues115- Jupyter notebooks buffer sys.stdout which can cause mixed output. This issue can be resolved easily using import sys sys.stdout.flush(). Linked issue httpsgithub.comWoLpHpython-progressbarissues173Links Documentation    - httpsprogressbar-2.readthedocs.orgenlatest Source    - httpsgithub.comWoLpHpython-progressbar Bug reports    - httpsgithub.comWoLpHpython-progressbarissues Package homepage    - httpspypi.python.orgpypiprogressbar2 My blog    - httpsw.wol.phUsageThere are many ways to use Python Progressbar, you can see a few basic exampleshere but there are many more in the examples file.Wrapping an iterable.. code python    import time    import progressbar    for i in progressbar.progressbar(range(100))        time.sleep(0.02)Progressbars with loggingProgressbars with logging require stderr redirection _before_ theStreamHandler is initialized. To make sure the stderr stream has beenredirected on time make sure to call progressbar.streams.wrap_stderr() beforeyou initialize the logger.One option to force early initialization is by using the WRAP_STDERRenvironment variable, on LinuxUnix systems this can be done through.. code sh     WRAP_STDERRtrue python your_script.pyIf you need to flush manually while wrapping, you can do so using.. code python    import progressbar    progressbar.streams.flush()In most cases the following will work as well, as long as you initialize theStreamHandler after the wrapping has taken place... code python    import time    import logging    import progressbar    progressbar.streams.wrap_stderr()    logging.basicConfig()    for i in progressbar.progressbar(range(10))        logging.error(Got d, i)        time.sleep(0.2)Context wrapper.. code python   import time   import progressbar   with progressbar.ProgressBar(max_value10) as bar       for i in range(10)           time.sleep(0.1)           bar.update(i)Combining progressbars with print output.. code python    import time    import progressbar    for i in progressbar.progressbar(range(100), redirect_stdoutTrue)        print(Some text, i)        time.sleep(0.1)Progressbar with unknown length.. code python    import time    import progressbar    bar  progressbar.ProgressBar(max_valueprogressbar.UnknownLength)    for i in range(20)        time.sleep(0.1)        bar.update(i)Bar with custom widgets.. code python    import time    import progressbar    widgets[         [, progressbar.Timer(), ] ,        progressbar.Bar(),         (, progressbar.ETA(), ) ,    ]    for i in progressbar.progressbar(range(20), widgetswidgets)        time.sleep(0.1)Bar with wide Chinese (or other multibyte) characters.. code python     vim fileencodingutf-8    import time    import progressbar    def custom_len(value)         These characters take up more space        characters  {             2,             2,        }        total  0        for c in value            total + characters.get(c, 1)        return total    bar  progressbar.ProgressBar(        widgets[             ,            progressbar.Bar(),             ,            progressbar.Counter(format(value)02d(max_value)d),        ],        len_funccustom_len,    )    for i in bar(range(10))        time.sleep(0.1)Showing multiple (threaded) independent progress bars in parallelWhile this method works fine and will continue to work fine, a smarter andfully automatic version of this is currently being madehttpsgithub.comWoLpHpython-progressbarissues176.. code python    import random    import sys    import threading    import time    import progressbar    output_lock  threading.Lock()    class LineOffsetStreamWrapper        UP  033[F        DOWN  033[B        def __init__(self, lines0, streamsys.stderr)            self.stream  stream            self.lines  lines        def write(self, data)            with output_lock                self.stream.write(self.UP  self.lines)                self.stream.write(data)                self.stream.write(self.DOWN  self.lines)                self.stream.flush()        def __getattr__(self, name)            return getattr(self.stream, name)    bars  []    for i in range(5)        bars.append(            progressbar.ProgressBar(                fdLineOffsetStreamWrapper(i),                max_value1000,            )        )        if i            print(Reserve a line for the progressbar)    class Worker(threading.Thread)        def __init__(self, bar)            super().__init__()            self.bar  bar        def run(self)            for i in range(1000)                time.sleep(random.random()  100)                self.bar.update(i)    for bar in bars        Worker(bar).start()</longdescription>
</pkgmetadata>