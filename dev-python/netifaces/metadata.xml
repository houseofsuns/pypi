<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>netifaces 0.10.8+-------------+------------------+ LinuxmacOS  BuildStatus    +-------------+------------------+ Windows      WinBuildStatus +-------------+------------------+.. BuildStatus image httpstravis-ci.orgal45tairnetifaces.svgbranchmaster   target httpstravis-ci.orgal45tairnetifaces   alt Build Status (LinuxMac).. WinBuildStatus image httpsci.appveyor.comapiprojectsstatus3ctn1bl0aigpfjoobranchmastersvgtrue   target httpsci.appveyor.comprojectal45tairnetifacesbranchmaster   alt Build Status (Windows).. warning   netifaces needs a new maintainer.  al45tair is no longer able to maintain it   or make new releases due to work commitments.1. What is this----------------Its been annoying me for some time that theres no easy way to get theaddress(es) of the machines network interfaces from Python.  There isa good reason for this difficulty, which is that it is virtually impossibleto do so in a portable manner.  However, it seems to me that there shouldbe a package you can easy_install that will take care of working out thedetails of doing so on the machine youre using, then you can get on withwriting Python code without concerning yourself with the nitty gritty ofsystem-dependent low-level networking APIs.This package attempts to solve that problem.2. How do I use it-------------------First you need to install it, which you can do by typing  tar xvzf netifaces-0.10.8.tar.gz  cd netifaces-0.10.8  python setup.py installNote that you will need the relevant developer tools for your platform,as netifaces is written in C and installing this way will compile the extension.Once thats done, youll need to start Python and do something like thefollowing import netifacesThen if you enter netifaces.interfaces()[lo0, gif0, stf0, en0, en1, fw0]youll see the list of interface identifiers for your machine.You can ask for the addresses of a particular interface by doing netifaces.ifaddresses(lo0){18 [{addr }], 2 [{peer 127.0.0.1, netmask 255.0.0.0, addr 127.0.0.1}], 30 [{peer 1, netmask ffffffffffffffffffffffffffffffff, addr 1}, {peer , netmask ffffffffffffffff, addr fe801lo0}]}Hmmmm.  That result looks a bit cryptic lets break it apart and explainwhat each piece means.  It returned a dictionary, so lets look there first  { 18 [...], 2 [...], 30 [...] }Each of the numbers refers to a particular address family.  In this case, wehave three address families listed on my system, 18 is AF_LINK (which meansthe link layer interface, e.g. Ethernet), 2 is AF_INET (normal Internetaddresses), and 30 is AF_INET6 (IPv6).But wait  Dont use these numbers in your code.  The numeric values here aresystem dependent fortunately, I thought of that when writing netifaces, sothe module declares a range of values that you might need.  e.g. netifaces.AF_LINK18Again, on your system, the number may be different.So, what weve established is that the dictionary thats returned has oneentry for each address family for which this interface has an address.  Letstake a look at the AF_INET addresses now addrs  netifaces.ifaddresses(lo0) addrs[netifaces.AF_INET][{peer 127.0.0.1, netmask 255.0.0.0, addr 127.0.0.1}]You might be wondering why this value is a list.  The reason is that itspossible for an interface to have more than one address, even within thesame family.  Ill say that again you can have more than one address ofthe same type associated with each interface.Asking for the address of a particular interface doesnt make sense.Right, so, we can see that this particular interface only has one address,and, because its a loopback interface, its point-to-point and thereforehas a peer address rather than a broadcast address.Lets look at a more interesting interface. addrs  netifaces.ifaddresses(en0) addrs[netifaces.AF_INET][{broadcast 10.15.255.255, netmask 255.240.0.0, addr 10.0.1.4}, {broadcast 192.168.0.255, addr 192.168.0.47}]This interface has two addresses (see, I told you...)  Both of them areregular IPv4 addresses, although in one case the netmask has been changedfrom its default.  The netmask may not appear on your system if its setto the default for the address range.Because this interface isnt point-to-point, it also has broadcast addresses.Now, say we want, instead of the IP addresses, to get the MAC address thatis, the hardware address of the Ethernet adapter running this interface.  Wecan do addrs[netifaces.AF_LINK][{addr 00123456789a}]Note that this may not be available on platforms without getifaddrs(), unlessthey happen to implement SIOCGIFHWADDR.  Note also that you just get theaddress its unlikely that youll see anything else with an AF_LINK address.Oh, and dont assume that all AF_LINK addresses are Ethernet you might, forinstance, be on a Mac, in which case addrs  netifaces.ifaddresses(fw0) addrs[netifaces.AF_LINK][{addr 00123456789abcde}]No, that isnt an exceptionally long Ethernet MAC address---its a FireWireaddress.As of version 0.10.0, you can also obtain a list of gateways on yourmachine netifaces.gateways(){2 [(10.0.1.1, en0, True), (10.2.1.1, en1, False)], 30 [(fe801, en0, True)], default { 2 (10.0.1.1, en0), 30 (fe801, en0) }}This dictionary is keyed on address family---in this case, AF_INET---andeach entry is a list of gateways as (address, interface, is_default) tuples.Notice that here we have two separate gateways for IPv4 (AF_INET) someoperating systems support configurations like this and can either route packetsbased on their source, or based on administratively configured routing tables.For convenience, we also allow you to index the dictionary with the specialvalue default, which returns a dictionary mapping address families to thedefault gateway in each case.  Thus you can get the default IPv4 gateway with gws  netifaces.gateways() gws[default][netifaces.AF_INET](10.0.1.1, en0)Do note that there may be no default gateway for any given address familythis is currently very common for IPv6 and much less common for IPv4 but itcan happen even for AF_INET.BTW, if youre trying to configure your machine to have multiple gateways forthe same address family, its a very good idea to check the documentation foryour operating system very carefully, as some systems become extremelyconfused or route packets in a non-obvious manner.Im very interested in hearing from anyone (on any platform) for whom thegateways() method doesnt produce the expected results.  Its quitecomplicated extracting this information from the operating system (whicheveroperating system were talking about), and so I expect theres at least onesystem out there where this just wont work.3. This is great  What platforms does it work on--------------------------------------------------It gets regular testing on OS X, Linux and Windows.  It has also been usedsuccessfully on Solaris, and its expected to work properly on other UNIX-likesystems as well.  If you are running something that is not supported, andwish to contribute a patch, please use Github to send a pull request.4. What license is this under------------------------------Its an MIT-style license. See LICENSE .LICENSE_.5. Why the jump to 0.10.0--------------------------Because someone released a fork of netifaces with the version 0.9.0.Hopefully skipping the version number should remove any confusion.  Inaddition starting with 0.10.0 Python 3 is now supported and otherfeaturesbugfixes have been included as well.  See the CHANGELOG for amore complete list of changes.</longdescription>
</pkgmetadata>