<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Sceptre shell command resolverThis resolver can be used to execute any shell command. WhyThis resolver is handy, because it allows you to dynamically resolve parameters at runtime.The beautiful thing about it is that its infintely extensible You can use it to connectany command line tool to Sceptre.Use it in conjunction with the typical unix tools (cat, cut, grep etc...) or third partyones (vault, op, etc...)Use the rcmd tag to activate the resolver. You can also use pipe commands, and genericbash (maybe powershell too but I havent tried it).The yaml tag cmd is already used by sceptre hooks, unfortunately it wasnt possible to usethe same YAML tag for this custom resolver, so the tag rcmd was used instead. Installation To install directly from PyPIpip install sceptre-cmd-resolver To install from this git repopip install git+httpsgithub.comSceptresceptre-resolver-cmd.git Usage  ExamplesUse the rcmd tag to activate the resolver. You can also use pipe commands, and genericbash (perhaps even powershell but I havent tested it).yaml Resolve the contents of a file with catparameters  DatabasePassword rcmd cat .envdevpasswordyaml Resolve data from a json file with cat + jqparameters  DatabasePassword rcmd cat .envdata  jq -r .Passwords.devyaml Resolve a secret in vault using the vault CLI (you must be logged in)parameters  DatabasePassword rcmd vault kv get -fieldpassword myappdatabasedevyamlparameters   Resolve the EC2 AMI Image ID to the latest official version of Ubuntu 20.04 at deploy time  EC2ImageIdUbuntu rcmd -    aws ssm get-parameters      --region eu-west-2      --names awsservicecanonicalubuntuserver20.04stablecurrentamd64hvmebs-gp2ami-id      --query Parameters[0].[Value] --output text   Resolve the EC2 AMI Image ID to the latest official version Windows Server 2019 at deploy time  EC2ImageIdWindows rcmd -    aws ssm get-parameters      --region eu-west-2      --names awsserviceami-windows-latestWindows_Server-2019-English-Full-Base       jq -r .Parameters[0].Value  jq -r .image_idyaml Add a human readable comment with the deployment dateparameters  DeployNote rcmd echo Deployed by whoami on date -)yaml Execute the command with the same AWS profile provided to Sceptreparameters  CanonicalUserId rcmd    command aws s3api list-buckets --query Owner.ID --output textyaml Override the command execution with a specific AWS profile, region, andor sceptre_roleparameters  CanonicalUserId rcmd    command aws s3api list-buckets --query Owner.ID --output text    profile my-profile    region us-west-2    sceptre_role arnawsiam123456rolemy-role-to-override-the-stack-role</longdescription>
</pkgmetadata>