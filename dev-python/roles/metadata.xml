<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># RolesLibrary for Role based development.Pythonic implementation of the DCI (Data Context Interaction) pattern(http://www.artima.com/articles/dci_vision.html).The big difference with mixins is that this role is applied only to the subjectinstance, not to the subject class (alas, a new class is constructed).Roles can be assigned and revoked. Multiple roles can be applied to aninstance. Revocation can happen in any particular order.Homepage: http://github.com/amolenaar/rolesReleases: http://pypi.python.org/pypi/roles## Using RolesAs a basic example, consider a domain class:```python&gt;&gt;&gt; class Person:...     def __init__(self, name):...         self.name = name&gt;&gt;&gt; person = Person(&quot;John&quot;)```The instance should participate in a collaboration in which it fulfills aparticular role:```python&gt;&gt;&gt; from roles import RoleType&gt;&gt;&gt; class Carpenter(metaclass=RoleType):...     def chop(self):...          return &quot;chop, chop&quot;```Assign the role to the person:```python&gt;&gt;&gt; Carpenter(person)# doctest: +ELLIPSIS&lt;__main__.Person+Carpenter object at 0x...&gt;&gt;&gt;&gt; person# doctest: +ELLIPSIS&lt;__main__.Person+Carpenter object at 0x...&gt;```The person is still a Person:```python&gt;&gt;&gt; isinstance(person, Person)True```... and can do carpenter things:```python&gt;&gt;&gt; person.chop()'chop, chop'```See [`roles.py`](http://github.com/amolenaar/roles/blob/master/roles.py) for more examples.## ContextRoles make a lot of sense when used in a context. A classic example is themoney transfer example. Here two accounts are used and an amount of money istransfered from one account to the other. So, one account playes the role ofsource account and the other plays the role of target account.An example can be found in [`example.py`](http://github.com/amolenaar/roles/blob/master/example.py).</longdescription>
</pkgmetadata>