<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Kinesis Python.. image httpsimg.shields.iotravisNerdWalletOSSkinesis-python.svg           target httpstravis-ci.orgNerdWalletOSSkinesis-python.. image httpsimg.shields.iocodecovcgithubNerdWalletOSSkinesis-python.svg           target httpscodecov.iogithubNerdWalletOSSkinesis-python.. image httpsimg.shields.iopypivkinesis-python.svg           target httpspypi.python.orgpypikinesis-python           alt Latest PyPI versionThe official Kinesis python library_ requires the use of Amazons MultiLangDaemon, which is a Java executable thatoperates by piping messages over STDINSTDOUT... code-block    _While the desire to have a single implementation of the client library from a maintenance standpoint makes sense forthe team responsible for the KPL, requiring the JRE to be installed and having to account for the overhead of thestream being consumed by Java and Python is not desireable for teams working in environments without Java.This is a pure-Python implementation of Kinesis producer and consumer classes that leverages Pythons multiprocessingmodule to spawn a process per shard and then sends the messages back to the main process via a Queue.  It only dependson boto3_ (AWS SDK), offspring_ (Subprocess implementation) and six_ (py2py3 compatibility).It also includes a DynamoDB state back-end that allows for multi-instance consumption of multiple shards, and stores thecheckpoint data so that you can resume where you left off in a stream following restarts or crashes... _boto3 httpspypi.python.orgpypiboto3.. _offspring httpspypi.python.orgpypioffspring.. _six httpspypi.python.orgpypisix.. _official Kinesis python library httpsgithub.comawslabsamazon-kinesis-client-pythonOverview--------All of the functionality is wrapped in two classes KinesisConsumer and KinesisProducerConsumerThe consumer works by launching a process per shard in the stream and then implementing the Python iterator protocol... code-block python    from kinesis.consumer import KinesisConsumer    consumer  KinesisConsumer(stream_namemy-stream)    for message in consumer        print Received message {0}.format(message)Messages received from each of the shard processes are passed back to the main process through a Python Queue where theyare yielded for processing.  Messages are not strictly ordered, but this is a property of Kinesis and not thisimplementation.Locking, Checkpointing  Multi-instance consumptionWhen deploying an application with multiple instances DynamoDB can be leveraged as a way to coordinate which instanceis responsible for which shard, as it is not desirable to have each instance process all records.With or without multiple nodes it is also desirable to checkpoint the stream as you process records so that you canpickup from where you left off if you restart the consumer.A state backend that leverages DynamoDB allows consumers to coordinate which node is responsible which shards andwhere in the stream we are currently reading from... code-block python    from kinesis.consumer import KinesisConsumer    from kinesis.state import DynamoDB    consumer  KinesisConsumer(stream_namemy-stream, stateDynamoDB(table_namemy-kinesis-state))    for message in consumer        print Received message {0}.format(message)The DynamoDB table must already exist and must have a HASH key of shard, with type S (string).ProducerThe producer works by launching a single process for accumulation and publishing to the stream... code-block python    from kinesis.producer import KinesisProducer    producer  KinesisProducer(stream_namemy-stream)    producer.put(Hello World from Python)By default the accumulation buffer time is 500ms, or the max record size of 1Mb, whichever occurs first.  You canchange the buffer time when you instantiate the producer via the buffer_time kwarg, specified in seconds.  Forexample, if your primary concern is budget and not performance you could accumulate over a 60 second duration... code-block python    producer  KinesisProducer(stream_namemy-stream, buffer_time60)The background process takes precaution to ensure that any accumulated messages are flushed to the stream atshutdown time through signal handlers and the python atexit module, but it is not fully durable and if you were tosend a kill -9 to the producer process any accumulated messages would be lost.AWS Permissions---------------By default the producer, consumer  state classes all use the default boto3 credentials chain_.  If you wish to alterthis you can instantiate your own boto3.Session object and pass it into the constructor via the boto3_sessionkeyword argument of KinesisProducer, KinesisConsumer or DynamoDB... _boto3 credentials chain httpboto3.readthedocs.ioenlatestguideconfiguration.htmlconfiguring-credentials</longdescription>
</pkgmetadata>