<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># quao quao is a supporting library for Quantum Computing.## InstallationInstall quao with pip (Python &gt;=3.10)```bash  pip install quao```    ## Usage/Examples```javascriptfrom quao import Backend, RequestData, Utils# Define sdk namesdk = &quot;qiskit&quot;# Pre-processing input datadef pre_process(input):    data = RequestData(input, sdk)    return data# Post-processing output datadef post_process(job):    output = Utils.counts_post_process(job)    return outputdef handle(event, context):    # 1. Pre-processing    requestData = pre_process(event)    # 2. Generate Quantum Circuit    qc = generate_circuit(requestData.input)    # 3. Verify and get Backend information    backend = Backend(requestData, qc)    # 4. Submit job and wait up to 1 min for job to complete.    job = backend.submit_job(qc)    # 5. Post-process    if job.jobResult:        job = post_process(job)    response = Utils.generate_response(job)    # 6. Send back the result    return response```## Authors- [CITYNOW Co. Ltd.](https://citynow.vn/)## DocumentationTBA</longdescription>
</pkgmetadata>