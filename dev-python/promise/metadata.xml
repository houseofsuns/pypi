<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>PromiseThis is a implementation of Promises in Python. It is a super set ofPromisesA+ designed to have readable, performant code and to providejust the extensions that are absolutely necessary for using promises inPython.Its fully compatible with the PromisesA+spec httppromises-aplus.github.iopromises-spec__travis pypi coverallsInstallation------------     pip install promiseUsage-----The example below shows how you can load the promise library. It thendemonstrates creating a promise from scratch. You simply callPromise(fn). There is a complete specification for what is returnedby this method inPromisesA+ httppromises-aplus.github.compromises-spec__... code python    from promise import Promise    promise  Promise(        lambda resolve, reject resolve(RESOLVED)    )API---Before all examples, you will need.. code python    from promise import PromisePromise(resolver)This creates and returns a new promise. resolver must be a function.The resolver function is passed two arguments1. resolve should be called with a single argument. If it is called   with a non-promise value then the promise is fulfilled with that   value. If it is called with a promise (A) then the returned promise   takes on the state of that new promise (A).2. reject should be called with a single argument. The returned   promise will be rejected with that argument.Class MethodsThese methods are invoked by calling Promise.methodName.Promise.resolve(value)Converts values and foreign promises into PromisesA+ promises. If youpass it a value then it returns a Promise for that value. If you pass itsomething that is close to a promise (such as a jQuery attempt at apromise) it returns a Promise that takes on the state of value(rejected or fulfilled).Promise.reject(value)Returns a rejected promise with the given value.Promise.all(list)Returns a promise for a list. If it is called with a single argumentthen this returns a promise for a copy of that list with any promisesreplaced by their fulfilled values. e.g... code python    p  Promise.all([Promise.resolve(a), b, Promise.resolve(c)])            .then(lambda res res  [a, b, c])    assert p.get() is TruePromise.cast(obj)This function wraps the obj act as a Promise if possible. PythonFuture s are supported, with a callback to promise.done whenresolved. Have the same effects as Promise.resolve(obj).Promise.for_dict(d)A special function that takes a dictionary of promises and turns theminto a promise for a dictionary of values. In other words, this turns andictionary of promises for values into a promise for a dictionary ofvalues.Promise.is_thenable(obj)This function checks if the obj is a Promise, or could becast ed.Promise.promisify(func)This function wraps the result of calling func in a Promiseinstance.Instance MethodsThese methods are invoked on a promise instance by callingmyPromise.methodNamepromise.then(did_fulfill, did_reject)This method follows the PromisesA+spec httppromises-aplus.github.iopromises-spec__. It explainsthings very clearly so I recommend you read it.Either did_fulfill or did_reject will be called and they willnot be called more than once. They will be passed a single argument andwill always be called asynchronously (in the next turn of the eventloop).If the promise is fulfilled then did_fulfill is called. If thepromise is rejected then did_reject is called.The call to .then also returns a promise. If the handler that iscalled returns a promise, the promise returned by .then takes on thestate of that returned promise. If the handler that is called returns avalue that is not a promise, the promise returned by .then will befulfilled with that value. If the handler that is called throws anexception then the promise returned by .then is rejected with thatexception.promise.catch(did_reject)Sugar for promise.then(None, did_reject), to mirror catch insynchronous code.promise.done(did_fulfill, did_reject)The same semantics as .then except that it does not return a promiseand any exceptions are re-thrown so that they can be logged (crashingthe application in non-browser environments)ContributingAfter cloning this repo, ensure dependencies are installed by running.. code sh    pip install -e .[test]After developing, the full test suite can be evaluated by running.. code sh    py.test tests --covpromise --benchmark-skip  Use -v -s for verbose modeYou can also run the benchmarks with.. code sh    py.test tests --benchmark-onlyStatic type checking--------------------Python type annotations are very useful for making sure we use thelibary the way is intended.You can run mypy static type checker.. code sh    pip install mypy    mypy promise  --ignore-missing-importsOr pyre.. code sh    pip install pyre-check    pyre --source-directory promise checkNotesThis package is heavily insipired inaplus httpsgithub.comxogenyaplus__.License-------MITLicense httpsgithub.comsyrusakbarypromiseblobmasterLICENSE__.. travis image httpsimg.shields.iotravissyrusakbarypromise.svgstyleflat   target httpstravis-ci.orgsyrusakbarypromise.. pypi image httpsimg.shields.iopypivpromise.svgstyleflat   target httpspypi.python.orgpypipromise.. coveralls image httpscoveralls.iorepossyrusakbarypromisebadge.svgbranchmasterservicegithub   target httpscoveralls.iogithubsyrusakbarypromisebranchmaster</longdescription>
</pkgmetadata>