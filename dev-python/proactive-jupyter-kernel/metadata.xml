<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>[License BSD](httpsimg.shields.iobadgeLicense-BSD-blue.svg License BSD)[Python 3](httpsimg.shields.iobadgePython-3-brightgreen.svg Python 3)[blabla](httpsimg.shields.iopypivproactive-jupyter-kernel.svg bla) proactive-jupyter-kernelThe ActiveEon Jupyter Kernel adds a kernel backend to Jupyter. This kernel interfaces directly with the ProActivescheduler and constructs tasks and workflows to execute them on the fly.With this interface, users can run their code locally and test it using a native python kernel, and by a simple switch toProActive kernel, run it on remote public or private infrastructures without having to modify the code. See the example below[Image not found](.imagesdirect_execution_from_jupyter.png Direct execution from Jupyter with ActiveEon Kernel) 1. Installation 1.1 RequirementsPython 2 or 3 1.2 Using PyPi open a terminal install the ProActive jupyter kernel with the following commandsbash pip install proactive proactive-jupyter-kernel --upgrade python -m proactive-jupyter-kernel.install 1.3 Using source code open a terminal clone the repository on your local machinebash git clone gitgithub.comow2-proactiveproactive-jupyter-kernel.git install the ProActive jupyter kernel with the following commandsbash pip install proactive-jupyter-kernel python -m proactive-jupyter-kernel.install 2. PlatformYou can use any jupyter platform.We recommend to use [jupyter lab](httpsjupyterlab.readthedocs.ioenstable). To launch it from your terminal afterhaving installed itbash jupyter labor in daemon modebash nohup jupyter lab devnull When opened, click on the ProActive icon to open a notebook based on the ProActive kernel. 3. HelpAs a quick start, we recommend the user to run the help() pragma using the following scriptpythonhelp()This script gives a brief description of all the different pragmas that the ProActive Kernel provides.To get a more detailed description of a needed pragma, the user can run the following scriptpythonhelp(pragmaPRAGMA_NAME) 4. Connection 4.1 Using connect()If you are trying ProActive for the first time, sign up on the [try platform](httpstry.activeeon.comsignup.html).Once you receive your login and password, connect to the trial platform using the connect() pragmapythonconnect(loginYOUR_LOGIN, passwordYOUR_PASSWORD)To connect to another ProActive server host, use the later pragma this waypythonconnect(hostYOUR_HOST, [portYOUR_PORT], loginYOUR_LOGIN, passwordYOUR_PASSWORD)Notice that the port parameter is optional. The default connexion port is 8080.You can also connect to a distant server by providing its url in the following waypythonconnect(urlYOUR_SERVER_URL, loginYOUR_LOGIN, passwordYOUR_PASSWORD)By providing the complete url of the server, users can eventually connect through the secure HTTPS protocol. 4.2 Using a configuration fileFor automatic sign in, create a file named proactive_config.ini in your notebook working directory.Fill your configuration file according to one of the following two formats- By providing the server host and portini[proactive_server]hostYOUR_HOSTportYOUR_PORT[user]loginYOUR_LOGINpasswordYOUR_PASSWORD- By providing the server urlini[proactive_server]urlYOUR_SERVER_URL[user]loginYOUR_LOGINpasswordYOUR_PASSWORDSave your changes and restart the ProActive kernel.You can also force the current kernel to connect using any _.ini_ config file through the connect() pragmapythonconnect(pathPATH_TOYOUR_CONFIG_FILE.ini)(For more information about this format please check[configParser](httpsdocs.python.org3libraryconfigparser.html)) 5. Usage 5.1 Creating a Python taskTo create a new task, use the pragma task() followed by the task implementation script written into a notebookblock code.To use this pragma, a task name has to be provided at least. Examplepythontask(namemyTask)print(Hello world)General usagepythontask(nameTASK_NAME, [languageSCRIPT_LANGUAGE], [dep[TASK_NAME1,TASK_NAME2,...]], [generic_info[(KEY1,VAL1), (KEY2,VALUE2),...]], [variables[(VAR1,VAL1), (VAR2,VALUE2),...]], [export[VAR_NAME1,VAR_NAME2,...]], [import[VAR_NAME1,VAR_NAME2,...]], [pathIMPLEMENTATION_FILE_PATH])nUsers can also provide more information about the task using the pragmas options. In the following, we give moredetails about the possible options 5.1.1 LanguageThe language parameter is needed when the task script is not written in native Python. If not provided, Python will be selected as the default language.The supported programming languages are Linux_Bash Windows_Cmd DockerCompose Scalaw Groovy Javascript Jython Python Ruby Perl PowerShell RHere is an example that shows a task implementation written in Linux_Bashbashtask(namemyTask, languageLinux_Bash)echo Hello, World 5.1.2 DependenciesOne of the most important notions in workflows is the dependencies between tasks. To specify this information, use thedep parameter. Its value should be a list of all tasks on which the new task depends. Examplepythontask(namemyTask,dep[parentTask1,parentTask2])print(Hello world) 5.1.3 VariablesTo specify [task variables](httpsdoc.activeeon.comlatestuserProActiveUserGuide.html_task_variables), you should provide the variables parameter. Its value should be a list of tuples (key,value) that corresponds to the names and adequate values of the corresponding task variables. Examplepythontask(namemyTask, variables[(var1,value1),(var2,value2)])print(Hello world) 5.1.4 Generic informationTo specify the values of some advanced ProActive variables called[generic_information](httpsdoc.activeeon.comlatestuserProActiveUserGuide.html_generic_information), you shouldprovide the generic_info parameter. Its value should be a list of tuples (key,value) that corresponds to the namesand adequate values of the Generic Information. Examplepythontask(namemyTask, generic_info[(var1,value1),(var2,value2)])print(Hello world) 5.1.5 Exportimport variablesThe export and import parameters ensure variables propagation between the different tasks of a workflow.If myTask1 variables var1 and var2 are needed in myTask2, both pragmas have to specify this information asfollows myTask1 should include an export parameter with a list of these variable names, myTask2 should include an import parameter with a list including the same names.ExamplemyTask1 implementation block would bepythontask(namemyTask1, export[var1,var2])var1  Hellovar2  ActiveEonand myTask2 implementation block would bepythontask(namemyTask2, dep[myTask1], import[var1,var2])print(var1 +  from  + var2) 5.1.6 Implementation fileIt is also possible to use an external implementation file to define the task implementation. To do so, the option path should be used.Examplepythontask(namemyTask,pathPATH_TOIMPLEMENTATION_FILE.py) 5.2 Importing librariesThe main difference between the ProActive and native language kernels resides in the way the memory is accessedduring blocks execution. In a common native language kernel, the whole script code (all the notebook blocks) islocally executed in the same shared memory space whereas the ProActive kernel will execute each created task in anindependent process. In order to facilitate the transition from native language to ProActive kernels, we included thepragma import(). This pragma gives the user the ability to add libraries that are common to all created tasks, andthus relative distributed processes, that are implemented in the same native script language.The import pragma is used as followsimport([languageSCRIPT_LANGUAGE]).Examplepythonimport(languagePython)import osimport pandasNOTE If the language is not specified, Python is considered as default language. 5.3 Adding a fork environmentTo configure a fork environment for a task, use the fork_env() pragma. To do so, you have to provide the name of thecorresponding task and the fork environment implementation.Examplepythonfork_env(nameTASK_NAME)containerName  activeeondlm3dockerRunCommand   docker run dockerParameters  --rm paHomeHost  variables.get(PA_SCHEDULER_HOME)paHomeContainer  variables.get(PA_SCHEDULER_HOME)proActiveHomeVolume  -v +paHomeHost ++paHomeContainer+ workspaceHost  localspaceworkspaceContainer  localspaceworkspaceVolume  -v +localspace ++localspace+ containerWorkingDirectory  -w +workspaceContainer+ preJavaHomeCmd  dockerRunCommand + dockerParameters + proActiveHomeVolume + workspaceVolume + containerWorkingDirectory + containerNameOr, you can provide the task name and the path of a _.py_ file containing the fork environment codepythonfork_env(nameTASK_NAME, pathPATH_TOFORK_ENV_FILE.py) 5.4 Adding a selection scriptTo add a selection script to a task, use the selection_script() pragma. To do so, you have to provide the name ofthe corresponding task and the selection code implementation.Examplepythonselection_script(nameTASK_NAME)selected  TrueOr, you can provide the task name and the path of a _.py_ file containing the selection codepythonselection_script(nameTASK_NAME, pathPATH_TOSELECTION_CODE_FILE.py) 5.5 Adding job fork environment andor selection scriptIf the selection scripts andor the fork environments are the same for all job tasks, we can add them just once usingthe job_selection_script andor the job_fork_env pragmas.UsageFor a job selection script, please usepythonjob_selection_script([languageSCRIPT_LANGUAGE], [path.SELECTION_CODE_FILE.py], [forceonoff])For a job fork environment, usepythonjob_fork_env([languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py], [forceonoff])The force parameter defines whether the pragma has to overwrite the task selection scripts or the fork environmentalready set. 5.6 Adding pre andor post scriptsSometimes, specific scripts has to be executed before andor after a particular task. To do that, the solution provides pre_script and post_script pragmas.To add a pre-script to a task, please usepythonpre_script(nameTASK_NAME, languageSCRIPT_LANGUAGE, [path.PRE_SCRIPT_FILE.py])To add a post-script to a task, usepythonpost_script(nameTASK_NAME, languageSCRIPT_LANGUAGE, [path.POST_SCRIPT_FILE.py]) 5.7 Branch controlThe [branch](httpsdoc.activeeon.comlatestuserProActiveUserGuide.html_branch) control provides the ability to choose between two alternative task flows, with the possibility to merge back to a common one.To add a branch control to the current workflow, four specific tasks and one control condition should be added in accordance with the following order1. a branch task,2. the related branching condition script,3. an if task that should be executed if the result of the condition task if true,4. an else task that should be executed if the result of the condition task if false,5. a continuation task that should be executed after the if or the else tasks.To add a branch task, you can rely on the following macropythonbranch([nameTASK_NAME], [dep[TASK_NAME1,TASK_NAME2,...]], [generic_info[(KEY1,VAL1), (KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py])For the branching condition script, usepythoncondition()For an if task, please usepythonif([nameTASK_NAME], [generic_info[(KEY1,VAL1),(KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py])For an else task, usepythonelse([nameTASK_NAME], [generic_info[(KEY1,VAL1),(KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py])And finally, for the continuation taskpythoncontinuation([nameTASK_NAME], [generic_info[(KEY1,VAL1),(KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py]) 5.8 Loop controlThe [loop](httpsdoc.activeeon.comlatestuserProActiveUserGuide.html_loop) control provides the ability to repeat a set of tasks.To add a loop control to the current workflow, two specific tasks and one control condition should be added in the following order1. a start task,2. the related looping condition script,3. a loop task.For a start task, usepythonstart([nameTASK_NAME], [dep[TASK_NAME1,TASK_NAME2,...]], [generic_info[(KEY1,VAL1), (KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py])For the looping condition script, usepythoncondition()For a loop task, please usepythonloop([nameTASK_NAME], [generic_info[(KEY1,VAL1),(KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py]) 5.9 Replicate controlThe [replication](httpsdoc.activeeon.comlatestuserProActiveUserGuide.html_replicate) allows the execution of multiple tasks in parallel when only one task is defined, and the number of tasks to run could change.Through the ProActive Jupyter Kernel, users can add replicate controls in two main ways, a generic and a straight forward way. 5.9.1 Generic usageTo add a replicate control to the current workflow in the generic method, three specific tasks and one control runs script should be added according to the following order1. a split task,2. the related replication runs script,3. a process task,4. a merge task.For a split task, usepythonsplit([nameTASK_NAME], [dep[TASK_NAME1,TASK_NAME2,...]], [generic_info[(KEY1,VAL1), (KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py])For the replication runs script, usepythonruns()For a process task, please usepythonprocess([nameTASK_NAME], [generic_info[(KEY1,VAL1),(KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py])And finally, for a merge task, usepythonmerge([nameTASK_NAME], [generic_info[(KEY1,VAL1),(KEY2,VALUE2),...]], [languageSCRIPT_LANGUAGE], [path.FORK_ENV_FILE.py]) 5.9.2 Straight forward usageThe straight forward method to add a replication is most of all useful when the parallelism that should be implemented is a task parallelism (the generic usage is more adapted to data parallelism).To add a replication to a task, just add the runs control script by providing the runs option of the task pragma.Examplepythontask(nameT2,dep[T1],runs3)print(This output should be displayed 3 times ...)NOTE To construct a valid workflow, straight forward replicated tasks must have one and only one parent task and one child task at most. More information about replicate validation criteria are available [here](httpsdoc.activeeon.comlatestuserProActiveUserGuide.html_replicate). 5.10 Delete a taskTo delete a task from the workflow, the user should run the pragma delete_task() in the following waypythondelete_task(nameTASK_NAME) 5.11 Create a jobTo create a job, specify job variables andor job generic information, use the job() pragmapythonjob(nameJOB_NAME, [generic_info[(KEY1,VAL1), (KEY2,VALUE2),...]], [variables[(VAR1,VAL1), (VAR2,VALUE2),...]])NOTE It is not necessary to create and assign a name explicitly to the job. If not done by the user, this step isimplicitly performed when the job is submitted (check section [Submit your job to the scheduler](510-submit-your-job-to-the-scheduler) for moreinformation). 5.12 Visualize jobTo visualize the created workflow, use the draw_job() pragma to plot the workflow graph that represents the jobinto a separate windowpythondraw_job()Two optional parameters can be used to configure the way the kernel plots the workflow graph.inline plottingIf this parameter is set to off, plotting the workflow graph is done through a [Matplotlib](httpsmatplotlib.org) external window. The default value is on.pythondraw_job(inlineoff)save the workflow graph locallyTo be sure that the workflow is saved into a _.png_ file, this option needs to be set to on. The default value isoff.pythondraw_job(saveon)Note that the jobs name can take one of the following possible values1. The parameter name s value, if provided2. The jobs name, if created3. The notebooks name, if the kernel can retrieve it4. Unnamed_job, otherwiseGeneral usagepythondraw_job([nameJOB_NAME], [inlineoff], [saveon]) 5.13 Export the workflow in dot formatTo export the created workflow into a [GraphViz](httpswww.graphviz.org) _.dot_ format, use the write_dot() pragmapythonwrite_dot(nameFILE_NAME) 5.14 Import a workflow from a dot fileTo create a workflow according to a [GraphViz](httpswww.graphviz.org) _.dot_ file, use the pragma import_dot()pythonimport_dot(pathPATH_TOFILE_NAME.dot)By default, the workflow will contain _Python_ tasks with empty implementation scripts. If you want to modify or add any information to a specific task, please use, as explained in [Creating a Task](51-creating-a-python-task), the task() pragma. 5.15 Submit your job to the schedulerTo submit the job to the ProActive Scheduler, the user has to use the submit_job() pragmapythonsubmit_job()If the job is not created, or is not up-to-date, the submit_job() creates a new job named as the old one.To provide a new name, use the same pragma and provide a name as parameterpythonsubmit_job([nameJOB_NAME])If the jobs name is not set, the ProActive kernel uses the current notebook name, if possible, or gives a random one. 5.16 List all submitted jobsTo get all submitted job IDs and names, use list_submitted_jobs pragma this waypythonlist_submitted_jobs() 5.17 Export the workflow in XML formatTo export the created workflow in _.xml_ format, use the export_xml() pragmapythonexport_xml([nameFILENAME])Notice that the _.xml_ file will be saved under one of the following names1. The parameter name s value, if provided2. The jobs name, if created3. The notebooks name, if the kernel can retrieve it4. Unnamed_job, otherwise 5.18 Get resultsAfter the execution of a ProActive workflow, two outputs can be obtained, results values that have been saved in the [task result variable](httpsdoc.activeeon.comlatestuserProActiveUserGuide.html_task_result), console outputs classic outputs that have been displayedprinted To get task results, please use the get_task_result() pragma by providing the task name, and either the job ID orthe job namepythonget_task_result([job_idJOB_ID], [job_nameJOB_NAME], task_nameTASK_NAME)The result(s) of all the tasks of a job can be obtained with the get_job_result() pragma, by providing the job nameor the job IDpythonget_job_result([job_idJOB_ID], [job_nameJOB_NAME])To get and display console outputs of a task, you can use the print_task_output() pragma in the followingwaypythonprint_task_output([job_idJOB_ID], [job_nameJOB_NAME], task_nameTASK_NAME)Finally, the  print_job_output() pragma allows to print all job outputs, by providing the job name or the job IDpythonprint_job_output([job_idJOB_ID], [job_nameJOB_NAME])NOTE If neither job_name nor the job_id are provided, the last submitted job is selected by default.  6. Display and use ActiveEon Portals directly in JupyterFinally, to have the hand on more parameters and features, the user should use ActiveEon Studio portals.The main ones are the _Resource Manager_, the _Scheduling Portal_ and the _Workflow Automation_.The example below shows how the user can directly monitor his submitted jobs execution in the scheduling portal[Image not found](.imagesdirect_submit_and_see_from_jupyter.png Directly in Jupyter Submit, See your Job executing, Get Results)To show the resource manager portal related to the host you are connected to, just runpythonshow_resource_manager([hostYOUR_HOST], [heightHEIGHT_VALUE], [widthWIDTH_VALUE])For the related scheduling portalpythonshow_scheduling_portal([hostYOUR_HOST], [heightHEIGHT_VALUE], [widthWIDTH_VALUE])And, for the related workflow automationpythonshow_workflow_automation([hostYOUR_HOST], [heightHEIGHT_VALUE], [widthWIDTH_VALUE])NOTE The parameters height and width allow the user to adjust the size of the window inside the notebook. Current statusFeatures help prints all different pragmasfeatures of the kernel connect connects to an ActiveEon server (OPTION connection using a configuration file) import import specified libraries to all tasks of a same script language task creates a task pre_script sets the pre-script of a task post_script sets the post-script of a task selection_script sets the selection script of a task job_selection_script sets the default selection script of a job fork_env sets the fork environment script job_fork_env sets the default fork environment of a job split createsmodifies a splitting task of a replicate control runs createsmodifies the configuration script of a replicate control process createsmodifies the script of a replicated processing task merge createsmodifies a merging task of a replicate control start createsmodifies a start task of a loop control loop createsmodifies a loop task of a loop control condition createsmodifies the condition script of a branchloop control branch createsmodifies a branch task of a branching control if createsmodifies an if task of a branching control else createsmodifies an else task of a branching control continuation createsmodifies a continuation task of a branching control delete_task deletes a task from the workflow job createsrenames the job draw_job plot the workflow write_dot writes the workflow in .dot format import_dot imports the workflow from a .dot file submit_job submits the job to the scheduler get_result gets and prints the job results get_job_result gets and prints the job results get_task_result gets and prints the results of a given task print_job_output gets and prints the job outputs print_task_output gets and prints the outputs of a given task list_submitted_jobs gets and prints the ids and names of the submitted jobs export_xml exports the workflow in .xml format show_resource_manager opens the ActiveEon resource manager portal show_scheduling_portal opens the ActiveEon scheduling portal show_workflow_automation opens the ActiveEon workflow automation portal TODO Features improvements execute in local a pragma free block add options import_as_jsonexport_as_json add draw(onoff), print_result(onoff) options in submit job pragma multiple pragmas in a block handling apply selection_script and fork_env to a list of names (tasks) add auto-complete Documentation add some examples pictures add configure pragma section (block, multiblock)</longdescription>
</pkgmetadata>