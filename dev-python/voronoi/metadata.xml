<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>voronoi=======[![](https://github.com/lycantropos/voronoi/workflows/CI/badge.svg)](https://github.com/lycantropos/voronoi/actions/workflows/ci.yml &quot;Github Actions&quot;)[![](https://codecov.io/gh/lycantropos/voronoi/branch/master/graph/badge.svg)](https://codecov.io/gh/lycantropos/voronoi &quot;Codecov&quot;)[![](https://img.shields.io/github/license/lycantropos/voronoi.svg)](https://github.com/lycantropos/voronoi/blob/master/LICENSE &quot;License&quot;)[![](https://badge.fury.io/py/voronoi.svg)](https://badge.fury.io/py/voronoi &quot;PyPI&quot;)In what follows `python` is an alias for `python3.7` or `pypy3.7`or any later version (`python3.8`, `pypy3.8` and so on).Installation------------Install the latest `pip` &amp; `setuptools` packages versions```bashpython -m pip install --upgrade pip setuptools```### UserDownload and install the latest stable version from `PyPI` repository```bashpython -m pip install --upgrade voronoi```### DeveloperDownload the latest version from `GitHub` repository```bashgit clone https://github.com/lycantropos/voronoi.gitcd voronoi```Install```bashpython setup.py install```Usage-----```python&gt;&gt;&gt; from voronoi.diagram import Diagram&gt;&gt;&gt; diagram = Diagram()&gt;&gt;&gt; diagram.construct([], [])&gt;&gt;&gt; diagram.cells == diagram.edges == diagram.vertices == []True&gt;&gt;&gt; from voronoi.point import Point&gt;&gt;&gt; diagram = Diagram()&gt;&gt;&gt; diagram.construct([Point(0, 0), Point(4, 0), Point(4, 4), Point(0, 4)], [])&gt;&gt;&gt; from voronoi.faces import Vertex&gt;&gt;&gt; diagram.vertices == [Vertex(2, 2)]True&gt;&gt;&gt; from voronoi.segment import Segment&gt;&gt;&gt; diagram.construct([], [Segment(Point(0, 0), Point(4, 0)),...                        Segment(Point(4, 4), Point(0, 4))])&gt;&gt;&gt; diagram.vertices == [Vertex(2, 2), Vertex(0, 2), Vertex(4, 2)]True```for `CPython` original C++ implementation can be invoked by importing from `_voronoi` module instead.Development-----------### Bumping version#### PreparationInstall[bump2version](https://github.com/c4urself/bump2version#installation).#### Pre-releaseChoose which version number category to bump following [semverspecification](http://semver.org/).Test bumping version```bashbump2version --dry-run --verbose $CATEGORY```where `$CATEGORY` is the target version number category name, possiblevalues are `patch`/`minor`/`major`.Bump version```bashbump2version --verbose $CATEGORY```This will set version to `major.minor.patch-alpha`. #### ReleaseTest bumping version```bashbump2version --dry-run --verbose release```Bump version```bashbump2version --verbose release```This will set version to `major.minor.patch`.### Running testsInstall dependencies```bashpython -m pip install --force-reinstall -r requirements-tests.txt```Plain```bashpytest```Inside `Docker` container:- with `CPython`  ```bash  docker-compose --file docker-compose.cpython.yml up  ```- with `PyPy`  ```bash  docker-compose --file docker-compose.pypy.yml up  ````Bash` script (e.g. can be used in `Git` hooks):- with `CPython`  ```bash  ./run-tests.sh  ```  or  ```bash  ./run-tests.sh cpython  ```- with `PyPy`  ```bash  ./run-tests.sh pypy  ````PowerShell` script (e.g. can be used in `Git` hooks):- with `CPython`  ```powershell  .\run-tests.ps1  ```  or  ```powershell  .\run-tests.ps1 cpython  ```- with `PyPy`  ```powershell  .\run-tests.ps1 pypy  ```</longdescription>
</pkgmetadata>