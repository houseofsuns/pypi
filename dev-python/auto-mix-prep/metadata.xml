<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>auto-mix-prep=============Performs operations on wave files in preperation for mixing.Current features^^^^^^^^^^^^^^^^- Gain staging, i.e. adjusting peak dB to desired level- Backup stems before editing- Split stereo files into separate mono tracks (L/R)Planned features^^^^^^^^^^^^^^^^-  Interactive usage-  Renaming files-  Ability to exclude filesRequirements-------------  python (&gt;=3)-  pip3-  `pydub`_Installation------------.. code:: bash    $ sudo pip3 install auto-mix-prepOr.. code:: bash    $ git clone https://github.com/dashj/auto-mix-prep.git .    $ cd auto-mix-prep    $ sudo setup.py installOptions-------``-g, --gainstage``, peak dB desired (-12 dB if not specified).``-s, --splitstereo``, splits stereo files into two mono files.``-d, --directory``, directory containing wave files (working directoryif not specified).``-f, --file``, select individual file instead of all wave files indirectory.``-b, --backup``, creates a backup before overwriting (creates folder inworking directory if not specified).Usage-----.. code:: bash    $ amp &lt;operation&gt;For example:.. code:: bash    $ amp -s -g -24 -d /path/to/stems -b /path/to/backup_directoryFirst creates a backup in ``/path/to/backup_directory`` then searches``/path/to/stems`` for wave files and performs the selected operations.Which in this case would be splitting the stereo files into mono andgain staging with -24 dB as the peak amplitude... _pydub: https://github.com/jiaaro/pydub</longdescription>
</pkgmetadata>