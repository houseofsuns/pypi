<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> JSONRPClib (patched for Pelix and Python 3)[[Latest Version](httpsimg.shields.iopypivjsonrpclib-pelix.svg)](httpspypi.python.orgpypijsonrpclib-pelix)[[License](httpsimg.shields.iopypiljsonrpclib-pelix.svg)](httpspypi.python.orgpypijsonrpclib-pelix)[[Travis-CI status](httpstravis-ci.orgtcalmantjsonrpclib.svgbranchmaster)](httpstravis-ci.orgtcalmantjsonrpclib)[[Coveralls status](httpscoveralls.iorepostcalmantjsonrpclibbadge.svgbranchmaster)](httpscoveralls.iortcalmantjsonrpclibbranchmaster)This library is an implementation of the JSON-RPC specification.It supports both the original 1.0 specification, as well as the new(proposed) 2.0 specification, which includes batch submission, keywordarguments, etc.This library is licensed under the terms of the[Apache Software License 2.0](httpwww.apache.orglicensesLICENSE-2.0.html). About this versionThis is a patched version of the original jsonrpclib project by Josh Marshall,available at[joshmarshalljsonrpclib](httpsgithub.comjoshmarshalljsonrpclib).The suffix -pelix only indicates that this version works with PelixRemote Services, but it is not a Pelix specific implementation. This version adds support for Python 3, staying compatible with Python 2.7.  The support for Python 2.6 has been dropped, as it was becoming to hard to  maintain. It is now possible to use the dispatch_method argument while extending the  SimpleJSONRPCDispatcher, to use a custom dispatcher.  This allows to use this package by Pelix Remote Services. It can use thread pools to control the number of threads spawned to handle  notification requests and clients connections. The modifications added in other forks of this project have been added     From [drdaemanjsonrpclib](httpsgithub.comdrdaemanjsonrpclib)       Improved JSON-RPC 1.0 support       Less strict error response handling     From [tuomassalojsonrpclib](httpsgithub.comtuomassalojsonrpclib)       In case of a non-predefined error, raise an AppError and give access        to error.data     From [dejwjsonrpclib](httpsgithub.comdejwjsonrpclib)       Custom headers can be sent with request and associated tests Since version 0.4, this package added back the support of Unix sockets. This package cannot be installed with the original jsonrpclib, as it uses  the same name. SummaryThis library implements the JSON-RPC 2.0 proposed specification in pure Python.It is designed to be as compatible with the syntax of xmlrpclib as possible(it extends where possible), so that projects using xmlrpclib could easily bemodified to use JSON and experiment with the differences.It is backwards-compatible with the 1.0 specification, and supports all of thenew proposed features of 2.0, including- Batch submission (via the MultiCall class)- Keyword arguments- Notifications (both in a batch and normal)- Class translation using the __jsonclass__ key.A SimpleJSONRPCServer class has been added. It is intended to emulate theSimpleXMLRPCServer from the default Python distribution. RequirementsThis library supports ujson, cjson and simplejson, and looks for theparsers in that order (searching first for ujson, cjson, simplejsonand finally for the built-in json).One of these must be installed to use this library, although if you have astandard distribution of 2.7+, you should already have one.Keep in mind that ujson is supposed to be the quickest, I believe, so if youare going for full-on optimization you may want to pick it up. InstallationYou can install this from PyPI with one of the following commands (sudomight be required) Global installationpip install jsonrpclib-pelix Local installationpip install --user jsonrpclib-pelixAlternatively, you can download the source from the GitHub repository at[tcalmantjsonrpclib](httpgithub.comtcalmantjsonrpclib) and manuallyinstall it with the following commandsgit clone gitgithub.comtcalmantjsonrpclib.gitcd jsonrpclibpython setup.py install A note on loggingjsonrpclib-pelix uses the logging module from the standard Pythonlibrary to trace warnings and errors, but doesnt set it up.As a result, you have to configure the Python logging to print out traces.The easiest way to do it is to add those lines at the beginning of your codepythonimport logginglogging.basiConfig()More information can be found in the[logging documentation page](httpsdocs.python.org3librarylogging.html). SimpleJSONRPCServerThis is identical in usage (or should be) to the SimpleXMLRPCServer in thePython standard library.Some of the differences in features are that it obviously supports notification,batch calls, class translation (if left on), etc.Note The import line is slightly different from the regularSimpleXMLRPCServer, since the SimpleJSONRPCServer is provided by thjsonrpclib library.pythonfrom jsonrpclib.SimpleJSONRPCServer import SimpleJSONRPCServerserver  SimpleJSONRPCServer((localhost, 8080))server.register_function(pow)server.register_function(lambda x,y x+y, add)server.register_function(lambda x x, ping)server.serve_forever()To start protect the server with SSL, use the following snippetpythonfrom jsonrpclib.SimpleJSONRPCServer import SimpleJSONRPCServerimport ssl Setup the SSL socketserver  SimpleJSONRPCServer((localhost, 8080), bind_and_activateFalse)server.socket  ssl.wrap_socket(server.socket, certfileserver.pem,                                server_sideTrue)server.server_bind()server.server_activate() ... register functions Start the serverserver.serve_forever() Notification Thread PoolBy default, notification calls are handled in the request handling thread.It is possible to use a thread pool to handle them, by giving it to the serverusing the set_notification_pool() methodpythonfrom jsonrpclib.SimpleJSONRPCServer import SimpleJSONRPCServerfrom jsonrpclib.threadpool import ThreadPool Setup the thread pool between 0 and 10 threadspool  ThreadPool(max_threads10, min_threads0) Dont forget to start itpool.start() Setup the serverserver  SimpleJSONRPCServer((localhost, 8080))server.set_notification_pool(pool) Register methodsserver.register_function(pow)server.register_function(lambda x,y x+y, add)server.register_function(lambda x x, ping)try    server.serve_forever()finally     Stop the thread pool (let threads finish their current task)    pool.stop()    server.set_notification_pool(None) Threaded serverIt is also possible to use a thread pool to handle clients requests, using thePooledJSONRPCServer class.By default, this class uses pool of 0 to 30 threads.A custom pool can be given with the thread_pool parameter of the classconstructor.The notification pool and the request pool are different by default, a serverwith a request pool doesnt have a notification pool.pythonfrom jsonrpclib.SimpleJSONRPCServer import PooledJSONRPCServerfrom jsonrpclib.threadpool import ThreadPool Setup the notification and request poolsnofif_pool  ThreadPool(max_threads10, min_threads0)request_pool  ThreadPool(max_threads50, min_threads10) Dont forget to start themnofif_pool.start()request_pool.start() Setup the serverserver  PooledJSONRPCServer((localhost, 8080), thread_poolrequest_pool)server.set_notification_pool(nofif_pool) Register methodsserver.register_function(pow)server.register_function(lambda x,y x+y, add)server.register_function(lambda x x, ping)try    server.serve_forever()finally     Stop the thread pools (let threads finish their current task)    request_pool.stop()    nofif_pool.stop()    server.set_notification_pool(None) Unix socketTo start a server listening on a Unix socket, you will have to use thefollowing snippetpythonfrom jsonrpclib.SimpleJSONRPCServer import SimpleJSONRPCServerimport osimport socket Set the path to the socket filesocket_name  tmpmy_socket.socket Ensure that the file doesnt exist yet (or an error will be raised)if os.path.exists(socket_name)   os.remove(socket_name)try    Start the server, indicating the socket family    The server will force some flags when in Unix socket mode    (no log request, no reuse address, ...)   srv  SimpleJSONRPCServer(socket_name, address_familysocket.AF_UNIX)    ... register methods to the server    Run the server   srv.serve_forever()except KeyboardInterrupt    Shutdown the server gracefully   srv.shutdown()   srv.server_close()finally    You should clean up after the server stopped   os.remove(socket_name)This feature is tested on Linux during Travis-CI builds. It also hasbeen tested on Windows Subsystem for Linux (WSL) on Windows 10 1809.This feature is not available on pure Windows, as it doesnt providethe AF_UNIX address family. Client UsageThis is (obviously) taken from a console session.python import jsonrpclib server  jsonrpclib.ServerProxy(httplocalhost8080) server.add(5,6)11 server.add(x5, y10)15 server._notify.add(5,6) No result returned... batch  jsonrpclib.MultiCall(server) batch.add(5, 6) batch.ping({keyvalue}) batch._notify.add(4, 30) results  batch() for result in results ... print(result)11{key value} Note that there are only two responses -- this is according to spec. Clean up server(close)() Using client history history  jsonrpclib.history.History() server  jsonrpclib.ServerProxy(httplocalhost8080, historyhistory) server.add(5,6)11 print(history.request){id f682b956-c8e1-4506-9db4-29fe8bc9fcaa, jsonrpc 2.0, method add, params [5, 6]} print(history.response){id f682b956-c8e1-4506-9db4-29fe8bc9fcaa, jsonrpc 2.0, result 11} Clean up server(close)()If you need 1.0 functionality, there are a bunch of places you can passthat in, although the best is just to give a specific configuration tojsonrpclib.ServerProxypython import jsonrpclib jsonrpclib.config.DEFAULT.version2.0 config  jsonrpclib.config.Config(version1.0) history  jsonrpclib.history.History() server  jsonrpclib.ServerProxy(httplocalhost8080, configconfig,                                    historyhistory) server.add(7, 10)17 print(history.request){id 827b2923-5b37-49a5-8b36-e73920a16d32, method add, params [7, 10]} print(history.response){id 827b2923-5b37-49a5-8b36-e73920a16d32, error null, result 17} server(close)()The equivalent loads and dumps functions also exist, although withminor modifications.The dumps arguments are almost identical, but it adds three argumentsrpcid for the id key, version to specify the JSON-RPC compatibility,and notify if its a request that you want to be a notification.Additionally, the loads method does not return the params and method likexmlrpclib, but insteada.) parses for errors, raising ProtocolErrors, andb.) returns the entire structure of the request  response for manual parsing. Unix socketsTo connect a JSON-RPC server over a Unix socket, you have to use a specificprotocol unix+http.When connecting to a Unix socket in the current working directory, you can usethe following syntax unix+httpmy.socketWhen you need to give an absolute path you must use the path part of the URL,the host part will be ignored. For example, you can use this URL to indicate aUnix socket in varlibdaemon.socket unix+http.varlibdaemon.socketNote Currently, only HTTP is supported over a Unix socket.If you want HTTPS support to be implemented, please create an[issue on GitHub](httpsgithub.comtcalmantjsonrpclibissues) Additional headersIf your remote service requires custom headers in request, you can pass themusing the headers keyword argument, when creating the ServerProxypython import jsonrpclib server  jsonrpclib.ServerProxy(httplocalhost8080,                                    headers{X-Test  Test})You can also put additional request headers only for certain methodinvocationpython import jsonrpclib server  jsonrpclib.Server(httplocalhost8080) with server._additional_headers({X-Test  Test}) as test_server...     test_server.ping(42)...  X-Test header will be no longer sent in requestsOf course _additional_headers contexts can be nested as well. Class TranslationThe library supports an automatic class translation process, although itis turned off by default.This can be devastatingly slow if improperly used, so the following is just ashort list of things to keep in mind when using it.- Keep It (the object) Simple Stupid. (for exceptions, keep reading)- Do not require init params (for exceptions, keep reading)- Getter properties without setters could be dangerous (read not tested)If any of the above are issues, use the _serialize method (see usage below).The server and client must BOTH have the use_jsonclass configurationitem on and they must both have access to the same libraries used by theobjects for this to work.If you have excessively nested arguments, it would be better to turn off thetranslation and manually invoke it on specific objects usingjsonrpclib.jsonclass.dump  jsonrpclib.jsonclass.load (since thedefault behavior recursively goes through attributes and listsdictstuples). Sample file test_obj.pypython This object is very simple, and the system will look through the attributes and serialize what it can.class TestObj(object)    foo  bar This object requires __init__ params, so it uses the _serialize method and returns a tuple of init params and attribute values (the init params can be a dict or a list, but the attribute values must be a dict.)class TestSerial(object)    foo  bar    def __init__(self, args)        self.args  args    def _serialize(self)        return (self.args, {fooself.foo,})- Sample usagepython import jsonrpclib import test_obj History is used only to print the serialized form of beans history  jsonrpclib.history.History() testobj1  test_obj.TestObj() testobj2  test_obj.TestSerial() server  jsonrpclib.Server(httplocalhost8080, historyhistory) The ping just returns whatever is sent ping1  server.ping(testobj1) ping2  server.ping(testobj2) print(history.request){id 7805f1f9-9abd-49c6-81dc-dbd47229fe13, jsonrpc 2.0, method ping, params [{__jsonclass__                               [test_obj.TestSerial, []], foo bar}                             ]} print(history.response){id 7805f1f9-9abd-49c6-81dc-dbd47229fe13, jsonrpc 2.0, result {__jsonclass__ [test_obj.TestSerial, []], foo bar}}This behavior is turned on by default.To deactivate it, just set the use_jsonclass member of a server Config toFalse.If you want to use a per-class serialization method, set its name in theserialize_method member of a server Config.Finally, if you are using classes that you have defined in the implementation(as in, not a separate library), youll need to add those(on BOTH the server and the client) using the config.classes.add() method.Feedback on this feature is very, VERY much appreciated. TestsTests are an almost-verbatim drop from the JSON-RPC specification 2.0page. They can be run using unittest or nosetestpython -m unittest discover testspython3 -m unittest discover testsnosetests tests Why JSON-RPCIn my opinion, there are several reasons to choose JSON over XML for RPC Much simpler to read (I suppose this is opinion, but I know Im right. ) Size  Bandwidth - Main reason, a JSON object representation is just much  smaller. Parsing - JSON should be much quicker to parse than XML. Easy class passing with jsonclass (when enabled)In the interest of being fair, there are also a few reasons to choose XML overJSON Your server doesnt do JSON (rather obvious) Wider XML-RPC support across APIs (can we change this )) Libraries are more established, i.e. more stable (Lets change this too)</longdescription>
</pkgmetadata>