<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> timebudget A stupidly-simple tool to see where your time is going in Python programsTrying to figure out where the times going in your python code  Tired of writing elapsed  time.time() - start_time  You can find out with just a few lines of code after youpip install timebudget The simplest wayWith just two lines of code (one is the import), you can see how long something takes...pythonfrom timebudget import timebudgetwith timebudget(Loading and processing the file)    raw  open(filename,rt).readlines()    lines  [line.rstrip() for line in raw]will printLoading and processing the file took 1.453sec Record times and print a reportTo get a report on the total time from functions you care about, just annotate those functionspythonfrom timebudget import timebudgettimebudget.set_quiet()   dont show measurements as they happentimebudget.report_at_exit()   Generate report when the program exitstimebudget   Record how long this function takesdef possibly_slow()    ...timebudget   ... and this function toodef should_be_fast()    ...And now when you run your program, youll see how much time was spent in each annotated functiontimebudget report...            possibly_slow  901.12ms for      3 calls           should_be_fast   61.35ms for      2 callsOr instead of calling report_at_exit() you can manually call pythontimebudget.report(resetTrue)   print out the report now, and reset the statisticsIf you dont set resetTrue then the statistics will accumulate into the next report.You can also wrap specific blocks of code to be recorded in the report, and optionally overridethe default set_quiet choice for any blockpythonwith timebudget(load-file, quietFalse)    text  open(filename,rt).readlines() Percent of time in a loopIf you are doing something repeatedly, and want to know the percent of time doing different things, time the loop itself, and pass the name to report.  That is, add a timebudget annotation or wrapper onto the thing which is happening repeatedly.  Each time this method or code-block is entered will now be considered one cycle and your report will tell you what fraction of time things take within this cycle.pythontimebudgetdef outer_loop()    if sometimes()        possibly_slow()    should_be_fast()    should_be_fast()for _ in range(NUM_CYCLES)    outer_loop()timebudget.report(outer_loop)Then the report looks liketimebudget report per outer_loop cycle...               outer_loop 100.0   440.79mscyc      1.0 callscyc            possibly_slow  40.9   180.31mscyc      0.6 callscyc           should_be_fast  13.7    60.19mscyc      2.0 callscycHere, the times in milliseconds are the totals (averages per cycle), not the average time per call.  So in the above example, should_be_fast is taking about 30ms per call, but being called twice per loop.  Similarly, possibly_slow is still about 300ms each time its called, but its only getting called on 60 of the cycles on average, so on average its using 41 of the time in outer_loop or 180ms. RequirementsNeeds Python 3.6 or higher.  Other libraries are in requirements.txt and can be installed likepip install -r requirements.txt   only needed for developing timebudget. TestingTo run testspytest InspirationThis tool is inspired by [TQDM](httpsgithub.comtqdmtqdm), the awesome progress bar.  TQDM is stupidly simple to add to your code, and just makes it better.  I aspire to imitate that.</longdescription>
</pkgmetadata>