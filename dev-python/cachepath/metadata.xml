<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Welcome to cachepaths documentation.. image httpsimg.shields.iopypivcachepath.svg        target httpspypi.python.orgpypicachepath.. image httpsimg.shields.iotravishaydenflinnercachepath.svg        target httpstravis-ci.orghaydenflinnercachepath.. image httpsreadthedocs.orgprojectscachepathbadgeversionlatest        target httpscachepath.readthedocs.ioenlatestbadgelatest        alt Documentation StatusA small package for pythonic parameterized cache paths.Getting Started----------------Install pip install cachepathImport from cachepath import CachePath, TempPath, PathDocs ReadTheDocs_  API doc is here_Why    1. Integrates pathlib with tempfile.gettempdir and shutil.rmtree by providing TempPath and Path.rm()          path  TempPath()          path.rm()           or would you rather..          path  None          with tempfile.NamedTemporaryFile(deleteFalse) as f              path  Path(f.name)           Only now can we use Path. If we tried using it within the With           (for example for path.read_text()), wed break on Windows          path.unlink()   only if file, doesnt work on folders    2. Wraps pathlib import for Py23 compat. (not in six)          from cachepath import Path           or          try from pathlib import Path except ImportError from pathlib2 import Path    3. Provides CachePath_, which lets you quickly get a parameterized temp filename, with all folders automatically created          r  CachePath(date, userid, expensive_results.txt)          assert (r  Path(tmp, date, userid, expensive_results.txt)                  and r.parent.exists())          r.rm()   File remove          r.parent.rm()   Symmetric with folder remove           Without cachepath          p  Path(tempfile.gettempdir(), date, userid, expensive_results.txt).           Dont update timestamp if it already exists so that we dont cause           Make-like tools to always think somethings changed          if not p.parent.exists()              p.parent.mkdir(parentsTrue, exist_okTrue)          p.unlink()   Why is it .unlink() instead of .remove()           Why .remove and .unlink, but mkdir instead of createdir          p.parent.remove()           .remove() might throw because there was another file in the folder,           but we didnt care, theyre tempfiles          import shutil          shutil.rmtree(p.parent)Why, but longerDo you need a temp path to pass to some random tool for its logfileBehold, a gaping hole in pathlib    import tempfile    import os    try from pathlib import Path except ImportError from pathlib2 import Path    def get_tempfile()        fd, loc  tempfile.mkstemp()        os.close(fd)   If we forgot do this, it would stay open until process exit        return Path(loc)     Easier way    from cachepath import TempPath    def get_tempfile()        return TempPath()   Path(tmp213kjdsrandom)But this module is called cachepath, not temppath, what givesSuppose Im running that same imaginary tool pretty often, but Id like to skip runningit if I already have results for a certain day. Just sticking some identifying info into a filenameshould be good enough.Something like Path(tmp20181204_toolresults.txt)      try from pathlib import Path except ImportError from pathlib2 import Path     Well cheat a little to get py23 compat without so much ugliness    from cachepath import Path    import tempfile    def get_tempfile(date)        filename  {}_toolresults.txt.format(date)        return Path(tempfile.gettempdir(), filename)     Easier to do this...    from cachepath import CachePath    def get_tempfile(date)        return CachePath(date, suffix.txt)Not bad, but not great. But our requirements changed, lets go a step further.Now Im running this tool a lot, over a tree of data that lookslike this    2018-12-23        person1        person2    2018-12-24        person1    2018-12-25        person1I want my logs to be structured the same way.  How hard can it be     2018-12-23        person1_output.txt        person2_output.txt    2018-12-24        person1_output.txt    2018-12-25        person1_output.txtLets find out     Lets get the easy way out of the way first )    def get_path(date, person)        return CachePath(date, person, suffix_output.txt)         Automatically ensures tmpdate exists when we create the CachePath     Now the hard way    def get_path(date, person)        personfilename  {p}_output.txt.format(pperson)        returning  Path(tempfile.gettempdir())datepersonfilename         Does this mkdir update the modified timestamp of the folders were in         Might matter if were part of a larger toolset...        returning.parent.mkdir(exist_okTrue, parentsTrue)        return returningSuppose we hadnt remembered to make the date folders. When we passed thePath out to another tool, or tried to .open it,we may have gotten a Permission Deniederror on Unix systems rather than the FileFolder not found you might expect.With CachePath, this cant happen. Creating a CachePath implicitly creates allof the preceding directories necessary for your file to exist.Now, suppose we found a bug in this external tool we were using and were goingto re-run it for a day.How do we clear out that days results so that we can be sure were lookingat fresh output from the tool Well, with CachePath, its just  def easy_clear_date(date)      CachePath(date).clear()   rm -r tmpdateBut if you dont have cachepath, youll find that most Python libs play itpretty safe when it comes to files. Path.remove() requires the folder to be empty,and doesnt provide a way to empty the folder. Not to mention, what if our resultsfolders had special permissions, or was actually a symlink, and we had write accessbut not delete Oh well,lets see what we can do  def hard_clear_date(date)       We happen to know that date is a folder and not a file (at least in our       current design), so we know we need some form of .remove() rather than       .unlink(). Unfortunately, pathlib doesnt offer one for folders with       files still in them. If you google how to do it, you will find plenty of       answers, one of which is a pure pathlib recursive solution But were lazy,       so lets bring in yet another module      p  Path(tempfile.gettempdir(), date)      import shutil      if p.exists()          shutil.rmtree(p)      p.mkdir(exist_okTrue, parentsTrue)       This still isnt exactly equivalent to CachePath.clear(), because weve       lost whatever permissions were set on the date folder, and if it were       actually a symlink to somewhere else, thats gone now.Convinced yet pip install cachepath or copy the source_ into your localutils.py (you know you have one.)API doc is here_.By the way, as a side effect of importing cachepath, all Paths get the abilityto do rm() and clear().Shameless Promo----------------Find yourself working with paths a lot in cmd-line tools You might likeinvoke_ andor magicinvoke_.. [] The source for CachePath can be downloaded from the Github repo_... _Github repo httpsgithub.comhaydenflinnercachepath.. [] This package was created with Cookiecutter_ and the audreyrcookiecutter-pypackage_ project template... _the source httpsgithub.comhaydenflinnercachepathblobmastercachepath__init__.py.. _Cookiecutter httpsgithub.comaudreyrcookiecutter.. _audreyrcookiecutter-pypackage httpsgithub.comaudreyrcookiecutter-pypackage.. _invoke httpswww.pyinvoke.org.. _magicinvoke httpsmagicinvoke.readthedocs.ioenlatest.. _ReadTheDocs httpscachepath.readthedocs.ioenlatest.. _API doc is here httpscachepath.readthedocs.ioenlatestcachepath.html.. _CachePath httpscachepath.readthedocs.ioenlatestcachepath.htmlcachepath.CachePathHistory1.0.0 (2018-12-08)------------------ Big doc updates. 1.0.0 to symbolize SemVer adherence.0.1.0 (2018-12-08)------------------ First release on PyPI. Adds CachePath, TempPath, Path.</longdescription>
</pkgmetadata>