<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Microsoft Azure Kusto Library for PythonOverview--------.. code-block python    from azure.kusto.data import KustoClient, KustoConnectionStringBuilder    cluster  insert here your cluster name    client_id  insert here your AAD application id    client_secret  insert here your AAD application key    authority_id  insert here your AAD tenant id    kcsb  KustoConnectionStringBuilder.with_aad_application_key_authentication(cluster, client_id, client_secret, authority_id)     It is a good practice to re-use the KustoClient instance, as it maintains a pool of connections to the Kusto service.     This sample shows how to create a client and close it in the same scope, for demonstration purposes.    with KustoClient(kcsb) as client        db  Samples        query  StormEvents  take 10        response  client.execute(db, query)        for row in response.primary_results[0]            print(row[0],  , row[EventType])Kusto Python Client Library provides the capability to query Kusto clusters using Python.It is Python 3.x compatible and supportsall data types through familiar Python DB API interface.Its possible to use the library, for instance, from Jupyter Notebookshttpjupyter.org_.which are attached to Spark clusters,including, but not exclusively, Azure Databrickshttpsazure.microsoft.comen-usservicesdatabricks_. instances.Async ClientKusto now provides an asynchronous client for queries.To use the client, first install the package with the aio extra.. code bash    pip install azure-kusto-data[aio]The async client uses exact same interface as the regular client, exceptthat it lives in the azure.kusto.data.aio namespace, and it returnsFutures you will need to await its.. code python    from azure.kusto.data import KustoConnectionStringBuilder    from azure.kusto.data.aio import KustoClient    cluster  insert here your cluster name    client_id  insert here your AAD application id    client_secret  insert here your AAD application key    authority_id  insert here your AAD tenant id    async def sample()        kcsb  KustoConnectionStringBuilder.with_aad_application_key_authentication(cluster, client_id, client_secret, authority_id)        async with KustoClient(kcsb) as client            db  Samples            query  StormEvents  take 10            response  await client.execute(db, query)            for row in response.primary_results[0]                print(row[0],  , row[EventType])Links How to install the package httpsgithub.comAzureazure-kusto-pythoninstall_. Kusto query sample httpsgithub.comAzureazure-kusto-pythonblobmasterazure-kusto-datatestssample.py_. GitHub Repository httpsgithub.comAzureazure-kusto-pythontreemasterazure-kusto-data_.</longdescription>
</pkgmetadata>