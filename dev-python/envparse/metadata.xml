<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>envparseenvparse is a simple utility to parse environment variables.If you use Heroku andor subscribe to the tenets of the12 Factor App httpwww.12factor.net_youll be using a lot of environment variable-based configuration in your app.os.environ is a great choice to start off with but over time youll findyourself duplicating quite a bit of code around handling raw environmentvariables.envparse aims to eliminate this duplicated, often inconsistent parsingcode and instead provide a single, easy-to-use wrapper.Ideas, and code portions, have been taken from django-environhttpsgithub.comjoke2kdjango-environ_ project but made frameworkagnostic.Installing----------Through PyPI     pip install envparseManually     pip install git+httpsgithub.comrconradharrisenvparse.git    OR     git clone httpsgithub.comrconradharrisenvparse  cd envparse     python setup.py installUsage-----In your settings or configuration module, first either import the standardparser or one with a schema     Standard    from envparse import env     Schema    from envparse import Env    env  Env(BOOLEAN_VARbool, LIST_VARdict(typelist, subtypeint))env can then be called in two ways Type explicit env(ENV_VAR_NAME, typeTYPE, ...) Type implicit (for Python builtin types only) env.TYPE(ENV_VAR_NAME, ...)If type is not specified, explicitly or implicitly, then the defaulttype is str.Casting to a specified type     Environment variable MAIL_ENABLED1    mail_enabled  env(MAIL_ENABLED, typebool)     OR mail_enabled  env.bool(MAIL_ENABLED)    assert mail_enabled is TrueCasting nested types     Environment variable FOO1,2,3    foo  env(FOO), subtypeint)     OR foo  env(FOO, typelist, subtypeint)     Note that there is no way to implicitly call subtypes.    assert foo  [1, 2, 3]Specifying defaults     Environment variable MAX_ROWS has not been defined    max_rows  env.int(MAX_ROWS, default100)    assert max_rows  100Proxying values, useful in Heroku for wiring up the environment variables theyprovide to the ones that your app actually uses     Environment variables MAILGUN_SMTP_LOGINfoo,     SMTP_LOGIN{{MAILGUN_SMTP_LOGIN}}    smtp_login  env(SMTP_LOGIN)    assert smtp_login  fooNow if you switch to using Mandrill as an email provider, instead of having tomodify your app, you can simply make a configuration change    SMTP_LOGIN{{MANDRILL_UESRNAME}}There are also a few convenience methods env.json parses JSON and returns a dict. env.url parses a url and returns a urlparse.ParseResult object.Type specific notes list the expected environment variable format is FOO1,2,3 and may  contain spaces between the commas as well as preceding or trailing whitespace. dict the expected environment variable format is FOOkey1val1,  key2val2. Spaces are also allowed. json a regular JSON string such as FOO{foo bar} is expected.SchemasDefine a schema so you can only need to provide the type, subtype, and defaultsonce     Environment variables MAIL_ENABLED0, LIST_INT1,2,3     Bind schema to Env object to get schema-based lookups    env  Env(MAIL_ENABLEDbool, SMTP_LOGINdict(typestr, defaultfoo),              LIST_INTdict(typelist, subtypeint))    assert env(MAIL_ENABLED) is False    assert env(SMTP_LOGIN)  foo  Not defined so uses default    assert env(LIST_INT)  [1, 2, 3]The Env constructor takes values in the form of either VAR_NAMEtypeor VAR_NAMEdict where dict is a dictionary with either one or more ofthe following keys specified type, subtype, default.Pre- and PostprocessorsPreprocessors are callables that are run on the environment variable stringbefore any type casting takes place     Environment variables FOObar     Preprocessor to change variable to uppercase    to_upper  lambda v v.upper()    foo  env(FOO, preprocessorto_upper)    assert foo  BARPostprocessors are callables that are run after the type casting takes place.An example of one might be returning a datastructure expected by a framework     Environment variable REDIS_URLredisredispass127.0.0.163790    def django_redis(url)      return {BACKEND django_redis.cache.RedisCache,          LOCATION {}{}{}.format(url.hostname, url.port, url.path.strip()),          OPTIONS {PASSWORD url.password}}    redis_config  env(REDIS_URL, postprocessordjango_redis)    assert redis_config  {BACKEND django_redis.cache.RedisCache,        LOCATION 127.0.0.163790, OPTIONS {PASSWORD redispass}}Environment FileRead from a .env file (line delimited KEYVALUE)     This recurses up the directory tree until a file called .env is found.    env.read_env()     Manually specifying a path    env.read_env(config.myenv)     Values can be read as normal    env.int(FOO)Tests-----.. image httpssecure.travis-ci.orgrconradharrisenvparse.pngbranchmasterTo run the tests install tox    pip install toxThen run them with    make test</longdescription>
</pkgmetadata>