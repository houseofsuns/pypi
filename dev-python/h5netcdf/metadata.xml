<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>h5netcdf.. image httpsgithub.comh5netcdfh5netcdfworkflowsCIbadge.svg    target httpsgithub.comh5netcdfh5netcdfactions.. image httpsbadge.fury.iopyh5netcdf.svg    target httpspypi.orgprojecth5netcdf.. image httpsgithub.comh5netcdfh5netcdfactionsworkflowspagespages-build-deploymentbadge.svgbranchgh-pages    target httpsh5netcdf.github.ioh5netcdfA Python interface for the netCDF4_ file-format that reads and writes local orremote HDF5 files directly via h5py_ or h5pyd_, without relying on the UnidatanetCDF library... _netCDF4 httpsdocs.unidata.ucar.edunetcdf-ccurrentfile_format_specifications.htmlnetcdf_4_spec.. _h5py httpswww.h5py.org.. _h5pyd httpsgithub.comHDFGrouph5pyd.. why-h5netcdfWhy h5netcdf-------------- It has one less binary dependency (netCDF C). If you already have h5py  installed, reading netCDF4 with h5netcdf may be much easier than installing  netCDF4-Python.- Weve seen occasional reports of better performance with h5py than  netCDF4-python, though in many cases performance is identical. For  one workflow_, h5netcdf was reported to be almost 4x faster than  netCDF4-python_.- Anecdotally, HDF5 users seem to be unexcited about switching to netCDF --  hopefully this will convince them that netCDF4 is actually quite sane- Finally, side-stepping the netCDF C library (and Cython bindings to it)  gives us an easier way to identify the source of performance issues and  bugs in the netCDF librariesspecification... _one workflow httpsgithub.comUnidatanetcdf4-pythonissues390issuecomment-93864839.. _xarray httpsgithub.compydataxarrayInstall-------Ensure you have a recent version of h5py installed (I recommend using conda_ orthe community effort conda-forge_).At least version 2.1 is required (for dimension scales) versions 2.3 and newerhave been verified to work, though some tests only pass on h5py 2.6. Then     pip install h5netcdfOr if you are already using conda     conda install h5netcdf.. _conda httpsconda.io.. _conda-forge httpsconda-forge.orgUsage-----h5netcdf has two APIs, a new API and a legacy API. Both interfaces currentlyreproduce most of the features of the netCDF interface, with the notableexception of support for operations that rename or delete existing objects.We simply havent gotten around to implementing this yet. Patcheswould be very welcome.New APIThe new API supports direct hierarchical access of variables and groups. Itsdesign is an adaptation of h5py to the netCDF data model. For example.. code-block python    import h5netcdf    import numpy as np    with h5netcdf.File(mydata.nc, w) as f         set dimensions with a dictionary        f.dimensions  {x 5}         and update them with a dict-like interface         f.dimensions[x]  5         f.dimensions.update({x 5})        v  f.create_variable(hello, (x,), float)        v[]  np.ones(5)         you dont need to create groups first         you also dont need to create dimensions first if you supply data         with the new variable        v  f.create_variable(groupeddata, (y,), datanp.arange(10))         access and modify attributes with a dict-like interface        v.attrs[foo]  bar         you can access variables and groups directly using a hierarchical         keys like h5py        print(f[groupeddata])         add an unlimited dimension        f.dimensions[z]  None         explicitly resize a dimension and all variables using it        f.resize_dimension(z, 3)Notes- Automatic resizing of unlimited dimensions with array indexing is not available.- Dimensions need to be manually resized with Group.resize_dimension(dimension, size).- Arrays are returned padded with fillvalue (taken from underlying hdf5 dataset) up to  current size of variables dimensions. The behaviour is equivalent to netCDF4-pythons  Dataset.set_auto_mask(False).Legacy APIThe legacy API is designed for compatibility with netCDF4-python_. To use it, importh5netcdf.legacyapi.. _netCDF4-python httpsgithub.comUnidatanetcdf4-python.. code-block python    import h5netcdf.legacyapi as netCDF4     everything here would also work with this instead     import netCDF4    import numpy as np    with netCDF4.Dataset(mydata.nc, w) as ds        ds.createDimension(x, 5)        v  ds.createVariable(hello, float, (x,))        v[]  np.ones(5)        g  ds.createGroup(grouped)        g.createDimension(y, 10)        g.createVariable(data, i8, (y,))        v  g[data]        v[]  np.arange(10)        v.foo  bar        print(ds.groups[grouped].variables[data])The legacy API is designed to be easy to try-out for netCDF4-python users, but it is not anexact match. Here is an incomplete list of functionality we dont include- Utility functions chartostring, num2date, etc., that are not directly necessary  for writing netCDF files.- h5netcdf variables do not support automatic masking or scaling (e.g., of values matching  the _FillValue attribute). We prefer to leave this functionality to client libraries  (e.g., xarray_), which can implement their exact desired scaling behavior. Nevertheless  arrays are returned padded with fillvalue (taken from underlying hdf5 dataset) up to  current size of variables dimensions. The behaviour is equivalent to netCDF4-pythons  Dataset.set_auto_mask(False)... _invalid netcdfInvalid netCDF filesh5py implements some features that do not (yet) result in valid netCDF files- Data types    - Booleans    - Complex values    - Non-string variable length types    - Enum types    - Reference types- Arbitrary filters    - Scale-offset filtersBy default []_, h5netcdf will not allow writing files using any of these features,as files with such features are not readable by other netCDF tools.However, these are still valid HDF5 files. If you dont care about netCDFcompatibility, you can use these features by setting invalid_netcdfTruewhen creating a file.. code-block python   avoid the .nc extension for non-netcdf files  f  h5netcdf.File(mydata.h5, invalid_netcdfTrue)  ...   works with the legacy API, too, though compression options are not exposed  ds  h5netcdf.legacyapi.Dataset(mydata.h5, invalid_netcdfTrue)  ...In such cases the _NCProperties attribute will not be saved to the file or be removedfrom an existing file. A warning will be issued if the file has .nc-extension... rubric Footnotes.. [] h5netcdf we will raise h5netcdf.CompatibilityError.Decoding variable length stringsh5py 3.0 introduced new behavior_ for handling variable length string.Instead of being automatically decoded with UTF-8 into NumPy arrays of str,they are required as arrays of bytes.The legacy API preserves the old behavior of h5py (which matches netCDF4),and automatically decodes strings.The new API matches h5py behavior. Explicitly set decode_vlen_stringsTruein the h5netcdf.File constructor to opt-in to automatic decoding... _new behavior httpsdocs.h5py.orgenstablestrings.html.. _phony dimsDatasets with missing dimension scalesBy default []_ h5netcdf raises a ValueError if variables with no dimensionscale associated with one of their axes are accessed.You can set phony_dimssort when opening a file to let h5netcdf inventphony dimensions according to netCDF_ behaviour... code-block python   mimic netCDF-behaviour for non-netcdf files  f  h5netcdf.File(mydata.h5, moder, phony_dimssort)  ...Note, that this iterates once over the whole group-hierarchy. This has affectson performance in case you rely on laziness of group access.You can set phony_dimsaccess instead to defer phony dimension creationto group access time. The created phony dimension naming will differ fromnetCDF_ behaviour... code-block python  f  h5netcdf.File(mydata.h5, moder, phony_dimsaccess)  ..... rubric Footnotes.. [] Keyword default setting phony_dimsNone for backwards compatibility... _netCDF httpsdocs.unidata.ucar.edunetcdf-ccurrentinteroperability_hdf5.htmlTrack OrderAs of h5netcdf 1.1.0, if h5py 3.7.0 or greater is detected, the track_orderparameter is set to True enabling order tracking_ for newly creatednetCDF4 files. This helps ensure that files created with the h5netcdf librarycan be modified by the netCDF4-c and netCDF4-python implementation used inother software stacks. Since this change should be transparent to most users,it was made without deprecation.Since track_order is set at creation time, any dataset that was created withtrack_orderFalse (h5netcdf version 1.0.2 and older except for 0.13.0) willcontinue to opened with order tracker disabled.The following describes the behavior of h5netcdf with respect to order trackingfor a few key versions- Version 0.12.0 and earlier, the track_order parameterorder was missing  and thus order tracking was implicitely set to False.- Version 0.13.0 enabled order tracking by setting the parameter  track_order to True by default without deprecation.- Versions 0.13.1 to 1.0.2 set track_order to False due to a bug in a  core dependency of h5netcdf, h5py upstream bug_ which was resolved in h5py  3.7.0 with the help of the h5netcdf team.- In version 1.1.0, if h5py 3.7.0 or above is detected, the track_order  parameter is set to True by default... _order tracking httpsdocs.unidata.ucar.edunetcdf-ccurrentfile_format_specifications.htmlcreation_order.. _upstream bug httpsgithub.comh5netcdfh5netcdfissues136.. _[] httpsgithub.comh5netcdfh5netcdfissues128.. changelogChangelog---------Changelog_.. _Changelog httpsgithub.comh5netcdfh5netcdfblobmainCHANGELOG.rst.. licenseLicense-------3-clause BSD_.. _3-clause BSD httpsgithub.comh5netcdfh5netcdfblobmainLICENSE</longdescription>
</pkgmetadata>