<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>HTTP Router.. _descriptionhttp-router -- A simple router for HTTP applications.. _badges.. image httpsgithub.comklenhttp-routerworkflowstestsbadge.svg    target httpsgithub.comklenhttp-routeractions    alt Tests Status.. image httpsimg.shields.iopypivhttp-router    target httpspypi.orgprojecthttp-router    alt PYPI Version.. image httpsimg.shields.iopypipyversionshttp-router    target httpspypi.orgprojecthttp-router    alt Python Versions.. _contents.. contents.. _requirementsRequirements- python  3.7.. _installationInstallationhttp-router should be installed using pip     pip install http-routerUsageCreate a router.. code python        from http_router import Router     Initialize the router    router  Router(trim_last_slashTrue)Define routes.. code python    router.route(simple)    def simple()        return result from the fnCall the router with HTTP path and optionally method to get a match result... code python   match  router(simple, methodGET)   assert match, HTTP path is ok   assert match.target is simpleThe router supports regex objects too.. code python    import re    router.route(re.compile(rregexpw{3}-d{2}))    def regex()        return result from the fnBut the lib has a simplier interface for the dynamic routes.. code python    router.route(users{username})    def users()        return result from the fnBy default this will capture characters up to the end of the path or the next.Optionally, you can use a converter to specify the type of the argument like{variable_nameconverter}.Converter types str   (default) accepts any text without a slashint   accepts positive integersfloat accepts positive floating point valuespath  like string but also accepts slashesuuid  accepts UUID strings Convertors are used by prefixing them with a colon, like so.. code python    router.route(orders{order_idint})    def orders()        return result from the fnAny unknown convertor will be parsed as a regex.. code python    router.route(orders{order_idd{3}})    def orders()        return result from the fnMultiple paths are supported as well.. code python    router.route(, home)    def index()        return indexHandling HTTP methods.. code python    router.route(only-post, methods[POST])    def only_post()        return only-postSubmounting routes.. code python   subrouter  Router()   subrouter(items)   def items()        pass    router  Router()    router.route(api)(subrouter)   match  router(apiitems, methodGET)   assert match, HTTP path is ok   assert match.target is items.. _bugtrackerBug trackerIf you have any suggestions, bug reports orannoyances please report them to the issue trackerat httpsgithub.comklenhttp-routerissues.. _contributingContributingDevelopment of the project happens at httpsgithub.comklenhttp-router.. _licenseLicenseLicensed under a MIT license_... _links.. _klen httpsgithub.comklen.. _MIT license httpopensource.orglicensesMIT</longdescription>
</pkgmetadata>