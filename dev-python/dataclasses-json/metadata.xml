<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Dataclasses JSON[](httpsgithub.comlidatongdataclasses-jsonworkflowsdataclasses-jsonbadge.svg)This library provides a simple API for encoding and decoding [dataclasses](httpsdocs.python.org3librarydataclasses.html) to and from JSON.Its very easy to get started.[README  Documentation website](httpslidatong.github.iodataclasses-json). Features a navigation bar and search functionality, and should mirror this README exactly -- take a look Quickstartpip install dataclasses-jsonpythonfrom dataclasses import dataclassfrom dataclasses_json import dataclass_jsondataclass_jsondataclassclass Person    name strperson  Person(namelidatong)person.to_json()   {name lidatong} - this is a stringperson.to_dict()   {name lidatong} - this is a dictPerson.from_json({name lidatong})   Person(1)Person.from_dict({name lidatong})   Person(1) You can also apply _schema validation_ using an alternative API This can be useful for typed Python codePerson.from_json({name 42})   This is ok. 42 is not a str, but                                   dataclass creation does not validate typesPerson.schema().loads({name 42})   Error Raises ValidationErrorWhat if you want to work with camelCase JSONpython same imports as above, with the additional LetterCase importfrom dataclasses import dataclassfrom dataclasses_json import dataclass_json, LetterCasedataclass_json(letter_caseLetterCase.CAMEL)   now all fields are encodeddecoded from camelCasedataclassclass ConfiguredSimpleExample    int_field intConfiguredSimpleExample(1).to_json()   {intField 1}ConfiguredSimpleExample.from_json({intField 1})   ConfiguredSimpleExample(1) Supported typesIts recursive (see caveats below), so you can easily work with nested dataclasses.In addition to the supported types in the [py to JSON table](httpsdocs.python.org3libraryjson.htmlpy-to-json-table), this library supports the following- any arbitrary [Collection](httpsdocs.python.org3librarycollections.abc.htmlcollections.abc.Collection) type is supported.[Mapping](httpsdocs.python.org3librarycollections.abc.htmlcollections.abc.Mapping) types are encoded as JSON objects and str types as JSON strings. Any other Collection types are encoded into JSON arrays, but decoded into the original collection types.- [datetime](httpsdocs.python.org3librarydatetime.htmlavailable-types) objects. datetime objects are encoded to float (JSON number) using [timestamp](httpsdocs.python.org3librarydatetime.htmldatetime.datetime.timestamp).As specified in the datetime docs, if your datetime object is naive, it will assume your system local timezone when calling .timestamp(). JSON numbers corresponding to a datetime field in your dataclass are decoded into a datetime-aware object, with tzinfo set to your system local timezone.Thus, if you encode a datetime-naive object, you will decode into a datetime-aware object. This is important, because encoding and decoding wont strictly be inverses. See [this section](Overriding) if you want to override this defaultbehavior (for example, if you want to use ISO).- [UUID](httpsdocs.python.org3libraryuuid.htmluuid.UUID) objects. They are encoded as str (JSON string).- [Decimal](httpsdocs.python.org3librarydecimal.html) objects. They arealso encoded as str.The [latest release](httpsgithub.comlidatongdataclasses-jsonreleaseslatest) is compatible with both Python 3.7 and Python 3.6 (with the dataclasses backport). Usage Approach 1 Class decoratorpythonfrom dataclasses import dataclassfrom dataclasses_json import dataclass_jsondataclass_jsondataclassclass Person    name strlidatong  Person(lidatong) Encoding to JSONlidatong.to_json()   {name lidatong} Decoding from JSONPerson.from_json({name lidatong})   Person(namelidatong)Note that the dataclass_json decorator must be stacked above the dataclassdecorator (order matters) Approach 2 Inherit from a mixinpythonfrom dataclasses import dataclassfrom dataclasses_json import DataClassJsonMixindataclassclass Person(DataClassJsonMixin)    name strlidatong  Person(lidatong) A different example from Approach 1 above, but usage is the exact sameassert Person.from_json(lidatong.to_json())  lidatongPick whichever approach suits your taste. Note that there is better support for the mixin approach when using _static analysis_ tools (e.g. linting, typing), but the differences in implementation will be invisible in _runtime_ usage. How do I... Use my dataclass with JSON arrays or objectspythonfrom dataclasses import dataclassfrom dataclasses_json import dataclass_jsondataclass_jsondataclassclass Person    name strEncode into a JSON array containing instances of my Data Classpythonpeople_json  [Person(lidatong)]Person.schema().dumps(people_json, manyTrue)   [{name lidatong}]Decode a JSON array containing instances of my Data Classpythonpeople_json  [{name lidatong}]Person.schema().loads(people_json, manyTrue)   [Person(namelidatong)]Encode as part of a larger JSON object containing my Data Class (e.g. an HTTP requestresponse)pythonimport jsonresponse_dict  {    response {        person Person(lidatong).to_dict()    }}response_json  json.dumps(response_dict)In this case, we do two steps. First, we encode the dataclass into a python dictionary rather than a JSON string, using .to_dict. Second, we leverage the built-in json.dumps to serialize our dataclass into a JSON string.Decode as part of a larger JSON object containing my Data Class (e.g. an HTTP response)pythonimport jsonresponse_dict  json.loads({response {person {name lidatong}}})person_dict  response_dict[response]person  Person.from_dict(person_dict)In a similar vein to encoding above, we leverage the built-in json module.First, call json.loads to read the entire JSON object into a dictionary. We then access the key of the value containing the encoded dict of our Person that we want to decode (response_dict[response]).Second, we load in the dictionary using Person.from_dict. Encode or decode into Python listsdictionaries rather than JSONThis can be by calling .schema() and then using the corresponding encoderdecoder methods, ie. .load(...).dump(...).Encode into a single Python dictionarypythonperson  Person(lidatong)person.to_dict()   {name lidatong}Encode into a list of Python dictionariespythonpeople  [Person(lidatong)]Person.schema().dump(people, manyTrue)   [{name lidatong}]Decode a dictionary into a single dataclass instancepythonperson_dict  {name lidatong}Person.from_dict(person_dict)   Person(namelidatong)Decode a list of dictionaries into a list of dataclass instancespythonpeople_dicts  [{name lidatong}]Person.schema().load(people_dicts, manyTrue)   [Person(namelidatong)] Encode or decode from camelCase (or kebab-case)JSON letter case by convention is camelCase, in Python members are by convention snake_case.You can configure it to encodedecode from other casing schemes at both the class level and the field level.pythonfrom dataclasses import dataclass, fieldfrom dataclasses_json import LetterCase, config, dataclass_json changing casing at the class leveldataclass_json(letter_caseLetterCase.CAMEL)dataclassclass Person    given_name str    family_name strPerson(Alice, Liddell).to_json()   {givenName Alice}Person.from_json({givenName Alice, familyName Liddell})   Person(Alice, Liddell) at the field leveldataclass_jsondataclassclass Person    given_name str  field(metadataconfig(letter_caseLetterCase.CAMEL))    family_name strPerson(Alice, Liddell).to_json()   {givenName Alice} notice how the family_name field is still snake_case, because it wasnt configured abovePerson.from_json({givenName Alice, family_name Liddell})   Person(Alice, Liddell)This library assumes your field follows the Python convention of snake_case naming.If your field is not snake_case to begin with and you attempt to parameterize LetterCase, the behavior of encodingdecoding is undefined (most likely it will result in subtle bugs). Encode or decode using a different namepythonfrom dataclasses import dataclass, fieldfrom dataclasses_json import config, dataclass_jsondataclass_jsondataclassclass Person    given_name str  field(metadataconfig(field_nameoverriddenGivenName))Person(given_nameAlice)   Person(Alice)Person.from_json({overriddenGivenName Alice})   Person(Alice)Person(Alice).to_json()   {overriddenGivenName Alice} Handle missing or optional field values when decodingBy default, any fields in your dataclass that use default or default_factory will have the values filled with the provided default, if thecorresponding field is missing from the JSON youre decoding.Decode JSON with missing fieldpythondataclass_jsondataclassclass Student    id int    name str  studentStudent.from_json({id 1})   Student(id1, namestudent)Notice from_json filled the field name with the specified default studentwhen it was missing from the JSON.Sometimes you have fields that are typed as Optional, but you dont necessarily want to assign a default. In that case, you can use the infer_missing kwarg to make from_json infer the missing field value as None.Decode optional field without defaultpythondataclass_jsondataclassclass Tutor    id int    student Optional[Student]  NoneTutor.from_json({id 1})   Tutor(id1, studentNone)Personally I recommend you leverage dataclass defaults rather than using infer_missing, but if for some reason you need to decouple the behavior of JSON decoding from the fields default value, this will allow you to do so. Handle unknown  extraneous fields in JSONBy default, it is up to the implementation what happens when a json_dataclass receives input parameters that are not defined.(the from_dict method ignores them, when loading using schema() a ValidationError is raised.)There are three ways to customize this behavior.Assume you want to instantiate a dataclass with the following dictionarypythondump_dict  {endpoint some_api_endpoint, data {foo 1, bar 2}, undefined_field_name [1, 2, 3]}1. You can enforce to always raise an error by setting the undefined keyword to Undefined.RAISE (RAISE as a case-insensitive string works as well). Of course it works normally if you dont pass any undefined parameters.pythonfrom dataclasses_json import Undefineddataclass_json(undefinedUndefined.RAISE)dataclass()class ExactAPIDump    endpoint str    data Dict[str, Any]dump  ExactAPIDump.from_dict(dump_dict)   raises UndefinedParameterError2. You can simply ignore any undefined parameters by setting the undefined keyword to Undefined.EXCLUDE (EXCLUDE as a case-insensitive string works as well). Note that you will not be able to retrieve them using to_dictpythonfrom dataclasses_json import Undefineddataclass_json(undefinedUndefined.EXCLUDE)dataclass()class DontCareAPIDump    endpoint str    data Dict[str, Any]dump  DontCareAPIDump.from_dict(dump_dict)   DontCareAPIDump(endpointsome_api_endpoint, data{foo 1, bar 2})dump.to_dict()   {endpoint some_api_endpoint, data {foo 1, bar 2}}3. You can save them in a catch-all field and do whatever needs to be done later. Simply set the undefinedkeyword to Undefined.INCLUDE (INCLUDE as a case-insensitive string works as well) and define a fieldof type CatchAll where all unknown values will end up. This simply represents a dictionary that can hold anything.  If there are no undefined parameters, this will be an empty dictionary.pythonfrom dataclasses_json import Undefined, CatchAlldataclass_json(undefinedUndefined.INCLUDE)dataclass()class UnknownAPIDump    endpoint str    data Dict[str, Any]    unknown_things CatchAlldump  UnknownAPIDump.from_dict(dump_dict)   UnknownAPIDump(endpointsome_api_endpoint, data{foo 1, bar 2}, unknown_things{undefined_field_name [1, 2, 3]})dump.to_dict()   {endpoint some_api_endpoint, data {foo 1, bar 2}, undefined_field_name [1, 2, 3]}Notes- When using Undefined.INCLUDE, an UndefinedParameterError will be raised if you dont specifyexactly one field of type CatchAll.- Note that LetterCase does not affect values written into the CatchAll field, they will be as they are given.- When specifying a default (or a default factory) for the the CatchAll-field, e.g. unknown_things CatchAll  None, the default value will be used instead of an empty dict if there are no undefined parameters.- Calling __init__ with non-keyword arguments resolves the arguments to the defined fields and writes everything else into the catch-all field.4. All 3 options work as well using schema().loads and schema().dumps, as long as you dont overwrite it by specifying schema(unknowna marshmallow value).marshmallow uses the same 3 keywords [include, exclude, raise](httpsmarshmallow.readthedocs.ioenstablequickstart.htmlhandling-unknown-fields).5. All 3 operations work as well using __init__, e.g. UnknownAPIDump(dump_dict) will not raise a TypeError, but write all unknown values to the field tagged as CatchAll.   Classes tagged with EXCLUDE will also simply ignore unknown parameters. Note that classes tagged as RAISE still raise a TypeError, and not a UndefinedParameterError if supplied with unknown keywords. Override the default encode  decode  marshmallow field of a specific fieldSee [Overriding](Overriding) Handle recursive dataclassesObject hierarchies where fields are of the type that they are declared within require a smalltype hinting trick to declare the forward reference.pythonfrom typing import Optionalfrom dataclasses import dataclassfrom dataclasses_json import dataclass_jsondataclass_jsondataclassclass Tree()    value str    left Optional[Tree]    right Optional[Tree]Avoid usingpythonfrom __future__ import annotationsas it will cause problems with the way dataclasses_json accesses the type annotations. Marshmallow interopUsing the dataclass_json decorator or mixing in DataClassJsonMixin willprovide you with an additional method .schema()..schema() generates a schema exactly equivalent to manually creating amarshmallow schema for your dataclass. You can reference the [marshmallow API docs](httpsmarshmallow.readthedocs.ioen3.0api_reference.htmlschema)to learn other ways you can use the schema returned by .schema().You can pass in the exact same arguments to .schema() that you would whenconstructing a PersonSchema instance, e.g. .schema(manyTrue), and they willget passed through to the marshmallow schema.pythonfrom dataclasses import dataclassfrom dataclasses_json import dataclass_jsondataclass_jsondataclassclass Person    name str You dont need to do this - its generated for you by .schema()from marshmallow import Schema, fieldsclass PersonSchema(Schema)    name  fields.Str()Briefly, on whats going on under the hood in the above examples calling .schema() will have this library generate a[marshmallow schema](httpsmarshmallow.readthedocs.ioen3.0api_reference.htmlschema)for you. It also fills in the corresponding object hook, so that marshmallowwill create an instance of your Data Class on load (e.g.Person.schema().load returns a Person) rather than a dict, which it doesby default in marshmallow.Performance note.schema() is not cached (it generates the schema on every call), so if youhave a nested Data Class you may want to save the result to a variable to avoid re-generation of the schema on every usage.pythonperson_schema  Person.schema()person_schema.dump(people, manyTrue) later in the code...person_schema.dump(person) Overriding  Extending OverridingFor example, you might want to encodedecode datetime objects using ISO formatrather than the default timestamp.pythonfrom dataclasses import dataclass, fieldfrom dataclasses_json import dataclass_json, configfrom datetime import datetimefrom marshmallow import fieldsdataclass_jsondataclassclass DataClassWithIsoDatetime    created_at datetime  field(        metadataconfig(            encoderdatetime.isoformat,            decoderdatetime.fromisoformat,            mm_fieldfields.DateTime(formatiso)        )    ) ExtendingSimilarly, you might want to extend dataclasses_json to encode date objects.pythonfrom dataclasses import dataclass, fieldfrom dataclasses_json import dataclass_json, configfrom datetime import datefrom marshmallow import fieldsdataclass_jsondataclassclass DataClassWithIsoDatetime    created_at date  field(        metadataconfig(            encoder date.isoformat,            decoder date.fromisoformat,            mm_field fields.DateTime(formatiso)        ))As you can see, you can override or extend the default codecs by providing a hook via a callable- encoder a callable, which will be invoked to convert the field value when encoding to JSON- decoder a callable, which will be invoked to convert the JSON value when decoding from JSON- mm_field a marshmallow field, which will affect the behavior of any operations involving .schema()Note that these hooks will be invoked regardless if youre using .to_jsondumpdumpsand .from_jsonloadloads. So apply overrides  extensions judiciously, making sure to carefully consider whether the interaction of the encodedecodemm_field is consistent with what you expect What if I have other dataclass field extensions that rely on metadataAll the dataclasses_json.config does is return a mapping, namespaced under the key dataclasses_json.Say theres another module, other_dataclass_package that uses metadata. Heres how you solve your problempythonmetadata  {other_dataclass_package some metadata...}   pre-existing metadata for another dataclass packagedataclass_json_config  config(            encoderdatetime.isoformat,            decoderdatetime.fromisoformat,            mm_fieldfields.DateTime(formatiso)        )metadata.update(dataclass_json_config)dataclass_jsondataclassclass DataClassWithIsoDatetime    created_at datetime  field(metadatametadata)You can also manually specify the dataclass_json configuration mapping.pythondataclass_jsondataclassclass DataClassWithIsoDatetime    created_at date  field(        metadata{dataclasses_json {            encoder date.isoformat,            decoder date.fromisoformat,            mm_field fields.DateTime(formatiso)        }}    ) A larger examplepythonfrom dataclasses import dataclassfrom dataclasses_json import dataclass_jsonfrom typing import Listdataclass_jsondataclass(frozenTrue)class Minion    name strdataclass_jsondataclass(frozenTrue)class Boss    minions List[Minion]boss  Boss([Minion(evil minion), Minion(very evil minion)])boss_json  {    minions [        {            name evil minion        },        {            name very evil minion        }    ]}.strip()assert boss.to_json(indent4)  boss_jsonassert Boss.from_json(boss_json)  boss PerformanceTake a look at [this issue](httpsgithub.comlidatongdataclasses-jsonissues228) VersioningNote this library is still pre-1.0.0 (SEMVER).The current convention is- PATCH version upgrades for bug fixes and minor feature additions.- MINOR version upgrades for big API features and breaking changes.Once this library is 1.0.0, it will follow standard SEMVER conventions. RoadmapCurrently the focus is on investigating and fixing bugs in this library, workingon performance, and finishing [this issue](httpsgithub.comlidatongdataclasses-jsonissues31).That said, if you think theres a feature missing  something new needed in thelibrary, please see the contributing section below. ContributingFirst of all, thank you for being interested in contributing to this library.I really appreciate you taking the time to work on this project.- If youre just interested in getting into the code, a good place to start are issues tagged as bugs.- If introducing a new feature, especially one that modifies the public API, consider submitting an issue for discussion before a PR. Please also take a look at existing issues  PRs to see what youre proposing has  already been covered before  exists.- I like to follow the commit conventions documented [here](httpswww.conventionalcommits.orgenv1.0.0summary)</longdescription>
</pkgmetadata>