<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>ECS Deploy----------.. image httpsbadge.fury.iopyecs-deploy.svg    target httpsbadge.fury.iopyecs-deploy.. image httpsgithub.comfabfuelecs-deployactionsworkflowsbuild.ymlbadge.svg    target httpsgithub.comfabfuelecs-deployactionsworkflowsbuild.ymlecs-deploy simplifies deployments on Amazon ECS by providing a convenience CLI tool for complex actions, which are executed pretty often.Key Features------------- support for complex task definitions (e.g. multiple containers  task role)- easily redeploy the current task definition (including docker pull of eventually updated images)- deploy new versionstags or all containers or just a single container in your task definition- scale up or down by adjusting the desired count of running tasks- add or adjust containers environment variables- run one-off tasks from the CLI- automatically monitor deployments in New RelicTLDR-----Deploy a new version of your service     ecs deploy my-cluster my-service --tag 1.2.3Redeploy the current version of a service     ecs deploy my-cluster my-serviceScale up or down a service     ecs scale my-cluster my-service 4Updating a cron job     ecs cron my-cluster my-task my-ruleUpdate a task definition (without running or deploying)     ecs update my-taskInstallation------------The project is available on PyPI. Simply run     pip install ecs-deployRun via Docker--------------Instead of installing ecs-deploy locally, which requires a Python environment, you can run ecs-deploy via Docker. All versions starting from 1.7.1 are available on Docker Hub httpscloud.docker.comrepositorydockerfabfuelecs-deployRunning ecs-deploy via Docker is easy as    docker run fabfuelecs-deploy1.10.2    In this example, the stable version 1.10.2 is executed. Alternatively you can use Docker tags master or latest for the latest stable version or Docker tag develop for the newest development version of ecs-deploy.Please be aware, that when running ecs-deploy via Docker, the configuration - as described below - does not apply. You have to provide credentials and the AWS region via the command as attributes or environment variables    docker run fabfuelecs-deploy1.10.2 ecs deploy my-cluster my-service --region eu-central-1 --access-key-id ABC --secret-access-key ABCConfiguration-------------As ecs-deploy is based on boto3 (the official AWS Python library), there are several ways to configure and store theauthentication credentials. Please read the boto3 documentation for more details(httpboto3.readthedocs.orgenlatestguideconfiguration.htmlconfiguration). The simplest way is by running     aws configureAlternatively you can pass the AWS credentials (via --access-key-id and --secret-access-key) or the AWSconfiguration profile (via --profile) as options when you run ecs.AWS IAM-------If you are using ecs-deploy with a role or user account that does not have full AWS access, such as in a deploy script, you willneed to use or create an IAM policy with the correct set of permissions in order for your deploys to succeed. One option is to use the pre-specified AmazonECS_FullAccess (httpsdocs.aws.amazon.comAmazonECSlatestuserguidesecurity-iam-awsmanpol.htmlsecurity-iam-awsmanpol-AmazonECS_FullAccess) policy. If you would prefer to create a role with a more minimal set of permissions,the following are required ecsListServices ecsUpdateService ecsListTasks ecsRegisterTaskDefinition ecsDescribeServices ecsDescribeTasks ecsListTaskDefinitions ecsDescribeTaskDefinition ecsDeregisterTaskDefinition If using custom IAM permissions, you will also need to set the iamPassRole policy for each IAM role. See here httpsdocs.aws.amazon.comIAMlatestUserGuideid_roles_use_passrole.html for more information.Note that not every permission is required for every action you can take in ecs-deploy. You may be able to adjust permissions based on your specific needs.Actions-------Currently the following actions are supporteddeployRedeploy a service either without any modifications or with a new image, environment variable, docker label, andor command definition.scaleScale a service up or down and change the number of running tasks.runRun a one-off task based on an existing task-definition and optionally override command, environment variables andor docker labels.updateUpdate a task definition by creating a new revision to set a new image,environment variable, docker label, andor command definition, etc.cron (scheduled task)Update a task definition and update a events rule (scheduled task) to use thenew task definition.Usage-----For detailed information about the available actions, arguments and options, run     ecs deploy --help     ecs scale --help     ecs run --helpExamples--------All examples assume, that authentication has already been configured.Deployment----------Simple RedeployTo redeploy a service without any modifications, but pulling the most recent image versions, run the following command.This will duplicate the current task definition and cause the service to redeploy all running tasks.     ecs deploy my-cluster my-serviceDeploy a new tagTo change the tag for all images in all containers in the task definition, run the following command     ecs deploy my-cluster my-service -t 1.2.3Deploy a new imageTo change the image of a specific container, run the following command     ecs deploy my-cluster my-service --image webserver nginx1.11.8This will modify the webserver container only and change its image to nginx1.11.8.Deploy several new imagesThe -i or --image option can also be passed several times     ecs deploy my-cluster my-service -i webserver nginx1.9 -i application my-app1.2.3This will change the webservers container image to nginx1.9 and the applications image to my-app1.2.3.Deploy a custom task definitionTo deploy any task definition (independent of which is currently used in the service), you can use the --task parameter. The value can beA fully qualified task ARN     ecs deploy my-cluster my-service --task arnawsecseu-central-1123456789012task-definitionmy-task20A task family name with revision     ecs deploy my-cluster my-service --task my-task20Or just a task family name. It this case, the most recent revision is used     ecs deploy my-cluster my-service --task my-task.. important   ecs will still create a new task definition, which then is used in the service.   This is done, to retain consistent behaviour and to ensure the ECS agent e.g. pulls all images.   But the newly created task definition will be based on the given task, not the currently used task.Set an environment variableTo add a new or adjust an existing environment variable of a specific container, run the following command     ecs deploy my-cluster my-service -e webserver SOME_VARIABLE SOME_VALUEThis will modify the webserver container definition and add or overwrite the environment variable SOME_VARIABLE with the value SOME_VALUE. This way you can add new or adjust already existing environment variables.Adjust multiple environment variablesYou can add or change multiple environment variables at once, by adding the -e (or --env) options several times     ecs deploy my-cluster my-service -e webserver SOME_VARIABLE SOME_VALUE -e webserver OTHER_VARIABLE OTHER_VALUE -e app APP_VARIABLE APP_VALUEThis will modify the definition of two containers.The webservers environment variable SOME_VARIABLE will be set to SOME_VALUE and the variable OTHER_VARIABLE to OTHER_VALUE.The apps environment variable APP_VARIABLE will be set to APP_VALUE.Set environment variables exclusively, remove all other pre-existing environment variablesTo reset all existing environment variables of a task definition, use the flag --exclusive-env      ecs deploy my-cluster my-service -e webserver SOME_VARIABLE SOME_VALUE --exclusive-envThis will remove all other existing environment variables of all containers of the task definition, except for the variable SOME_VARIABLE with the value SOME_VALUE in the webserver container.Set a secret environment variable from the AWS Parameter Store.. important    This option was introduced by AWS in ECS Agent v1.22.0. Make sure your ECS agent version is  1.22.0 or else your task will not deploy.To add a new or adjust an existing secret of a specific container, run the following command     ecs deploy my-cluster my-service -s webserver SOME_SECRET KEY_OF_SECRET_IN_PARAMETER_STOREYou can also specify the full arn of the parameter     ecs deploy my-cluster my-service -s webserver SOME_SECRET arnawsssmaws regionaws account idparameterKEY_OF_SECRET_IN_PARAMETER_STOREThis will modify the webserver container definition and add or overwrite the environment variable SOME_SECRET with the value of the KEY_OF_SECRET_IN_PARAMETER_STORE in the AWS Parameter Store of the AWS Systems Manager.Set secrets exclusively, remove all other pre-existing secret environment variablesTo reset all existing secrets (secret environment variables) of a task definition, use the flag --exclusive-secrets      ecs deploy my-cluster my-service -s webserver NEW_SECRET KEY_OF_SECRET_IN_PARAMETER_STORE --exclusive-secretThis will remove all other existing secret environment variables of all containers of the task definition, except for the new secret variable NEW_SECRET with the value coming from the AWS Parameter Store with the name KEY_OF_SECRET_IN_PARAMETER_STORE in the webserver container.Set environment via .env filesInstead of setting environment variables separately, you can pass a .env file per container to set the whole environment at once. You can either point to a local file or a file stored on S3, via     ecs deploy my-cluster my-service --env-file my-app envmy-app.env     ecs deploy my-cluster my-service --s3-env-file my-app arnawss3my-ecs-environmentmy-app.envSet secrets via .env filesInstead of setting secrets separately, you can pass a .env file per container to set all secrets at once.This will expect an env file format, but any values will be set as the valueFrom parameter in the secrets config.This value can be either the path or the full ARN of a secret in the AWS Parameter Store. For example, with a secrets.envfile like the followingSOME_SECRETarnawsssmaws regionaws account idparameterKEY_OF_SECRET_IN_PARAMETER_STORE ecs deploy my-cluster my-service --secret-env-file webserver envsecrets.envThis will modify the webserver container definition and add or overwrite the environment variable SOME_SECRET with the value of the KEY_OF_SECRET_IN_PARAMETER_STORE in the AWS Parameter Store of the AWS Systems Manager.Set a docker labelTo add a new or adjust an existing docker labels of a specific container, run the following command     ecs deploy my-cluster my-service -d webserver somelabel somevalueThis will modify the webserver container definition and add or overwrite the docker label somelabel with the value somevalue. This way you can add new or adjust already existing docker labels.Adjust multiple docker labelsYou can add or change multiple docker labels at once, by adding the -d (or --docker-label) options several times     ecs deploy my-cluster my-service -d webserver somelabel somevalue -d webserver otherlabel othervalue -d app applabel appvalueThis will modify the definition of two containers.The webservers docker label somelabel will be set to somevalue and the label otherlabel to othervalue.The apps docker label applabel will be set to appvalue.Set docker labels exclusively, remove all other pre-existing docker labelsTo reset all existing docker labels of a task definition, use the flag --exclusive-docker-labels      ecs deploy my-cluster my-service -d webserver somelabel somevalue --exclusive-docker-labelsThis will remove all other existing docker labels of all containers of the task definition, except for the label somelabel with the value somevalue in the webserver container.Modify a commandTo change the command of a specific container, run the following command     ecs deploy my-cluster my-service --command webserver nginxThis will modify the webserver container and change its command to nginx. If you have a command that requires arguments as well, then you can simply specify it like this as you would normally do     ecs deploy my-cluster my-service --command webserver ngnix -c etcngnixngnix.confThis works fine as long as any of the arguments do not contain any spaces. In case arguments to thecommand itself contain spaces, then you can use the JSON format ecs deploy my-cluster my-service --command webserver [sh, -c, while true do echo Time files like an arrow (date) sleep 1 done]More about this can be looked up in documentation.httpsdocs.aws.amazon.comAmazonECSlatestdeveloperguidetask_definition_parameters.htmlcontainer_definitionsSet a task roleTo change or set the role, the services task should run as, use the following command     ecs deploy my-cluster my-service -r arnawsiam123456789012roleMySpecialEcsTaskRoleThis will set the task role to MySpecialEcsTaskRole.Set CPU and memory reservation- Set the cpu value for a task code--task-cpu 0.- Set the cpu value for a task container code--cpu container_name 0.- Set the memory value (hard limit) for a task code--task-memory 256.- Set the memory value (hard limit) for a task container code--memory container_name 256.- Set the memoryreservation value (soft limit) for a task definition code--memoryreservation container_name 256.Set privileged or essential flags- Set the privileged value for a task definition code--privileged container_name TrueFalse.- Set the essential value for a task definition code--essential container_name TrueFalse.Set logging configurationSet the logConfiguration values for a task definition    --log container_name awslogs awslogs-group log_group_name    --log container_name awslogs awslogs-region region    --log container_name awslogs awslogs-stream-prefix stream_prefixSet port mapping- Set the port mappings values for a task definition code--port container_name container_port host_port.  - Supports code--exclusive-ports.  - The protocol is fixed to tcp.Set volumes  mount points- Set the volumes values for a task definition code--volume volume_name hostpath.  - codevolume_name can then be used with  code--mount.- Set the mount points values for a task definition code--mount container_name volume_name containerpath.  - Supports code--exclusive-mounts.  - codevolume_name is the one set by code--volume.- Set the ulimits values for a task definition code--ulimit container_name memlock 67108864 67108864.  - Supports code--exclusive-ulimits.- Set the systemControls values for a task definition code--system-control container_name net.core.somaxconn 511.  - Supports code--exclusive-system-controls.- Set the healthCheck values for a task definition code--health-check container_name command interval timeout retries start_period.Set Health Checks  - Example code--health-check webserver curl -f httplocalhostalive 30 5 3 0Placeholder Container- Add placeholder containers code--add-container container_name.- To comply with the minimum requirements for a task definition, a placeholder container is set like this    + The container name is codecontainer_name.    + The container image is codePLACEHOLDER.    + The container soft limit is code128.- The idea is to set sensible values with the deployment.It is possible to add and define a new container with the same deployment      --add-container redis --image redis redis6 --port redis 6379 6379Remove containers- Containers can be removed code--remove-container container_name.  - Leaves the original containers, if all containers would be removed.All but the container flags can be used with ecs deploy and ecs cron.The container flags are used with ecs deploy only.Ignore capacity issuesIf your cluster is undersized or the services deployment options are not optimally set, the clustermight be incapable to run blue-green-deployments. In this case, you might see errors like these    ERROR (service my-service) was unable to place a task because no container instance met all of    its requirements. The closest matching (container-instance 123456-1234-1234-1234-1234567890) is    already using a port required by your task. For more information, see the Troubleshooting    section of the Amazon ECS Developer Guide.There might also be warnings about insufficient memory or CPU.To ignore these warnings, you can run the deployment with the flag --ignore-warnings     ecs deploy my-cluster my-service --ignore-warningsIn that case, the warning is printed, but the script continues and waits for a successfuldeployment until it times out.Deployment timeoutThe deploy and scale actions allow defining a timeout (in seconds) via the --timeout parameter.This instructs ecs-deploy to wait for ECS to finish the deployment for the given number of seconds.To run a deployment without waiting for the successful or failed result at all, set --timeout to the value of -1.Multi-Account SetupIf you manage different environments of your system in multiple differnt AWS accounts, you can now easily assume adeployment role in the target account in which your ECS cluster is running. You only need to provide --accountwith the AWS account id and --assume-role with the name of the role you want to assume in the target account.ecs-deploy automatically assumes this role and deploys inside your target accountExample     ecs deploy my-cluster my-service --account 1234567890 --assume-role ecsDeployRoleScaling-------Scale a serviceTo change the number of running tasks and scale a service up and down, run this command     ecs scale my-cluster my-service 4Running a Task--------------Run a one-off taskTo run a one-off task, based on an existing task-definition, run this command     ecs run my-cluster my-taskYou can define just the task family (e.g. my-task) or you can run a specific revision of the task-definition (e.g.my-task123). And optionally you can add or adjust environment variables like this     ecs run my-cluster my-task123 -e my-container MY_VARIABLE my valueRun a task with a custom commandYou can override the command definition via option -c or --command followed by the container name and thecommand in a natural syntax, e.g. no conversion to comma-separation required     ecs run my-cluster my-task -c my-container python some-script.py param1 param2The JSON syntax explained above regarding modifying a command is also applicable here.Run a task in a Fargate ClusterIf you want to run a one-off task in a Fargate cluster, additional configuration is required, to instruct AWS e.g. whichsubnets or security groups to use. The required parameters for this are- launchtype- securitygroup- subnet- public-ipExample     ecs run my-fargate-cluster my-task --launchtypeFARGATE --securitygroup sg-01234567890123456 --subnet subnet-01234567890123456 --public-ipYou can pass multiple subnet as well as multiple securitygroup values. the public-ip flag determines, if the task receives a public IP address or not.Please see ecs run --help for more details.Monitoring----------With ECS deploy you can track your deployments automatically. Currently only New Relic is supportedNew RelicTo record a deployment in New Relic, you can provide the the API Key (Attention this is a specific REST API Key, not the license key) and the application id in two waysVia cli options     ecs deploy my-cluster my-service --newrelic-apikey ABCDEFGHIJKLMN --newrelic-appid 1234567890Or implicitly via environment variables NEW_RELIC_API_KEY and NEW_RELIC_APP_ID      export NEW_RELIC_API_KEYABCDEFGHIJKLMN     export NEW_RELIC_APP_ID1234567890     ecs deploy my-cluster my-serviceOptionally you can provide additional information for the deployment- --comment New feature X - comment to the deployment- --user john.doe - the name of the user who deployed with- --newrelic-revision 1.0.0 - explicitly set the revision to use for the deploymentNote If neither --tag nor --newrelic-revision are provided, the deployment will not be recorded.Troubleshooting---------------If the service configuration in ECS is not optimally set, you might be seeingtimeout or other errors during the deployment.TimeoutThe timeout error means, that AWS ECS takes longer for the full deployment cycle then ecs-deploy is told to wait. The deployment itself still might finish successfully, if there are no other problems with the deployed containers.You can increase the time (in seconds) to wait for finishing the deployment via the --timeout parameter. This time includes the full cycle of stopping all old containers and (re)starting all new containers. Different stacks require different timeout values, the default is 300 seconds.The overall deployment time depends on different things- the type of the application. For example node.js containers tend to take a long time to get stopped. But nginx containers tend to stop immediately, etc.- are old and new containers able to run in parallel (e.g. using dynamic ports)- the deployment options and strategy (Maximum percent  100)- the desired count of running tasks, compared to- the number of ECS instances in the clusterAlternative Implementation--------------------------There are some other librariestools available on GitHub, which also handle the deployment of containers in AWS ECS. If you prefer another language over Python, have a look at these projectsShell  ecs-deploy - httpsgithub.comsilinternationalecs-deployRuby  broadside - httpsgithub.comlumoslabsbroadside</longdescription>
</pkgmetadata>