<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># BitcoinShamir 1.0.1A pure python implementation of the Shamir Secret Sharing scheme for use with BIP-39 mnemonic codes. Split a 24-word mnemonic phrase into several shares, and set a minimum threshold to recover your original phrase.## Install`pip install bitcoinshamir`## Quick Start```&gt;&gt;&gt; from bitcoinshamir import *&gt;&gt;&gt;&gt;&gt;&gt; # Generate a random mnemonic phrase.&gt;&gt;&gt; mnemonic = Mnemonic.generate_random()&gt;&gt;&gt; mnemonic_phrase = get_phrase(mnemonic, Language.English)&gt;&gt;&gt;&gt;&gt;&gt; # Create a 3 of 5 sharing scheme, for which any 3 shares can recover the&gt;&gt;&gt; # original mnemonic phrase.&gt;&gt;&gt; shares = create_shares(3, 5, mnemonic)&gt;&gt;&gt;&gt;&gt;&gt; # Write down each share phrase and pass out to participants.&gt;&gt;&gt; phrase_1 = get_phrase(shares[0], Language.English)&gt;&gt;&gt; phrase_2 = get_phrase(shares[1], Language.English)&gt;&gt;&gt; phrase_3 = get_phrase(shares[2], Language.English)&gt;&gt;&gt; phrase_4 = get_phrase(shares[3], Language.English)&gt;&gt;&gt; phrase_5 = get_phrase(shares[4], Language.English)&gt;&gt;&gt;&gt;&gt;&gt; # The original mnemonic phrase can be recovered with any three of the share&gt;&gt;&gt; # phrases.&gt;&gt;&gt; recovery_share_1 = Share.from_share_phrase(phrase_4, Language.English)&gt;&gt;&gt; recovery_share_2 = Share.from_share_phrase(phrase_2, Language.English)&gt;&gt;&gt; recovery_share_3 = Share.from_share_phrase(phrase_5, Language.English)&gt;&gt;&gt; recovery_shares = [recovery_share_1, recovery_share_2, recovery_share_3]&gt;&gt;&gt;&gt;&gt;&gt; recovered_mnemonic = recover_mnemonic(recovery_shares)&gt;&gt;&gt; recovered_phrase = get_phrase(recovered_mnemonic, Language.English)&gt;&gt;&gt;&gt;&gt;&gt; # You can also use recovery shares to generate new shares.&gt;&gt;&gt; recovery_points = [share.point for share in recovery_shares]&gt;&gt;&gt; X = 7&gt;&gt;&gt; Y = Lagrange.interpolate(recovery_points, PRIME_MODULUS, X)&gt;&gt;&gt; point = Point(X, Y)&gt;&gt;&gt; threshold = recovery_shares[0].threshold&gt;&gt;&gt; seed_checksum = recovery_shares[0].seed_checksum&gt;&gt;&gt;&gt;&gt;&gt; share_6 = Share(point, threshold, seed_checksum)&gt;&gt;&gt;&gt;&gt;&gt; # You can change the language of your share phrase or mnemonic phrase.&gt;&gt;&gt; mnemonic = Mnemonic()&gt;&gt;&gt; english_phrase = [&quot;team&quot;, &quot;lend&quot;, &quot;rice&quot;] # Set this to your actual phrase&gt;&gt;&gt;&gt;&gt;&gt; for i, word in enumerate(english_phrase):&gt;&gt;&gt; mnemonic.set_word(i, word, Language.English)&gt;&gt;&gt;&gt;&gt;&gt; spanish_phrase = get_phrase(mnemonic, Language.Spanish)&gt;&gt;&gt; italian_phrase = get_phrase(mnemonic, Language.Italian)&gt;&gt;&gt; korean_phrase = get_phrase(mnemonic, Language.Korean)```## Notes- You must have the minimum threshold shares to recover your original phrase.- Even 1 less share will not reveal a single word from the original phrase.- Using more shares than the threshold will still recover the original phrase.- Share phrases use the same BIP-39 word lists as mnemonic phrases do.- Each share has a checksum, which will raise an error for any miskeyed words.- Each share uses the original mnemonic checksum as a group ID.- Shares do not need to be in the same language. They are not stored as text.## Share ConstructionEach share represents an (X, Y) coordinate on a graph. It is 37 bytes, in order of the following:- 32 bytes - The Y-value of the share- 1 byte - The original mnemonic's checksum- 5 bits - The version, xor the share checksum- 4 bits - The threshold, xor the share checksum- 7 bits - The X-value, xor the share checksum- 2 bytes - The share checksumThe version, threshold, and X-value are all concatenated and applied the xor of the share checksum at the same time. The share checksum is the first 2 bytes of the sha256 hash of the first 35 bytes.</longdescription>
</pkgmetadata>