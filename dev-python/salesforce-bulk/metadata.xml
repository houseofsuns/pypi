<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. figure httpstravis-ci.orgherokusalesforce-bulk.svgbranchmain   alt travis-badgeSalesforce BulkPython client library for accessing the asynchronous Salesforce.com BulkAPI.Installation------------.. code-block bash    pip install salesforce-bulkAuthentication--------------To access the Bulk API you need to authenticate a user into Salesforce.The easiest way to do this is just to supply username, passwordand security_token. This library will use the simple-salesforcepackage to handle password based authentication... code-block python    from salesforce_bulk import SalesforceBulk    bulk  SalesforceBulk(usernameusername, passwordpassword, security_tokensecurity_token)    ...Alternatively if you run have access to a session ID and instance_urlyou can use those directly.. code-block python    from urlparse import urlparse    from salesforce_bulk import SalesforceBulk    bulk  SalesforceBulk(sessionIdsessionId, hosturlparse(instance_url).hostname)    ...Operations----------The basic sequence for driving the Bulk API is1. Create a new job2. Add one or more batches to the job3. Close the job4. Wait for each batch to finishBulk Query----------bulk.create_query_job(object_name, contentTypeJSON)Using API v39.0 or higher, you can also use the queryAll operationbulk.create_queryall_job(object_name, contentTypeJSON)Example.. code-block python    import json    from salesforce_bulk.util import IteratorBytesIO    job  bulk.create_query_job(Contact, contentTypeJSON)    batch  bulk.query(job, select Id,LastName from Contact)    bulk.close_job(job)    while not bulk.is_batch_done(batch)        sleep(10)    for result in bulk.get_all_results_for_query_batch(batch)        result  json.load(IteratorBytesIO(result))        for row in result            print row  dictionary rowsSame example but for CSV.. code-block python    import unicodecsv        job  bulk.create_query_job(Contact, contentTypeCSV)    batch  bulk.query(job, select Id,LastName from Contact)    bulk.close_job(job)    while not bulk.is_batch_done(batch)        sleep(10)    for result in bulk.get_all_results_for_query_batch(batch)        reader  unicodecsv.DictReader(result, encodingutf-8)        for row in reader            print(row)  dictionary rowsNote that while CSV is the default for historical reasons, JSON shouldbe prefered since CSV has some drawbacks including its handling of NULLvs empty string.PK Chunk HeaderIf you are querying a large number of records you probably want to turn on PK Chunkinghttpsdeveloper.salesforce.comdocsatlas.en-us.api_asynch.metaapi_asynchasync_api_headers_enable_pk_chunking.htm_bulk.create_query_job(object_name, contentTypeCSV, pk_chunkingTrue)That will use the default setting for chunk size. You can use a different chunk size by providing anumber of records per chunkbulk.create_query_job(object_name, contentTypeCSV, pk_chunking100000)Additionally if you want to do something more sophisticated you can provide a header valuebulk.create_query_job(object_name, contentTypeCSV, pk_chunkingchunkSize50000 startRow00130000000xEftMGH)Bulk Insert, Update, Delete---------------------------All Bulk upload operations work the same. You set the operation when youcreate the job. Then you submit one or more documents that specifyrecords with columns to insertupdatedelete. When deleting you shouldonly submit the Id for each record.For efficiency you should use the post_batch method to post eachbatch of data. (Note that a batch can have a maximum 10,000 records andbe 1GB in size.) You pass a generator or iterator into this function andit will stream data via POST to Salesforce. For help sending CSVformatted data you can use the salesforce_bulk.CsvDictsAdapter class.It takes an iterator returning dictionaries and returns an iteratorwhich produces CSV data.Full example.. code-block python    from salesforce_bulk import CsvDictsAdapter    job  bulk.create_insert_job(Account, contentTypeCSV)    accounts  [dict(NameAccountd  idx) for idx in xrange(5)]    csv_iter  CsvDictsAdapter(iter(accounts))    batch  bulk.post_batch(job, csv_iter)    bulk.wait_for_batch(job, batch)    bulk.close_job(job)    print(Done. Accounts uploaded.)Concurrency modeWhen creating the job, pass concurrencySerial orconcurrencyParallel to set the concurrency mode for the job.</longdescription>
</pkgmetadata>