<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pyinstrument[[PyPI version](httpsbadge.fury.iopypyinstrument.svg)](httpsbadge.fury.iopypyinstrument)[[.githubworkflowstest.yml](httpsgithub.comjoerickpyinstrumentactionsworkflowstest.ymlbadge.svg)](httpsgithub.comjoerickpyinstrumentactionsworkflowstest.yml)[[Build wheels](httpsgithub.comjoerickpyinstrumentactionsworkflowswheels.ymlbadge.svg)](httpsgithub.comjoerickpyinstrumentactionsworkflowswheels.yml)[Documentation](httpspyinstrument.readthedocs.io)-- MARK intro start --[[Screenshot](docsimgscreenshot.jpg)](httpsraw.githubusercontent.comjoerickpyinstrumentmaindocsimgscreenshot.jpg)Pyinstrument is a Python profiler. A profiler is a tool to help you optimizeyour code - make it faster. To get the biggest speed increase you should[focus on the slowest part of your program](httpsen.wikipedia.orgwikiAmdahl27s_law).Pyinstrument helps you find it  Not sure where to start Check out this [video tutorial from calmcode.io](httpscalmcode.iopyinstrumentintroduction.html)-- MARK intro end --Installation-------------- MARK installation start --    pip install pyinstrumentPyinstrument supports Python 3.7+.-- MARK installation end -- To run Pyinstrument from a git checkout, theres a build step.Take a look at [Contributing](contributing) for more info.Documentation-------------To learn how to use pyinstrument, or to check the reference, head to the[documentation](httpspyinstrument.readthedocs.io).Known issues------------- Profiling code inside a Docker container can cause some strange results,  because the gettimeofday syscall that pyinstrument uses is slow in that  environment. See [83](httpsgithub.comjoerickpyinstrumentissues83)- When using pyinstrument script.py where script.py contains a class  serialized with pickle, you might encounter errors because the  serialisation machinery doesnt know where __main__ is. [See this issue  for workarounds](httpsgithub.comjoerickpyinstrumentissues109issuecomment-722276263)Changelog--------- v4.4.0_5 November 2022_-   Adds the class name to methods in the console output (203)-   Fix a bug that caused pyinstrument machinery to appear at the start of a profile (215)-   Frames that set a __traceback_hide__ local variable will now be removed from the output (217)-   JupyterIPython magic now supports asyncawait, if you run with a --async_modeenabled flag. (212)-   A big refactor to the backend, allowing more than just static information to be captured. This currently is just powering the class name feature, but more is to come v4.3.0_21 August 2022_-   Adds buttons in the HTML output to switch between absolute and    proportional (percentage) time.-   Adds a command line flag --interval (seconds, default 0.001) to change the interval that    pyinstrument samples a program. This is useful for long-running programs,    where increasing the interval reduces the memory overhead.-   Includes wheels for CPython 3.11. v4.2.0-   Adds a command-line option -p --render-option that allows arbitrary    setting of render options. This lets you set options like    filter_threshold from the command line, by doing something like    pyinstrument -p processor_options.filter_threshold0.    Heres the help output for the option          -p RENDER_OPTION, --render-optionRENDER_OPTION                        options to pass to the renderer, in the format                        flag_name or option_nameoption_value. For                        example, to set the option time, pass -p                        timepercent_of_total. To pass multiple options, use                        the -p option multiple times. You can set processor                        options using dot-syntax, like -p                        processor_options.filter_threshold0. option_value is                        parsed as a JSON value or a string.    -   Adds the ability to view times in the console output as percentages,    rather than absolute times. Use the ConsoleRenderer option    timepercent_of_total, or on the command line, use -p, like    pyinstrument -p timepercent_of_total.-   Adds command line options for loading and saving pyinstrument sessions.    You can save the raw data for a pyinstrument session with -r session,    like pyinstrument -r session -o session.pyisession myscript.py. Loading    is via --load, e.g. pyinstrument --load session.pyisession.-   Command line output format is inferred from the -o output file    extension. So if you do pyinstrument -o profile.html myscript.py, you    dont need to supply -r html, pyinstrument will automatically use the    HTML renderer. Or if you do    pyinstrument -o profile.pyisession myscript.py, it will save a raw    session object.-   Adds [usage examples for FastAPI and pytest](httpspyinstrument.readthedocs.ioenlatestguide.htmlprofile-a-web-request-in-fastapi) to the documentation.-   Fixes a bug causing NotImplementedError when using async_modestrict.-   Adds support for Python 3.11 v4.1.1-   Fixed an issue causing PYINSTRUMENT_PROFILE_DIR_RENDERER to output the    wrong file extension when used with the speedscope renderer. v4.1.0-   You can now use pyinstrument natively in an IPython notebook Just use    load_ext pyinstrument at the top of your notebook, and then    pyinstrument in the cell you want to profile.-   Added support for the [speedscope](httpswww.speedscope.app) format.    This provides a way to view interactive flamecharts using pyinstrument. To    use, profile with pyinstrument -r speedscope, and upload to the    speedscope web app.-   You can now configure renderers for the Django middleware file output,    using the PYINSTRUMENT_PROFILE_DIR_RENDERER option.-   Added wheels for Linux aarch64 (64-bit ARM). v4.0.4-   Fix a packaging issue where a package called test was installed    alongside pyinstrument-   Use more modern C APIs to resolve deprecation warnings on Python 3.10.-   Minor docs fixes v4.0.3-   CPython 3.10 support-   Improve error messages when trying to use Profiler from multiple threads-   Fix crash when rendering sessions that contain a module in a FrameGroup v4.0.2-   Fix some packaging issues v4.0.0-   Async support Pyinstrument now detects when an async task hits an await,    and tracks time spent outside of the async context under this await.    So, for example, heres a simple script with an async task that does a    sleep    python    import asyncio    from pyinstrument import Profiler    async def main()        p  Profiler(async_modedisabled)        with p            print(Hello ...)            await asyncio.sleep(1)            print(... World)        p.print()    asyncio.run(main())        Before Pyinstrument 4.0.0, wed see only time spent in the run loop, like    this          _     ._   ____   _ _  _  _ __   Recorded 183303  Samples  2     __ _  _  _      Duration 1.006     CPU time 0.001       _                      v3.4.2    Program examplesasync_example_simple.py    1.006 _run_once  asynciobase_events.py1784     1.005 select  selectors.py553          [3 frames hidden]  selectors, built-in             1.005 kqueue.control  built-in0        Now, with pyinstrument 4.0.0, we get          _     ._   ____   _ _  _  _ __   Recorded 183043  Samples  2         __ _  _  _      Duration 1.007     CPU time 0.001           _                      v4.0.0        Program examplesasync_example_simple.py        1.006 main  async_example_simple.py4         1.005 sleep  asynciotasks.py641              [2 frames hidden]  asyncio                 1.005 [await]    For more information, check out the [async profiling documentation] and    the [Profiler.async_mode] property.-   Pyinstrument has a [documentation site], including full Python API docs[async profiling documentation] httpspyinstrument.readthedocs.ioenlatesthow-it-works.htmlasync-profiling[Profiler.async_mode] httpspyinstrument.readthedocs.ioenlatestreference.htmlpyinstrument.Profiler.async_mode[documentation site] httpspyinstrument.readthedocs.io v3.4.2- Fix a bug that caused --show, --show-regex, --show-all to be ignored  on the command line. v3.4.1- Under-the-hood modernisation v3.4.0- Added timeline option (boolean) to Profiler methods output_html() and  open_in_browser(). v3.3.0- Fixed issue with pyinstrument -m module, where pyinstrument wouldnt find  modules in the current directory.- Dropped support for Python 2.7 and 3.5. Old versions will remain available  on PyPI, and pip should choose the correct one automatically. v3.2.0- Added the ability to track time in C functions. Minor note - Pyinstrument  will record time spent C functions as leaf functions, due to a limitation  in how Python records frames. Python - C - Python is recorded as  Python - Python, but Python - Python - C will be attributed correctly.  (103) v3.1.2- Fix __array_function__ internals frames appearing as app code in reports v3.1.1- Added support for timeline mode on HTML and JSON renderers- Released as a tarball as well as a universal wheel v3.1.0- Added PYINSTRUMENT_SHOW_CALLBACK option on the Django middleware to  add a condition to showing the profile (could be used to run pyinstrument  on a live server)- Fixed bug in the Django middleware where file would not be written because  of a unicode error v3.0.3- Fixed bug with the Django middleware on Windows where profiling would fail  because we were trying to put an illegal character  in the profile path.  (66) v3.0.2- Add --show and --show-regex options, to mark certain files to be  displayed. This helps to profile inside specific modules, while hiding  others. For example, pyinstrument --show sympy script.py. v3.0.1- Fix 60 pass all arguments after -m module_name to the called module- Fix crash during HTMLJSON output when no frames were captured. v3.0.0- Pyinstrument will now hide traces through libraries that youre using by default. So instead of showing you loads of frames going through the internals of something external e.g. urllib, it lets you focus on your code.     Before  After      ---  ---     [image](httpsuser-images.githubusercontent.com124430750928250-1e50db00-1452-11e9-9164-6050a3c950ed.png)  [image](httpsuser-images.githubusercontent.com124430750928326-4c361f80-1452-11e9-91e8-cea735584806.png)   To go back to the old behaviour, use --show-all on the command line.- Entry frames of hidden groups are shown, so you know which call is the problem- Really slow frames in the groups are shown too, e.g. the read call on the socket- Application code is highlighted in the console- Additional metrics are shown at the top of the trace - timestamp, number of samples, duration, CPU time- Hidden code is controlled by the --hide or --hide-regex options - matching on the path of the code files.      --hideEXPR           glob-style pattern matching the file paths whose                          frames to hide. Defaults to lib.    --hide-regexREGEX    regex matching the file paths whose frames to hide.                          Useful if --hide doesnt give enough control.  - Outputting a timeline is supported from the command line.      -t, --timeline        render as a timeline - preserve ordering and dont                          condense repeated calls  - Because there are a few rendering options now, you can load a previous profiling session using --load-prev - pyinstrument keeps the last 10 sessions.- Hidden groups can also call back into application code, that looks like this    [image](httpsuser-images.githubusercontent.com124430750928591-fca42380-1452-11e9-8320-3c851cf5210e.png)- (internal) When recording timelines, frame trees are completely linear now, allowing  for the creation of super-accurate frame charts.- (internal) The HTML renderer has been rewritten as a Vue.js app. All the console improvements apply to the HTML output too, plus its interactive.- (internal) A lot of unit and integration tests addedYikes See 49 for the gory details. I hope you like it. v2.3.0-   Big refactor    -   Recorders have been removed. The frame recording is now internal to the Profiler object.        This means the frame objects are more general-purpose, which paves the way for...    -   Processors These are functions that mutate the tree to sculpt the output.        They are used by the renderers to filter the output to the correct form. Now, instead of        a time-aggregating recorder, the profiler just uses timeline-style recording (this is        lower-overhead anyway) and the aggregation is done as a processing step.    -   The upshot of this is that its now way easier to alter the tree to filter stuff out, and        do more advanced things like combining frames that we dont care about. More features to        come that use this in v3.0-   Importlib frames are removed - you wont see them at all. Their children are retained, so    imports are just transparent.-   Django profile file name is now limited to a hundred of characters (50)-   Fix bug with --html option (53)-   Add --version command line option v2.2.1-   Fix crash when using on the command line. v2.2.0-   Added support for JSON output. Use pyinstrument --rendererjson scriptfile.py.    [PR](httpsgithub.comjoerickpyinstrumentpull46)-   [iddan](httpsgithub.comiddan) has put together an    [interactive viewer](httpspython-flame-chart.netlify.com) using the JSON output    [image](httpsuser-images.githubusercontent.com124430744622790-3ca9a600-a8b8-11e8-8dc2-f33ce433c03d.png)-   When running pyinstrument --html and you dont pipe the output to a file, pyinstrument will write the console output to a temp file and open that in a browser. v2.1.0-   Added support for running modules with pyinstrument via the command line. The new syntax    is the -m flag e.g. pyinstrument -m module_name [PR](httpsgithub.comjoerickpyinstrumentpull45pullrequestreview-143383557) v2.0.4-   Fix crashes due to multi-threaded use of pyinstrument. The fix is in the C extension,    over at httpsgithub.comjoerickpyinstrument_cextpull3 v2.0.3-   Pyinstrument can now be used in a with block.    For exampleprofiler  pyinstrument.Profiler()with profiler     do some work here...print(profiler.output_text())-   Middleware fix for older versions of Django v2.0.2-   Fix for max recursion error when used to profile programs with a lot of frames on the stack. v2.0.1-   Ensure license is included in the sdist. v2.0.0-   Pyinstrument uses a new profiling mode. Rather than using    signals, pyintrument uses a new statistical profiler built on    PyEval_SetProfile. This means no more main thread restriction, no more    IO errors when using Pyinstrument, and no need for a separate more    setprofile mode-   Renderers. Users can customize Pyinstrument to use alternative renderers    with the renderer argument on Profiler.output(), or using the --renderer    argument on the command line.-   Recorders. To support other use cases of Pyinstrument (e.g. flame charts),    pyinstrument now has a timeline recorder mode. This mode records captured    frames in a linear way, so the program execution can be viewed on a    timeline. v0.13-   pyinstrument command. You can now profile python scripts from the shell    by running  pyinstrument script.py. This is now equivalent to    python -m pyinstrument. Thanks asmeurer v0.12-   Application code is highlighted in HTML traces to make it easier to spot-   Added PYINSTRUMENT_PROFILE_DIR option to the Django interface, which    will log profiles of all requests to a file the specified folder. Useful    for profiling API calls.-   Added PYINSTRUMENT_USE_SIGNAL option to the Django interface, for use    when signal mode presents problems.Contributing------------To setup a dev environment    virtualenv --pythonpython3 env    . envbinactivate    pip install --upgrade pip    pip install -r requirements-dev.txt    pre-commit install --install-hooksTo get some sample output    pyinstrument exampleswikipedia_article_word_count.pyTo run the tests    pytestTo run linting checks locally    pre-commit run --all-filesSome of the pre-commit checks, like isort or black, will auto-fixthe problems they find. So if the above command returns an error, tryrunning it again, it might succeed the second time )Running all the checks can be slow, so you can also run checksindividually, e.g., to format source code that fails isort or blackchecks    pre-commit run --all-files isort    pre-commit run --all-files blackTo diagnose why pyright checks are failing    pre-commit run --all-files pyright The HTML renderer Vue.js appThe HTML renderer works by embedding a JSON representation of the sample witha Javascript bundle inside an HTML file that can be viewed in any webbrowser.To edit the html renderer style, do    cd html_renderer    npm ci    npm run serveWhen launched without a top-level window.profileSession object, it willfetch a sample profile so you can work with it.To compile the JS app and bundle it back into the pyinstrument python tool    binbuild_js_bundle.py [--force]</longdescription>
</pkgmetadata>