<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpsimg.shields.iopypivoldest-supported-numpy   target httpspypi.orgprojectoldest-supported-numpy   alt PyPIAbout-----This is a meta-package which can be used in pyproject.toml filesto automatically provide as a build-time dependency the oldest versionof NumPy that supports the given Python version and platform. In caseof platforms for which NumPy has prebuilt wheels, the provided versionalso has a prebuilt NumPy wheel.The reason to use the oldest available NumPy version as a build-timedependency is because of ABI compatibility. Binaries compiled with oldNumPy versions are binary compatible with newer NumPy versions, butnot vice versa. This meta-package exists to make dealing with thismore convenient, without having to duplicate the same list manually inall packages requiring it.In other words.. code toml    [build-system]    requires  [        wheel,        setuptools,        numpy1.13.3 python_version3.5,        numpy1.13.3 python_version3.6,        numpy1.14.5 python_version3.7,         more numpy requirements...    ]can be replaced by.. code toml    [build-system]    requires  [wheel, setuptools, oldest-supported-numpy]And as new Python versions are released, the pyproject.toml file does notneed to be updated.QA---Why define the NumPy pinnings using install_requires in this repositoryThe NumPy version pinnings are defined inside the setup.cfg file asinstall_requires dependencies, rather than as build-time dependenciesinside pyproject.toml. This is deliberate, since NumPy is not actuallyrequired to build wheels of oldest-supported-numpy. What we need hereis to make sure that when oldest-supported-numpy is installed intothe build environment of a package using it, NumPy gets installed tooas a runtime dependency inside the build environment.Another way to think about this is that since we only publish (universal)wheels of oldest-supported-numpy, the wheel contains no pyproject.toml,setup.cfg, or setup.py code - it only contains metadata includingdependencies which get installed by pip when oldest-supported-numpy isinstalled.Can I use this if my package requires a recent version of NumPyIn many cases, even though your package may require a version ofNumPy that is more recent than the pinned versions here, thisis often a runtime requirement, i.e. for running (rather thanbuilding) your package. In many cases, unless you use recentfeatures of the NumPy C API, you will still be able to build yourpackage with an older version of NumPy and therefore you will stillbe able to use oldest-supported-numpy. You can still impose amore recent NumPy requirement in install_requiresWhat if a bug in NumPy that affects me is fixed only in a newer releaseIf oldest-supported-numpy pins a numpy version that is broken foreveryone using a certain OSplatforminterpreter combination, we can update the pin to a newer release. In general, building against a newer bugfixrelease (i.e., a higher Y value for a 1.X.Y version number) is safe todo. Newer minor versions will likely not be ABI-compatible, so are much moredifficult to change. If a bug only affects some uses cases (e.g., versions 1.20.3 dont work on Windows when using f2py), the pin cannot be updatedbecause it will affect backwards compatibility of oldest-supported-numpy.In that case, it is recommended that you add the needed constraint directlyin your own pyproject.toml file. For example.. code toml    [build-system]    requires  [        wheel,        numpy1.19.0 python_version3.8 and platform_systemWindows and platform_python_implementation  PyPy,        oldest-supported-numpy python_version3.8 or platform_systemWindows or platform_python_implementation  PyPy,         more requirements (if needed) ...    ]Note that when you do this, it is important to ensure the conditions are suchthat there is exactly one pin possible for a given platform configuration.Otherwise your build will fail or pip may refuse to install your packageonly on that configuration (so you likely wont see it in CI).The oldest-supported-numpy repository contains tests, so for safety youmay want to implement your constraints in its setup.cfg and run thetests with pytest to validate those constraints.Why isnt oldest-supported-numpy available for Conda, Homebrew, Debian, etc.The pyproject.toml format is specific to PyPI. Other packaging systems havetheir own metadata formats and ways of specifying dependencies. Typically theydont need anything like oldest-supported-numpy because either (a) they shiponly a single NumPy version for a given release (typically the case for Linuxdistros and Homebrew), or (b) they have a more explicit way of managing ABIcompatibility (see for example conda-forges pin_compatible featurehttpsconda-forge.orgdocsmaintainerknowledge_base.htmllinking-numpy).What about having a catchier name for this packageThe current name is not very catchy as package names go, but itis very descriptive. This package is only meant to be used inpyproject.toml files for defining build-time dependencies,so its more important to have a descriptive than a catchy nameWhat if I think that one of the pinnings is wrong or out of datePlease feel free to open an issue httpsgithub.comscipyoldest-supported-numpyissuesnew_or a pull request if you think something is wrong or could be improved</longdescription>
</pkgmetadata>