<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>JMESPath.. image httpsbadges.gitter.imJoin Chat.svg   target httpsgitter.imjmespathchatJMESPath (pronounced james path) allows you to declaratively specify how toextract elements from a JSON document.For example, given this document    {foo {bar baz}}The jmespath expression foo.bar will return baz.JMESPath also supportsReferencing elements in a list.  Given the data    {foo {bar [one, two]}}The expression foo.bar[0] will return one.You can also reference all the items in a list using the syntax   {foo {bar [{name one}, {name two}]}}The expression foo.bar[].name will return [one, two].Negative indexing is also supported (-1 refers to the last elementin the list).  Given the data above, the expressionfoo.bar[-1].name will return two.The  can also be used for hash types   {foo {bar {name one}, baz {name two}}}The expression foo..name will return [one, two].InstallationYou can install JMESPath from pypi with.. code bash    pip install jmespathAPIThe jmespath.py library has two functionsthat operate on python data structures.  You can use searchand give it the jmespath expression and the data.. code python     import jmespath     path  jmespath.search(foo.bar, {foo {bar baz}})    bazSimilar to the re module, you can use the compile functionto compile the JMESPath expression and use this parsed expressionto perform repeated searches.. code python     import jmespath     expression  jmespath.compile(foo.bar)     expression.search({foo {bar baz}})    baz     expression.search({foo {bar other}})    otherThis is useful if youre going to use the same jmespath expression tosearch multiple documents.  This avoids having to reparse theJMESPath expression each time you search a new document.Options-------You can provide an instance of jmespath.Options to control howa JMESPath expression is evaluated.  The most common scenario forusing an Options instance is if you want to have ordered outputof your dict keys.  To do this you can use either of these options.. code python     import jmespath     jmespath.search({a a, b b},    ...                 mydata,    ...                 jmespath.Options(dict_clscollections.OrderedDict))     import jmespath     parsed  jmespath.compile({a a, b b})     parsed.search(mydata,    ...               jmespath.Options(dict_clscollections.OrderedDict))Custom FunctionsThe JMESPath language has numerousbuilt-in functionshttpjmespath.orgspecification.htmlbuilt-in-functions__, but it isalso possible to add your own custom functions.  Keep in mind thatcustom function support in jmespath.py is experimental and the API maychange based on feedback.If you have a custom function that youve found useful, consider submittingit to jmespath.site and propose that it be added to the JMESPath language.You can submit proposalshere httpsgithub.comjmespathjmespath.siteissues__.To create custom functions Create a subclass of jmespath.functions.Functions. Create a method with the name _func_your function name. Apply the jmespath.functions.signature decorator that indicates  the expected types of the function arguments. Provide an instance of your subclass in a jmespath.Options object.Below are a few examples.. code python    import jmespath    from jmespath import functions     1. Create a subclass of functions.Functions.        The function.Functions base class has logic        that introspects all of its methods and automatically        registers your custom functions in its function table.    class CustomFunctions(functions.Functions)         2 and 3.  Create a function that starts with _func_         and decorate it with signature which indicates its         expected types.         In this example, were creating a jmespath function         called unique_letters that accepts a single argument         with an expected type string.        functions.signature({types [string]})        def _func_unique_letters(self, s)             Given a string s, return a sorted             string of unique letters ccbbadd -  abcd            return .join(sorted(set(s)))         Heres another example.  This is creating         a jmespath function called my_add that expects         two arguments, both of which should be of type number.        functions.signature({types [number]}, {types [number]})        def _func_my_add(self, x, y)            return x + y     4. Provide an instance of your subclass in a Options object.    options  jmespath.Options(custom_functionsCustomFunctions())     Provide this value to jmespath.search     This will print 3    print(        jmespath.search(            my_add(1, 2), {}, optionsoptions)    )     This will print abcd    print(        jmespath.search(            foo.bar  unique_letters(),            {foo {bar ccbbadd}},            optionsoptions)    )Again, if you come up with useful functions that you think makesense in the JMESPath language (and make sense to implement in allJMESPath libraries, not just python), please let us know atjmespath.site httpsgithub.comjmespathjmespath.siteissues__.SpecificationIf youd like to learn more about the JMESPath language, you can check outthe JMESPath tutorial httpjmespath.orgtutorial.html__.  Also checkout the JMESPath examples page httpjmespath.orgexamples.html__ forexamples of more complex jmespath queries.The grammar is specified using ABNF, as described inRFC4234 httpwww.ietf.orgrfcrfc4234.txt_.You can find the most up to dategrammar for JMESPath here httpjmespath.orgspecification.htmlgrammar__.You can read the fullJMESPath specification here httpjmespath.orgspecification.html__.TestingIn addition to the unit tests for the jmespath modules,there is a testscompliance directory that contains.json files with test cases.  This allows other implementationsto verify they are producing the correct output.  Each jsonfile is grouped by feature.DiscussJoin us on our Gitter channel httpsgitter.imjmespathchat__if you want to chat or if you have any questions.</longdescription>
</pkgmetadata>