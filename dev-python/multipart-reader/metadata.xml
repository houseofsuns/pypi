<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>================Multipart Reader================.. image:: https://travis-ci.org/novafloss/multipart-reader.svg   :target: https://travis-ci.org/novafloss/multipart-reader   :alt: We are under CI!!Permits to read a multipart content like, mixed, related, etc.Thanks to the *aiohttp* project for the implementation, cf.:http://aiohttp.readthedocs.org/en/stable/multipart.html. Unfortunately lot ofpeople have not yet moved to asyncio, or do not want the full *aiohttp* stackto read the multipart content. Here we tried to keep all the *aiohttp* logic and coverage but the coroutinesmechanism.What it does============It reads the same way multpart/x contents. Lets say we have the following*multipart/related* content::    &gt;&gt;&gt; import json    &gt;&gt;&gt; from email.mime.multipart import MIMEMultipart    &gt;&gt;&gt; from email.mime.base import MIMEBase    &gt;&gt;&gt; multipart = MIMEMultipart('related')    &gt;&gt;&gt; part = MIMEBase('application', 'json')    &gt;&gt;&gt; part.set_payload(json.dumps({'foo': 'bar'}))    &gt;&gt;&gt; multipart.attach(part)    &gt;&gt;&gt; part = MIMEBase('application', 'octet-stream')    &gt;&gt;&gt; part.set_payload(b&quot;Python will save the world. I don't know how. But it will.&quot;)    &gt;&gt;&gt; part.add_header('Content-Disposition', 'attachment', filename='python-save-the-world.txt')    &gt;&gt;&gt; multipart.attach(part)Here is how we can read it::    &gt;&gt;&gt; import io    &gt;&gt;&gt; from multipart_reader import MultipartReader    &gt;&gt;&gt; content = multipart.as_string().split('\n\n', 1)[1]    &gt;&gt;&gt; headers = dict(multipart.items())    &gt;&gt;&gt; stream = io.BytesIO()    &gt;&gt;&gt; stream.write(content)    &gt;&gt;&gt; stream.seek(0)    &gt;&gt;&gt; reader = MultipartReader(headers, stream)    &gt;&gt;&gt; json_part = reader.next()    &gt;&gt;&gt; json_part.json()    {'foo': 'bar'}    &gt;&gt;&gt; file_part = reader.next()    &gt;&gt;&gt; file_part.read()    &quot;Python will save the world. I don't know how. But it will.&quot;    &gt;&gt;&gt; file_part.filename    'python-save-the-world.txt'That's it ...</longdescription>
</pkgmetadata>