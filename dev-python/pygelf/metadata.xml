<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pygelftravis coveralls pypi downloads.. travis image httpstravis-ci.orgkeeprockingpygelf.svgbranchmaster    target httpstravis-ci.orgkeeprockingpygelf.. pypi image httpsbadge.fury.iopypygelf.svg    target httpspypi.python.orgpypipygelf.. coveralls image httpscoveralls.ioreposgithubkeeprockingpygelfbadge.svgbranchmaster    target httpscoveralls.iogithubkeeprockingpygelfbranchmaster.. downloads image httpsimg.shields.iopypidmpygelf.svg    target httpspypi.python.orgpypipygelfPython logging handlers with GELF (Graylog Extended Log Format) support.Currently TCP, UDP, TLS (encrypted TCP) and HTTP logging handlers are supported.Get pygelf.. code python    pip install pygelfUsage.. code python    from pygelf import GelfTcpHandler, GelfUdpHandler, GelfTlsHandler, GelfHttpHandler    import logging    logging.basicConfig(levellogging.INFO)    logger  logging.getLogger()    logger.addHandler(GelfTcpHandler(host127.0.0.1, port9401))    logger.addHandler(GelfUdpHandler(host127.0.0.1, port9402))    logger.addHandler(GelfTlsHandler(host127.0.0.1, port9403))    logger.addHandler(GelfHttpHandler(host127.0.0.1, port9404))    logger.info(hello gelf)Message structureAccording to the GELF spec, each message has the following mandatory fields- version 1.1, can be overridden when creating a logger- short_message the log message itself- timestamp current timestamp- level syslog-compliant_ log level number (e.g. WARNING will be sent as 4)- host hostname of the machine that sent the message- full_message this field contains stack trace and is being written ONLY when logging an exception, e.g... code python    try        10    except ZeroDivisionError as e        logger.exception(e).. _syslog-compliant httpsen.wikipedia.orgwikiSyslogSeverity_levelIn debug mode (when handler was created with debugTrue option) each message contains some extra fields (which are pretty self-explanatory) - _file- _line- _module- _func- _logger_nameConfigurationEach handler has the following parameters- host IP address of the GELF input- port port of the GELF input- debug (False by default) if true, each log message will include debugging info module name, file name, line number, method name- version (1.1 by default) GELF protocol version, can be overridden- include_extra_fields (False by default) if true, each log message will include all the extra fields set to LogRecord- json_default (codestr with exception for several codedatetime objects) function that is called for objects that cannot be serialized to JSON natively by python. Default implementation is custom function that returns result of codeisoformat() method for codedatetime.datetime, codedatetime.time, codedatetime.date objects and result of codestr(obj) call for other objects (which is string representation of an object with fallback to coderepr)Also, there are some handler-specific parameters.UDP- chunk_size (1300 by default) - maximum length of the message. If log length exceeds this value, it splits into multiple chunks (see httpswww.graylog.orgresourcesgelf section chunked GELF) with the length equals to this value. This parameter must be less than the MTU_. If the logs dont seem to be delivered, try to reduce this value.- compress (True by default) - if true, compress log messages before sending them to the server.. _MTU httpsen.wikipedia.orgwikiMaximum_transmission_unitTLS- validate (False by default) - if true, validate server certificate. If server provides a certificate that doesnt exist in ca_certs, you wont be able to send logs over TLS- ca_certs (None by default) - path to CA bundle file. This parameter is required if validate is true.- certfile (None by default) - path to certificate file that will be used to identify ourselves to the remote endpoint. This is necessary when the remote server has client authentication required. If certfile contains the private key, it should be placed before the certificate.- keyfile (None by default) - path to the private key. If the private key is stored in certfile this parameter can be None.HTTP- compress (True by default) - if true, compress log messages before sending them to the server- path (gelf by default) - path of the HTTP input (httpdocs.graylog.orgenlatestpagessending_data.htmlgelf-via-http)- timeout (5 by default) - amount of seconds that HTTP client should wait before it discards the request if the server doesnt respondStatic fieldsIf you need to include some static fields into your logs, simply pass them to the handler constructor. Each additional field should start with underscore. You cant add field _id.Example.. code python    handler  GelfUdpHandler(host127.0.0.1, port9402, _app_namepygelf, _something11)    logger.addHandler(handler)Dynamic fieldsIf you need to include some dynamic fields into your logs, add them to record by using LoggingAdapter or logging.Filter and create handler with include_extra_fields set to True.All the non-trivial fields of the record will be sent to graylog2 with _ added before the nameExample.. code python    class ContextFilter(logging.Filter)        def filter(self, record)            record.job_id  threading.local().process_id            return True    logger.addFilter(ContextFilter())    handler  GelfUdpHandler(host127.0.0.1, port9402, include_extra_fieldsTrue)    logger.addHandler(handler)Defining fields from environmentIf you need to include some fields from the environment into your logs, add them to record by using additional_env_fields.The following example will add an env field to the logs, taking its value from the environment variable FLASK_ENV... code python    handler  GelfTcpHandler(host127.0.0.1, port9402, include_extra_fieldsTrue, additional_env_fields{env FLASK_ENV})    logger.addHandler(handler)The following can also be used in defining logging from configuration files (yamlini).. code ini    [formatters]    keysstandard    [formatter_standard]    classlogging.Formatter    format(message)s    [handlers]    keysgraylog    [handler_graylog]    classpygelf.GelfTcpHandler    formatterstandard    args(127.0.0.1, 12201)    kwargs{include_extra_fields True, debug True, additional_env_fields {env FLASK_ENV}}    [loggers]    keysroot    [logger_root]    levelWARN    handlersgraylogRunning testsTo run tests, youll need tox_. After installing, simply run it.. code    tox.. _tox httpspypi.python.orgpypitox</longdescription>
</pkgmetadata>