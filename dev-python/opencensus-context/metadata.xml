<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>OpenCensus Runtime Contextpypi.. pypi image httpsbadge.fury.iopyopencensus-context.svg   target httpspypi.orgprojectopencensus-contextThe OpenCensus Runtime Context provides in-process context propagation.By default, thread local storage is used for Python 2.7, 3.4 and 3.5contextvars is used for Python  3.6, which provides asyncio support.Installation------------This library is installed by default with opencensus, there is no needto install it explicitly.Usage-----In most cases context propagation happens automatically within a process,following the control flow of threads and asynchronous coroutines. The runtimecontext is a dictionary stored in a context variable httpsdocs.python.org3librarycontextvars.html_when available, and in thread local storage httpsdocs.python.org2librarythreading.htmlthreading.local_otherwise.There are cases where you may want to propagate the context explicitlyExplicit Thread Creation.. code python    from threading import Thread    from opencensus.common.runtime_context import RuntimeContext    def work(name)         here you will get the context from the parent thread        print(RuntimeContext)    thread  Thread(         propagate context explicitly        targetRuntimeContext.with_current_context(work),        args(foobar,),    )    thread.start()    thread.join()Thread Pool.. code python    from multiprocessing.dummy import Pool as ThreadPool    from opencensus.common.runtime_context import RuntimeContext    def work(name)         here you will get the context from the parent thread        print(RuntimeContext)    pool  ThreadPool(2)     propagate context explicitly    pool.map(RuntimeContext.with_current_context(work), [        bear,        cat,        dog,        horse,        rabbit,    ])    pool.close()    pool.join()References---------- Examples httpsgithub.comcensus-instrumentationopencensus-pythontreemastercontextopencensus-contextexamples_ OpenCensus Project httpsopencensus.io_</longdescription>
</pkgmetadata>