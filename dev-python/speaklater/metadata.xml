<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>speaklaterA module that provides lazy strings for translations.  Basically youget an object that appears to be a string but changes the value everytime the value is evaluated based on a callable you provide.For example you can have a global lazy_gettext function that returnsa lazy string with the value of the current set language.Example from speaklater import make_lazy_string sval  uHello World string  make_lazy_string(lambda sval)This lazy string will evaluate to the value of the sval variable. stringluHello World unicode(string)uHello World string.upper()uHELLO WORLDIf you change the value, the lazy string will change as well sval  uHallo Welt string.upper()uHALLO WELTThis is especially handy when combined with a thread local and gettexttranslations or dicts of translatable strings from speaklater import make_lazy_gettext from threading import local l  local() l.translations  {uYes Ja} lazy_gettext  make_lazy_gettext(lambda l.translations.get) yes  lazy_gettext(uYes) print yesJa l.translations[uYes]  uSi print yesSiLazy strings are no real strings so if you pass this sort of string toa function that performs an instance check, it will fail.  In that caseyou have to explicitly convert it with unicode andor string dependingon what string type the lazy string encapsulates.To check if a string is lazy, you can use the is_lazy_string function from speaklater import is_lazy_string is_lazy_string(uyes)False is_lazy_string(yes)TrueNew in version 1.2 Its now also possible to pass keyword arguments tothe callback used with make_lazy_string.</longdescription>
</pkgmetadata>