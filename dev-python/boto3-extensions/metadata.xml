<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>================Boto3 Extensions================Overview--------This module adds more resource files to the Boto3 library and includes some functionality enhancements.Quick Start-----------First, install the library:.. code-block:: sh    $ pip install boto3_extensionsFollow the Boto3 docs on setting up your region and credentials (https://github.com/boto/boto3).Then, from a Python interpreter:.. code-block:: python    &gt;&gt;&gt; import boto3    &gt;&gt;&gt; import boto3_extensions    &gt;&gt;&gt; r = boto3.resource('cloudtrail', region_name='us-east-1')    &gt;&gt;&gt; for trail in r.trails.all():          print(trail.trail_arn)Resource Files--------------The following resource files are **added** to the Boto3 library.  * acm  * autoscaling  * cloudfront  * cloudtrail  * cur  * datapipeline  * directconnect  * elasticache  * elb  * elbv2  * emr  * glue  * health  * kinesis  * lambda  * rds  * redshift  * route53  * supportThe following resource files are **updated** in the Boto3 library.  * dynamodb  * ec2  * iam  * s3RefreshableAssumeRoleProvider-----------------------------If your code needs to AssumeRole into another role before performing actions against the AWS API (be it in the same or another AWS account), you run the risk that the credentials you are using expire during their use. You can either add code to your application to constantly check the credential expiry time or using this extension offload the credential refresh to boto3 itself. By using the ConnectionManager in boto3_extensions not only will it automattically assumeRole when the credentials get below 15 mins left, but it will also cache the credentials. This means that if your application is calling boto3 to get credentials for another role more than once the ConnectionManager will cache the first call and then hand out the same session for the subsequent calls. .. code-block:: python    &gt;&gt;&gt; role_arn = 'arn:aws:iam::1234567890:role/test-role'    &gt;&gt;&gt; role_session_name = 'test'    &gt;&gt;&gt; connections = boto3_extensions.ConnectionManager(region_name='us-east-1')    &gt;&gt;&gt; session = connections.get_session(role_arn=role_arn, role_session_name=role_session_name)    &gt;&gt;&gt;    &gt;&gt;&gt; r = session.resource('cloudtrail', region_name='us-east-1')    &gt;&gt;&gt; for trail in r.trails.all():    &gt;&gt;&gt;     print(trail.trail_arn)ARN Patch---------It would be nice to have a consistent way to get the ARN of resources. The ARN patch feature of boto3_extensions allows you to get the arn from resources via an arn attribute. .. code-block:: python    &gt;&gt;&gt; import boto3    &gt;&gt;&gt; import boto3_extensions    &gt;&gt;&gt; from imp import reload    &gt;&gt;&gt; boto3_extensions.arn_patch_boto3()    &gt;&gt;&gt; reload(boto3)    &gt;&gt;&gt;     &gt;&gt;&gt; r = boto3.resource('rds', region_name='us-east-1')    &gt;&gt;&gt; for db in r.db_instances.all():    &gt;&gt;&gt;   print(db.arn)Session Caching------------------There is basic Session caching builtin that simply stores the :code:`Session` for a each assumed Role into a dict, but if you require another caching mechanism you can monkey patch the :code:`boto3_extensions.Cache` class, as long as you supply the :code:`Cache.get()` and :code:`Cache.set()` methods... code-block:: python    &gt;&gt;&gt; import boto3    &gt;&gt;&gt; import boto3_extensions    &gt;&gt;&gt; from boto3_extensions import ConnectionManager    &gt;&gt;&gt; class MyCache:            def __init__(self):                self._cache = {}            def get(self, role_arn, role_session_name):                print(&quot;inside MyCache.get()&quot;)                return self._cache.get((role_arn, role_session_name), None)            def set(self, role_arn, role_session_name, session):                print(&quot;inside MyCache.set()&quot;)                self._cache[(role_arn, role_session_name)] = session    &gt;&gt;&gt; boto3_extensions.Cache = MyCache    &gt;&gt;&gt; connections = ConnectionManager()    &gt;&gt;&gt; connections.get_session(role_arn=&quot;arn:aws:iam::012345678912:role/test_role&quot;, role_session_name=&quot;testing&quot;)        inside MyCache.get()        inside MyCache.set()    &gt;&gt;&gt; connections.get_session(role_arn=&quot;arn:aws:iam::012345678912:role/test_role&quot;, role_session_name=&quot;testing&quot;)        inside MyCache.get()In the above we monkey patch the :code:`Cache` class and call :code:`get_session()` twice. The output shows that the first time we have a cache get which results in a cache miss and thena cache set after the credential is retrieved from STS. We then run :code:`get_session()` a second time and as the :code:`Session` is now cached we only see a cache get resulting in a cache hit.Getting Help------------Please raise issue ticket inside our Bitbucket repo: https://bitbucket.org/atlassian/boto3_extensions/issues</longdescription>
</pkgmetadata>