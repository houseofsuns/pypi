<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Database-backed Periodic Tasksbuild-status coverage license wheel pyversion pyimpVersion 2.4.0Web httpdjango-celery-beat.readthedocs.ioDownload httppypi.python.orgpypidjango-celery-beatSource httpgithub.comcelerydjango-celery-beatKeywords django, celery, beat, periodic task, cron, schedulingAboutThis extension enables you to store the periodic task schedule in thedatabase.The periodic tasks can be managed from the Django Admin interface, where youcan create, edit and delete periodic tasks and how often they should run.Using the ExtensionUsage and installation instructions for this extension are availablefrom the Celery documentation_... _Celery documentation    httpdocs.celeryproject.orgenlatestuserguideperiodic-tasks.htmlusing-custom-scheduler-classesImportant Warning about Time Zones.. warning   If you change the Django TIME_ZONE setting your periodic task schedule   will still be based on the old timezone.   To fix that you would have to reset the last run time for each periodic task.. code-block Python         from django_celery_beat.models import PeriodicTask, PeriodicTasks         PeriodicTask.objects.all().update(last_run_atNone)         for task in PeriodicTask.objects.all()             PeriodicTasks.changed(task).. note   This will reset the state as if the periodic tasks have never run before.Models- django_celery_beat.models.PeriodicTaskThis model defines a single periodic task to be run.It must be associated with a schedule, which defines how often the task shouldrun.- django_celery_beat.models.IntervalScheduleA schedule that runs at a specific interval (e.g. every 5 seconds).- django_celery_beat.models.CrontabScheduleA schedule with fields like entries in cronminute hour day-of-week day_of_month month_of_year.- django_celery_beat.models.PeriodicTasksThis model is only used as an index to keep track of when the schedule haschanged.Whenever you update a PeriodicTask a counter in this table is alsoincremented, which tells the celery beat service to reload the schedulefrom the database.If you update periodic tasks in bulk, you will need to update the countermanually.. code-block Python         from django_celery_beat.models import PeriodicTasks         PeriodicTasks.update_changed()Example creating interval-based periodic task---------------------------------------------To create a periodic task executing at an interval you must firstcreate the interval object.. code-block Python         from django_celery_beat.models import PeriodicTask, IntervalSchedule         executes every 10 seconds.         schedule, created  IntervalSchedule.objects.get_or_create(        ...     every10,        ...     periodIntervalSchedule.SECONDS,        ... )Thats all the fields you need a period type and the frequency.You can choose between a specific set of periods- IntervalSchedule.DAYS- IntervalSchedule.HOURS- IntervalSchedule.MINUTES- IntervalSchedule.SECONDS- IntervalSchedule.MICROSECONDS.. note    If you have multiple periodic tasks executing every 10 seconds,    then they should all point to the same schedule object.Theres also a choices tuple available should you need to present thisto the user.. code-block Python         IntervalSchedule.PERIOD_CHOICESNow that we have defined the schedule object, we can create the periodic taskentry.. code-block Python         PeriodicTask.objects.create(        ...     intervalschedule,                   we created this above.        ...     nameImporting contacts,           simply describes this periodic task.        ...     taskproj.tasks.import_contacts,   name of task.        ... )Note that this is a very basic example, you can also specify the argumentsand keyword arguments used to execute the task, the queue to send itto[], and set an expiry time.Heres an example specifying the arguments, note how JSON serialization isrequired.. code-block Python         import json         from datetime import datetime, timedelta         PeriodicTask.objects.create(        ...     intervalschedule,                   we created this above.        ...     nameImporting contacts,           simply describes this periodic task.        ...     taskproj.tasks.import_contacts,   name of task.        ...     argsjson.dumps([arg1, arg2]),        ...     kwargsjson.dumps({        ...        be_careful True,        ...     }),        ...     expiresdatetime.utcnow() + timedelta(seconds30)        ... ).. [] you can also use low-level AMQP routing using the exchange and       routing_key fields.Example creating crontab-based periodic task--------------------------------------------A crontab schedule has the fields minute, hour, day_of_week,day_of_month and month_of_year, so if you want the equivalentof a 30     (execute every 30 minutes) crontab entry you specify.. code-block Python         from django_celery_beat.models import CrontabSchedule, PeriodicTask         schedule, _  CrontabSchedule.objects.get_or_create(        ...     minute30,        ...     hour,        ...     day_of_week,        ...     day_of_month,        ...     month_of_year,        ...     timezonezoneinfo.ZoneInfo(CanadaPacific)        ... )The crontab schedule is linked to a specific timezone using the timezone input parameter.Then to create a periodic task using this schedule, use the same approach asthe interval-based periodic task earlier in this document, but insteadof intervalschedule, specify crontabschedule.. code-block Python         PeriodicTask.objects.create(        ...     crontabschedule,        ...     nameImporting contacts,        ...     taskproj.tasks.import_contacts,        ... )Temporarily disable a periodic task-----------------------------------You can use the enabled flag to temporarily disable a periodic task.. code-block Python         periodic_task.enabled  False         periodic_task.save()Example running periodic tasks-----------------------------------The periodic tasks still need workers to execute them.So make sure the default Celery package is installed.(If not installed, please follow the installation instructionshere httpsgithub.comcelerycelery)Both the worker and beat services need to be running at the same time.1. Start a Celery worker service (specify your Django project name)    celery -A [project-name] worker --loglevelinfo2. As a separate process, start the beat service (specify the Django scheduler)     celery -A [project-name] beat -l info --scheduler django_celery_beat.schedulersDatabaseScheduler   OR you can use the -S (scheduler flag), for more options see celery beat --help)     celery -A [project-name] beat -l info -S django   Also, as an alternative, you can run the two steps above (worker and beat services)   with only one command (recommended for development environment only)     celery -A [project-name] worker --beat --scheduler django --loglevelinfo3. Now you can add and manage your periodic tasks from the Django Admin interface.InstallationYou can install django-celery-beat either via the Python Package Index (PyPI)or from source.To install using pip.. code-block bash         pip install -U django-celery-beatDownloading and installing from source--------------------------------------Download the latest version of django-celery-beat fromhttppypi.python.orgpypidjango-celery-beatYou can install it by doing the following .. code-block bash         tar xvfz django-celery-beat-0.0.0.tar.gz         cd django-celery-beat-0.0.0         python setup.py build         python setup.py installThe last command must be executed as a privileged user ifyou are not currently using a virtualenv.After installation, add django_celery_beat to Djangos settings module.. code-block Python        INSTALLED_APPS  [            ...,            django_celery_beat,        ]Run the django_celery_beat migrations using.. code-block bash         python manage.py migrate django_celery_beatUsing the development version-----------------------------With pipYou can install the latest snapshot of django-celery-beat using the followingpip command.. code-block bash         pip install httpsgithub.comcelerydjango-celery-beatzipballmastereggdjango-celery-beatDeveloping django-celery-beat-----------------------------To spin up a local development copy of django-celery-beat with Django admin at http127.0.0.158000admin run.. code-block bash         docker-compose up --buildLog-in as user admin with password admin.TZ Awareness-------------If you have a project that is time zone naive, you can set DJANGO_CELERY_BEAT_TZ_AWAREFalse in your settings file... build-status image httpssecure.travis-ci.orgcelerydjango-celery-beat.svgbranchmaster    alt Build status    target httpstravis-ci.orgcelerydjango-celery-beat.. coverage image httpscodecov.iogithubcelerydjango-celery-beatcoverage.svgbranchmaster    target httpscodecov.iogithubcelerydjango-celery-beatbranchmaster.. license image httpsimg.shields.iopypildjango-celery-beat.svgfoo    alt BSD License    target httpsopensource.orglicensesBSD-3-Clause.. wheel image httpsimg.shields.iopypiwheeldjango-celery-beat.svgfoo    alt django-celery-beat can be installed via wheel    target httppypi.python.orgpypidjango-celery-beat.. pyversion image httpsimg.shields.iopypipyversionsdjango-celery-beat.svgfoo    alt Supported Python versions.    target httppypi.python.orgpypidjango-celery-beat.. pyimp image httpsimg.shields.iopypiimplementationdjango-celery-beat.svgfoo    alt Support Python implementations.    target httppypi.python.orgpypidjango-celery-beatdjango-celery-beat as part of the Tidelift Subscription-------------------------------------------------------The maintainers of django-celery-beat and thousands of other packages are working with Tidelift to deliver commercial support and maintenance for the open source dependencies you use to build your applications. Save time, reduce risk, and improve code health, while paying the maintainers of the exact dependencies you use. Learn more_... _Learn more httpstidelift.comsubscriptionpkgpypi-django-celery-beatutm_sourcepypi-django-celery-beatutm_mediumreferralutm_campaignreadmeutm_termrepo</longdescription>
</pkgmetadata>