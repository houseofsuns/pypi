<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>flask-apispec.. image httpsimg.shields.iopypivflask-apispec.svg    target httpbadge.fury.iopyflask-apispec    alt Latest version.. image httpsreadthedocs.orgprojectsflask-apispecbadgeversionlatest    target httpsflask-apispec.readthedocs.ioenlatestbadgelatest    alt Documentation status.. image httpsimg.shields.iotravisjmcarpflask-apispecmaster.svg    target httpstravis-ci.orgjmcarpflask-apispec    alt Travis-CI.. image httpsimg.shields.iocodecovcgithubjmcarpflask-apispecmaster.svg    target httpscodecov.iogithubjmcarpflask-apispec    alt Code coverageflask-apispec is a lightweight tool for building REST APIs in Flask. flask-apispec uses webargs_ for request parsing, marshmallow_ for response formatting, and apispec_ to automatically generate Swagger markup. You can use flask-apispec with vanilla Flask or a fuller-featured framework like Flask-RESTful_.Install-------    pip install flask-apispecQuickstart----------.. code-block python    from flask import Flask    from flask_apispec import use_kwargs, marshal_with    from marshmallow import Schema    from webargs import fields    from .models import Pet    app  Flask(__name__)    class PetSchema(Schema)        class Meta            fields  (name, category, size)    app.route(pets)    use_kwargs({category fields.Str(), size fields.Str()})    marshal_with(PetSchema(manyTrue))    def get_pets(kwargs)        return Pet.query.filter_by(kwargs)flask-apispec works with function- and class-based views.. code-block python    from flask import make_response    from flask_apispec.views import MethodResource    class PetResource(MethodResource)        marshal_with(PetSchema)        def get(self, pet_id)            return Pet.query.filter(Pet.id  pet_id).one()        use_kwargs(PetSchema)        marshal_with(PetSchema, code201)        def post(self, kwargs)            return Pet(kwargs)        use_kwargs(PetSchema)        marshal_with(PetSchema)        def put(self, pet_id, kwargs)            pet  Pet.query.filter(Pet.id  pet_id).one()            pet.__dict__.update(kwargs)            return pet        marshal_with(None, code204)        def delete(self, pet_id)            pet  Pet.query.filter(Pet.id  pet_id).one()            pet.delete()            return make_response(, 204)flask-apispec generates Swagger markup for your view functions and classes. By default, Swagger JSON is served at swagger, and Swagger-UI at swagger-ui... code-block python    from apispec import APISpec    from apispec.ext.marshmallow import MarshmallowPlugin    from flask_apispec.extension import FlaskApiSpec    app.config.update({        APISPEC_SPEC APISpec(            titlepets,            versionv1,            plugins[MarshmallowPlugin()],        ),        APISPEC_SWAGGER_URL swagger,    })    docs  FlaskApiSpec(app)    docs.register(get_pets)    docs.register(PetResource)Documentation-------------httpsflask-apispec.readthedocs.ioNotes-----flask-apispec is strongly inspired by Flask-RESTful_ and Flask-RESTplus_, but attempts to provide similar functionality with greater flexibility and less code... _webargs httpswebargs.readthedocs.io.. _marshmallow httpsmarshmallow.readthedocs.io.. _apispec httpsapispec.readthedocs.io.. _Flask-RESTful httpsflask-restful.readthedocs.io.. _Flask-RESTplus httpsflask-restplus.readthedocs.io</longdescription>
</pkgmetadata>