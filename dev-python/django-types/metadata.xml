<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> django-types [[PyPI](httpsimg.shields.iopypivdjango-types.svg)](httpspypi.orgprojectdjango-types)Type stubs for [Django](httpswww.djangoproject.com). Note this project was forked from httpsgithub.comtypeddjangodjango-stubs with the goal of removing the [mypy](httpsgithub.compythonmypy) plugin dependency so that mypy cant [crash due to Django config](httpsgithub.comtypeddjangodjango-stubsissues318), and that non-mypy type checkers like [pyright](httpsgithub.commicrosoftpyright) will work better with Django. installbashpip install django-typesYoull need to monkey patch Djangos QuerySet, Manager (not needed for Django 3.1+) andForeignKey  (not needed for Django 4.1+) classes so we can index into them with a genericargument. Add this to your settings.pypython in settings.pyfrom django.db.models import ForeignKeyfrom django.db.models.manager import BaseManagerfrom django.db.models.query import QuerySet NOTE there are probably other items youll need to monkey patch depending on your version.for cls in [QuerySet, BaseManager, ForeignKey]    cls.__class_getitem__  classmethod(lambda cls, args, kwargs cls)   type ignore [attr-defined] usage ForeignKey ids and related names as properties in ORM modelsWhen defining a Django ORM model with a foreign key, like sopythonclass User(models.Model)    team  models.ForeignKey(        Team,        nullTrue,        on_deletemodels.SET_NULL,    )    role  models.ForeignKey(        Role,        nullTrue,        on_deletemodels.SET_NULL,        related_nameusers,    )two properties are created, team as expected, and team_id. Also, a relatedmanager called user_set is created on Team for the reverse access.In order to properly add typing to the foreign key itself and also for the created ids you can dosomething like thispythonfrom typing import TYPE_CHECKINGfrom someapp.models import Teamif TYPE_CHECKING     In this example Role cannot be imported due to circular import issues,     but doing so inside TYPE_CHECKING will make sure that the typing below     knows what Role means    from anotherapp.models import Roleclass User(models.Model)    team_id Optional[int]    team  models.ForeignKey(        Team,        nullTrue,        on_deletemodels.SET_NULL,    )    role_id int    role  models.ForeignKey[Role](        Role,        nullFalse,        on_deletemodels.SET_NULL,        related_nameusers,    )reveal_type(User().team) note Revealed type is Optional[Team]reveal_type(User().role) note Revealed type is RoleThis will make sure that team_id and role_id can be accessed. Also, team and rolewill be typed to their right objects.To be able to access the related manager Team and Role you could dopythonfrom typing import TYPE_CHECKINGif TYPE_CHECKING     This doesnt really exists on django so it always need to be imported this way    from django.db.models.manager import RelatedManager    from user.models import Userclass Team(models.Model)    if TYPE_CHECKING        user_set  RelatedManager[User]()class Role(models.Model)    if TYPE_CHECKING        users  RelatedManager[User]()reveal_type(Team().user_set) note Revealed type is RelatedManager[User]reveal_type(Role().users) note Revealed type is RelatedManager[User]An alternative is using annotationspythonfrom __future__ import annotations   or just be in python 3.11from typing import TYPE_CHECKINGif TYPE_CHECKING    from django.db.models import Manager    from user.models import Userclass Team(models.Model)    user_set Manager[User]class Role(models.Model)    users Manager[User]reveal_type(Team().user_set) note Revealed type is Manager[User]reveal_type(Role().users) note Revealed type is Manager[User] id FieldBy default Django will create an AutoField for you if one doesnt exist.For type checkers to know about the id field youll need to declare thefield explicitly.python beforeclass Post(models.Model)    ... afterclass Post(models.Model)    id  models.AutoField(primary_keyTrue)     OR    id int HttpRequests user propertyThe HttpRequests user property has a type of Union[AbstractBaseUser, AnonymousUser],but for most of your views youll probably want either an authed user or anAnonymousUser.So we can define a subclass for each casepythonclass AuthedHttpRequest(HttpRequest)    user User   type ignore [assignment]And then you can use it in your viewspythonauth.login_requireddef activity(request AuthedHttpRequest, team_id str) - HttpResponse    ...You can also get more strict with your login_required decorator so that thefirst argument of the function it is decorating is AuthedHttpRequestpythonfrom typing import Any, Union, TypeVar, castfrom django.http import HttpRequest, HttpResponsefrom typing_extensions import Protocolfrom functools import wrapsclass RequestHandler1(Protocol)    def __call__(self, request AuthedHttpRequest) - HttpResponse        ...class RequestHandler2(Protocol)    def __call__(self, request AuthedHttpRequest, __arg1 Any) - HttpResponse        ...RequestHandler  Union[RequestHandler1, RequestHandler2] Verbose bound arg due to limitations of Python typing. see httpsgithub.compythonmypyissues5876_F  TypeVar(_F, boundRequestHandler)def login_required(view_func _F) - _F    wraps(view_func)    def wrapped_view(        request AuthedHttpRequest, args object, kwargs object    ) - HttpResponse        if request.user.is_authenticated            return view_func(request, args, kwargs)   type ignore [call-arg]        raise AuthenticationRequired    return cast(_F, wrapped_view)Then the following will type errorpythonauth.login_requireddef activity(request HttpRequest, team_id str) - HttpResponse    ... related- httpsgithub.comsbdchddjangorestframework-types- httpsgithub.comsbdchdcelery-types- httpsgithub.comsbdchdmongo-types- httpsgithub.comsbdchdmsgpack-types</longdescription>
</pkgmetadata>