<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>[[tests](httpsgithub.comsondrelgasgi-idempotency-headeractionsworkflowstest.ymlbadge.svg)](httpsgithub.comsondrelgasgi-idempotency-headeractionsworkflowstest.yml)[[pypi](httpsimg.shields.iopypivasgi-idempotency-header.svg)](httpspypi.orgprojectdrf-openapi-tester)[[python-versions](httpsimg.shields.iobadgepython-3.82B-blue)](httpspypi.orgprojectasgi-idempotency-header)[[codecov](httpscodecov.ioghsondrelgasgi-idempotency-headerbranchmaingraphbadge.svgtokenUOJTCSY8H7)](httpscodecov.ioghsondrelgasgi-idempotency-header) Idempotency Header ASGI MiddlewareA middleware for making endpoints idempotent.The purpose of the middleware is to guarantee that execution of mutating endpoints happens exactly once,regardless of the number of requests.We achieve this by caching responses, and returning already-saved responses to the user on repeated requests.Responses are only cached when an idempotency-key HTTP header is present, so clients must opt-into this behaviour.Additionally, only configured HTTP methods (by default, POST and PATCH) that return JSON payloads are cached and replayed.This is largely modelled after [stripe implementation](httpsstripe.comdocsapiidempotent_requests).The middleware is compatible with both [Starlette](httpsgithub.comencodestarlette)and [FastAPI](httpsgithub.comtiangolofastapi) apps. Installationpip install asgi-idempotency-header SetupAdd the middleware to your app like thispythonfrom fastapi import FastAPIfrom idempotency_header_middleware import IdempotencyHeaderMiddlewarefrom idempotency_header_middleware.backends import RedisBackendbackend  RedisBackend(redisredis)app  FastAPI()app.add_middleware(IdempotencyHeaderMiddleware(backendbackend))or like thispythonfrom fastapi import FastAPIfrom fastapi.middleware import Middlewarefrom idempotency_header_middleware import IdempotencyHeaderMiddlewarefrom idempotency_header_middleware.backends import RedisBackendbackend  RedisBackend(redisredis)app  FastAPI(    middleware[        Middleware(            IdempotencyHeaderMiddleware,            backendbackend,        )    ])If youre using Starlette, just substitute FastAPI for Starlette and it should work the same. ConfigurationThe middleware takes a few arguments. A full example looks like thispythonfrom aioredis import from_urlfrom idempotency_header_middleware import IdempotencyHeaderMiddlewarefrom idempotency_header_middleware.backends import RedisBackendredis  from_url(redis_url)backend  RedisBackend(redisredis)IdempotencyHeaderMiddleware(    backend,    idempotency_header_keyIdempotency-Key,    replay_header_keyIdempotent-Replayed,    enforce_uuid4_formattingFalse,    expiry60  60  24,    applicable_methods[POST, PATCH])The following section describes each argument Backendpythonfrom idempotency_header_middleware.backends import RedisBackend, MemoryBackendbackend Union[RedisBackend, MemoryBackend]The backend is the only required argument, as it defines how and where to store a response.The package comes with an [aioredis](httpsgithub.comaio-libsaioredis-py) backend implementation, and amemory-backend for testing.Contributions for more backends are welcomed, and configuring a custom backend is pretty simple - just take a look atthe existing ones. Idempotency header keypythonidempotency_header_key str  Idempotency-KeyThe idempotency header key is the header value to check for. When present, the middleware will be used if the HTTPmethod is in the [applicable methods](applicable-methods).The default value is Idempotency-Key, but it can be defined as any string. Replay header keypythonreplay_header_key str  Idempotent-ReplayedThe replay header is added to replayed responses. It provides a way for the client to tell whether the action wasperformed for the first time or not. Enforce UUID formattingpythonenforce_uuid4_formatting bool  FalseConvenience option for stricter header value validation.Clients can technically set any value they want in their header,but the shorter the key value is, the higher the risk of value-collisions is from other users.If two users accidentally send in the same header value for whats meant to be two separate requests,the middleware will interpret them as the same.By enabling this option, you can force users to use UUIDs as header values, and pretty much eliminate this risk.When validation fails, a 422 response is returned from the middleware, informing the user that the header value is malformed. Expirypythonexpiry int  60  60  24How long to cache responses for, measured in seconds. Set to 24 hours by default. Applicable Methodspythonapplicable_methods[POST, PATCH]What HTTP methods to consider for idempotency. If the request method is one of the methods in this list, and the[idempotency header](idempotency-header-key) is sent, the middleware will be used. By default, only POSTand PATCH methods are cached and replayed. Quick summary of behavioursBriefly summarized, this is how the middleware functions- The first request is processed, and consequent requests are replayed, until the response expires.  expiry can be set to None to skip expiry, but most likely you will want to expire responses  after a while.- If two requests comes in at the same time - i.e., if a second request hits the middlware before  the first request has finished, the middleware will return a 409, informing the user that a request  is being processed, and that we cannot handle the second request.- The middleware only handles HTTP requests.- By default, the middleware only handles requests with POST and PATCH methods. Other HTTP methods skip this middleware.- Only valid JSON responses with content-type  applicationjson are cached.</longdescription>
</pkgmetadata>