<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image:: https://ci.appveyor.com/api/projects/status/gidnb9ulj3rje5s2?svg=true    :target: https://ci.appveyor.com/project/plusvic/yara-pythonyara-python===========With this library you can use `YARA &lt;https://github.com/VirusTotal/yara&gt;`_ fromyour Python programs. It covers all YARA's features, from compiling, savingand loading rules to scanning files, strings and processes.Here it goes a little example:.. code-block:: python    &gt;&gt;&gt; import yara    &gt;&gt;&gt; rule = yara.compile(source='rule foo: bar {strings: $a = &quot;lmn&quot; condition: $a}')    &gt;&gt;&gt; matches = rule.match(data='abcdefgjiklmnoprstuvwxyz')    &gt;&gt;&gt; print(matches)    [foo]    &gt;&gt;&gt; print(matches[0].rule)    foo    &gt;&gt;&gt; print(matches[0].tags)    ['bar']    &gt;&gt;&gt; print(matches[0].strings)    [$a]    &gt;&gt;&gt; print(matches[0].strings[0].identifier)    $a    &gt;&gt;&gt; print(matches[0].strings[0].instances)    [lmn]    &gt;&gt;&gt; print(matches[0].strings[0].instances[0].offset)    10    &gt;&gt;&gt; print(matches[0].strings[0].instances[0].matched_length)    3Installation------------The easiest way of installing YARA is by using ``pip``:.. code-block:: bash  $ pip install yara-pythonBut you can also get the source from GitHub and compile it yourself:.. code-block:: bash  $ git clone --recursive https://github.com/VirusTotal/yara-python  $ cd yara-python  $ python setup.py build  $ sudo python setup.py installNotice the ``--recursive`` option used with ``git``. This is important becausewe need to download the ``yara`` subproject containing the source code for``libyara`` (the core YARA library). It's also important to note that the twomethods above link ``libyara`` statically into yara-python. If you want to linkdynamically against a shared ``libyara`` library use:.. code-block:: bash  $ python setup.py build --dynamic-linkingFor this option to work you must build and install`YARA &lt;https://github.com/VirusTotal/yara&gt;`_ separately before installing``yara-python``.Documentation-------------Find more information about how to use yara-python athttps://yara.readthedocs.org/en/latest/yarapython.html.</longdescription>
</pkgmetadata>