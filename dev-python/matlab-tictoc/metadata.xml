<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># MATLAB Tic TocA Python module to make timing in python easier by mimicing MATLAB's `tic` and `toc` functions.`tic` starts a timer by recording the current time, and `toc` uses the start time to find the elapsed time.The included `func_timer` decorator uses `tic` and `toc` to give the elapsed time of the wrapped function.## Usage```from tictoc import tic, toctic()              # starts the global timertimer = tic()      # assigns an instance of the Timer class to the timer var without affecting the global timertoc()              # Prints elapsed time from tic() calltoc(timer)         # Prints elapsed time for the timer giventime = toc(timer)  # Assigns elapsed time to a variabletimer.reset()      # Resets the timer var start timetic()              # Restarts the global timer```## Examples```from time import sleepfrom tictoc import tic, toc, func_timertic()  # starts global timersleep(1)toc()sleep(2)toc()```&gt;Elapsed time is 1.00547 seconds.&gt;Elapsed time is 3.00886 seconds.```timer = tic()sleep(1)print(f'Timer Assigned to var: {toc(timer)}')print(f'Global Timer: {toc()}')```&gt;Timer Assigned to var: 1.0057699&gt;Global Timer: 4.013537```@func_timerdef func():    sleep(2)func()```&gt;Elapsed time for function func is 2.00556969 seconds.</longdescription>
</pkgmetadata>