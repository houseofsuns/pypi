<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> janus.. image httpsgithub.comaio-libsjanusactionsworkflowsci.ymlbadge.svg    target httpsgithub.comaio-libsjanusactionsworkflowsci.yml.. image httpscodecov.ioghaio-libsjanusbranchmastergraphbadge.svg    target httpscodecov.ioghaio-libsjanus.. image httpsimg.shields.iopypivjanus.svg    target httpspypi.python.orgpypijanus.. image httpsbadges.gitter.imJoin20Chat.svg    target httpsgitter.imaio-libsLobby    alt Chat on GitterMixed sync-async queue, supposed to be used for communicating betweenclassic synchronous (threaded) code and asynchronous (in terms ofasyncio_) one.Like Janus god httpsen.wikipedia.orgwikiJanus_ the queueobject from the library has two faces synchronous and asynchronousinterface.Synchronous is fully compatible with standard queuehttpsdocs.python.org3libraryqueue.html_, asynchronous onefollows asyncio queue designhttpsdocs.python.org3libraryasyncio-queue.html_.Usage example (Python 3.7+).. code python    import asyncio    import janus    def threaded(sync_q janus.SyncQueue[int]) - None        for i in range(100)            sync_q.put(i)        sync_q.join()    async def async_coro(async_q janus.AsyncQueue[int]) - None        for i in range(100)            val  await async_q.get()            assert val  i            async_q.task_done()    async def main() - None        queue janus.Queue[int]  janus.Queue()        loop  asyncio.get_running_loop()        fut  loop.run_in_executor(None, threaded, queue.sync_q)        await async_coro(queue.async_q)        await fut        queue.close()        await queue.wait_closed()    asyncio.run(main())Usage example (Python 3.5 and 3.6).. code python    import asyncio    import janus    loop  asyncio.get_event_loop()    def threaded(sync_q)        for i in range(100)            sync_q.put(i)        sync_q.join()    async def async_coro(async_q)        for i in range(100)            val  await async_q.get()            assert val  i            async_q.task_done()    async def main()        queue  janus.Queue()        fut  loop.run_in_executor(None, threaded, queue.sync_q)        await async_coro(queue.async_q)        await fut        queue.close()        await queue.wait_closed()    try        loop.run_until_complete(main())    finally        loop.close()Communication channelsGitHub Discussions httpsgithub.comaio-libsjanusdiscussionsFeel free to post your questions and ideas here.gitter chat httpsgitter.imaio-libsLobbyLicensejanus library is offered under Apache 2 license.ThanksThe library development is sponsored by DataRobot (httpsdatarobot.com).. _asyncio httpsdocs.python.org3libraryasyncio.html</longdescription>
</pkgmetadata>