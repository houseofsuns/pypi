<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Kafka Python client------------------------.. image:: https://img.shields.io/badge/kafka-0.11%2C%200.10%2C%200.9%2C%200.8-brightgreen.svg    :target: https://kafka-python.readthedocs.io/compatibility.html.. image:: https://img.shields.io/pypi/pyversions/kafka.svg    :target: https://pypi.python.org/pypi/kafka.. image:: https://coveralls.io/repos/dpkp/kafka-python/badge.svg?branch=master&amp;service=github    :target: https://coveralls.io/github/dpkp/kafka-python?branch=master.. image:: https://travis-ci.org/dpkp/kafka-python.svg?branch=master    :target: https://travis-ci.org/dpkp/kafka-python.. image:: https://img.shields.io/badge/license-Apache%202-blue.svg    :target: https://github.com/dpkp/kafka-python/blob/master/LICENSEPython client for the Apache Kafka distributed stream processing system.kafka-python is designed to function much like the official java client, with asprinkling of pythonic interfaces (e.g., consumer iterators).kafka-python is best used with newer brokers (0.9+), but is backwards-compatible witholder versions (to 0.8.0). Some features will only be enabled on newer brokers.For example, fully coordinated consumer groups -- i.e., dynamic partitionassignment to multiple consumers in the same group -- requires use of 0.9+ kafkabrokers. Supporting this feature for earlier broker releases would requirewriting and maintaining custom leadership election and membership / healthcheck code (perhaps using zookeeper or consul). For older brokers, you canachieve something similar by manually assigning different partitions to eachconsumer instance with config management tools like chef, ansible, etc. Thisapproach will work fine, though it does not support rebalancing on failures.See &lt;https://kafka-python.readthedocs.io/en/master/compatibility.html&gt;for more details.Please note that the master branch may contain unreleased features. For releasedocumentation, please see readthedocs and/or python's inline help.&gt;&gt;&gt; pip install kafkaKafkaConsumer*************KafkaConsumer is a high-level message consumer, intended to operate as similarlyas possible to the official java client. Full support for coordinatedconsumer groups requires use of kafka brokers that support the Group APIs: kafka v0.9+.See &lt;https://kafka-python.readthedocs.io/en/master/apidoc/KafkaConsumer.html&gt;for API and configuration details.The consumer iterator returns ConsumerRecords, which are simple namedtuplesthat expose basic message attributes: topic, partition, offset, key, and value:&gt;&gt;&gt; from kafka import KafkaConsumer&gt;&gt;&gt; consumer = KafkaConsumer('my_favorite_topic')&gt;&gt;&gt; for msg in consumer:...     print (msg)&gt;&gt;&gt; # join a consumer group for dynamic partition assignment and offset commits&gt;&gt;&gt; from kafka import KafkaConsumer&gt;&gt;&gt; consumer = KafkaConsumer('my_favorite_topic', group_id='my_favorite_group')&gt;&gt;&gt; for msg in consumer:...     print (msg)&gt;&gt;&gt; # manually assign the partition list for the consumer&gt;&gt;&gt; from kafka import TopicPartition&gt;&gt;&gt; consumer = KafkaConsumer(bootstrap_servers='localhost:1234')&gt;&gt;&gt; consumer.assign([TopicPartition('foobar', 2)])&gt;&gt;&gt; msg = next(consumer)&gt;&gt;&gt; # Deserialize msgpack-encoded values&gt;&gt;&gt; consumer = KafkaConsumer(value_deserializer=msgpack.loads)&gt;&gt;&gt; consumer.subscribe(['msgpackfoo'])&gt;&gt;&gt; for msg in consumer:...     assert isinstance(msg.value, dict)KafkaProducer*************KafkaProducer is a high-level, asynchronous message producer. The class isintended to operate as similarly as possible to the official java client.See &lt;https://kafka-python.readthedocs.io/en/master/apidoc/KafkaProducer.html&gt;for more details.&gt;&gt;&gt; from kafka import KafkaProducer&gt;&gt;&gt; producer = KafkaProducer(bootstrap_servers='localhost:1234')&gt;&gt;&gt; for _ in range(100):...     producer.send('foobar', b'some_message_bytes')&gt;&gt;&gt; # Block until a single message is sent (or timeout)&gt;&gt;&gt; future = producer.send('foobar', b'another_message')&gt;&gt;&gt; result = future.get(timeout=60)&gt;&gt;&gt; # Block until all pending messages are at least put on the network&gt;&gt;&gt; # NOTE: This does not guarantee delivery or success! It is really&gt;&gt;&gt; # only useful if you configure internal batching using linger_ms&gt;&gt;&gt; producer.flush()&gt;&gt;&gt; # Use a key for hashed-partitioning&gt;&gt;&gt; producer.send('foobar', key=b'foo', value=b'bar')&gt;&gt;&gt; # Serialize json messages&gt;&gt;&gt; import json&gt;&gt;&gt; producer = KafkaProducer(value_serializer=lambda v: json.dumps(v).encode('utf-8'))&gt;&gt;&gt; producer.send('fizzbuzz', {'foo': 'bar'})&gt;&gt;&gt; # Serialize string keys&gt;&gt;&gt; producer = KafkaProducer(key_serializer=str.encode)&gt;&gt;&gt; producer.send('flipflap', key='ping', value=b'1234')&gt;&gt;&gt; # Compress messages&gt;&gt;&gt; producer = KafkaProducer(compression_type='gzip')&gt;&gt;&gt; for i in range(1000):...     producer.send('foobar', b'msg %d' % i)Thread safety*************The KafkaProducer can be used across threads without issue, unlike theKafkaConsumer which cannot.While it is possible to use the KafkaConsumer in a thread-local manner,multiprocessing is recommended.Compression***********kafka-python supports gzip compression/decompression natively. To produce or consume lz4 compressed messages, you should install python-lz4 (pip install lz4). To enable snappy compression/decompression install python-snappy (also requires snappy library).See &lt;https://kafka-python.readthedocs.io/en/master/install.html#optional-snappy-install&gt;for more information.Protocol********A secondary goal of kafka-python is to provide an easy-to-use protocol layerfor interacting with kafka brokers via the python repl. This is useful fortesting, probing, and general experimentation. The protocol support isleveraged to enable a KafkaClient.check_version() method thatprobes a kafka broker and attempts to identify which version it is running(0.8.0 to 0.11).Low-level*********Legacy support is maintained for low-level consumer and producer classes,SimpleConsumer and SimpleProducer. See&lt;https://kafka-python.readthedocs.io/en/master/simple.html?highlight=SimpleProducer&gt; for API details.</longdescription>
</pkgmetadata>