<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Healthcheck-----------.. image httpsgithub.comateliedocodigopy-healthcheckrawdeveloppy-healthcheck.jpg    target httpspypi.python.orgpypipy-healthcheck.. image httpsbadge.fury.iopypy-healthcheck.svg    target httpsbadge.fury.iopypy-healthcheck.. image httpsrequires.iogithubateliedocodigopy-healthcheckrequirements.svgbranchdevelop     target httpsrequires.iogithubateliedocodigopy-healthcheckrequirementsbranchdevelop     alt Requirements Status.. image httpstravis-ci.orgateliedocodigopy-healthcheck.svgbranchdevelop    target httpstravis-ci.orgateliedocodigopy-healthcheckHealthcheck is a library to write simple healthcheck functions that canbe used to monitor your application. It is possible to use in a Flaskapp or Tornado app. Its useful for asserting that your dependenciesare up and running and your application can respond to HTTP requests.The Healthcheck functions can be exposed via a user defined Flaskroute so you can use an external monitoring application (monit,nagios, Runscope, etc.) to check the status and uptime of yourapplication.New in version 1.1 Healthcheck also gives you a simple Flask route toview information about your applications environment. By default, thisincludes data about the operating system, the Python environment, thecurrent process, and the application config. You can customize whichsections are included, or add your own sections to the output.Installing----------    pip install py-healthcheckUsage-----Heres an example of basic usage with Flask.. code python    from flask import Flask    from healthcheck import HealthCheck, EnvironmentDump    app  Flask(__name__)    health  HealthCheck()    envdump  EnvironmentDump()     add your own check function to the healthcheck    def redis_available()        client  _redis_client()        info  client.info()        return True, redis ok    health.add_check(redis_available)     add your own data to the environment dump    def application_data()        return {maintainer Luis Fernando Gomes,                git_repo httpsgithub.comateliedocodigopy-healthcheck}    envdump.add_section(application, application_data)     Add a flask route to expose information    app.add_url_rule(healthcheck, healthcheck, view_funclambda health.run())    app.add_url_rule(environment, environment, view_funclambda envdump.run())To use with Tornado you can import the TornadoHandler.. code python    import tornado.web    from healthcheck import TornadoHandler, HealthCheck, EnvironmentDump    app  tornado.web.Application()    health  HealthCheck()    envdump  EnvironmentDump()     add your own check function to the healthcheck    def redis_available()        client  _redis_client()        info  client.info()        return True, redis ok    health.add_check(redis_available)     add your own data to the environment dump or healthcheck    def application_data()        return {maintainer Luis Fernando Gomes,                git_repo httpsgithub.comateliedocodigopy-healthcheck}     ou choose where you want to output this information    health.add_section(application, application_data)    health.add_section(version, __version__)    envdump.add_section(application, application_data)     Add a tornado handler to expose information    app.add_handlers(        r.,        [            (                healthcheck,                TornadoHandler, dict(checkerhealth)            ),            (                environment,                TornadoHandler, dict(checkerenvdump)            ),        ]    )Alternatively you can set all together.. code python    import tornado.web    from healthcheck import TornadoHandler, HealthCheck, EnvironmentDump     add your own check function to the healthcheck    def redis_available()        client  _redis_client()        info  client.info()        return True, redis ok    health  HealthCheck(checkers[redis_available])     add your own data to the environment dump    def application_data()        return {maintainer Luis Fernando Gomes,                git_repo httpsgithub.comateliedocodigopy-healthcheck}    envdump  EnvironmentDump(applicationapplication_data)    app  tornado.web.Application([        (healthcheck, TornadoHandler, dict(checkerhealth)),        (environment, TornadoHandler, dict(checkerenvdump)),    ])To run all of your check functions, make a request to the healthcheckURL you specified, like this    curl httplocalhost5000healthcheckAnd to view the environment data, make a check to the URL you specifiedfor EnvironmentDump    curl httplocalhost5000environmentThe HealthCheck class---------------------Check FunctionsCheck functions take no arguments and should return a tuple of (bool,str). The boolean is whether or not the check passed. The message is anystring or output that should be rendered for this check. Useful forerror messagesdebugging... code python     add check functions    def addition_works()        if 1 + 1  2            return True, addition works        else            return False, the universe is brokenAny exceptions that get thrown by your code will be caught and handledas errors in the healthcheck.. code python     add check functions    def throws_exception()        bad_var  None        bad_var[explode]Will output.. code json    {        status failure,        results [            {                output NoneType object has no attribute __getitem__,                checker throws_exception,                passed false            }        ]    }Note, all checkers will get run and all failures will be reported. Itsintended that they are all separate checks and if any one fails thehealthcheck overall is failed.CachingIn Runscopes infrastructure, the healthcheck endpoint is hitsurprisingly often. haproxy runs on every server, and each haproxy hitsevery healthcheck twice a minute. (So if we have 30 servers in ourinfrastructure, thats 60 healthchecks per minute to every Flaskservice.) Plus, monit hits every healthcheck 6 times a minute.To avoid putting too much strain on backend services, health checkresults can be cached in process memory. By default, health checks thatsucceed are cached for 27 seconds, and failures are cached for 9seconds. These can be overridden with the success_ttl andfailed_ttl parameters. If you dont want to use the cache at all,initialize the Healthcheck object withsuccess_ttlNone, failed_ttlNone.CustomizingYou can customize the status codes, headers, and output format forsuccess and failure responses.The EnvironmentDump class-------------------------Built-in data sectionsBy default, EnvironmentDump data includes these 4 sections-  os information about your operating system.-  python information about your Python executable, Python path,   and installed packages.-  process information about the currently running Python process,   including the PID, command line arguments, and all environment   variables.Some of the data is scrubbed to avoid accidentally exposing passwords oraccess keystokens. Config keys and environment variable names arescanned for key, token, or pass. If those strings arepresent in the name of the variable, the value is not included.Disabling built-in data sectionsFor security reasons, you may want to disable an entire section. You candisable sections when you instantiate the EnvironmentDump object,like this.. code python    envdump  EnvironmentDump(include_pythonFalse,                               include_osFalse,                              include_processFalse)Adding custom data sectionsYou can add a new section to the output by registering a function ofyour own. Heres an example of how this would be used.. code python    def application_data()        return {maintainer Luis Fernando Gomes,                git_repo httpsgithub.comateliedocodigopy-healthcheck                config app.config}    envdump  EnvironmentDump()    envdump.add_section(application, application_data)Credits-------This project was forked from Runscopehealthcheck httpsgithub.comRunscopehealthcheck_. since 1.3.1</longdescription>
</pkgmetadata>