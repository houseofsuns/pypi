<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># tc-fetchFetch artifacts from taskcluster.This is a small utility application designed to download and fetchlogs from [TaskCluster](https://taskcluster.net/)-based CI systems.In particular it's built for use with [Mozilla'sCI](https://treeherder.mozilla.org) and[web-platform-tests](https://github.com/web-platform/tests) CI.## Command line usage```tcfetch [--out-dir &lt;path&gt;] [--artifact-name &lt;name&gt;] [--filter-jobs &lt;expression&gt;]* &lt;repo&gt; &lt;commit&gt;```By default tcfetch is configured to fetch web-platform-tests resultsin wptreport format.Valid `repo` names are:* `mozilla-central`, `mozilla-beta`, `autoland`, `try` - Mozilla  repositories hosted on [hg.mozilla.org](https://hg.mozilla.org).* `wpt` - The [web-platform-tests](https://github.com/web-platform/tests) repository.`commit` must be the hash of a commit in the correspondingrepository. For Mozilla repositories the minimum commit prefix is 12characters. For web-platform-tests, anything non-ambiguous shouldwork.`--out-dir` - The path to put the downloaded artifact files.`--artifact-name` - The name of the artifact to download (currentlyimplemented as a suffix match on the full path).`--filter-jobs` - A filter string used to select the task names toinclude. This is a string that's interpreted as a regex. If the stringstarts with `!`, any matching jobs are excluded. If the string startswith `^` (after removing any `!`), it's used as a regexp against thefull task name, otherwise it's used as a substring match.For example to fetch all Firefox logs from web-platform-tests commit0f123ad and put them in a directory called `logs`:```tcfetch --out-dir logs --filter-jobs '-firefox-' wpt 0f123ad```</longdescription>
</pkgmetadata>