<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>requests KerberosGSSAPI authentication library.. image httpsgithub.comrequestsrequests-kerberosactionsworkflowsci.ymlbadge.svg    target httpsgithub.comrequestsrequests-kerberosactionsworkflowsci.ymlRequests is an HTTP library, written in Python, for human beings. This libraryadds optional KerberosGSSAPI authentication support and supports mutualauthentication. Basic GET usage.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth     r  requests.get(httpexample.org, authHTTPKerberosAuth())    ...The entire requests.api should be supported.Setup-----In order to use this library, there must already be a Kerberos Ticket-GrantingTicket(TGT) cached in a Kerberos credential cache. Whether a TGT is availablecan be easily determined by running the klist command. If no TGT isavailable, then it first must be obtained by running the kinit command, orpointing the KRB5CCNAME to a credential cache with a valid TGT.In short, the library will handle the negotiations of Kerberos authentication,but ensuring that an initial TGT is available and valid is the responsibilityof the user.Authentication Failures-----------------------Client authentication failures will be communicated to the caller by returningthe 401 response. A 401 response may also come from an expired Ticket-GrantingTicket.Mutual Authentication---------------------REQUIREDBy default, HTTPKerberosAuth will require mutual authentication from theserver, and if a server emits a non-error response which cannot beauthenticated, a requests_kerberos.errors.MutualAuthenticationError willbe raised. If a server emits an error which cannot be authenticated, it willbe returned to the user but with its contents and headers stripped. If theresponse content is more important than the need for mutual auth on errors,(eg, for certain WinRM calls) the stripping behavior can be suppressed bysetting sanitize_mutual_error_responseFalse.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth, REQUIRED     kerberos_auth  HTTPKerberosAuth(mutual_authenticationREQUIRED, sanitize_mutual_error_responseFalse)     r  requests.get(httpswindows.example.orgwsman, authkerberos_auth)    ...OPTIONALIf youd prefer to not require mutual authentication, you can set yourpreference when constructing your HTTPKerberosAuth object.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth, OPTIONAL     kerberos_auth  HTTPKerberosAuth(mutual_authenticationOPTIONAL)     r  requests.get(httpexample.org, authkerberos_auth)    ...This will cause requests_kerberos to attempt mutual authentication if theserver advertises that it supports it, and cause a failure if authenticationfails, but not if the server does not support it at all.DISABLEDWhile we dont recommend it, if youd prefer to never attempt mutualauthentication, you can do that as well.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth, DISABLED     kerberos_auth  HTTPKerberosAuth(mutual_authenticationDISABLED)     r  requests.get(httpexample.org, authkerberos_auth)    ...Preemptive Authentication-------------------------HTTPKerberosAuth can be forced to preemptively initiate the KerberosGSS exchange and present a Kerberos ticket on the initial request (and allsubsequent). By default, authentication only occurs after a401 Unauthorized response containing a Kerberos or Negotiate challengeis received from the origin server. This can cause mutual authenticationfailures for hosts that use a persistent connection (eg, WindowsWinRM), asno Kerberos challenges are sent after the initial auth handshake. Thisbehavior can be altered by setting  force_preemptiveTrue.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth, REQUIRED     kerberos_auth  HTTPKerberosAuth(mutual_authenticationREQUIRED, force_preemptiveTrue)     r  requests.get(httpswindows.example.orgwsman, authkerberos_auth)    ...Hostname Override-----------------If communicating with a host whose DNS name doesnt match itskerberos hostname (eg, behind a content switch or load balancer),the hostname used for the Kerberos GSS exchange can be overridden bysetting the hostname_override arg.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth, REQUIRED     kerberos_auth  HTTPKerberosAuth(hostname_overrideinternalhost.local)     r  requests.get(httpsexternalhost.example.org, authkerberos_auth)    ...Explicit Principal------------------HTTPKerberosAuth normally uses the default principal (ie, the user forwhom you last ran kinit or kswitch, or an SSO credential ifapplicable). However, an explicit principal can be specified, which willcause Kerberos to look for a matching credential cache for the named user.This feature depends on OS support for collection-type credential caches,as well as working principal support in PyKerberos (it is broken in manybuilds). An explicit principal can be specified with the principal arg.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth, REQUIRED     kerberos_auth  HTTPKerberosAuth(principaluserREALM)     r  requests.get(httpexample.org, authkerberos_auth)    ...On Windows, WinKerberos is used instead of PyKerberos. WinKerberos allows theuse of arbitrary principals instead of a credential cache. Passwords can bespecified by following the form userrealmpassword for principal.Delegation----------requests_kerberos supports credential delegation (GSS_C_DELEG_FLAG).To enable delegation of credentials to a server that requests delegation, passdelegateTrue to HTTPKerberosAuth.. code-block python     import requests     from requests_kerberos import HTTPKerberosAuth     r  requests.get(httpexample.org, authHTTPKerberosAuth(delegateTrue))    ...Be careful to only allow delegation to servers you trust as they will be ableto impersonate you using the delegated credentials.Logging-------This library makes extensive use of Pythons logging facilities.Log messages are logged to the requests_kerberos andrequests_kerberos.kerberos_ named loggers.If you are having difficulty we suggest you configure logging. Issues with theunderlying kerberos libraries will be made apparent. Additionally, copious debuginformation is made available which may assist in troubleshooting if youincrease your log level all the way up to debug.History0.14.0 2021-12-05------------------- Added support for proxy authentication with HTTP endpoints.- Support for proxying HTTPS endpoints is not available due to limitations  of the underlying requestsurllib3 library.- Fixed up stray bytes to str conversion.0.13.0 2021-11-03------------------- Change Kerberos dependencies to pyspnego_ to modernise the underlying  Kerberos library that is used.- Removed the wrap_winrm and unwrap_winrm functions- Dropped support for Python 2 and raised minimum Python version to 3.6.- Renamed the context attribute to _context to indicate its meant for  internal use only.- Fix Negotiate header regex pattern to avoid DoS affected patterns.. _pyspnego httpsgithub.comjborean93pyspnego0.12.0 2017-12-20------------------------- Add support for channel binding tokens (assumes pykerberos support  1.2.1)- Add support for kerberos message encryption (assumes pykerberos support  1.2.1)- Misc CItest fixes0.11.0 2016-11-02------------------- Switch dependency on Windows from kerberos-sspipywin32 to WinKerberos.  This brings Custom Principal support to Windows users.0.10.0 2016-05-18------------------- Make it possible to receive errors without having their contents and headers  stripped.- Resolve a bug caused by passing the principal keyword argument to  kerberos-sspi on Windows.0.9.0 2016-05-06------------------ Support for principal, hostname, and realm override.- Added support for mutual auth.0.8.0 2016-01-07------------------ Support for Kerberos delegation.- Fixed problems declaring kerberos-sspi on Windows installs.0.7.0 2015-05-04------------------ Added Windows native authentication support by adding kerberos-sspi as an  alternative backend.- Prevent infinite recursion when a server returns 401 to an authorization  attempt.- Reduce the logging during successful responses.0.6.1 2014-11-14------------------ Fix HTTPKerberosAuth not to treat non-file as a file- Prevent infinite recursion when GSSErrors occurs0.6 2014-11-04---------------- Handle mutual authentication (see pull request 36_)  All users should upgrade immediately. This has been reported to  oss-security_ and we are awaiting a proper CVE identifier.  Update We were issued CVE-2014-8650- Distribute as a wheel... _36 httpsgithub.comrequestsrequests-kerberospull36.. _oss-security httpwww.openwall.comlistsoss-security0.5 2014-05-14---------------- Allow non-HTTP service principals with HTTPKerberosAuth using a new optional  argument service.- Fix bug in setup.py on distributions where the compiler module is  not available.- Add test dependencies to setup.py so python setup.py test will work.0.4 2013-10-26---------------- Minor updates in the README- Change requirements to depend on requests above 1.1.00.3 2013-06-02---------------- Work with servers operating on non-standard ports0.2 2013-03-26---------------- Not documented0.1 Never released-------------------- Initial Release</longdescription>
</pkgmetadata>