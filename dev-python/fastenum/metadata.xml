<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>fastenum========A roughly 3x faster drop-in replacement of Python's ``enum``.Why----Python's `enum &lt;https://docs.python.org/3/library/enum.html&gt;`_ type is a useful building block for creatingsemantic types and constants in your programs.The problem is, if you're doing millions and millions of enum lookups (even simple expressions like``Color.BLUE``) it's a little bit slow.``fastenum`` is a stripped-down enum implementation that's up to 3.5x faster than the standard libraryimplementation. At `Quantlane &lt;https://quantlane.com&gt;`_ we use it in production code as a drop-inreplacement. Read more, including benchmarks, on our blog:`A 3x faster enum type for Python &lt;https://quantlane.com/blog/fastenum/&gt;`_.Installation-------------``fastenum`` is `available on PyPI &lt;https://pypi.org/project/fastenum/&gt;`_ and you can install it with:::pip install fastenumor::poetry add fastenumHow to use it--------------Simply use ``fastenum.Enum`` instead of ``enum.Enum``:.. code:: pythonimport fastenumclass Color(fastenum.Enum):RED = 0BLUE = 1GREEN = 2assert isinstance(Color.RED, Color)assert Color.RED is Color['RED']assert Color.BLUE != 1assert Color.GREEN.value == 2def is_red(c: Color) -&gt; bool:return c is Color.REDThere is also a mypy plugin that you'll want to enable in ``mypy.ini`` to help mypy understand ``fastenum``just like it understands ``enum``:::[mypy]plugins = fastenum.mypy_plugin:pluginNote you may experience odd mypy crashes relating to the mypy cache when using this plugin.It's an unfortunate bug we have not yet tracked down. If you run into it, it is unfortunately necessaryto disable the mypy cache by setting ``cache_dir = /dev/null`` 🤦♂️Tradeoffs and disadvantages----------------------------There is no support for automaticvalues, unique value checks, aliases, custom ``__init__`` implementations on members, ``IntEnum``, ``Flag``,or the functional API. If you require any of these features it's probably best to just use ``enum``.``fastenum``'s mypy plugin may cause issues with your mypy cache (see above).Running tests &amp; benchmarks---------------------------::pip install -r dev-requirements.txtPYTHONPATH=. cq &amp;&amp; pytestContributing-------------Pull requests are welcome! Especially if you know how to fix that pesky mypy cache bug 🙈We will accept pull requests adding missing functionality *provided* they do not impact base ``fastenum``performance (it's best to verify that with benchmarks).****.. image:: quantlane.png``fastenum`` was made by `Quantlane &lt;https://quantlane.com&gt;`_, a systematic trading firm.We design, build and run our own stock trading platform.</longdescription>
</pkgmetadata>