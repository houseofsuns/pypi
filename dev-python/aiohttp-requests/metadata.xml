<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>aiohttp-requests============================================================Behold, the power of aiohttp_ client with `Requests &lt;http://docs.python-requests.org/&gt;`_ simplicity:.. code-block:: python    import asyncio    import aiohttp    from aiohttp_requests import requests    async def main():        response = await requests.get('https://api.github.com', auth=aiohttp.BasicAuth('user', 'password'))        text = await response.text()        json = await response.json()        return response, text, json    r, text, json = asyncio.run(main())    &gt;&gt;&gt; r    &lt;ClientResponse(https://api.github.com/) [200 OK]&gt;    &gt;&gt;&gt; r.status    200    &gt;&gt;&gt; r.headers['Content-Type']    'application/json; charset=utf-8'    &gt;&gt;&gt; r.get_encoding()    'utf-8'    &gt;&gt;&gt; text    '{&quot;current_user_url&quot;:&quot;https://api.github.com/user&quot;,...'    &gt;&gt;&gt; json    {'current_user_url': 'https://api.github.com/user', ... }Plus built-in concurrency control to do multiple requests safely:.. code-block:: python    async def main():        # Pass in a list of urls instead of just one. Optionally pass in as_iterator=True to iterate the responses.        responses = await requests.get(['https://api.github.com'] * 2, auth=aiohttp.BasicAuth('user', 'password'))        print(responses)    # [&lt;ClientResponse(https://...) [200 OK]&gt;, , &lt;ClientResponse(https://...) [200 OK]&gt;]        # It defaults to 10 concurrent requests. If you can handle more, then set it higher:        requests.max_concurrency = 100    asyncio.run(main())The `requests` object is just proxying `get` and other HTTP verb methods to `aiohttp.ClientSession`_, which returns `aiohttp.ClientResponse`_. To do anything else, read the aiohttp_ doc... _`aiohttp.ClientSession`: https://docs.aiohttp.org/en/stable/client_reference.html?#aiohttp.ClientSession.. _`aiohttp.ClientResponse`: https://docs.aiohttp.org/en/stable/client_reference.html?#aiohttp.ClientResponse.. _aiohttp: https://docs.aiohttp.org/en/stable/Links &amp; Contact Info====================| PyPI Package: https://pypi.python.org/pypi/aiohttp-requests| GitHub Source: https://github.com/maxzheng/aiohttp-requests| Report Issues/Bugs: https://github.com/maxzheng/aiohttp-requests/issues|| Connect: https://www.linkedin.com/in/maxzheng| Contact: maxzheng.os @t gmail.com</longdescription>
</pkgmetadata>