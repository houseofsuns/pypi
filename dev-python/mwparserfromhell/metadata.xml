<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>mwparserfromhell.. image httpsapi.travis-ci.comearwigmwparserfromhell.svgbranchdevelop  alt Build Status  target httpstravis-ci.orgearwigmwparserfromhell.. image httpsimg.shields.iocoverallsearwigmwparserfromhelldevelop.svg  alt Coverage Status  target httpscoveralls.iorearwigmwparserfromhellmwparserfromhell (the MediaWiki Parser from Hell) is a Python packagethat provides an easy-to-use and outrageously powerful parser for MediaWiki_wikicode. It supports Python 3.5+.Developed by Earwig_ with contributions from _, Legoktm_, and others.Full documentation is available on ReadTheDocs_. Development occurs on GitHub_.Installation------------The easiest way to install the parser is through the Python Package Index_you can install the latest release with pip install mwparserfromhell(get pip_). Make sure your pip is up-to-date first, especially on Windows.Alternatively, get the latest development version    git clone httpsgithub.comearwigmwparserfromhell.git    cd mwparserfromhell    python setup.py installThe comprehensive unit testing suite requires pytest_ (pip install pytest)and can be run with python -m pytest.Usage-----Normal usage is rather straightforward (where text is page text) import mwparserfromhell wikicode  mwparserfromhell.parse(text)wikicode is a mwparserfromhell.Wikicode object, which acts like anordinary str object with some extra methods. For example text  I has a template {{foobarbazeggsspam}} See it wikicode  mwparserfromhell.parse(text) print(wikicode)I has a template {{foobarbazeggsspam}} See it templates  wikicode.filter_templates() print(templates)[{{foobarbazeggsspam}}] template  templates[0] print(template.name)foo print(template.params)[bar, baz, eggsspam] print(template.get(1).value)bar print(template.get(eggs).value)spamSince nodes can contain other nodes, getting nested templates is trivial text  {{foo{{bar}}{{baz{{spam}}}}}} mwparserfromhell.parse(text).filter_templates()[{{foo{{bar}}{{baz{{spam}}}}}}, {{bar}}, {{baz{{spam}}}}, {{spam}}]You can also pass recursiveFalse to filter_templates() and exploretemplates manually. This is possible because nodes can contain additionalWikicode objects code  mwparserfromhell.parse({{foothis {{includes atemplate}}}}) print(code.filter_templates(recursiveFalse))[{{foothis {{includes atemplate}}}}] foo  code.filter_templates(recursiveFalse)[0] print(foo.get(1).value)this {{includes atemplate}} print(foo.get(1).value.filter_templates()[0]){{includes atemplate}} print(foo.get(1).value.filter_templates()[0].get(1).value)templateTemplates can be easily modified to add, remove, or alter params. Wikicodeobjects can be treated like lists, with append(), insert(),remove(), replace(), and more. They also have a matches() methodfor comparing page or template names, which takes care of capitalization andwhitespace text  {{cleanup}} Foo is a [[bar]]. {{uncategorized}} code  mwparserfromhell.parse(text) for template in code.filter_templates()...     if template.name.matches(Cleanup) and not template.has(date)...         template.add(date, July 2012)... print(code){{cleanupdateJuly 2012}} Foo is a [[bar]]. {{uncategorized}} code.replace({{uncategorized}}, {{bar-stub}}) print(code){{cleanupdateJuly 2012}} Foo is a [[bar]]. {{bar-stub}} print(code.filter_templates())[{{cleanupdateJuly 2012}}, {{bar-stub}}]You can then convert code back into a regular str object (forsaving the page) by calling str() on it text  str(code) print(text){{cleanupdateJuly 2012}} Foo is a [[bar]]. {{bar-stub}} text  codeTrueLimitations-----------While the MediaWiki parser generates HTML and has access to the contents oftemplates, among other things, mwparserfromhell acts as a direct interface tothe source code only. This has several implications Syntax elements produced by a template transclusion cannot be detected. For  example, imagine a hypothetical page TemplateEnd-bold that contained  the text b. While MediaWiki would correctly understand that  bfoobar{{end-bold}} translates to bfoobarb, mwparserfromhell  has no way of examining the contents of {{end-bold}}. Instead, it would  treat the bold tag as unfinished, possibly extending further down the page. Templates adjacent to external links, as in httpexample.com{{foo}},  are considered part of the link. In reality, this would depend on the  contents of the template. When different syntax elements cross over each other, as in  {{echoHello}}, world, the parser gets confused because this cannot  be represented by an ordinary syntax tree. Instead, the parser will treat the  first syntax construct as plain text. In this case, only the italic tag would  be properly parsed.  Workaround Since this commonly occurs with text formatting and text  formatting is often not of interest to users, you may pass  skip_style_tagsTrue to mwparserfromhell.parse(). This treats   and  as plain text.  A future version of mwparserfromhell may include multiple parsing modes to  get around this restriction more sensibly.Additionally, the parser lacks awareness of certain wiki-specific settings Word-ending links_ are not supported, since the linktrail rules are  language-specific. Localized namespace names arent recognized, so file links (such as  [[File...]]) are treated as regular wikilinks. Anything that looks like an XML tag is treated as a tag, even if it is not a  recognized tag name, since the list of valid tags depends on loaded MediaWiki  extensions.Integration-----------mwparserfromhell is used by and originally developed for EarwigBot_Page objects have a parse method that essentially callsmwparserfromhell.parse() on page.get().If youre using Pywikibot_, your code might look like this.. code-block python    import mwparserfromhell    import pywikibot    def parse(title)        site  pywikibot.Site()        page  pywikibot.Page(site, title)        text  page.get()        return mwparserfromhell.parse(text)If youre not using a library, you can parse any page with the followingPython 3 code (using the API_ and the requests_ library).. code-block python    import mwparserfromhell    import requests    API_URL  httpsen.wikipedia.orgwapi.php    def parse(title)        params  {            action query,            prop revisions,            rvprop content,            rvslots main,            rvlimit 1,            titles title,            format json,            formatversion 2,        }        headers  {User-Agent My-Bot-Name1.0}        req  requests.get(API_URL, headersheaders, paramsparams)        res  req.json()        revision  res[query][pages][0][revisions][0]        text  revision[slots][main][content]        return mwparserfromhell.parse(text).. _MediaWiki              httpswww.mediawiki.org.. _ReadTheDocs            httpsmwparserfromhell.readthedocs.io.. _Earwig                 httpsen.wikipedia.orgwikiUserThe_Earwig.. _                      httpsen.wikipedia.orgwikiUserCEA3.. _Legoktm                httpsen.wikipedia.orgwikiUserLegoktm.. _GitHub                 httpsgithub.comearwigmwparserfromhell.. _Python Package Index   httpspypi.org.. _get pip                httpspypi.orgprojectpip.. _pytest                 httpsdocs.pytest.org.. _Word-ending links      httpswww.mediawiki.orgwikiHelpLinkslinktrail.. _EarwigBot              httpsgithub.comearwigearwigbot.. _Pywikibot              httpswww.mediawiki.orgwikiManualPywikibot.. _API                    httpswww.mediawiki.orgwikiAPIMain_page.. _requests               https2.python-requests.org</longdescription>
</pkgmetadata>