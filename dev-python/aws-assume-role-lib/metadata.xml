<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> aws-assume-role-libAssumed role session chaining (with credential refreshing) for boto3The typical way to use boto3 when programmatically assuming a role is to explicitly call [sts.AssumeRole](httpsboto3.amazonaws.comv1documentationapilatestreferenceservicessts.htmlSTS.Client.assume_role) and use the returned credentials to create a new [boto3.Session](httpsboto3.amazonaws.comv1documentationapilatestreferencecoresession.html) or client.It looks like this mess of codepythonrole_arn  arnawsiam123456789012roleMyRolesession  boto3.Session()sts  session.client(sts)response  sts.assume_role(    RoleArnrole_arn,    RoleSessionNamesomething_you_have_to_think_about)credentials  response[Credentials]assumed_role_session  boto3.Session(    aws_access_key_idcredentials[AccessKeyId],    aws_secret_access_keycredentials[SecretAccessKey],    aws_session_tokencredentials[SessionToken]) use the sessionprint(assumed_role_session.client(sts).get_caller_identity())This code is verbose, requires specifying a role session name even if you dont care what it is, and must explicitly handle credential expiration and refreshing if needed (in a Lambda function, this is typically handled by calling AssumeRole in every invocation).With aws-assume-role-lib, all that collapses down to a single line. The assumed role session automatically refreshes expired credentials and generates a role session name if one is not provided.pythonrole_arn  arnawsiam123456789012roleMyRolesession  boto3.Session()assumed_role_session  aws_assume_role_lib.assume_role(session, role_arn) use the sessionprint(assumed_role_session.client(sts).get_caller_identity())In a Lambda function that needs to assume a role, you can create the assumed role session during initialization and use it for the lifetime of the execution environment, with AssumeRole calls only being made when necessary, not on every invocation.Note that in .awsconfig, [you have the option to have profiles that assume a role based on another profile](httpsdocs.aws.amazon.comclilatestuserguidecli-configure-role.html), and this automatically handles refreshing expired credentials as well.If youve only used boto3.client() and are not familiar with boto3 sessions, [heres an explainer](httpsben11kehoe.medium.comboto3-sessions-and-why-you-should-use-them-9b094eb5ca8e). Installationbashpip install --user aws-assume-role-libOr just add [aws_assume_role_lib.py](httpsraw.githubusercontent.combenkehoeaws-assume-role-libstableaws_assume_role_libaws_assume_role_lib.py) to your project.View the release history [here](CHANGELOG.md). Usagepythonimport boto3from aws_assume_role_lib import assume_role Get a sessionsession  boto3.Session() or with a profile session  boto3.Session(profile_namemy-profile) Assume the sessionassumed_role_session  assume_role(session, arnawsiam123456789012roleMyRole) do stuff with the original credentialsprint(session.client(sts).get_caller_identity()[Arn]) do stuff with the assumed roleprint(assumed_role_session.client(sts).get_caller_identity()[Arn])In Lambda, initialize the sessions outside the handler, and AssumeRole will only get called when necessary, rather than on every invocationpythonimport osimport boto3from aws_assume_role_lib import assume_role, generate_lambda_session_name Get the Lambda sessionSESSION  boto3.Session() Get the configROLE_ARN  os.environ[ROLE_ARN]ROLE_SESSION_NAME  generate_lambda_session_name()  see below for details Assume the sessionASSUMED_ROLE_SESSION  assume_role(SESSION, ROLE_ARN, RoleSessionNameROLE_SESSION_NAME)def handler(event, context)     do stuff with the Lambda role using SESSION    print(SESSION.client(sts).get_caller_identity()[Arn])     do stuff with the assumed role using ASSUMED_ROLE_SESSION    print(ASSUMED_ROLE_SESSION.client(sts).get_caller_identity()[Arn])Learn more about the benefits of aws-assume-role-lib in Lambda functions in the [demo](lambda-demoREADME.md). Interfaceassume_role(     required arguments    session           boto3.Session,    RoleArn           str,    ,     keyword-only arguments for AssumeRole    RoleSessionName   str                                     None,    PolicyArns        Union[list[dict[str, str]], list[str]]  None,    Policy            Union[str, dict]                        None,    DurationSeconds   Union[int, datetime.timedelta]          None,    Tags              list[dict[str, str]]                    None,    TransitiveTagKeys list[str]                               None,    ExternalId        str                                     None,    SerialNumber      str                                     None,    TokenCode         str                                     None,    SourceIdentity    str                                     None,    additional_kwargs dict                                    None,     keyword-only arguments for returned session    region_name       Union[str, bool]                        None,     keyword-only arguments for assume_role() itself    validate          bool                                    True,    cache             dict                                    None,)assume_role() takes a session and a role ARN, and optionally [other keyword arguments for sts.AssumeRole](httpsboto3.amazonaws.comv1documentationapilatestreferenceservicessts.htmlSTS.Client.assume_role).Unlike the AssumeRole API call itself, RoleArn is required, but RoleSessionName is not.The RoleSessionName is set for you if it is not provided it will use the [SourceIdentity](httpsdocs.aws.amazon.comIAMlatestUserGuideid_credentials_temp_control-access_monitor.html) if that is provided, otherwise it will generated.If you want this generated value for RoleSessionName when SourceIdentity is provided (the behavior in v2.8 and before), set RoleSessionName to the special value aws_assume_role_lib.AUTOMATIC_ROLE_SESSION_NAME.Note that unlike the boto3 sts client method, you can provide the Policy parameter (the [inline session policy](httpsdocs.aws.amazon.comIAMlatestUserGuideaccess_policies.htmlpolicies_session)) as a dict instead of as a serialized JSON string, PolicyArns as a list of ARNs, and DurationSeconds as a datetime.timedelta instead of as an integer.By default, the session returned by assume_role() links its region configuration to the input session.If you would like to set the region explicitly, pass it in the region_name parameter.Note that if the parent session was created without a region passed in to the Session constructor, it has an implicit region, based on searching potential configuration locations.This means that the region used by the session can change (for example, if you set or change os.environ[AWS_DEFAULT_REGION]).By default, the child session region is linked to the parent session, so if the parent session has an implicit region, or if the parent sessions region is changed directly, they would both change.If you would like to fix the child session region to be explicitly the current value, pass region_nameTrue.If, for some reason, you have an explicit region set on the parent, and want the child to have implicit region config, pass region_nameFalse.By default, assume_role() checks if the parameters are invalid.Without this validation, errors for these issues are more confusingly raised when the child session is first used to make an API call (boto3 doesnt make the call to retrieve credentials until they are needed).However, this incurs a small time penalty, so parameter validation can be disabled by passing validateFalse.If any new arguments are added to AssumeRole in the future and this library is not updated to allow them directly, they can be passed in as a dict via the additional_kwargs argument.The parent session is available on the child session in the assume_role_parent_session property.Note this property is added by this library ordinary boto3 sessions do not have it. Patching boto3You can make the assume_role() function available directly in boto3 by calling patch_boto3().This creates a boto3.assume_role(RoleArn, ...) function (note that it does not take a session, it uses the same default session as boto3.client()), and adds a boto3.Session.assume_role() method.So usage for that looks likepythonimport boto3import aws_assume_role_libaws_assume_role_lib.patch_boto3()assumed_role_session  boto3.assume_role(arnawsiam123456789012roleMyRole) the above is basically equivalent to aws_assume_role_lib.assume_role(boto3.Session(), arnawsiam123456789012roleMyRole)session  boto3.Session(profile_namemy-profile)assumed_role_session  session.assume_role(arnawsiam123456789012roleMyRole) Role session names for Lambda functionsLearn more about the benefits of aws-assume-role-lib in Lambda functions in the [demo](lambda-demoREADME.md).If you dont provide a role session name, but you provide a [SourceIdentity](httpsdocs.aws.amazon.comIAMlatestUserGuideid_credentials_temp_control-access_monitor.html), this value is used for the role session name as well.If SourceIdentity is not provided either, the underlying botocore library [generates one using a timestamp](httpsgithub.combotobotocoreblobc53072ec257ef47e2fc749c384a9488fd3f3e626botocorecredentials.pyL730).Thats the best it can do, because it doesnt have any other context.But in a Lambda function, we do have additional context, the Lambda function itself.If you call generate_lambda_session_name() inside an instance of a Lambda function, it returns a session name that corresponds to the function instance, which you can use when assuming a role in the Lambda function (either with this librarys assume_role() or any other method).The purpose of this is to simplify tracing usage of the session back to the function instance.The returned value is in one of the following forms, depending on the length of the values, to keep the session name within the maximum of 64 characters {function_name} {function_name}.{identifier} {function_name}.{function_version}.{identifier}The function version is never included if it is LATEST.The maximum role session name length is 64 characters. To ensure this, andto provide at least 4 characters of the identifier when it is used, thefollowing rules apply, in order1. If the function name is longer than 59 characters, the session name is the truncated function name.2. If the function name plus the function version is longer than 59 characters, the session name is the function name plus the identifier, truncated.3. Otherwise, the session name is the function name plus the version (if one is found and not LATEST) plus the identifier, truncated.The identifier is the function instances unique ID taken from the CloudWatch log stream name if this cannot be found, its a timestamp if the identifier can be at least 14 characters long (to provide for second-level precision), otherwise it is a random string.The identifier will not be included unless at least 4 charactersThe values are automatically extracted from [the relevant environment variables](httpsdocs.aws.amazon.comlambdalatestdgconfiguration-envvars.htmlconfiguration-envvars-runtime) you can override any of them by providing them as arguments to the function. ARN formattingassume_role() requires a role ARN, and if you know the role name and account id but have trouble remembering the [exact format of role ARNs](httpsdocs.aws.amazon.comservice-authorizationlatestreferencelist_identityandaccessmanagement.htmlidentityandaccessmanagement-resources-for-iam-policies), theres get_role_arn() for you.Theres additionally a get_assumed_role_session_arn() for formatting assumed role session ARNs.get_role_arn(    account_id Union[str, int],    role_name  str,    path       str              ,    partition  str              aws,)get_assumed_role_session_arn(    account_id        Union[str, int],    role_name         str,    role_session_name str,    partition         str              aws,)For get_role_arn(), if the role name has a path, it can be provided as part of the name or as the separate path argument (but not both).Assumed role session ARNs do not include the role path if it is used in the role name it is removed. CachingIf you would like to cache the credentials on the file system, you can use the JSONFileCache class, which will create files under the directory you provide in the constructor (which it will create if it doesnt exist).Use it likepythonassumed_role_session  assume_role(session, arnawsiam123456789012roleMyRole, cacheJSONFileCache(pathtodir))You can also use any dict-like object for the cache (supporting __getitem____setitem____contains__). Command line useaws-assume-role-lib has basic support for retrieving assumed role credentials from the command line.In general, its better to make profiles in .awsconfig for role assumption, like thisini this is a pre-existing profile you already have[profile profile-to-call-assume-role-with] maybe its IAM User credentials or AWS SSO config or whatever else you may have[profile my-assumed-role]role_arn  arnawsiam123456789012roleMyRole optional role_session_name  MyRoleSessionNamesource_profile  profile-to-call-assume-role-with or instead of source_profile, you can tell it to use external credentials. one of credential_source  Environment credential_source  Ec2InstanceMetadata credential_source  EcsContainerYou can use my-assumed-role like any other profile.It uses the AWS SDKs built-in support for role assumption, rather than relying on this third party library.It also gets you credential refreshing from the SDKs, where getting the credentials in the manner below cannot refresh them when they expire.But if you absolutely must have ad hoc role assumption on the command line, use the module invocation syntax python -m aws_assume_role_lib ROLE_ARN [OPTIONS].The options are --profile use a specific configuration profile. --env print the credentials as environment variables (the default), suitable for export (python -m aws_assume_role_lib ...). --json print the credentials in [credential_process-formatted JSON format](httpsdocs.aws.amazon.comclilatestuserguidecli-configure-sourcing-external.html). Note that you dont normally need to use this as a credential_process in a profile, because you can just directly make the profile do role assumption as shown above. The remaining options are the arguments to assume_role()   --RoleSessionName   --PolicyArns must be a comma-separated list of ARNs, a JSON list of ARNs, or a JSON object per the API   --Policy must be a JSON object   --DurationSeconds   --Tags must be formatted as Key1Value1,Key2Value2, or a JSON object.   --TransitiveTagKeys must be a comma-separated list or a JSON list.   --ExternalId   --SerialNumber   --TokenCode   --SourceIdentity   --additional-kwargs must be a JSON object.</longdescription>
</pkgmetadata>