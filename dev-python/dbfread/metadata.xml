<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>dbfread - Read DBF Files with PythonDBF is a file format used by databases such dBase, Visual FoxPro, andFoxBase+. This library reads DBF files and returns the data as nativePython data types for further processing. It is primarily intended forbatch jobs and one-off scripts.     from dbfread import DBF     for record in DBF(people.dbf)    ...     print(record)    OrderedDict([(NAME, Alice), (BIRTHDATE, datetime.date(1987, 3, 1))])    OrderedDict([(NAME, Bob), (BIRTHDATE, datetime.date(1980, 11, 12))])By default records are streamed directly from the file.  If you haveenough memory you can instead load them into a list. This allows forrandom access     table  DBF(people.dbf, loadTrue)     print(table.records[1][NAME])    Bob     print(table.records[0][NAME])    AliceFull documentation at httpsdbfread.readthedocs.ioSee docschanges.rst for a full list of changes in each version.Main Features------------- written for Python 3, but also works in 2.7 simple but flexible API data is returned as native Python data types records are ordered dictionaries, but can be reconfigured to be of  any type aims to handle all variants of DBF files. (Currently only widely  tested with Visual FoxPro, but should work well with other  variants.) support for 18 field types. Custom types can be added by subclassing  FieldParser reads FPT and DBT memo files, both text and binary data handles mixed case file names gracefully on case sensitive file systems can retrieve deleted recordsInstalling----------Requires Python 3.2 or 2.7.  pip install dbfreaddbfread is a pure Python module and doesnt depend on any packagesoutside the standard library.To build documentation locally    python setup.py docsThis requires Sphinx. The resulting files can be found indocs_build.Source code------------Latest stable release httpgithub.comolembdbfreadDevelopment version httpgithub.comolembdbfreadtreedevelopAPI Changes-----------dbfread.open() and dbfread.read() are deprecated as of version2.0, and will be removed in 2.2.The DBF class is no longer a subclass of list. This makes theAPI a lot cleaner and easier to understand, but old code that reliedon this behaviour will be broken. Iteration and record counting worksthe same as before. Other list operations can be rewritten using therecord attribute. For example    table  dbfread.read(people.dbf)    print(table[1])can be rewritten as    table  DBF(people.dbf, loadTrue)    print(table.records[1])open() and read() both return DeprecatedDBF, which is asubclass of DBF and list and thus backward compatible.License-------dbfread is released under the terms of the MIT licensehttpen.wikipedia.orgwikiMIT_License_.Contact-------Ole Martin Bjorndalen - ombdalengmail.com</longdescription>
</pkgmetadata>