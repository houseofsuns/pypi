<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>ASTTokens.. image httpsimg.shields.iopypivasttokens.svg    target httpspypi.python.orgpypiasttokens.. image httpsimg.shields.iopypipyversionsasttokens.svg    target httpspypi.python.orgpypiasttokens.. image httpsgithub.comgristlabsasttokensactionsworkflowsbuild-and-test.ymlbadge.svg    target httpsgithub.comgristlabsasttokensactionsworkflowsbuild-and-test.yml.. image httpsreadthedocs.orgprojectsasttokensbadgeversionlatest    target httpasttokens.readthedocs.ioenlatestindex.html.. image httpscoveralls.ioreposgithubgristlabsasttokensbadge.svg    target httpscoveralls.iogithubgristlabsasttokens.. Start of user-guideThe asttokens module annotates Python abstract syntax trees (ASTs) with the positions of tokensand text in the source code that generated them.It makes it possible for tools that work with logical AST nodes to find the particular text thatresulted in those nodes, for example for automated refactoring or highlighting.Installation------------asttokens is available on PyPI httpspypi.python.orgpypiasttokens    pip install asttokensThe code is on GitHub httpsgithub.comgristlabsasttokens.The API Reference is here httpasttokens.readthedocs.ioenlatestapi-index.html.Usage-----ASTTokens works with both Python2 and Python3.ASTTokens can annotate both trees built by ast httpsdocs.python.org2libraryast.html_,AND those built by astroid httpsgithub.comPyCQAastroid_.Heres an example.. code-block python    import asttokens, ast    source  Robot(blue).walk(steps10n)    atok  asttokens.ASTTokens(source, parseTrue)Once the tree has been marked, nodes get .first_token, .last_token attributes, andthe ASTTokens object offers helpful methods.. code-block python    attr_node  next(n for n in ast.walk(atok.tree) if isinstance(n, ast.Attribute))    print(atok.get_text(attr_node))    start, end  attr_node.last_token.startpos, attr_node.last_token.endpos    print(atok.text[start] + RUN + atok.text[end])Which produces this output.. code-block text    Robot(blue).walk    Robot(blue).RUN(steps10n)The ASTTokens object also offers methods to walk and search the list of tokens that make upthe code (or a particular AST node), which is more useful and powerful than dealing with the textdirectly.Contribute----------To contribute1. Fork this repository, and clone your fork.2. Install the package with test dependencies (ideally in a virtualenv) with    pip install -e .[test]3. Run tests in your current interpreter with the command pytest or python -m pytest.4. Run tests across all supported interpreters with the tox command. You will need to have the interpreters installed separately. We recommend pyenv for that. Use tox -p auto to run the tests in parallel.5. By default certain tests which take a very long time to run are skipped, but they are run on travis CI. To run them locally, set the environment variable ASTTOKENS_SLOW_TESTS. For example run ASTTOKENS_SLOW_TESTS1 tox to run the full suite of tests.</longdescription>
</pkgmetadata>