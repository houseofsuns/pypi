<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Python smart getenv===================Since environment variables in os.environ are strings, it often appears inconvenient to store and retrieve otherdata types such as bool or list. The package provides a single function that wraps os.getenv and allowsyou to specify the desired variable type.Tested and supported types: str, int, float, list. tuple, dict.`PyPi page`_Usage-----Install:.. code:: bash    $ pip install smart-getenvPrepare the variables:.. code:: bash    $ export BOOLEAN=true    $ export LIST=a,b,c    $ export TRICKY_LIST=d:e:f    $ export DICT=&quot;{'foo':'bar'}&quot;Get them:.. code:: python    &gt;&gt;&gt; from smart_getenv import getenv    &gt;&gt;&gt;    &gt;&gt;&gt; getenv('BOOLEAN', type=str)    'true'    &gt;&gt;&gt; getenv('BOOLEAN', type=bool)    True    &gt;&gt;&gt; getenv('LIST', type=list)    ['a', 'b', 'c']    &gt;&gt;&gt; getenv('LIST', type=tuple)    ('a', 'b', 'c')    &gt;&gt;&gt; getenv('TRICKY_LIST', type=list, separator=':')    ['d', 'e', 'f']    &gt;&gt;&gt; getenv('DICT', type=dict)    {'foo': 'bar'}    &gt;&gt;&gt; getenv('LOST', default='default value anyone?')    'default value anyone?'Run tests:.. code:: bash    $ python tests.py.. _PyPi page: https://pypi.python.org/pypi/smart-getenv</longdescription>
</pkgmetadata>