<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>h1 aligncenterEnvierh1h2 aligncenterPython application configuration from the environmenth2 SynopsisEnvier is a Python library for extracting configuration from environmentvariables in a declarative and (eventually) 12-factor-app-compliant way. UsageThe following example shows how to declare the configuration for an applicationthat uses the MYAPP_DEBUG, MYAPP_SERVICE_HOST and MYAPP_SERVICE_PORTvariables from the environment. python from envier import Env  class GlobalConfig(Env)     __prefix__  myapp          debug_mode  Env.var(bool, debug, defaultFalse)      service_host  Env.var(str, service.host, defaultlocalhost)     service_port  Env.var(int, service.port, default3000)      _is_default_port  Env.der(bool, lambda c c.service_port  c.spec.service_port.default)  config  GlobalConfig() config.service_port3000 config._is_default_portTrueConfigurations can also be nested to create namespaces python from envier import Env  class ServiceConfig(Env)     __prefix__  service      host  Env.var(str, host, defaultlocalhost)     port  Env.var(int, port, default3000)  class GlobalConfig(Env)     __prefix__  myapp          debug_mode  Env.var(bool, debug, defaultFalse)      service  ServiceConfig  config  GlobalConfig() config.service.port3000The same configuration can be obtained with implicit nesting by declaring theServiceConfig subclass inside GlobalConfig, and setting the class attribute__item__ to the name of the item the sub-configuration should be assigned to,viz. python from envier import Env  class GlobalConfig(Env)     __prefix__  myapp          debug_mode  Env.var(bool, debug, defaultFalse)      class ServiceConfig(Env)         __item__  __prefix__  service                  host  Env.var(str, host, defaultlocalhost)         port  Env.var(int, port, default3000)  config  GlobalConfig() config.service.port3000 Type CheckingThe library ships with a mypy plugin to allow for type checking. If you wantto use it, either install the library with the mypy extra or ensure thatmypy is installed, and then add envier.mypy to the list of extra plugins inthe mypy configuration. Sphinx PluginThe library comes with a Sphinx plugin at envier.sphinx to allow generatingdocumentation from the configuration spec class directly. It exposes theenvier directive that takes a mandatory argument, the configuration specclass in the form moduleclass additionally, the options heading andrecursive can be used to control whether to add heading and whether torecursively get help information from nested configuration spec classesrespectively. By default, the plugin will display the table heading and will notrecurse over nested configuration spec classes.Here is an example for a configuration class GlobalConfig located in themyapp.config module. We omit the table header and recurse over nestedconfiguration. rst.. envier myapp.configGlobalConfig   heading false   recursive true Roadmap- Add support for environment files.- Rely on type hints as support for older versions of Python is dropped.- Derivations might require an evaluation order.</longdescription>
</pkgmetadata>