<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Google Cloud Python profiling agentPython profiling agent for[Google Cloud Profiler](httpscloud.google.comprofiler).See[Google Cloud Profiler profiling Python code](httpscloud.google.comprofilerdocsprofiling-python)for detailed documentation. Supported OSLinux. Profiling Python applications is supported for Linux kernels whosestandard C library is implemented with glibc or with musl. For configurationinformation specific to Linux Alpine kernels, see[Running on Linux Alpine](httpscloud.google.comprofilerdocsprofiling-pythonrunning_with_linux_alpine). Supported Python VersionsPython  3.6 Installation  usage1.  Install the profiler package using PyPI    shell    pip3 install google-cloud-profiler    2.  Enable the profiler in your application    python    import googlecloudprofiler    def main()         Profiler initialization. It starts a daemon thread which continuously         collects and uploads profiles. Best done as early as possible.        try            googlecloudprofiler.start(                servicehello-profiler,                service_version1.0.1,                 verbose is the logging level. 0-error, 1-warning, 2-info,                 3-debug. It defaults to 0 (error) if not set.                verbose3,                 project_id must be set if not running on GCP.                 project_idmy-project-id,            )        except (ValueError, NotImplementedError) as exc            print(exc)   Handle errors here     Installation on Linux AlpineThe Python profiling agent has a native component. The base Alpine image forPython does not have all dependencies required to build this native componentinstalled. To build the Python profiling agent on Alpine, one must install thepackage build-base.To use the Python profiling agent on Alpine without installing additionaldependencies on to the final Alpine image, one can use a two-stage build andcompile the Python profiling agent in the first stage.Here is an example of a Docker image that uses a multi-stage build to compileand install the Python profiling agentFROM python3.7-alpine as builder Install build-base to allow for compilation of the profiling agent.RUN apk add --update --no-cache build-base Compile the profiling agent, generating wheels for it.RUN pip3 wheel --wheel-dirtmpwheels google-cloud-profilerFROM python3.7-alpine Copy over the directory containing wheels for the profiling agent.COPY --frombuilder tmpwheels tmpwheels Install the profiling agent.RUN pip3 install --no-index --find-linkstmpwheels google-cloud-profiler Install any other required modules or dependencies, and copy an app which enables the profiler as described in Enable the profiler in your application.COPY .bench.py . Run the application when the docker image is run, using either CMD (as is done here) or ENTRYPOINT.CMD python3 -u bench.py Troubleshooting Resource temporarily unavailable errors with PythonIf you see the following log entries after enabling the ProfilerBlockingIOError [Errno 11] Resource temporarily unavailableException ignored when trying to write to the signal wakeup fdsee httpscloud.google.comprofilerdocstroubleshootingpython-blocking forthe cause and the workaround.</longdescription>
</pkgmetadata>