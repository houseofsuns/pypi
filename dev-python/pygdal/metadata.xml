<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># Virtualenv friendly GDAL bindings for PythonThis package is for you if you had problems installing GDAL in your virtualenv.You can install GDAL into your virtualenv using this package but you still needto install GDAL library and its header files on your system. On Ubuntu it can bedone this way:```bash$ sudo apt-get install libgdal1-dev```Version of the same package, and GDAL, so that if you have installed GDAL 3.6.4you need to install 3.6.4.\* of this package:```bash$ gdal-config --version3.6.4$ virtualenv --no-site-packages env$ env/bin/pip install pygdal==&quot;3.6.4.*&quot;```The trick with range of versions required to support pygdal versioning.Package `numpy` is also listed as a dependency, so you do not need to install itbefore installing GDAL. After package is installed you can use is same way asstandard GDAL bindings:```pythonfrom osgeo import gdal, ogr, osr# ...```</longdescription>
</pkgmetadata>