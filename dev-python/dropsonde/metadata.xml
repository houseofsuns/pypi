<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># Cloud Foundry Dropsonde Protocol for PythonCompiled python generated from the Cloud Foundry Dropsonde Protobuf definition files.The [Dropsonde Protocol](https://github.com/cloudfoundry/dropsonde-protocol)is defined here, and this repo provides &quot;protoc&quot; compiled Python and Python3modules.## ExampleThe Standard protobuf files are referenced as follows.```pythonfrom dropsonde.pb import envelope_pb2from dropsonde.pb import error_pb2from dropsonde.pb import http_pb2from dropsonde.pb import log_pb2from dropsonde.pb import metric_pb2from dropsonde.pb import uuid_pb2```The UUID protobuf type is rather cryptic, so a utility function is includedfor parsing the type into a UUID formatted string.```python&gt;&gt;&gt; from dropsonde.util import get_uuid_string&gt;&gt;&gt; application_guid = {...   'low': '12413561682238894606',...   'high': '3611542221973970511'... }&gt;&gt;&gt; application_guid = get_uuid_string(**application_guid)&gt;&gt;&gt; print(application_guid)0ef23f83-94d4-45ac-e455-014268ec2103&gt;&gt;&gt;```## DevelopmentThe `Makefile` provides all core development tasks.### Install environment`make install`This will1. clone the Dropsonde protobuf repo1. install dependencies for python21. install dependencies for python3### Refactor and compile the protobuf files`make compile`This will1. create a copy of the protobuf source files1. refactor the &quot;import&quot; directives in the source files to match the required python package structure1. run &quot;protoc&quot; on the refactored source files and output the generated Python code into the &quot;dropsonde.pb&quot; python package tree.### Test`make test`This will1. run the tests to ensure that imports work1. ensure that the UUID conversion tool works### Clean`make clean`This will1. remove all the refactored protobuf files1. remove the virtualenv environments for python2 and python3### Package`make package`This will only package the compiled python files using `setup.py`### Build`make build`This will1. run the &quot;clone&quot; task1. run the &quot;compile&quot; task1. run the &quot;test&quot; task1. run the &quot;package&quot; task### Deploy`make deploy`This will1. run the &quot;clean&quot; task1. run the &quot;clone&quot; task1. run the &quot;compile&quot; task1. run the &quot;test&quot; task1. run the `python setup.py sdist upload` to deploy to PyPI.</longdescription>
</pkgmetadata>