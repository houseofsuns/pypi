<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>bunchBunch is a dictionary that supports attribute-style access, a la JavaScript. b  Bunch() b.hello  world b.helloworld b[hello] +  b.helloworld b.foo  Bunch(lolTrue) b.foo.lolTrue b.foo is b[foo]TrueDictionary Methods------------------A Bunch is a subclass of dict it supports all the methods a dict does b.keys()[foo, hello]Including update() b.update({ ponies are pretty }, hello42) print repr(b)Bunch(fooBunch(lolTrue), hello42, poniesare pretty)As well as iteration [ (k,b[k]) for k in b ][(ponies, are pretty), (foo, Bunch(lolTrue)), (hello, 42)]And splats The {knights} who say {ni}.format(Bunch(knightslolcats, nican haz))The lolcats who say can hazSerialization-------------Bunches happily and transparently serialize to JSON and YAML. b  Bunch(fooBunch(lolTrue), hello42, poniesare pretty) import json json.dumps(b){ponies are pretty, foo {lol true}, hello 42}If JSON support is present (json or simplejson), Bunch will have a toJSON() method which returns the object as a JSON string.If you have PyYAML_ installed, Bunch attempts to register itself with the various YAML Representers so that Bunches can be transparently dumped and loaded. b  Bunch(fooBunch(lolTrue), hello42, poniesare pretty) import yaml yaml.dump(b)bunch.Bunchnfoo bunch.Bunch {lol true}nhello 42nponies are prettyn yaml.safe_dump(b)foo {lol true}nhello 42nponies are prettynIn addition, Bunch instances will have a toYAML() method that returns the YAML string using yaml.safe_dump(). This method also replaces __str__ if present, as I find it far more readable. You can revert back to Pythons default use of __repr__ with a simple assignment Bunch.__str__  Bunch.__repr__. The Bunch class will also have a static method Bunch.fromYAML(), which loads a Bunch out of a YAML string.Finally, Bunch converts easily and recursively to (unbunchify(), Bunch.toDict()) and from (bunchify(), Bunch.fromDict()) a normal dict, making it easy to cleanly serialize them in other formats.Miscellaneous------------- It is safe to import  from this module. Youll get Bunch, bunchify, and unbunchify. Ample doctests     python -m bunch.test -vFeedback--------Open a ticket at httpgithub.comdscbunch or send me an email at dscless.ly ... _PyYAML httppyyaml.orgwikiPyYAML</longdescription>
</pkgmetadata>