<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Injector - Python dependency injection framework, inspired by Guiceimage Coverage StatusIntroduction------------While dependency injection is easy to do in Python due to its supportfor keyword arguments, the ease with which objects can be mocked and itsdynamic nature, a framework for assisting in this process can remove alot of boiler-plate from larger applications. Thats where Injector canhelp. It automatically and transitively provides dependencies for you.As an added benefit, Injector encourages nicely compartmentalised codethrough the use of refmodules module.If youre not sure what dependency injection is or youd like to learnmore about it see-  The Clean Code Talks - Dont Look For Things (a talk by Miko   Hevery) httpswww.youtube.comwatchvRlfLCWKxHJ0__-  Inversion of Control Containers and the Dependency Injection pattern   (an article by Martin   Fowler) httpsmartinfowler.comarticlesinjection.html__The core values of Injector are-  Simplicity - while being inspired by Guice, Injector does not   slavishly replicate its API. Providing a Pythonic API trumps   faithfulness. Additionally some features are omitted because   supporting them would be cumbersome and introduce a little bit too   much magic (member injection, method injection).   Connected to this, Injector tries to be as nonintrusive as possible.   For example while you may declare a class constructor to expect some   injectable parameters, the class constructor remains a standard   constructor you may instantiate the class just the same manually,   if you want.-  No global state  you can have as many   Injector httpsinjector.readthedocs.ioenlatestapi.htmlinjector.Injector__   instances as you like, each with a different configuration and each   with different objects in different scopes. Code like this wont work   for this very reason   .. code python        class MyClass            inject            def __init__(t SomeType)                 ...        MyClass()   This is simply because theres no global Injector to use. You   need to be explicit and use   Injector.get httpsinjector.readthedocs.ioenlatestapi.htmlinjector.Injector.get__,   Injector.create_object httpsinjector.readthedocs.ioenlatestapi.htmlinjector.Injector.create_object__   or inject MyClass into the place that needs it.-  Cooperation with static type checking infrastructure the API   provides as much static type safety as possible and only breaks it   where theres no other option. For example the   Injector.get httpsinjector.readthedocs.ioenlatestapi.htmlinjector.Injector.get__   method is typed such that injector.get(SomeType) is statically   declared to return an instance of SomeType, therefore making it   possible for tools such as mypy httpsgithub.compythonmypy__   to type-check correctly the code using it.-  The client code only knows about dependency injection to the extent   it needs    inject httpsinjector.readthedocs.ioenlatestapi.htmlinjector.inject__,   Inject httpsinjector.readthedocs.ioenlatestapi.htmlinjector.Inject__   and   NoInject httpsinjector.readthedocs.ioenlatestapi.htmlinjector.NoInject__   are simple markers that dont really do anything on their own and   your code can run just fine without Injector orchestrating things.How to get Injector-  GitHub (code repository, issues)   httpsgithub.comalecthomasinjector-  PyPI (installable, stable distributions)   httpspypi.orgprojectinjector. You can install it using pip   .. code bash      pip install injector-  Documentation httpsinjector.readthedocs.org-  Change log httpsinjector.readthedocs.ioenlatestchangelog.htmlInjector works with CPython 3.7+ and PyPy 3 implementing Python 3.7+.A Quick Example---------------.. code python    from injector import Injector, inject    class Inner   ...     def __init__(self)   ...         self.forty_two  42   ...    class Outer   ...     inject   ...     def __init__(self, inner Inner)   ...         self.inner  inner   ...    injector  Injector()    outer  injector.get(Outer)    outer.inner.forty_two   42Or with dataclasses if you like.. code python   from dataclasses import dataclass   from injector import Injector, inject   class Inner       def __init__(self)           self.forty_two  42   inject   dataclass   class Outer       inner Inner   injector  Injector()   outer  injector.get(Outer)   print(outer.inner.forty_two)   Prints 42A Full Example--------------Heres a full example to give you a taste of how Injector works.. code python    from injector import Module, provider, Injector, inject, singletonWell use an in-memory SQLite database for our example.. code python    import sqlite3And make up an imaginary RequestHandler class that uses the SQLiteconnection.. code python    class RequestHandler   ...   inject   ...   def __init__(self, db sqlite3.Connection)   ...     self._db  db   ...   ...   def get(self)   ...     cursor  self._db.cursor()   ...     cursor.execute(SELECT key, value FROM data ORDER by key)   ...     return cursor.fetchall()Next, for the sake of the example, well create a configuration type.. code python    class Configuration   ...     def __init__(self, connection_string)   ...         self.connection_string  connection_stringNext, we bind the configuration to the injector, using a module.. code python    def configure_for_testing(binder)   ...     configuration  Configuration(memory)   ...     binder.bind(Configuration, toconfiguration, scopesingleton)Next we create a module that initialises the DB. It depends on theconfiguration provided by the above module to create a new DBconnection, then populates it with some dummy data, and provides aConnection object.. code python    class DatabaseModule(Module)   ...   singleton   ...   provider   ...   def provide_sqlite_connection(self, configuration Configuration) - sqlite3.Connection   ...     conn  sqlite3.connect(configuration.connection_string)   ...     cursor  conn.cursor()   ...     cursor.execute(CREATE TABLE IF NOT EXISTS data (key PRIMARY KEY, value))   ...     cursor.execute(INSERT OR REPLACE INTO data VALUES (hello, world))   ...     return conn(Note how we have decoupled configuration from our databaseinitialisation code.)Finally, we initialise an Injector and use it to instantiate aRequestHandler instance. This first transitively constructs asqlite3.Connection object, and the Configuration dictionary that itin turn requires, then instantiates our RequestHandler.. code python    injector  Injector([configure_for_testing, DatabaseModule()])    handler  injector.get(RequestHandler)    tuple(map(str, handler.get()[0]))   py3py2 compatibility hack   (hello, world)We can also verify that our Configuration and SQLite connectionsare indeed singletons within the Injector.. code python    injector.get(Configuration) is injector.get(Configuration)   True    injector.get(sqlite3.Connection) is injector.get(sqlite3.Connection)   TrueYoure probably thinking something like this is a large amount of workjust to give me a database connection, and you are correct dependencyinjection is typically not that useful for smaller projects. It comesinto its own on large projects where the up-front effort pays for itselfin two ways1. Forces decoupling. In our example, this is illustrated by decoupling   our configuration and database configuration.2. After a type is configured, it can be injected anywhere with no   additional effort. Simply inject and it appears. We dont really   illustrate that here, but you can imagine adding an arbitrary number   of RequestHandler subclasses, all of which will automatically   have a DB connection provided.Footnote--------This framework is similar to snake-guice, but aims for simplification. Copyright 2010-2013 to Alec Thomas, under the BSD license.. image image httpsgithub.comalecthomasinjectorworkflowsCIbadge.svg   target httpsgithub.comalecthomasinjectoractionsqueryworkflow3ACI+branch3Amaster.. Coverage Status image httpscodecov.ioghalecthomasinjectorbranchmastergraphbadge.svg   target httpscodecov.ioghalecthomasinjector</longdescription>
</pkgmetadata>