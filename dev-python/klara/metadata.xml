<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> KlaraKlara is a static analysis tools to automatic generate test case, basedon SMT (z3) solver, with a powerful ast level inference system. Klarawill take python file as input and generate corresponding test file inpytest format, that attempt to cover all return values. For example,following function in file test.py pythondef triangle(x int, y int, z int) - str    if x  y  z        return Equilateral triangle    elif x  y or y  z or x  z        return Isosceles triangle    else        return Scalene trianglewill generate pythonimport testdef test_triangle_0()    assert test.triangle(0, 0, 0)  Equilateral triangle    assert test.triangle(0, 0, 1)  Isosceles triangle    assert test.triangle(2, 0, 1)  Scalene triangleSee the Klaras documentation at httpsklara-py.readthedocs.ioNote Klara is still in early experimental stage, notable missing features are loop, comprehension, module import, exceptions and many more.See [limitations](httpsklara-py.readthedocs.ioenlatestlimitation.html) for full list. It probably will not run on real world projects, so its bestto cherry-pick a few interesting functions to generate the corresponding test case. InstallingKlara can be installed via pip tool by using    pip install klara UsageWe can invoke klara on any python source file, and it will generate acorresponding pytest test file. shell cat source.pydef foo(x int, y int, z str)    if x + y  2        return x + y + 12    elif x  y        return x + y    elif (z + me)  some        return z + thing    else        return x - y klara source.py cat test_source.pyimport contract_testdef test_foo_0()    assert contract_test.foo(0, 3, )  15    assert contract_test.foo(0, 1, )  1    assert contract_test.foo(0, 0, so)  sothing    assert contract_test.foo(0, 0, )  0Consult the [quick start](httpsklara-py.readthedocs.ioenlatestquick_start.html) manual for more examples andguidance. To use it as a static analysis library, go to[Inference](httpsklara-py.readthedocs.ioenlatestinference.html). Why KlaraKlara works on ast level and it doesnt execute user code in any way,which is a very important difference compared to similar tool like[Crosshair](httpsgithub.compschanelyCrossHair) and[Pynguin](httpsgithub.comse2ppynguin) that utilize concolicsymbolic execution that required user code execution that might causeunwanted side effects. Klara work on ast level, combine with data flow analysisthat utilize Control Flow Graph(CFG), Static Single Assignment(SSA), use-def chain, etc... to build apowerful python inference system that leverages Z3-solver forconstraints solving and path feasibility check. Because of this, Klarais able to operate on both python23 source code with the help of[typed_ast](httpsgithub.compythontyped_ast). To specify the sourcecode is in python 2, pass in -py 2 argument. Its python 3 bydefault.Klara can also be used as a static analysis tool, allow user to definecustom rule to identify programming bugs, error or enforcing codingstandard. With SMT solver support, analysis will be more accurate andgreatly reduce false-positive case. For example pythonimport klaratree  klara.parse(    def foo(v1 int)        if v1  4            if v1  3                z  1            else                z  2        else            z  3        s  z)with klara.MANAGER.initialize_z3_var_from_func(tree.body[0])    print(list(tree.body[0].body[-1].value.infer()))Will print out    [2, 3]Because z  1 is not possible due to v1  4 and v1  3 is unsatisfiableThe inference system architecture and api is largely inspired by[Astroid](httpsgithub.comPyCQAastroid), a static inference libraryused by [Pylint](httpsgithub.comPyCQApylint).Klara utilize the inference system to generate test case, in otherwords, it generate test case for all possible return values of thefunction, instead of generate test case for all control path of thefunction.To illustrate the point, consider the function below, with divide byzero vulnerabilities at line 3 pythondef foo(v1 int, v2 float)    if v1  10000        s  v1  0   unused statement    if v1  v2        s  v1    else        s  v2    return sKlara will generate test inputs below pythonimport contract_testdef test_foo_0()    assert contract_test.foo(0, -1.0)  0    assert contract_test.foo(0, 0.0)  0.0It doesnt generate input v1  10000, so the test case would not beable to find out the exceptions. This is because the s atline 3 is unused in the return value.If we modify the second if statement to elif, which wellbe able to return the [s]{.title-ref} at line 3, klara will generatetest inputs that cover v1  10000 case.This is an important distinction with other automatic test casegeneration available now, because by only generate test case for returnvalues, we can generate a minimal test case, and its easier tocustomize how do Klara cover the function.For example, say we are composing a complex system python    def main(number int, cm int, dc int, wn int)        mc  0        if wn  2            if number  2 and number  2 or number  2                if number  0                    if wn  2 or wn  2                        mc  2                    else                        mc  5                else                    mc  100        else            mc  1        nnn  number  cm        if cm  4            num_incr  4        else            num_incr  cm        n_num_incr  nnn  num_incr        nnn_left  dc  num_incr  (n_num_incr  2 + n_num_incr  2)        nnn_right  nnn - nnn_left        is_flag  nnn_right        if is_flag            cell  Component(nnn_right, options[mc])        else            cell  Component(nnn_right)        return cellIt isnt immediately clear to us how many possible return values thereare. But we can utilize Klara to generate inputs instantly, below is thegenerated test pythonimport contract_testdef test_main_0()    assert contract_test.main(2, 4, 1, 3) is not None    assert contract_test.main(2, 4, -1, 6) is not None    assert contract_test.main(2, 4, 1, 4) is not None    assert contract_test.main(-2, 4, 3, 4) is not None    assert contract_test.main(-1, -1, -1, 2) is not None    assert contract_test.main(0, 0, 0, 3) is not None    assert contract_test.main(0, 0, 0, 6) is not None    assert contract_test.main(0, 0, 0, 4) is not None    assert contract_test.main(-2, 0, 0, 4) is not None    assert contract_test.main(0, 0, 0, 0) is not NoneAbove generated 10 total results, which is product ofnnn_right which have 2 possibilities and mc which have 5 possibilities.Suppose that 10 tests input is too much, and we have determine that theoptions argument to Component is redundant to test, wecan use Klaras custom plugin to selectively determine which part toignore in test generation. Go to [customize coveragestrategy](httpsklara-py.readthedocs.ioenlatestcustomize_coverage_strategy.html) for more information.After we have setup the plugin, Klara will generate following test pythonimport contract_testdef test_main_0()    assert contract_test.main(1, 3, 0, 0) is not None    assert contract_test.main(0, 0, 0, 0) is not NoneWhich is only 2 combinations of nnn_rightBecause Klara cant dynamically execute the code, it will provide extension to specify how to infer specific ast node or user defined type to make Klara smarter. Its described in[extending](httpsklara-py.readthedocs.ioenlatestextending.html), [extending usertype](httpsklara-py.readthedocs.ioenlatestextending_user_type.html) and [customize coveragestrategy](httpsklara-py.readthedocs.ioenlatestcustomize_coverage_strategy.html). ContributingWe use [Poetry](httpspython-poetry.orgdocs) to manage dependencies.After poetry is installed, run     poetry shell     poetry installTo run the test case, do     poetry run pytest test Acknowledgements- The architecture of the inference system is largely inspired by [Astroid](httpsgithub.comPyCQAastroid).- Special thanks to Dr. Poh for guiding the early stages of the project. LicenseThis project is licensed under the terms of the GNU Lesser GeneralPublic License.</longdescription>
</pkgmetadata>