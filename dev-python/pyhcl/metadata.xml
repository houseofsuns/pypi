<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pyhclBuild StatusImplements a parser for HCL (HashiCorp ConfigurationLanguage) httpsgithub.comhashicorphcl__ in Python. Thisimplementation aims to be compatible with the original golang version ofthe parser.The grammar and many of the testsfixtures were copiedported from thegolang parser into pyhcl. All releases are tested with a variety of python versions from Python 2.7 onward.This version has been modified to work with terraform 0.12 syntax.  It should be backward compatible with earlier versions.  It doesnt cover every situation.  See discussion in pull requesthttpsgithub.comvirtualdpyhclpull57Installation    pip install pyhclUsageThis module is intended to be used in mostly the same way that one woulduse the json module in python, and loadloadsdumps are implemented.    import hcl    with open(file.hcl, r) as fp        obj  hcl.load(fp)Currently the dumps function outputs JSON, and not HCL.Convert HCL to JSON-------------------pyhcl comes with a script that you can use to easily convert HCL to JSON,similar to the json.tool that comes with pythonhcltool INFILE [OUTFILE]Structure Validation--------------------Similar to JSON, the output of parsing HCL is a python dictionary withno defined structure. The golang library for HCL implements support forparsing HCL according to defined objects, but this implementation doesnot currently support such constructs.Instead, I recommend that you use tools designed to validate JSON, suchas the schematics httpspypi.python.orgpypischematics_ library. Syntax-  Single line comments start with  or -  Multi-line comments are wrapped in  and -  Values are assigned with the syntax key  value (whitespace   doesnt matter). The value can be any primitive a string, number,   boolean, object, or list.-  Strings are double-quoted and can contain any UTF-8 characters.   Example Hello, World-  Numbers are assumed to be base 10. If you prefix a number with 0x, it   is treated as a hexadecimal. If it is prefixed with 0, it is treated   as an octal. Numbers can be in scientific notation 1e10.-  Boolean values true, false-  Arrays can be made by wrapping it in []. Example   [foo, bar, 42]. Arrays can contain primitives and other   arrays, but cannot contain objects. Objects must use the block syntax   shown below.Objects and nested objects are created using the structure shown below    variable ami {        description  the AMI to use    }TestingTo run the tests    pip install -r testing-requirements.txt    testsrun_tests.sh    Debug Mode----------To enable debug mode    import hcl    hcl.parser.DEBUG  TrueAuthorsDustin Spicuzza (dustinvirtualroadside.com)Note This project is not associated with Hashicorp.. Build Status image httpstravis-ci.orgvirtualdpyhcl.svgbranchmaster   target httpstravis-ci.orgvirtualdpyhcl</longdescription>
</pkgmetadata>