<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>threaded.. image httpstravis-ci.compython-useful-helpersthreaded.svgbranchmaster    target httpstravis-ci.compython-useful-helpersthreaded.. image httpsgithub.compython-useful-helpersthreadedworkflowsPython20packagebadge.svg    target httpsgithub.compython-useful-helpersthreadedactions.. image httpscoveralls.ioreposgithubpython-useful-helpersthreadedbadge.svgbranchmaster    target httpscoveralls.iogithubpython-useful-helpersthreadedbranchmaster.. image httpsreadthedocs.orgprojectsthreadedbadgeversionlatest    target httpsthreaded.readthedocs.io    alt Documentation Status.. image httpsimg.shields.iopypivthreaded.svg    target httpspypi.python.orgpypithreaded.. image httpsimg.shields.iopypipyversionsthreaded.svg    target httpspypi.python.orgpypithreaded.. image httpsimg.shields.iopypistatusthreaded.svg    target httpspypi.python.orgpypithreaded.. image httpsimg.shields.iogithublicensepython-useful-helpersthreaded.svg    target httpsraw.githubusercontent.compython-useful-helpersthreadedmasterLICENSE.. image httpsimg.shields.iobadgecode20style-black-000000.svg    target httpsgithub.comambvblackthreaded is a set of decorators, which wrap functions in   concurrent.futures.ThreadPool   threading.Thread   asyncio.Task in Python 3.Why Because copy-paste of loop.create_task, threading.Thread and thread_pool.submit is boring,especially if target functions is used by this way only.Pros Free software Apache license Open Source httpsgithub.compython-useful-helpersthreaded PyPI packaged httpspypi.python.orgpypithreaded Tested see bages on top Support multiple Python versions    Python 3.4    Python 3.5    Python 3.6    Python 3.7    PyPy3 3.5+.. note For python 2.7PyPy you can use versions 1.x.xDecorators ThreadPooled - native concurrent.futures.ThreadPool. threadpooled is alias for ThreadPooled. Threaded - wrap in threading.Thread. threaded is alias for Threaded. AsyncIOTask - wrap in asyncio.Task. Uses the same API, as ThreadPooled. asynciotask is alias for AsyncIOTask.UsageThreadPooled------------Mostly it is required decorator submit function to ThreadPoolExecutor on call... note    API quite differs between Python 3 and Python 2.7. See API section below... code-block python    threaded.ThreadPooled.configure(max_workers3).. note    By default, if executor is not configured - it configures with default parameters max_workersCPU_COUNT  5.. code-block python    threaded.ThreadPooled    def func()        pass    concurrent.futures.wait([func()])Python 3.5+ usage with asyncio.. note    if loop_getter is not callable, loop_getter_need_context is ignored... code-block python    loop  asyncio.get_event_loop()    threaded.ThreadPooled(loop_getterloop, loop_getter_need_contextFalse)    def func()        pass    loop.run_until_complete(asyncio.wait_for(func(), timeout))Python 3.5+ usage with asyncio and loop extraction from call arguments.. code-block python    loop_getter  lambda tgt_loop tgt_loop    threaded.ThreadPooled(loop_getterloop_getter, loop_getter_need_contextTrue)   loop_getter_need_context is required    def func(args, kwargs)        pass    loop  asyncio.get_event_loop()    loop.run_until_complete(asyncio.wait_for(func(loop), timeout))During application shutdown, pool can be stopped (while it will be recreated automatically, if some component will request)... code-block python    threaded.ThreadPooled.shutdown()Threaded--------Classic threading.Thread. Useful for running until close and self-closing threads without return.Usage example.. code-block python    threaded.Threaded    def func(args, kwargs)        pass    thread  func()    thread.start()    thread.join()Without arguments, thread name will use pattern Threaded  + func.__name__.. note    If func.__name__ is not accessible, str(hash(func)) will be used instead.Override name can be don via corresponding argument.. code-block python    threaded.Threaded(nameFunction in thread)    def func(args, kwargs)        passThread can be daemonized automatically.. code-block python    threaded.Threaded(daemonTrue)    def func(args, kwargs)        passAlso, if no any addition manipulations expected before thread start,it can be started automatically before return.. code-block python    threaded.Threaded(startedTrue)    def func(args, kwargs)        passAsyncIOTask-----------Wrap in asyncio.Task.usage with asyncio.. code-block python    threaded.AsyncIOTask    def func()        pass    loop  asyncio.get_event_loop()    loop.run_until_complete(asyncio.wait_for(func(), timeout))Provide event loop directly.. note    if loop_getter is not callable, loop_getter_need_context is ignored... code-block python    loop  asyncio.get_event_loop()    threaded.AsyncIOTask(loop_getterloop)    def func()        pass    loop.run_until_complete(asyncio.wait_for(func(), timeout))Usage with loop extraction from call arguments.. code-block python    loop_getter  lambda tgt_loop tgt_loop    threaded.AsyncIOTask(loop_getterloop_getter, loop_getter_need_contextTrue)    def func(args, kwargs)        pass    loop  asyncio.get_event_loop()    loop.run_until_complete(asyncio.wait_for(func(loop), timeout))TestingThe main test mechanism for the package threaded is using tox.Available environments can be collected via tox -lCI systemsFor code checking several CI systems is used in parallel1. Travis CI httpstravis-ci.compython-useful-helpersthreaded_ is used for checking PEP8, pylint, bandit, installation possibility and unit tests. Also its publishes coverage on coveralls.2. GitHub actions httpsgithub.compython-useful-helpersthreadedactions_ is used for checking PEP8, pylint, bandit, installation possibility and unit tests.3. coveralls httpscoveralls.iogithubpython-useful-helpersthreaded_ is used for coverage display.CD systemTravis CI httpstravis-ci.compython-useful-helpersthreaded_ is used for package delivery on PyPI.</longdescription>
</pkgmetadata>