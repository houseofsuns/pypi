<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> graphene-django-optimizer[[build status](httpsimg.shields.iotravistfoxygraphene-django-optimizer.svg)](httpstravis-ci.comgithubtfoxygraphene-django-optimizer)[[coverage](httpsimg.shields.iocodecovcgithubtfoxygraphene-django-optimizer.svg)](httpscodecov.ioghtfoxygraphene-django-optimizer)[[PyPI version](httpsimg.shields.iopypivgraphene-django-optimizer.svg)](httpspypi.orgprojectgraphene-django-optimizer)[python version](httpsimg.shields.iopypipyversionsgraphene-django-optimizer.svg)[django version](httpsimg.shields.iopypidjversionsgraphene-django-optimizer.svg)Optimize queries executed by [graphene-django](httpsgithub.comgraphql-pythongraphene-django) automatically, using [select_related](httpsdocs.djangoproject.comen2.0refmodelsquerysetsselect-related), [prefetch_related](httpsdocs.djangoproject.comen2.0refmodelsquerysetsprefetch-related) and [only](httpsdocs.djangoproject.comen2.0refmodelsquerysetsonly) methods of Django QuerySet. Installbashpip install graphene-django-optimizerNote If you are using Graphene V2, please install version 0.8. v0.9 and forward will support only Graphene V3 UsageHaving the following schema based on [the tutorial of graphene-django](httpdocs.graphene-python.orgprojectsdjangoenlatesttutorial-plainhello-graphql-schema-and-object-types) (notice the use of gql_optimizer)py cookbookingredientsschema.pyimport graphenefrom graphene_django.types import DjangoObjectTypeimport graphene_django_optimizer as gql_optimizerfrom cookbook.ingredients.models import Category, Ingredientclass CategoryType(DjangoObjectType)    class Meta        model  Categoryclass IngredientType(DjangoObjectType)    class Meta        model  Ingredientclass Query(graphene.ObjectType)    all_categories  graphene.List(CategoryType)    all_ingredients  graphene.List(IngredientType)    def resolve_all_categories(root, info)        return gql_optimizer.query(Category.objects.all(), info)    def resolve_all_ingredients(root, info)        return gql_optimizer.query(Ingredient.objects.all(), info)We will show some graphql queries and the queryset that will be executed.Fetching all the ingredients with the related categorygraphql{  allIngredients {    id    name    category {        id        name    }  }}py optimized querysetingredients  (    Ingredient.objects    .select_related(category)    .only(id, name, category__id, category__name))Fetching all the categories with the related ingredientsgraphql{  allCategories {    id    name    ingredients {        id        name    }  }}py optimized querysetcategories  (    Category.objects    .only(id, name)    .prefetch_related(Prefetch(        ingredients,        querysetIngredient.objects.only(id, name),    ))) Advanced usageSometimes we need to have a custom resolver function. In those cases, the field cant be auto optimized.So we need to use gql_optimizer.resolver_hints decorator to indicate the optimizations.If the resolver returns a model field, we can use the model_field argumentpyimport grapheneimport graphene_django_optimizer as gql_optimizerclass ItemType(gql_optimizer.OptimizedDjangoObjectType)    product  graphene.Field(ProductType)    gql_optimizer.resolver_hints(        model_fieldproduct,    )    def resolve_product(root, info)         check if user have permission for seeing the product        if info.context.user.is_anonymous()            return None        return root.productThis will automatically optimize any subfield of product.Now, if the resolver uses related fields, you can use the select_related argumentpyimport grapheneimport graphene_django_optimizer as gql_optimizerclass ItemType(gql_optimizer.OptimizedDjangoObjectType)    name  graphene.String()    gql_optimizer.resolver_hints(        select_related(product, shipping),        only(product__name, shipping__name),    )    def resolve_name(root, info)        return {} {}.format(root.product.name, root.shipping.name)Notice the usage of the type OptimizedDjangoObjectType, which enablesoptimization of any single node queries.Finally, if your field has an argument for filtering results,you can use the prefetch_related argument with a functionthat returns a Prefetch instance as the value.pyfrom django.db.models import Prefetchimport grapheneimport graphene_django_optimizer as gql_optimizerclass CartType(gql_optimizer.OptimizedDjangoObjectType)    items  graphene.List(        ItemType,        product_idgraphene.ID(),    )    gql_optimizer.resolver_hints(        prefetch_relatedlambda info, product_id Prefetch(            items,            querysetgql_optimizer.query(Item.objects.filter(product_idproduct_id), info),            to_attrgql_product_id_ + product_id,        ),    )    def resolve_items(root, info, product_id)        return getattr(root, gql_product_id_ + product_id)With these hints, any field can be optimized. Optimize with non model fieldsSometimes we need to have a custom non model fields. In those cases, the optimizer would not optimize with the Django .only() method.So if we still want to optimize with the .only() method, we need to use disable_abort_only optionpyclass IngredientType(gql_optimizer.OptimizedDjangoObjectType)    calculated_calories  graphene.String()    class Meta        model  Ingredient        def resolve_calculated_calories(root, info)        return get_calories_for_ingredient(root.id)class Query(object)    all_ingredients  graphene.List(IngredientType)    def resolve_all_ingredients(root, info)        return gql_optimizer.query(Ingredient.objects.all(), info, disable_abort_onlyTrue)</longdescription>
</pkgmetadata>