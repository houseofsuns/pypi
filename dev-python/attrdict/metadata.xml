<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>AttrDict.. image httpstravis-ci.orgbcjAttrDict.svgbranchmaster  target httpstravis-ci.orgbcjAttrDictbranchmaster.. image httpscoveralls.ioreposbcjAttrDictbadge.pngbranchmaster  target httpscoveralls.iorbcjAttrDictbranchmasterAttrDict is an MIT-licensed library that provides mapping objects that allowtheir elements to be accessed both as keys and as attributes     from attrdict import AttrDict     a  AttrDict({foo bar})     a.foo    bar     a[foo]    barAttribute access makes it easy to create convenient, hierarchical settingsobjects    with open(settings.yaml) as fileobj        settings  AttrDict(yaml.safe_load(fileobj))    cursor  connect(settings.db.credentials).cursor()    cursor.execute(SELECT column FROM table)InstallationAttrDict is in PyPI, so it can be installed directly using     pip install attrdictOr from Github     git clone httpsgithub.combcjAttrDict     cd AttrDict     python setup.py installBasic UsageAttrDict comes with three different classes, AttrMap, AttrDict, andAttrDefault. They are all fairly similar, as they all are MutableMappings (read dictionaries) that allow creating, accessing, and deleting key-valuepairs as attributes.Valid Names-----------Any key can be used as an attribute as long as. The key represents a valid attribute (i.e., it is a string comprised only of   alphanumeric characters and underscores that doesnt start with a number). The key represents a public attribute (i.e., it doesnt start with an   underscore). This is done (in part) so that implementation changes between   minor and micro versions dont force major version changes.. The key does not shadow a class attribute (e.g., get).Attributes vs. Keys-------------------There is a minor difference between accessing a value as an attribute vs.accessing it as a key, is that when a dict is accessed as an attribute, it willautomatically be converted to an Attr object. This allows you to recursivelyaccess keys     attr  AttrDict({foo {bar baz}})     attr.foo.bar    bazRelatedly, by default, sequence types that arent bytes, str, or unicode(e.g., lists, tuples) will automatically be converted to tuples, with anymappings converted to Attrs     attr  AttrDict({foo [{bar baz}, {bar qux}]})     for sub_attr in attr.foo         print(sub_attr.foo)    baz    quxTo get this recursive functionality for keys that cannot be used as attributes,you can replicate the behavior by calling the Attr object     attr  AttrDict({1 {two 3}})     attr(1).two    3Classes-------AttrDict comes with three different objects, AttrMap, AttrDict, andAttrDefault.AttrMapThe most basic implementation. Use this if you want to limit the number ofinvalid keys, or otherwise cannot use AttrDictAttrDictAn Attr object that subclasses dict. You should be able to use thisabsolutely anywhere you can use a dict. While this is probably the class youwant to use, there are a few caveats that follow from this being a dict underthe hood.The copy method (which returns a shallow copy of the mapping) returns adict instead of an AttrDict.Recursive attribute access results in a shallow copy, so recursive assignmentwill fail (as you will be writing to a copy of that dictionary)     attr  AttrDict(foo {})     attr.foo.bar  baz     attr.foo    {}Assignment as keys will still work     attr  AttrDict(foo {})     attr[foo][bar]  baz     attr.foo    {bar baz}If either of these caveats are deal-breakers, or you dont need your object tobe a dict, consider using AttrMap instead.AttrDefaultAt Attr object that behaves like a defaultdict. This allows on-the-fly,automatic key creation     attr  AttrDefault(int, {})     attr.foo + 1     attr.foo    1AttrDefault also has a pass_key option that passes the supplied key to thedefault_factory     attr  AttrDefault(sorted, {}, pass_keyTrue)     attr.banana    [a, a, a, b, n, n]Merging-------All three Attr classes can be merged with eachother or other Mappings using the+ operator. For conflicting keys, the right dicts value will bepreferred, but in the case of two dictionary values, they will berecursively merged     a  {foo bar, alpha {beta a, a a}}     b  {lorem ipsum, alpha {bravo b, a b}}     AttrDict(a) + b    {foo bar, lorem ipsum, alpha {beta a, bravo b, a b}}NOTE AttrDicts add is not commutative, a + b  b + a     a  {foo bar, alpha {beta b, a 0}}     b  {lorem ipsum, alpha {bravo b, a 1}}     b + AttrDict(a)    {foo bar, lorem ipsum, alpha {beta a, bravo b, a }}Sequences---------By default, items in non-string Sequences (e.g. lists, tuples) will beconverted to AttrDicts     adict  AttrDict({list [{value 1}, {value 2}]})     for element in adict.list         element.value    1    2This will not occur if you access the AttrDict as a dictionary     adict  AttrDict({list [{value 1}, {value 2}]})     for element in adict[list]         isinstance(element, AttrDict)    False    FalseTo disable this behavior globally, pass the attribute recursiveFalse tothe constructor     adict  AttrDict({list [{value 1}, {value 2}]}, recursiveFalse)     for element in adict.list         isinstance(element, AttrDict)    False    FalseWhen merging an AttrDict with another mapping, this behavior will be disabledif at least one of the merged items is an AttrDict that has set recursiveto False.LicenseAttrDict is released under a MIT license.</longdescription>
</pkgmetadata>