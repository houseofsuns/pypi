<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># `aircraft_models` Documentation## InstallationInstallation is easy using `pip` and will install all requiredlibraries.```bash$ pip install aircraft-list```## How to use aircraft_list```pythonfrom aircraft_list import aircraft_modelsaircrafts = aircraft_models()```It returns a list of dictionaries which can be addressed as follows:```pythonfor ac in aircrafts:    ac['manufacturer'] #it returns the aircraft manufacturer    ac['model'] #it returns the aircraft model    ac['icao'] #it returns the icao_code designator    ac['type'] #it returns the aircraft type    ac['engine'] #it returns the aircraft engine type    ac['engine_number'] #it returns the aircraft engine number    ac['wake'] #it returns the aircraft wake turbulence category```The list of dictionary is updated as per ICAO DOC 8643 - Aircraft Type Designator</longdescription>
</pkgmetadata>