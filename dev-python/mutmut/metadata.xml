<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>mutmut - python mutation tester.. image httpstravis-ci.orgboxedmutmut.svgbranchmaster    target httpstravis-ci.orgboxedmutmut.. image httpsreadthedocs.orgprojectsmutmutbadgeversionlatest    target httpsmutmut.readthedocs.ioenlatestbadgelatest    alt Documentation Status.. image httpscodecov.ioghboxedmutmutbranchmastergraphbadge.svg  target httpscodecov.ioghboxedmutmut.. image httpsimg.shields.iodiscord767914934016802818.svg  target httpsdiscord.ggcwb9uNtMutmut is a mutation testing system for Python, with a strong focus on easeof use. If you dont know what mutation testing is try starting withthis article httpshackernoon.commutmut-a-python-mutation-testing-system-9b9639356c78_.Some highlight features- Found mutants can be applied on disk with a simple command making it very  easy to work with the results- Remembers work that has been done, so you can work incrementally- Supports all test runners (because mutmut only needs an exit code from the  test command)- If you use the hammett httpsgithub.comboxedhammett_ test runner  you can go extremely fast Theres special handling for this runner  that has some pretty dramatic results.- Can use coverage data to only do mutation testing on covered lines- Battle tested on real libraries by multiple companiesIf you need to run mutmut on a python 2 code base use mutmut 1.5.0. Mutmut1.9.0 is the last version to support python 3.4, 3.5 and 3.6.Install and run---------------You can get started with a simple.. code-block console    pip install mutmut    mutmut runThis will by default run pytest (or unittest if pytest is unavailable)on tests in the tests or test folder andit will try to figure out where the code to mutate lies. Run.. code-block console    mutmut --helpfor the available flags, to use other runners, etc. The recommended way to usemutmut if the defaults arent working for you is to add a block in setup.cfg.Then when you come back to mutmut weeks later you dont have to figure out theflags again, just run mutmut run and it works. Like this.. code-block ini    [mutmut]    paths_to_mutatesrc    backupFalse    runnerpython -m hammett -x    tests_dirtests    dict_synonymsStruct, NamedStructTo use multiple paths either in the paths_to_mutate or tests_dir optionuse a comma or colon separated list. For example.. code-block ini    [mutmut]    paths_to_mutatesrc,src2    tests_dirteststests2You can stop the mutation run at any time and mutmut will restart where youleft off. Its also smart enough to retest only the surviving mutants when thetest suite changes.To print the results run mutmut show. It will give you a list of the mutantsgrouped by file. You can now look at a specific mutant diff with mutmut show 3,all mutants for a specific file with mutmut show pathtofile.py or all mutantswith mutmut show all.You can also write a mutant to disk with mutmut apply 3. You should REALLYhave the file you mutate under source code control and committed before you applya mutantWhitelisting------------You can mark lines like this.. code-block python    some_code_here()   pragma no mutateto stop mutation on those lines. Some cases weve found where you need towhitelist lines are- The version string on your library. You really shouldnt have a test for this P- Optimizing break instead of continue. The code runs fine when mutating break  to continue, but its slower.See also Advanced whitelisting and configuration_Example mutations------------------ Integer literals are changed by adding 1. So 0 becomes 1, 5 becomes 6, etc.-  is changed to - break is changed to continue and vice versaIn general the idea is that the mutations should be as subtle as possible.See __init__.py for the full list.Workflow--------This section describes how to work with mutmut to enhance your test suite.1. Run mutmut with mutmut run. A full run is preferred but if youre just   getting started you can exit in the middle and start working with what you   have found so far.2. Show the mutants with mutmut results3. Apply a surviving mutant to disk running mutmut apply 3 (replace 3 with   the relevant mutant ID from mutmut results)4. Write a new test that fails5. Revert the mutant on disk6. Rerun the new test to see that it now passes7. Go back to point 2.Mutmut keeps a result cache in .mutmut-cache so if you want to make sure yourun a full mutmut run just delete this file.If you want to re-run all survivors after changing a lot of code or even the configuration,you can use for ID in (mutmut result-ids survived) do mutmut run ID done (for bash).You can also tell mutmut to just check a single mutant.. code-block console    mutmut run 3Advanced whitelisting and configuration---------------------------------------mutmut has an advanced configuration system. You create a file calledmutmut_config.py. You can define two functions there init() andpre_mutation(context). init gets called when mutmut starts andpre_mutation gets called before each mutant is applied and tested. You canmutate the context object as you need. You can modify the test command likethis.. code-block python    def pre_mutation(context)        context.config.test_command  python -m pytest -x  + something_elseor skip a mutant.. code-block python    def pre_mutation(context)        if context.filename  foo.py            context.skip  Trueor skip logging.. code-block python    def pre_mutation(context)        line  context.current_source_line.strip()        if line.startswith(log.)            context.skip  Truelook at the code for the Context class for what you can modify. Pleaseopen a github issue if you need help.It is also possible to disable mutation of specific node types by passing the--disable-mutation-types option. Multiple types can be specified by separating themby comma.. code-block console    mutmut run --disable-mutation-typesstring,decoratorInversly, you can also only specify to only run specific mutations with --enable-mutation-types.Note that --disable-mutation-types and --enable-mutation-types are exclusive and cannotbe combined.Selecting tests to run----------------------If you have a large test suite or long running tests, it can be beneficial to narrow the set of tests torun for each mutant down to the tests that have a chance of killing it.Determining the relevant subset of tests depends on your project, its structure, and the metadata that youknow about your tests.mutmut provides information like the file to mutate and coverage contexts httpscoverage.readthedocs.ioencoverage-5.5contexts.html_(if used with the --use-coverage switch).You can set the context.config.test_command in the pre_mutation(context) hook of mutmut_config.py.The test_command is reset after each mutant, so you dont have to explicitly (re)set it for each mutant.This section gives examples to show how this could be done for some concrete use cases.All examples use the default test runner (python -m pytest -x --assertplain).Selection based on source and test layoutIf the location of the test module has a strict correlation with your source code layout, you can simplyconstruct the path to the corresponding test file from context.filename.Suppose your layout follows the following structure where the test file is always located right beside theproduction code.. code-block console    mypackage     production_module.py     test_production_module.py     subpackage         submodule.py         test_submodule.pyYour mutmut_config.py in this case would look like this.. code-block python    import os.path    def pre_mutation(context)        dirname, filename  os.path.split(context.filename)        testfile  test_ + filename        context.config.test_command +   + os.path.join(dirname, testfile)Selection based on importsIf you cant rely on the directory structure or naming of the test files, you may assume that the tests most likelyto kill the mutant are located in test files that directly import the module that is affected by the mutant.Using the ast module of the Python standard library, you can use the init() hook to build a map which test fileimports which module, and then lookup all test files importing the mutated module and only run those.. code-block python    import ast    from pathlib import Path    test_imports  {}    class ImportVisitor(ast.NodeVisitor)        Visitor which records which modules are imported.        def __init__(self) - None            super().__init__()            self.imports  []        def visit_Import(self, node ast.Import) - None            for alias in node.names                self.imports.append(alias.name)        def visit_ImportFrom(self, node ast.ImportFrom) - None            self.imports.append(node.module)    def init()        Find all test files located under the tests directory and create an abstract syntax tree for each.        Let the ImportVisitor find out what modules they import and store the information in a global dictionary        which can be accessed by pre_mutation(context).        test_files  (Path(__file__).parent  tests).rglob(test.py)        for fpath in test_files            visitor  ImportVisitor()            visitor.visit(ast.parse(fpath.read_bytes()))            test_imports[str(fpath)]  visitor.imports    def pre_mutation(context)        Construct the module name from the filename and run all test files which import that module.        tests_to_run  []        for testfile, imports in test_imports.items()            module_name  context.filename.rstrip(.py).replace(, .)            if module_name in imports                tests_to_run.append(testfile)        context.config.test_command + f{ .join(tests_to_run)}Selection based on coverage contextsIf you recorded coverage contexts httpscoverage.readthedocs.ioencoverage-5.5contexts.html_ and usethe --use-coverage switch, you can access this coverage data inside the pre_mutation(context) hookvia the context.config.coverage_data attribute. This attribute is a dictionary in the form{filename {lineno [contexts]}}.Lets say you have used the built-in dynamic context option of Coverage.py by adding the following toyour .coveragerc file.. code-block console    [run]    dynamic_context  test_functioncoverage will create a new context for each test function that you run in the form module_name.function_name.With pytest, we can use the -k switch to filter tests that match a given expression... code-block python    import os.path    def pre_mutation(context)        Extract the coverage contexts if possible and only run the tests matching this data.        if not context.config.coverage_data             mutmut was run without --use-coverage            return        fname  os.path.abspath(context.filename)        contexts_for_file  context.config.coverage_data.get(fname, {})        contexts_for_line  contexts_for_file.get(context.current_line_index, [])        test_names  [            ctx.rsplit(., 1)[-1]   extract only the final part after the last dot, which is the test function name            for ctx in contexts_for_line            if ctx   skip empty strings        ]        if not test_names            return        context.config.test_command + f -k { or .join(test_names)}Pay attention that the format of the context name varies depending on the tool you use for creating the contexts.For example, the pytest-cov plugin uses  as separator between module and test function.Furthermore, not all tools are able to correctly pick up the correct contexts. coverage.py for instance is (at the time of writing)unable to pick up tests that are inside a class when using pytest.You will have to inspect your .coverage database using the Coverage.py API httpscoverage.readthedocs.ioencoverage-5.5api.html_first to determine how you can extract the correct information to use with your test runner.Making things more robustDespite your best efforts in picking the right subset of tests, it may happen that the mutant survives because the test which is ableto kill it was not included in the test set. You can tell mutmut to re-run the full test suite in that case, to verify that thismutant indeed survives.You can do so by passing the --rerun-all option to mutmut run. This option is disabled by default.JUnit XML support-----------------In order to better integrate with CICD systems, mutmut supports thegeneration of a JUnit XML report (using httpspypi.orgprojectjunit-xml).This option is available by calling mutmut junitxml. In order to define howto deal with suspicious and untested mutants, you can use.. code-block console    mutmut junitxml --suspicious-policyignore --untested-policyignoreThe possible values for these policies are- ignore Do not include the results on the report at all- skipped Include the mutant on the report as skipped- error Include the mutant on the report as error- failure Include the mutant on the report as failureIf a failed mutant is included in the report, then the unified diff of themutant will also be included for debugging purposes.</longdescription>
</pkgmetadata>