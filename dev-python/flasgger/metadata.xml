<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Flasgger Easy Swagger UI for your Flask API[[Build Status](httpstravis-ci.comflasggerflasgger.svgbranchmaster)](httpstravis-ci.comflasggerflasgger)[[Code Health](httpslandscape.iogithubrochacbrunoflasggermasterlandscape.svgstyleflat)](httpslandscape.iogithubrochacbrunoflasggermaster)[[Coverage Status](httpscoveralls.ioreposgithubrochacbrunoflasggerbadge.svgbranchmaster)](httpscoveralls.iogithubrochacbrunoflasggerbranchmaster)[[PyPI](httpsimg.shields.iopypivflasgger.svg)](httpspypi.python.orgpypiflasgger) a target_blank hrefhttpswww.paypal.comcgi-binwebscrcmd_donationsampbusinessrochacbruno40gmail2ecomamplcBRampitem_nameFlasggerampno_note0ampcurrency_codeUSDampbnPP2dDonationsBF3abtn_donate_SM2egif3aNonHostedGuestimg altDonate with Paypal srchttpwww.paypalobjects.comen_USibtnbtn_donate_SM.gif a[flasgger](docsflasgger.png)Flasgger is a Flask extension to extract [OpenAPI-Specification](httpsgithub.comOAIOpenAPI-Specificationblobmasterversions2.0.mdoperation-object) from all Flask views registered in your API.Flasgger also comes with [SwaggerUI](httpswagger.ioswagger-ui) embedded so you can access [httplocalhost5000apidocs](localhost5000apidocs) and visualize and interact with your API resources.Flasgger also provides validation of the incoming data, using the same specification it can validates if the data received as as a POST, PUT, PATCH is valid against the schema defined using YAML, Python dictionaries or Marshmallow Schemas.Flasgger can work with simple function views or MethodViews using docstring as specification, or using swag_from decorator to get specification from YAML or dict and also provides SwaggerView which can use Marshmallow Schemas  as specification.Flasgger is compatible with Flask-RESTful so you can use Resources and swag specifications together, take a look at [restful example.](examplesrestful.py)Flasgger also supports Marshmallow APISpec as base template for specification, if you are using APISPec from Marshmallow take a look at [apispec example.](examplesapispec_example.py) Top Contributors[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages0)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks0)[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages1)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks1)[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages2)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks2)[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages3)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks3)[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages4)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks4)[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages5)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks5)[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages6)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks6)[[](httpssourcerer.iofamerochacbrunorochacbrunoflasggerimages7)](httpssourcerer.iofamerochacbrunorochacbrunoflasggerlinks7) Examples and demo appThere are some [example applications](examples) and you can also play with examples in [Flasgger demo app](httpflasgger.pythonanywhere.com) NOTE all the examples apps are also test cases and run automatically in Travis CI to ensure quality and coverage. DockerThe examples and demo app can also be built and run as a Docker imagecontainerdocker build -t flasgger .docker run -it --rm -p 50005000 --name flasgger flasggerThen access the Flasgger demo app at httplocalhost5000 . Installation under your virtualenv doEnsure you have latest setuptoolspip install -U setuptoolsthenpip install flasggeror (dev version)pip install httpsgithub.comrochacbrunoflasggertarballmaster NOTE If you want to use Marshmallow Schemas you also need to run pip install marshmallow apispec Getting started Using docstrings as specificationCreate a file called for example colors.pypythonfrom flask import Flask, jsonifyfrom flasgger import Swaggerapp  Flask(__name__)swagger  Swagger(app)app.route(colorspalette)def colors(palette)    Example endpoint returning a list of colors by palette    This is using docstrings for specifications.    ---    parameters      - name palette        in path        type string        enum [all, rgb, cmyk]        required true        default all    definitions      Palette        type object        properties          palette_name            type array            items              ref definitionsColor      Color        type string    responses      200        description A list of colors (may be filtered by palette)        schema          ref definitionsPalette        examples          rgb [red, green, blue]        all_colors  {        cmyk [cian, magenta, yellow, black],        rgb [red, green, blue]    }    if palette  all        result  all_colors    else        result  {palette all_colors.get(palette)}    return jsonify(result)app.run(debugTrue)Now runpython colors.pyAnd go to [httplocalhost5000apidocs](httplocalhost5000apidocs)You should get[colors](docscolors.png) Using external YAML filesSave a new file colors.ymlyamlExample endpoint returning a list of colors by paletteIn this example the specification is taken from external YAML file---parameters  - name palette    in path    type string    enum [all, rgb, cmyk]    required true    default alldefinitions  Palette    type object    properties      palette_name        type array        items          ref definitionsColor  Color    type stringresponses  200    description A list of colors (may be filtered by palette)    schema      ref definitionsPalette    examples      rgb [red, green, blue]lets use the same example changing only the view function.pythonfrom flasgger import swag_fromapp.route(colorspalette)swag_from(colors.yml)def colors(palette)    ...If you do not want to use the decorator you can use the docstring file shortcut.pythonapp.route(colorspalette)def colors(palette)        file colors.yml        ... Using dictionaries as raw specsCreate a Python dictionary aspythonspecs_dict  {  parameters [    {      name palette,      in path,      type string,      enum [        all,        rgb,        cmyk      ],      required true,      default all    }  ],  definitions {    Palette {      type object,      properties {        palette_name {          type array,          items {            ref definitionsColor          }        }      }    },    Color {      type string    }  },  responses {    200 {      description A list of colors (may be filtered by palette),      schema {        ref definitionsPalette      },      examples {        rgb [          red,          green,          blue        ]      }    }  }}Now take the same function and use the dict in the place of YAML file.pythonapp.route(colorspalette)swag_from(specs_dict)def colors(palette)    Example endpoint returning a list of colors by palette    In this example the specification is taken from specs_dict        ... Using Marshmallow Schemas FIRST pip install marshmallow apispecpythonfrom flask import Flask, jsonifyfrom flasgger import Swagger, SwaggerView, Schema, fieldsclass Color(Schema)    name  fields.Str()class Palette(Schema)    pallete_name  fields.Str()    colors  fields.Nested(Color, manyTrue)class PaletteView(SwaggerView)    parameters  [        {            name palette,            in path,            type string,            enum [all, rgb, cmyk],            required True,            default all        }    ]    responses  {        200 {            description A list of colors (may be filtered by palette),            schema Palette        }    }    def get(self, palette)                Colors API using schema        This example is using marshmallow schemas                all_colors  {            cmyk [cian, magenta, yellow, black],            rgb [red, green, blue]        }        if palette  all            result  all_colors        else            result  {palette all_colors.get(palette)}        return jsonify(result)app  Flask(__name__)swagger  Swagger(app)app.add_url_rule(    colorspalette,    view_funcPaletteView.as_view(colors),    methods[GET])app.run(debugTrue) NOTE take a look at examplesvalidation.py for a more complete example. NOTE when catching arguments in path rule always use explicit types, bad apiusername good apistringusername Using Flask RESTful ResourcesFlasgger is compatible with Flask-RESTful you only need to install pip install flask-restful and thenpythonfrom flask import Flaskfrom flasgger import Swaggerfrom flask_restful import Api, Resourceapp  Flask(__name__)api  Api(app)swagger  Swagger(app)class Username(Resource)    def get(self, username)              This examples uses FlaskRESTful Resource       It works also with swag_from, schemas and spec_dict       ---       parameters         - in path           name username           type string           required true       responses         200           description A single user item           schema             id User             properties               username                 type string                 description The name of the user                 default Steven Wilson                return {username username}, 200api.add_resource(Username, usernameusername)app.run(debugTrue) Auto-parsing external YAML docs and MethodViewsFlasgger can be configured to auto-parse external YAML API docs.  [Set a doc_dir](httpsgithub.comrochacbrunoflasggerblobaaef05c17cc559d01b7436211093463642eb6ae2examplesparsed_view_func.pyL16) in your app.config[SWAGGER] and Swagger will load API docs by looking in doc_dir for YAML files stored by endpoint-name and method-name.  For example, doc_dir .examplesdocs and a file .examplesdocsitemsget.yml will provide a Swagger doc for ItemsView method get.Additionally, when using Flask RESTful per above, by passing parseTrue when constructing Swagger, Flasgger will use  flask_restful.reqparse.RequestParser, locate all MethodViews and parsed and validated data will be stored in flask.request.parsed_data. Handling multiple http methods and routes for a single functionYou can separate specifications by endpoint or methodspythonfrom flasgger.utils import swag_fromapp.route(apistringusername, endpointwith_user_name, methods[PUT, GET])app.route(api, endpointwithout_user_name)swag_from(pathtoexternal_file.yml, endpointwith_user_name)swag_from(pathtoexternal_file_no_user_get.yml, endpointwithout_user_name, methods[GET])swag_from(pathtoexternal_file_no_user_put.yml, endpointwithout_user_name, methods[PUT])def fromfile_decorated(usernameNone)    if not username        return No user    return jsonify({username username})And the same can be achieved with multiple methods in a MethodView or SwaggerView byregistering the url_rule many times. Take a look at examplesexample_app Use the same data to validate your API POST body.Setting swag_froms _validation_ parameter to True will validate incoming data automaticallypythonfrom flasgger import swag_fromswag_from(defs.yml, validationTrue)def post()     if not validate returns ValidationError response with status 400     also returns the validation message.Using swagger.validate annotation is also possiblepythonfrom flasgger import Swaggerswagger  Swagger(app)swagger.validate(UserSchema)def post()        file defs.yml         if not validate returns ValidationError response with status 400     also returns the validation message.Yet you can call validate manuallypythonfrom flasgger import swag_from, validateswag_from(defs.yml)def post()    validate(request.json, UserSchema, defs.yml)     if not validate returns ValidationError response with status 400     also returns the validation message.It is also possible to define validationTrue in SwaggerView and also usespecs_dict for validation.Take a look at examplesvalidation.py for more information.All validation options can be found at httpjson-schema.orglatestjson-schema-validation.html Custom validationBy default Flasgger will use [python-jsonschema](httpspython-jsonschema.readthedocs.ioenlatest)to perform validation.Custom validation functions are supported as long as they meet the requirements - take two, and only two, positional arguments    - the data to be validated as the first and    - the schema to validate against as the second argument - raise any kind of exception when validation fails.Any return value is discarded.Providing the function to the Swagger instance will make it the defaultpythonfrom flasgger import Swaggerswagger  Swagger(app, validation_functionmy_validation_function)Providing the function as parameter of swag_from or swagger.validateannotations or directly to the validate function will force its useover the default validation function for Swaggerpythonfrom flasgger import swag_fromswag_from(spec.yml, validationTrue, validation_functionmy_function)...pythonfrom flasgger import Swaggerswagger  Swagger(app)swagger.validate(Pet, validation_functionmy_function)...pythonfrom flasgger import validate...    validate(        request.json, Pet, defs.yml, validation_functionmy_function) Validation Error handlingBy default Flasgger will handle validation errors by aborting therequest with a 400 BAD REQUEST response with the error message.A custom validation error handling function can be provided tosupersede default behavior as long as it meets the requirements - take three, and only three, positional arguments    - the error raised as the first    - the data which failed validation as the second and    - the schema used in to validate as the third argumentProviding the function to the Swagger instance will make it the defaultpythonfrom flasgger import Swaggerswagger  Swagger(app, validation_error_handlermy_handler)Providing the function as parameter of swag_from or swagger.validateannotations or directly to the validate function will force its useover the default validation function for Swaggerpythonfrom flasgger import swag_fromswag_from(    spec.yml, validationTrue, validation_error_handlermy_handler)...pythonfrom flasgger import Swaggerswagger  Swagger(app)swagger.validate(Pet, validation_error_handlermy_handler)...pythonfrom flasgger import validate...    validate(        request.json, Pet, defs.yml,        validation_error_handlermy_handler)Examples of use of a custom validation error handler function can befound at [example validation_error_handler.py](examplesvalidation_error_handler.py) Get defined schemas as python dictionariesYou may wish to use schemas you defined in your Swagger specs as dictionarieswithout replicating the specification. For that you can use the get_schemamethodpythonfrom flask import Flask, jsonifyfrom flasgger import Swagger, swag_fromapp  Flask(__name__)swagger  Swagger(app)swagger.validate(Product)def post()        post endpoint    ---    tags      - products    parameters      - name body        in body        required true        schema          id Product          required            - name          properties            name              type string              description The products name.              default Guarana    responses      200        description The product inserted in the database        schema          ref definitionsProduct        rv  db.insert(request.json)    return jsonify(rv)...product_schema  swagger.get_schema(product)This method returns a dictionary which contains the Flasgger schema id,all defined parameters and a list of required parameters. HTML sanitizerBy default Flasgger will try to sanitize the content in YAML definitionsreplacing every n with br but you can change this behavioursetting another kind of sanitizer.pythonfrom flasgger import Swagger, NO_SANITIZERapp Flask()swagger  Swagger(app, sanitizerNO_SANITIZER)You can write your own sanitizerpythonswagger  Swagger(app, sanitizerlambda text do_anything_with(text))There is also a Markdown parser available, if you want to be able to renderMarkdown in your specs description use MK_SANITIZER Swagger UI and templatesYou can override the templatesflasggerindex.html in your application andthis template will be the index.html for SwaggerUI. Use flasggerui2templatesindex.htmlas base for your customization.Flasgger supports Swagger UI versions 2 and 3, The version 3 is still experimental but youcan try setting app.config[SWAGGER][uiversion].pythonapp  Flask(__name__)app.config[SWAGGER]  {    title My API,    uiversion 3}swagger  Swagger(app) OpenAPI 3.0 SupportThere is experimental support for OpenAPI 3.0 that should work when using SwaggerUI 3. To use OpenAPI 3.0, set app.config[SWAGGER][openapi] to a version that the current SwaggerUI 3 supports such as 3.0.2.For an example of this that uses callbacks and requestBody, see the [callbacks example](examplescallbacks.py). Externally loading Swagger UI and jQuery JSCSSStarting with Flasgger 0.9.2 you can specify external URL locations for loading the JavaScript and CSS for the Swagger and jQuery libraries loaded in the Flasgger default templates.  If the configuration properties below are omitted, Flasgger will serve static versions it includes - these versions may be older than the current Swagger UI v2 or v3 releases.The following example loads Swagger UI and jQuery versions from unpkg.comswagger_config  Swagger.DEFAULT_CONFIGswagger_config[swagger_ui_bundle_js]  unpkg.comswagger-ui-dist3swagger-ui-bundle.jsswagger_config[swagger_ui_standalone_preset_js]  unpkg.comswagger-ui-dist3swagger-ui-standalone-preset.jsswagger_config[jquery_js]  unpkg.comjquery2.2.4distjquery.min.jsswagger_config[swagger_ui_css]  unpkg.comswagger-ui-dist3swagger-ui.cssSwagger(app, configswagger_config) Initializing Flasgger with default data.You can start your Swagger spec with any default data providing a templatepythontemplate  {  swagger 2.0,  info {    title My API,    description API for my data,    contact {      responsibleOrganization ME,      responsibleDeveloper Me,      email meme.com,      url www.me.com,    },    termsOfService httpme.comterms,    version 0.0.1  },  host mysite.com,   overrides localhost500  basePath api,   base bash for blueprint registration  schemes [    http,    https  ],  operationId getmyData}swagger  Swagger(app, templatetemplate)And then the template is the default data unless some view changes it. Youcan also provide all your specs as template and have no views. Or views inexternal APP. Getting default data at runtimeSometimes you need to get some data at runtime depending on dynamic values ex you want to check request.is_secure to decide if schemes will be https you can do that by using LazyString.pyfrom flask import Flaskfrom flasgger import, Swagger, LazyString, LazyJSONEncoderapp  Flask(__init__) Set the custom Encoder (Inherit it if you need to customize)app.json_encoder  LazyJSONEncodertemplate  dict(    info{        title LazyString(lambda Lazy Title),        version LazyString(lambda 99.9.9),        description LazyString(lambda Hello Lazy World),        termsOfService LazyString(lambda there_is_no_tos)    },    hostLazyString(lambda request.host),    schemes[LazyString(lambda https if request.is_secure else http)],    fooLazyString(lambda Bar))Swagger(app, templatetemplate)The LazyString values will be evaluated only when jsonify encodes the value at runtime, so you have access to Flask request, session, g, etc.. and also may want to access a database. Behind a reverse proxySometimes youre serving your swagger docs behind an reverse proxy (e.g. NGINX).  When following the [Flask guidance](httpflask.pocoo.orgsnippets35),the swagger docs will load correctly, but the Try it Out button points to the wrong place.  This can be fixed with the following codepythonfrom flask import Flask, requestfrom flasgger import Swagger, LazyString, LazyJSONEncoderapp  Flask(__name__)app.json_encoder  LazyJSONEncodertemplate  dict(swaggerUiPrefixLazyString(lambda  request.environ.get(HTTP_X_SCRIPT_NAME, )))swagger  Swagger(app, templatetemplate)  Customize default configurationsCustom configurations such as a different specs route or disabling Swagger UI can be provided to Flasggerpythonswagger_config  {    headers [    ],    specs [        {            endpoint apispec_1,            route apispec_1.json,            rule_filter lambda rule True,   all in            model_filter lambda tag True,   all in        }    ],    static_url_path flasgger_static,     static_folder static,   must be set by user    swagger_ui True,    specs_route apidocs}swagger  Swagger(app, configswagger_config) Extracting DefinitionsDefinitions can be extracted when id is found in spec, examplepythonfrom flask import Flask, jsonifyfrom flasgger import Swaggerapp  Flask(__name__)swagger  Swagger(app)app.route(colorspalette)def colors(palette)    Example endpoint returning a list of colors by palette    ---    parameters      - name palette        in path        type string        enum [all, rgb, cmyk]        required true        default all    responses      200        description A list of colors (may be filtered by palette)        schema          id Palette          type object          properties            palette_name              type array              items                schema                  id Color                  type string        examples          rgb [red, green, blue]        all_colors  {        cmyk [cian, magenta, yellow, black],        rgb [red, green, blue]    }    if palette  all        result  all_colors    else        result  {palette all_colors.get(palette)}    return jsonify(result)app.run(debugTrue)In this example you do not have to pass definitions but need to add id toyour schemas. Python2 CompatibilityVersion 0.9.5. will be the last verison that supports Python2. Please direct discussions to [399](httpsgithub.comflasggerflasggerissues399). </longdescription>
</pkgmetadata>