<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Flask-AppConfigAllows you to configure an application using pre-set methods... code-block python    from flask_appconfig import AppConfig    def create_app(configfileNone)        app  Flask(myapp)        AppConfig(app, configfile)        return appThe application returned by create_app will, in order1. Load default settings from a module called myapp.default_config, if it   exists. (method described in   httpflask.pocoo.orgdocsconfigconfiguring-from-files )2. Load settings from a configuration file whose name is given in the   environment variable MYAPP_CONFIG (see link from 1.).3. Load json or string values directly from environment variables that start   with a prefix of MYAPP_, i.e. setting MYAPP_SQLALCHEMY_ECHOtrue   will cause the setting of SQLALCHEMY_ECHO to be True.Any of these behaviors can be altered or disabled by passing the appropriateoptions to the constructor or init_app().Heroku support--------------Flask-AppConfig supports configuring a number of services throughHerokuConfig.. code-block python    from flask_appconfig import HerokuConfig    def create_app(configfileNone)        app  Flask(myapp)        HerokuConfig(app, configfile)        return appWorks like the example above, but environment variables set by various Herokuaddons will be parsed as json and converted to configuration variablesaccordingly. Forexample, when enabling Mailgunhttpsaddons.heroku.commailgun_, the configuration of Flask-Mailhttppythonhosted.orgFlask-Mail_ will be automatically be set correctly.Using ENV-only----------------If you only want to use the environment-parsing functions of Flask-AppConfig,the appropriate functions are exposed.. code-block python    from flask_appconfig.heroku import from_heroku_envvars    from flask_appconfig.env import from_envvars     from environment variables. note that you need to set the prefix, as     no auto-detection can be done without an app object    from_envvars(app.config, prefixapp.name.upper() + _)     also possible parse heroku configuration values     any dict-like object will do as the first parameter    from_heroku_envvars(app.config)Installation------------Via PyPI httppypi.python.orgpypiflask-appconfig_     pip install flask-appconfigRequires Python 2.7.flask utility-------------If you want to get started quickly without thinking a lot about writing a runscript, the flask utility supports the create_appfactory pattern     flask --appmyapp devThis will import a module myapp, and call myapp.run(debugTrue).Other options can come in handy as well     flask --appmyapp dev -S -p 8000Runs the app on port 8080, with SSL enabled. You can also set the FLASK_APPenvironment variable or set FLASK_APP inside .env and omit the--app parameter.Note that the flask utility is subject to change, as it will conflict withthe CLI functionality of Flask 1.0. The API is currently kept close, but itwill see changes once Flask 1.0 is released.Flask-Debug and Flask-DebugToolbar supportflask automatically activates Flask-Debug_ and Flask-DebugToolbar_ onyour application this allows to have it installed locally while not having toinstall any debug code in production. You can suppress this behavior with the-E--no-flask-debug flag.Note that these features are only enabled if you install either of theseextensions manually they are not dependencies of Flask-Appconfig... _Flask-Debug httpsgithub.commbrflask-debug.. _Flask-DebugToolbar httpsflask-debugtoolbar.readthedocs.orgThoughts on Configuration-------------------------There is a lot of ways to configure a Flask application and often times,less-than-optimal ones are chosen in a hurry.This extension aims to do three things1. Set a standard of doing configuration that is flexible and in-line with   the official docs and (what I consider) good practices.2. Make it as convenient as possible to provide these configuration methods in   an application.3. Auto-configure on Heroku as much as possible without sacrificing 1. and 2.12factor.net http12factor.net_ seems to capture a good amount of goodthoughts on the issue and Flask-Appconfig should aid you in writing anapplication that follows the principles laid out there.Providing defaultsDefaults should be included and overridable, without altering the filecontaining the defaults.Separate code and configurationIt should be possible to install the app to a read-only (possibly system-wide)location, without having to store configuration files (or, even worse,configuration modules) inside its folders.Environment variables and instance folders make this possible. As an addedbenefit, configuration does not need to be stored alongside the code in versioncontrol.No code necessary for most deployments using the factory-method patternWhen deploying with gunicorn, passing myappcreate_app() suffices to createan app instance, no boilerplate code to create the WSGI app should be necessary.Multiple instancesRunning multiple apps inside the same interpreter should also be possible. Whilethis is slightly more complicated and may occasionally violate the no-codeguideline above, its still straightforward by using configuration fileparameters.Development-----------Flask-AppConfig is under conceptional development. The API or semanticsmay change in the future.Send pull requests for more Heroku-apps to be supported. Send feedback via mail.Changelog---------Backwards-incompatible changes, as they were introduced0.11 The flaskdev tool has been replaced with flask. Using the new flask tool auto-reloading will also change by default. If a  syntax error is introduced to the code, the app will try to restart after two  seconds by default, instead of crashing. This can be suppressed with the  --extended-reload 0 flag. If the app import fails, flask will add . to sys.path and try to  to import once again. Experimental commands serve and db have been added.0.4 Environment variables are no longer prefixed with FLASK_ by default, but  rather use APPNAME_ (with APPNAME being the applications name in  uppercase). MYAPP_SETTINGS became MYAPP_CONFIG, default_settings became  default_config.</longdescription>
</pkgmetadata>