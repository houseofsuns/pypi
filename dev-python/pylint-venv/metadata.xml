<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pylint-venvPylint_ does not respect the currently activated virtualenv_ if it is notinstalled in every virtual environment individually.  This module providesa Pylint init-hook to use the same Pylint installation with different virtualenvironments.Installation------------.. code bash    pip install pylint-venvAdd the following to your .pylintrc.. code ini    [MAIN]    init-hook        try import pylint_venv        except ImportError pass        else pylint_venv.inithook()The hook will then be used automatically if- a virtualenv without pylint is active,- or a Conda environment without pylint is active,- or no environment is active but your CWD contains virtualenv directory.Anything listed in the PYLINT_VENV_PATH environment variable is considereda virtualenv directory. The default, if the variable is unset, is .venv. Usea colon () as path separator. Example for checking directories .venv and.virtualenv.. code console    PYLINT_VENV_PATH.venv.virtualenvYou can also call the hook via a command line argument.. code console     pylint --init-hookimport pylint_venv pylint_venv.inithook()This way you can also explicitly set an environment to be used.. code console     pylint --init-hookimport pylint_venv pylint_venv.inithook((pwd)env)If pylint itself is installed in a virtualenv, then you can ignore it by passingforce_venv_activationTrue to force the activation of a different virtualenv.. code console     pylint --init-hookimport pylint_venv pylint_venv.inithook(force_venv_activationTrue)This will try to automatically detect virtualenv and activate it.Troubleshooting---------------Generalpylint_venv fails to importMost likely pylint-venv is not installed in the same virtual environment aspylint. Either make sure to ensure pylint-venv into the same virtual environmentas pylint, or add the appropriate path in the init hook.. code python    import sys    sys.path.append(pathtoinstallationfolderofpylint_venv)pylint_venv breaks parsing with toolsWhen tools call pylint with code-f json, an extra line may break the parser, as the output is no longer valid json. To avoid printing using venv ..., pass codequietTrueto codeinithook.. code console    pylint -f json --init-hookimport pylint_venv pylint_venv.inithook(quietTrue)Virtual environment does not get used (installed modules are reported as unable to import)Most likely the virtual environment does not get activated because pylint itselfruns in a virtual environment. You can force the activation of the virtualenvironment with the codeforce_venv_activationTrue flag to thecodepylint_venv.inithook function.HomebrewHomebrew installs pylint into a separate virtual environment, thus you willneed to set the force_venv_activationTrue flag. This also means, thatpylint_venv will be in a different search path and you must add the properpath to sys.path. You can use the following configuration adjusted to yourPython version.. code ini    [MAIN]    init-hook        import sys        sys.path.append(usrlocallibpython3.8site-packages)        try import pylint_venv        except ImportError pass        else pylint_venv.inithook(force_venv_activationTrue).. _Pylint httpwww.pylint.org.. _virtualenv httpsvirtualenv.pypa.ioenlatest</longdescription>
</pkgmetadata>