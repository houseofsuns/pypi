<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> blake3-py [[Actions Status](httpsgithub.comoconnor663blake3-pyworkflowstestsbadge.svg)](httpsgithub.comoconnor663blake3-pyactions) [[PyPI version](httpsbadge.fury.iopyblake3.svg)](httpspypi.python.orgpypiblake3)Python bindings for the [official Rust implementation ofBLAKE3](httpsgithub.comBLAKE3-teamBLAKE3), based on[PyO3](httpsgithub.comPyO3pyo3). These bindings expose all the features ofBLAKE3, including extendable output, keying, and multithreading. The basic APImatches that of Pythons standard[hashlib](httpsdocs.python.org3libraryhashlib.html) module. Examplespythonfrom blake3 import blake3 Hash some input all at once. The input can be bytes, a bytearray, or a memoryview.hash1  blake3(bfoobarbaz).digest() Hash the same input incrementally.hasher  blake3()hasher.update(bfoo)hasher.update(bbar)hasher.update(bbaz)hash2  hasher.digest()assert hash1  hash2 Hexadecimal output.print(The hash of hello world is, blake3(bhello world).hexdigest()) Use the keyed hashing mode, which takes a 32-byte key.import secretsrandom_key  secrets.token_bytes(32)message  ba message to authenticatemac  blake3(message, keyrandom_key).digest() Use the key derivation mode, which takes a context string. Context strings should be hardcoded, globally unique, and application-specific.context  blake3-py 2020-03-04 111310 example contextkey_material  busually at least 32 random bytes, not a passwordderived_key  blake3(key_material, derive_key_contextcontext).digest() Extendable output. The default digest size is 32 bytes.extended  blake3(bfoo).digest(length100)assert extended[32]  blake3(bfoo).digest()assert extended[75100]  blake3(bfoo).digest(length25, seek75) Hash a large input using multiple threads. Note that this can be slower for inputs shorter than 1 MB, and its a good idea to benchmark it for your use case on your platform.large_input  bytearray(1_000_000)hash_single  blake3(large_input).digest()hash_two  blake3(large_input, max_threads2).digest()hash_many  blake3(large_input, max_threadsblake3.AUTO).digest()assert hash_single  hash_two  hash_many Copy a hasher that has already accepted some input.hasher1  blake3(bfoo)hasher2  hasher1.copy()hasher1.update(bbar)hasher2.update(bbaz)assert hasher1.digest()  blake3(bfoobar).digest()assert hasher2.digest()  blake3(bfoobaz).digest() Installationpip install blake3As usual with Pip, you might need to use sudo or the --user flagwith the command above, depending on how you installed Python on yoursystem.There are binary wheels [available onPyPI](httpspypi.orgprojectblake3files) for most environments. Butif youre building the source distribution, or if a binary wheel isntavailable for your environment, youll need to [install the Rusttoolchain](httpsrustup.rs). C BindingsExperimental bindings for the official BLAKE3 C implementation are available inthe [c_impl](c_impl) directory. These will probably not be published on PyPI,and most applications should prefer the Rust-based bindings. But if you cantdepend on the Rust toolchain, and youre on some platform that this projectdoesnt provide binary wheels for, the C-based bindings might be analternative.</longdescription>
</pkgmetadata>