<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>tryme - Error handling for humansIntroductionTryme is a module that makes it easier to treat errors as values. Additionally,tryme helps you handle the absence of a value (None) in a more composable way.The entire tryme library is contained in a single file tryme.py tomake it as easy as possible to drop into an existing project or script.Why should I care--------------------  Treating errors as values makes it easier to retain information related to errors andlets us defer error handling until the last possible moment. In contrast, exceptions arefundamentally lossy in they by convention contain tracebacks and error messages not values.Further, exceptions abort execution by default and break normal code flow.Here are a few tasks where we might wish to defer error handling. Processing multiple operations in batch where one or more operations may fail Executing long running operations that require multiple retriesTreating failed operations as values rather than exceptions can simplify conditional logic andmake programs more composable.  But cmon exceptions are pythonicA common convention for handling errors in Python is to raise exceptions. Thereis no reason however that this is always the best mechanism to handle errors,especially expected errors. The author of this package feels stronglythat exceptions are best used to represent exceptional circumstancessuch as when undefined behavior is encountered.Error Handling Strategies------------------------------A good error handling strategy retains relevant information and defers sideeffects related to a failure as long as possible to allow upstream code todecide how to handle the failure.Here are four error handling strategies that I have encountered1. Return a boolean to indicate successfailure2. Return a tuple of (err, value) where err represents a possible error and value   is well, a value3. Return a value for success and raise an exception in the case of an error4. Return a value in case of success and a custom Error class that contains information   about the failure but is not necessarily a subclass of Exception5. Return an instance of Success or Failure containing a valueStrategy 1 Return a boolean-------------------------------The simplest possible strategy is to return a boolean to indicate the success orfailure. For a simple example, lets build a trivial program to check if Google is accessible.Our first iteration will simply return a boolean to indicate if Google is accessible.  import requests  def google_is_accessible()      response  requests.get(httpgoogle.com)      return response.ok  is_accessible  google_is_accessible()  if is_accessible      print(It works)  else      print(Google is not accessible. No idea why)Awesome We now can check if Google is accessible from our remote location. The big drawbackis that google_is_accessible doesnt tell me what went wrong in case of a failure nordoes give any information to figure that out for myself.Strategy 2 Return a tuple of (err, value)---------------------------------------------A similar approach is to return a tuple of (err, value) where err indicates if an erroroccurred. This approach is idiomatic in the Go programming language.  import requests  def google_is_accessible()      response  requests.get(httpgoogle.com)      return response.ok, response  is_accessible, response  google_is_accessible()  if is_accessible      print(It works)  else      print(Google is not accessible, received http status s  response.status_code)This is a big improvement We now can determine what went wrong should we care.The main drawback of returning a tuple to indicate errors is that it makes it harder tocompose functions. Lets extend our simple example to try multiple search engines in caseGoogle is not accessible.  import requests  def duck_duck_go_is_accessible()      response  requests.get(httpgoogle.com)      return response.ok, response    def google_is_accessible()      response  requests.get(httpgoogle.com)      return response.ok, response        is_accessible, response  google_is_accessible()  if is_accessible      print(It works Using Google)  else      print(Google is not accessible, received http status s. Trying duckduckgo             response.status_code))      is_accessible, response  duck_duck_go_is_accessible()      if is_accessible           print(It works Using DuckDuckGo)      else           print(DuckDuckGo is not accessible, received http status s. Out of options                  response.status_code))The conditionals in the above example can be reduced but it is apparent that returning a tupleadds more conditional logic to your code.Strategy 3 Raise an Exception---------------------------------Here is our example using good old tryexcept  import requests  class GoogleNotAccessibleError(Exception)      pass      def google_is_accessible()      response  requests.get(httpgoogle.com)      if response.ok          return response.ok      else          return GoogleNotAccessibleError(                http request to google.com failed with status code s  response.status_code)   try       is_accessible  google_is_accessible()   except GoogleNotAccessibleError as e       is_accessible  False       print(e.message)There are pros and cons to the above. We do get back some useful information about the failure.Unfortunately, we do not get back the response object so we cannot dig deeper into the responseto determine the cause of the error. To get the HTTP status code we have search the error message.Another drawback is that the raised exception is a side effect that we have tohandle immediately and cannot be deferred until later. Raising an exception alsogenerates something we dont need, a stacktrace.One big positive here is that we can subclass exception to indicate the particular problem thatoccurred.Strategy 4 Return a custom Error in case of Failure------------------------------------------------------Instead of raising an Exception, you can simply return an Error in case offailure where Error is an object that is an exception or looks a lot like one.  import requests  class GoogleNotAccessibleError()      def __init__(self, message, response)          self.message  message          self.response  response    def google_is_accessible()      response  requests.get(httpgoogle.com)      if response.ok          return response.ok      else          return GoogleNotAccessibleError(                http request to google.com failed, response)   result  google_is_accessible()   if result is True       print(It worked)   else       print(result.message)       print(HTTP request failed with status code d result.value.status_code)This is a big improvement We can quickly determine if google is accessible and haveaccess to all the information in the request. The main drawback to returning a customerror is that each implementation is likely custom. The calling code hasto know the internals of the returned Error class.Strategy 5 Return an instance of Success or Failure containing a value-------------------------------------------------------------------------This final strategy refines the custom Error with standard semantics. As it turns out therea standard paradigm in the Either class httpswww.ibm.comdeveloperworkslibraryj-ft13index.html_ that is present in Clojure, Scala, and other languages. This packageimplements the Either class under the name Try as your dear author believes itis a more intuitive name.The Try class has two subclasses, Success and Failure. Success is used tocontain the result of an operation that-you guessed it-succeeded. Likewise, Failurecontains the result of an operation that failed.Here is the same task using the Success and Failure  import requests  from tryme import Success, Failure  def google_is_accessible()      response  requests.get(httpgoogle.com)      if response.ok          return Success(response)      else          return Failure(response)   result  google_is_accessible()   if result.succeeded()       print(it worked)   else       response  result.get_failure()       print(HTTP request failed with status d  response.status_code)We noted earlier that an advantage of returning exceptions is that we can subclassException to more specifically indicate the failure. We can do the same withSuccess in Failure. One obvious omission from our google_is_accessible isthat it does not account for a network failure.  import requests  from tryme import Success, Failure  class ConnectionFailure(Failure)      pass          def google_is_accessible()      try          response  requests.get(httpgoogle.com)      except requests.exceptions.ConnectionError as e          return ConnectionFailure(e.message)      if response.ok          return Success(response)      else          return Failure(response)   result  google_is_accessible()   if result.succeeded()       print(it worked)   elif isinstance(result, ConnectionFailure)       print(result.get_message())   else       response  result.get_failure()       print(HTTP request failed with status d  response.status_code)Note that while we return a custom Failure in this case there are many cases where itis quite reasonable to raise an exception. As mentioned earlier, exceptions work wellfor unexpected behavior and not expected behavior.Success and Failure have some useful helpers for reporting to the terminal.The constructors for both Success and Failure take the optional argument messageto capture a message intended for the end user. the to_console method prints themessage to the terminal if it is not None otherwise prints a a string representationof the contained value. Success.to_console prints the message if set otherwise prints a string representation of  the contained value to stdout Failure.to_console prints the message if set otherwise prints a string representation of  the contained value to stderr Try.raise_for_error raise an exception if the instance is a Failure Try.fail_for_error if a Failure, print the message and exit with a non-zero return code  Retrying with Style---------------------------------------------------Lets say we want to create a single server using a new Cloud computing providernamed HighlyVariable Inc. HighlyVariable can provision our new server in a fewseconds, several minutes, or occasionally not at all. Your dear author has usedcloud services where the not at all is not so uncommon an outcomeLets create a server_ready function that returns a Success when the serveris ready, a Failure when the operation times out. A terminal state such asReady or Failed will terminate retries immediately whereas a Failed willcontinue execution of the server_ready function until 300 seconds after thefunction was first called.If our new server is not ready after 300 seconds, server_ready will return aninstance of Failure.   from tryme import retry, Success, Failure      def create_server(name)       return {Name name}   status_iterator  iter([Preparing, Preparing, Preparing, Ready])      def check_instance_status(name)       return next(status_iterator)        retry   def wait_for_server_ready_or_failed(name)       status  check_instance_status(name)       if status in [Ready, Failed]           return Success(status)       else           return Failure(status)              def server_ready(name)        the decorated function will return two values,        the result of wrapped function is updated with start and end times of the        retry loop and the total count of attempts        note that the wrapped value is not modified       result  wait_for_server_ready_or_failed(name)        a failure here only indicates a timeout       if result.failed()           return Failure(Server s not ready after d seconds and d attempts                           (name, result.elapsed, result.count))        unwrap the value to see what really happened       status  result.get()       if status  Ready           return Success(server s is ready after d seconds and d attempts                           (name, result.elapsed, result.count))       else           return Failure(server s failed after d seconds                           (name, result.elapsed))   result  server_ready(jenkins)   assert result.succeeded()   print(Server jenkins is ready after d seconds and d attempts          (result.elapsed, result.count))   There something a little weird about the above example. Why did we returnSuccess when the status was Failed This is because the return value ofFailure in the wrapped function is a signal to the retry decorator tocontinue retrying until the timeout is reached. As noted earlier, youcan subclass Success and Failure with names that make more sense for your context.Tryme in fact comes with two subclasses pyclassStop and pyclassAgain. Letsrefactor the previous example to use them.   from tryme import retry, Success, Failure, Stop, Again   def create_server(name)       return {Name name}   status_iterator  iter([Preparing, Preparing, Preparing, Ready])   def check_instance_status(name)       return next(status_iterator)   retry   def wait_for_server_ready_or_failed(name)       status  check_instance_status(name)       if status in [Ready, Failed]           return Stop(status)       else           return Again(status)   def server_ready(name)        the decorated function will return two values,        the result of wrapped function is updated with start and end times of the        retry loop and the total count of attempts        note that the wrapped value is not modified       result  wait_for_server_ready_or_failed(name)        a failure here only indicates a timeout       if result.failed()           return Failure(Server s not ready after d seconds and d attempts                           (name, result.elapsed, result.count))        unwrap the value to see what really happened       status  result.get()       if status  Ready           return Success(server s is ready after d seconds and d attempts                           (name, result.elapsed, result.count))       else           return Failure(server s failed after d seconds                           (name, result.elapsed))   result  server_ready(jenkins)   assert result.succeeded()   print(Server jenkins is ready after d seconds and d attempts          (result.elapsed, result.count))       Utility methods--------------------       The utility method try_out executes a callable and wraps a raised exceptionin a Failure class. If an exception was not raised, a Success is returned   from tryme import try_out   result  try_out(lambda 1  0)   print(result)   doctest +SKIP  Failure(ZeroDivisionError(integer division or modulo by zero,))   exc  result.get_failure()   exc  doctest +SKIP  ZeroDivisionError(integer division or modulo by zero,)    the following would fail as it does not catch the correct exception, ZeroDivisionError    result  try_out(lambda 1  0, exceptionValueError)   result  try_out(lambda 1  1)   print(result)  doctest +SKIP  Success(1)   result.get()  doctest +SKIP  1Requirements- CPython  2.7BackgroundThis package is inspired by Philip Xus excellent monad package httpsgithub.compyxmonad_.It also takes some inspiration from the excellent vavr httpsvavr.io_ library for java and the Scala language.See this excellent tutorial httpdanielwestheide.comblog20121226the-neophytes-guide-to-scala-part-6-error-handling-with-try.html_on the Try utility in Scala.Pssssh This package uses gasp monads as the core abstraction. Dont tellanyone They will sick the procedural programming police on your dear author. While thispackage does have a Monad class, it does not provide general purposeimplementations of monad, applicative, and functor. Further it does notattempt to overload common Python operators to support function composition. This isbecause your dear author is of the opinion that Pythons syntax is too limitedto support monadic composition.InstallationInstall from PyPI  pip install trymeInstall from source, download source package, decompress, then cd into source directory, run  make installLicenseBSD New, see LICENSE for details.LinksDocumentation  httptryme.readthedocs.orgIssue Tracker  httpsgithub.combryanwbtrymeissuesSource Package  PyPI  httpspypi.python.orgbryanwbtrymeGit Repository  Github  httpsgithub.combryanwbtryme</longdescription>
</pkgmetadata>