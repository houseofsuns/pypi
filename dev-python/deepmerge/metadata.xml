<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>deepmerge.. image httpsgithub.comtoumorokoshideepmergeactionsworkflowspython-package.yamlbadge.svg    target httpsgithub.comtoumorokoshideepmergeactionsworkflowspython-package.yamlA tools to handle merging ofnested data structures in python.------------Installation------------deepmerge is available on pypi httpspypi.python.org_.. code-block bash   pip install deepmerge-------Example-------Generic Strategy.. code-block python    from deepmerge import always_merger    base  {foo [bar]}    next  {foo [baz]}    expected_result  {foo [bar, baz]}    result  always_merger.merge(base, next)    assert expected_result  resultCustom Strategy.. code-block python    from deepmerge import Merger    my_merger  Merger(         pass in a list of tuple, with the         strategies you are looking to apply         to each type.        [            (list, [append]),            (dict, [merge]),            (set, [union])        ],         next, choose the fallback strategies,         applied to all other types        [override],         finally, choose the strategies in         the case where the types conflict        [override]    )    base  {foo [bar]}    next  {bar baz}    my_merger.merge(base, next)    assert base  {foo [bar], bar baz}You can also pass in your own merge functions, instead of a string.For more information, see the docs httpsdeepmerge.readthedocs.ioenlatest_</longdescription>
</pkgmetadata>