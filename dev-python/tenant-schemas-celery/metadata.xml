<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>tenant-schemas-celery Celery application implementation that allows celery tasks to cooperate withmulti-tenancy provided by [django-tenant-schemas](httpsgithub.combernardopiresdjango-tenant-schemas) and[django-tenants](httpsgithub.comtomturnerdjango-tenants) packages.This project might not seem frequently updated, but it just has all the functionality needed. Issues and questions are answered quickly.Installation------------bash    pip install tenant-schemas-celery    pip install django-tenantsUsage-----    Define a celery app using given CeleryApp class.python   import os   os.environ.setdefault(DJANGO_SETTINGS_MODULE, app.settings)   from django.conf import settings   from tenant_schemas_celery.app import CeleryApp as TenantAwareCeleryApp   app  TenantAwareCeleryApp()   app.config_from_object(django.confsettings)   app.autodiscover_tasks(lambda settings.INSTALLED_APPS)This assumes a fresh Celery 4.3.0 application. For previous versions, the key is to create a new CeleryApp instance that will be used to access task decorator from.    Replace your task decorator with app.taskpython   from django.db import connection   from myproject.celery import app   app.task   def my_task()      print(connection.schema_name)    Run celery worker (myproject.celery is where youve defined the app variable)bash     celery worker -A myproject.celery    Post registered task. The schema name will get automatically added to the tasks arguments.python   from myproject.tasks import my_task   my_task.delay()The TenantTask class transparently inserts current connections schema intothe tasks kwargs. The schema name is then popped from the tasks kwargs intask_prerun signal handler, and the connections schema is changedaccordingly. Multiple databases supportNew in 2.0.0.Inside your celery tasks you might be working with multiple databases. You might want to change the schema forall of the connections, or just a subset of them.You can now use the CELERY_TASK_TENANT_CACHE_SECONDS django setting, or TASK_TENANT_CACHE_SECONDS celery setting, orthe tenant_databases attribute of the TenantTask to a list of database names (the key in the settings.DATABASES dictionary).If not set, the settings defaults to [default]. Tenant objects cacheNew in 0.3.0.Every time a celery task is executed, the tenant object of the connection object is being refetched.For some use cases, this can introduce significant performance hit.In such scenarios, you can pass tenant_cache_seconds argument to the app.task() decorator. This willcause the tenant objects to be cached for given period of time. 0 turns this off. You can also enable cache globallyby setting celerys TASK_TENANT_CACHE_SECONDS (app-specific, usually its CELERY_TASK_TENANT_CACHE_SECONDS).pythonapp.task(tenant_cache_seconds30)def some_task()    ...Celery beat integration-----------------------This package does not provide support for scheduling periodic tasks inside given schema. Instead, you can use {django_tenants,django_tenants_schemas}.utils.{get_tenant_model,tenant_context} methods to launch given tasks within specific tenant.Lets say that you would like to run a reset_remaining_jobs tasks periodically, for every tenant that you have. Instead of scheduling the task for each schema separately, you can schedule one dispatcher task that will iterate over all schemas and send specific task for each schema you want, insteadpythonfrom django_tenants.utils import get_tenant_model, tenant_contextfrom django_tenant_schemas.utils import get_tenant_model, tenant_contextapp.taskdef reset_remaining_jobs_in_all_schemas()    for tenant in get_tenant_model().objects.exclude(schema_namepublic)        with tenant_context(tenant)            reset_remaining_jobs_in_schema.delay()app.taskdef reset_remaining_jobs_in_schema()    do some logicThe reset_remaining_jobs_in_all_schemas task (called the dispatch task) should be registered in your celery beat schedule. The reset_remaining_jobs_in_schema task should be called from the dispatch task.That way you have full control over which schemas the task should be scheduled in.Python compatibilityThe 2.x series support Python3.7.The 1.x series support Python3.6. Python 3.6 reached EOL 2021-12.The 0.x series are the last one to support Python3.6.</longdescription>
</pkgmetadata>