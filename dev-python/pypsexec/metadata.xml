<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Python PsExec Library[[Test workflow](httpsgithub.comjborean93pypsexecactionsworkflowsci.ymlbadge.svg)](httpsgithub.comjborean93pypsexecactionsworkflowsci.yml)[[codecov](httpscodecov.ioghjborean93pypsexecbranchmastergraphbadge.svgtokenHi2Nk4RfMF)](httpscodecov.ioghjborean93pypsexec)[[PyPI version](httpsbadge.fury.iopypypsexec.svg)](httpsbadge.fury.iopypypsexec)[[License](httpsimg.shields.iobadgelicense-MIT-blue.svg)](httpsgithub.comjborean93pypsexecblobmasterLICENSE)This library can run commands on a remote Windows host through Python. Thismeans that it can be run on any host with Python and does not require anybinaries to be present or a specific OS. It uses SMBRPC to executable commandsin a similar fashion to the popular PsExec tool. More details on this toolcan be read on[this blog post](httpswww.bloggingforlogging.com20180312introducing-psexec-for-python).The executable wrapper that is sent to the service is based on the[PAExec](httpsgithub.compoweradminllcPAExec) library. PAExec is an free,redistributable and open source equivalent to Microsofts[PsExec](httpsdocs.microsoft.comen-ussysinternalsdownloadspsexec)application. This program is stored as a binary in this package and is usedto run the remote service and start the process execution.I would like to thank the developers of Power Admin for creating this libraryas it has made this library a lot less complex than what it would have been. FeaturesWith pypsexec you can run commands of a remote Windows host like you would withPsExec. Current you can use pypsexec to do the following Run as a specific local or domain user or the user Run as the local SYSTEM account Run as an interactive process Specify the session the interactive process should run on Specify the run level of the user token, highest or limited Set the priority of the process Set a timeout for the remote process Send input through the stdin pipe to the running process Set the processors the process can run on Further InfoWhile this info is not necessary for you to use this library it can help peopleunderstand what is happening under the hood. This library runs the followingsteps when running a command Create an SMB connection to the host Copies across the PAExec binary to the ADMIN share of the remote host Binds the Windows Service Manager to the opened IPC tree using RPC Creates and starts a Windows service as the SYSTEM account to run the binary copied Connect to the PAExec named pipe the service creates Sends the process details to the PAExec service through the pipe Send a request to the PAExec service to start the process based on the settings sent Connect to the newly spawned processs stdout, stderr, stdin pipe (if not interactive or async) Read the stdoutstderr pipe until the process is complete Get the return code of the new process Stop and remove the PAExec service Remove the PAExec binary from the ADMIN share Disconnects from the SMB connectionIn the case of a failed process, the PAExec service and binary may not beremoved from the host and may need to be done manually. This is only the casefor a critical error or the cleanup functions not being called.By default the data being sent to and from the server is encrypted to stoppeople listening in on the network from snooping your data. Unfortunately thisuses SMB encryption which was added in the SMB 3.x dialects so hosts runningWindows 7, Server 2008, or Server 2008 R2 will not work with encryption.This means that any data sent over the wire on these older versions of Windowsis viewable by anyone reading those packets. Any input or output of the processcomes through these packets so any secrets sent over the network wont beencrypted. PAExec tries to reduce this risk by doing a simple XOR scramble ofthe settings set in run_executable so it isnt plaintext but it can bedecoded by someone who knows the protocol. Requirements Python 3.6+ [smbprotocol](httpsgithub.comjborean93smbprotocol)To install pypsexec, simply runbashpip install pypsexecThis will download the required packages that are required and get yourPython environment ready to do.Out of the box, pypsexec supports authenticating to a Windows host with NTLMauthentication but users in a domain environment can take advantage of Kerberosauthentication as well for added security. The Kerberos libraries are anoptional install which can be installed withbash for DebianUbuntuetcsudo apt-get install gcc python-dev libkrb5-devpip install smbprotocol[kerberos] for RHELCentOSetcsudo yum install gcc python-devel krb5-devel krb5-workstation python-develpip install smbprotocol[kerberos] Remote Host RequirementsThe goal of this package to be able to run executables on a vanilla remoteWindows host with as little setup as possible. Unfortunately there is stillsome setup required to get working depending on the OS version and typethat is being used. What pypsexec requires on the host is SMB to be up and running on the Windows port and readable from the Python host The ADMIN share to be enabled with readwrite access of the user configured The above usually means the configured user is an administrator of the Windows host At least SMB 2 on the host (Server 2008 and newer) The connection user has a full logon token that is not filtered by UAC If connecting to localhost and pywin32 is installed, the script must be run as a user with Administrator privileges Firewall SetupBy default, Windows blocks the SMB port 445 and it needs to be opened up beforepypsexec can connect to the host. To do this run either one of the followingcommandspowershell PowerShell (Windows 8 and Server 2012 or Newer)Set-NetFirewallRule -Name FPS-SMB-In-TCP -Enabled True CMD (All OSs)netsh advfirewall firewall set rule nameFile and Printer Sharing (SMB-In) dirin new enableYesThis will open up inbound traffic to port 445 which is used by SMB. User Account ControlIn some circumstances, UAC will filter any remote logon token and limit therights that are available to it. This causes issues with pypsexec and it willfail with an ACCESS_IS_DENIED error message when trying to interact with theremote SCMR API. This restriction is enforced in various different scenariosand to get it working with pypsexec you can either In a domain environment, use any domain account that is a member of the local Administrators group Use any local account that is a member of the local Administrators group if [LocalAccountTokenFilterPolicy](httpssupport.microsoft.comen-ushelp951016description-of-user-account-control-and-remote-restrictions-in-windows) is set to 1     This means any remote logon token will not be filtered and will have the full rights of that user     By default this is not defined and needs to be created     This only affects remote tokens, any local tokensprocesses will still be limited as per usual Use the builtin local Administrator account (SID S-1-5-21--500) that is created when Windows was installed     The builtin Administrator account for English installs is typically called Administrator but it can be renamed     This account is typically disabled by default on the desktop variants of Windows, e.g. Windows 7, 8.1, 10     When [AdminApprovalMode](httpsdocs.microsoft.comen-usprevious-versionswindowsit-prowindows-server-2008-R2-and-2008dd835564(vws.10)BKMK_BuiltInAdmin) is Enabled this will not work. AdminApprovalMode is not Enabled by default Use any local account that is a member of the local Administrators group if [EnableLUA](httpsdocs.microsoft.comen-uswindows-hardwarecustomizedesktopunattendmicrosoft-windows-lua-settings-enablelua) is Disabled     Unlike the LocalAccountTokenFilterPolicy option, this affects local tokens and processes spawned locally     This effectively disables UAC for any Administrator accounts and should be avoidedTo set LocalAccountTokenFilterPolicy to allow a full token on a remote logon,run the following PowerShell commandspowershellreg_path  HKLMSOFTWAREMicrosoftWindowsCurrentVersionPoliciesSystemreg_prop_name  LocalAccountTokenFilterPolicyreg_key  Get-Item -Path reg_pathreg_prop  reg_key.GetValue(reg_prop_name)if (null -ne reg_prop) {    Remove-ItemProperty -Path reg_path -Name reg_prop_name}New-ItemProperty -Path reg_path -Name reg_prop_name -Value 1 -PropertyType DWordTo get the name of the builtin Administrator (SID S-1-5-21--500), you canrun the following PowerShell commandspowershellAdd-Type -AssemblyName System.DirectoryServices.AccountManagementprincipal_context  New-Object -TypeName System.DirectoryServices.AccountManagement.PrincipalContext([System.DirectoryServices.AccountManagement.ContextType]Machine)user_principal  New-Object -TypeName System.DirectoryServices.AccountManagement.UserPrincipal(principal_context)searcher  New-Object -TypeName System.DirectoryServices.AccountManagement.PrincipalSearcher(user_principal)users  searcher.FindAll()  Where-Object { _.Sid -like -500 }users[0].NameThe last resort would be to disable UAC for any local Administrator account.Once again this should be avoided as there are other options available and thiswill reduce the security of your Windows host, but to do so you can run thefollowing PowerShell commandspowershellreg_path  HKLMSOFTWAREMicrosoftWindowsCurrentVersionPoliciesSystemreg_prop_name  EnableLUAreg_key  Get-Item -Path reg_pathreg_prop  reg_key.GetValue(reg_prop_name)if (null -ne reg_prop) {    Remove-ItemProperty -Path reg_path -Name reg_prop_name}New-ItemProperty -Path reg_path -Name reg_prop_name -Value 0 -PropertyType DWordAfter changing the EnableLUA setting, the Windows host needs to be rebootedbefore the policies are enacted. ExamplesHere is an example of how to run a command with this librarypythonfrom pypsexec.client import Client creates an encrypted connection to the host with the username and passwordc  Client(hostname, usernameusername, passwordpassword) set encryptFalse for Windows 7, Server 2008c  Client(hostname, usernameusername, passwordpassword, encryptFalse) if Kerberos is available, this will use the default credentials in the credential cachec  Client(hostname) you can also tell it to use a specific Kerberos principal in the cache without a passwordc  Client(hostname, usernameusernameDOMAIN.LOCAL)c.connect()try    c.create_service()     After creating the service, you can run multiple exes without     reconnecting     run a simple cmd.exe program with arguments    stdout, stderr, rc  c.run_executable(cmd.exe,                                          argumentsc echo Hello World)     run whoami.exe as the SYSTEM account    stdout, stderr, rc  c.run_executable(whoami.exe, use_system_accountTrue)     run command asynchronously (in background), the rc is the PID of the spawned service    stdout, stderr, rc  c.run_executable(longrunning.exe,                                          argumentss other args,                                          asynchronousTrue)     run whoami.exe as a specific user    stdout, stderr, rc  c.run_executable(whoami,                                          argumentsall,                                          usernamelocal-user,                                          passwordpassword,                                          run_elevatedTrue)finally    c.remove_service()    c.disconnect()In the case of a fatal failure, this project may leave behind some the PAExecpayload in CWindows or the service still installed. As these are uniquelynamed they can build up over time. They can be manually removed but you canalso use pypsexec to cleanup them all up at once. To do this runpythonfrom pypsexec.client import Clientc  Client(server, usernameusername, passwordpassword)c.connect()c.cleanup()   this is where the magic happensc.disconnect()The script will delete any files that match CWindowsPAExec- and anyservices that match PAExec-. For an individual run, the remove_service()function should still be used. Client OptionsWhen creating the main pypsexec Client object there are some configurationoptions that can be set to control the process. These args are server This needs to be set and is the host or IP address of the server to connect to username The username to connect with. Can be None if python-gssapi is installed and a ticket has been granted in the local credential cache password The password for username. Can be None if python-gssapi is installed and a ticket has been granted for the user specified port Override the default port of 445 when connecting to the server encrypt Whether to encrypt the messages or not, default is True. Server 2008, 2008 R2 and Windows 7 hosts do not support SMB Encryption and need this to be set to False Run Executable OptionsWhen calling run_executable, there are multiple kwargs that can definehow the remote process will work. These args are executable (string) The path to the executable to be run arguments (string) Arguments for the executable processors (listint) A list of processor numbers that the process can run on asynchronous (bool) Doesnt wait until the process is complete before returning. The rc returned by the function is the PID of the async process, default is False load_profile (bool) Load the users profile, default is True interactive_session (int) The session ID to display the interactive process when interactiveTrue, default is 0 interactive (bool) Runs the process as an interactive process. The stdout and stderr buffers will be None if True, default False run_elevated (bool) When username is defined, will elevated permissions, default False run_limited (bool) When username is defined, will run the process under limited permissions, default False username (string) Used to run the process under a different user than the one that authenticated the SMB session password (string) The password for username use_system_account (bool) Run the process as NT AUTHORITYSYSTEM working_dir (string) The working directory of the process, default CWindowsSystem32 show_ui_on_win_logon (bool) Displays the UI on the Winlogon secure desktop when use_system_accountTrue, default False priority (pypsexec.ProcessPriority) The priority level of the process, default NORMAL_PRIORITY_CLASS remote_log_path (string) A path on the remote host to log the PAExec service details timeout_seconds (int) The maximum time the process can run for, default is 0 (no timeout) stdout (pipe.OutputPipe) A class that implements pipe.OutputPipe that controls how the stdout output is processed and returned, will default to returning the byte string of the stdout. Is ignored when interactiveTrue and asynchronousTrue stderr (pipe.OutputPipe) A class that implements pipe.OutputPipe that controls how the stderr output is processed and returned, will default to returning the byte string of the stderr. Is ignored when interactiveTrue and asynchronousTrue stdin (bytesgenerator) A byte string or generator that yields a byte string to send over the stdin pipe, does not work with interactiveTrue and asynchronousTrue wow64 (bool) Set to True to run the executable in 32-bit mode on 64-bit systems. This does nothing on 32-bit systems, default False LoggingThis library uses the builtin Python logging library and can be used to findout what is happening in the pypsexec process. Log messages are logged to thepypsexec named logger as well as pypsexec. where  is each python scriptin the pypsexec directory.A way to enable the logging in your scripts through code is to add thefollowing to the top of the script being usedpythonimport logginglogger  logging.getLogger(pypsexec)logger.setLevel(logging.DEBUG)   set to logging.INFO if you dont want DEBUG logsch  logging.StreamHandler()ch.setLevel(logging.DEBUG)formatter  logging.Formatter((asctime)s - (name)s - (levelname)s -                               (message)s)ch.setFormatter(formatter)logger.addHandler(ch)These logs are generally useful when debugging issues as they give you a morestep by step snapshot of what it is doing and what may be going wrong. Thedebug level will also print out a human readable string of each SMB packet thatis sent out from the client but this level can get really verbose. TestingTo this module, you need to install some pre-requisites first. This can be doneby runningbashpip install -r requirements-test.txt you can also run tox by installing toxpip install toxFrom there to run the basic tests runbashpy.test -v --cov pypsexec --cov-report term-missing or with toxtoxThere are extra tests that only run when certain environment variables are set.To run these tests set the following variables PYPSEXEC_SERVER The hostname or IP to a Windows host PYPSEXEC_USERNAME The username to use authenticate with PYPSEXEC_PASSWORD The password for PYPSEXEC_USERNAMEFrom there, you can just run tox or py.test with these environmentvariables to run the integration tests. FutureSome things I would be interested in looking at adding in the future would be Add a Python script that can be called to run adhoc commands like PsExec.exe</longdescription>
</pkgmetadata>