<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>## Installation`pip install volprofile`## Volume Profile Analysis PackageThis package provides functions for analyzing volume profile data using Python. The package includes the following functions:## Functions### `getVP(df: pd.DataFrame, nBins: int = 20) -&gt; pd.DataFrame`This function takes a Pandas DataFrame with columns price and volume, and returns a DataFrame consisting of minPrice, maxPrice, and aggregateVolume for each price bin.### `getVPWithOHLC(df: pd.DataFrame, nBins: int = 20) -&gt; pd.DataFrame`This function takes a Pandas DataFrame with columns open, high, low, close, and volume, and returns a DataFrame consisting of minPrice, maxPrice, and aggregateVolume for each price bin. It uses the OHLC data to calculate more accurate price bins.### `getKMaxBars(volprofile_result: pd.DataFrame, k: int) -&gt; pd.DataFrame`This function takes a DataFrame generated by getVP or getVPWithOHLC, and returns the top k price bins with the highest aggregate volume.### `getUnusualIncreasingBars(df: pd.DataFrame, isUpward: bool) -&gt; pd.DataFrame`This function takes a DataFrame generated by getVP or getVPWithOHLC, and returns the price bins that have experienced unusual increases in volume. The isUpward parameter determines whether to search for upward or downward trends.### `plot(df: pd.DataFrame, price) -&gt; None`This function takes a DataFrame generated by getVP or getVPWithOHLC, and a price series, and generates a plot of the volume profile.InstallationTo install the package, run:`pip install volprofile`## Example Usage```pythonimport pandas as pdfrom volprofile import getVP, plot# Load datadf = pd.read_csv('mydata.csv')# Calculate volume profilevp = getVPWithOHLC(df)# Plot volume profileplot(vp, df['price'])```</longdescription>
</pkgmetadata>