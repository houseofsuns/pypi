<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Django Soloa hrefhttpspypi.orgprojectdjango-solo altCurrent version on PyPiimg srchttpsimg.shields.iopypivdjango-solo.svg a    +---------------------------+                                                                                             Django Solo helps working with singletons                              database tables that only have one row.               )            Singletons are useful for things like global                              settings that you want to edit from the admin                               instead of having them in Django settings.py.                                                                    +---------------------------+Features--------Solo helps you enforce instantiating only one instance of a model in django. You define the model that will hold your singleton object. django-solo gives helper parent class for your model and the admin classes. You get an admin interface thats aware you only have one object. You can retrieve the object from templates. By enabling caching, the database is not queried intensively.Use Cases--------Django Solo is also great for use with singleton objects that have a one to many relationship. Like the use case below where you have a Home Slider that has many Slides. Global or default settings An image slider that has many slides A page section that has sub-sections A team bio with many team membersThere are many cases where it makes sense for the parent in a one to many relationship to be limited to a single instance.Usage Examplepython models.pyfrom django.db import modelsfrom solo.models import SingletonModelclass SiteConfiguration(SingletonModel)    site_name  models.CharField(max_length255, defaultSite Name)    maintenance_mode  models.BooleanField(defaultFalse)    def __str__(self)        return Site Configuration    class Meta        verbose_name  Site Configurationpython admin.pyfrom django.contrib import adminfrom solo.admin import SingletonModelAdminfrom config.models import SiteConfigurationadmin.site.register(SiteConfiguration, SingletonModelAdmin)python There is only one item in the table, you can get it this wayfrom .models import SiteConfigurationconfig  SiteConfiguration.objects.get() get_solo will create the item if it does not already existconfig  SiteConfiguration.get_solo()In your model, note how you did not have to provide a verbose_name_plural field -Thats because Django Solo uses the verbose_name instead.If youre changing an existing model (which already has some objects stored in the database) to a singleton model, you can explicitly provide the id of the row in the database for django-solo to use. This can be done by setting singleton_instance_id property on the modelpythonclass SiteConfiguration(SingletonModel)    singleton_instance_id  24     (...)Installation------------This application requires Django 2.2, 3.2, or 4.0. Install the package using pip install django-solo Add solo or solo.apps.SoloAppConfig to your INSTALLED_APPS setting.This is how you run tests    .manage.py test solo --settingssolo.tests.settingsAnd from within toxpython -m pip install toxtoxSupported Languages-------------------- English- SpanishAdmin-----The standard Django admin does not fit well when working with singleton,for instance, if you need some global site settings to be edited in the admin.Django Solo provides a modified admin for that.[django-solo admin](httpsraw.github.comlazybirddjango-solomasterdocsimagesdjango-solo-admin.jpg django-solo admin) In the admin home page where all applications are listed, we have a config  application that holds a singleton model for site configuration. The configuration object can only be changed, theres no link for add (1). The link to the configuration page (2) directly goes to the form page - no  need for an intermediary object list page, since theres only one object. The edit page has a modified breadcrumb (3) to avoid linking to the  intermediary object list page. From the edit page, we cannot delete the object (4) nor can we add a new one (5).If you wish to disable the skipping of the object list page, and have the defaultbreadcrumbs, you should set SOLO_ADMIN_SKIP_OBJECT_LIST_PAGE to False in your settings.Availability from templates---------------------------The singleton object can be retrieved from template by giving the Django modeldotted pathdjango{ get_solo app_label.ModelName as my_config }Exampledjango{ load solo_tags }{ get_solo config.SiteConfiguration as site_config }{{ site_config.site_name }}{{ site_config.maintenance_mode }}If youre extending a template, be sure to use the tag in the proper scope.Rightdjango{ extends index.html }{ load solo_tags }{ block content }    { get_solo config.SiteConfiguration as site_config }    {{ site_config.site_name }}{ endblock content }Wrongdjango{ extends index.html }{ load solo_tags }{ get_solo config.SiteConfiguration as site_config }{ block content }    {{ site_config.site_name }}{ endblock content }Caching-------By default caching is disabled every time get_solo retrieves the singletonobject, there will be a database query.You can enable caching to only query the database when initially retrieving theobject. The cache will also be updated when updates are made from the admin.The cache timeout is controlled via the SOLO_CACHE_TIMEOUT settings.The cache backend to be used is controlled via the SOLO_CACHE settings.Settings-------- Template tag nameYou can retrieve your singleton object in templates using the get_solotemplate tag.You can change the name get_solo using theGET_SOLO_TEMPLATE_TAG_NAME setting.pythonGET_SOLO_TEMPLATE_TAG_NAME  get_config Admin override flagBy default, the admin is overridden. But if you wish to keep the object listpage (e.g. to customize actions), you can set the SOLO_ADMIN_SKIP_OBJECT_LIST_PAGEto False.pythonSOLO_ADMIN_SKIP_OBJECT_LIST_PAGE  True Cache backendDjango provides a way to define multiple cache backends with the CACHESsettings. If you want the singleton object to be cached separately, youcould define the CACHES and the SOLO_CACHE settings like thispythonCACHES  {    default {        BACKEND django.core.cache.backends.memcached.MemcachedCache,        LOCATION 127.0.0.111211,    },    local {        BACKEND django.core.cache.backends.locmem.LocMemCache,    },}SOLO_CACHE  localCaching will be disabled if set to None. Cache timeoutThe cache timeout in seconds.pythonSOLO_CACHE_TIMEOUT  605   5 mins Cache prefixThe prefix to use for the cache key.pythonSOLO_CACHE_PREFIX  soloGetting the codeThe code is hosted at httpsgithub.comlazybirddjango-soloCheck out the latest development version anonymously with     git clone gitgithub.comlazybirddjango-solo.gitYou can install the package in the editable mode like this    pip uninstall django-solo   just in case...    pip install -e git+httpsgithub.comlazybirddjango-solo.giteggdjango-soloYou can also install a specific branch    pip install -e git+httpsgithub.comlazybirddjango-solo.gitmy-brancheggdjango-soloThe package is now installed in your project and you can find the code.To run the unit tests    pip install tox    tox</longdescription>
</pkgmetadata>