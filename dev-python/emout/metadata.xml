<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># emoutEMSESの出力ファイルを取り扱うパッケージ* Documentation: https://nkzono99.github.io/emout/## Requirement* numpy* h5py* matplotlib* f90nml## Installation```&gt; pip install emoutor&gt; pip install git+https://github.com/Nkzono99/emout.git```## Usage以下のようなフォルダ構成の場合のサンプルコード.```.└── output_dir    ├── plasma.inp    ├── phisp00_0000.h5    ├── nd1p00_0000.h5    ├── nd2p00_0000.h5    ├── j1x00_0000.h5    ├── j1y00_0000.h5    ...    └── bz00_0000.h5```### データをロードする```&gt;&gt;&gt; import emout&gt;&gt;&gt; data = emout.Emout('output_dir')&gt;&gt;&gt;&gt;&gt;&gt; data.phisp  # data of &quot;phisp00_0000.h5&quot;&gt;&gt;&gt; len(data.phisp)11&gt;&gt;&gt; data.phisp[0].shape(513, 65, 65)&gt;&gt;&gt; data.j1x  # data of &quot;j1x00_0000.h5&quot;&gt;&gt;&gt; data.bz  # data of &quot;bz00_0000.h5&quot;```### データをプロットする```&gt;&gt;&gt; x, y, z = 32, 32, 100&gt;&gt;&gt; data.phisp[1][z, :, :].plot()  # plot xy-plane at z=100&gt;&gt;&gt; data.phisp[1][:, y, x].plot()  # plot center line along z-axis&gt;&gt;&gt; data.phisp[1][z, :, :].plot(use_si=True)  # can plot with SI-unit (such as x[m], y[m], phisp[V])&gt;&gt;&gt; data.phisp[1][z, :, :].plot(show=True)  # to view the plot on the fly (same as matplotlib.pyplot.show())&gt;&gt;&gt; data.phisp[1][z, :, :].plot(savefilename='phisp.png')  # to save to the file```### パラメータファイル(plasma.inp)を取得する```&gt;&gt;&gt; data.inp  # namelist of 'plasma.inp'&gt;&gt;&gt; data.inp['tmgrid']['nx']  # inp[group_name][parameter_name]64&gt;&gt;&gt; data.inp['nx']  # can omit group name64&gt;&gt;&gt; data.inp.tmgrid.nx  # can access like attribute&gt;&gt;&gt; data.inp.nx  # can also omit group name```### 単位変換を行う```&gt;&gt;&gt; data.unit.v.trans(1)  # velocity: Physical unit to EMSES unit3.3356409519815205e-05&gt;&gt;&gt; data.unit.v.reverse(1)  # velocity: EMSES unit to Physical unit29979.2458```### データマスクを適用する```&gt;&gt;&gt; # mask below average values&gt;&gt;&gt; data.phisp[1].masked(lambda phi: phi &lt; phi.mean())&gt;&gt;&gt;&gt;&gt;&gt; # above code does the same as this code&gt;&gt;&gt; phi = data.phisp[1].copy()&gt;&gt;&gt; phi[phi &lt; phi.mean()] = np.nan```### グループ化する```&gt;&gt;&gt; from emout.utils import Group&gt;&gt;&gt; data1 = emout.Emout('output_dir1')&gt;&gt;&gt; data2 = emout.Emout('output_dir2')&gt;&gt;&gt;&gt;&gt;&gt; # grouping&gt;&gt;&gt; group = Group([data1, data2])&gt;&gt;&gt; group.phisp[10][100, :, :].plot(show=True, title=group.directory.name)&gt;&gt;&gt;&gt;&gt;&gt; # above code does the same as this code&gt;&gt;&gt; data1.phisp[10][100, :, :].plot(show=True, title=data1.directory.name)&gt;&gt;&gt; data2.phisp[10][100, :, :].plot(show=True, title=data2.directory.name)```### 継続したシミュレーション結果を扱う```&gt;&gt;&gt; import emout&gt;&gt;&gt; data = emout.Emout('output_dir', append_directories=['output_dir2', 'output_dir3'])```</longdescription>
</pkgmetadata>