<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Query Pandas-like Dataframes Using SQLQPD let you run the same SQL (SELECT for now) statements on different computing frameworks with pandas-like interfaces.Currently, it support [Pandas](httpspandas.pydata.org), [Dask](httpsdask.org) and [Ray](httpsray.io)(via [Modin](httpsgithub.commodin-projectmodin) on Ray).QPD directly translates SQL into pandas-like operations to run on the backend computing frameworks, so it can be significantlyfaster than some other approaches, for example, to dump pandas dataframes into SQLite, run SQL and convert the result back intoa pandas dataframe. However, the top priorities of QPD are correctness and consistency. It ensures the results ofimplemented SQL features following SQL convention, and it ensures consistent behavior regardless of backend computing frameworks.A typical case is groupby().agg(). In pandas or pandas like frameworks, if any of the group keys is null, the defaultbehavior is to drop that group, however, in SQL they are not dropped. QPD follows the SQL way.QPD syntax is a subset of the intersection of [Spark SQL](httpsspark.apache.orgsql) and [SQLite](httpswww.sqlite.orgindex.html).The correctness and consistency are extensively tested against SQLite. Practically, Spark SQL and SQLite are highly consistenton both syntax and behavior. So, in other words, QPD enables you to run common SQLs and get the same result on Pandas, SQLite, Spark, Dask,Ray and other backends that QPD will support in the future.SQL is one of the most important data processing languages. It is very scale agnostic, and one of the major goals of the Fugue projectis to enrich SQL and make SQL platform agnostic. QPD, as a subproject of Fugue, focuses on running SQL on pandas-like frameworks, it isan essential component to achieve the ultimate goal. InstallationQPD can be installed from PyPIbashpip install qpd  install qpd + pandasIf you want to use Ray or Dask as the backend, you will need to install QPD with one of the targetsbashpip install qpd[dask]  install qpd + dask[dataframe]pip install qpd[ray]  install qpd + raypip install qpd[all]  install all dependencies above Using QPD On Pandaspythonfrom qpd_pandas import run_sql_on_pandasimport pandas as pddf  pd.DataFrame([[0,1],[2,3],[0,5]], columns[a,b])res  run_sql_on_pandas(SELECT a, SUM(b) AS b, COUNT() AS c FROM df GROUP BY a, {df df})print(res) On DaskPlease read [this](httpsdistributed.dask.orgenlatestquickstart.html) to learn the bestpractice for initializing dask.pythonfrom qpd_dask import run_sql_on_daskimport dask.dataframe as pdimport pandasdf  pd.from_pandas(pandas.DataFrame([[0,1],[2,3],[0,5]], columns[a,b]))res  run_sql_on_dask(SELECT a, SUM(b) AS b, COUNT() AS c FROM df GROUP BY a, {df df})print(res.compute())   dask dataframe is lazy, you need to call compute On RayPlease read [this](httpsdocs.ray.ioenray-0.3.1api.htmlstarting-ray) to learn the bestpractice for initializing ray. And read [this](httpsmodin.readthedocs.ioenlatestusing_modin.html)for initializing modin + ray.Please dont use dask as modin backend if you want to use QPD, its not testedpythonimport rayray.init()from qpd_ray import run_sql_on_rayimport modin.pandas as pddf  pd.DataFrame([[0,1],[2,3],[0,5]], columns[a,b])res  run_sql_on_ray(SELECT a, SUM(b) AS b, COUNT() AS c FROM df GROUP BY a, {df df})print(res) Ignoring Case in SQLBy default, QPD requires users to use upper cased keywords, otherwise syntax errors will be raised.However if you really dont like this behavior, you can turn it off, the parameter is ignore_case,here is an examplepythonfrom qpd_pandas import run_sql_on_pandasimport pandas as pddf  pd.DataFrame([[0,1],[2,3],[0,5]], columns[a,b])res  run_sql_on_pandas(    select a, sum(b) as b, count() as c from df group by a,    {df df}, ignore_caseTrue)print(res) Things to clarify QPD on Spark (Koalas)No, that will not happen. QPD is using Spark SQL[syntax file](httpsgithub.comapachesparkblobmastersqlcatalystsrcmainantlr4orgapachesparksqlcatalystparserSqlBase.g4).Spark SQL is highly optimized. If we create a Koalas backend, correctness and consistency canbe guaranteed, but there will be no performance advantage. So for Spark, please use Spark SQL.If you use Fugue SQL on Spark backend, it will also directly use Spark to run the SQL statements.We dont see the value to make QPD run on Spark. Update History 0.4.0 Support arbitrary column name 0.2.6 Update pandas indexer import 0.2.5 Update antlr to 4.9 0.2.4 Fix a bug set operations will alter the input dataframe to add columns 0.2.3 Refactor and extract out PandasLikeUtils class 0.2.2 Accept constant select without FROM, SELECT 1 AS a, b AS b  0.2.1 Pandas, Dask, Ray SQL support</longdescription>
</pkgmetadata>