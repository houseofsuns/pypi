<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Atlassian JWT authentication.. image httpsgithub.comatlassianasap-authentication-pythonworkflowsTestsbadge.svg.. image httpsimg.shields.iopypivatlassian-jwt-auth.svg   target httpspypi.orgprojectatlassian-jwt-authThis package provides an implementation of the Service to Service Authentication httpss2sauth.bitbucket.iospec_ specification.----InstallationTo install simply run.. code sh     pip install atlassian-jwt-authUsing this libraryTo create a JWT for authentication.. code python    import atlassian_jwt_auth    signer  atlassian_jwt_auth.create_signer(issuer, issuerkey, private_key_pem)    a_jwt  signer.generate_jwt(audience)To create a JWT using a file on disk in the conventional locationEach time you call generate_jwt this will find the latest active key file (ends with .pem) and use it to generate your JWT... code python    import atlassian_jwt_auth    signer  atlassian_jwt_auth.create_signer_from_file_private_key_repository(issuer, optjwtprivatekeys)    a_jwt  signer.generate_jwt(audience)To create a JWT using a data uri.. code python    import atlassian_jwt_auth    from atlassian_jwt_auth.key import DataUriPrivateKeyRetriever    key_id, private_key_pem  DataUriPrivateKeyRetriever(Your base64 encoded data uri).load(issuer)    signer  atlassian_jwt_auth.create_signer(issuer, issuerkey, private_key_pem)    a_jwt  signer.generate_jwt(audience)To make an authenticated HTTP requestIf you use the atlassian_jwt_auth.contrib.requests.JWTAuth provider, youcan automatically generate JWT tokens when using the requests library toperform authenticated HTTP requests... code python    import atlassian_jwt_auth    from atlassian_jwt_auth.contrib.requests import JWTAuth    signer  atlassian_jwt_auth.create_signer(issuer, issuerkey, private_key_pem)    response  requests.get(        httpsyour-url,        authJWTAuth(signer, audience)    )One can also use atlassian_jwt_auth.contrib.aiohttp.JWTAuthto authenticate aiohttp requests.. code python    import aiohttp    import atlassian_jwt_auth    from atlassian_jwt_auth.contrib.aiohttp import JWTAuth    signer  atlassian_jwt_auth.create_signer(issuer, issuerkey, private_key_pem)    async with aiohttp.ClientSession() as session        async with session.get(httpsyour-url,                               authJWTAuth(signer, audience)) as resp            ...If you want to reuse tokens that have the same claim within their period of validitythen pass through reuse_jwtsTrue when calling  create_signer.For example.. code python    import atlassian_jwt_auth    from atlassian_jwt_auth.contrib.requests import JWTAuth    signer  atlassian_jwt_auth.create_signer(issuer, issuerkey, private_key_pem, reuse_jwtsTrue)    response  requests.get(        httpsyour-url,        authJWTAuth(signer, audience)    )To verify a JWT.. code python    import atlassian_jwt_auth    public_key_retriever  atlassian_jwt_auth.HTTPSPublicKeyRetriever(httpsexample.com)    verifier  atlassian_jwt_auth.JWTAuthVerifier(public_key_retriever)    verified_claims  verifier.verify_jwt(a_jwt, audience)For Python versions starting from Python 3.5, note this library no longer supports python 3.5, atlassian_jwt_auth.contrib.aiohttpprovides drop-in replacements for the components thatperform HTTP requests, so that they use aiohttp instead of requests.. code python    import atlassian_jwt_auth.contrib.aiohttp    public_key_retriever  atlassian_jwt_auth.contrib.aiohttp.HTTPSPublicKeyRetriever(httpsexample.com)    verifier  atlassian_jwt_auth.contrib.aiohttp.JWTAuthVerifier(public_key_retriever)    verified_claims  await verifier.verify_jwt(a_jwt, audience)</longdescription>
</pkgmetadata>