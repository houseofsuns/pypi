<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpstravis-ci.orgVirusTotalyara-python.svg    target httpstravis-ci.orgVirusTotalyara-python.. image httpsci.appveyor.comapiprojectsstatusgidnb9ulj3rje5s2svgtrue    target httpsci.appveyor.comprojectplusvicyara-pythonyara-pythonWith this library you can use YARA httpsgithub.comVirusTotalyara_ fromyour Python programs. It covers all YARAs features, from compiling, savingand loading rules to scanning files, strings and processes.Here it goes a little example.. code-block python     import yara     rule  yara.compile(sourcerule foo bar {strings a  lmn condition a})     matches  rule.match(dataabcdefgjiklmnoprstuvwxyz)     print(matches)    [foo]     print(matches[0].rule)    foo     print(matches[0].tags)    [bar]     print(matches[0].strings)    [(10L, a, lmn)]Installation------------The easiest way of installing YARA is by using pip.. code-block bash   pip install yara-pythonBut you can also get the source from GitHub and compile it yourself.. code-block bash   git clone --recursive httpsgithub.comVirusTotalyara-python   cd yara-python   python setup.py build   sudo python setup.py installNotice the --recursive option used with git. This is important becausewe need to download the yara subproject containing the source code forlibyara (the core YARA library). Its also important to note that the twomethods above link libyara statically into yara-python. If you want to linkdynamically against a shared libyara library use.. code-block bash   python setup.py build --dynamic-linkingFor this option to work you must build and installYARA httpsgithub.comVirusTotalyara_ separately before installingyara-python.Documentation-------------Find more information about how to use yara-python athttpsyara.readthedocs.orgenlatestyarapython.html.</longdescription>
</pkgmetadata>