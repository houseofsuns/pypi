<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Chia VDF[Build](httpsgithub.comChia-NetworkchiavdfworkflowsBuildbadge.svg)[PyPI](httpsimg.shields.iopypivchiavdflogopypi)[PyPI - Format](httpsimg.shields.iopypiformatchiavdflogopypi)[GitHub](httpsimg.shields.iogithublicenseChia-NetworkchiavdflogoGithub)[[Total alerts](httpsimg.shields.iolgtmalertsgChia-Networkchiavdf.svglogolgtmlogoWidth18)](httpslgtm.comprojectsgChia-Networkchiavdfalerts)[[Language grade Python](httpsimg.shields.iolgtmgradepythongChia-Networkchiavdf.svglogolgtmlogoWidth18)](httpslgtm.comprojectsgChia-Networkchiavdfcontextpython)[[Language grade CC++](httpsimg.shields.iolgtmgradecppgChia-Networkchiavdf.svglogolgtmlogoWidth18)](httpslgtm.comprojectsgChia-Networkchiavdfcontextcpp) Building a wheelCompiling chiavdf requires cmake, boost and GMPMPIR.bashpython3 -m venv venvsource venvbinactivatepip install wheel setuptools_scm pybind11pip wheel .The primary build process for this repository is to use GitHub Actions tobuild binary wheels for MacOS, Linux (x64 and aarch64), and Windows andpublish them with a source wheel on PyPi. See .githubworkflowsbuild.yml.CMake uses[FetchContent](httpscmake.orgcmakehelplatestmoduleFetchContent.html)to download [pybind11](httpsgithub.compybindpybind11).Building is then managed by[cibuildwheel](httpsgithub.comjoerickcibuildwheel). Further installationis then available via pip install chiavdf e.g. Building Timelord and related binariesIn addition to building the required binary and source wheels for Windows,MacOS and Linux, chiavdf can be used to compile vdf_client and vdf_bench.vdf_client is the core VDF process that completes the Proof of Time submittedto it by the Timelord. The repo also includes a benchmarking tool to get asense of the iterations per second of a given CPU called vdf_bench. Try.vdf_bench square_asm 250000 for an ips estimate.To build vdf_client set the environment variable BUILD_VDF_CLIENT to Y.export BUILD_VDF_CLIENTY.Similarly, to build vdf_bench set the environment variable BUILD_VDF_BENCH toY. export BUILD_VDF_BENCHY.This is currently automated via pip in the[install-timelord.sh](httpsgithub.comChia-Networkchia-blockchainblobmasterinstall-timelord.sh)script in the[chia-blockchain repository](httpsgithub.comChia-Networkchia-blockchain)which depends on this repository.If youre running a timelord, the following tests are available, depending of which type of timelord you are running.1weso_test, in case youre running in sanitizer_mode..2weso_test, in case youre running a timelord that extends the chain and youre running the slow algorithm..prover_test, in case youre running a timelord that extends the chain and youre running the fast algorithm.Those tests will simulate the vdf_client and verify for correctness the produced proofs. Contributing and workflowContributions are welcome and more details are available in chia-blockchains[CONTRIBUTING.md](httpsgithub.comChia-Networkchia-blockchainblobmasterCONTRIBUTING.md).The master branch is the currently released latest version on PyPI. Note thatat times chiavdf will be ahead of the release version that chia-blockchainrequires in its masterrelease version in preparation for a new chia-blockchainrelease. Please branch or fork master and then create a pull request to themaster branch. Linear merging is enforced on master and merging requires acompleted review. PRs will kick off a ci build and analysis of chiavdf at[lgtm.com](httpslgtm.comprojectsgChia-Networkchiavdfmodelist). Pleasemake sure your build is passing and that it does not increase alerts at lgtm. Background from prior VDF competitionsCopyright 2018 [Ilya Gorodetskov](httpwww.sundersoft.com)genericsundersoft.comLicensed under the Apache License, Version 2.0 (the License)you may not use this file except in compliance with the License.You may obtain a copy of the License at[httpwww.apache.orglicensesLICENSE-2.0](httpwww.apache.orglicensesLICENSE-2.0)Unless required by applicable law or agreed to in writing, softwaredistributed under the License is distributed on an AS IS BASIS,WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.See the License for the specific language governing permissions andlimitations under the License.Our VDF construction is described in classgroup.pdf. The implementation details about squaring and proving phrases are described below. Main VDF LoopThe main VDF loop produces repeated squarings of the generator form (i.e. calculates y(n)  g(2n)) as fast as possible, until the program is interrupted. Sundersofts entry from [Chias 2nd VDF contest](httpsgithub.comChia-Networkvdfcontest2results) is used, together with the fast reducer used in Pulmarks entry. This approach is described belowThe NUDUPL algorithm is used. The equations are based on cryptoslavas equations from the 1st contest. They were modified slightly to increase the level of parallelism.The GCD is a custom implementation with scalar integers. There are two base cases one uses a lookup table with continued fractions and the other uses the euclidean algorithm with a division table. The division table algorithm is slightly faster even though it has about 2x as many iterations.After the base case, there is a 128 bit GCD that generates 64 bit cofactor matricies with Lehmers algorithm. This is required to make the long integer multiplications efficient (Flints implementation doesnt do this).The GCD also implements Flints partial xgcd function, but the output is slightly different. This implementation will always return an A value which is  the threshold and a B value which is  the threshold. For a normal GCD, the threshold is 0, B is 0, and A is the GCD. Also the interfaces are slightly different.Scalar integers are used for the GCD. I dont expect any speedup for the SIMD integers that were used in the last implementation since the GCD only uses 64x1024 multiplications, which are too small and have too high of a carry overhead for the SIMD version to be faster. In either case, most of the time seems to be spent in the base case so it shouldnt matter too much.If SIMD integers are used with AVX-512, doubles have to be used because the multiplier sizes for doubles are significantly larger than for integers. There is an AVX-512 extension to support larger integer multiplications but no processor implements it yet. It should be possible to do a 50 bit multiply-add into a 100 bit accumulator with 4 fused multiply-adds if the accumulators have a special nonzero initial value and the inputs are scaled before the multiplication. This would make AVX-512 about 2.5x faster than scalar code for 1024x1024 integer multiplications (assuming the scalar code is unrolled and uses ADOXADCXMULX properly, and the CPU can execute this at 1 cycle per iteration which it probably cant).The GCD is parallelized by calculating the cofactors in a separate slave thread. The master thread will calculate the cofactor matricies and send them to the slave thread. Other calculations are also parallelized.The VDF implementation from the first contest is still used as a fallback and is called about once every 5000 iterations. The GCD will encounter large quotients about this often and these are not implemented. This has a negligible effect on performance. Also, the NUDUPL case where AL is not implemented it will fall back to the old implementation in this case (this never happens outside of the first 20 or so iterations).There is also corruption detection by calculating C with a non-exact division and making sure the remainder is 0. This detected all injected random corruptions that I tested. No corruptions caused by bugs were observed during testing. This cannot correct for the sign of B being wrong. GCD continued fraction lookup tableThe is implemented in gcd_base_continued_fractions.h and asm_gcd_base_continued_fractions.h. The division table implementation is the same as the previous entry and was discussed there. Currently the division table is only used if AVX2 is enabled but it could be ported to SSE or scalar code easily. Both implementations have about the same performance.The initial quotient sequence of gcd(a,b) is the same as the initial quotient sequence of gcd(a2nb, 2n) for any n. This is because the GCD quotients are the same as the continued fraction quotients of ab, and the initial continued fraction quotients only depend on the initial bits of ab. This makes it feasible to have a lookup table since it now only has one input.a2nb is calculated by doing a double precision division of ab, and then truncating the lower bits. Some of the exponent bits are used in the table in addition to the fraction bits this makes each slot of the table vary in size depending on what the exponent is. If the result is outside the table bounds, then the division result is floored to fall back to the euclidean algorithm (this is very rare).The table is calculated by iterating all of the possible continued fractions that have a certain initial quotient sequence. Iteration ends when all of these fractions are either outside the table or they dont fully contain at least one slot of the table. Each slot that is fully contained by such a fraction is updated so that its quotient sequence equals the fractions initial quotient sequence. Once this is complete, the cofactor matricies are calculated from the quotient sequences. Each cofactor matrix is 4 doubles.The resulting code seems to have too many instructions so it doesnt perform very well. There might be some way to optimize it. It was written for SSE so that it would run on both processors.This might work better on an FPGA possibly with low latency DRAM or SRAM (compared to the euclidean algorithm with a division table). There is no limit to the size of the table but doubling the latency would require the number of bits in the table to also be doubled to have the same performance. Other GCD codeThe gcd_128 function calculates a 128 bit GCD using Lehmers algorithm. It is pretty straightforward and uses only unsigned arithmetic. Each cofactor matrix can only have two possible signs [+ - - +] or [- + + -]. The gcd_unsigned function uses unsigned arithmetic and a jump table to apply the 64-bit cofactor matricies to the A and B values. It uses ADOXADCXMULX if they are available and falls back to ADCMUL otherwise. It will track the last known size of A to speed up the bit shifts required to get the top 128 bits of A.No attempt was made to try to do the A and B long integer multiplications on a separate thread I wouldnt expect any performance improvement from this. ThreadsThere is a master thread and a slave thread. The slave thread only exists for each batch of 5000 or so squarings and is then destroyed and recreated for the next batch (this has no measurable overhead). If the original VDF is used as a fallback, the batch ends and the slave thread is destroyed.Each thread has a 64-bit counter that only it can write to. Also, during a squaring iteration, it will not overwrite any value that it has previously written and transmitted to the other thread. Each squaring is split up into phases. Each thread will update its counter at the start of the phase (the counter can only be increased, not decreased). It can then wait on the other threads counter to reach a certain value as part of a spin loop. If the spin loop takes too long, an error condition is raised and the batch ends this should prevent any deadlocks from happening.No CPU fences or atomics are required since each value can only be written to by one thread and since x86 enforces acquirerelease ordering on all memory operations. Compiler memory fences are still required to prevent the compiler from caching or reordering memory operations.The GCD master thread will increment the counter when a new cofactor matrix has been outputted. The slave thread will spin on this counter and then apply the cofactor matrix to the U or V vector to get a new U or V vector.It was attempted to use modular arithmetic to calculate k directly but this slowed down the program due to GMPs modulo or integer multiply operations not having enough performance. This also makes the integer multiplications bigger.The speedup isnt very high since most of the time is spent in the GCD base case and these cant be parallelized. Generating proofsThe nested wesolowski proofs (n-wesolowski) are used to check the correctness of a VDF result. (Simple) Wesolowski proofs are described in [A Survey of Two Verifiable Delay Functions](httpstheory.stanford.edudabopapersVDFsurvey.pdf). In order to prove h  g(2T), a n-wesolowski proof uses n intermediate simple wesolowski proofs. Given h, g, T, t1, t2, ..., tn, h1, h2, ..., hn, a correct n-wesolowski proof will verify the followingcpph1  g(2t1)h2  h1(2t2)h3  h2(2t3)...hn  h(n-1)(2tn)Additionally, we must havecppt1 + t2 + ... + tn  Thn  hThe algorithm will generate at most 64-wesolowski proofs. Some intermediates wesolowski proofs are stored in parallel with the main VDF loop. The goal is to have a n-wesolowski proof almost ready as soon as the main VDF loop finishes computing h  g(2T), for a T that were interested in. Well call a segment a tuple (y, x, T) for which were interested in a simple wesolowski proof that y  x(2T). Well call a segment finished when weve finished computing its proof. Segmenets storedWell store finished segments of length 2x for x being multiples of 2 greater than or equal to 16. The current implementation limits the maximum segment size to 230, but this can be increased if needed. Let P  16+2astl. After each 2P steps calculated by the main VDF loop, well store a segment proving that weve correctly done the 2P steps. Formally, let x be the form after kast2P steps, y be the form after (k+1)ast2P steps, for each k  0, for each P  16+2astl. Then, well store a segment (y, x, 2P), together with a simple wesolowski proof. Segment threadsIn order to finish a segment of length T2P, the number of iterations to run for is Tk + last2(k+1) and the intermediate storage required is T(kastl), for some parameters k and l, as described in the paper. The squarings used to finish a segment are about 2 times as slow as the ones used by the main VDF loop. Even so, finishing a segment is much faster than producing its y value by the main VDF loop. This allows, by the time the main VDF loop finishes 216 more steps, to perform work on finishing multiple segments.The parameters used in finishing segments, for T216, are k10 and l1. Above that, parameters are k12 and l2(P-18). Note that, for P  18, the intermediate storage needed for a segment is constant (i.e. 21812 forms stored in memory).Prover class is responsible to finish a segment. It implements pauseresume functionality, so its work can be paused, and later resumed from the point it stopped. For each unfinished segment generated by the main VDF loop, a Prover instance is created, which will eventually finish the segment.Segment threads are responsible for deciding which Prover instance is currently running. In the current implementation, there are 3 segment threads (however the number is configurable), so at most 3 Prover instances will run at once, at different threads (other Provers will be paused). The segment threads will always pick the segments with the shortest length to run. In case of a tie, the segments received the earliest will have priority. Every time a new segment arrives, or a segment gets finished, some pausingresuming of Provers is done, if needed. Pausing is done to have at most 3 Provers running at any time, whilst resuming is done if less than 3 Provers are working, but some Provers are paused.All the segments of lengths 216, 218 and 220 will be finished relatively soon after the main VDF worker produced them, while the segments of length 222 and upwards will lag behind the main VDF worker a little. Eventually, all the higher size segments will be finished, the work on them being done repeatedly via pausing (when a smaller size segment arrives) and resuming (when all smaller size segments are finished).Currently, 4 more segment threads are added after the main VDF loop finishes 500 million iterations (after about 1 hour of running). This is done to be completely sure even the very big sized segments will be finished. This optimisation is only allowed on machines supporting at least 16 concurrent threads. Generating n-wesolowski proofLet T an iteration we are interested in. Firstly, the main VDF Loop will need to calculate at least T iterations. Then, in order to get fast a n-wesolowski proof, well concatenate finished segments. We want the proof to be as short as possible, so well always pick finished segments of the maximum length possible. If such segments arent finished, well choose lower length segments. A segment of length 2(16 + 2astp) can always be replaced with 4 segments of length 2(16 + 2astp - 2). The proof will be created shortly after the main VDF loop produced the result, as the 216 length segments will always be up to date with the main VDF loop (and, at worst case, we can always concatenate 216 length segments, if bigger sizes are not finished yet). Its possible after the concatenation that well still need to prove up to 216 iterations (no segment is able to cover anything less than 216). This last work is done in parallel with the main VDF loop, as an optimisation.The program limits the proof size to 64-wesolowski. If number of iterations is very large, its possible the concatenation wont fit into this. In this case, the program will attempt again to prove every minute, until there are enough large segments to fit the 64-wesolowski limit. However, almost in all cases, the concatenation will fit the 64-wesolowski limit in the first try.Since the maximum segment size is 230 and we can use at most 64 segments in a concatenation, the program will prove at most 236 iterations. This can be increased if needed. Intermediates storageIn order to finish segments, some intermediate values need to be stored for each segment. For each different possible segment length, we use a sliding window of length 20 to store those. Hence, for each segment length, well store only the intermediates values needed for the last 20 segments produced by the main VDF loop. Since finishing segments is faster than producing them by the main VDF loop, we assume the segment threads wont be behind by more than 20 segments from the main VDF loop, for each segment length. Thanks to the sliding window technique, the memory used will always be constant.Generally, the main VDF loop performs all the storing, after computing a form were interested in. However, since storing is very frequent and expensive (GMP operations), this will slow down the main VDF loop.For the machines having at least 16 concurrent threads, an optimization is provided the main VDF loop does only repeated squaring, without storing any form. After each 215 steps are performed, a new thread starts redoing the work for 215 more steps, this time storing the intermediate values as well. All the intermediates threads and the main VDF loop will work in parallel. The only purpose of the main VDF loop becomes now to produce the starting values for the intermediate threads, as fast as possible. The squarings used in the intermediates threads will be 2 times slower than the ones used in the main VDF loop. Its expected the intermediates will only lag behind the main VDF loop by 215 iterations, at any point after 216 iterations are done by the main VDF loop, the first thread doing the first 215 intermediate values is already finished. Also, at that point, half of the work of the second thread doing the last 215 intermediates values should be already done.</longdescription>
</pkgmetadata>