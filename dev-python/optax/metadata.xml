<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Optax[CI status](httpsgithub.comdeepmindoptaxworkflowstestsbadge.svg)[[Documentation Status](httpsreadthedocs.orgprojectsoptaxbadgeversionlatest)](httpoptax.readthedocs.io)[pypi](httpsimg.shields.iopypivoptax) IntroductionOptax is a gradient processing and optimization library for JAX.Optax is designed to facilitate research by providing building blocksthat can be easily recombined in custom ways.Our goals are to   Provide simple, well-tested, efficient implementations of core components.   Improve research productivity by enabling to easily combine low level    ingredients into custom optimisers (or other gradient processing components).   Accelerate adoption of new ideas by making it easy for anyone to contribute.We favour focusing on small composable building blocks that can be effectivelycombined into custom solutions. Others may build upon these basic componentsmore complicated abstractions. Whenever reasonable, implementations prioritisereadability and structuring code to match standard equations, over code reuse.An initial prototype of this library was made available in JAXs experimentalfolder as jax.experimental.optix. Given the wide adoption across DeepMindof optix, and after a few iterations on the API, optix was eventually movedout of experimental as a standalone open-source library, renamed optax.Documentation on Optax can be found at [optax.readthedocs.io](httpsoptax.readthedocs.io). InstallationYou can install the latest released version of Optax from PyPI viashpip install optaxor you can install the latest development version from GitHubshpip install git+httpsgithub.comdeepmindoptax.git QuickstartOptax contains implementations of [many popular optimizers](httpsoptax.readthedocs.ioenlatestapi.htmlCommon-Optimizers) and[loss functions](httpsoptax.readthedocs.ioenlatestapi.htmlcommon-losses).For example the following code snippet uses the Adam optimizer from optax.adamand the mean squared error from optax.l2_loss. We initialize the optimizerstate using the init function and params of the model.pythonoptimizer  optax.adam(learning_rate) Obtain the opt_state that contains statistics for the optimizer.params  {w jnp.ones((num_weights,))}opt_state  optimizer.init(params)To write the update loop we need a loss function that can be differentiated byJax (with jax.grad in thisexample) to obtain the gradients.pythoncompute_loss  lambda params, x, y optax.l2_loss(params[w].dot(x), y)grads  jax.grad(compute_loss)(params, xs, ys)The gradients are then converted via optimizer.update to obtain the updatesthat should be applied to the current params to obtain the new ones.optax.apply_updates is a convinience utility to do this.pythonupdates, opt_state  optimizer.update(grads, opt_state)params  optax.apply_updates(params, updates)You can continue the quick start in [the Optax quickstart notebook.](httpsgithub.comdeepmindoptaxblobmasterexamplesquick_start.ipynb) ComponentsWe refer to the [docs](httpsoptax.readthedocs.ioenlatestindex.html)for a detailed list of available Optax components. Here, we highlightthe main categories of buiilding blocks provided by Optax. Gradient Transformations ([transform.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srctransform.py))One of the key building blocks of optax is a GradientTransformation.Each transformation is defined two functions   state  init(params)   grads, state  update(grads, state, paramsNone)The init function initializes a (possibly empty) set of statistics (aka state)and the update function transforms a candidate gradient given some statistics,and (optionally) the current value of the parameters.For examplepythontx  scale_by_rms()state  tx.init(params)   init statsgrads, state  tx.update(grads, state, params)   transform  update stats. Composing Gradient Transformations ([combine.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srccombine.py))The fact that transformations take candidate gradients as input and returnprocessed gradients as output (in contrast to returning the updated parameters)is critical to allow to combine arbitrary transformations into a customoptimiser  gradient processor, and also allows to combine transformations fordifferent gradients that operate on a shared set of variables.For instance, chain combines them sequentially, and returns anew GradientTransformation that applies several transformations in sequence.For examplepythonmy_optimiser  chain(    clip_by_global_norm(max_norm),    scale_by_adam(eps1e-4),    scale(-learning_rate)) Wrapping Gradient Transformations ([wrappers.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srcwrappers.py))Optax also provides several wrappers that take a GradientTransformation asinput and return a new GradientTransformation that modifies the behaviourof the inner transformation in a specific way.For instance the flatten wrapper flattens gradients into a single large vectorbefore applying the inner GradientTransformation. The transformed updated arethen unflattened before being returned to the user. This can be used to reducethe overhead of performing many calculations on lots of small variables,at the cost of increasing memory usage.For examplepythonmy_optimiser  flatten(adam(learning_rate))Other examples of wrappers include accumulating gradients over multiple steps,or applying the inner transformation only to specific parameters or atspecific steps. Schedules ([schedule.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srcschedule.py))Many popular transformations use time dependent components, e.g. to annealsome hyper-parameter (e.g. the learning rate). Optax provides for this purposeschedules that can be used to decay scalars as a function of a step count.For example you may use a polynomial schedule (with power1) to decaya hyper-parameter linearly over a number of stepspythonschedule_fn  polynomial_schedule(    init_value1., end_value0., power1, transition_steps5)for step_count in range(6)  print(schedule_fn(step_count))   [1., 0.8, 0.6, 0.4, 0.2, 0.]Schedules are used by certain gradient transformation, for instancepythonschedule_fn  polynomial_schedule(    init_value-learning_rate, end_value0., power1, transition_steps5)optimiser  chain(    clip_by_global_norm(max_norm),    scale_by_adam(eps1e-4),    scale_by_schedule(schedule_fn)) Popular optimisers ([alias.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srcalias.py))In addition to the low level building blocks we also provide aliases for popularoptimisers built using these components (e.g. RMSProp, Adam, AdamW, etc, ...).These are all still instances of a GradientTransformation, and can thereforebe further combined with any of the individual building blocks.For examplepythondef adamw(learning_rate, b1, b2, eps, weight_decay)  return chain(      scale_by_adam(b1b1, b2b2, epseps),      scale_and_decay(-learning_rate, weight_decayweight_decay)) Applying updates ([update.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srcupdate.py))After transforming an update using a GradientTransformation or any custommanipulation of the update, you will typically apply the update to a setof parameters. This can be done trivially using tree_map. For convenience, we expose an apply_updates function to apply updates toparameters. The function just adds the updates and the parameters together,i.e. tree_map(lambda p, u p + u, params, updates).pythonupdates, state  tx.update(grads, state, params)   transform  update stats.new_params  optax.apply_updates(params, updates)   update the parameters.Note that separating gradient transformations from the parameter update iscritical to support composing sequence of transformations (e.g. chain), aswell as combine multiple updates to the same parameters (e.g. in multi-tasksettings where different tasks need different sets of gradient transformations). Losses ([loss.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srcloss.py))Optax provides a number of standard losses used in deep learning, such asl2_loss, softmax_cross_entropy, cosine_distance, etc.pythonloss  huber_loss(predictions, targets)The losses accept batches as inputs, however they perform no reduction acrossthe batch dimension(s). This is trivial to do in JAX, for examplepythonavg_loss  jnp.mean(huber_loss(predictions, targets))sum_loss  jnp.sum(huber_loss(predictions, targets)) Second Order ([second_order.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srcsecond_order.py))Computing the Hessian or Fisher information matrices for neural networks istypically intractable due to the quadratic memory requirements. Solving for thediagonals of these matrices is often a better solution. The library offersfunctions for computing these diagonals with sub-quadratic memory requirements. Stochastic gradient estimators ([stochastic_gradient_estimators.py](httpsgithub.comdeepmindoptaxblobmasteroptax_srcstochastic_gradient_estimators.py))Stochastic gradient estimators compute Monte Carlo estimates of gradients ofthe expectation of a function under a distribution with respect to thedistributions parameters.Unbiased estimators, such as the score function estimator (REINFORCE),pathwise estimator (reparameterization trick) or measure valued estimator,are implemented score_function_jacobians, pathwise_jacobians and measure_valued_jacobians. Their applicability (both in terms of functions anddistributions) is discussed in their respective documentation.Stochastic gradient estimators can be combined with common control variates forvariance reduction via control_variates_jacobians. For provided controlvariates see delta and moving_avg_baseline.The result of a gradient estimator or control_variates_jacobians contains theJacobians of the function with respect to the samples from the inputdistribution. These can then be used to update distributional parameters, orto assess gradient variance.Example of how to use the pathwise_jacobians estimatorpythondist_params  [mean, log_scale]function  lambda x jnp.sum(x  weights)jacobians  pathwise_jacobians(      function, dist_params,      utils.multi_normal, rng, num_samples)mean_grads  jnp.mean(jacobians[0], axis0)log_scale_grads  jnp.mean(jacobians[1], axis0)grads  [mean_grads, log_scale_grads]optim_update, optim_state  optim.update(grads, optim_state)updated_dist_params  optax.apply_updates(dist_params, optim_update)where optim is an optax optimizer. Citing OptaxOptax is part of the [DeepMind JAX Ecosystem], to cite Optax please usethe [DeepMind JAX Ecosystem citation].[DeepMind JAX Ecosystem] httpsdeepmind.comblogarticleusing-jax-to-accelerate-our-research DeepMind JAX Ecosystem[DeepMind JAX Ecosystem citation] httpsgithub.comdeepmindjaxblobmaindeepmind2020jax.txt Citation</longdescription>
</pkgmetadata>