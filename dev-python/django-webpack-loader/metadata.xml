<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> django-webpack-loader[[Build Status](httpscircleci.comghdjango-webpackdjango-webpack-loadertreemaster.svgstylesvg)](httpscircleci.comghdjango-webpackdjango-webpack-loadertreemaster)[[Coverage Status](httpscoveralls.ioreposgithubdjango-webpackdjango-webpack-loaderbadge.svgbranchmaster)](httpscoveralls.iogithubdjango-webpackdjango-webpack-loaderbranchmaster)[pyversions](httpsimg.shields.iopypipyversionsdjango-webpack-loader)[djversions](httpsimg.shields.iopypidjversionsdjango-webpack-loader)Use webpack to generate your static bundles without djangos staticfiles or opaque wrappers.Django webpack loader consumes the output generated by [webpack-bundle-tracker](httpsgithub.comowaiswebpack-bundle-tracker) and lets you use the generated bundles in django.A [changelog](CHANGELOG.md) is also available. CompatibilityTest cases cover Django2.0 on Python3.5. 100 code coverage is the target so we can be sure everything works anytime. It should probably work on older version of django as well but the package does not ship any test cases for them. Installbashnpm install --save-dev webpack-bundle-trackerpip install django-webpack-loader Configuration Configuring webpack-bundle-trackerBefore configuring django-webpack-loader, lets first configure whats necessary on webpack-bundle-tracker side. Update your Webpack configuration file (its usually on webpack.config.js in the project root). Make sure your file looks like this (adapt to your needs)javascriptconst path  require(path)const webpack  require(webpack)const BundleTracker  require(webpack-bundle-tracker)module.exports  {  context __dirname,  entry .assetsjsindex,  output {    path path.resolve(.assetswebpack_bundles),    filename [name]-[hash].js  },  plugins [    new BundleTracker({filename .webpack-stats.json})  ],}The configuration above expects the index.js (the app entrypoint file) to live inside the assetsjs directory (this guide going forward will assume that all front-end related files are placed inside the assets directory, with the different kinds of files arranged within its subdirectories).The generated compiled files will be placed inside the assetswebpack_bundles directory and the file with the information regarding the bundles and assets (webpack-stats.json) will be stored in the project root. Compiling the front-end assetsYou must generate the front-end bundle using webpack-bundle-tracker before using django-webpack-loader. You can compile the assets and generate the bundles by running bashnpx webpack --config webpack.config.js --watchThis will also generate the stats file. You can also refer to how django-react-boilerplate configure the [package.json](httpsgithub.comvintasoftwaredjango-react-boilerplateblobmasterpackage.json) scripts for different situations.  Hot reload is available through a specific config. Check [this section](hot-reload).  This is the recommended usage for the development environment. For usage in production, please refer to [this section](usage-in-production) Configuring the settings fileFirst of all, add webpack_loader to INSTALLED_APPS.pythonINSTALLED_APPS  (  ...  webpack_loader,  ...)Below is the recommended setup for the Django settings file when using django-webpack-loader.pythonWEBPACK_LOADER  {  DEFAULT {    CACHE not DEBUG,    STATS_FILE os.path.join(BASE_DIR, webpack-stats.json),    POLL_INTERVAL 0.1,    IGNORE [r.+.hot-update.js, r.+.map],  }}For that setup, were using the DEBUG variable provided by Django. Since in a production environment (DEBUG  False) the assets files wont constantly change, we can safely cache the results (CACHETrue) and optimize our flow, as django-webpack-loader will read the stats file only once and store the assets files paths in memory. From that point onwards, it will use these stored paths as the source of truth. If CACHEFalse, well always read the stats file to get the assets paths.  If CACHETrue, any changes made in the assets files will only be read when the web workers are restarted.During development, when the stats file changes a lot, we want to always poll for its updated version (in our case, well fetch it every 0.1s, as defined on POLL_INTERVAL).  In production (DEBUGFalse), well only fetch the stats file once, so POLL_INTERVAL is ignored.While CACHE isnt directly related to POLL_INTERVAL, its interesting to keep CACHE binded to the DEBUG logic value (in this case, the negation of the logic value) in order to only cache the assets in production, as wed not continuously poll the stats file in that environment.The STATS_FILE parameter represents the output file produced by webpack-bundle-tracker. Since in the Webpack configuration file weve named it webpack-stats.json and stored it on the project root, we must replicate that setting on the back-end side.IGNORE is a list of regular expressions. If a file generated by Webpack matches one of the expressions, the file will not be included in the template. Extra settings- TIMEOUT is the number of seconds webpack_loader should wait for webpack to finish compiling before raising an exception. 0, None or leaving the value out of settings disables timeouts- INTEGRITY is flag enabling [Subresource Integrity](httpsdeveloper.mozilla.orgen-USdocsWebSecuritySubresource_Integrity) on rendered script and link tags.  Integrity hash is get from stats file and configuration on side of BundleTracker, where configuration option integrity true is required.- LOADER_CLASS is the fully qualified name of a python class as a string that holds the custom webpack loader. This is where behavior can be customized as to how the stats file is loaded. Examples include loading the stats file from a database, cache, external url, etc. For convenience, webpack_loader.loader.WebpackLoader can be extended. The load_assets method is likely where custom behavior will be added. This should return the stats file as an object.- SKIP_COMMON_CHUNKS is a flag which prevents already generated chunks from being included again in the same page. This should only happen if you use more than one entrypoint per Django template (multiple render_bundle calls). By enabling this, you can get the same default behavior of the [HtmlWebpackPlugin](httpswebpack.js.orgpluginshtml-webpack-plugin). The same caveats apply as when using skip_common_chunks on render_bundle, see that section below for more details.Heres a simple example of loading from an external url  py  import requests  from webpack_loader.loader import WebpackLoader  class ExternalWebpackLoader(WebpackLoader)    def load_assets(self)      url  self.config[STATS_URL]      return requests.get(url).json()   RenderingIn order to render the front-end code into the Django templates, we use the render_bundle template tag.Its behavior is to accept a string with the name of an entrypoint from the stats file (in our case, were using main, which is [the default](httpswebpack.js.orgconceptsentry-pointssingle-entry-shorthand-syntax)) and itll proceed to include all files under that entrypoint. You can read more about the entrypoints concept [here](httpswebpack.js.orgconceptsentry-points).  You can also check an example on how to use multiple entry values [here](httpsgithub.comdjango-webpackdjango-webpack-loadertreemasterexamplescode-splitting).Below is the basic usage for render_bundle within a templateHTML+Django{ load render_bundle from webpack_loader }{ render_bundle main }That will render the proper script and link tags needed in your template. Running in developmentFor django-webpack-loader to work, you must run the webpack pipeline. Please refer to [this section](compiling-the-front-end-assets).In summary, you should do the followingbash in one shellnpx webpack --config webpack.config.js --watch in another shellpython manage.py runserver  You can also check [this example](httpsgithub.comdjango-webpackdjango-webpack-loadertreemasterexamplessimple) on how to run a project with django-webpack-loader and webpack-bundle-track. Usage in productionWe recommend that you keep your local bundles and the stats file outside the version control, having a production pipeline that will compile and collect the assets during the deployment phase.You must add STATICFILES_DIRS to your settings file, pointing to the directory where the static files are located. This will let collectstatic know where it should look atpythonSTATICFILES_DIRS  (  os.path.join(BASE_DIR, assets),)Below are the commands that should be run to compile and collect the static files (please note this may change from platform to platform)npm run buildpython manage.py collectstatic --noinputFirst we build the assets and, since we have webpack-bundle-tracker in our front-end building pipeline, the stats file will be populated. Then, we manually run collecstatic to collect the compiled assets.  Heroku is one platform that automatically runs collectstatic for you, so you need to set DISABLE_COLLECTSTATIC1 environment var. Instead, you must manually run collectstatic after running webpack. In Heroku, this is achieved with a post_compile hook. You can see an example on how to implement this flow on [django-react-boilerplate](httpsgithub.comvintasoftwaredjango-react-boilerplatetreemasterbin).However, production usage for this package is fairly flexible. Other approaches may include keeping the production bundles in the version control and take that responsibility from the automatic pipeline. However, you must remember to always build the frontend and generate the bundle before pushing to remote. Usage in testsTo run tests where render_bundle shows up, since we dont have webpack-bundle-tracker at that point to generate the stats file, the calls to render the bundle will fail. The solution is to use the FakeWebpackLoader in your test settingspythonWEBPACK_LOADER[DEFAULT][LOADER_CLASS]  webpack_loader.loader.FakeWebpackLoader Advanced Usage Rendering by file extensionrender_bundle also takes a second argument which can be a file extension to match. This is useful when you want to render different types for files in separately. For example, to render CSS in head and JS at bottom we can do something like this,HTML+Django{ load render_bundle from webpack_loader }html  head    { render_bundle main css }  head  body    ....    { render_bundle main js }  bodyhead Using preloadThe is_preloadTrue option in the render_bundle template tag can be used to add relpreload link tags.HTML+Django{ load render_bundle from webpack_loader }html  head    { render_bundle main css is_preloadTrue }    { render_bundle main js is_preloadTrue }    { render_bundle main css }  head  body    { render_bundle main js }  bodyhtml Accessing other webpack assetswebpack_static template tag provides facilities to load static assets managed by webpack in Django templates. It is like Djangos built in static tag but for webpack assets instead.In the below example, logo.png can be any static asset shipped with any npm package.HTML+Django{ load webpack_static from webpack_loader }-- render full public path of logo.png --img src{ webpack_static logo.png }The public path is based on webpack.config.js [output.publicPath](httpswebpack.js.orgconfigurationoutputoutput-publicpath).Please note that this approach will use the original asset file, and not a post-processed one from the Webpack pipeline, in case that file had gone through such flow (i.e. Youve imported an image on the React side and used it there, the file used within the React components will probably have a hash string on its name, etc. This processed file will be different than the one youll grab with webpack_static). Use skip_common_chunks on render_bundleYou can use the parameter skip_common_chunksTrue or skip_common_chunksFalse to override the global SKIP_COMMON_CHUNKS setting for a specific bundle.In order for this option to work, django-webpack-loader requires the request object to be in the context, to be able to keep track of the generated chunks.The request object is passed by default via the django.template.context_processors.request middleware with using the Django built-in templating system, and also with using Jinja2.If you dont have request in the context for some reason (e.g. using Template.render or render_to_string directly without passing the request), youll get warnings on the console and the common chunks will remain duplicated. Appending file extensionsThe suffix option can be used to append a string at the end of the file URL. For instance, it can be used if your webpack configuration emits compressed .gz files.qweHTML+Django{ load render_bundle from webpack_loader }html  head    meta charsetUTF-8    titleExampletitle    { render_bundle main css }  head  body    { render_bundle main js suffix.gz }  bodyhtml Multiple Webpack configurationsVersion 1.0 and up of django-webpack-loader also supports multiple Webpack configurations. The following configuration defines 2 Webpack stats files in settings and uses the config argument in the template tags to influence which stats file to load the bundles from.pythonWEBPACK_LOADER  {    DEFAULT {        BUNDLE_DIR_NAME bundles,        STATS_FILE os.path.join(BASE_DIR, webpack-stats.json),    },    DASHBOARD {        BUNDLE_DIR_NAME dashboard_bundles,        STATS_FILE os.path.join(BASE_DIR, webpack-stats-dashboard.json),    }}HTML+Django{ load render_bundle from webpack_loader }html  body    ....    { render_bundle main js DEFAULT }    { render_bundle main js DASHBOARD }    -- or render all files from a bundle --    { render_bundle main configDASHBOARD }    -- the following tags do the same thing --    { render_bundle main css DASHBOARD }    { render_bundle main extensioncss configDASHBOARD }    { render_bundle main configDASHBOARD extensioncss }    -- add some extra attributes to the tag --    { render_bundle main js DEFAULT attrsasync charsetUTF-8}  bodyhead File URLs instead of html tagsIf you need the URL to an asset without the HTML tags, the get_files template tag can be used. A common use case is specifying the URL to a custom css file for a Javascript plugin.get_files works exactly like render_bundle except it returns a list of matching files and lets you assign the list to a custom template variable.Each object in the returned list has 2 properties1. name, which is the name of a chunk from the stats file2. url, which can be  1. The publicPath if the asset has one  2. The path to the asset in the static files storage, if the asset doesnt have a publicPath.For exampleHTML+Django{ load get_files from webpack_loader }{ get_files editor css as editor_css_files }CKEDITOR.config.contentsCss  {{ editor_css_files.0.url }}-- or list down name and url for every file --ul{ for css_file in editor_css_files }    li{{ css_file.name }}  {{ css_file.url }}li{ endfor }ul Code splittingIn case you wish to use [code-splitting](httpswebpack.js.orgguidescode-splitting), follow the recipe below on the Javascript side.Create your entrypoint file and add elements to the DOM, while leveraging the lazy imports.js srcprincipal.jsfunction getComponent() {  return import( webpackChunkName lodash  lodash).then(({ default _ })  {    const element  document.createElement(div)    element.innerHTML  _.join([Hello, webpack],  )    return element  }).catch(error  An error occurred while loading the component)}getComponent().then((component)  {  document.body.appendChild(component)})On your configuration file, do the followingjs webpack.config.jsmodule.exports  {  context __dirname,  entry {    principal .srcprincipal,  },  output {    path path.resolve(.dist),     publicPath should match your STATIC_URL config.     This is required otherwise webpack will try to fetch      our chunk generated by the dynamic import from  instead of dist.    publicPath dist,     chunkFilename [name].bundle.js,    filename [name]-[hash].js  },  plugins [    new BundleTracker({ filename .webpack-stats.json })  ]}If youre using Webpack 5 instead of 4, do the following- Change filename [name]-[hash].js to filename [name]-[fullhash].js- Remove  webpackChunkName lodash , which is not needed anymore.On your template, render the bundle as usualHTML+Django-- index.html --{ load render_bundle from webpack_loader }DOCTYPE htmlhtml  head    meta charsetutf-8    titleMy test pagetitle  head  body    pThis is my pagep    { render_bundle principal js }  bodyhtml Hot reloadIn case you wish to enable hot reload for your project using django-webpack-loader and webpack-bundle-tracker, please check out [this example](httpsgithub.comdjango-webpackdjango-webpack-loadertreemasterexampleshot-reload), in particular how [server.js](httpsgithub.comdjango-webpackdjango-webpack-loaderblobmasterexampleshot-reloadserver.js) and [webpack.config.js](httpsgithub.comdjango-webpackdjango-webpack-loaderblobmasterexampleshot-reloadwebpack.config.js) are configured. Jinja2 ConfigurationIf you need to output your assets in a jinja template, we provide a Jinja2 extension thats compatible with [django-jinja](httpsgithub.comniwinzdjango-jinja).To install the extension, add it to the TEMPLATES configuration in the [OPTIONS][extension] list.pythonfrom django_jinja.builtins import DEFAULT_EXTENSIONSTEMPLATES  [  {    BACKEND django_jinja.backend.Jinja2,    OPTIONS {      extensions DEFAULT_EXTENSIONS + [        webpack_loader.contrib.jinja2ext.WebpackExtension,      ],    }  }]Then in your base jinja template, doHTML{{ render_bundle(main) }} Migrating from version  1.0.0In order to use django-webpack-loader1.0.0, you must ensure that webpack-bundle-tracker1.0.0 is being used on the JavaScript side. Its recommended that you always keep at least minor version parity across both packages, for full compatibility.This is necessary because the formatting of webpack-stats.json that webpack-bundle-tracker outputs has changed starting at version 1.0.0-alpha.1. Starting at django-webpack-loader1.0.0, this is the only formatting accepted here, meaning that other versions of that package dont output compatible files anymore, thereby breaking compatibility with older webpack-bundle-tracker releases. Commercial Support[[alt text](httpsavatars2.githubusercontent.comu5529080s80v4 Vinta Logo)](httpswww.vinta.com.br)This project is maintained by [Vinta Software](httpswww.vinta.com.br) and is used in products of Vintas clients. We are always looking for exciting work, so if you need any commercial support, feel free to get in touch contactvinta.com.br</longdescription>
</pkgmetadata>