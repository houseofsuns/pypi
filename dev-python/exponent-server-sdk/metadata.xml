<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> exponent-server-sdk-pythonThis repo is maintained by Expos awesome community heart_eyes So, if you have problems with the code in this repository, please feel free to open an issue, and make a PR. Thanks Installationpip install exponent_server_sdk UsageUse to send push notifications to Exponent Experiences from a Python server.[Full documentation](httpsdocs.expo.ioversionslatestguidespush-notificationshttp2-api) on the API is available if you want to dive into the details.Heres an example on how to use this with retries and reporting via [pyrollbar](httpsgithub.comrollbarpyrollbar).pythonfrom exponent_server_sdk import (    DeviceNotRegisteredError,    PushClient,    PushMessage,    PushResponseError,    PushServerError,) from requests.exceptions import ConnectionError, HTTPError Basic arguments. You should extend this function with the push features you want to use, or simply pass in a PushMessage object.def send_push_message(token, message, extraNone)    try        response  PushClient().publish(            PushMessage(totoken,                        bodymessage,                        dataextra))    except PushServerError as exc         Encountered some likely formattingvalidation error.        rollbar.report_exc_info(            extra_data{                token token,                message message,                extra extra,                errors exc.errors,                response_data exc.response_data,            })        raise    except (ConnectionError, HTTPError) as exc         Encountered some Connection or HTTP error - retry a few times in         case it is transient.        rollbar.report_exc_info(            extra_data{token token, message message, extra extra})        raise self.retry(excexc)    try         We got a response back, but we dont know whether its an error yet.         This call raises errors so we can handle them with normal exception         flows.        response.validate_response()    except DeviceNotRegisteredError         Mark the push token as inactive        from notifications.models import PushToken        PushToken.objects.filter(tokentoken).update(activeFalse)    except PushTicketError as exc         Encountered some other per-notification error.        rollbar.report_exc_info(            extra_data{                token token,                message message,                extra extra,                push_response exc.push_response._asdict(),            })        raise self.retry(excexc)</longdescription>
</pkgmetadata>