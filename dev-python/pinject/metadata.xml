<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Pinject.. image httpsbadge.fury.iopypinject.svg    target httpspypi.orgprojectpinject.. image httpstravis-ci.comgooglepinject.svgbranchmaster    target httpstravis-ci.comgooglepinject.. image httpspepy.techbadgepinject    target httpspepy.techbadgepinject.. image httpspepy.techbadgepinjectmonth    target httpspepy.techbadgepinjectPinject is a dependency injection library for python.The primary goal of Pinject is to help you assemble objects into graphs in aneasy, maintainable way.If you are already familiar with other dependency injection libraries, you maywant to read the condensed summary section at the end, so that you get an ideaof what Pinject is like and how it might differ from libraries youre used to.There is a changelog of differences between released versions near the end ofthis README.Why PinjectIf youre wondering why to use a dependency injection library at all ifyoure writing a lot of object-oriented code in python, then it will make yourlife easier.  See, for instance httpsen.wikipedia.orgwikiDependency_injection httplmgtfy.comqdependency+injectionIf youre wondering why to use Pinject instead of another python dependencyinjection library, a few of reasons are Pinject is much easier to get started with.  Forget having to decorate your code with inject_this and annotate_that just to get started.  With Pinject, you call new_object_graph(), one line, and youre good to go. Pinject is a pythonic dependency injection library.  Python ports of other libraries, like Spring or Guice, retain the feel (and verbosity) of being designed for a statically typed language.  Pinject is designed from the ground up for python. The design choices in Pinject are informed by several dependency injection experts working at Google, based on many years of experience.  Several common confusing or misguided features are omitted altogether from Pinject. Pinject has great error messages.  They tell you exactly what you did wrong, and exactly where.  This should be a welcome change from other dependency frameworks, with their voluminous and yet inscrutable stack traces.Look at the simplest getting-started examples for Pinject and for othersimilar libraries.  Pinject should be uniformly easier to use, clearer toread, and less boilerplate that you need to add.  If you dont find this to bethe case, emailInstallationThe easiest way to install Pinject is to get the latest released version fromPyPI.. code-block shell    sudo pip install pinjectIf you are interested in the developing version, you can install the next version from Test PyPI.. code-block shell    sudo pip install         --no-deps         --no-cache         --upgrade         --index-url httpstest.pypi.orgsimple         pinjectYou can also check out all the source code, including tests, designs, andTODOs.. code-block shell   git clone httpsgithub.comgooglepinjectBasic dependency injectionThe most important function in the pinject module isnew_object_graph().  This creates an ObjectGraph, which you can use toinstantiate objects using dependency injection.  If you pass no args tonew_object_graph(), it will return a reasonably configured defaultObjectGraph... code-block python     class OuterClass(object)    ...     def __init__(self, inner_class)    ...         self.inner_class  inner_class    ...     class InnerClass(object)    ...     def __init__(self)    ...         self.forty_two  42    ...     obj_graph  pinject.new_object_graph()     outer_class  obj_graph.provide(OuterClass)     print outer_class.inner_class.forty_two    42    As you can see, you dont need to tell Pinject how to construct itsObjectGraph, and you dont need to put decorators in your code.  Pinject hasreasonable defaults that allow it to work out of the box.A Pinject binding is an association between an arg name and a provider.In the example above, Pinject created a binding between the arg nameinner_class and an implicitly created provider for the classInnerClass.  The binding it had created was how Pinject knew that itshould pass an instance of InnerClass as the value of the inner_classarg when instantiating OuterClass.Implicit class bindingsPinject creates implicit bindings for classes.  The implicit bindings assumeyour code follows PEP8 conventions your classes are named in CamelCase,and your args are named in lower_with_underscores.  Pinject transformsclass names to injectable arg names by lowercasing words and connecting themwith underscores.  It will also ignore any leading underscore on the classname.+-------------+-------------+ Class name   Arg name    +++ Foo      foo     +-------------+-------------+ FooBar   foo_bar +-------------+-------------+ _Foo     foo     +-------------+-------------+ _FooBar  foo_bar +-------------+-------------+If two classes map to the same arg name, whether those classes are in the samemodule or different modules, Pinject will not create an implicit binding forthat arg name (though it will not raise an error).Finding classes and providers for implicit bindingsSo far, the examples have not told new_object_graph() the classes forwhich it should create implicit bindings.  new_object_graph() by defaultlooks in all imported modules, but you may occasionally want to restrict theclasses for which new_object_graph() creates implicit bindings.  If so,new_object_graph() has two args for this purpose. The modules arg specifies in which (python) modules to look for classes this defaults to ALL_IMPORTED_MODULES. The classes arg specifies a exact list of classes this defaults to None... code-block python     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class Foo(object)    ...     pass    ...     obj_graph  pinject.new_object_graph(modulesNone, classes[SomeClass])      obj_graph.provide(SomeClass)   would raise a NothingInjectableForArgError     obj_graph  pinject.new_object_graph(modulesNone, classes[SomeClass, Foo])     some_class  obj_graph.provide(SomeClass)    Auto-copying args to fieldsOne thing that can get tedious about dependency injection via initializers isthat you need to write __init__() methods that copy args to fields.  These__init__() methods can get repetitive, especially when you have severalinitializer args... code-block python     class ClassWithTediousInitializer(object)    ...     def __init__(self, foo, bar, baz, quux)    ...         self._foo  foo    ...         self._bar  bar    ...         self._baz  baz    ...         self._quux  quux    ...    Pinject provides decorators that you can use to avoid repetitive initializerbodies. copy_args_to_internal_fields prepends an underscore, i.e., it copies an arg named foo to a field named _foo.  Its useful for normal classes. copy_args_to_public_fields copies the arg named as-is, i.e., it copies an arg named foo to a field named foo.  Its useful for data objects... code-block python     class ClassWithTediousInitializer(object)    ...     pinject.copy_args_to_internal_fields    ...     def __init__(self, foo, bar, baz, quux)    ...         pass    ...     cwti  ClassWithTediousInitializer(a-foo, a-bar, a-baz, a-quux)     print cwti._foo    a-foo    When using these decorators, youll normally pass in the body of theinitializer, but you can put other statements there if you need to.  The argswill be copied to fields before the initializer body is executed.These decorators can be applied to initializers that take kwargs but notinitializers that take pargs (since it would be unclear what field nameto use).Binding specsTo create any bindings more complex than the implicit class bindings describedabove, you use a binding spec.  A binding spec is any python class thatinherits from BindingSpec.  A binding spec can do three things Its configure() method can create explicit bindings to classes or instances, as well as requiring bindings without creating them. Its dependencies() method can return depended-on binding specs. It can have provider methods, for which explicit bindings are created.The new_object_graph() function takes a sequence of binding spec instancesas its binding_specs arg.  new_object_graph() takes binding specinstances, rather than binding spec classes, so that you can manually injectany initial dependencies into the binding specs as needed.Binding specs should generally live in files named binding_specs.py, whereeach file is named in the plural even if there is exactly one binding spec init.  Ideally, a directorys worth of functionality should be coverable with asingle binding spec.  If not, you can create multiple binding specs in thesame binding_specs.py file.  If you have so many binding specs that youneed to split them into multiple files, you should name them each with a_binding_specs.py suffix.Binding spec configure() methods------------------------------------Pinject creates implicit bindings for classes, but sometimes the implicitbindings arent what you want.  For instance, if you haveSomeReallyLongClassName, you may not want to name your initializer argssome_really_long_class_name but instead use something shorter likelong_name, just for this class.For such situations, you can create explicit bindings using theconfigure() method of a binding spec.  The configure() method takes afunction bind() as an arg and calls that function to create explicitbindings... code-block python     class SomeClass(object)    ...     def __init__(self, long_name)    ...         self.long_name  long_name    ...     class SomeReallyLongClassName(object)    ...     def __init__(self)    ...         self.foo  foo    ...     class MyBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(long_name, to_classSomeReallyLongClassName)    ...     obj_graph  pinject.new_object_graph(binding_specs[MyBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.long_name.foo    foo    The bind() function passed to a binding function binds its first arg,which must be an arg name (as a str), to exactly one of two kinds ofthings. Using to_class binds to a class.  When the binding is used, Pinject injects an instance of the class. Using to_instance binds to an instance of some object.  Every time the binding is used, Pinject uses that instance... code-block python     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class MyBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_instancea-foo)    ...     obj_graph  pinject.new_object_graph(binding_specs[MyBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.foo    a-foo    The configure() method of a binding spec also may take a functionrequire() as an arg and use that function to require that a binding bepresent without actually defining that binding.  require() takes as argsthe name of the arg for which to require a binding... code-block python     class MainBindingSpec(pinject.BindingSpec)    ...     def configure(self, require)    ...         require(foo)    ...     class RealFooBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_instancea-real-foo)    ...     class StubFooBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_instancea-stub-foo)    ...     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     obj_graph  pinject.new_object_graph(    ...     binding_specs[MainBindingSpec(), RealFooBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.foo    a-real-foo      pinject.new_object_graph(    ...     binding_specs[MainBindingSpec()])   would raise a MissingRequiredBindingError    ...    Being able to require a binding without defining the binding is useful whenyou know the code will need some dependency satisfied, but there is more thanone implementation that satisfies that dependency, e.g., there may be a realRPC client and a fake RPC client.  Declaring the dependency means that anyexpected but missing bindings will be detected early, whennew_object_graph() is called, rather than in the middle of running yourprogram.Youll notice that the configure() methods above have differentsignatures, sometimes taking the arg bind and sometimes taking the argrequire.  configure() methods must take at least one arg that iseither bind or require, and they may have both args.  Pinject willpass whichever arg or args your configure() method needs.Binding spec dependencies-------------------------Binding specs can declare dependencies.  A binding spec declares itsdependencies by returning a sequence of instances of the dependent bindingspecs from its dependencies() method... code-block python     class ClassOne(object)    ...    def __init__(self, foo)    ...        self.foo  foo    ....     class BindingSpecOne(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_instancefoo-)    ...     class ClassTwo(object)    ...     def __init__(self, class_one, bar)    ...         self.foobar  class_one.foo + bar    ...     class BindingSpecTwo(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(bar, to_instance-bar)    ...     def dependencies(self)    ...         return [BindingSpecOne()]    ...     obj_graph  pinject.new_object_graph(binding_specs[BindingSpecTwo()])     class_two  obj_graph.provide(ClassTwo)     print class_two.foobar    foo--bar    If classes from module A are injected as collaborators into classes frommodule B, then you should consider having the binding spec for module B dependon the binding spec for module A.  In the example above, ClassOne isinjected as a collaborator into ClassTwo, and so BindingSpecTwo (thebinding spec for ClassTwo) depends on BindingSpecOne (the binding specfor ClassOne).In this way, you can build a graph of binding spec dependencies that mirrorsthe graph of collaborator dependencies.Since explicit bindings cannot conflict (see the section below on bindingprecedence), a binding spec should only have dependencies that there willnever be a choice about using.  If there may be a choice, then it is better tolist the binding specs separately and explicitly when callingnew_object_graph().The binding spec dependencies can be a directed acyclic graph (DAG) that is,binding spec A can be a dependency of B and of C, and binding spec D can havedependencies on B and C.  Even though there are multiple dependency paths fromD to A, the bindings in binding spec A will only be evaluated once.The binding spec instance of A that is a dependency of B is considered thesame as the instance that is a dependency of C if the two instances are equal(via __eq__()).  The default implementation of __eq__() inBindingSpec says that two binding specs are equal if they are of exactlythe same python type.  You will need to override __eq__() (as well as__hash__()) if your binding spec is parameterized, i.e., if it takes oneor more initializer args so that two instances of the binding spec may behavedifferently... code-block python     class SomeBindingSpec(pinject.BindingSpec)    ...     def __init__(self, the_instance)    ...         self._the_instance  the_instance    ...     def configure(self, bind)    ...         bind(foo, to_instanceself._the_instance)    ...     def __eq__(self, other)    ...         return (type(self)  type(other) and    ...                 self._the_instance  other._the_instance)    ...     def __hash__(self)    ...         return hash(type(self))  hash(self._the_instance)    ...    Provider methods----------------If it takes more to instantiate a class than calling its initializer andinjecting initializer args, then you can write a provider method for it.Pinject can use provider methods to instantiate objects used to inject as thevalues of other args.Pinject looks on binding specs for methods named like provider methods andthen creates explicit bindings for them... code-block python     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def provide_foo(self)    ...         return some-complex-foo    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.foo    some-complex-foo    Pinject looks on binding specs for methods whose names start withprovide_, and it assumes that the methods are providers for whatever therest of their method names are.  For instance, Pinject assumes that the methodprovide_foo_bar() is a provider method for the arg name foo_bar.Pinject injects all args of provider methods that have no default when itcalls the provider method... code-block python     class SomeClass(object)    ...     def __init__(self, foobar)    ...         self.foobar  foobar    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def provide_foobar(self, bar, hyphen-)    ...         return foo + hyphen + bar    ...     def provide_bar(self)    ...         return bar    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.foobar    foo-bar    Binding precedenceBindings have precedence explicit bindings take precedence over implicitbindings. Explicit bindings are the bindings that come from binding specs. Implicit bindings are the bindings created for classes in the modules and classes args passed to new_object_graph().Pinject will prefer an explicit to an implicit binding... code-block python     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class Foo(object)    ...     pass    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_instancefoo-instance)    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.foo    foo-instance    If you have two classes named the same thing, Pinject will have two different(and thus conflicting) implicit bindings.  But Pinject will not complainunless you try to use those bindings.  Pinject will complain if you try tocreate different (and thus conflicting) explicit bindings.SafetyPinject tries to strike a balance between being helpful and being safe.Sometimes, you may want or need to change this balance.new_object_graph() uses implicit bindings by default.  If you worry thatyou may accidentally inject a class or use a provider functionunintentionally, then you can make new_object_graph() ignore implicitbindings, by setting only_use_explicit_bindingsTrue.  If you do so, thenPinject will only use explicit bindings.If you want to promote an implicit binding to be an explicit binding, you canannotate the corresponding class with inject().  The inject()decorator lets you create explicit bindings without needing to create bindingspecs, as long as you can live with the binding defaults (e.g., no annotationson args, see below)... code-block python     class ExplicitlyBoundClass(object)    ...     pinject.inject()    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class ImplicitlyBoundClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_instanceexplicit-foo)    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()],    ...     only_use_explicit_bindingsTrue)      obj_graph.provide(ImplicitlyBoundClass)   would raise a NonExplicitlyBoundClassError     some_class  obj_graph.provide(ExplicitlyBoundClass)     print some_class.foo    explicit-foo    You can also promote an implicit binding to explicit by usingannotated_arg() (see below), with or without inject() as well.(Previous versions of Pinject included an injectable decorator.  That isdeprecated in favor of inject().  Note that inject() needs parens,whereas injectable didnt.)On the opposite side of permissiveness, Pinject by default will complain if aprovider method returns None.  If you really want to turn off this defaultbehavior, you can pass allow_injecting_noneTrue tonew_object_graph().AnnotationsPinject annotations let you have different objects injected for the same argname.  For instance, you may have two classes in different parts of yourcodebase named the same thing, and you want to use the same arg name indifferent parts of your codebase.On the arg side, an annotation tells Pinject only to inject using a bindingwhose binding key includes the annotation object.  You can useannotate_arg() on an initializer, or on a provider method, to specify theannotation object.On the binding side, an annotation changes the binding so that the key of thebinding includes the annotation object.  When using bind() in a bindingspecs configure() method, you can pass an annotated_with arg tospecify the annotation object... code-block python     class SomeClass(object)    ...     pinject.annotate_arg(foo, annot)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, annotated_withannot, to_instancefoo-with-annot)    ...         bind(foo, annotated_with12345, to_instance12345-foo)    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.foo    foo-with-annot    Also on the binding side, when defining a provider method, you can use theprovides() decorator.  The decorator lets you pass an annotated_witharg to specify the annotation object.  The decorators first param,arg_name also lets you override what arg name you want the provider to befor.  This is optional but useful if you want the same binding spec to havetwo provider methods for the same arg name but annotated differently.(Otherwise, the methods would need to be named the same, since theyre for thesame arg name.).. code-block python     class SomeClass(object)    ...     pinject.annotate_arg(foo, annot)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     pinject.provides(foo, annotated_withannot)    ...     def provide_annot_foo(self)    ...         return foo-with-annot    ...     pinject.provides(foo, annotated_with12345)    ...     def provide_12345_foo(self)    ...         return 12345-foo    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     some_class  obj_graph.provide(SomeClass)     print some_class.foo    foo-with-annot    When requiring a binding, via the require arg passed into theconfigure() method of a binding spec, you can pass the argannotated_with to require an annotated binding... code-block python     class MainBindingSpec(pinject.BindingSpec)    ...     def configure(self, require)    ...         require(foo, annotated_withannot)    ...     class NonSatisfyingBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_instancean-unannotated-foo)    ...     class SatisfyingBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, annotated_withannot, to_instancean-annotated-foo)    ...     obj_graph  pinject.new_object_graph(    ...     binding_specs[MainBindingSpec(), SatisfyingBindingSpec()])   works      obj_graph  pinject.new_object_graph(    ...      binding_specs[MainBindingSpec(),    ...                     NonSatisfyingBindingSpec()])   would raise a MissingRequiredBindingError    You can use any kind of object as an annotation object as long as itimplements __eq__() and __hash__().ScopesBy default, Pinject remembers the object it injected into a (possiblyannotated) arg, so that it can inject the same object into other args with thesame name.  This means that, for each arg name, a single instance of thebound-to class, or a single instance returned by a provider method, is createdby default... code-block python     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def provide_foo(self)    ...         return object()    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     some_class_1  obj_graph.provide(SomeClass)     some_class_2  obj_graph.provide(SomeClass)     print some_class_1.foo is some_class_2.foo    True    In some cases, you may want to create new instances, always or sometimes,instead of reusing them each time theyre injected.  If so, you want to usescopes.A scope controls memoization (i.e., caching).  A scope can choose to cachenever, sometimes, or always.Pinject has two built-in scopes.  Singleton scope (SINGLETON) is thedefault and always caches.  Prototype scope (PROTOTYPE) is the otherbuilt-in option and does no caching whatsoever.Every binding is associated with a scope.  You can specify a scope for abinding by decorating a provider method with in_scope(), or by passing anin_scope arg to bind() in a binding specs configure() method... code-block python     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     pinject.provides(in_scopepinject.PROTOTYPE)    ...     def provide_foo(self)    ...         return object()    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     some_class_1  obj_graph.provide(SomeClass)     some_class_2  obj_graph.provide(SomeClass)     print some_class_1.foo is some_class_2.foo    False    If a binding specifies no scope explicitly, then it is in singleton scope.Implicit class bindings are always in singleton scope.Memoization of class bindings works at the class level, not at the binding keylevel.  This means that, if you bind two arg names (or the same arg name withtwo different annotations) to the same class, and the class is in a memoizingscope, then the same class instance will be provided when you inject thedifferent arg names... code-block python     class InjectedClass(object)    ...     pass    ...     class SomeObject(object)    ...     def __init__(self, foo, bar)    ...         self.foo  foo    ...         self.bar  bar    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_classInjectedClass)    ...         bind(bar, to_classInjectedClass)    ...     obj_graph  pinject.new_object_graph(    ...     binding_specs[SomeBindingSpec()])     some_object  obj_graph.provide(SomeObject)     print some_object.foo is some_object.bar    True    Pinject memoizes class bindings this way because this is more likely to bewhat you mean if you bind two different arg names to the same class insingleton scope you want only one instance of the class, even though it maybe injected in multiple places.Provider bindingsSometimes, you need to inject not just a single instance of some class, butrather you need to inject the ability to create instances on demand.(Clearly, this is most useful when the binding youre using is not in thesingleton scope, otherwise youll always get the same instance, and you may aswell just inject that..)You could inject the Pinject object graph, but youd have to do thatdependency injection manually (Pinject doesnt inject itself), and youd beinjecting a huge set of capabilities when your class really only needs toinstantiate objects of one type.To solve this, Pinject creates provider bindings for each bound arg name.It will look at the arg name for the prefix provide_, and if it finds thatprefix, it assumes you want to inject a provider function for whatever therest of the arg name is.  For instance, if you have an arg namedprovide_foo_bar, then Pinject will inject a zero-arg function that, whencalled, provides whatever the arg name foo_bar is bound to... code-block python     class Foo(object)    ...   def __init__(self)    ...     self.forty_two  42    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def configure(self, bind)    ...         bind(foo, to_classFoo, in_scopepinject.PROTOTYPE)    ...     class NeedsProvider(object)    ...     def __init__(self, provide_foo)    ...         self.provide_foo  provide_foo    ...     obj_graph  pinject.new_object_graph(binding_specs[SomeBindingSpec()])     needs_provider  obj_graph.provide(NeedsProvider)     print needs_provider.provide_foo() is needs_provider.provide_foo()    False     print needs_provider.provide_foo().forty_two    42    Pinject will always look for the provide_ prefix as a signal to inject aprovider function, anywhere it injects dependencies (initializer args, bindingspec provider methods, etc.).  This does mean that its quite difficult, say,to inject an instance of a class named ProvideFooBar into an arg namedprovide_foo_bar, but assuming youre naming your classes as noun phrasesinstead of verb phrases, this shouldnt be a problem.Watch out dont confuse provider bindings, which let you inject args named provide_something with provider functions and provider methods, which are methods of binding specs that provide instances of some arg name.Partial injectionProvider bindings are useful when you want to create instances of a class ondemand.  But a zero arg provider function will always return an instanceconfigured the same way (within a given scope).  Sometimes, you want theability to parameterize the provided instances, e.g., based on run-time userconfiguration.  You want the ability to create instances where part of theinitialization data is provided per-instance at run-time and part of theinitialization data is injected as dependencies.To do this, other dependency injection libraries have you define factoryclasses.  You inject dependencies into the factory classs initializerfunction, and then you call the factory classs creation method with theper-instance data... code-block python     class WidgetFactory(object)    ...     def __init__(self, widget_polisher)    ...         self._widget_polisher  widget_polisher    ...     def new(self, color)   normally would contain some non-trivial code...    ...         return some_function_of(self._widget_polisher, color)    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def provide_something_with_colored_widgets(self, colors, widget_factory)    ...         return SomethingWithColoredWidgets(    ...             [widget_factory.new(color) for color in colors])    ...    You can follow this pattern in Pinject, but it involves boring boilerplate forthe factory class, saving away the initializer-injected dependencies to beused in the creation method.  Plus, you have to create yet another...Factory class, which makes you feel like youre programming in java,not python.As a less repetitive alternative, Pinject lets you use partial injection onthe provider functions returned by provider bindings.  You use theinject() decorator to tell Pinject ahead of time which args you expect topass directly (vs. automatic injection), and then you pass those args directlywhen calling the provider function... code-block python     class SomeBindingSpec(pinject.BindingSpec)    ...     pinject.inject([widget_polisher])    ...     def provide_widget(self, color, widget_polisher)    ...         return some_function_of(widget_polisher, color)    ...     def provide_something_needing_widgets(self, colors, provide_widget)    ...         return SomethingNeedingWidgets(    ...             [provide_widget(color) for color in colors])    ...    The first arg to inject(), arg_names, specifies which args of thedecorated method should be injected as dependencies.  If specified, it must bea non-empty sequence of names of the decorated methods args.  The remainingdecorated method args will be passed directly.In the example above, note that, although there is a method calledprovide_widget() and an arg of provide_something_needing_widgets()called provide_widget, these are not exactly the same  The latter is adependency-injected wrapper around the former.  The wrapper ensures that thecolor arg is passed directly and then injects the widget_polisherdependency.The inject() decorator works to specify args passed directly both forprovider bindings to provider methods (as in the example above) and forprovider bindings to classes (where you can pass args directly to theinitializer, as in the example below)... code-block python     class Widget(object)    ...     pinject.inject([widget_polisher])    ...     def __init__(self, color, widget_polisher)    ...         pass   normally something involving color and widget_polisher    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def provide_something_needing_widgets(self, colors, provide_widget)    ...         return SomethingNeedingWidgets(    ...             [provide_widget(color) for color in colors])    ...    The inject() decorator also takes an all_except arg.  You can usethis, instead of the (first positional) arg_names arg, if its clearer andmore concise to say which args are not injected (i.e., which args are passeddirectly)... code-block python     class Widget(object)    ...      equivalent to pinject.inject([widget_polisher])    ...     pinject.inject(all_except[color])    ...     def __init__(self, color, widget_polisher)    ...         pass   normally something involving color and widget_polisher    ...    If both arg_names and all_except are omitted, then all args areinjected by Pinject, and none are passed directly.  (Both arg_names andall_except may not be specified at the same time.)  Wildcard positionaland keyword args (i.e., pargs and kwargs) are always passeddirectly, not injected.If you use inject() to mark at least one arg of a provider method (orinitializer) as passed directly, then you may no longer directly inject thatprovider methods corresponding arg name.  You must instead use a providerbinding to inject a provider function, and then pass the required directarg(s), as in the examples above.Custom scopesIf you want to, you can create your own custom scope.  A custom scope isuseful when you have some objects that need to be reused (i.e., cached) butwhose lifetime is shorter than the entire lifetime of your program.A custom scope is any class that implements the Scope interface... code-block python    class Scope(object)        def provide(self, binding_key, default_provider_fn)            raise NotImplementedError()The binding_key passed to provide() will be an object implementing__eq__() and __hash__() but otherwise opaque (you shouldnt need tointrospect it).  You can think of the binding key roughly as encapsulating thearg name and annotation (if any).  The default_provider_fn passed toprovide() is a zero-arg function that, when called, provides an instanceof whatever should be provided.The job of a scopes provide() function is to return a cached object ifavailable and appropriate, otherwise to return (and possibly cache) the resultof calling the default provider function.Scopes almost always have other methods that control clearing the scopescache.  For instance, a scope may have enter scope and exit scope methods,or a single direct clear cache method.  When passing a custom scope toPinject, your code should keep a handle to the custom scope and use thathandle to clear the scopes cache at the appropriate time.You can use one or more custom scopes by passing a map from scope identifierto scope as the id_to_scope arg of new_object_graph()... code-block python     class MyScope(pinject.Scope)    ...     def __init__(self)    ...         self._cache  {}    ...     def provide(self, binding_key, default_provider_fn)    ...         if binding_key not in self._cache    ...             self._cache[binding_key]  default_provider_fn()    ...         return self._cache[binding_key]    ...     def clear(self)    ...         self._cache  {}    ...     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     pinject.provides(in_scopemy custom scope)    ...     def provide_foo(self)    ...         return object()    ...     my_scope  MyScope()     obj_graph  pinject.new_object_graph(    ...     binding_specs[SomeBindingSpec()],    ...     id_to_scope{my custom scope my_scope})     some_class_1  obj_graph.provide(SomeClass)     some_class_2  obj_graph.provide(SomeClass)     my_scope.clear()     some_class_3  obj_graph.provide(SomeClass)     print some_class_1.foo is some_class_2.foo    True     print some_class_2.foo is some_class_3.foo    False    A scope identifier can be any object implementing __eq__() and__hash__().If you plan to use Pinject in a multi-threaded environment (and even if youdont plan to now but may some day), you should make your custom scopethread-safe.  The example custom scope above could be trivially (but moreverbosely) rewritten to be thread-safe, as in the example below.  The lock isreentrant so that something in MyScope can be injected into something elsein MyScope... code-block python     class MyScope(pinject.Scope)    ...     def __init__(self)    ...         self._cache  {}    ...         self._rlock  threading.RLock()    ...     def provide(self, binding_key, default_provider_fn)    ...         with self._rlock    ...             if binding_key not in self._cache    ...                 self._cache[binding_key]  default_provider_fn()    ...             return self._cache[binding_key]    ...     def clear(self)    ...         with self._rlock    ...             self._cache  {}    Scope accessibilityTo prevent yourself from injecting objects where they dont belong, you maywant to validate one object being injected into another w.r.t. scope.For instance, you may have created a custom scope for HTTP requests handled byyour program.  Objects in request scope would be cached for the duration of asingle HTTP request.  You may want to verify that objects in request scopenever get injected into objects in singleton scope.  Such an injection islikely not to make semantic sense, since it would make something tied to oneHTTP request be used for the duration of your program.Pinject lets you pass a validation function as theis_scope_usable_from_scope arg to new_object_graph().  This functiontakes two scope identifiers and returns True iff an object in the firstscope can be injected into an object of the second scope... code-block python     class RequestScope(pinject.Scope)    ...     def start_request(self)    ...         self._cache  {}    ...     def provide(self, binding_key, default_provider_fn)    ...         if binding_key not in self._cache    ...             self._cache[binding_key]  default_provider_fn()    ...         return self._cache[binding_key]    ...     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     pinject.provides(in_scopepinject.SINGLETON)    ...     def provide_foo(bar)    ...         return foo- + bar    ...     pinject.provides(in_scoperequest scope)    ...     def provide_bar()    ...         return -bar    ...     def is_usable(scope_id_inner, scope_id_outer)    ...     return not (scope_id_inner  request scope and    ...                 scope_id_outer  scoping.SINGLETON)    ...     my_request_scope  RequestScope()     obj_graph  pinject.new_object_graph(    ...     binding_specs[SomeBindingSpec()],    ...     id_to_scope{request scope my_request_scope},    ...     is_scope_usable_from_scopeis_usable)     my_request_scope.start_request()      obj_graph.provide(SomeClass)   would raise a BadDependencyScopeError    The default scope accessibility validator allows objects from any scope to beinjected into objects from any other scope.Changing naming conventionsIf your code follows PEP8 naming coventions, then youre likely happy with thedefault implicit bindings (where the class FooBar gets bound to the argname foo_bar) and where provide_foo_bar() is a binding specs providermethod for the arg name foo_bar.But if not, read onCustomizing implicit bindings-----------------------------new_object_graph() takes a get_arg_names_from_class_name arg.  This isthe function that is used to determine implicit class bindings.  This functiontakes in a class name (e.g., FooBar) and returns the arg names to whichthat class should be implicitly bound (e.g., [foo_bar]).  Its defaultbehavior is described in the implicit class bindings section above, but thatdefault behavior can be overridden.For instance, suppose that your code uses a library that names many classeswith the leading letter X (e.g., XFooBar), and youd like to be able tobind that to a corresponding arg name without the leading X (e.g.,foo_bar)... code-block python     import re     def custom_get_arg_names(class_name)    ...     stripped_class_name  re.sub(_X, , class_name)    ...     return [re.sub(()([A-Z]+), r_1, stripped_class_name).lower()]    ...     print custom_get_arg_names(XFooBar)    [foo_bar]     print custom_get_arg_names(XLibraryClass)    [library_class]     class OuterClass(object)    ...     def __init__(self, library_class)    ...         self.library_class  library_class    ...     class XLibraryClass(object)    ...     def __init__(self)    ...         self.forty_two  42    ...     obj_graph  pinject.new_object_graph(    ...     get_arg_names_from_class_namecustom_get_arg_names)     outer_class  obj_graph.provide(OuterClass)     print outer_class.library_class.forty_two    42    The function passed as the get_arg_names_from_class_name arg tonew_object_graph() can return as many or as few arg names as it wants.  Ifit always returns the empty list (i.e., if it is lambda _ []), then thatdisables implicit class bindings.Customizing binding spec method names-------------------------------------The standard binding spec methods to configure bindings and declaredependencies are named configure and dependencies, by default.  If youneed to, you can change their names by passing configure_method_nameandor dependencies_method_name as args to new_object_graph()... code-block python     class NonStandardBindingSpec(pinject.BindingSpec)    ...     def Configure(self, bind)    ...         bind(forty_two, to_instance42)    ...     class SomeClass(object)    ...     def __init__(self, forty_two)    ...         self.forty_two  forty_two    ...     obj_graph  pinject.new_object_graph(    ...     binding_specs[NonStandardBindingSpec()],    ...     configure_method_nameConfigure)     some_class  obj_graph.provide(SomeClass)     print some_class.forty_two    42    Customizing provider method names---------------------------------new_object_graph() takes a get_arg_names_from_provider_fn_name arg.This is the function that is used to identify provider methods on bindingspecs.  This function takes in the name of a potential provider method (e.g.,provide_foo_bar) and returns the arg names for which the provider methodis a provider, if any (e.g., [foo_bar]).  Its default behavior isdescribed in the provider methods section above, but that default behaviorcan be overridden.For instance, suppose that you work for a certain large corporation whosepython style guide makes you name functions in CamelCase, and so you needto name the provider method for the arg name foo_bar more likeProvideFooBar than provide_foo_bar... code-block python     import re     def CustomGetArgNames(provider_fn_name)    ...     if provider_fn_name.startswith(Provide)    ...         provided_camelcase  provider_fn_name[len(Provide)]    ...         return [re.sub(()([A-Z]+), r_1, provided_camelcase).lower()]    ...     else    ...         return []    ...     print CustomGetArgNames(ProvideFooBar)    [foo_bar]     print CustomGetArgNames(ProvideFoo)    [foo]     class SomeClass(object)    ...     def __init__(self, foo)    ...         self.foo  foo    ...     class SomeBindingSpec(pinject.BindingSpec)    ...     def ProvideFoo(self)    ...         return some-foo    ...     obj_graph  pinject.new_object_graph(    ...     binding_specs[SomeBindingSpec()],    ...     get_arg_names_from_provider_fn_nameCustomGetArgNames)     some_class  obj_graph.provide(SomeClass)     print some_class.foo    some-foo    The function passed as the get_arg_names_from_provider_fn_name arg tonew_object_graph() can return as many or as few arg names as it wants.  Ifit returns an empty list, then that potential provider method is assumed notactually to be a provider method.MiscellaneousPinject raises helpful exceptions whose messages include the file and linenumber of errors.  So, Pinject by default will shorten the stack trace ofexceptions that it raises, so that you dont see the many levels of functioncalls within the Pinject library.In some situations, though, the complete stack trace is helpful, e.g., whendebugging Pinject, or when your code calls Pinject, which calls back into yourcode, which calls back into Pinject.  In such cases, to disable exceptionstack shortening, you can pass use_short_stack_tracesFalse tonew_object_graph().GotchasPinject has a few things to watch out for.Thread safety-------------Pinjects default scope is SINGLETON.  If you have a multi-threadedprogram, its likely that some or all of the things that Pinject provides fromsingleton scope will be used in multiple threads.  So, its important that youensure that such classes are thread-safe.Similarly, its important that your custom scope classes are thread-safe.Even if the objects they provide are only used in a single thread, it may bethat the object graph (and therefore the scope itself) will be usedsimultaneously in multiple threads.Remember to make locks re-entrant on your custom scope classes, or otherwisedeal with one object in your custom scope trying to inject another object inyour custom scope.Thats it for gotchas, for now.Condensed summaryIf you are already familiar with dependency injection libraries such as Guice,this section gives you a condensed high level summary of Pinject and how itmight be similar to or different than other dependency injection libraries.(If you dont understand it, no problem.  The rest of the documentation coverseverything listed here.) Pinject uses code and decorators to configure injection, not a separate config file. Bindings are keyed by arg name, (not class type, since Python is dynamically typed). Pinject automatically creates bindings to some_class arg names for SomeClass classes. You can ask Pinject only to create bindings from binding specs and classes whose __init__() is marked with inject(). A binding spec is a class that creates explicit bindings. A binding spec can bind arg names to classes or to instances. A binding spec can bind arg names foo to provider methods provide_foo(). Binding specs can depend on (i.e., include) other binding specs. You can annotate args and bindings to distinguish among argsbindings for the same arg name. Pinject has two built-in scopes singleton (always memoized the default) and prototype (never memoized). You can define custom scopes, and you can configure which scopes are accessible from which other scopes. Pinject doesnt allow injecting None by default, but you can turn off that check.Changelogv0.13 masterv0.12 28 Nov, 2018 Support Python 3 Add two maintainers trein and huanv0.10.2 Fixed bug allows binding specs containing only provider methods.v0.10.1 Fixed bug allows omitting custom named configure() binding spec method.v0.10 Added default __eq__() to BindingSpec, so that DAG binding spec dependencies can have equal but not identical dependencies. Allowed customizing configure() and dependencies() binding spec method names. Deprecated injectable in favor of inject. Added partial injection. Added require arg to allow binding spec configure methods to declare but not define bindings. Sped up tests (and probably general functionality) by 10x. Documented more design decisions. Added copy_args_to_internal_fields and copy_args_to_public_fields. Renamed InjectableDecoratorAppliedToNonInitError to DecoratorAppliedToNonInitError.v0.9 Added validation of python types of public args. Improved error messages for all Pinject-raised exceptions. Added use_short_stack_traces arg to new_object_graph(). Allowed multiple provides on single provider method.v0.8 First released version.Maintainers Kurt Steinkraus kurt Guilherme Trein trein Huan LI huanLicenseApache-2.0Pinject and GoogleThough Google owns this projects copyright, this project is not an officialGoogle product.</longdescription>
</pkgmetadata>