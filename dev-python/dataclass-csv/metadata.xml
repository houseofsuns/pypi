<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>[[Build Status](httpstravis-ci.orgdfurtadodataclass-csv.svgbranchmaster)](httpstravis-ci.orgdfurtadodataclass-csv)[[pypi](httpsimg.shields.iopypivdataclass-csv.svg)](httpspypi.python.orgpypidataclass-csv)[[Downloads](httpspepy.techbadgedataclass-csv)](httpspepy.techprojectdataclass-csv) Dataclass CSVDataclass CSV makes working with CSV files easier and much better than working with Dicts. It uses Pythons Dataclasses to store data of every row on the CSV file and also uses type annotations which enables proper type checking and validation. Main features- Use dataclasses instead of dictionaries to represent the rows in the CSV file.- Take advantage of the dataclass properties type annotation. DataclassReader use the type annotation to perform validation of the data of the CSV file.- Automatic type conversion. DataclassReader supports str, int, float, complex, datetime and bool, as well as any type whose constructor accepts a string as its single argument.- Helps you troubleshoot issues with the data in the CSV file. DataclassReader will show exactly in which line of the CSV file contain errors.- Extract only the data you need. It will only parse the properties defined in the dataclass- Familiar syntax. The DataclassReader is used almost the same way as the DictReader in the standard library.- It uses dataclass features that let you define metadata properties so the data can be parsed exactly the way you want.- Make the code cleaner. No more extra loops to convert data to the correct type, perform validation, set default values, the DataclassReader will do all this for you.- In additon of the DataclassReader the library also provides a DataclassWriter which enables creating a CSV fileusing a list of instances of a dataclass. Installationshellpipenv install dataclass-csv Getting started Using the DataclassReaderFirst, add the necessary importspythonfrom dataclasses import dataclassfrom dataclass_csv import DataclassReaderAssuming that we have a CSV file with the contents belowtextfirstname,email,ageElsa,elsatest.com, 11Astor,astortest.com, 7Edit,edittest.com, 3Ella,ellatest.com, 2Lets create a dataclass that will represent a row in the CSV file abovepythondataclassclass User    firstname str    email str    age intThe dataclass User has 3 properties, firstname and email is of type str and age is of type int.To load and read the contents of the CSV file we do the same thing as if we would be using the DictReader from the csv module in the Pythons standard library. After opening the file we create an instance of the DataclassReader passing two arguments. The first is the file and the second is the dataclass that we wish to use to represent the data of every row of the CSV file. Like sopythonwith open(filename) as users_csv    reader  DataclassReader(users_csv, User)    for row in reader        print(row)The DataclassReader internally uses the DictReader from the csv module to read the CSV file which means that you can pass the same arguments that you would pass to the DictReader. The complete argument list is shown belowpythondataclass_csv.DataclassReader(    f,    cls,    fieldnamesNone,    restkeyNone,    restvalNone,    dialectexcel,    args,    kwds)All keyword arguments support by DictReader are supported by the DataclassReader, with the addition ofvalidate_header - The DataclassReader will raise a ValueError if the CSV file cointain columns with the same name. Thisvalidation is performed to avoid data being overwritten. To skip this validation set validate_headerFalse when creating ainstance of the DataclassReader, see an example belowpythonreader  DataclassReader(f, User, validate_headerFalse)If you run this code you should see an output like thispythonUser(firstnameElsa, emailelsatest.com, age11)User(firstnameAstor, emailastortest.com, age7)User(firstnameEdit, emailedittest.com, age3)User(firstnameElla, emailellatest.com, age2) Error handlingOne of the advantages of using the DataclassReader is that it makes it easy to detect when the type of data in the CSV file is not what your applications model is expecting. And, the DataclassReader shows errors that will help to identify the rows with problem in your CSV file.For example, say we change the contents of the CSV file shown in the Getting started section and, modify the age of the user Astor, lets change it to a string valuetextAstor, astortest.com, testRemember that in the dataclass User the age property is annotated with int. If we run the code again an exception will be raised with the message belowtextdataclass_csv.exceptions.CsvValueError The field age is defined as class int butreceived a value of type class str. [CSV Line number 3]Note that apart from telling what the error was, the DataclassReader will also show which line of the CSV file contain the data with errors. Default valuesThe DataclassReader also handles properties with default values. Lets modify the dataclass User and add a default value for the field emailpythonfrom dataclasses import dataclassdataclassclass User    firstname str    email str  Not specified    age intAnd we modify the CSV file and remove the email for the user AstorpythonAstor,, 7If we run the code we should see the output belowtextUser(firstnameElsa, emailelsatest.com, age11)User(firstnameAstor, emailNot specified, age7)User(firstnameEdit, emailedittest.com, age3)User(firstnameElla, emailellatest.com, age2)Note that now the object for the user Astor have the default value Not specified assigned to the email property.Default values can also be set using dataclasses.field like sopythonfrom dataclasses import dataclass, fielddataclassclass User    firstname str    email str  field(defaultNot specified)    age int Mapping dataclass fields to columnsThe mapping between a dataclass property and a column in the CSV file will be done automatically if the names match, however, there are situations that the name of the header for a column is different. We can easily tell the DataclassReader how the mapping should be done using the method map. Assuming that we have a CSV file with the contents belowtextFirst Name,email,ageElsa,elsatest.com, 11Note that now, the column is called First Name and not firstnameAnd we can use the method map, like sopythonreader  DataclassReader(users_csv, User)reader.map(First name).to(firstname)Now the DataclassReader will know how to extract the data from the column First Name and add it to the to dataclass property firstname Supported type annotationAt the moment the DataclassReader support int, str, float, complex, datetime, and bool. When defining a datetime property, it is necessary to use the dateformat decorator, for examplepythonfrom dataclasses import dataclassfrom datetime import datetimefrom dataclass_csv import DataclassReader, dateformatdataclassdateformat(Ymd)class User    name str    email str    birthday datetimeif __name__  __main__    with open(users.csv) as f        reader  DataclassReader(f, User)        for row in reader            print(row)Assuming that the CSV file have the following contentstextname,email,birthdayEdit,edittest.com,20181123The output would look like thistextUser(nameEdit, emailedittest.com, birthdaydatetime.datetime(2018, 11, 23, 0, 0)) Fields metadataIt is important to note that the dateformat decorator will define the date format that will be used to parse date to all propertiesin the class. Now there are situations where the data in a CSV file contains two or more columns with date values in different formats. It is possibleto set a format specific for every property using the dataclasses.field. Lets say that we now have a CSV file with the following contentstextname,email,birthday, create_dateEdit,edittest.com,20181123,20181123 1043As you can see the create_date contains time information as well.The dataclass User can be defined like thispythonfrom dataclasses import dataclass, fieldfrom datetime import datetimefrom dataclass_csv import DataclassReader, dateformatdataclassdateformat(Ymd)class User    name str    email str    birthday datetime    create_date datetime  field(metadata{dateformat Ymd HM})Note that the format for the birthday field was not speficied using the field metadata. In this case the format specified in the dateformatdecorator will be used. Handling values with empty spacesWhen defining a property of type str in the dataclass, the DataclassReader will treat values with only white spaces as invalid. To change thisbehavior, there is a decorator called accept_whitespaces. When decorating the class with the accept_whitespaces all the properties in the classwill accept values with only white spaces.For examplepythonfrom dataclass_csv import DataclassReader, accept_whitespacesaccept_whitespacesdataclassclass User    name str    email str    birthday datetime    created_at datetimeIf you need a specific field to accept white spaces, you can set the property accept_whitespaces in the fields metadata, like sopythondataclassclass User    name str    email str  field(metadata{accept_whitespaces True})    birthday datetime    created_at datetime User-defined typesYou can use any type for a field as long as its constructor accepts a stringpythonclass SSN    def __init__(self, val)        if re.match(rd{9}, val)            self.val  f{val[03]}-{val[35]}-{val[59]}        elif re.match(rd{3}-d{2}-d{4}, val)            self.val  val        else            raise ValueError(fInvalid SSN {valr})dataclasses.dataclassclass User    name str    ssn SSN Using the DataclassWriterReading a CSV file using the DataclassReader is great and gives us the type-safety of Pythons dataclasses and type annotation, however, there are situations where we would like to use dataclasses for creating CSV files, thats where the DataclassWriter comes in handy.Using the DataclassWriter is quite simple. Given that we have a dataclass Userpythonfrom dataclasses import dataclassdataclassclass User    firstname str    lastname str    age intAnd in your program we have a list of userspythonusers  [    User(firstnameJohn, lastnameSmith, age40),    User(firstnameDaniel, lastnameNilsson, age10),    User(firstnameElla, Fralla, age4)]In order to create a CSV using the DataclassWriter import it from dataclass_csvpythonfrom dataclass_csv import DataclassWriterInitialize it with the required arguments and call the method writepythonwith open(users.csv, w) as f    w  DataclassWriter(f, users, User)    w.write()Thats it Lets break down the snippet above.First, we open a file called user.csv for writing. After that, an instance of the DataclassWriter is created. To create a DataclassWriter we need to pass the file, the list of User instances, and lastly, the type, which in this case is User.The type is required since the writer uses it when trying to figure out the CSV header. By default, it will use the names of theproperties defined in the dataclass, in the case of the dataclass User the title of each columnwill be firstname, lastname and age.See below the CSV created out of a list of Usertextfirstname,lastname,ageJohn,Smith,40Daniel,Nilsson,10Ella,Fralla,4The DataclassWriter also takes a fmtparams which accepts the same parameters as the csv.writer, for moreinformation see httpsdocs.python.org3librarycsv.htmlcsv-fmt-paramsNow, there are situations where we dont want to write the CSV header. In this case, the method write ofthe DataclassWriter accepts an extra argument, called skip_header. The default value is False and when set toTrue it will skip the header. Modifying the CSV headerAs previously mentioned the DataclassWriter uses the names of the properties defined in the dataclass as the CSV header titles, however,depending on your use case it makes sense to change it. The DataclassWriter has a map method just for this purpose. Using the User dataclass with the properties firstname, lastname and age. The snippet below shows how to change firstname to First name and lastname to Last name python with open(users.csv, w) as f    w  DataclassWriter(f, users, User)     Add mappings for firstname and lastname    w.map(firstname).to(First name)    w.map(lastname).to(Last name)    w.write()  The CSV output of the snippet above will betextFirst name,Last name,ageJohn,Smith,40Daniel,Nilsson,10Ella,Fralla,4 Copyright and LicenseCopyright (c) 2018 Daniel Furtado. Code released under BSD 3-clause license CreditsThis package was created with [Cookiecutter](httpsgithub.comaudreyrcookiecutter) and the [audreyrcookiecutter-pypackage](httpsgithub.comaudreyrcookiecutter-pypackage) project template. History 0.1.0 (2018-11-25) First release on PyPI. 0.1.1 (2018-11-25) Documentation fixes. 0.1.2 (2018-11-25) Documentation fixes. 0.1.3 (2018-11-26) Bug fixes Removed the requirement of setting the dataclass init to True 0.1.5 (2018-11-29) Support for parsing datetime values. Better handling when default values are set to None 0.1.6 (2018-12-01) Added support for reader default values from the default property of the dataclasses.field. Added support for allowing string values with only white spaces in a class level using the accept_whitespaces decorator or through the dataclasses.field metadata. Added support for specifying date format using the dataclasses.field metadata. 0.1.7 (2018-12-01) Added support for default values from default_factory in the fields metadata. This allows adding mutable default values to the dataclass properties. 1.0.0 (2018-12-16) When a data does not pass validation it shows the line number in the CSV file where the data contain errors. Improved error handling. Changed the usage of the accept_whitespaces decorator. Updated documentation. 1.0.1 (2019-01-29) Fixed issue when parsing headers on a CSV file with trailing white spaces. 1.1.0 (2019-02-17) Added support for boolean values. Docstrings 1.1.1 (2019-02-17) Documentation fixes. 1.1.2 (2019-02-17) Documentation fixes. 1.1.3 (2020-03-01) Handle properties with init set to False Handle Option type annotation 1.2.0 (2021-03-02) Introduction of a DataclassWriter Added type hinting to external API Documentation updates Bug fixes 1.3.0 (2021-04-10) Included stub files check if the CSV file has duplicated header values Fixed issues 22 and 33 code cleanup 1.4.0 (2021-12-13) Bug fixes Support for date types</longdescription>
</pkgmetadata>