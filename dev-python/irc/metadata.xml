<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpsimg.shields.iopypivirc.svg   target PyPI link_.. image httpsimg.shields.iopypipyversionsirc.svg   target PyPI link_.. _PyPI link httpspypi.orgprojectirc.. image httpsgithub.comjaracoircworkflowstestsbadge.svg   target httpsgithub.comjaracoircactionsqueryworkflow3A22tests22   alt tests.. image httpsimg.shields.iobadgecode20style-black-000000.svg   target httpsgithub.compsfblack   alt Code style Black.. image httpsreadthedocs.orgprojectspython-ircbadgeversionlatest   target httpspython-irc.readthedocs.ioenlatestbadgelatest.. image httpsimg.shields.iobadgeskeleton-2022-informational   target httpsblog.jaraco.comskeleton.. image httpsbadges.gitter.imjaracoirc.svg   alt Join the chat at httpsgitter.imjaracoirc   target httpsgitter.imjaracoircutm_sourcebadgeutm_mediumbadgeutm_campaignpr-badgeutm_contentbadge.. image httpstidelift.combadgesgithubjaracoirc   target httpstidelift.comsubscriptionpkgpypi-ircutm_sourcepypi-ircutm_mediumreferralutm_campaignreadmeFull-featured Python IRC library for Python.- Project home httpsgithub.comjaracoirc_- Docs httpspython-irc.readthedocs.io_- History httpspython-irc.readthedocs.ioenlatesthistory.html_OverviewThis library provides a low-level implementation of the IRC protocol forPython.  It provides an event-driven IRC client framework.  It hasa fairly thorough support for the basic IRC protocol, CTCP, and DCCconnections.In order to understand how to make an IRC client, its best to read up firston the IRC specificationshttpweb.archive.orgweb20160628193730httpwww.irchelp.orgirchelprfc_.Client FeaturesThe main features of the IRC client framework are Abstraction of the IRC protocol. Handles multiple simultaneous IRC server connections. Handles server PONGing transparently. Messages to the IRC server are done by calling methods on an IRC  connection object. Messages from an IRC server triggers events, which can be caught  by event handlers. Multiple options for reading from and writing to an IRC server  you can use sockets in an internal select() loop OR use  Python3s asyncio event loop Functions can be registered to execute at specified times by the  event-loop. Decodes CTCP tagging correctly (hopefully) I havent seen any  other IRC client implementation that handles the CTCP  specification subtleties. A kind of simple, single-server, object-oriented IRC client class  that dispatches events to instance methods is included. DCC connection support.Current limitations The IRC protocol shines through the abstraction a bit too much. Data is not written asynchronously to the server (and DCC peers),  i.e. the write() may block if the TCP buffers are stuffed. Like most projects, documentation is lacking ... DCC is not currently implemented in the asyncio-based versionUnfortunately, this library isnt as well-documented as I would likeit to be.  I think the best way to get started is to read andunderstand the example program irccat, which is included in thedistribution.The following modules might be of interest irc.client  The library itself.  Read the code along with comments and  docstrings to get a grip of what it does.  Use it at your own risk  and read the source, Luke irc.client_aio  All the functionality of the above library, but utilizing  Python 3s native asyncio library for the core event loop.  InterfaceAPI is otherwise functionally identical to the classes  in irc.client irc.bot  An IRC bot implementation. irc.server  A basic IRC server implementation. Suitable for testing, but not  intended as a production service.  Invoke the server with python -m irc.server.ExamplesExample scripts in the scripts directory irccat  A simple example of how to use the IRC client.  irccat reads  text from stdin and writes it to a specified user or channel on  an IRC server. irccat2  The same as above, but using the SimpleIRCClient class. aio_irccat  Same as above, but uses the asyncio-based event loop in  AioReactor instead of the select() based Reactor. aio_irccat2  Same as above, but using the AioSimpleIRCClient class servermap  Another simple example.  servermap connects to an IRC server,  finds out what other IRC servers there are in the net and prints  a tree-like map of their interconnections. testbot  An example bot that uses the SingleServerIRCBot class from  irc.bot.  The bot enters a channel and listens for commands in  private messages or channel traffic.  It also accepts DCC  invitations and echos back sent DCC chat messages. dccreceive  Receives a file over DCC. dccsend  Sends a file over DCC.NOTE If youre running one of the examples on a unix command line, you needto escape the  symbol in the channel. For example, use test ortest instead of test.Scheduling EventsThe library includes a default event Scheduler asirc.schedule.DefaultScheduler,but this scheduler can be replaced with any other scheduler. For example,to use the schedule httpspypi.orgprojectschedule_ package,include itin your dependencies and install it into the IRC library as so    class ScheduleScheduler(irc.schedule.IScheduler)        def execute_every(self, period, func)            schedule.every(period).do(func)        def execute_at(self, when, func)            schedule.at(when).do(func)        def execute_after(self, delay, func)            raise NotImplementedError(Not supported)        def run_pending(self)            schedule.run_pending()    irc.client.Reactor.scheduler_class  ScheduleSchedulerDecoding InputBy default, the IRC library attempts to decode all incoming streams asUTF-8, even though the IRC spec stipulates that no specific encoding can beexpected. Since assuming UTF-8 is not reasonable in the general case, the IRClibrary provides options to customize decoding of input by customizing theServerConnection class. The buffer_class attribute on theServerConnection determines which class is used for buffering lines from theinput stream, using the buffer module in jaraco.streamhttpspypi.python.orgpypijaraco.stream_. By default it isbuffer.DecodingLineBuffer, but may bere-assigned with another class, following the interface of buffer.LineBuffer.The buffer_class attribute may be assigned for all instances ofServerConnection by overriding the class attribute.For example.. code python    from jaraco.stream import buffer    irc.client.ServerConnection.buffer_class  buffer.LenientDecodingLineBufferThe LenientDecodingLineBuffer attempts UTF-8 but falls back to latin-1, whichwill avoid UnicodeDecodeError in all cases (but may produce unexpectedbehavior if an IRC user is using another encoding).The buffer may be overridden on a per-instance basis (as long as itsoverridden before the connection is established).. code python    server  irc.client.Reactor().server()    server.buffer_class  buffer.LenientDecodingLineBuffer    server.connect()Alternatively, some clients may still want to decode the input using adifferent encoding. To decode all input as latin-1 (which decodes any input),use the following.. code python    irc.client.ServerConnection.buffer_class.encoding  latin-1Or decode to UTF-8, but use a replacement character for unrecognized bytesequences.. code python    irc.client.ServerConnection.buffer_class.errors  replaceOr, to simply ignore all input that cannot be decoded.. code python    class IgnoreErrorsBuffer(buffer.DecodingLineBuffer)        def handle_exception(self)            pass    irc.client.ServerConnection.buffer_class  IgnoreErrorsBufferThe library requires text for messageprocessing, so a decoding buffer must be used. Clientsmust use one of the above techniques for decoding input to text.Notes and Contact InfoEnjoy.MaintainerJason R. Coombs jaracojaraco.comOriginal AuthorJoel Rosdahl joelrosdahl.netCopyright  1999-2002 Joel RosdahlCopyright  2011-2016 Jason R. CoombsCopyright  2009 Ferry BoenderFor EnterpriseAvailable as part of the Tidelift Subscription.This project and the maintainers of thousands of other packages are working with Tidelift to deliver one enterprise subscription that covers all of the open source you use.Learn more httpstidelift.comsubscriptionpkgpypi-PROJECTutm_sourcepypi-PROJECTutm_mediumreferralutm_campaigngithub_.Security ContactTo report a security vulnerability, please use theTidelift security contact httpstidelift.comsecurity_.Tidelift will coordinate the fix and disclosure.</longdescription>
</pkgmetadata>