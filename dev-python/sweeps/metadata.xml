<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>p aligncenter  img src.githubwb-logo-lightbg.pnggh-light-mode-only width600 altWeights  Biases  img src.githubwb-logo-darkbg.pnggh-dark-mode-only width600 altWeights  Biasesp __Sweeps__ Hyperparameter Optimization EngineThis repo contains the routines that generate hyperparameter sweep suggestions inthe WB backend and [client](httpsgithub.comwandbclient) local controller.Issues are not enabled in this repository.__Please [open issues related to sweeps in the wandb client library github issues page](httpsgithub.comwandbclientissuesnewchoose).__ InstallationTo installpip install sweeps Examples__Get next run in a sweep.__Requires two arguments, config, the config that defines the sweep, and runs, the other runs in the sweepconfigpython{    metric {name loss, goal minimize},    method bayes,    parameters {        v1 {min 1, max 10},        v2 {min 1.0, max 10.0},    },}runspython[    SweepRun(        nameb,        stateRunState.finished,        history[            {loss 5.0},        ],        config{v1 {value 7}, v2 {value 6}},        summary_metrics{zloss 1.2},    ),    SweepRun(        nameb2,        stateRunState.finished,        config{v1 {value 1}, v2 {value 8}},        summary_metrics{loss 52.0},        history[],    )]Codepathpythonsuggestion  next_run(config, runs)next_run validates that sweep config conforms to the jsonschema in configschema.json, if not, it raises a ValidationError parses the config file and determines the method that it should use to find the next run (in this case bayes_search_next_run) calls bayes_search_next_run(config, runs) and returns the suggested SweepRun__Return list of runs to stop in a sweep.__Requires two arguments, config, the config that defines the sweep, and runs, the other runs in the sweepconfigpython{    method grid,    metric {name loss, goal minimize},    early_terminate {        type hyperband,        max_iter 5,        eta 2,        s 2,    },    parameters {a {values [1, 2, 3]}},}runspython[    SweepRun(        namea,        stateRunState.finished,   This is already stopped        history[            {loss 10},            {loss 9},        ],    ),    SweepRun(        nameb,        stateRunState.running,   This should be stopped        history[            {loss 10},            {loss 10},        ],    ),    SweepRun(        namec,        stateRunState.running,   This passes band 1 but not band 2        history[            {loss 10},            {loss 8},            {loss 8},        ],    ),    SweepRun(        named,        stateRunState.running,        history[            {loss 10},            {loss 7},            {loss 7},        ],    ),    SweepRun(        namee,        stateRunState.finished,        history[            {loss 10},            {loss 6},            {loss 6},        ],    ),]Codepathpythonto_stop  stop_runs(config, runs)stop_runs validates that sweep config conforms to the jsonschema in configschema.json, if not, it raises a ValidationError parses the config file and determines the method that it should use to early terminate runs (in this case hyperband_stop_runs) calls hyperband_stop_runs(config, runs) and returns the SweepRuns to stop TestingTests are run using tox, the makefile defines convenience commands for short and long testsmake test-shortmake test-full Contributing__Review the [Contributing Instructions](httpsgithub.comwandbclientblobmasterCONTRIBUTING.md)__Install the development requirementspip install -r requirements.dev.txtInstall the pre-commit hookspre-commit install .Run formatting and testsmake formatmake test</longdescription>
</pkgmetadata>