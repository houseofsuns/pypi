<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>junitparser -- Pythonic JUnitxUnit Result XML Parser.. image httpsgithub.comweiweijunitparserworkflowsbuildbadge.svgbranchmaster   target httpsgithub.comweiweijunitparseractions.. image httpscodecov.ioghweiweijunitparserbranchmastergraphbadge.svgtokenUotlfRXNnK   target httpscodecov.ioghweiweijunitparserjunitparser handles JUnitxUnit Result XML files. Use it to parse and manipulateexisting Result XML files, or create new JUnitxUnit result XMLs from scratch.Features-------- Parse or modify existing JUnitxUnit xml files. Parse or modify non-standard or customized JUnitxUnit xml files, by monkey  patching existing element definitions. Create JUnitxUnit test results from scratch. Merge test result xml files. Specify xml parser. For example you can use lxml to speed things up. Invoke from command line, or python -m junitparser Python 2 and 3 support (As of Nov 2020, 14 of the users are still on Python   2, so there is no plan to drop Python 2 support)Note on version 2-----------------Version 2 improved support for pytest result xml files by fixing a few issues, notably that there could be multiple Failure or Error entries. There is a breaking change that TestCase.result is now a list instead of a single item.If you are using this attribute, please update your code accordingly.Installation-------------    pip install junitparserUsage-----You should be relatively familiar with the Junit XML format. If not, runpydoc on the exposed classes and functions to see how its structured.Create Junit XML format reports from scratchYou have some test result data, and you want to convert them into junit.xmlformat... code-block python    from junitparser import TestCase, TestSuite, JUnitXml, Skipped, Error     Create cases    case1  TestCase(case1, class.name, 0.5)  params are optional    case1.classname  modified.class.name  specify or change case attrs    case1.result  [Skipped()]  You can have a list of results    case2  TestCase(case2)    case2.result  [Error(Example error message, the_error_type)]     Create suite and add cases    suite  TestSuite(suite1)    suite.add_property(build, 55)    suite.add_testcase(case1)    suite.add_testcase(case2)    suite.remove_testcase(case2)    Bulk add cases to suite    case3  TestCase(case3)    case4  TestCase(case4)    suite.add_testcases([case3, case4])     Add suite to JunitXml    xml  JUnitXml()    xml.add_testsuite(suite)    xml.write(junit.xml)Read and manipulate existing JUnitxUnit XML filesYou have some existing junit.xml files, and you want to modify the content... code-block python    from junitparser import JUnitXml    xml  JUnitXml.fromfile(pathtojunit.xml)    for suite in xml         handle suites        for case in suite             handle cases    xml.write()  Writes back to fileIt is also possible to use a custom parser. For example lxml provides a plethoraof parsing options_. We can use them this way.. code-block python    from lxml.etree import XMLParser, parse    from junitparser import JUnitXml    def parse_func(file_path)        xml_parser  XMLParser(huge_treeTrue)        return parse(file_path, xml_parser)    xml  JUnitXml.fromfile(pathtojunit.xml, parse_func)     process xml..... _options httpslxml.deapilxml.etree.XMLParser-class.htmlMerge XML filesYou have two or more XML files, and you want to merge them into one... code-block python    from junitparser import JUnitXml    xml1  JUnitXml.fromfile(pathtojunit1.xml)    xml2  JUnitXml.fromfile(pathtojunit2.xml)    newxml  xml1 + xml2     Alternatively, merge in place    xml1 + xml2Note that it wont check for duplicate entries. You need to deal with them onyour own.Create XML with custom attributesYou want to use an attribute that is not supported by default... code-block python    from junitparser import TestCase, Attr, IntAttr, FloatAttr     Add the custom attribute    TestCase.id  IntAttr(id)    TestCase.rate  FloatAttr(rate)    TestCase.custom  Attr(custom)    case  TestCase()    case.id  123    case.rate  0.95    case.custom  foobarHandling XML with custom elementThere may be once in 1000 years you want to it this way, but anyways.Suppose you want to add element CustomElement to TestCase... code-block python    from junitparser import Element, Attr, TestSuite     Create the new element by subclassing Element,     and add custom attributes to it.    class CustomElement(Element)        _tag  custom        foo  Attr()        bar  Attr()    testcase  TestCase()    custom  CustomElement()    testcase.append(custom)     To find a single sub-element    testcase.child(CustomElement)     To iterate over custom elements    for custom in testcase.iterchildren(CustomElement)        ...  Do things with custom elementHandling custom XML attributesSay you have some data stored in the XML as custom attributes and you want toread them out.. code-block python    from junitparser import TestCase, Attr, JUnitXml     Create the new element by subclassing Element or one of its child class,     and add custom attributes to it.    class MyTestCase(TestCase)        foo  Attr()    xml  JUnitXml.fromfile(pathtojunit.xml)    for suite in xml         handle suites        for case in suite            my_case  MyTestCase.fromelem(case)            print(my_case.foo)Command Line------------.. code-block shell     junitparser --help    usage junitparser [-h] [-v] {merge} ...    Junitparser CLI helper.    positional arguments    {merge}        command      merge        Merge Junit XML format reports with junitparser.      verify       Return a non-zero exit code if one of the testcases failed or errored.    optional arguments    -h, --help     show this help message and exit    -v, --version  show programs version number and exit.. code-block shell     junitparser merge --help    usage junitparser merge [-h] [--glob] paths [paths ...] output    positional arguments      paths       Original XML path(s).      output      Merged XML Path, setting to - will output console    optional arguments      -h, --help  show this help message and exit      --glob      Treat original XML path(s) as glob(s).      --suite-name SUITE_NAME                  Name added to testsuites... code-block shell     junitparser verify --help    usage junitparser verify [-h] [--glob] paths [paths ...]    positional arguments      paths       XML path(s) of reports to verify.    optional arguments      -h, --help  show this help message and exit      --glob      Treat original XML path(s) as glob(s).Test----The tests are written with python unittest, to run them, use pytest    pytest test.pyContribute----------PRs are welcome</longdescription>
</pkgmetadata>