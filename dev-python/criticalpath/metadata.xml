<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>CriticalPath============[![](https://img.shields.io/pypi/v/criticalpath.svg)](https://pypi.python.org/pypi/criticalpath) [![Build Status](https://img.shields.io/travis/chrisspen/criticalpath.svg?branch=master)](https://travis-ci.org/chrisspen/criticalpath)Calculates the[critical path](http://en.wikipedia.org/wiki/Critical_path_method)through a network of tasks.Assumes the given graph is acyclic (has no loops).A task network is composed of nodes, but it's also organized withina parent node. This allows my node/task model to support recursive nestingof tasks. e.g. An overall &quot;project&quot; node containing multiple tasks can betreated as a single task in itself, and therefore included in more abstracttask groupings.Installation------------    pip install criticalpathUsage-----    &gt;&gt;&gt; from criticalpath import Node    &gt;&gt;&gt; p = Node('project')    &gt;&gt;&gt; a = p.add(Node('A', duration=3))    &gt;&gt;&gt; b = p.add(Node('B', duration=3, lag=0))    &gt;&gt;&gt; c = p.add(Node('C', duration=4, lag=0))    &gt;&gt;&gt; d = p.add(Node('D', duration=6, lag=0))    &gt;&gt;&gt; e = p.add(Node('E', duration=5, lag=0))    &gt;&gt;&gt; p.link(a, b).link(a, c).link(a, d).link(b, e).link(c, e).link(d, e)    project    &gt;&gt;&gt; p.update_all()    &gt;&gt;&gt; p.get_critical_path()    [A, D, E]    &gt;&gt;&gt; p.duration    14Development-----------To run unittests across multiple Python versions, install:    sudo add-apt-repository ppa:deadsnakes/ppa    sudo apt-get update    sudo apt-get install python-dev python3-dev python3.3-minimal python3.3-dev python3.4-minimal python3.4-dev python3.5-minimal python3.5-dev python3.6 python3.6-devTo run all [tests](http://tox.readthedocs.org/en/latest/):    export TESTNAME=; toxTo run tests for a specific environment (e.g. Python 2.7):        export TESTNAME=; tox -e py27</longdescription>
</pkgmetadata>