<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>|Build Status|numerizer=========A Python module to convert natural language numerics into ints and floats.This is a port of the Ruby gem `numerizer&lt;https://github.com/jduff/numerizer.git&gt;`_Installation------------The numerizer library can be installed from PyPI as follows:.. code:: bash    $ pip install numerizeror from source as follows:.. code:: bash    $ git clone https://github.com/jaidevd/numerizer.git    $ cd numerizer    $ pip install -e .Usage-----.. code:: python    &gt;&gt;&gt; from numerizer import numerize    &gt;&gt;&gt; numerize('forty two')    '42'    &gt;&gt;&gt; numerize('forty-two')    '42'    &gt;&gt;&gt; numerize('four hundred and sixty two')    '462'    &gt;&gt;&gt; numerize('one fifty')    '150'    &gt;&gt;&gt; numerize('twelve hundred')    '1200'    &gt;&gt;&gt; numerize('twenty one thousand four hundred and seventy three')    '21473'    &gt;&gt;&gt; numerize('one million two hundred and fifty thousand and seven')    '1250007'    &gt;&gt;&gt; numerize('one billion and one')    '1000000001'    &gt;&gt;&gt; numerize('nine and three quarters')    '9.75'    &gt;&gt;&gt; numerize('platform nine and three quarters')    'platform 9.75'Using the SpaCy extension^^^^^^^^^^^^^^^^^^^^^^^^^Since version 0.2, numerizer is available as a `SpaCy extension &lt;https://spacy.io/usage/processing-pipelines#custom-components-attributes&gt;`_.Any named entities of a quantitative nature within a SpaCy document can be numerized as follows:.. code:: python    &gt;&gt;&gt; from spacy import load    &gt;&gt;&gt; nlp = load('en_core_web_sm')  # or load any other spaCy model    &gt;&gt;&gt; doc = nlp('The projected revenue for the next quarter is over two million dollars.')    &gt;&gt;&gt; doc._.numerize()    {the next quarter: 'the next 1/4', over two million dollars: 'over 2000000 dollars'}Users can specify which entity types are to be numerized, by using the `labels` argument in the extension function, as follows:.. code:: python    &gt;&gt;&gt; doc._.numerize(labels=['MONEY'])  # only numerize entities of type 'MONEY'    {over two million dollars: 'over 2000000 dollars'}The extension is available for tokens and spans as well... code:: python    &gt;&gt;&gt; two_million = doc[-4:-2]  # span corresponding to &quot;two million&quot;    &gt;&gt;&gt; two_million._.numerize()    '2000000'    &gt;&gt;&gt; quarter = doc[6]  # token corresponding to &quot;quarter&quot;    &gt;&gt;&gt; quarter._.numerized    '1/4'Extras------For R users, a wrapper library has been developed by `@amrrs &lt;https://github.com/amrrs&gt;`_. Try it out `here &lt;https://github.com/amrrs/numerizer.git&gt;`_... |Build Status| image:: https://app.travis-ci.com/jaidevd/numerizer.svg?branch=master   :target: https://app.travis-ci.com/jaidevd/numerizer</longdescription>
</pkgmetadata>