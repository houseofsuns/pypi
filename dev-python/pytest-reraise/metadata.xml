<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> pytest-reraise[[PyPI](httpsimg.shields.iopypivpytest-reraise)](httpspypi.python.orgpypipytest-reraise)[[GitHub Workflow Status](httpsimg.shields.iogithubworkflowstatusbjolucpytest-reraisebuild)](httpsgithub.combjolucpytest-reraiseactions)[[codecov](httpscodecov.ioghbjolucpytest-reraisebranchmaingraphbadge.svg)](httpscodecov.ioghbjolucpytest-reraise)[[PyPI pyversions](httpsimg.shields.iopypipyversionspytest-reraise)](httpspypi.python.orgpypipytest-reraise)[PyPI - Downloads](httpsimg.shields.iopypidmpytest-reraise)[[Code style black](httpsimg.shields.iobadgecode20style-black-000000)](httpsgithub.compsfblack)[[semantic-release](httpsimg.shields.iobadge2020F09F93A6F09F9A80-semantic--release-e10079)](httpsgithub.combjolucsemantic-release-config-poetry)Lets assume you write a pytest test case that includes assertions in another thread, like sopythonfrom threading import Threaddef test_assert()    def run()        assert False    Thread(targetrun).start()This test will pass, as the AssertionError is not raised in the main thread.pytest-reraise is here to help you capture the exception and raise it in the main threadshpip install pytest-reraisepythonfrom threading import Threaddef test_assert(reraise)    def run()        with reraise            assert False    Thread(targetrun).start()The above test will fail, as pytest-reraise captures the exception and raises it at the end of the test case. Advanced Usage and Special Cases Wrapping FunctionsInstead of using the reraise context manager in a function, you can also wrap the entire function with it via the reraise.wrap() method.Hence, the examplepythondef run()    with reraise        assert FalseThread(targetrun).start()can also be written aspythondef run()    assert FalseThread(targetreraise.wrap(run)).start()or evenpythonreraise.wrapdef run()    assert FalseThread(targetrun).start() Manual Re-raisingBy default, the captured exception (if any) is raised at the end of the test case.If you want to raise it before then, call reraise() in your test case.If an exception has been raised within a with reraise block by then, reraise() will raise it right awaypythondef test_assert(reraise)    def run()        with reraise            assert False    reraise()  This will not raise anything yet    t  Thread(targetrun)    t.start()    t.join()    reraise()  This will raise the assertion errorAs seen in the example above, reraise() can be called multiple times during a test case. Whenever an exception has been raised in a with reraise block since the last call, it will be raised on the next call. Multiple ExceptionsWhen the reraise context manager is used multiple times in a single test case, only the first-raised exception will be re-raised in the end.In the below example, both threads raise an exception but only one of these exceptions will be re-raised.pythondef test_assert(reraise)    def run()        with reraise            assert False    for _ in range(2)        Thread(targetrun).start() Catching ExceptionsBy default, the reraise context manager does not catch exceptions, so they will not be hidden from the thread in which they are raised.If you want to change this, use reraise(catchTrue) instead of reraisepythondef test_assert(reraise)    def run()        with reraise(catchTrue)            assert False        print(Im alive)    Thread(targetrun).start()Note that you cannot use reraise() (without the catch argument) as a context manager, as it is used to raise exceptions. Exception PriorityIf reraise captures an exception and the main thread raises an exception as well, the exception captured by reraise will mask the main threads exception unless that exception was already re-raised.The objective behind this is that the outcome of the main thread often depends on the work performed in other threads.Thus, failures in in other threads are likely to cause failures in the main thread, and other threads exceptions (if any) are of greater importance for the developer than main thread exceptions.The example below will report assert False, not assert foo  bar.pythondef test_assert(reraise)    def run()        with reraise            assert False  This will be reported    t  Thread(targetrun)    t.start()    t.join()    assert foo  bar  This wont Accessing and Modifying Exceptionsreraise provides an exception property to retrieve the exception that was captured, if any.reraise.exception can also be used to assign an exception if no exception has been captured yet.In addition to that, reraise.reset() returns the value of reraise.exception and resets it to None so that the exception will not be raised anymore.Heres a quick demonstration test case that passespythondef test_assert(reraise)    def run()        with reraise            assert False    t  Thread(targetrun)    t.start()    t.join()     Return the captured exception    assert type(reraise.exception) is AssertionError     This wont do anything, since an exception has already been captured    reraise.exception  Exception()     Return the exception and set reraise.exception to None    assert type(reraise.reset()) is AssertionError     Reraise will not fail the test case because    assert reraise.exception is None</longdescription>
</pkgmetadata>