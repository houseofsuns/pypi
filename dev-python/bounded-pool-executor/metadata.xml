<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Bounded ProcessThread Pool ExecutorBoundedSemaphore for [ProcessPoolExecutor](httpsdocs.python.org3libraryconcurrent.futures.htmlprocesspoolexecutor)  [ThreadPoolExecutor](httpsdocs.python.org3libraryconcurrent.futures.htmlthreadpoolexecutor) from [concurrent.futures](httpsdocs.python.org3libraryconcurrent.futures.html) Installationbashpip install bounded-pool-executor What is the main problemIf you use the standard module concurrent.futures and want to simultaneously process several million data, then a queue of workers will take up all the free memory.If the script is run on a weak VPS, this will lead to a memory leak. BoundedProcessPoolExecutor VS ProcessPoolExecutor BoundedProcessPoolExecutorBoundedProcessPoolExecutor will put a new worker in queue only when another worker has finished his work.pythonfrom bounded_pool_executor import BoundedProcessPoolExecutorfrom time import sleepfrom random import randintdef do_job(num)    sleep_sec  randint(1, 10)    print(value d, sleep d sec.  (num, sleep_sec))    sleep(sleep_sec)with BoundedProcessPoolExecutor(max_workers5) as worker    for num in range(10000)        print(d Worker initialization  num)        worker.submit(do_job, num) Result[BoundedProcessPoolExecutor](httpspython-scripts.comwp-contentuploads201812bounded.gif) Classic concurrent.futures.ProcessPoolExecutorProcessPoolExecutor inserts all workers into the queue and expects tasks to be performed as the new worker is released, depending on the value of max_workers.pythonimport concurrent.futuresfrom time import sleepfrom random import randintdef do_job(num)    sleep_sec  randint(1, 3)    print(value d, sleep d sec.  (num, sleep_sec))    sleep(sleep_sec)with concurrent.futures.ProcessPoolExecutor(max_workers5) as worker    for num in range(100000)        print(d Worker initialization  num)        worker.submit(do_job, num) Result[concurrent.futures.ProcessPoolExecutor](httpspython-scripts.comwp-contentuploads201812future-ProcessPoolExecutor.gif)</longdescription>
</pkgmetadata>