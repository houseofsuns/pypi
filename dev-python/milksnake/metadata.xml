<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Milksnakea hrefhttpspypi.python.orgpypimilksnakeimg srchttpsimg.shields.iopypivmilksnake.svg altaa hrefhttpstravis-ci.orggetsentrymilksnakeimg srchttpstravis-ci.orggetsentrymilksnake.svgbranchmaster altaa hrefhttpsgithub.comgetsentrymilksnakeblobmasterLICENSEimg srchttpsimg.shields.iopypilmilksnake.svg altaMilksnake is an extension for setuptools that allows you to distributedynamic linked libraries in Python wheels in the most portable way imaginable.It gives you a hook to invoke your own build process and to then take theresulting dynamic linked library. WhyThere are already other projects that make Python and native libraries playalong but this one is different.  Unlike other projects that build Pythonextension modules the goal of this project is to build regular native librariesthat are then loaded with CFFI at runtime.  Why not just use CFFI  BecauseCFFIs setuptools support alone does not properly work with such wheels (itdoes not provide a way to build and properly tag wheels for shared libraries) andit does not provide a good way to invoke an external build process (like amakefile, cargo to build rust binaries etc.)In particular you will most likely only need two wheels for Linux, one for macsand soon one for Windows independently of how many Python interpreters you wantto target. What is supported Platforms Linux, Mac, Windows setuptools commands bdist_wheel, build, build_ext, develop pip install --editable . Universal wheels (PACKAGE-py2.py3-none-PLATFORM.whl) this can be disabled  with milksnake_universalFalse in setup() in case the package also contains  stuff that does link against libpython. HowThis example shows how to build a rust project with itThis is what a setup.py file looks likepythonfrom setuptools import setupdef build_native(spec)     build an example rust library    build  spec.add_external_build(        cmd[cargo, build, --release],        path.rust    )    spec.add_cffi_module(        module_pathexample._native,        dyliblambda build.find_dylib(example, in_pathtargetrelease),        header_filenamelambda build.find_header(example.h, in_pathtarget),        rtld_flags[NOW, NODELETE]    )setup(    nameexample,    version0.0.1,    packages[example],    zip_safeFalse,    platformsany,    setup_requires[milksnake],    install_requires[milksnake],    milksnake_tasks[        build_native    ])You then need a rust folder that has a Rust library (with a crate typeof cdylib) and a example python package.Example example__init__.py filepythonfrom example._native import ffi, libdef test()    return lib.a_function_from_rust()And a rustsrclib.rsrust[no_mangle]pub unsafe extern C fn a_function_from_rust() - i32 {    42}And the rustCargo.tomltoml[package]name  exampleversion  0.1.0build  build.rs[lib]name  examplecrate-type  [cdylib][build-dependencies]cbindgen  0.4And finally the build.rs filerustextern crate cbindgenuse stdenvfn main() {    let crate_dir  envvar(CARGO_MANIFEST_DIR).unwrap()    let mut config cbindgenConfig  Defaultdefault()    config.language  cbindgenLanguageC    cbindgengenerate_with_config(crate_dir, config)      .unwrap()      .write_to_file(targetexample.h)}</longdescription>
</pkgmetadata>