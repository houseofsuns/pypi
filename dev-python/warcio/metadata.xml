<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>WARCIO WARC (and ARC) Streaming Library.. image httpstravis-ci.orgwebrecorderwarcio.svgbranchmaster      target httpstravis-ci.orgwebrecorderwarcio.. image httpscodecov.ioghwebrecorderwarciobranchmastergraphbadge.svg      target httpscodecov.ioghwebrecorderwarcioBackground----------This library provides a fast, standalone way to read and write WARCFormat httpsen.wikipedia.orgwikiWeb_ARChive__ commonly used inweb archives. Supports Python 2.7+ and Python 3.4+ (usingsix httpspythonhosted.orgsix__, the only external dependency)warcio supports reading and writing of WARC files compliant with both the WARC 1.0 httpbibnum.bnf.frWARCWARC_ISO_28500_version1_latestdraft.pdf__and WARC 1.1 httpbibnum.bnf.frWARCWARC_ISO_28500_version1-1_latestdraft.pdf__ ISO standards.Install with pip install warcioThis library is a spin-off of the WARC reading and writing component ofthe pywb httpsgithub.comwebrecorderpywb__ high-fidelity replaylibrary, a key component ofWebrecorder httpsgithub.comwebrecorderwebrecorder__The library is designed for fast, low-level access to web archivalcontent, oriented around a stream of WARC records rather than files.Reading WARC Records--------------------A key feature of the library is to be able to iterate over a stream ofWARC records using the ArchiveIterator.It includes the following features- Reading a WARC 1.0, WARC 1.1 or ARC stream- On the fly ARC to WARC record conversion- Decompressing and de-chunking HTTP payload content stored in WARCARC files.For example, the following prints the the url for each WARC responserecord.. code python    from warcio.archiveiterator import ArchiveIterator    with open(pathtofile, rb) as stream        for record in ArchiveIterator(stream)            if record.rec_type  response                print(record.rec_headers.get_header(WARC-Target-URI))The stream object could be a file on disk or a remote network stream.The ArchiveIterator reads the WARC content in a single pass. Therecord is represented by an ArcWarcRecord object which containsthe format (ARC or WARC), record type, the record headers, http headers(if any), and raw stream for reading the payload... code python    class ArcWarcRecord(object)        def __init__(self, args)            (self.format, self.rec_type, self.rec_headers, self.raw_stream,             self.http_headers, self.content_type, self.length)  argsReading WARC ContentThe raw_stream can be used to read the rest of the payload directly.A special ArcWarcRecord.content_stream() function provides a stream thatautomatically decompresses and de-chunks the HTTP payload, if it iscompressed andor transfer-encoding chunked.ARC FilesThe library provides support for reading (but not writing ARC) files.The ARC format is legacy but is important to support in a consistentmatter. The ArchiveIterator can equally iterate over ARC and WARCfiles to emit ArcWarcRecord objects. The special arc2warc optionconverts ARC records to WARCs on the fly, allowing for them to beaccessed using the same API.(Special WARCIterator and ARCIterator subclasses of ArchiveIteratorare also available to read only WARC or only ARC files).WARC and ARC StreamingFor example, here is a snippet for reading an ARC and a WARC using thesame API.The example streams a WARC and ARC file over HTTP usingrequests httpdocs.python-requests.orgenmaster__, printing thewarcinfo record (or ARC header) and any response records (or all ARCrecords) that contain HTML.. code python    import requests    from warcio.archiveiterator import ArchiveIterator    def print_records(url)        resp  requests.get(url, streamTrue)        for record in ArchiveIterator(resp.raw, arc2warcTrue)            if record.rec_type  warcinfo                print(record.raw_stream.read())            elif record.rec_type  response                if record.http_headers.get_header(Content-Type)  texthtml                    print(record.rec_headers.get_header(WARC-Target-URI))                    print(record.content_stream().read())                    print()     WARC    print_records(httpsarchive.orgdownloadExampleArcAndWarcFilesIAH-20080430204825-00000-blackbook.warc.gz)     ARC with arc2warc    print_records(httpsarchive.orgdownloadExampleArcAndWarcFilesIAH-20080430204825-00000-blackbook.arc.gz)Writing WARC Records--------------------Starting with 1.6, warcio introduces a way to capture HTTPS traffic directlyto a WARC file, by monkey-patching Pythons http.client library.This approach works well with the popular requests library often used to fetchHTTPS content. Note that requests must be imported after the capture_http module.Quick Start to Writing a WARCFetching the url httpsexample.com while capturing the response and requestinto a gzip compressed WARC file named example.warc.gz can be done with the following four lines.. code python    from warcio.capture_http import capture_http    import requests   requests must be imported after capture_http    with capture_http(example.warc.gz)        requests.get(httpsexample.com)The WARC example.warc.gz will contain two records (the response is written first, then the request).To write to a default in-memory buffer (BufferWARCWriter), dont specify a filename, using with capture_http() as writer.Additional requests in the capture_http context and will be appended to the WARC as expected.The WARC-IP-Address header will also be added for each record if the IP address is available.The following example (similar to a unit test from the test suite testtest_capture_http.py__) demonstrates the resulting records created with capture_http.. code python    with capture_http() as writer        requests.get(httpexample.com)        requests.get(httpsgoogle.com)    expected  [(httpexample.com, response, True),                (httpexample.com, request, True),                (httpsgoogle.com, response, True),                (httpsgoogle.com, request, True),                (httpswww.google.com, response, True),                (httpswww.google.com, request, True)               ]     actual  [                (record.rec_headers[WARC-Target-URI],                 record.rec_type,                 WARC-IP-Address in record.rec_headers)                for record in ArchiveIterator(writer.get_stream())              ]     assert actual  expectedCustomizing WARC WritingThe library provides a simple and extensible interface for writingstandards-compliant WARC files.The library comes with a basic WARCWriter class for writing to asingle WARC file and BufferWARCWriter for writing to an in-memorybuffer. The BaseWARCWriter can be extended to support more complexoperations.(There is no support for writing legacy ARC files)For more flexibility, such as to use a custom WARCWriter class,the above example can be written as.. code python    from warcio.capture_http import capture_http    from warcio import WARCWriter    import requests   requests must be imported after capture_http    with open(example.warc.gz, wb) as fh        warc_writer  WARCWriter(fh)        with capture_http(warc_writer)            requests.get(httpsexample.com)WARC1.1 SupportBy default, warcio creates WARC 1.0 records for maximum compatibility with existing tools.To create WARC1.1 records, simply specify the warc version as follows.. code python    with capture_http(example.warc.gz, warc_version1.1)        ..... code python    WARCWriter(fh, warc_version1.1)    ...When using WARC 1.1, the main difference is that the WARC-Date timestamp headerwill be written with microsecond precision, while WARC 1.0 only supports second precision.WARC 1.0.. code    WARC1.0    ...    WARC-Date 2018-12-26T101112ZWARC 1.1.. code    WARC1.1    ...    WARC-Date 2018-12-26T101112.456789ZFiltering HTTP CaptureWhen capturing via HTTP, it is possible to provide a custom filter function, which can be used to determine if a particular request and response recordsshould be written to the WARC file or skipped.The filter function is called with the request and response recordbefore they are written, and can be used to substitute a different record (for example, a revisitinstead of a response), or to skip writing altogether by returning nothing, as shown below.. code python    def filter_records(request, response, request_recorder)         return None, None to indicate records should be skipped        if response.http_headers.get_statuscode()  200            return None, None         the response record can be replaced with a revisit record        elif check_for_dedup()            response  create_revisit_record(...)        return request, response    with capture_http(example.warc.gz, filter_records)         requests.get(httpsexample.com)Please refer totesttest_capture_http.py testtest_capture_http.py__ for additional examplesof capturing requests traffic to WARC.ManualAdvanced WARC WritingBefore 1.6, this was the primary method for fetching a url and thenwriting to a WARC. This process is a bit more verbose,but provides for full control of WARC creation and avoid monkey-patching.The following example loads httpexample.com, creates a WARCresponse record, and writes it, gzip compressed, to example.warc.gzThe block and payload digests are computed automatically... code python    from warcio.warcwriter import WARCWriter    from warcio.statusandheaders import StatusAndHeaders    import requests    with open(example.warc.gz, wb) as output        writer  WARCWriter(output, gzipTrue)        resp  requests.get(httpexample.com,                            headers{Accept-Encoding identity},                            streamTrue)         get raw headers from urllib3        headers_list  resp.raw.headers.items()        http_headers  StatusAndHeaders(200 OK, headers_list, protocolHTTP1.0)        record  writer.create_warc_record(httpexample.com, response,                                            payloadresp.raw,                                            http_headershttp_headers)        writer.write_record(record)The library also includes additional semantics for - Creating warcinfo and revisit records - Writing response and request records together - Writing custom WARC records - Reading a full WARC record from a streamPlease refer to warcwriter.py warciowarcwriter.py__ andtesttest_writer.py testtest_writer.py__ for additional examples.WARCIO CLI Indexing and Recompression--------------------------------------The library currently ships with a few simple command line tools.IndexThe warcio index cmd will print a simple index of the records in thewarc file as newline delimited JSON lines (NDJSON).WARC header fields to include in the index can be specified via the-f flag, and are included in the JSON block (in order, forconvenience).    warcio index .testdataexample-iana.org-chunked.warc -f warc-type,warc-target-uri,content-length    {warc-type warcinfo, content-length 137}    {warc-type response, warc-target-uri httpwww.iana.org, content-length 7566}    {warc-type request, warc-target-uri httpwww.iana.org, content-length 76}HTTP header fields can be included by prefixing them with the prefixhttp. The special field offset refers to the record offset withinthe warc file.    warcio index .testdataexample-iana.org-chunked.warc -f offset,content-type,httpcontent-type,warc-target-uri    {offset 0, content-type applicationwarc-fields}    {offset 405, content-type applicationhttpmsgtyperesponse, httpcontent-type texthtml charsetUTF-8, warc-target-uri httpwww.iana.org}    {offset 8379, content-type applicationhttpmsgtyperequest, warc-target-uri httpwww.iana.org}(Note this library does not produce CDX or CDXJ format indexes oftenassociated with web archives. To create these indexes, please see thecdxj-indexer httpsgithub.comwebrecordercdxj-indexer__ tool which extends warcio indexing to provide this functionality)CheckThe warcio check command will check the payload and block digestsof WARC records, if possible. An exit value of 1 indicates a failure.warcio check -v will print verbose output for each record in theWARC file.RecompressThe recompress command allows for re-compressing or normalizing WARC(or ARC) files to a record-compressed, gzipped WARC file.Each WARC record is compressed individually and concatenated. This isthe canonical WARC storage format used byWebrecorder httpsgithub.comwebrecorderwebrecorder__ and otherweb archiving institutions, and usually stored with a .warc.gzextension.It can be used to - Compress an uncompressed WARC - Convert any ARCfile to a compressed WARC - Fix an improperly compressed WARC file (eg.a WARC compressed entirely instead of by record)    warcio recompress .input.arc.gz .output.warc.gzExtractThe  extract command provides a way to extract either the WARC and HTTP headers andor payload of a WARC recordto stdout. Given a WARC filename and an offset, extract will print the (decompressed) record at that offsetin the file to stdoutSpecifying --payload or --headers will output only the payload or only the WARC + HTTP headers (if any), respectively.    warcio extract [--payload  --headers] filename offsetLicensewarcio is licensed under the Apache 2.0 License and is part of theWebrecorder project.See NOTICE NOTICE__ and LICENSE LICENSE__ for details.</longdescription>
</pkgmetadata>