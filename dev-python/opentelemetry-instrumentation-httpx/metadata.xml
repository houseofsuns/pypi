<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>OpenTelemetry HTTPX Instrumentationpypi.. pypi image httpsbadge.fury.iopyopentelemetry-instrumentation-httpx.svg   target httpspypi.orgprojectopentelemetry-instrumentation-httpxThis library allows tracing HTTP requests made by thehttpx httpswww.python-httpx.org_ library.Installation------------     pip install opentelemetry-instrumentation-httpxUsage-----Instrumenting all clientsWhen using the instrumentor, all clients will automatically trace requests... code-block python     import httpx     from opentelemetry.instrumentation.httpx import HTTPXClientInstrumentor     url  httpshttpbin.orgget     HTTPXClientInstrumentor().instrument()     with httpx.Client() as client          response  client.get(url)     async with httpx.AsyncClient() as client          response  await client.get(url)Instrumenting single clientsIf you only want to instrument requests for specific client instances, you canuse the instrument_client method... code-block python    import httpx    from opentelemetry.instrumentation.httpx import HTTPXClientInstrumentor    url  httpshttpbin.orgget    with httpx.Client(transporttelemetry_transport) as client        HTTPXClientInstrumentor.instrument_client(client)        response  client.get(url)    async with httpx.AsyncClient(transporttelemetry_transport) as client        HTTPXClientInstrumentor.instrument_client(client)        response  await client.get(url)UninstrumentIf you need to uninstrument clients, there are two options available... code-block python    import httpx    from opentelemetry.instrumentation.httpx import HTTPXClientInstrumentor    HTTPXClientInstrumentor().instrument()    client  httpx.Client()     Uninstrument a specific client    HTTPXClientInstrumentor.uninstrument_client(client)     Uninstrument all clients    HTTPXClientInstrumentor().uninstrument()Using transports directlyIf you dont want to use the instrumentor class, you can use the transport classes directly... code-block python    import httpx    from opentelemetry.instrumentation.httpx import (        AsyncOpenTelemetryTransport,        SyncOpenTelemetryTransport,    )    url  httpshttpbin.orgget    transport  httpx.HTTPTransport()    telemetry_transport  SyncOpenTelemetryTransport(transport)    with httpx.Client(transporttelemetry_transport) as client        response  client.get(url)    transport  httpx.AsyncHTTPTransport()    telemetry_transport  AsyncOpenTelemetryTransport(transport)    async with httpx.AsyncClient(transporttelemetry_transport) as client        response  await client.get(url)Request and response hooksThe instrumentation supports specifying request and response hooks. These are functions that get called back by the instrumentation right after a span is created for a requestand right before the span is finished while processing a response... note    The request hook receives the raw arguments provided to the transport layer. The response hook receives the raw return values from the transport layer.The hooks can be configured as follows.. code-block python    from opentelemetry.instrumentation.httpx import HTTPXClientInstrumentor    def request_hook(span, request)         method, url, headers, stream, extensions  request        pass    def response_hook(span, request, response)         method, url, headers, stream, extensions  request         status_code, headers, stream, extensions  response        pass    HTTPXClientInstrumentor().instrument(request_hookrequest_hook, response_hookresponse_hook)Or if you are using the transport classes directly.. code-block python    from opentelemetry.instrumentation.httpx import SyncOpenTelemetryTransport    def request_hook(span, request)         method, url, headers, stream, extensions  request        pass    def response_hook(span, request, response)         method, url, headers, stream, extensions  request         status_code, headers, stream, extensions  response        pass    transport  httpx.HTTPTransport()    telemetry_transport  SyncOpenTelemetryTransport(        transport,        request_hookrequest_hook,        response_hookresponse_hook    )References---------- OpenTelemetry HTTPX Instrumentation httpsopentelemetry-python-contrib.readthedocs.ioenlatestinstrumentationhttpxhttpx.html_ OpenTelemetry Project httpsopentelemetry.io_</longdescription>
</pkgmetadata>