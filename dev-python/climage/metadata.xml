<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># CLImageConvert images to beautiful ANSI escape codes for display in command line interfaces.Available as both a CLI application and a Python library.![demo](https://raw.github.com/pnappa/CLImage/master/extra/demo.png)## Features - 8/16/256/Truecolor supports - Custom system color palettes - Toggleable Unicode mode, allowing up to 4x more detail - Custom output size## Example usage### CLI`$ climage --unicode --truecolor --cols 80 barney.jpg`![cliusage](https://raw.github.com/pnappa/CLImage/master/extra/clibarney.png)For more detail and available options, run `$ climage --help`.### Python```python3import climageoutput = climage.convert('image.png', is_unicode=True)print(output)# Converting downloaded filefrom PIL import Imageimport requestsresponse = requests.get('https://www.python.org/static/community_logos/python-logo-master-v3-TM-flattened.png')# Convert to RGB, as files on the Internet may be greyscale, which are not# supported.img = Image.open(BytesIO(response.content)).convert('RGB')# Convert the image to 80col, in 256 color mode, using unicode for higher def.converted = climage.convert_pil(img, is_unicode=True)print(converted)# Convert the image into 50px * 50px, as the convert_array function does not# perform resizing.img = Image.open('image.png').convert('RGB').resize((50, 50))arr = np.array(img)output = climage.convert_array(arr, is_unicode=True)print(output)```View additional examples on the [project homepage](https://github.com/pnappa/CLImage).</longdescription>
</pkgmetadata>