<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Single-source There is only one truth single-source helps to reduce the entropy in your Python project by keeping single source of truth.The targets of this library are modern Python projects which want to haveone source of truth for version, name and etc.At the moment, the library provides the single point for a package version.It supports Python 3.6+. Quick startpython root_package__init__.pyfrom pathlib import Pathfrom single_source import get_version__version__  get_version(__name__, Path(__file__).parent.parent) Root of the problemYou use modern pyproject.toml and want to keep the version of your packageheretoml pyproject.toml[tool.poetry]name  modern-projectversion  0.1.0Lets imagine the version of your package is required in some place of the code.Since you need the version in your Python code, you may want to duplicate the version by putting it as a string variable to some python filepython modern_project__init__.py__version__  0.1.0 modern_projectversion.pyversion  0.1.0Then you realize you dont want to have the version in a python file and in pyproject.toml at the same time. Its harder to keep them consistent and easier to forget to bump both versions before release.Also, you dont want to build the wheel by creating some script for auto incrementing the version in both places (and use it in your CI flow, for example). Instead you want use poetry version commands. InstallationYou can install single-source via [pip](httpspip.pypa.ioenstable)bashpip3 install single-sourceor via [poetry](httpspython-poetry.orgdocsinstallation)bashpoetry add single-sourceThe library also available as[a conda package](httpsdocs.conda.ioprojectscondaenlatest) in[conda-forge](httpsanaconda.orgconda-forgerepo) channelbashconda install single-source --channel conda-forge Advanced usage Changing default valueIf its not possible to get the version from package metadata orthere is no pyproject.toml get_version returns  - empty string by default.You can change this value by providing a value as a default_return keyword argument.pythonfrom pathlib import Pathfrom single_source import get_versionpath_to_pyproject_dir  Path(__file__).parent.parent__version__  get_version(__name__, path_to_pyproject_dir, default_returnNone) Raising an exceptionYou may want to raise an exception in case the version of the packagehas not been found.pythonfrom pathlib import Pathfrom single_source import get_version, VersionNotFoundErrorpath_to_pyproject_dir  Path(__file__).parent.parenttry    __version__  get_version(__name__, path_to_pyproject_dir, failTrue)except VersionNotFoundError    pass Not only pyproject.tomlYou can use single-source even if you still store the version of your libraryin setup.py or in any other utf-8 encoded text file.First, try without custom regex, probably it can parse the versionIf the default internal regex does not find the version in your file,the only thing you need to provide is a custom regex to get_versionpythonfrom single_source import get_versioncustom_regex  rsversionss[]s([-.w]{3,})s[]spath_to_file  my-projectsome_file_with_version.txt__version__  get_version(__name__, path_to_file, version_regexcustom_regex)Version must be in the first group () in the custom regex. ContributingPull requests are welcome. For major changes, please open an issue first todiscuss what you would like to change.Please make sure to update tests as appropriate. License[MIT](httpschoosealicense.comlicensesmit)</longdescription>
</pkgmetadata>