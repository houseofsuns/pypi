<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> django-admin-cursor-paginatorDrop-in replacement for django admin default pagination that works fast with huge tables. Prerequisites- You are using django (2.0) admin- Some of your modelstables contains so much data that list page lags- You dont want to make separate CRM page for them and just need fast and simple solution- Youre ok that you wont see total objects or pages count and will navigate only by +-1 page- Index must be created for the ordering field, otherwise you wont get any significant speedup  (default integer pks work perfectly out-of-box)- Ordering field type can be almost anything that can be encoded via str()  and then be used in queryset.filter() NOTE   If ordering field is not unique then pagination can mess a bit in edge cases. But for the most often case with datetime fields with milliseconds like created_at it wont be a problem. It can be solved in future by adding support for multiple ordering fields. InstallationAll you need is to install it via pip like thatshellpip install django-admin-cursor-paginator...and extend INSTALLED_APPS (it is needed only for templates)pythonINSTALLED_APPS  [    django.contrib.admin,     ...    admin_cursor_paginator,     ...] NOTE   This app overrides { block pagination } in default change_list.html to insert custom pagination template tag, so if you use patched change_list.html too, dont forget to copy pagination block or (seems better) to inherit this app tpl like { extends admin_cursor_paginatorchange_list.html }. Also, theres a known [issue with grapelli](httpsgithub.coma1tusdjango-admin-cursor-paginatorissues4) that is fixed the same way.  Usage exampleSo this app will certainly help you if youre using admin to see some (usually) last inserted entriesand may be also want to search for particular objects.But anyways its just a regular django admin page.pythonfrom admin_cursor_paginator import CursorPaginatorAdminclass YourModelAdmin(CursorPaginatorAdmin)    cursor_ordering_field  -created_at   default is -pkclass AnotherModelAdmin(CursorPaginatorAdmin)    cursor_ordering_field  parent__sort   joins are supported as well The problemDefault django admin list view makes several queries to show data.The first one is something like select count() from app_model to calculate pages rangeand select  from app_model limit 10 offset 1000 to fetch current page data.Both of them start to produce performance problems as your table grows.For example, with 1M entries in postgresql table count() took a few seconds (in our case) because of seq scan. Limit+offset also performs badly on far pages. The solutionThe solution is to use cursor pagination instead of default one (you can find many standalone packages for it).It doesnt need count() query at all and performs fast by using filter instead of limit+offset.Some useful links- httpscra.mr20110308building-cursors-for-the-disqus-api- httpswww.django-rest-framework.orgapi-guidepaginationcursorpagination- httpshakibenita.comoptimizing-the-django-admin-paginator Test projectIn testproject folder you can (surprisingly) find test project that uses this appfor one model so that you can see live example and check how it works if you want.All you need is to install requirements from requirements.txt in some env,run .manage.py runserver, login as adminadmin to django admin and open Product list.Project includes sqlite3 file with some test data and preinstalled debug_toolbarto observe db queries.You can play with ordering field or data amount or may be test it in your target db.Example of how pagination will look like[](assetstestproject-example.png)</longdescription>
</pkgmetadata>