<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> flake8-pep585This plugin enforces the changes proposed by [PEP 585](httpspeps.python.orgpep-0585). What does PEP 585 changeBefore PEP 585, you had to import stuff from typing to annotate some objects from the standard library- For context managers, youd import typing.ContextManager- For lists, youd import typing.List- For callables, youd import typing.Callable- ...and so onWith PEP 585, you can now use classes already present in the standard library. For example- For a context manager giving an int, use contextlib.AbstractContextManager[int]- For a list of dicts mapping strs to ints, use list[dict[str, int]]- For a callable taking a float and returning an int, use collections.abc.Callable[[float], int]typing.List, typing.Callable etc. are now deprecated. This is pretty hard to discover, since theseimports dont cause a deprecation warning. IDEs dont help either the auto-import feature often suggestsimporting a deprecated item.This plugin lets you find these deprecated imports. Examples Direct importpyfrom typing import CallablePEA001 typing.Callable is deprecated, use collections.abc.Callable instead. See PEP 585 for details Qualified importpyfrom datetime import timeimport typing as tydef construct_time(match ty.Match) - time    return time(        hourint(match[hour]),        minuteint(match[minute]),    )PEA001 typing.Match is deprecated, use re.Match instead. See PEP 585 for details Installation1. Make sure you have flake8 installed2. Run pip install flake8-pep5853. Run flake8 on your code ConfigurationVia your setup.cfg filetoml[flake8]pep585-activation  always   always, auto or never Symbols that youre okay with being imported from typingpep585-whitelisted-symbols     Callable    Match    PatternVia the CLIpython -m flake8 --pep585-activationalways your_projectfile.pyThis only changes how the plugin behaves on Python 3.7.x and Python 3.8.x. By default (auto), it will be enabledif a from __future__ import annotations line is found.</longdescription>
</pkgmetadata>