<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># Pottery: Redis for Humans 🌎🌍🌏[Redis](http://redis.io/) is awesome, but [Rediscommands](http://redis.io/commands) are not always intuitive.  Pottery is aPythonic way to access Redis.  If you know how to use Python dicts, then youalready know how to use Pottery.  Pottery is useful for accessing Redis moreeasily, and also for implementing microservice resilience patterns; and it hasbeen battle tested in production at scale.[![Build status](https://img.shields.io/github/workflow/status/brainix/pottery/Python%20package/master)](https://github.com/brainix/pottery/actions?query=branch%3Amaster)[![Security status](https://img.shields.io/badge/security-bandit-dark.svg)](https://github.com/PyCQA/bandit)[![Latest released version](https://badge.fury.io/py/pottery.svg)](https://badge.fury.io/py/pottery)![Supported Python versions](https://img.shields.io/pypi/pyversions/pottery)![Number of lines of code](https://img.shields.io/tokei/lines/github/brainix/pottery)[![Total number of downloads](https://pepy.tech/badge/pottery)](https://pepy.tech/project/pottery)[![Downloads per month](https://pepy.tech/badge/pottery/month)](https://pepy.tech/project/pottery)[![Downloads per week](https://pepy.tech/badge/pottery/week)](https://pepy.tech/project/pottery)## Table of Contents- [Dicts 📖](#dicts)- [Sets 🛍️](#sets)- [Lists ⛓](#lists)- [Counters 🧮](#counters)- [Deques 🖇️](#deques)- [Queues 🚶♂️🚶♀️🚶♂️](#queues)- [Redlock 🔒](#redlock)    - [synchronize() 👯♀️](#synchronize)- [NextId 🔢](#nextid)- [redis_cache()](#redis_cache)- [CachedOrderedDict](#cachedordereddict)- [Bloom filters 🌸](#bloom-filters)- [HyperLogLogs 🪵](#hyperloglogs)- [ContextTimer ⏱️](#contexttimer)## Installation```shell$ pip3 install pottery```## UsageFirst, set up your Redis client:```python&gt;&gt;&gt; from redis import Redis&gt;&gt;&gt; redis = Redis.from_url('redis://localhost:6379/1')&gt;&gt;&gt;```## &lt;a name=&quot;dicts&quot;&gt;&lt;/a&gt;Dicts 📖`RedisDict` is a Redis-backed container compatible with Python&amp;rsquo;s[`dict`](https://docs.python.org/3/tutorial/datastructures.html#dictionaries).Here is a small example using a `RedisDict`:```python&gt;&gt;&gt; from pottery import RedisDict&gt;&gt;&gt; tel = RedisDict({'jack': 4098, 'sape': 4139}, redis=redis, key='tel')&gt;&gt;&gt; tel['guido'] = 4127&gt;&gt;&gt; telRedisDict{'jack': 4098, 'sape': 4139, 'guido': 4127}&gt;&gt;&gt; tel['jack']4098&gt;&gt;&gt; del tel['sape']&gt;&gt;&gt; tel['irv'] = 4127&gt;&gt;&gt; telRedisDict{'jack': 4098, 'guido': 4127, 'irv': 4127}&gt;&gt;&gt; list(tel)['jack', 'guido', 'irv']&gt;&gt;&gt; sorted(tel)['guido', 'irv', 'jack']&gt;&gt;&gt; 'guido' in telTrue&gt;&gt;&gt; 'jack' not in telFalse&gt;&gt;&gt;```Notice the first two keyword arguments to `RedisDict()`:  The first is yourRedis client.  The second is the Redis key name for your dict.  Other thanthat, you can use your `RedisDict` the same way that you use any other Python`dict`.*Limitations:*1. Keys and values must be JSON serializable.## &lt;a name=&quot;sets&quot;&gt;&lt;/a&gt;Sets 🛍️`RedisSet` is a Redis-backed container compatible with Python&amp;rsquo;s[`set`](https://docs.python.org/3/tutorial/datastructures.html#sets).Here is a brief demonstration:```python&gt;&gt;&gt; from pottery import RedisSet&gt;&gt;&gt; basket = RedisSet({'apple', 'orange', 'apple', 'pear', 'orange', 'banana'}, redis=redis, key='basket')&gt;&gt;&gt; sorted(basket)['apple', 'banana', 'orange', 'pear']&gt;&gt;&gt; 'orange' in basketTrue&gt;&gt;&gt; 'crabgrass' in basketFalse&gt;&gt;&gt; a = RedisSet('abracadabra', redis=redis, key='magic')&gt;&gt;&gt; b = set('alacazam')&gt;&gt;&gt; sorted(a)['a', 'b', 'c', 'd', 'r']&gt;&gt;&gt; sorted(a - b)['b', 'd', 'r']&gt;&gt;&gt; sorted(a | b)['a', 'b', 'c', 'd', 'l', 'm', 'r', 'z']&gt;&gt;&gt; sorted(a &amp; b)['a', 'c']&gt;&gt;&gt; sorted(a ^ b)['b', 'd', 'l', 'm', 'r', 'z']&gt;&gt;&gt;```Notice the two keyword arguments to `RedisSet()`:  The first is your Redisclient.  The second is the Redis key name for your set.  Other than that, youcan use your `RedisSet` the same way that you use any other Python `set`.Do more efficient membership testing for multiple elements using`.contains_many()`:```python&gt;&gt;&gt; nirvana = RedisSet({'kurt', 'krist', 'dave'}, redis=redis, key='nirvana')&gt;&gt;&gt; tuple(nirvana.contains_many('kurt', 'krist', 'chat', 'dave'))(True, True, False, True)&gt;&gt;&gt;```*Limitations:*1. Elements must be JSON serializable.## &lt;a name=&quot;lists&quot;&gt;&lt;/a&gt;Lists ⛓`RedisList` is a Redis-backed container compatible with Python&amp;rsquo;s[`list`](https://docs.python.org/3/tutorial/introduction.html#lists).```python&gt;&gt;&gt; from pottery import RedisList&gt;&gt;&gt; squares = RedisList([1, 4, 9, 16, 25], redis=redis, key='squares')&gt;&gt;&gt; squaresRedisList[1, 4, 9, 16, 25]&gt;&gt;&gt; squares[0]1&gt;&gt;&gt; squares[-1]25&gt;&gt;&gt; squares[-3:][9, 16, 25]&gt;&gt;&gt; squares[:][1, 4, 9, 16, 25]&gt;&gt;&gt; squares + [36, 49, 64, 81, 100]RedisList[1, 4, 9, 16, 25, 36, 49, 64, 81, 100]&gt;&gt;&gt;```Notice the two keyword arguments to `RedisList()`:  The first is your Redisclient.  The second is the Redis key name for your list.  Other than that, youcan use your `RedisList` the same way that you use any other Python `list`.*Limitations:*1. Elements must be JSON serializable.2. Under the hood, Python implements `list` using an array.  Redis implements   list using a   [doubly linked list](https://redis.io/topics/data-types-intro#redis-lists).   As such, inserting elements at the head or tail of a `RedisList` is fast,   O(1).  However, accessing `RedisList` elements by index is slow, O(n).  So   in terms of performance and ideal use cases, `RedisList` is more similar to   Python&amp;rsquo;s `deque` than Python&amp;rsquo;s `list`.  Instead of `RedisList`,   consider using [`RedisDeque`](#deques).## &lt;a name=&quot;counters&quot;&gt;&lt;/a&gt;Counters 🧮`RedisCounter` is a Redis-backed container compatible with Python&amp;rsquo;s[`collections.Counter`](https://docs.python.org/3/library/collections.html#collections.Counter).```python&gt;&gt;&gt; from pottery import RedisCounter&gt;&gt;&gt; c = RedisCounter(redis=redis, key='my-counter')&gt;&gt;&gt; c = RedisCounter('gallahad', redis=redis, key='my-counter')&gt;&gt;&gt; c.clear()&gt;&gt;&gt; c = RedisCounter({'red': 4, 'blue': 2}, redis=redis, key='my-counter')&gt;&gt;&gt; c.clear()&gt;&gt;&gt; c = RedisCounter(redis=redis, key='my-counter', cats=4, dogs=8)&gt;&gt;&gt; c.clear()&gt;&gt;&gt; c = RedisCounter(['eggs', 'ham'], redis=redis, key='my-counter')&gt;&gt;&gt; c['bacon']0&gt;&gt;&gt; c['sausage'] = 0&gt;&gt;&gt; del c['sausage']&gt;&gt;&gt; c.clear()&gt;&gt;&gt; c = RedisCounter(redis=redis, key='my-counter', a=4, b=2, c=0, d=-2)&gt;&gt;&gt; sorted(c.elements())['a', 'a', 'a', 'a', 'b', 'b']&gt;&gt;&gt; c.clear()&gt;&gt;&gt; RedisCounter('abracadabra', redis=redis, key='my-counter').most_common(3)[('a', 5), ('b', 2), ('r', 2)]&gt;&gt;&gt; c.clear()&gt;&gt;&gt; c = RedisCounter(redis=redis, key='my-counter', a=4, b=2, c=0, d=-2)&gt;&gt;&gt; from collections import Counter&gt;&gt;&gt; d = Counter(a=1, b=2, c=3, d=4)&gt;&gt;&gt; c.subtract(d)&gt;&gt;&gt; cRedisCounter{'a': 3, 'b': 0, 'c': -3, 'd': -6}&gt;&gt;&gt;```Notice the first two keyword arguments to `RedisCounter()`:  The first is yourRedis client.  The second is the Redis key name for your counter.  Other thanthat, you can use your `RedisCounter` the same way that you use any otherPython `Counter`.*Limitations:*1. Keys must be JSON serializable.## &lt;a name=&quot;deques&quot;&gt;&lt;/a&gt;Deques 🖇️`RedisDeque` is a Redis-backed container compatible with Python&amp;rsquo;s[`collections.deque`](https://docs.python.org/3/library/collections.html#collections.deque).Example:```python&gt;&gt;&gt; from pottery import RedisDeque&gt;&gt;&gt; d = RedisDeque('ghi', redis=redis, key='letters')&gt;&gt;&gt; for elem in d:...     print(elem.upper())GHI&gt;&gt;&gt; d.append('j')&gt;&gt;&gt; d.appendleft('f')&gt;&gt;&gt; dRedisDeque(['f', 'g', 'h', 'i', 'j'])&gt;&gt;&gt; d.pop()'j'&gt;&gt;&gt; d.popleft()'f'&gt;&gt;&gt; list(d)['g', 'h', 'i']&gt;&gt;&gt; d[0]'g'&gt;&gt;&gt; d[-1]'i'&gt;&gt;&gt; list(reversed(d))['i', 'h', 'g']&gt;&gt;&gt; 'h' in dTrue&gt;&gt;&gt; d.extend('jkl')&gt;&gt;&gt; dRedisDeque(['g', 'h', 'i', 'j', 'k', 'l'])&gt;&gt;&gt; d.rotate(1)&gt;&gt;&gt; dRedisDeque(['l', 'g', 'h', 'i', 'j', 'k'])&gt;&gt;&gt; d.rotate(-1)&gt;&gt;&gt; dRedisDeque(['g', 'h', 'i', 'j', 'k', 'l'])&gt;&gt;&gt; RedisDeque(reversed(d), redis=redis)RedisDeque(['l', 'k', 'j', 'i', 'h', 'g'])&gt;&gt;&gt; d.clear()&gt;&gt;&gt; d.extendleft('abc')&gt;&gt;&gt; dRedisDeque(['c', 'b', 'a'])&gt;&gt;&gt;```Notice the two keyword arguments to `RedisDeque()`:  The first is your Redisclient.  The second is the Redis key name for your deque.  Other than that, youcan use your `RedisDeque` the same way that you use any other Python `deque`.*Limitations:*1. Elements must be JSON serializable.## &lt;a name=&quot;queues&quot;&gt;&lt;/a&gt;Queues 🚶♂️🚶♀️🚶♂️`RedisSimpleQueue` is a Redis-backed multi-producer, multi-consumer FIFO queuecompatible with Python&amp;rsquo;s[`queue.SimpleQueue`](https://docs.python.org/3/library/queue.html#simplequeue-objects).In general, use a Python `queue.Queue` if you&amp;rsquo;re using it in one or morethreads, use `multiprocessing.Queue` if you&amp;rsquo;re using it between processes,and use `RedisSimpleQueue` if you&amp;rsquo;re sharing it across machines or if youneed for your queue to persist across application crashes or restarts.Instantiate a `RedisSimpleQueue`:```python&gt;&gt;&gt; from pottery import RedisSimpleQueue&gt;&gt;&gt; cars = RedisSimpleQueue(redis=redis, key='cars')&gt;&gt;&gt;```Notice the two keyword arguments to `RedisSimpleQueue()`:  The first is yourRedis client.  The second is the Redis key name for your queue.  Other thanthat, you can use your `RedisSimpleQueue` the same way that you use any otherPython `queue.SimpleQueue`.Check the queue state, put some items in the queue, and get those items backout:```python&gt;&gt;&gt; cars.empty()True&gt;&gt;&gt; cars.qsize()0&gt;&gt;&gt; cars.put('Jeep')&gt;&gt;&gt; cars.put('Honda')&gt;&gt;&gt; cars.put('Audi')&gt;&gt;&gt; cars.empty()False&gt;&gt;&gt; cars.qsize()3&gt;&gt;&gt; cars.get()'Jeep'&gt;&gt;&gt; cars.get()'Honda'&gt;&gt;&gt; cars.get()'Audi'&gt;&gt;&gt; cars.empty()True&gt;&gt;&gt; cars.qsize()0&gt;&gt;&gt;```*Limitations:*1. Items must be JSON serializable.## &lt;a name=&quot;redlock&quot;&gt;&lt;/a&gt;Redlock 🔒`Redlock` is a safe and reliable lock to coordinate access to a resource sharedacross threads, processes, and even machines, without a single point offailure.  [Rationale and algorithmdescription.](http://redis.io/topics/distlock)`Redlock` implements Python&amp;rsquo;s excellent[`threading.Lock`](https://docs.python.org/3/library/threading.html#lock-objects)API as closely as is feasible.  In other words, you can use `Redlock` the sameway that you use `threading.Lock`.  The main reason to use `Redlock` over`threading.Lock` is that `Redlock` can coordinate access to a resource sharedacross different machines; `threading.Lock` can&amp;rsquo;t.Instantiate a `Redlock`:```python&gt;&gt;&gt; from pottery import Redlock&gt;&gt;&gt; printer_lock = Redlock(key='printer', masters={redis})&gt;&gt;&gt;```The `key` argument represents the resource, and the `masters` argumentspecifies your Redis masters across which to distribute the lock.  Inproduction, you should have 5 Redis masters.  This is to eliminate a singlepoint of failure &amp;mdash; you can lose up to 2 out of the 5 Redis masters andyour `Redlock` will remain available and performant.  Now you can protectaccess to your resource:```python&gt;&gt;&gt; if printer_lock.acquire():...     print('printer_lock is locked')...     # Critical section - print stuff here....     printer_lock.release()printer_lock is locked&gt;&gt;&gt; bool(printer_lock.locked())False&gt;&gt;&gt;```Or you can protect access to your resource inside a context manager:```python&gt;&gt;&gt; with printer_lock:...     print('printer_lock is locked')...     # Critical section - print stuff here.printer_lock is locked&gt;&gt;&gt; bool(printer_lock.locked())False&gt;&gt;&gt;```It&amp;rsquo;s safest to instantiate a new `Redlock` object every time you need toprotect your resource and to not share `Redlock` instances across differentparts of code.  In other words, think of the `key` as identifying the resource;don&amp;rsquo;t think of any particular `Redlock` as identifying the resource.Instantiating a new `Redlock` every time you need a lock sidesteps bugs bydecoupling how you use `Redlock` from the forking/threading model of yourapplication/service.`Redlock`s are automatically released (by default, after 10 seconds).  Youshould take care to ensure that your critical section completes well withinthat timeout.  The reasons that `Redlock`s are automatically released are topreserve[&amp;ldquo;liveness&amp;rdquo;](http://redis.io/topics/distlock#liveness-arguments)and to avoid deadlocks (in the event that a process dies inside a criticalsection before it releases its lock).```python&gt;&gt;&gt; import time&gt;&gt;&gt; if printer_lock.acquire():...     # Critical section - print stuff here....     time.sleep(10)&gt;&gt;&gt; bool(printer_lock.locked())False&gt;&gt;&gt;```If 10 seconds isn&amp;rsquo;t enough to complete executing your critical section,then you can specify your own auto release time (in seconds):```python&gt;&gt;&gt; printer_lock = Redlock(key='printer', masters={redis}, auto_release_time=15)&gt;&gt;&gt; if printer_lock.acquire():...     # Critical section - print stuff here....     time.sleep(10)&gt;&gt;&gt; bool(printer_lock.locked())True&gt;&gt;&gt; time.sleep(5)&gt;&gt;&gt; bool(printer_lock.locked())False&gt;&gt;&gt;```By default, `.acquire()` blocks indefinitely until the lock is acquired.  Youcan make `.acquire()` return immediately with the `blocking` argument.`.acquire()` returns `True` if the lock was acquired; `False` if not.```python&gt;&gt;&gt; printer_lock_1 = Redlock(key='printer', masters={redis})&gt;&gt;&gt; printer_lock_2 = Redlock(key='printer', masters={redis})&gt;&gt;&gt; printer_lock_1.acquire(blocking=False)True&gt;&gt;&gt; printer_lock_2.acquire(blocking=False)  # Returns immediately.False&gt;&gt;&gt; printer_lock_1.release()&gt;&gt;&gt;```You can make `.acquire()` block but not indefinitely by specifying the`timeout` argument (in seconds):```python&gt;&gt;&gt; printer_lock_1.acquire(timeout=1)True&gt;&gt;&gt; printer_lock_2.acquire(timeout=1)  # Waits 1 second.False&gt;&gt;&gt; printer_lock_1.release()&gt;&gt;&gt;```You can similarly configure the Redlock context manager&amp;rsquo;sblocking/timeout behavior during Redlock initialization.  If the contextmanager fails to acquire the lock, it raises the `QuorumNotAchieved` exception.```python&gt;&gt;&gt; import contextlib&gt;&gt;&gt; from pottery import QuorumNotAchieved&gt;&gt;&gt; printer_lock_1 = Redlock(key='printer', masters={redis}, context_manager_blocking=True, context_manager_timeout=0.2)&gt;&gt;&gt; printer_lock_2 = Redlock(key='printer', masters={redis}, context_manager_blocking=True, context_manager_timeout=0.2)&gt;&gt;&gt; with printer_lock_1:...     with contextlib.suppress(QuorumNotAchieved):...         with printer_lock_2:  # Waits 0.2 seconds; raises QuorumNotAchieved....             pass...     print(f&quot;printer_lock_1 is {'locked' if printer_lock_1.locked() else 'unlocked'}&quot;)...     print(f&quot;printer_lock_2 is {'locked' if printer_lock_2.locked() else 'unlocked'}&quot;)printer_lock_1 is lockedprinter_lock_2 is unlocked&gt;&gt;&gt;```### &lt;a name=&quot;synchronize&quot;&gt;&lt;/a&gt;synchronize() 👯♀️`synchronize()` is a decorator that allows only one thread to execute afunction at a time.  Under the hood, `synchronize()` uses a Redlock, so referto the [Redlock documentation](#redlock) for more details.Here&amp;rsquo;s how to use `synchronize()`:```python&gt;&gt;&gt; from pottery import synchronize&gt;&gt;&gt; @synchronize(key='synchronized-func', masters={redis}, auto_release_time=.5, blocking=True, timeout=-1)... def func():...   # Only one thread can execute this function at a time....   return True...&gt;&gt;&gt;```## &lt;a name=&quot;nextid&quot;&gt;&lt;/a&gt;NextId 🔢`NextId` safely and reliably produces increasing IDs across threads, processes,and even machines, without a single point of failure.  [Rationale and algorithmdescription.](http://antirez.com/news/102)Instantiate an ID generator:```python&gt;&gt;&gt; from pottery import NextId&gt;&gt;&gt; tweet_ids = NextId(key='tweet-ids', masters={redis})&gt;&gt;&gt;```The `key` argument represents the sequence (so that you can have differentsequences for user IDs, comment IDs, etc.), and the `masters` argumentspecifies your Redis masters across which to distribute ID generation (inproduction, you should have 5 Redis masters).  Now, whenever you need a userID, call `next()` on the ID generator:```python&gt;&gt;&gt; next(tweet_ids)1&gt;&gt;&gt; next(tweet_ids)2&gt;&gt;&gt; next(tweet_ids)3&gt;&gt;&gt;```Two caveats:1. If many clients are generating IDs concurrently, then there may be   &amp;ldquo;holes&amp;rdquo; in the sequence of IDs (e.g.: 1, 2, 6, 10, 11, 21,   &amp;hellip;).2. This algorithm scales to about 5,000 IDs per second (with 5 Redis masters).   If you need IDs faster than that, then you may want to consider other   techniques.## redis_cache()`redis_cache()` is a simple lightweight unbounded function return value cache,sometimes called[&amp;ldquo;memoize&amp;rdquo;](https://en.wikipedia.org/wiki/Memoization).`redis_cache()` implements Python&amp;rsquo;s excellent[`functools.cache()`](https://docs.python.org/3/library/functools.html#functools.cache)API as closely as is feasible.  In other words, you can use `redis_cache()` thesame way that you use `functools.cache()`.*Limitations:*1. Arguments to the function must be hashable.2. Return values from the function must be JSON serializable.3. Just like `functools.cache()`, `redis_cache()` does not allow for a maximum   size, and does not evict old values, and grows unbounded.  Only use   `redis_cache()` in one of these cases:    1. Your function&amp;rsquo;s argument space has a known small cardinality.    2. You specify a `timeout` when calling `redis_cache()` to decorate your       function, to dump your _entire_ return value cache `timeout` seconds       after the last cache access (hit or miss).    3. You periodically call `.cache_clear()` to dump your _entire_ return       value cache.    4. You&amp;rsquo;re ok with your return value cache growing unbounded, and you       [understand the implications](https://docs.redislabs.com/latest/rs/administering/database-operations/eviction-policy/)       of this for your underlying Redis instance.In general, you should only use `redis_cache()` when you want to reusepreviously computed values.  Accordingly, it doesn&amp;rsquo;t make sense to cachefunctions with side-effects or impure functions such as `time()` or `random()`.Decorate a function:```python&gt;&gt;&gt; import time&gt;&gt;&gt; from pottery import redis_cache&gt;&gt;&gt; @redis_cache(redis=redis, key='expensive-function-cache')... def expensive_function(n):...     time.sleep(1)  # Simulate an expensive computation or database lookup....     return n...&gt;&gt;&gt;```Notice the two keyword arguments to `redis_cache()`: The first is your Redisclient.  The second is the Redis key name for your function&amp;rsquo;s returnvalue cache.Call your function and observe the cache hit/miss rates:```python&gt;&gt;&gt; expensive_function(5)5&gt;&gt;&gt; expensive_function.cache_info()CacheInfo(hits=0, misses=1, maxsize=None, currsize=1)&gt;&gt;&gt; expensive_function(5)5&gt;&gt;&gt; expensive_function.cache_info()CacheInfo(hits=1, misses=1, maxsize=None, currsize=1)&gt;&gt;&gt; expensive_function(6)6&gt;&gt;&gt; expensive_function.cache_info()CacheInfo(hits=1, misses=2, maxsize=None, currsize=2)&gt;&gt;&gt;```Notice that the first call to `expensive_function()` takes 1 second and resultsin a cache miss; but the second call returns almost immediately and results ina cache hit.  This is because after the first call, `redis_cache()` cached thereturn value for the call when `n == 5`.You can access your original undecorated underlying `expensive_function()` as`expensive_function.__wrapped__`.  This is useful for introspection, forbypassing the cache, or for rewrapping the original function with a differentcache.You can force a cache reset for a particular combination of `args`/`kwargs`with `expensive_function.__bypass__`.  A call to`expensive_function.__bypass__(*args, **kwargs)` bypasses the cache lookup,calls the original underlying function, then caches the results for futurecalls to `expensive_function(*args, **kwargs)`.  Note that a call to`expensive_function.__bypass__(*args, **kwargs)` results in neither a cache hitnor a cache miss.Finally, clear/invalidate your function&amp;rsquo;s entire return value cache with`expensive_function.cache_clear()`:```python&gt;&gt;&gt; expensive_function.cache_info()CacheInfo(hits=1, misses=2, maxsize=None, currsize=2)&gt;&gt;&gt; expensive_function.cache_clear()&gt;&gt;&gt; expensive_function.cache_info()CacheInfo(hits=0, misses=0, maxsize=None, currsize=0)&gt;&gt;&gt;```## CachedOrderedDictThe best way that I can explain `CachedOrderedDict` is through an exampleuse-case.  Imagine that your search engine returns document IDs, which then youhave to hydrate into full documents via the database to return to the client.The data structure used to represent such search results must have thefollowing properties:1. It must preserve the order of the document IDs returned by the search engine.2. It must map document IDs to hydrated documents.3. It must cache previously hydrated documents.Properties 1 and 2 are satisfied by Python&amp;rsquo;s[`collections.OrderedDict`](https://docs.python.org/3/library/collections.html#collections.OrderedDict).However, `CachedOrderedDict` extends Python&amp;rsquo;s `OrderedDict` to alsosatisfy property 3.The most common usage pattern for `CachedOrderedDict` is as follows:1. Instantiate `CachedOrderedDict` with the IDs that you must look up or   compute passed in as the `dict_keys` argument to the initializer.2. Compute and store the cache misses for future lookups.3. Return some representation of your `CachedOrderedDict` to the client.Instantiate a `CachedOrderedDict`:```python&gt;&gt;&gt; from pottery import CachedOrderedDict&gt;&gt;&gt; search_results_1 = CachedOrderedDict(...     redis_client=redis,...     redis_key='search-results',...     dict_keys=(1, 2, 3, 4, 5),... )&gt;&gt;&gt;```The `redis_client` argument to the initializer is your Redis client, and the`redis_key` argument is the Redis key for the Redis Hash backing your cache.The `dict_keys` argument represents an ordered iterable of keys to be looked upand automatically populated in your `CachedOrderedDict` (on cache hits), orthat you&amp;rsquo;ll have to compute and populate for future lookups (on cachemisses).  Regardless of whether keys are cache hits or misses,`CachedOrderedDict` preserves the order of `dict_keys` (like a list), mapsthose keys to values (like a dict), and maintains an underlying cache forfuture key lookups.In the beginning, the cache is empty, so let&amp;rsquo;s populate it:```python&gt;&gt;&gt; sorted(search_results_1.misses())[1, 2, 3, 4, 5]&gt;&gt;&gt; search_results_1[1] = 'one'&gt;&gt;&gt; search_results_1[2] = 'two'&gt;&gt;&gt; search_results_1[3] = 'three'&gt;&gt;&gt; search_results_1[4] = 'four'&gt;&gt;&gt; search_results_1[5] = 'five'&gt;&gt;&gt; sorted(search_results_1.misses())[]&gt;&gt;&gt;```Note that `CachedOrderedDict` preserves the order of `dict_keys`:```python&gt;&gt;&gt; for key, value in search_results_1.items():...     print(f'{key}: {value}')1: one2: two3: three4: four5: five&gt;&gt;&gt;```Now, let&amp;rsquo;s look at a combination of cache hits and misses:```python&gt;&gt;&gt; search_results_2 = CachedOrderedDict(...     redis_client=redis,...     redis_key='search-results',...     dict_keys=(2, 4, 6, 8, 10),... )&gt;&gt;&gt; sorted(search_results_2.misses())[6, 8, 10]&gt;&gt;&gt; search_results_2[2]'two'&gt;&gt;&gt; search_results_2[6] = 'six'&gt;&gt;&gt; search_results_2[8] = 'eight'&gt;&gt;&gt; search_results_2[10] = 'ten'&gt;&gt;&gt; sorted(search_results_2.misses())[]&gt;&gt;&gt; for key, value in search_results_2.items():...     print(f'{key}: {value}')2: two4: four6: six8: eight10: ten&gt;&gt;&gt;```*Limitations:*1. Keys and values must be JSON serializable.## &lt;a name=&quot;bloom-filters&quot;&gt;&lt;/a&gt;Bloom filters 🌸Bloom filters are a powerful data structure that help you to answer thequestions, _&amp;ldquo;Have I seen this element before?&amp;rdquo;_ and _&amp;ldquo;Howmany distinct elements have I seen?&amp;rdquo;_; but not the question, _&amp;ldquo;Whatare all of the elements that I&amp;rsquo;ve seen before?&amp;rdquo;_  So think of Bloomfilters as Python sets that you can add elements to, use to test elementmembership, and get the length of; but that you can&amp;rsquo;t iterate through orget elements back out of.Bloom filters are probabilistic, which means that they can sometimes generatefalse positives (as in, they may report that you&amp;rsquo;ve seen a particularelement before even though you haven&amp;rsquo;t).  But they will never generatefalse negatives (so every time that they report that you haven&amp;rsquo;t seen aparticular element before, you really must never have seen it).  You can tuneyour acceptable false positive probability, though at the expense of thestorage size and the element insertion/lookup time of your Bloom filter.Create a `BloomFilter`:```python&gt;&gt;&gt; from pottery import BloomFilter&gt;&gt;&gt; dilberts = BloomFilter(...     num_elements=100,...     false_positives=0.01,...     redis=redis,...     key='dilberts',... )&gt;&gt;&gt;```Here, `num_elements` represents the number of elements that you expect toinsert into your `BloomFilter`, and `false_positives` represents youracceptable false positive probability.  Using these two parameters,`BloomFilter` automatically computes its own storage size and number of timesto run its hash functions on element insertion/lookup such that it canguarantee a false positive rate at or below what you can tolerate, given thatyou&amp;rsquo;re going to insert your specified number of elements.Insert an element into the `BloomFilter`:```python&gt;&gt;&gt; dilberts.add('rajiv')&gt;&gt;&gt;```Test for membership in the `BloomFilter`:```python&gt;&gt;&gt; 'rajiv' in dilbertsTrue&gt;&gt;&gt; 'raj' in dilbertsFalse&gt;&gt;&gt; 'dan' in dilbertsFalse&gt;&gt;&gt;```See how many elements we&amp;rsquo;ve inserted into the `BloomFilter`:```python&gt;&gt;&gt; len(dilberts)1&gt;&gt;&gt;```Note that `BloomFilter.__len__()` is an approximation, not an exact value,though it&amp;rsquo;s quite accurate.Insert multiple elements into the `BloomFilter`:```python&gt;&gt;&gt; dilberts.update({'raj', 'dan'})&gt;&gt;&gt;```Do more efficient membership testing for multiple elements using`.contains_many()`:```python&gt;&gt;&gt; tuple(dilberts.contains_many('rajiv', 'raj', 'dan', 'luis'))(True, True, True, False)&gt;&gt;&gt;```Remove all of the elements from the `BloomFilter`:```python&gt;&gt;&gt; dilberts.clear()&gt;&gt;&gt; len(dilberts)0&gt;&gt;&gt;```*Limitations:*1. Elements must be JSON serializable.2. `len(bf)` is probabilistic in that it&amp;rsquo;s an accurate approximation.  You   can tune how accurate you want it to be with the `num_elements` and   `false_positives` arguments to `.__init__()`, at the expense of storage space   and insertion/lookup time.3. Membership testing against a Bloom filter is probabilistic in that it *may*   return false positives, but *never* returns false negatives.  This means that   if `element in bf` evaluates to `True`, then you *may* have inserted the   element into the Bloom filter.  But if `element in bf` evaluates to `False`,   then you *must not* have inserted it.  Again, you can tune accuracy with the   `num_elements` and `false_positives` arguments to `.__init__()`, at the   expense of storage space and insertion/lookup time.## &lt;a name=&quot;hyperloglogs&quot;&gt;&lt;/a&gt;HyperLogLogs 🪵HyperLogLogs are an interesting data structure designed to answer the question,_&amp;ldquo;How many distinct elements have I seen?&amp;rdquo;_; but not the questions,_&amp;ldquo;Have I seen this element before?&amp;rdquo;_ or _&amp;ldquo;What are all of theelements that I&amp;rsquo;ve seen before?&amp;rdquo;_  So think of HyperLogLogs asPython sets that you can add elements to and get the length of; but that youcan&amp;rsquo;t use to test element membership, iterate through, or get elementsout of.HyperLogLogs are probabilistic, which means that they&amp;rsquo;re accurate withina margin of error up to 2%.  However, they can reasonably accurately estimatethe cardinality (size) of vast datasets (like the number of unique Googlesearches issued in a day) with a tiny amount of storage (1.5 KB).Create a `HyperLogLog`:```python&gt;&gt;&gt; from pottery import HyperLogLog&gt;&gt;&gt; google_searches = HyperLogLog(redis=redis, key='google-searches')&gt;&gt;&gt;```Insert an element into the `HyperLogLog`:```python&gt;&gt;&gt; google_searches.add('sonic the hedgehog video game')&gt;&gt;&gt;```See how many elements we&amp;rsquo;ve inserted into the `HyperLogLog`:```python&gt;&gt;&gt; len(google_searches)1&gt;&gt;&gt;```Insert multiple elements into the `HyperLogLog`:```python&gt;&gt;&gt; google_searches.update({...     'google in 1998',...     'minesweeper',...     'joey tribbiani',...     'wizard of oz',...     'rgb to hex',...     'pac-man',...     'breathing exercise',...     'do a barrel roll',...     'snake',... })&gt;&gt;&gt; len(google_searches)10&gt;&gt;&gt;```Through a clever hack, we can do membership testing against a `HyperLogLog`,even though it was never designed for this purpose.  The way that the hack worksis that it creates a temporary copy of the `HyperLogLog`, then inserts theelement that you&amp;rsquo;re running the membership test for into the temporarycopy.  If the insertion changes the temporary `HyperLogLog`&amp;rsquo;s cardinality,then the element must not have been inserted into the original `HyperLogLog`.```python&gt;&gt;&gt; 'joey tribbiani' in google_searchesTrue&gt;&gt;&gt; 'jennifer aniston' in google_searchesFalse&gt;&gt;&gt;```Do more efficient membership testing for multiple elements using`.contains_many()`:```python&gt;&gt;&gt; tuple(google_searches.contains_many('joey tribbiani', 'jennifer aniston'))(True, False)&gt;&gt;&gt;```Remove all of the elements from the `HyperLogLog`:```python&gt;&gt;&gt; google_searches.clear()&gt;&gt;&gt; len(google_searches)0&gt;&gt;&gt;```*Limitations:*1. Elements must be JSON serializable.2. `len(hll)` is probabilistic in that it&amp;rsquo;s an accurate approximation.3. Membership testing against a HyperLogLog is probabilistic in that it *may*   return false positives, but *never* returns false negatives.  This means that   if `element in hll` evaluates to `True`, then you *may* have inserted the   element into the HyperLogLog.  But if `element in hll` evaluates to `False`,   then you *must not* have inserted it.## &lt;a name=&quot;contexttimer&quot;&gt;&lt;/a&gt;ContextTimer ⏱️`ContextTimer` helps you easily and accurately measure elapsed time.  Note that`ContextTimer` measures wall (real-world) time, not CPU time; and that`elapsed()` returns time in milliseconds.You can use `ContextTimer` stand-alone&amp;hellip;```python&gt;&gt;&gt; import time&gt;&gt;&gt; from pottery import ContextTimer&gt;&gt;&gt; timer = ContextTimer()&gt;&gt;&gt; timer.start()&gt;&gt;&gt; time.sleep(0.1)&gt;&gt;&gt; 100 &lt;= timer.elapsed() &lt; 200True&gt;&gt;&gt; timer.stop()&gt;&gt;&gt; time.sleep(0.1)&gt;&gt;&gt; 100 &lt;= timer.elapsed() &lt; 200True&gt;&gt;&gt;```&amp;hellip;or as a context manager:```python&gt;&gt;&gt; tests = []&gt;&gt;&gt; with ContextTimer() as timer:...     time.sleep(0.1)...     tests.append(100 &lt;= timer.elapsed() &lt; 200)&gt;&gt;&gt; time.sleep(0.1)&gt;&gt;&gt; tests.append(100 &lt;= timer.elapsed() &lt; 200)&gt;&gt;&gt; tests[True, True]&gt;&gt;&gt;```## Contributing### Obtain source code1. Clone the git repo:    1. `$ git clone git@github.com:brainix/pottery.git`    2. `$ cd pottery/`2. Install project-level dependencies:    1. `$ make install`### Run tests1. In one Terminal session:    1. `$ cd pottery/`    2. `$ redis-server`2. In a second Terminal session:    1. `$ cd pottery/`    2. `$ make test`    3. `$ make test-readme``make test` runs all of the unit tests as well as the coverage test.  However,sometimes, when debugging, it can be useful to run an individual test module,class, or method:1. In one Terminal session:    1. `$ cd pottery/`    2. `$ redis-server`2. In a second Terminal session:    1. Run a test module with `$ make test tests=tests.test_dict`    2. Run a test class with: `$ make test tests=tests.test_dict.DictTests`    3. Run a test method with: `$ make test tests=tests.test_dict.DictTests.test_keyexistserror``make test-readme` doctests the Python code examples in this README to ensurethat they&amp;rsquo;re correct.</longdescription>
</pkgmetadata>