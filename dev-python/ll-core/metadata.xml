<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>**This package is no longer maintained separately. It is now part of ll.xist(http://pypi.python.org/pypi/ll-xist)**.ll-core is a collection of the following modules:*``ansistyle`` can be used for colored terminal output (via ANSIescape sequences).*``color`` provides classes and functions for handling RGB color values.This includes the ability to convert between different color models(RGB, HSV, HLS) as well as to and from CSS format, and several functionsfor modifying and mixing colors.*``make`` is an object oriented make replacement. Like make it allows youto specify dependencies between files and actions to be executedwhen files don't exist or are out of date with respect to oneof their sources. But unlike make you can do this in a object orientedway and targets are not only limited to files, but you can implemente.g. dependencies on database records.*``misc`` provides several small utility functions and classes.*``sisyphus`` provides classes for running Python scripts as cron jobs.*``daemon`` can be used on UNIX to fork a daemon process.*``url`` contains an RFC2396 compliant implementation of URLs and classes foraccessing resource metadata (like modification dates or permission bits) aswell as file like classes for reading data from URLs and writing data to URLs.*``xpit`` is a module that makes it possible to embed Python expressionsin text (as XML style processing instructions).*``xml_codec`` provides a meta codec for decoding XML input.Changes in 1.11.1 (released 01/11/2008)---------------------------------------*Added missing source file ``_xml_codec_include.c`` to the sourcedistributions.</longdescription>
</pkgmetadata>