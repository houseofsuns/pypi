<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>base62Build Status Coveralls PyPIA Python module for base62 encoding. Ported from PHP code that I wrotein mid-2000, which can be foundhere httpphilosophical.onepostsbase62__... Build Status image httpsgithub.comsuminbbase62actionsworkflowsci.ymlbadge.svg   target httpsgithub.comsuminbbase62actionsworkflowsci.ymlquerybranch3Adevelop.. PyPI image httpsimg.shields.iopypivpybase62.svg   target httpspypi.python.orgpypipybase62.. Coveralls image httpscoveralls.ioreposgithubsuminbbase62badge.svgbranchmaster   target httpscoveralls.iogithubsuminbbase62branchdevelopRationale---------When writing a web application, often times we would like to keep the URLsshort.    httplocalhostpostsV1BiicwtThis certainly gives a more concise look than the following.    httplocalhostposts109237591284123This was the original motivation to write this module, but there shall be muchmore broader potential use cases of this module. The main advantage ofbase62 is that it is URL-safe (as opposed to base64) due to the lack ofspecial characters such as  or . Another key aspect is that thealphabetical orders of the original (unencoded) data is preserved when encoded.In other words, encoded data can be sorted without being decoded at all.Installationbase62 can be installed via pypi. Unfortunately, the package namebase62 on pypi had already been occupied by someone else, so we had togo by pybase62.    pip install pybase62Alternatively, you may clone the code to manually install it.    git clone httpsgithub.comsuminbbase62    cd base62  python setup.py installUsageThe following section describes a basic usage of base62... code python     import base62     base62.encode(34441886726)    base62     base62.decode(base62)    34441886726From version 0.2.0, base62 supports bytes array encoding as well... code python     base62.encodebytes(b0)    0     base62.encodebytes(bxffxff)    H31     base62.decodebytes(0)    b     base62.decodebytes(1)    bx01Some may be inclined to assume that they both take bytes types as inputdue to their namings. However, encodebytes() takes bytes typeswhereas decodebytes() takes str types as an input. They are intendedto be commutative, so that a roundtrip between both functions yields theoriginal value.Formally speaking, we say function f and g commute if fg  gf wheref(g(x))  (fg)(x).Therefore, we may expect the following relationships value  encodebytes(decodebytes(value)) value  decodebytes(encodebytes(value))TestsYou may run some test cases to ensure all functionalities are operational.    pytest -vIf pytest is not installed, you may want to run the following command    pip install -r testsrequirements.txtDeploymentDeploy a source package (to pypi httpspypi.org_) as follows    python setup.py sdist upload</longdescription>
</pkgmetadata>