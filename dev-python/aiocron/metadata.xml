<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>aiocron - Crontabs for asyncio.. image httpstravis-ci.orggawelaiocron.svgbranchmaster  target httpstravis-ci.orggawelaiocron.. image httpsimg.shields.iopypivaiocron.svg  target httpspypi.python.orgpypiaiocron.. image httpsimg.shields.iopypidmaiocron.svg  target httpspypi.python.orgpypiaiocronUsageaiocron provide a decorator to run function at time     import aiocron     import asyncio         aiocron.crontab(30    )    ... async def attime()    ...     print(run)    ...     asyncio.get_event_loop().run_forever()You can also use it as an object     aiocron.crontab(1 9   1-5, startFalse)    ... async def attime()    ...     print(run)    ...     attime.start()     asyncio.get_event_loop().run_forever()Your function still be available at attime.funcYou can also await a crontab. In this case, your coroutine can acceptarguments     aiocron.crontab(0 9,10    mon,fri, startFalse)    ... async def attime(i)    ...     print(run i  i)    ...     async def once()    ...     try    ...         res  await attime.next(1)    ...     except Exception as e    ...         print(It failed (r)  e)    ...     else    ...         print(res)    ...     asyncio.get_event_loop().run_forever()Finally you can use it as a sleep coroutine. The following will wait untilnext hour     await crontab(0    ).next()If you dont like the decorator magic you can set the function by yourself     cron  crontab(0    , funcyourcoroutine, startFalse)Notice that unlike standard unix crontab you can specify seconds at the 6thposition.aiocron use croniter httpspypi.python.orgpypicroniter_. Refer toits documentation to know more about crontab format.</longdescription>
</pkgmetadata>