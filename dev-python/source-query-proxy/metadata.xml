<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>source-query-proxy==================Motivation----------Basically Source game-servers works in one thread and can't use more than one core for in-game logic.For example - CS:GO, CS:Source, Left 4 Dead 2, etc.Yes, you can use SourceMod to offload calculations (use threading), but we talking about common game logic.E.g. you can try use `DoS Protection extension &lt;https://forums.alliedmods.net/showpost.php?p=2518787&amp;postcount=117&gt;`_, but caching is not fast solution, cause server spent time to receiving and sending answer from cache.This solution allow redirect some (A2S query) packets to backend and game server don't spent time to answer anymore.IPTables (or any NAT) can't help!---------------------------------If you use IPTables (NAT) to redirect queries to proxy, this rule will be remembered in routing table and if client try to connect - connection will be redirected to proxy too.We use right way to redirect - eBPF: https://github.com/sqproxy/sqredirectPrerequisites-------------Python 3.7 or aboveYou can use `pyenv &lt;https://github.com/pyenv/pyenv&gt;`_ to install any version of Python without root privilegesInstalling----------.. code-block:: bash    pip install source-query-proxy==2.5.0Configuring-----------sqproxy search configs in ``/etc/sqproxy/conf.d`` and ``./conf.d`` directories.You should place your config files only in this directories.For more info see `examples &lt;examples/conf.d&gt;`_Run---.. code-block:: bash    sqproxy runRun with eBPF-------------Please read the instruction and install: https://github.com/sqproxy/sqredirect1. Enable eBPF in config (see ``examples/00-globals.yaml``)2. Run.. code-block:: bash    sqproxy runRun daemonized via systemd--------------------------1. Copy the systemd unit file ``examples/systemd/system/sqproxy.service`` to ``/etc/systemd/system/sqproxy.service`` (Optional: Adjust the ``ExecStart`` path if you have installed sqproxy into a different directory)2. Enable and start the service.. code-block:: bash    systemctl enable --now sqproxy.serviceDevelopment-----------.. code-block:: bash    git clone https://github.com/spumer/source-query-proxy.git    cd source-query-proxy    poetry install    Credits-------Source Engine messages inspired by **Python-valve**https://github.com/serverstf/python-valve</longdescription>
</pkgmetadata>