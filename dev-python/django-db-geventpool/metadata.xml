<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-db-geventpool.. image httpimg.shields.iotravisjneightdjango-db-geventpool.svg   target httpstravis-ci.orgjneightdjango-db-geventpool   alt travis.. image httpsimg.shields.iopypivdjango-db-geventpool.svg   target httpspypi.python.orgpypidjango-db-geventpool   alt pypi version.. image httpimg.shields.iopypildjango-db-geventpool.svg   target httpspypi.python.orgpypidjango-db-geventpool   alt pypi licenseAnother DB pool using gevent for PostgreSQL DB.Python 3 is supported, but if gevent is not installed successfully it will use fallback to eventlet.psycopg2--------django-db-geventpool requires psycopg2 psycopg22.5.1 for CPython 2 and 3 (or psycopg2-binary httpspypi.orgprojectpsycopg2-binary_see notes in the psycopg2 2.7.4 release httpinitd.orgpsycopgarticles20180208psycopg-274-released_) psycopg2cffi2.7 for PyPyPatch psycopg2--------------Before using the pool, psycopg2 must be patched with psycogreen, if you are using gunicorn webserver httpwww.gunicorn.org_,a good place is the post_fork() httpdocs.gunicorn.orgenlatestsettings.htmlpost-fork_ function at the config file.. code python   from psycogreen.gevent import patch_psycopg      use this if you use gevent workers   from psycogreen.eventlet import patch_psycopg    use this if you use eventlet workers   def post_fork(server, worker)       patch_psycopg()       worker.log.info(Made Psycopg2 Green)Settings---------  + Set ENGINE in your database settings to       + django_db_geventpool.backends.postgresql_psycopg2      + For postgis django_db_geventpool.backends.postgis  + Add MAX_CONNS to OPTIONS to set the maximun number of connections allowed to database (default4)  + Add REUSE_CONNS to OPTIONS to indicate how many of the MAX_CONNS should be reused by new requests. Will fallback to the same value as MAX_CONNS if not defined  + Add CONN_MAX_AGE 0 to settings to disable default django persistent connection feature. And read below note if you are manually spawning greenlets .. code python    DATABASES  {        default {            ENGINE django_db_geventpool.backends.postgresql_psycopg2,            NAME db,            USER postgres,            PASSWORD postgres,            HOST ,            PORT ,            ATOMIC_REQUESTS False,            CONN_MAX_AGE 0,            OPTIONS {                MAX_CONNS 20,                REUSE_CONNS 10            }        }    }Using ORM when not serving requests-----------------------------------------------If you are using django with celery (or other), or have code that manually spawn greenlets it will not be sufficient to set CONN_MAX_AGE to 0.Django only checks for long-live connections when finishing a request - So if you manually spawn a greenlet (or task spawning one) its connections willnot get cleaned up and will live until timeout. In production this can cause quite some open connections and while developing it can hamper your tests cases.To solve it make sure that each greenlet function (or task) either sends the django.core.signals.request_finished signal or calls django.db.close_old_connections() right before it endsThe decorator method with your function is preferred, but the other alternatives are also valid.. code python   from django_db_geventpool.utils import close_connection   close_connection   def foo_func()        ...or .. code python   from django.core.signals import request_finished   def foo_func()      ...      request_finished.send(sendergreenlet)or.. code python   from django.db import close_old_connections      def foo_func()      ...      close_old_connections()Other pools------------ django-db-pool httpsgithub.comgmcguiredjango-db-pool_ django-postgresql httpsgithub.comkennethreitzdjango-postgrespool_</longdescription>
</pkgmetadata>