<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>[[Build Status](httpstravis-ci.comjanehmuellerpython-json-config.svgtokentGKCTy4zTZfGNfjpEgEXbranchmaster)](httpstravis-ci.comjanehmuellerpython-json-config)[License](httpsimg.shields.iopypilpython-json-config.svg)[[Version](httpsimg.shields.iopypivpython-json-config.svg)](httpspypi.python.orgpypipython-json-config) OverviewThis library allows to load json configs and access the values like members (i.e., config.server.portinstead of config[server][port]), validate the data types of fields and transform the values of fields. Installingpip install python-json-config Usagefrom python_json_config import ConfigBuilder create config parserbuilder  ConfigBuilder() parse configconfig  builder.parse_config(pathtoconfig.json) access elementshost  config.server.hostport  config.server.portmyfield  config.myfield Validate field typesbuilder.validate_field_type(server.ip, str)builder.validate_field_type(server.port, int)builder.validate_field_type(jwt.access_token_expires, str) Validate field valuesfrom python_json_config.validators import is_unreserved_port, is_ipv4_address, is_timedelta use provided methodsbuilder.validate_field_value(server.ip, is_ipv4_address)builder.validate_field_value(server.port, is_unreserved_port)builder.validate_field_value(jwt.access_token_expires, is_timedelta) use custom validation functionbuilder.validate_field_value(server.ip, lambda ip ip  0.0.0.0) return custom error messages in your lambdabuilder.validate_field_value(server.ip, lambda ip (ip  0.0.0.0, IP is unroutable.)) chain validation functionsbuilder.validate_field_value(server.ip, [lambda ip ip  localhost, lambda ip ip  127.0.0.1]) Transform field valuesfrom python_json_config.transformers import to_timedelta use provided methodsbuilder.transform_field_value(jwt.access_token_expires, to_timedelta)from datetime import datetime parse a timedelta (e.g., Jun 1 2005) into a datetime objectbuilder.transform_field_value(important_date, lambda date datetime.strptime(date, b d Y)) Define field access settings required means an error is thrown if a non-existing field is accessed builder.set_field_access_required() return None for the following fields instead of throwing an errorbuilder.add_optional_field(server.host)builder.add_optional_fields([cache.ttl, server.path]) optional means None is returned if a non-existing field is accessed builder.set_field_access_optional() throw an error for the following fields instead of returning Nonebuilder.add_required_field(server.user)builder.add_required_fields([cache.backend, server.password]) Access config valuesport  config.server.portassert port  1023ip  config.server.ipassert ip not in [0.0.0.0, localhost, 127.0.0.1]important_date  config.important_dateassert isinstance(important_date, datetime)jwt_access_token_expires  config.jwt.access_token_expiresassert isinstance(jwt_access_token_expires, timedelta) Change config valuesconfig  ConfigBuilder().parse_config({server.port 1024})config.add(server.host, localhost)assert config.server.host  localhostconfig.add(cache, redis)assert config.cache  redisconfig.update(server.port, 1025)assert config.server.port  1025config.update(server.user, user, upsertTrue)assert config.server.user  user Overwrite fields with environment variablesFirst, set environment variables (e.g., via bash) MYPROJECT_SERVER_HOSTlocalhost MYPROJECT_CACHEredis MYPYTHONPROJECTS_USERuserEscape underscores in names of variables with another underscore MYPYTHONPROJECTS_LOG__FILEproject.logThen just tell the builder, which prefixes should be mergedbuilder  ConfigBuilder() you can also just pass a single prefix (builder.merge_with_env_variables(MYPROJECT)builder.merge_with_env_variables([MYPROJECT, MYPYTHONPROJECTS])config  builder.parse_config({server.host 0.0.0.0})assert config.server.host  localhostassert config.cache  redisassert config.user  userassert config.log_file  project.logAlternatively you can also do the merging after creating the config objectbuilder  ConfigBuilder()config  builder.parse_config({server.host 0.0.0.0})config.merge_with_env_variables([MYPROJECT, MYPYTHONPROJECTS])assert config.server.host  localhostassert config.cache  redisassert config.user  user SerializationThe config can be serialized to a dictionary, json or binary (via pickle or msgpack).builder  ConfigBuilder()config  builder.parse_config({server.host 0.0.0.0})import picklepickle_config  pickle.loads(pickle.dumps(config))dict_config  builder.parse_config(config.to_dict())import jsonjson_config  builder.parse_config(config.to_json())import msgpackmsgpack_config  Config.from_msgpack(config.to_msgpack())Important note serializing via json or msgpack will stringify any non-serializable value (e.g., datetime objects).</longdescription>
</pkgmetadata>