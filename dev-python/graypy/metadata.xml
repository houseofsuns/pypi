<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>graypy.. image httpsimg.shields.iopypivgraypy.svg    target httpspypi.python.orgpypigraypy    alt PyPI Status.. image httpstravis-ci.orgseverbgraypy.svgbranchmaster    target httpstravis-ci.orgseverbgraypy    alt Build Status.. image httpsreadthedocs.orgprojectsgraypybadgeversionstable    target httpsgraypy.readthedocs.ioenstablebadgestable    alt Documentation Status.. image httpscodecov.ioghseverbgraypybranchmastergraphbadge.svg    target httpscodecov.ioghseverbgraypy    alt Coverage StatusDescriptionPython logging handlers that send log messages in theGraylog Extended Log Format (GELF_).graypy supports sending GELF logs to both Graylog2 and Graylog3 servers.InstallingUsing pip---------Install the basic graypy python logging handlers.. code-block console    pip install graypyInstall with requirements for GELFRabbitHandler.. code-block console    pip install graypy[amqp]Using easy_install------------------Install the basic graypy python logging handlers.. code-block console    easy_install graypyInstall with requirements for GELFRabbitHandler.. code-block console    easy_install graypy[amqp]Usagegraypy sends GELF logs to a Graylog server via subclasses of the pythonlogging.Handler_ class.Below is the list of ready to run GELF logging handlers defined by graypy GELFUDPHandler - UDP log forwarding GELFTCPHandler - TCP log forwarding GELFTLSHandler - TCP log forwarding with TLS support GELFHTTPHandler - HTTP log forwarding GELFRabbitHandler - RabbitMQ log forwardingUDP Logging-----------UDP Log forwarding to a locally hosted Graylog server can be easily done withthe GELFUDPHandler.. code-block python    import logging    import graypy    my_logger  logging.getLogger(test_logger)    my_logger.setLevel(logging.DEBUG)    handler  graypy.GELFUDPHandler(localhost, 12201)    my_logger.addHandler(handler)    my_logger.debug(Hello Graylog.)UDP GELF ChunkersGELF UDP Chunking_ is supported by the GELFUDPHandler and is defined bythe gelf_chunker argument within its constructor. By default theGELFWarningChunker is used, thus, GELF messages that chunk overflow(i.e. consisting of more than 128 chunks) will issue aGELFChunkOverflowWarning and will be dropped.Other gelf_chunker options are also available BaseGELFChunker silently drops GELF messages that chunk overflow GELFTruncatingChunker issues a GELFChunkOverflowWarning and  simplifies and truncates GELF messages that chunk overflow in a attempt  to send some content to Graylog. If this process fails to prevent  another chunk overflow a GELFTruncationFailureWarning is issued.RabbitMQ Logging----------------Alternately, use GELFRabbitHandler to send messages to RabbitMQ andconfigure your Graylog server to consume messages via AMQP. This prevents logmessages from being lost due to dropped UDP packets (GELFUDPHandler sendsmessages to Graylog using UDP). You will need to configure RabbitMQ with agelf_log queue and bind it to the logging.gelf exchange so messagesare properly routed to a queue that can be consumed by Graylog (the queue andexchange names may be customized to your liking)... code-block python    import logging    import graypy    my_logger  logging.getLogger(test_logger)    my_logger.setLevel(logging.DEBUG)    handler  graypy.GELFRabbitHandler(amqpguestguestlocalhost, exchangelogging.gelf)    my_logger.addHandler(handler)    my_logger.debug(Hello Graylog.)Django Logging--------------Its easy to integrate graypy with Djangos logging settings. Just add anew handler in your settings.py.. code-block python    LOGGING  {        version 1,         other dictConfig keys here...        handlers {            graypy {                level WARNING,                class graypy.GELFUDPHandler,                host localhost,                port 12201,            },        },        loggers {            django.request {                handlers [graypy],                level ERROR,                propagate True,            },        },    }Traceback Logging-----------------By default log captured exception tracebacks are added to the GELF log asfull_message fields.. code-block python    import logging    import graypy    my_logger  logging.getLogger(test_logger)    my_logger.setLevel(logging.DEBUG)    handler  graypy.GELFUDPHandler(localhost, 12201)    my_logger.addHandler(handler)    try        puff_the_magic_dragon()    except NameError        my_logger.debug(No dragons here., exc_info1)Default Logging Fields----------------------By default a number of debugging logging fields are automatically added to theGELF log if available     function     pid     process_name     thread_nameYou can disable automatically adding these debugging logging fields byspecifying debugging_fieldsFalse in the handlers constructor.. code-block python    handler  graypy.GELFUDPHandler(localhost, 12201, debugging_fieldsFalse)Adding Custom Logging Fields----------------------------graypy also supports including custom fields in the GELF logs sent to Graylog.This can be done by using Pythons LoggerAdapter_ and Filter_ classes.Using LoggerAdapterLoggerAdapter_ makes it easy to add static information to your GELF logmessages.. code-block python    import logging    import graypy    my_logger  logging.getLogger(test_logger)    my_logger.setLevel(logging.DEBUG)    handler  graypy.GELFUDPHandler(localhost, 12201)    my_logger.addHandler(handler)    my_adapter  logging.LoggerAdapter(logging.getLogger(test_logger),                                       {username John})    my_adapter.debug(Hello Graylog from John.)Using FilterFilter_ gives more flexibility and allows for dynamic information to beadded to your GELF logs.. code-block python    import logging    import graypy    class UsernameFilter(logging.Filter)        def __init__(self)             In an actual use case would dynamically get this             (e.g. from memcache)            self.username  John        def filter(self, record)            record.username  self.username            return True    my_logger  logging.getLogger(test_logger)    my_logger.setLevel(logging.DEBUG)    handler  graypy.GELFUDPHandler(localhost, 12201)    my_logger.addHandler(handler)    my_logger.addFilter(UsernameFilter())    my_logger.debug(Hello Graylog from John.)Contributors   Sever Banesiu   Daniel Miller   Tushar Makkar   Nathan Klapstein.. _GELF httpsdocs.graylog.orgenlatestpagesgelf.html.. _logging.Handler httpsdocs.python.org3librarylogging.htmllogging.Handler.. _GELF UDP Chunking httpsdocs.graylog.orgenlatestpagesgelf.htmlchunking.. _LoggerAdapter httpsdocs.python.orghowtologging-cookbook.htmlusing-loggeradapters-to-impart-contextual-information.. _Filter httpsdocs.python.orghowtologging-cookbook.htmlusing-filters-to-impart-contextual-information</longdescription>
</pkgmetadata>