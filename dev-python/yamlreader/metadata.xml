<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpstravis-ci.orgImmobilienScout24yamlreader.pngbranchmaster   alt Travis build status image   align left   target httpstravis-ci.orgImmobilienScout24yamlreaderyamlreaderMerge YAML data from a directory, a list of files or a file glob. With adirectory, the YAML files (.yaml) are sorted alphabetically. The YAMLfiles are expected to contain a complex key-value structure and mergedwith the following rules lists get appended hashes get merged by key scalars (numbers, strings) are overwritten everything else will failThe purpose is to allow several YAML files instead of a single YAML file. Weuse it to help our software read configuration data from an arbitrary amountof YAML files instead of a single YAML file.Read the unit test to see some examples.Building and InstallationUsing pip---------yamlreader is available with pip    pip install yamlreaderManual build and installation-----------------------------If you want to make changes or use e.g. fpm for packaging this, you need toprepare the development environment to make further steps.Prepare the source    git clone httpsgithub.comImmobilienScout24yamlreader.git    cd yamlreader    virtualenv venv    source venvbinactivate    pip install pybuilder    pyb install_dependenciesRunning tests    pyb verifyGenerating a setup.py    pyb    cd targetdistyamlreader-VERSION    .setup.py whatever you wantRunningThe package installs a command line script yamlreader that can be used toread one or many YAML files and dump the merge result as a YAML document.Use it in your softwareWherever you had been using the safe_load function ofPyYAML httppyyaml.org_ to read a single YAML file you can usethe yamlreader.yaml_load function as a replacement to read all .yamlfiles in a directory    from yamlreader import yaml_load    defaultconfig  {            loglevel  error,            some  value    }    config  yaml_load(etcmyapp, defaultconfig)yaml_load---------    def yaml_load(source,defaultdataNone)        merge YAML data from files found in source        Always returns a dict. The YAML files are expected to contain some kind of        keyvalue structures, possibly deeply nested. When merging, lists are        appended and dict keys are replaced. The YAML files are read with the        yaml.safe_load function.        source can be a file, a dir, a listtuple of files or a string containing        a glob expression (with []).        For a dir all .yaml files will be read in alphabetical order.        defaultdata can be used to initialize the data.        </longdescription>
</pkgmetadata>