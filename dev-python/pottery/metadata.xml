<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Pottery Redis for Humans [Redis](httpredis.io) is awesome, but [Rediscommands](httpredis.iocommands) are not always intuitive.  Pottery is aPythonic way to access Redis.  If you know how to use Python dicts, then youalready know how to use Pottery.  Pottery is useful for accessing Redis moreeasily, and also for implementing microservice resilience patterns and it hasbeen battle tested in production at scale.[[Build status](httpsimg.shields.iogithubworkflowstatusbrainixpotteryPython20packagemaster)](httpsgithub.combrainixpotteryactionsquerybranch3Amaster)[[Security status](httpsimg.shields.iobadgesecurity-bandit-dark.svg)](httpsgithub.comPyCQAbandit)[[Latest released version](httpsbadge.fury.iopypottery.svg)](httpsbadge.fury.iopypottery)[Supported Python versions](httpsimg.shields.iopypipyversionspottery)[Number of lines of code](httpsimg.shields.iotokeilinesgithubbrainixpottery)[[Total number of downloads](httpspepy.techbadgepottery)](httpspepy.techprojectpottery)[[Downloads per month](httpspepy.techbadgepotterymonth)](httpspepy.techprojectpottery)[[Downloads per week](httpspepy.techbadgepotteryweek)](httpspepy.techprojectpottery) Table of Contents- [Dicts ](dicts)- [Sets ](sets)- [Lists ](lists)- [Counters ](counters)- [Deques ](deques)- [Queues ](queues)- [Redlock ](redlock)    - [synchronize() ](synchronize)- [NextId ](nextid)- [redis_cache()](redis_cache)- [CachedOrderedDict](cachedordereddict)- [Bloom filters ](bloom-filters)- [HyperLogLogs ](hyperloglogs)- [ContextTimer ](contexttimer) Installationshell pip3 install pottery UsageFirst, set up your Redis clientpython from redis import Redis redis  Redis.from_url(redislocalhost63791) a namedictsaDicts RedisDict is a Redis-backed container compatible with Pythonrsquos[dict](httpsdocs.python.org3tutorialdatastructures.htmldictionaries).Here is a small example using a RedisDictpython from pottery import RedisDict tel  RedisDict({jack 4098, sape 4139}, redisredis, keytel) tel[guido]  4127 telRedisDict{jack 4098, sape 4139, guido 4127} tel[jack]4098 del tel[sape] tel[irv]  4127 telRedisDict{jack 4098, guido 4127, irv 4127} list(tel)[jack, guido, irv] sorted(tel)[guido, irv, jack] guido in telTrue jack not in telFalseNotice the first two keyword arguments to RedisDict()  The first is yourRedis client.  The second is the Redis key name for your dict.  Other thanthat, you can use your RedisDict the same way that you use any other Pythondict.Limitations1. Keys and values must be JSON serializable. a namesetsaSets RedisSet is a Redis-backed container compatible with Pythonrsquos[set](httpsdocs.python.org3tutorialdatastructures.htmlsets).Here is a brief demonstrationpython from pottery import RedisSet basket  RedisSet({apple, orange, apple, pear, orange, banana}, redisredis, keybasket) sorted(basket)[apple, banana, orange, pear] orange in basketTrue crabgrass in basketFalse a  RedisSet(abracadabra, redisredis, keymagic) b  set(alacazam) sorted(a)[a, b, c, d, r] sorted(a - b)[b, d, r] sorted(a  b)[a, b, c, d, l, m, r, z] sorted(a  b)[a, c] sorted(a  b)[b, d, l, m, r, z]Notice the two keyword arguments to RedisSet()  The first is your Redisclient.  The second is the Redis key name for your set.  Other than that, youcan use your RedisSet the same way that you use any other Python set.Do more efficient membership testing for multiple elements using.contains_many()python nirvana  RedisSet({kurt, krist, dave}, redisredis, keynirvana) tuple(nirvana.contains_many(kurt, krist, chat, dave))(True, True, False, True)Limitations1. Elements must be JSON serializable. a namelistsaLists RedisList is a Redis-backed container compatible with Pythonrsquos[list](httpsdocs.python.org3tutorialintroduction.htmllists).python from pottery import RedisList squares  RedisList([1, 4, 9, 16, 25], redisredis, keysquares) squaresRedisList[1, 4, 9, 16, 25] squares[0]1 squares[-1]25 squares[-3][9, 16, 25] squares[][1, 4, 9, 16, 25] squares + [36, 49, 64, 81, 100]RedisList[1, 4, 9, 16, 25, 36, 49, 64, 81, 100]Notice the two keyword arguments to RedisList()  The first is your Redisclient.  The second is the Redis key name for your list.  Other than that, youcan use your RedisList the same way that you use any other Python list.Limitations1. Elements must be JSON serializable.2. Under the hood, Python implements list using an array.  Redis implements   list using a   [doubly linked list](httpsredis.iotopicsdata-types-introredis-lists).   As such, inserting elements at the head or tail of a RedisList is fast,   O(1).  However, accessing RedisList elements by index is slow, O(n).  So   in terms of performance and ideal use cases, RedisList is more similar to   Pythonrsquos deque than Pythonrsquos list.  Instead of RedisList,   consider using [RedisDeque](deques). a namecountersaCounters RedisCounter is a Redis-backed container compatible with Pythonrsquos[collections.Counter](httpsdocs.python.org3librarycollections.htmlcollections.Counter).python from pottery import RedisCounter c  RedisCounter(redisredis, keymy-counter) c  RedisCounter(gallahad, redisredis, keymy-counter) c.clear() c  RedisCounter({red 4, blue 2}, redisredis, keymy-counter) c.clear() c  RedisCounter(redisredis, keymy-counter, cats4, dogs8) c.clear() c  RedisCounter([eggs, ham], redisredis, keymy-counter) c[bacon]0 c[sausage]  0 del c[sausage] c.clear() c  RedisCounter(redisredis, keymy-counter, a4, b2, c0, d-2) sorted(c.elements())[a, a, a, a, b, b] c.clear() RedisCounter(abracadabra, redisredis, keymy-counter).most_common(3)[(a, 5), (b, 2), (r, 2)] c.clear() c  RedisCounter(redisredis, keymy-counter, a4, b2, c0, d-2) from collections import Counter d  Counter(a1, b2, c3, d4) c.subtract(d) cRedisCounter{a 3, b 0, c -3, d -6}Notice the first two keyword arguments to RedisCounter()  The first is yourRedis client.  The second is the Redis key name for your counter.  Other thanthat, you can use your RedisCounter the same way that you use any otherPython Counter.Limitations1. Keys must be JSON serializable. a namedequesaDeques RedisDeque is a Redis-backed container compatible with Pythonrsquos[collections.deque](httpsdocs.python.org3librarycollections.htmlcollections.deque).Examplepython from pottery import RedisDeque d  RedisDeque(ghi, redisredis, keyletters) for elem in d...     print(elem.upper())GHI d.append(j) d.appendleft(f) dRedisDeque([f, g, h, i, j]) d.pop()j d.popleft()f list(d)[g, h, i] d[0]g d[-1]i list(reversed(d))[i, h, g] h in dTrue d.extend(jkl) dRedisDeque([g, h, i, j, k, l]) d.rotate(1) dRedisDeque([l, g, h, i, j, k]) d.rotate(-1) dRedisDeque([g, h, i, j, k, l]) RedisDeque(reversed(d), redisredis)RedisDeque([l, k, j, i, h, g]) d.clear() d.extendleft(abc) dRedisDeque([c, b, a])Notice the two keyword arguments to RedisDeque()  The first is your Redisclient.  The second is the Redis key name for your deque.  Other than that, youcan use your RedisDeque the same way that you use any other Python deque.Limitations1. Elements must be JSON serializable. a namequeuesaQueues RedisSimpleQueue is a Redis-backed multi-producer, multi-consumer FIFO queuecompatible with Pythonrsquos[queue.SimpleQueue](httpsdocs.python.org3libraryqueue.htmlsimplequeue-objects).In general, use a Python queue.Queue if yoursquore using it in one or morethreads, use multiprocessing.Queue if yoursquore using it between processes,and use RedisSimpleQueue if yoursquore sharing it across machines or if youneed for your queue to persist across application crashes or restarts.Instantiate a RedisSimpleQueuepython from pottery import RedisSimpleQueue cars  RedisSimpleQueue(redisredis, keycars)Notice the two keyword arguments to RedisSimpleQueue()  The first is yourRedis client.  The second is the Redis key name for your queue.  Other thanthat, you can use your RedisSimpleQueue the same way that you use any otherPython queue.SimpleQueue.Check the queue state, put some items in the queue, and get those items backoutpython cars.empty()True cars.qsize()0 cars.put(Jeep) cars.put(Honda) cars.put(Audi) cars.empty()False cars.qsize()3 cars.get()Jeep cars.get()Honda cars.get()Audi cars.empty()True cars.qsize()0Limitations1. Items must be JSON serializable. a nameredlockaRedlock Redlock is a safe and reliable lock to coordinate access to a resource sharedacross threads, processes, and even machines, without a single point offailure.  [Rationale and algorithmdescription.](httpredis.iotopicsdistlock)Redlock implements Pythonrsquos excellent[threading.Lock](httpsdocs.python.org3librarythreading.htmllock-objects)API as closely as is feasible.  In other words, you can use Redlock the sameway that you use threading.Lock.  The main reason to use Redlock overthreading.Lock is that Redlock can coordinate access to a resource sharedacross different machines threading.Lock canrsquot.Instantiate a Redlockpython from pottery import Redlock printer_lock  Redlock(keyprinter, masters{redis})The key argument represents the resource, and the masters argumentspecifies your Redis masters across which to distribute the lock.  Inproduction, you should have 5 Redis masters.  This is to eliminate a singlepoint of failure mdash you can lose up to 2 out of the 5 Redis masters andyour Redlock will remain available and performant.  Now you can protectaccess to your resourcepython if printer_lock.acquire()...     print(printer_lock is locked)...      Critical section - print stuff here....     printer_lock.release()printer_lock is locked bool(printer_lock.locked())FalseOr you can protect access to your resource inside a context managerpython with printer_lock...     print(printer_lock is locked)...      Critical section - print stuff here.printer_lock is locked bool(printer_lock.locked())FalseItrsquos safest to instantiate a new Redlock object every time you need toprotect your resource and to not share Redlock instances across differentparts of code.  In other words, think of the key as identifying the resourcedonrsquot think of any particular Redlock as identifying the resource.Instantiating a new Redlock every time you need a lock sidesteps bugs bydecoupling how you use Redlock from the forkingthreading model of yourapplicationservice.Redlocks are automatically released (by default, after 10 seconds).  Youshould take care to ensure that your critical section completes well withinthat timeout.  The reasons that Redlocks are automatically released are topreserve[ldquolivenessrdquo](httpredis.iotopicsdistlockliveness-arguments)and to avoid deadlocks (in the event that a process dies inside a criticalsection before it releases its lock).python import time if printer_lock.acquire()...      Critical section - print stuff here....     time.sleep(10) bool(printer_lock.locked())FalseIf 10 seconds isnrsquot enough to complete executing your critical section,then you can specify your own auto release time (in seconds)python printer_lock  Redlock(keyprinter, masters{redis}, auto_release_time15) if printer_lock.acquire()...      Critical section - print stuff here....     time.sleep(10) bool(printer_lock.locked())True time.sleep(5) bool(printer_lock.locked())FalseBy default, .acquire() blocks indefinitely until the lock is acquired.  Youcan make .acquire() return immediately with the blocking argument..acquire() returns True if the lock was acquired False if not.python printer_lock_1  Redlock(keyprinter, masters{redis}) printer_lock_2  Redlock(keyprinter, masters{redis}) printer_lock_1.acquire(blockingFalse)True printer_lock_2.acquire(blockingFalse)   Returns immediately.False printer_lock_1.release()You can make .acquire() block but not indefinitely by specifying thetimeout argument (in seconds)python printer_lock_1.acquire(timeout1)True printer_lock_2.acquire(timeout1)   Waits 1 second.False printer_lock_1.release()You can similarly configure the Redlock context managerrsquosblockingtimeout behavior during Redlock initialization.  If the contextmanager fails to acquire the lock, it raises the QuorumNotAchieved exception.python import contextlib from pottery import QuorumNotAchieved printer_lock_1  Redlock(keyprinter, masters{redis}, context_manager_blockingTrue, context_manager_timeout0.2) printer_lock_2  Redlock(keyprinter, masters{redis}, context_manager_blockingTrue, context_manager_timeout0.2) with printer_lock_1...     with contextlib.suppress(QuorumNotAchieved)...         with printer_lock_2   Waits 0.2 seconds raises QuorumNotAchieved....             pass...     print(fprinter_lock_1 is {locked if printer_lock_1.locked() else unlocked})...     print(fprinter_lock_2 is {locked if printer_lock_2.locked() else unlocked})printer_lock_1 is lockedprinter_lock_2 is unlocked a namesynchronizeasynchronize() synchronize() is a decorator that allows only one thread to execute afunction at a time.  Under the hood, synchronize() uses a Redlock, so referto the [Redlock documentation](redlock) for more details.Herersquos how to use synchronize()python from pottery import synchronize synchronize(keysynchronized-func, masters{redis}, auto_release_time.5, blockingTrue, timeout-1)... def func()...    Only one thread can execute this function at a time....   return True... a namenextidaNextId NextId safely and reliably produces increasing IDs across threads, processes,and even machines, without a single point of failure.  [Rationale and algorithmdescription.](httpantirez.comnews102)Instantiate an ID generatorpython from pottery import NextId tweet_ids  NextId(keytweet-ids, masters{redis})The key argument represents the sequence (so that you can have differentsequences for user IDs, comment IDs, etc.), and the masters argumentspecifies your Redis masters across which to distribute ID generation (inproduction, you should have 5 Redis masters).  Now, whenever you need a userID, call next() on the ID generatorpython next(tweet_ids)1 next(tweet_ids)2 next(tweet_ids)3Two caveats1. If many clients are generating IDs concurrently, then there may be   ldquoholesrdquo in the sequence of IDs (e.g. 1, 2, 6, 10, 11, 21,   hellip).2. This algorithm scales to about 5,000 IDs per second (with 5 Redis masters).   If you need IDs faster than that, then you may want to consider other   techniques. redis_cache()redis_cache() is a simple lightweight unbounded function return value cache,sometimes called[ldquomemoizerdquo](httpsen.wikipedia.orgwikiMemoization).redis_cache() implements Pythonrsquos excellent[functools.cache()](httpsdocs.python.org3libraryfunctools.htmlfunctools.cache)API as closely as is feasible.  In other words, you can use redis_cache() thesame way that you use functools.cache().Limitations1. Arguments to the function must be hashable.2. Return values from the function must be JSON serializable.3. Just like functools.cache(), redis_cache() does not allow for a maximum   size, and does not evict old values, and grows unbounded.  Only use   redis_cache() in one of these cases    1. Your functionrsquos argument space has a known small cardinality.    2. You specify a timeout when calling redis_cache() to decorate your       function, to dump your _entire_ return value cache timeout seconds       after the last cache access (hit or miss).    3. You periodically call .cache_clear() to dump your _entire_ return       value cache.    4. Yoursquore ok with your return value cache growing unbounded, and you       [understand the implications](httpsdocs.redislabs.comlatestrsadministeringdatabase-operationseviction-policy)       of this for your underlying Redis instance.In general, you should only use redis_cache() when you want to reusepreviously computed values.  Accordingly, it doesnrsquot make sense to cachefunctions with side-effects or impure functions such as time() or random().Decorate a functionpython import time from pottery import redis_cache redis_cache(redisredis, keyexpensive-function-cache)... def expensive_function(n)...     time.sleep(1)   Simulate an expensive computation or database lookup....     return n...Notice the two keyword arguments to redis_cache() The first is your Redisclient.  The second is the Redis key name for your functionrsquos returnvalue cache.Call your function and observe the cache hitmiss ratespython expensive_function(5)5 expensive_function.cache_info()CacheInfo(hits0, misses1, maxsizeNone, currsize1) expensive_function(5)5 expensive_function.cache_info()CacheInfo(hits1, misses1, maxsizeNone, currsize1) expensive_function(6)6 expensive_function.cache_info()CacheInfo(hits1, misses2, maxsizeNone, currsize2)Notice that the first call to expensive_function() takes 1 second and resultsin a cache miss but the second call returns almost immediately and results ina cache hit.  This is because after the first call, redis_cache() cached thereturn value for the call when n  5.You can access your original undecorated underlying expensive_function() asexpensive_function.__wrapped__.  This is useful for introspection, forbypassing the cache, or for rewrapping the original function with a differentcache.You can force a cache reset for a particular combination of argskwargswith expensive_function.__bypass__.  A call toexpensive_function.__bypass__(args, kwargs) bypasses the cache lookup,calls the original underlying function, then caches the results for futurecalls to expensive_function(args, kwargs).  Note that a call toexpensive_function.__bypass__(args, kwargs) results in neither a cache hitnor a cache miss.Finally, clearinvalidate your functionrsquos entire return value cache withexpensive_function.cache_clear()python expensive_function.cache_info()CacheInfo(hits1, misses2, maxsizeNone, currsize2) expensive_function.cache_clear() expensive_function.cache_info()CacheInfo(hits0, misses0, maxsizeNone, currsize0) CachedOrderedDictThe best way that I can explain CachedOrderedDict is through an exampleuse-case.  Imagine that your search engine returns document IDs, which then youhave to hydrate into full documents via the database to return to the client.The data structure used to represent such search results must have thefollowing properties1. It must preserve the order of the document IDs returned by the search engine.2. It must map document IDs to hydrated documents.3. It must cache previously hydrated documents.Properties 1 and 2 are satisfied by Pythonrsquos[collections.OrderedDict](httpsdocs.python.org3librarycollections.htmlcollections.OrderedDict).However, CachedOrderedDict extends Pythonrsquos OrderedDict to alsosatisfy property 3.The most common usage pattern for CachedOrderedDict is as follows1. Instantiate CachedOrderedDict with the IDs that you must look up or   compute passed in as the dict_keys argument to the initializer.2. Compute and store the cache misses for future lookups.3. Return some representation of your CachedOrderedDict to the client.Instantiate a CachedOrderedDictpython from pottery import CachedOrderedDict search_results_1  CachedOrderedDict(...     redis_clientredis,...     redis_keysearch-results,...     dict_keys(1, 2, 3, 4, 5),... )The redis_client argument to the initializer is your Redis client, and theredis_key argument is the Redis key for the Redis Hash backing your cache.The dict_keys argument represents an ordered iterable of keys to be looked upand automatically populated in your CachedOrderedDict (on cache hits), orthat yoursquoll have to compute and populate for future lookups (on cachemisses).  Regardless of whether keys are cache hits or misses,CachedOrderedDict preserves the order of dict_keys (like a list), mapsthose keys to values (like a dict), and maintains an underlying cache forfuture key lookups.In the beginning, the cache is empty, so letrsquos populate itpython sorted(search_results_1.misses())[1, 2, 3, 4, 5] search_results_1[1]  one search_results_1[2]  two search_results_1[3]  three search_results_1[4]  four search_results_1[5]  five sorted(search_results_1.misses())[]Note that CachedOrderedDict preserves the order of dict_keyspython for key, value in search_results_1.items()...     print(f{key} {value})1 one2 two3 three4 four5 fiveNow, letrsquos look at a combination of cache hits and missespython search_results_2  CachedOrderedDict(...     redis_clientredis,...     redis_keysearch-results,...     dict_keys(2, 4, 6, 8, 10),... ) sorted(search_results_2.misses())[6, 8, 10] search_results_2[2]two search_results_2[6]  six search_results_2[8]  eight search_results_2[10]  ten sorted(search_results_2.misses())[] for key, value in search_results_2.items()...     print(f{key} {value})2 two4 four6 six8 eight10 tenLimitations1. Keys and values must be JSON serializable. a namebloom-filtersaBloom filters Bloom filters are a powerful data structure that help you to answer thequestions, _ldquoHave I seen this element beforerdquo_ and _ldquoHowmany distinct elements have I seenrdquo_ but not the question, _ldquoWhatare all of the elements that Irsquove seen beforerdquo_  So think of Bloomfilters as Python sets that you can add elements to, use to test elementmembership, and get the length of but that you canrsquot iterate through orget elements back out of.Bloom filters are probabilistic, which means that they can sometimes generatefalse positives (as in, they may report that yoursquove seen a particularelement before even though you havenrsquot).  But they will never generatefalse negatives (so every time that they report that you havenrsquot seen aparticular element before, you really must never have seen it).  You can tuneyour acceptable false positive probability, though at the expense of thestorage size and the element insertionlookup time of your Bloom filter.Create a BloomFilterpython from pottery import BloomFilter dilberts  BloomFilter(...     num_elements100,...     false_positives0.01,...     redisredis,...     keydilberts,... )Here, num_elements represents the number of elements that you expect toinsert into your BloomFilter, and false_positives represents youracceptable false positive probability.  Using these two parameters,BloomFilter automatically computes its own storage size and number of timesto run its hash functions on element insertionlookup such that it canguarantee a false positive rate at or below what you can tolerate, given thatyoursquore going to insert your specified number of elements.Insert an element into the BloomFilterpython dilberts.add(rajiv)Test for membership in the BloomFilterpython rajiv in dilbertsTrue raj in dilbertsFalse dan in dilbertsFalseSee how many elements wersquove inserted into the BloomFilterpython len(dilberts)1Note that BloomFilter.__len__() is an approximation, not an exact value,though itrsquos quite accurate.Insert multiple elements into the BloomFilterpython dilberts.update({raj, dan})Do more efficient membership testing for multiple elements using.contains_many()python tuple(dilberts.contains_many(rajiv, raj, dan, luis))(True, True, True, False)Remove all of the elements from the BloomFilterpython dilberts.clear() len(dilberts)0Limitations1. Elements must be JSON serializable.2. len(bf) is probabilistic in that itrsquos an accurate approximation.  You   can tune how accurate you want it to be with the num_elements and   false_positives arguments to .__init__(), at the expense of storage space   and insertionlookup time.3. Membership testing against a Bloom filter is probabilistic in that it may   return false positives, but never returns false negatives.  This means that   if element in bf evaluates to True, then you may have inserted the   element into the Bloom filter.  But if element in bf evaluates to False,   then you must not have inserted it.  Again, you can tune accuracy with the   num_elements and false_positives arguments to .__init__(), at the   expense of storage space and insertionlookup time. a namehyperloglogsaHyperLogLogs HyperLogLogs are an interesting data structure designed to answer the question,_ldquoHow many distinct elements have I seenrdquo_ but not the questions,_ldquoHave I seen this element beforerdquo_ or _ldquoWhat are all of theelements that Irsquove seen beforerdquo_  So think of HyperLogLogs asPython sets that you can add elements to and get the length of but that youcanrsquot use to test element membership, iterate through, or get elementsout of.HyperLogLogs are probabilistic, which means that theyrsquore accurate withina margin of error up to 2.  However, they can reasonably accurately estimatethe cardinality (size) of vast datasets (like the number of unique Googlesearches issued in a day) with a tiny amount of storage (1.5 KB).Create a HyperLogLogpython from pottery import HyperLogLog google_searches  HyperLogLog(redisredis, keygoogle-searches)Insert an element into the HyperLogLogpython google_searches.add(sonic the hedgehog video game)See how many elements wersquove inserted into the HyperLogLogpython len(google_searches)1Insert multiple elements into the HyperLogLogpython google_searches.update({...     google in 1998,...     minesweeper,...     joey tribbiani,...     wizard of oz,...     rgb to hex,...     pac-man,...     breathing exercise,...     do a barrel roll,...     snake,... }) len(google_searches)10Through a clever hack, we can do membership testing against a HyperLogLog,even though it was never designed for this purpose.  The way that the hack worksis that it creates a temporary copy of the HyperLogLog, then inserts theelement that yoursquore running the membership test for into the temporarycopy.  If the insertion changes the temporary HyperLogLogrsquos cardinality,then the element must not have been inserted into the original HyperLogLog.python joey tribbiani in google_searchesTrue jennifer aniston in google_searchesFalseDo more efficient membership testing for multiple elements using.contains_many()python tuple(google_searches.contains_many(joey tribbiani, jennifer aniston))(True, False)Remove all of the elements from the HyperLogLogpython google_searches.clear() len(google_searches)0Limitations1. Elements must be JSON serializable.2. len(hll) is probabilistic in that itrsquos an accurate approximation.3. Membership testing against a HyperLogLog is probabilistic in that it may   return false positives, but never returns false negatives.  This means that   if element in hll evaluates to True, then you may have inserted the   element into the HyperLogLog.  But if element in hll evaluates to False,   then you must not have inserted it. a namecontexttimeraContextTimer ContextTimer helps you easily and accurately measure elapsed time.  Note thatContextTimer measures wall (real-world) time, not CPU time and thatelapsed() returns time in milliseconds.You can use ContextTimer stand-alonehellippython import time from pottery import ContextTimer timer  ContextTimer() timer.start() time.sleep(0.1) 100  timer.elapsed()  200True timer.stop() time.sleep(0.1) 100  timer.elapsed()  200Truehellipor as a context managerpython tests  [] with ContextTimer() as timer...     time.sleep(0.1)...     tests.append(100  timer.elapsed()  200) time.sleep(0.1) tests.append(100  timer.elapsed()  200) tests[True, True] Contributing Obtain source code1. Clone the git repo    1.  git clone gitgithub.combrainixpottery.git    2.  cd pottery2. Install project-level dependencies    1.  make install Run tests1. In one Terminal session    1.  cd pottery    2.  redis-server2. In a second Terminal session    1.  cd pottery    2.  make test    3.  make test-readmemake test runs all of the unit tests as well as the coverage test.  However,sometimes, when debugging, it can be useful to run an individual test module,class, or method1. In one Terminal session    1.  cd pottery    2.  redis-server2. In a second Terminal session    1. Run a test module with  make test teststests.test_dict    2. Run a test class with  make test teststests.test_dict.DictTests    3. Run a test method with  make test teststests.test_dict.DictTests.test_keyexistserrormake test-readme doctests the Python code examples in this README to ensurethat theyrsquore correct.</longdescription>
</pkgmetadata>