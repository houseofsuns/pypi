<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>simpleeval (Simple Eval).. image httpsgithub.comdanthedeckiesimpleevalactionsworkflowsci.ymlbadge.svgbranchgh-actions-build   target httpsgithub.comdanthedeckiesimpleevalactions   alt Build Status.. image httpscodecov.ioghdanthedeckiesimpleevalbranchmastergraphbadge.svgtokenisRnN1yrca   target httpscodecov.ioghdanthedeckiesimpleeval   alt Code Coverage.. image httpsbadge.fury.iopysimpleeval.svg   target httpsbadge.fury.iopysimpleeval   alt PyPI Version.. image httpsimg.shields.iobadgecode20style-black-000000.svg   target httpsgithub.compsfblack.. image httpsimg.shields.iobadgelinting-pylint-yellowgreen   target httpsgithub.comPyCQApylintA single file library for easily adding evaluatable expressions intopython projects.  Say you want to allow a user to set an alarm volume, whichcould depend on the time of day, alarm level, how many previous alarms had goneoff, and if there is music playing at the time.Or if you want to allow simple formulae in a web application, but dont want togive full eval() access, or dont want to run in javascript on the client side.Its deliberately trying to stay simple to use and not have millions of features,pull it in from PyPI (pip or easy_install), or even just a single file you can dumpinto a project.Internally, its using the amazing python ast module to parse theexpression, which allows very fine control of what is and isnt allowed.  Itshould be completely safe in terms of what operations can be performed by theexpression.The only issue I know to be aware of is that you can create an expression whichtakes a long time to evaluate, or which evaluating requires an awful lot ofmemory, which leaves the potential for DOS attacks.  There is basic protectionagainst this, and you can lock it down further if you desire. (see theOperators_ section below)You should be aware of this when deploying in a public setting.The defaults are pretty locked down and basic, and its easy to addwhatever extra specific functionality you need (your own functions,variablename lookup, etc).Basic Usage-----------To get very simple evaluating.. code-block python    from simpleeval import simple_eval    simple_eval(21 + 21)returns 42.Expressions can be as complex and convoluted as you want.. code-block python    simple_eval(21 + 19  7 + (8  3)  9)returns 535.714285714.You can add your own functions in as well... code-block python    simple_eval(square(11), functions{square lambda x xx})returns 121.For more details of working with functions, read further down.Noteall further examples use  to designate python code, as if you are usingthe python interactive prompt... _OperatorsOperators---------You can add operators yourself, using the operators argument, but these arethe defaults+--------+------------------------------------+  +  add two things. x + y                   1 + 1 - 2                 +--------+------------------------------------+  -  subtract two things x - y               100 - 1 - 99              +--------+------------------------------------+    divide one thing by another                 x  y                                   10010 - 10               +--------+------------------------------------+    multiple one thing by another               x  y                                   10  10 - 100             +--------+------------------------------------+   to the power of xy                  2  10 - 1024            +--------+------------------------------------+    modulus. (remainder)  x  y             15  4 - 3                +--------+------------------------------------+   equals  x  y                          15  4 - False           +--------+------------------------------------+    Less than. x  y                        1  4 - True              +--------+------------------------------------+    Greater than. x  y                     1  4 - False             +--------+------------------------------------+   Less than or Equal to. x  y           1  4 - True              +--------+------------------------------------+   Greater or Equal to x  21             1  4 - False            +--------+------------------------------------+   Right shift the number.                   100  2 - 25             +--------+------------------------------------+   Left shift the number.                    100  2 - 400            +--------+------------------------------------+ in  is something contained within               something else.                             spam in my breakfast                - False                       +--------+------------------------------------+    bitwise exclusive OR (xor)                62  20 - 42              +--------+------------------------------------+    bitwise OR                                8  34 - 42               +--------+------------------------------------+    bitwise AND                               100  63 - 36             +--------+------------------------------------+    bitwise invert                             -43 - 42                +--------+------------------------------------+The  operator is often mistaken for a exponent operator, not the bitwise operation that it is in python, so if you want 3  2 to equal 9, you canreplace the operator like this.. code-block python     import ast     from simpleeval import safe_power     s  SimpleEval()     s.operators[ast.BitXor]  safe_power     s.eval(3  2)    9for example.Limited PowerAlso note, the  operator has been locked down by default to have amaximum input value of 4000000, which makes it somewhat harder to makeexpressions which go on for ever.  You can change this limit by changing thesimpleeval.POWER_MAX module level value to whatever is an appropriate valuefor you (and the hardware that youre running on) or if you want to completelyremove all limitations, you can set the s.operators[ast.Pow]  operator.powor make your own function.On my computer, 995 evaluates almost instantly, but 996 takesover 30 seconds.  Since 97 is 4782969, and so over the POWER_MAXlimit, it throws a NumberTooHigh exception for you. (Otherwise it would goon for hours, or until the computer runs out of memory)Strings (and other Iterables) SafetyThere are also limits on string length (100000 characters,MAX_STRING_LENGTH).  This can be changed if you wish.Related to this, if you try to create a silly long stringbyteslist, by doingi want to break free.split()  9999999999 for instance, it will block you.If Expressions--------------You can use python style if x then y else z type expressions.. code-block python     simple_eval(equal if x  y else not equal,                    names{x 1, y 2})    not equalwhich, of course, can be nested.. code-block python     simple_eval(a if 1  2 else b if 2  3 else c)    cFunctions---------You can define functions which youd like the expresssions to have access to.. code-block python     simple_eval(double(21), functions{double lambda xx2})    42You can define real functions to pass in rather than lambdas, of course too,and even re-name them so that expressions can be shorter.. code-block python     def double(x)            return x  2     simple_eval(d(100) + double(1), functions{d double, doubledouble})    202If you dont provide your own functions dict, then the the following defaultsare provided in the DEFAULT_FUNCTIONS dict+----------------+--------------------------------------------------+ randint(x)  Return a random int below x              +----------------+--------------------------------------------------+ rand()      Return a random float between 0 and 1        +----------------+--------------------------------------------------+ int(x)      Convert x to an int.                     +----------------+--------------------------------------------------+ float(x)    Convert x to a float.                    +----------------+--------------------------------------------------+ str(x)      Convert x to a str (unicode in py2)  +----------------+--------------------------------------------------+If you want to provide a list of functions, but want to keep these as well,then you can do a normal python .copy()  .update.. code-block python     my_functions  simpleeval.DEFAULT_FUNCTIONS.copy()     my_functions.update(            square(lambda xxx),            double(lambda xx+x),        )     simple_eval(square(randint(100)), functionsmy_functions)Names-----Sometimes its useful to have variables available, which in python terminologyare called names... code-block python     simple_eval(a + b, names{a 11, b 100})    111You can also hand the handling of names over to a function, if you prefer.. code-block python     def name_handler(node)            return ord(node.id[0].lower(a))-96     simple_eval(a + b, namesname_handler)    3That was a bit of a silly example, but you could use this for pulling valuesfrom a database or file, say, or doing some kind of caching system.The two default names that are provided are True and False.  So if you want to provide your own names, but want True and False to keep working, either provide them yourself, or .copy() and .update the DEFAULT_NAMES. (See functions example above).Creating an Evaluator Class---------------------------Rather than creating a new evaluator each time, if you are doing a lot ofevaluations, you can create a SimpleEval object, and pass it expressions eachtime (which should be a bit quicker, and certainly more convenient for some usecases).. code-block python     s  SimpleEval()     s.eval(1 + 1)    2     s.eval(100  10)    1000     and so on...One useful feature of using the SimpleEval object is that you can parse an expressiononce, and then evaluate it mulitple times using different names.. code-block python     Set up  Cache the parse tree    expression  foo + bar    parsed  s.parse(expression)     evaluate the expression multiple times    for names in [{foo 1, bar 10}, {foo 100, bar 42}]        s.names  names        print(s.eval(expression, previously_parsedparsed))for instance.  This may help with performance.You can assign  edit the various options of the SimpleEval object if youwant to.  Either assign them during creation (like the simple_evalfunction).. code-block python    def boo()        return Boo    s  SimpleEval(functions{boo boo})or edit them after creation.. code-block python    s.names[fortytwo]  42this actually means you can modify names (or functions) with functions, if youreally feel so inclined.. code-block python    s  SimpleEval()    def set_val(name, value)        s.names[name.value]  value.value        return value.value    s.functions  {set set_val}    s.eval(set(age, 111))Say.  This would allow a certain level of scriptyness if you had theseevaluations happening as callbacks in a program.  Although you really arereaching the end of what this library is intended for at this stage.Compound Types--------------Compound types (dict, tuple, list, set) in general just work ifyou pass them in as named objects.  If you want to allow creation of these, theEvalWithCompoundTypes class works.  Just replace any use of SimpleEval withthat.The EvalWithCompoundTypes class also contains support for simple comprehensions.eg [x + 1 for x in [1,2,3]].  Theres a safety MAX_COMPREHENSION_LENGTH to controlhow many items itll allow before bailing too.  This also takes into account nestedcomprehensions.Since the primary intention of this library is short expressions - an extra sweetener isenabled by default.  You can access a dict (or similars) keys using the .attr syntax.. code-block python      simple_eval(foo.bar, names{foo {bar 42}})    42for instance.  You can turn this off either by setting the module global ATTR_INDEX_FALLBACKto False, or on the SimpleEval instance itself. e.g. evaller.ATTR_INDEX_FALLBACKFalse.Extending---------The SimpleEval class is pretty easy to extend.  For instance, to create aversion that disallows method invocation on objects.. code-block python    import ast    import simpleeval    class EvalNoMethods(simpleeval.SimpleEval)        def _eval_call(self, node)            if isinstance(node.func, ast.Attribute)                raise simpleeval.FeatureNotAvailable(No methods please, were British)            return super(EvalNoMethods, self)._eval_call(node)and then use EvalNoMethods instead of the SimpleEval class.Other...--------The library supports python 3 - but should be mostly compatible (and tested before 0.9.11)with python 2.7 as well.Object attributes that start with _ or func_ are disallowed by default.If you really need that (BE CAREFUL), then modify the module globalsimpleeval.DISALLOW_PREFIXES.A few builtin functions are listed in simpleeval.DISALLOW_FUNCTIONS.  type, open, etc.If you need to give access to this kind of functionality to your expressions, then be verycareful.  Youd be better wrapping the functions in your own safe wrappers.The initial idea came from J.F. Sebastian on Stack Overflow( httpstackoverflow.coma95580011973500 ) with modifications and many improvements,see the head of the main file for contributors list.Please read the test_simpleeval.py file for other potential gotchas ordetails.  Im very happy to accept pull requests, suggestions, or other issues.EnjoyDeveloping----------Run tests     make testOr to set the tests running on every file change     make autotest(requires entr) Im trying to keep the codebase relatively clean with Black, isort, pylint  mypy.See     make formatand     make lintBEWARE------Ive done the best I can with this library - but theres no warranty, no guarantee, nada.  A lot ofvery clever people think the whole idea of trying to sandbox CPython is impossible.  Read the codeyourself, and use it at your own risk.</longdescription>
</pkgmetadata>