<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>[[Returns logo](httpsraw.githubusercontent.comdry-pythonbrandmasterlogoreturns_white-outline.png)](httpsgithub.comdry-pythonreturns)-----[[Build Status](httpsgithub.comdry-pythonreturnsworkflowstestbadge.svgbranchmastereventpush)](httpsgithub.comdry-pythonreturnsactionsqueryworkflow3Atest)[[codecov](httpscodecov.ioghdry-pythonreturnsbranchmastergraphbadge.svg)](httpscodecov.ioghdry-pythonreturns)[[Documentation Status](httpsreadthedocs.orgprojectsreturnsbadgeversionlatest)](httpsreturns.readthedocs.ioenlatestbadgelatest)[[Python Version](httpsimg.shields.iopypipyversionsreturns.svg)](httpspypi.orgprojectreturns)[[wemake-python-styleguide](httpsimg.shields.iobadgestyle-wemake-000000.svg)](httpsgithub.comwemake-serviceswemake-python-styleguide)[[Telegram chat](httpsimg.shields.iobadgechat-join-bluelogotelegram)](httpst.medrypython)-----Make your functions return something meaningful, typed, and safe Features- Brings functional programming to Python land- Provides a bunch of primitives to write declarative business logic- Enforces better architecture- Fully typed with annotations and checked with mypy, [PEP561 compatible](httpswww.python.orgdevpepspep-0561)- Adds emulated Higher Kinded Types support- Provides type-safe interfaces to create your own data-types with enforced laws- Has a bunch of helpers for better composition- Pythonic and pleasant to write and to read - Support functions and coroutines, framework agnostic- Easy to start has lots of docs, tests, and tutorials[Quickstart](httpsreturns.readthedocs.ioenlatestpagesquickstart.html) right now Installationbashpip install returnsYou are also required to [configure](httpsreturns.readthedocs.ioenlatestpagescontainer.htmltype-safety)mypy correctly and install our pluginto fix [this existing issue](httpsgithub.compythonmypyissues3157)ini In setup.cfg or mypy.ini[mypy]plugins   returns.contrib.mypy.returns_pluginWe also recommend to use the same mypy settings [we use](httpsgithub.comwemake-serviceswemake-python-styleguideblobmasterstylesmypy.toml).Make sure you know how to get started, [check out our docs](httpsreturns.readthedocs.ioenlatest)[Try our demo](httpsrepl.itsobolevnreturnsex.py). Contents- [Maybe container](maybe-container) that allows you to write None-free code- [RequiresContext container](requirescontext-container) that allows you to use typed functional dependency injection- [Result container](result-container) that lets you to get rid of exceptions- [IO container](io-container) and [IOResult](troublesome-io) that marks all impure operations and structures them- [Future container](future-container) and [FutureResult](async-code-without-exceptions) to work with async code- [Write your own container](httpsreturns.readthedocs.ioenlatestpagescreate-your-own-container.html) You would still have all the features for your own types (including full existing code reuse and type-safety) Maybe containerNone is called the [worst mistake in the history of Computer Science](httpswww.infoq.compresentationsNull-References-The-Billion-Dollar-Mistake-Tony-Hoare).So, what can we do to check for None in our programsYou can use builtin [Optional](httpsmypy.readthedocs.ioenstablekinds_of_types.htmloptional-types-and-the-none-type) typeand write a lot of if some is not None conditions.But, having null checks here and there makes your code unreadable.pythonuser Optional[User]discount_program Optional[DiscountProgram]  Noneif user is not None     balance  user.get_balance()     if balance is not None         credit  balance.credit_amount()         if credit is not None and credit  0             discount_program  choose_discount(credit)Or you can use[Maybe](httpsreturns.readthedocs.ioenlatestpagesmaybe.html) containerIt consists of Some and Nothing types,representing existing state and empty (instead of None) state respectively.pythonfrom typing import Optionalfrom returns.maybe import Maybe, maybemaybe   decorator to convert existing Optional[int] to Maybe[int]def bad_function() - Optional[int]    ...maybe_number Maybe[float]  bad_function().bind_optional(    lambda number number  2,)  Maybe will return Some[float] only if theres a non-None value    Otherwise, will return NothingYou can be sure that .bind_optional() method wont be called for Nothing.Forget about None-related errors foreverWe can also bind a Optional-returning function over a container.To achieve this, we are going to use .bind_optional method.And thats how your initial refactored code will look likepythonuser Optional[User] Type hint here is optional, it only helps the reader herediscount_program Maybe[DiscountProgram]  Maybe.from_optional(    user,).bind_optional(   This wont be called if user is None    lambda real_user real_user.get_balance(),).bind_optional(   This wont be called if real_user.get_balance() is None    lambda balance balance.credit_amount(),).bind_optional(   And so on    lambda credit choose_discount(credit) if credit  0 else None,)Much better, isnt it RequiresContext containerMany developers do use some kind of dependency injection in Python.And usually it is based on the ideathat theres some kind of a container and assembly process.Functional approach is much simplerImagine that you have a django based game, where you award users with points for each guessed letter in a word (unguessed letters are marked as .)pythonfrom django.http import HttpRequest, HttpResponsefrom words_app.logic import calculate_pointsdef view(request HttpRequest) - HttpResponse    user_word str  request.POST[word]   just an example    points  calculate_points(user_word)    ...   later you show the result to user somehow Somewhere in your words_applogic.pydef calculate_points(word str) - int    guessed_letters_count  len([letter for letter in word if letter  .])    return _award_points_for_letters(guessed_letters_count)def _award_points_for_letters(guessed int) - int    return 0 if guessed  5 else guessed   minimum 6 points possibleAwesome It works, users are happy, your logic is pure and awesome.But, later you decide to make the game more funlets make the minimal accountable letters thresholdconfigurable for an extra challenge.You can just do it directlypythondef _award_points_for_letters(guessed int, threshold int) - int    return 0 if guessed  threshold else guessedThe problem is that _award_points_for_letters is deeply nested.And then you have to pass threshold through the whole callstack,including calculate_points and all other functions that might be on the way.All of them will have to accept threshold as a parameterThis is not useful at allLarge code bases will struggle a lot from this change.Ok, you can directly use django.settings (or similar)in your _award_points_for_letters function.And ruin your pure logic with framework specific details. Thats uglyOr you can use RequiresContext container. Lets see how our code changespythonfrom django.conf import settingsfrom django.http import HttpRequest, HttpResponsefrom words_app.logic import calculate_pointsdef view(request HttpRequest) - HttpResponse    user_word str  request.POST[word]   just an example    points  calculate_points(user_words)(settings)   passing the dependencies    ...   later you show the result to user somehow Somewhere in your words_applogic.pyfrom typing_extensions import Protocolfrom returns.context import RequiresContextclass _Deps(Protocol)   we rely on abstractions, not direct values or types    WORD_THRESHOLD intdef calculate_points(word str) - RequiresContext[int, _Deps]    guessed_letters_count  len([letter for letter in word if letter  .])    return _award_points_for_letters(guessed_letters_count)def _award_points_for_letters(guessed int) - RequiresContext[int, _Deps]    return RequiresContext(        lambda deps 0 if guessed  deps.WORD_THRESHOLD else guessed,    )And now you can pass your dependencies in a really direct and explicit way.And have the type-safety to check what you pass to cover your back.Check out [RequiresContext](httpsreturns.readthedocs.ioenlatestpagescontext.html) docs for more. There you will learn how to make . also configurable.We also have [RequiresContextResult](httpsreturns.readthedocs.ioenlatestpagescontext.htmlrequirescontextresult-container)for context-related operations that might fail. And also [RequiresContextIOResult](httpsreturns.readthedocs.ioenlatestpagescontext.htmlrequirescontextioresult-container) and [RequiresContextFutureResult](httpsreturns.readthedocs.ioenlatestpagescontext.htmlrequirescontextfutureresult-container). Result containerPlease, make sure that you are also aware of[Railway Oriented Programming](httpsfsharpforfunandprofit.comrop). Straight-forward approachConsider this code that you can find in any python project.pythonimport requestsdef fetch_user_profile(user_id int) - UserProfile    Fetches UserProfile dict from foreign API.    response  requests.get(apiusers{0}.format(user_id))    response.raise_for_status()    return response.json()Seems legit, does it notIt also seems like a pretty straightforward code to test.All you need is to mock requests.get to return the structure you need.But, there are hidden problems in this tiny code samplethat are almost impossible to spot at the first glance. Hidden problemsLets have a look at the exact same code,but with the all hidden problems explained.pythonimport requestsdef fetch_user_profile(user_id int) - UserProfile    Fetches UserProfile dict from foreign API.    response  requests.get(apiusers{0}.format(user_id))     What if we try to find user that does not exist     Or network will go down Or the server will return 500     In this case the next line will fail with an exception.     We need to handle all possible errors in this function     and do not return corrupt data to consumers.    response.raise_for_status()     What if we have received invalid JSON     Next line will raise an exception    return response.json()Now, all (probably all) problems are clear.How can we be sure that this function will be safeto use inside our complex business logicWe really cannot be sureWe will have to create lots of try and except casesjust to catch the expected exceptions. Our code will become complex and unreadable with all this messOr we can go with the top level except Exception caseto catch literally everything.And this way we would end up with catching unwanted ones.This approach can hide serious problems from us for a long time. Pipe examplepythonimport requestsfrom returns.result import Result, safefrom returns.pipeline import flowfrom returns.pointfree import binddef fetch_user_profile(user_id int) - Result[UserProfile, Exception]    Fetches UserProfile TypedDict from foreign API.    return flow(        user_id,        _make_request,        bind(_parse_json),    )safedef _make_request(user_id int) - requests.Response     TODO we are not yet done with this example, read more about IO    response  requests.get(apiusers{0}.format(user_id))    response.raise_for_status()    return responsesafedef _parse_json(response requests.Response) - UserProfile    return response.json()Now we have a clean and a safe and declarative wayto express our business needs- We start from making a request, that might fail at any moment,- Then parsing the response if the request was successful,- And then return the result.Now, instead of returning regular valueswe return values wrapped inside a special containerthanks to the[safe](httpsreturns.readthedocs.ioenlatestpagesresult.htmlsafe)decorator. It will return [Success[YourType] or Failure[Exception]](httpsreturns.readthedocs.ioenlatestpagesresult.html).And will never throw exception at usWe also use [flow](httpsreturns.readthedocs.ioenlatestpagespipeline.htmlflow)and [bind](httpsreturns.readthedocs.ioenlatestpagespointfree.htmlbind)functions for handy and declarative composition.This way we can be sure that our code wont break inrandom places due to some implicit exception.Now we control all parts and are prepared for the explicit errors.We are not yet done with this example,lets continue to improve it in the next chapter. IO containerLets look at our example from another angle.All its functions look like regular onesit is impossible to tell whether they are [pure](httpsen.wikipedia.orgwikiPure_function)or impure from the first sight.It leads to a very important consequencewe start to mix pure and impure code together.We should not do thatWhen these two concepts are mixedwe suffer really bad when testing or reusing it.Almost everything should be pure by default.And we should explicitly mark impure parts of the program.Thats why we have created IO containerto mark impure functions that never fail.These impure functions use random, current datetime, environment, or consolepythonimport randomimport datetime as dtfrom returns.io import IOdef get_random_number() - IO[int]   or use impure decorator    return IO(random.randint(1, 10))   isnt pure, because randomnow Callable[[], IO[dt.datetime]]  impure(dt.datetime.now)impuredef return_and_show_next_number(previous int) - int    next_number  previous + 1    print(next_number)   isnt pure, because does IO    return next_numberNow we can clearly see which functions are pure and which ones are impure.This helps us a lot in building large applications, unit testing you code,and composing business logic together. Troublesome IOAs it was already said, we use IO when we handle functions that do not fail.What if our function can fail and is impureLike requests.get() we had earlier in our example.Then we have to use a special IOResult type instead of a regular Result.Lets find the difference- Our _parse_json function always returns  the same result (hopefully) for the same input  you can either parse valid json or fail on invalid one.  Thats why we return pure Result, theres no IO inside- Our _make_request function is impure and can fail.  Try to send two similar requests with and without internet connection.  The result will be different for the same input.  Thats why we must use IOResult here it can fail and has IOSo, in order to fulfill our requirement and separate pure code from impure one,we have to refactor our example. Explicit IOLets make our [IO](httpsreturns.readthedocs.ioenlatestpagesio.html)explicitpythonimport requestsfrom returns.io import IOResult, impure_safefrom returns.result import safefrom returns.pipeline import flowfrom returns.pointfree import bind_resultdef fetch_user_profile(user_id int) - IOResult[UserProfile, Exception]    Fetches UserProfile TypedDict from foreign API.    return flow(        user_id,        _make_request,         before def (Response) - UserProfile         after safe def (Response) - ResultE[UserProfile]         after bind_result def (IOResultE[Response]) - IOResultE[UserProfile]        bind_result(_parse_json),    )impure_safedef _make_request(user_id int) - requests.Response    response  requests.get(apiusers{0}.format(user_id))    response.raise_for_status()    return responsesafedef _parse_json(response requests.Response) - UserProfile    return response.json()And later we can use [unsafe_perform_io](httpsreturns.readthedocs.ioenlatestpagesio.htmlunsafe-perform-io)somewhere at the top level of our program to get the pure (or real) value.As a result of this refactoring session, we know everything about our code- Which parts can fail,- Which parts are impure,- How to compose them in a smart, readable, and typesafe manner. Future containerThere are several issues with async code in Python1. You cannot call async function from a sync one2. Any unexpectedly thrown exception can ruin your whole event loop3. Ugly composition with lots of await statementsFuture and FutureResult containers solve these issues Mixing sync and async codeThe main feature of [Future](httpsreturns.readthedocs.ioenlatestpagesfuture.html)is that it allows to run async codewhile maintaining sync context. Lets see an example.Lets say we have two functions,the first one returns a number and the second one increments itpythonasync def first() - int    return 1def second()   How can we call first() from here    return first() + 1   Boom Dont do this. We illustrate a problem here.If we try to just run first(), we will just create an unawaited coroutine.It wont return the value we want.But, if we would try to run await first(),then we would need to change second to be async.And sometimes it is not possible for various reasons.However, with Future we can pretend to call async code from sync codepythonfrom returns.future import Futuredef second() - Future[int]    return Future(first()).map(lambda num num + 1)Without touching our first async functionor making second async we have achieved our goal.Now, our async value is incremented inside a sync function.However, Future still requires to be executed inside a proper eventlooppythonimport anyio   or asyncio, or any other lib We can then pass our Future to any library asyncio, trio, curio. And use any event loop regular, uvloop, even a custom one, etcassert anyio.run(second().awaitable)  2As you can see Future allows youto work with async functions from a sync context.And to mix these two realms together.Use raw Future for operations that cannot fail or raise exceptions.Pretty much the same logic we had with our IO container. Async code without exceptionsWe have already covered how [Result](result-container) worksfor both pure and impure code.The main idea is we dont raise exceptions, we return them.It is especially critical in async code,because a single exception can ruinall our coroutines running in a single eventloop.We have a handy combination of Future and Result containers FutureResult.Again, this is exactly like IOResult, but for impure async code.Use it when your Future might have problemslike HTTP requests or filesystem operations.You can easily turn any wild throwing coroutine into a calm FutureResultpythonimport anyiofrom returns.future import future_safefrom returns.io import IOFailurefuture_safeasync def raising()    raise ValueError(Not so fast)ioresult  anyio.run(raising.awaitable)   all Futures return IO containersassert ioresult  IOFailure(ValueError(Not so fast))   TrueUsing FutureResult will keep your code safe from exceptions.You can always await or execute inside an eventloop any FutureResultto get sync IOResult instance to work with it in a sync manner. Better async compositionPreviously, you had to do quite a lot of awaiting while writing async codepythonasync def fetch_user(user_id int) - User    ...async def get_user_permissions(user User) - Permissions    ...async def ensure_allowed(permissions Permissions) - bool    ...async def main(user_id int) - bool     Also, dont forget to handle all possible errors with try  except    user  await fetch_user(user_id)   We will await each time we use a coro    permissions  await get_user_permissions(user)    return await ensure_allowed(permissions)Some people are ok with it, but some people dont like this imperative style.The problem is that there was no choice.But now, you can do the same thing in functional styleWith the help of Future and FutureResult containerspythonimport anyiofrom returns.future import FutureResultE, future_safefrom returns.io import IOSuccess, IOFailurefuture_safeasync def fetch_user(user_id int) - User    ...future_safeasync def get_user_permissions(user User) - Permissions    ...future_safeasync def ensure_allowed(permissions Permissions) - bool    ...def main(user_id int) - FutureResultE[bool]     We can now turn main into a sync function, it does not await at all.     We also dont care about exceptions anymore, they are already handled.    return fetch_user(user_id).bind(get_user_permissions).bind(ensure_allowed)correct_user_id int   has required permissionsbanned_user_id int   does not have required permissionswrong_user_id int   does not exist We can have correct business resultsassert anyio.run(main(correct_user_id).awaitable)  IOSuccess(True)assert anyio.run(main(banned_user_id).awaitable)  IOSuccess(False) Or we can have errors along the wayassert anyio.run(main(wrong_user_id).awaitable)  IOFailure(    UserDoesNotExistError(...),)Or even something really fancypythonfrom returns.pointfree import bindfrom returns.pipeline import flowdef main(user_id int) - FutureResultE[bool]    return flow(        fetch_user(user_id),        bind(get_user_permissions),        bind(ensure_allowed),    )Later we can also refactor our logical functions to be syncand to return FutureResult.Lovely, isnt it MoreWant more[Go to the docs](httpsreturns.readthedocs.io)Or read these articles- [Python exceptions considered an anti-pattern](httpssobolevn.me201902python-exceptions-considered-an-antipattern)- [Enforcing Single Responsibility Principle in Python](httpssobolevn.me201903enforcing-srp)- [Typed functional Dependency Injection in Python](httpssobolevn.me202002typed-functional-dependency-injection)- [How Async Should Have Been](httpssobolevn.me202006how-async-should-have-been)- [Higher Kinded Types in Python](httpssobolevn.me202010higher-kinded-types-in-python)- [Make Tests a Part of Your App](httpssobolevn.me202102make-tests-a-part-of-your-app)Do you have an article to submit Feel free to open a pull requestp aligncentermdash  mdashpp aligncenteriDrylabs maintains dry-python and helps those who want to use it inside their organizations.ipp aligncenteriRead more at a hrefhttpsdrylabs.iodrylabs.ioaip</longdescription>
</pkgmetadata>