<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># Compendium[![License](https://img.shields.io/badge/License-Apache%202.0-blue.svg)](https://opensource.org/licenses/Apache-2.0)[![Build Status](https://travis-ci.org/kuwv/python-compendium.svg?branch=master)](https://travis-ci.org/kuwv/python-compendium)[![codecov](https://codecov.io/gh/kuwv/python-compendium/branch/master/graph/badge.svg)](https://codecov.io/gh/kuwv/python-compendium)## OverviewCompendium is a layered configuration management tool. It has the capabilityto manage configuration files writen in JSON, TOML, XML and YAML. Settingsfrom these configuration files can then be managed easily with the help ofdpath.## Documentationhttps://kuwv.github.io/python-compendium/### Install```pip install compendium```### Manage a configuration file```python&gt;&gt;&gt; import os&gt;&gt;&gt; from compendium import ConfigFile&gt;&gt;&gt; basedir = os.path.join(os.getcwd(), 'tests')&gt;&gt;&gt; filepath = os.path.join(basedir, 'config.toml')&gt;&gt;&gt; cfg = ConfigFile(filepath)&gt;&gt;&gt; settings = cfg.load()Simple lookup for title&gt;&gt;&gt; settings['/title']'TOML Example'Query values within list&gt;&gt;&gt; settings.values('/servers/**/ip')['10.0.0.1', '10.0.0.2']Check the current server IP address&gt;&gt;&gt; settings['/database/server']'192.168.1.1'Update the server IP address&gt;&gt;&gt; settings['/database/server'] = '192.168.1.2'&gt;&gt;&gt; settings['/database/server']'192.168.1.2'Check the database max connections&gt;&gt;&gt; settings['/database/connection_max']5000Delete the max connections&gt;&gt;&gt; del settings['/database/connection_max']Check that the max connections have been removed&gt;&gt;&gt; settings.get('/database/connection_max')```### Manage multiple layered configurationsThe `ConfigManager` is a layered dictionary mapping. It allows multipleconfigurations to be loaded from various files. Settings from each fileis overlapped in order so that the first setting found will be used.```python&gt;&gt;&gt; import os&gt;&gt;&gt; from compendium import ConfigManagerReference config files from examples&gt;&gt;&gt; basedir = os.path.join(os.getcwd(), 'examples', 'config_manager')&gt;&gt;&gt; config1 = os.path.join(basedir, 'config1.toml')&gt;&gt;&gt; config2 = os.path.join(basedir, 'config2.toml')Retrieve settings from config files&gt;&gt;&gt; cfg = ConfigManager(name='app', filepaths=[config1, config2])Get using dpath&gt;&gt;&gt; cfg.get('/default/foo2')'bar2'Lookup with multi-query&gt;&gt;&gt; cfg.lookup('/example/settings/foo', '/default/foo')'baz'```### Manage nested configurations```python&gt;&gt;&gt; import os&gt;&gt;&gt; from anytree import RenderTree&gt;&gt;&gt; from compendium.config_manager import ConfigManager, TreeConfigManager&gt;&gt;&gt; basedir = os.path.join(os.getcwd(), 'examples', 'tree')&gt;&gt;&gt; cfg = TreeConfigManager(...     name='fruit',...     basedir=basedir,...     filename='node.toml',...     load_root=True,...     load_children=True,... )&gt;&gt;&gt; cfg.defaults == {}True&gt;&gt;&gt; 'succulant' in cfg['/fruit/children']True&gt;&gt;&gt; succulant = cfg.get_config('/fruit/succulant')&gt;&gt;&gt; succulant.name'succulant'```### Manage configurations using Hierarchy File System (HFS)TBD&lt;!--# ```python# import os## from compendium.config_manager import HierarchyConfigManager## import pytest## Setup base paths# &gt;&gt;&gt; base_filepath = os.path.dirname(__file__)# &gt;&gt;&gt; global_filepath = os.path.expanduser('~')## System paths# &gt;&gt;&gt; fs.add_real_file(# ...     source_path=os.path.join(base_filepath, 'settings1.toml'),# ...     target_path=os.path.join(os.sep, 'etc', 'hierarchy', 'config.toml')# ... )## User paths# &gt;&gt;&gt; fs.add_real_file(# ...     source_path=os.path.join(base_filepath, 'settings2.toml'),# ...     target_path=os.path.join(global_filepath, '.hierarchy.toml')# ... )## &gt;&gt;&gt; fs.add_real_file(# ...     source_path=os.path.join(base_filepath, 'settings3.toml'),# ...     target_path=os.path.join(# ...         global_filepath, '.hierarchy.d', 'config.toml'# ...     )# ... )## &gt;&gt;&gt; cfg = HierarchyConfigManager(# ...     name='hierarchy',# ...     filename='config.toml',# ...     merge_strategy='overlay',# ...     enable_system_filepaths=True,# ...     enable_global_filepaths=True# ... )# &gt;&gt;&gt; cfg.load_configs()## ```--&gt;### Development```poetry shellpoetry installpython -m doctest README.md```</longdescription>
</pkgmetadata>