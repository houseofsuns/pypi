<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Cacheops A slick app that supports automatic or manual queryset caching and automaticgranular event-driven invalidation httpsuor.github.ioblog20140309on-orm-cache-invalidation_.It uses redis httpredis.io_ as backend for ORM cache and redis orfilesystem for simple time-invalidated one.And there is more to it- decorators to cache any user function or view as a queryset or by time- extensions for django and jinja2 templates- transparent transaction support- dog-pile prevention mechanism- a couple of hacks to make django faster.. contents Contents    local    backlinks topRequirements++++++++++++Python 3.5+, Django 2.1+ and Redis 4.0+.Installation++++++++++++Using pip.. code bash     pip install django-cacheops     Or from github directly     pip install git+httpsgithub.comSuordjango-cacheops.gitmasterSetup+++++Add cacheops to your INSTALLED_APPS.Setup redis connection and enable caching for desired models.. code python    CACHEOPS_REDIS  {        host localhost,  redis-server is on same machine        port 6379,         default redis port        db 1,              SELECT non-default redis database                              using separate redis db or redis instance                              is highly recommended        socket_timeout 3,    connection timeout in seconds, optional        password ...,      optional        unix_socket_path   replaces host and port    }     Alternatively the redis connection can be defined using a URL    CACHEOPS_REDIS  redislocalhost63791     or    CACHEOPS_REDIS  unixpathtosocketdb1     or with password (note a colon)    CACHEOPS_REDIS  redispasswordlocalhost63791     If you want to use sentinel, specify this variable    CACHEOPS_SENTINEL  {        locations [(localhost, 26379)],  sentinel locations, required        service_name mymaster,           sentinel service name, required        socket_timeout 0.1,                connection timeout in seconds, optional        db 0                               redis database, default 0        ...                                   everything else is passed to Sentinel()    }     To use your own redis client class,     should be compatible or subclass cacheops.redis.CacheopsRedis    CACHEOPS_CLIENT_CLASS  your.redis.ClientClass    CACHEOPS  {         Automatically cache any User.objects.get() calls for 15 minutes         This also includes .first() and .last() calls,         as well as request.user or post.author access,         where Post.author is a foreign key to auth.User        auth.user {ops get, timeout 6015},         Automatically cache all gets and queryset fetches         to other django.contrib.auth models for an hour        auth. {ops {fetch, get}, timeout 6060},         Cache all queries to Permission         all is an alias for {get, fetch, count, aggregate, exists}        auth.permission {ops all, timeout 6060},         Enable manual caching on all other models with default timeout of an hour         Use Post.objects.cache().get(...)          or Tags.objects.filter(...).order_by(...).cache()         to cache particular ORM request.         Invalidation is still automatic        . {ops (), timeout 6060},         And since ops is empty by default you can rewrite last line as        . {timeout 6060},         NOTE binding signals has its overhead, like preventing fast mass deletes,               you might want to only register whatever you cache and dependencies.         Finally you can explicitely forbid even manual caching with        some_app. None,    }You can configure default profile setting with CACHEOPS_DEFAULTS. This way you can rewrite the config above.. code python    CACHEOPS_DEFAULTS  {        timeout 6060    }    CACHEOPS  {        auth.user {ops get, timeout 6015},        auth. {ops (fetch, get)},        auth.permission {ops all},        . {},    }Using . with non-empty ops is not recommendedsince it will easily cache something you dont intent to or even know about like migrations tables.The better approach will be restricting by app with app_name..Besides ops and timeout options you can also uselocal_get True    To cache simple gets for this model in process local memory.    This is very fast, but is not invalidated in any way until process is restarted.    Still could be useful for extremely rarely changed things.cache_on_saveTrue  field_name    To write an instance to cache upon save.    Cached instance will be retrieved on .get(field_name...) request.    Setting to True causes caching by primary key.Additionally, you can tell cacheops to degrade gracefully on redis fail with.. code python    CACHEOPS_DEGRADE_ON_FAILURE  TrueThere is also a possibility to make all cacheops methods and decorators no-op, e.g. for testing.. code python    from django.test import override_settings    override_settings(CACHEOPS_ENABLEDFalse)    def test_something()         ...        assert condUsage+++++ Automatic cachingIts automatic you just need to set it up. Manual cachingYou can force any queryset to use cache by calling its .cache() method.. code python    Article.objects.filter(tag2).cache()Here you can specify which ops should be cached for the queryset, for example, this code.. code python    qs  Article.objects.filter(tag2).cache(ops[count])    paginator  Paginator(objects, ipp)    articles  list(pager.page(page_num))  hits databasewill cache count call in Paginator but not later articles fetch.There are five possible actions - get, fetch, count, aggregate and exists.You can pass any subset of this ops to .cache() method even empty - to turn off caching.There is, however, a shortcut for the latter.. code python    qs  Article.objects.filter(visibleTrue).nocache()    qs1  qs.filter(tag2)        hits database    qs2  qs.filter(category3)   hits it once moreIt is useful when you want to disable automatic caching on particular queryset.You can also override default timeout for particular queryset with .cache(timeout...). Function cachingYou can cache and invalidate result of a function the same way as a queryset.Cached results of the next function will be invalidated on any Article change,addition or deletion.. code python    from cacheops import cached_as    cached_as(Article, timeout120)    def article_stats()        return {            tags list(Article.objects.values(tag).annotate(Count(id)))            categories list(Article.objects.values(category).annotate(Count(id)))        }Note that we are using list on both querysets here, its because we dont wantto cache queryset objects but their results.Also note that if you want to filter queryset based on arguments,e.g. to make invalidation more granular, you can use a local function.. code python    def articles_block(category, count5)        qs  Article.objects.filter(categorycategory)        cached_as(qs, extracount)        def _articles_block()            articles  list(qs.filter(photoTrue)[count])            if len(articles)  count                articles + list(qs.filter(photoFalse)[count-len(articles)])            return articles        return _articles_block()We added extra here to make different keys for calls with same category but differentcount. Cache key will also depend on function arguments, so we could just pass count asan argument to inner function. We also omitted timeout here, so a default for the modelwill be used.Another possibility is to make function cache invalidate on changes to any one of several models.. code python    cached_as(Article.objects.filter(publicTrue), Tag)    def article_stats()        return {...}As you can see, we can mix querysets and models here. View cachingYou can also cache and invalidate a view as a queryset. This works mostly the same way as functioncaching, but only path of the request parameter is used to construct cache key.. code python    from cacheops import cached_view_as    cached_view_as(News)    def news_index(request)         ...        return render(...)You can pass timeout, extra and several samples the same way as to cached_as(). Note that you can pass a function as extra.. code python    cached_view_as(News, extralambda req req.user.is_staff)    def news_index(request)         ... add extra things for staff        return render(...)A function passed as extra receives the same arguments as the cached function.Class based views can also be cached.. code python    class NewsIndex(ListView)        model  News    news_index  cached_view_as(News, ...)(NewsIndex.as_view())Invalidation++++++++++++Cacheops uses both time and event-driven invalidation. The event-driven onelistens on model signals and invalidates appropriate caches on Model.save(), .delete()and m2m changes.Invalidation tries to be granular which means it wont invalidate a querysetthat cannot be influenced by addedupdateddeleted object judging by queryconditions. Most of the time this will do what you want, if it wont you can useone of the following.. code python    from cacheops import invalidate_obj, invalidate_model, invalidate_all    invalidate_obj(some_article)   invalidates queries affected by some_article    invalidate_model(Article)      invalidates all queries for model    invalidate_all()               flush redis cache databaseAnd last there is invalidate command    .manage.py invalidate articles.Article.34   same as invalidate_obj    .manage.py invalidate articles.Article      same as invalidate_model    .manage.py invalidate articles    invalidate all models in articlesAnd the one that FLUSHES cacheops redis database    .manage.py invalidate allDont use that if you share redis database for both cache and something else. Turning off and postponing invalidationThere is also a way to turn off invalidation for a while.. code python    from cacheops import no_invalidation    with no_invalidation         ... do some changes        obj.save()Also works as decorator.. code python    no_invalidation    def some_work(...)         ... do some changes        obj.save()Combined with try ... finally it could be used to postpone invalidation.. code python    try        with no_invalidation             ...    finally        invalidate_obj(...)         ... or        invalidate_model(...)Postponing invalidation can speed up batch jobs. Mass updatesNormally qs.update(...) doesnt emit any events and thus doesnt trigger invalidation.And there is no transparent and efficient way to do that trying to act on conditions willinvalidate too much if update conditions are orthogonal to many queries conditions,and to act on specific objects we will need to fetch all of them,which QuerySet.update() users generally try to avoid.In the case you actually want to perform the latter cacheops provides a shortcut.. code python    qs.invalidated_update(...)Note that all the updated objects are fetched twice, prior and post the update.Components++++++++++Simple time-invalidated cache-----------------------------To cache result of a function call or a view for some time use.. code python    from cacheops import cached, cached_view    cached(timeoutnumber_of_seconds)    def top_articles(category)        return ...  Some costly queries    cached_view(timeoutnumber_of_seconds)    def top_articles(request, categoryNone)         Some costly queries        return HttpResponse(...)cached() will generate separate entry for each combination of decorated function and itsarguments. Also you can use extra same way as in cached_as(), most useful for nestedfunctions.. code python    property    def articles_json(self)        cached(timeout1060, extraself.category_id)        def _articles_json()            ...            return json.dumps(...)        return _articles_json()You can manually invalidate or update a result of a cached function.. code python    top_articles.invalidate(some_category)    top_articles.key(some_category).set(new_value)To invalidate cached view you can pass absolute uri instead of request.. code python    top_articles.invalidate(httpexample.compage, some_category)Cacheops also provides getset primitives for simple cache.. code python    from cacheops import cache    cache.set(cache_key, data, timeoutNone)    cache.get(cache_key)    cache.delete(cache_key)cache.get will raise CacheMiss if nothing is stored for given key.. code python    from cacheops import cache, CacheMiss    try        result  cache.get(key)    except CacheMiss        ...  deal with itFile Cache----------File based cache can be used the same way as simple time-invalidated one.. code python    from cacheops import file_cache    file_cache.cached(timeoutnumber_of_seconds)    def top_articles(category)        return ...  Some costly queries    file_cache.cached_view(timeoutnumber_of_seconds)    def top_articles(request, category)         Some costly queries        return HttpResponse(...)     later, on appropriate event    top_articles.invalidate(some_category)     or    top_articles.key(some_category).set(some_value)     primitives    file_cache.set(cache_key, data, timeoutNone)    file_cache.get(cache_key)    file_cache.delete(cache_key)It has several improvements upon django built-in file cache, both about high load.First, its safe against concurrent writes. Second, its invalidation is done as separate task,youll need to call this from crontab for that to work    pathmanage.py cleanfilecache    pathmanage.py cleanfilecache pathtonon-defaultcachedirDjango templates integration----------------------------Cacheops provides tags to cache template fragments. They mimic cached_asand cached decorators, however, they require explicit naming of each fragment.. code django    { load cacheops }    { cached_as queryset timeout fragment_name [extra1 extra2 ...] }        ... some template code ...    { endcached_as }    { cached timeout fragment_name [extra1 extra2 ...] }        ... some template code ...    { endcached }You can use None for timeout in cached_as to use its default value for model.To invalidate cached fragment use.. code python    from cacheops import invalidate_fragment    invalidate_fragment(fragment_name, extra1, ...)If you have more complex fragment caching needs, cacheops provides a helper tomake your own template tags which decorate a template fragment in a wayanalogous to decorating a function with cached or cached_as.This is experimental feature for now.To use it create myapptemplatetagsmycachetags.py and add something like this there.. code python    from cacheops import cached_as, CacheopsLibrary    register  CacheopsLibrary()    register.decorator_tag(takes_contextTrue)    def cache_menu(context, menu_name)        from django.utils import translation        from myapp.models import Flag, MenuItem        request  context.get(request)        if request and request.user.is_staff()             Use noop decorator to bypass caching for staff            return lambda func func        return cached_as(             Invalidate cache if any menu item or a flag for menu changes            MenuItem,            Flag.objects.filter(namemenu),             Vary for menu name and language, also stamp it as menu to be safe            extra(menu, menu_name, translation.get_language()),            timeout24  60  60        )decorator_tag here creates a template tag behaving the same as returned decoratorupon wrapped template fragment. Resulting template tag could be used as follows.. code django    { load mycachetags }    { cache_menu top }        ... the top menu template code ...    { endcache_menu }    ... some template code ..    { cache_menu bottom }        ... the bottom menu template code ...    { endcache_menu }Jinja2 extension----------------Add cacheops.jinja2.cache to your extensions and use.. code jinja    { cached_as queryset [, timeouttimeout] [, extrakey addition] }        ... some template code ...    { endcached_as }or.. code jinja    { cached [timeouttimeout] [, extrakey addition] }        ...    { endcached }Tags work the same way as corresponding decorators.Special topics++++++++++++++Transactions------------Cacheops transparently supports transactions. This is implemented by following simple rules1. Once transaction is dirty (has changes) caching turns off. The reason is that the state of database at this point is only visible to current transaction and should not affect other users and vice versa.2. Any invalidating calls are scheduled to run on the outer commit of transaction.3. Savepoints and rollbacks are also handled appropriately.Mind that simple and file cache dont turn itself off in transactions but work as usual.Dog-pile effect prevention--------------------------There is optional locking mechanism to prevent several threads or processes simultaneously performing same heavy task. It works with cached_as() and querysets.. code python    cached_as(qs, lockTrue)    def heavy_func(...)         ...    for item in qs.cache(lockTrue)         ...It is also possible to specify lock True in CACHEOPS setting but that would probably be a waste. Locking has no overhead on cache hit though.Multiple database support-------------------------By default cacheops considers query result is same for same query, not dependingon database queried. That could be changed with db_agnostic cache profile option.. code python    CACHEOPS  {        some.model {ops get, db_agnostic False, timeout ...}    }Sharing redis instance----------------------Cacheops provides a way to share a redis instance by adding prefix to cache keys.. code python    CACHEOPS_PREFIX  lambda query ...     or    CACHEOPS_PREFIX  some.module.cacheops_prefixA most common usage would probably be a prefix by host name.. code python     get_request() returns current request saved to threadlocal by some middleware    cacheops_prefix  lambda _ get_request().get_host()A query object passed to callback also enables reflection on used databases and tables.. code python    def cacheops_prefix(query)        query.dbs     A list of databases queried        query.tables  A list of tables query is invalidated on        if set(query.tables)  HELPER_TABLES            return helper        if query.tables  [blog_post]            return blogNOTE prefix is not used in simple and file cache. This might change in future cacheops.Custom serialization--------------------Cacheops uses pickle by default, employing its default protocol. But you can specify your ownit might be any module or a class having .dumps() and .loads() functions. For example you can use dill instead, which can serialize more things like anonymous functions.. code python    CACHEOPS_SERIALIZER  dillOne less obvious use is to fix pickle protocol, to use cacheops cache across python versions.. code python    import pickle    class CACHEOPS_SERIALIZER        dumps  lambda data pickle.dumps(data, 3)        loads  pickle.loadsUsing memory limit------------------If your cache never grows too large you may not bother. But if you do you have some options.Cacheops stores cached data along with invalidation data,so you cant just set maxmemory and let redis evict at its will.For now cacheops offers 2 imperfect strategies, which are considered experimental.So be careful and consider leaving feedback httpsgithub.comSuordjango-cacheopsissues143_.First strategy is configuring maxmemory-policy volatile-ttl. Invalidation data is guaranteed to have higher TTL than referenced keys.Redis however doesnt guarantee perfect TTL eviction order, it selects several keys and removesone with the least TTL, thus invalidator could be evicted before cache key it refers leaving it orphan and causing it survive next invalidation.You can reduce this chance by increasing maxmemory-samples redis config option and by reducing cache timeout.Second strategy, probably more efficient one is adding CACHEOPS_LRU  True to your settings and then using maxmemory-policy volatile-lru.However, this makes invalidation structures persistent, they are still removed on associated events, but in absence of them can clutter redis database.Keeping stats-------------Cacheops provides cache_read and cache_invalidated signals for you to keep track.Cache read signal is emitted immediately after each cache lookup. Passed arguments are sender - model class if queryset cache is fetched,func - decorated function and hit - fetch success as boolean value.Here is a simple stats implementation.. code python    from cacheops.signals import cache_read    from statsd.defaults.django import statsd    def stats_collector(sender, func, hit, kwargs)        event  hit if hit else miss        statsd.incr(cacheops.s  event)    cache_read.connect(stats_collector)Cache invalidation signal is emitted after object, model or global invalidation passing sender and obj_dict args. Note that during normal operation cacheops only uses object invalidation, calling it once for each model createdelete and twice for update passing old and new object dictionary.Memory usage cleanup--------------------In some cases, cacheops may leave some conjunction keys of expired cache keys in redis without being able to invalidate them. Those will still expire with age, but in the meantime may cause issues like slow invalidation (even BUSY Redis ...) and extra memory usage. To prevent that it is advised to not cache complex queries, see Perfomance tips performance-tips_, 5.Cacheops ships with a cacheops.reap_conjs function that can clean up these keys,ignoring conjunction sets with some reasonable size. It can be called using the reapconjs management command    .manage.py reapconjs --chunk-size100 --min-conj-set-size10000   with custom values    .manage.py reapconjs                                              with default values (chunks1000, min size1000)The command is a small wrapper that calls a function with the main logic. You can also call it from your code, for example from a Celery task.. code python    from cacheops import reap_conjs    app.task    def reap_conjs_task()        reap_conjs(            chunk_size2000,            min_conj_set_size100,        )Troubleshooting+++++++++++++++CAVEATS-------1. Conditions other than __exact, __in and __isnullTrue dont make invalidation   more granular.2. Conditions on TextFields, FileFields and BinaryFields dont make it either.   One should not test on their equality anyway. See CACHEOPS_SKIP_FIELDS though.3. Update of selected_related object does not invalidate cache for queryset.   Use .prefetch_related() instead.4. Mass updates dont trigger invalidation by default. But see .invalidated_update().5. Sliced queries are invalidated as non-sliced ones.6. Doesnt work with .raw() and other sql queries.7. Conditions on subqueries dont affect invalidation.8. Doesnt work right with multi-table inheritance.Here 1, 2, 3, 5 are part of the design compromise, trying to solve them will makethings complicated and slow. 7 can be implemented if needed, but itsprobably counter-productive since one can just break queries into simpler ones,which cache better. 4 is a deliberate choice, making it right will flushcache too much when update conditions are orthogonal to most queries conditions,see, however, .invalidated_update(). 8 is postponed until it will gainmore interest or a champion willing to implement it emerges.All unsupported things could still be used easily enough with the help of cached_as().Performance tips----------------Here come some performance tips to make cacheops and Django ORM faster.1. When you use cache you pickle and unpickle lots of django model instances, which could be slow. You can optimize django models serialization with django-pickling httpgithub.comSuordjango-pickling_.2. Constructing querysets is rather slow in django, mainly because most of QuerySet methods clone self, then change it and return the clone. Original queryset is usually thrown away. Cacheops adds .inplace() method, which makes queryset mutating, preventing useless cloning    items  Item.objects.inplace().filter(category12).order_by(-date)[20]   You can revert queryset to cloning state using .cloning() call.   Note that this is a micro-optimization technique. Using it is only desirable in the hottest places, not everywhere.3. Use template fragment caching when possible, its way more fast because you dont need to generate anything. Also picklingunpickling a string is much faster than a list of model instances.4. Run separate redis instance for cache with disabled persistence httpredis.iotopicspersistence_. You can manually call SAVE httpredis.iocommandssave_ or BGSAVE httpredis.iocommandsbgsave_ to stay hot upon server restart.5. If you filter queryset on many different or complex conditions cache could degrade performance (comparing to uncached db calls) in consequence of frequent cache misses. Disable cache in such cases entirely or on some heuristics which detect if this request would be probably hit. E.g. enable cache if only some primary fields are used in filter.   Caching querysets with large amount of filters also slows down all subsequent invalidation on that model. You can disable caching if more than some amount of fields is used in filter simultaneously.Writing a test--------------Writing a test for an issue you are experiencing can speed up its resolution a lot.Here is how you do that. I suppose you have some application code causing it.1. Make a fork.2. Install all from requirements-test.txt.3. Ensure you can run tests with .run_tests.py.4. Copy relevant models code to testsmodels.py.5. Go to teststests.py and paste code causing exception to IssueTests.test_{issue_number}.6. Execute .run_tests.py {issue_number} and see it failing.7. Cut down model and test code until error disappears and make a step back.8. Commit changes and make a pull request.TODO++++- faster .get() handling for simple cases such as get by pkid, with simple key calculation- integrate previous one with prefetch_related()- shard cache between multiple redises- respect subqueries- respect headers in cached_view- group invalidate_obj() calls- a postpone invalidation context managerdecorator- fast mode store cache in local memory, but check in with redis if its valid- an interface for complex fields to extract exact on parts or transforms ArrayField.len  field__len, ArrayField[0]  field__0, JSONField[some_key]  field__some_key- custom cache eviction strategy in lua- cache a string directly (no pickle) for direct serving (custom key function).. Build Status image httpsgithub.comSuordjango-cacheopsactionsworkflowsci.ymlbadge.svg   target httpsgithub.comSuordjango-cacheopsactionsworkflowsci.ymlquerybranch3Amaster</longdescription>
</pkgmetadata>