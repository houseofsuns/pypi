<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Browser Cookie==============The **browsercookie** module loads cookies used by your web browserinto a cookiejar object. This can be useful if you want to use python todownload the same content you see in the web browser without needing tologin.Install-------.. sourcecode:: bash        pip install browsercookieOn Windows the builtin sqlite module will raise an error when loadingthe FireFox database. An updated version of sqlite can be installed with:.. sourcecode:: bash        pip install pysqliteUsage-----Here is a hack to extract the title from a webpage:.. sourcecode:: python    &gt;&gt;&gt; import re    &gt;&gt;&gt; get_title = lambda html: re.findall('&lt;title&gt;(.*?)&lt;/title&gt;', html, flags=re.DOTALL)[0].strip()And here is the webpage title when downloaded normally:.. sourcecode:: python    &gt;&gt;&gt; import urllib2    &gt;&gt;&gt; url = 'https://bitbucket.org/'    &gt;&gt;&gt; public_html = urllib2.urlopen(url).read()    &gt;&gt;&gt; get_title(public_html)    'Git and Mercurial code management for teams'Now let's try with **browsercookie** - make sure you are logged intoBitbucket in Firefox before trying this example:.. sourcecode:: python    &gt;&gt;&gt; import browsercookie    &gt;&gt;&gt; cj = browsercookie.firefox()    &gt;&gt;&gt; opener = urllib2.build_opener(urllib2.HTTPCookieProcessor(cj))    &gt;&gt;&gt; login_html = opener.open(url).read()    &gt;&gt;&gt; get_title(login_html)    'richardpenman / home &amp;mdash; Bitbucket'Differences with Python3:.. sourcecode:: python     &gt;&gt;&gt; import urllib.request    &gt;&gt;&gt; public_html = urllib.request.urlopen(url).read()    &gt;&gt;&gt; opener = urllib.request.build_opener(urllib.request.HTTPCookieProcessor(cj))You should see your own username here, meaning the module successfullyloaded the cookies from Firefox.Here is an alternative example with`requests &lt;http://docs.python-requests.org/en/latest/&gt;`__, this timeloading the Chrome cookies. Again make sure you are logged intoBitbucket in Chrome before running this:.. sourcecode:: python    &gt;&gt;&gt; import requests    &gt;&gt;&gt; cj = browsercookie.chrome()    &gt;&gt;&gt; r = requests.get(url, cookies=cj)    &gt;&gt;&gt; get_title(r.content)    'richardpenman / home &amp;mdash; Bitbucket'Alternatively if you don't know/care which browser has the cookies youwant then all available browser cookies can be loaded:.. sourcecode:: python        &gt;&gt;&gt; cj = browsercookie.load()    &gt;&gt;&gt; r = requests.get(url, cookies=cj)    &gt;&gt;&gt; get_title(r.content)    'richardpenman / home &amp;mdash; Bitbucket'Contribute----------So far the following platforms are supported:-  **Chrome:** Linux, OSX, Windows-  **Firefox:** Linux, OSX, WindowsHowever I only tested on a single version of each browser and so am notsure if the cookie sqlite format changes location or format inearlier/later versions. If you experience a problem please `open anissue &lt;https://bitbucket.org/richardpenman/browsercookie/issues/new&gt;`__which includes details of the browser version and operating system. Alsopatches to support other browsers are very welcome, particularly forInternet Explorer on Windows.Acknowledgements----------------* Nathan Henrie for his example of `how to decode the Chrome cookies &lt;http://n8henrie.com/2013/11/use-chromes-cookies-for-easier-downloading-with-python-requests/&gt;`__* Graeme Robinson for his Chrome Windows patch</longdescription>
</pkgmetadata>