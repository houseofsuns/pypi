<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Rank-BM25 A two line search engine[Build Status](httpsgithub.comdorianbrownrank_bm25workflowspytestbadge.svg)[[PyPI version](httpsbadge.fury.iopyrank-bm25.svg)](httpsbadge.fury.iopyrank-bm25)[[DOI](httpszenodo.orgbadge166720547.svg)](httpszenodo.orgbadgelatestdoi166720547)A collection of algorithms for querying a set of documents and returning the ones most relevant to the query. The most common use case for these algorithms is, as you might have guessed, to create search engines.So far the algorithms that have been implemented are- [x] Okapi BM25- [x] BM25L- [x] BM25+- [ ] BM25-Adpt- [ ] BM25T These algorithms were taken from [this paper](httpwww.cs.otago.ac.nzhomepagesandrewpapers2014-2.pdf), which gives a nice overview of each method, and also benchmarks them against each other. A nice inclusion is that they compare different kinds of preprocessing like stemming vs no-stemming, stopword removal or not, etc. Great read if youre new to the topic.  InstallationThe easiest way to install this package is through pip, usingbashpip install rank_bm25If you want to be sure youre getting the newest version, you can install it directly from github withbashpip install git+sshgitgithub.comdorianbrownrank_bm25.git UsageFor this example well be using the BM25Okapi algorithm, but the others are used in pretty much the same way. InitalizingFirst thing to do is create an instance of the BM25 class, which reads in a corpus of text and does some indexing on itpythonfrom rank_bm25 import BM25Okapicorpus  [    Hello there good man,    It is quite windy in London,    How is the weather today]tokenized_corpus  [doc.split( ) for doc in corpus]bm25  BM25Okapi(tokenized_corpus) rank_bm25.BM25Okapi at 0x1047881d0Note that this package doesnt do any text preprocessing. If you want to do things like lowercasing, stopword removal, stemming, etc, you need to do it yourself. The only requirements is that the class receives a list of lists of strings, which are the document tokens. Ranking of documentsNow that weve created our document indexes, we can give it queries and see which documents are the most relevantpythonquery  windy Londontokenized_query  query.split( )doc_scores  bm25.get_scores(tokenized_query) array([0.        , 0.93729472, 0.        ])Good to note that we also need to tokenize our query, and apply the same preprocessing steps we did to the documents in order to have an apples-to-apples comparisonInstead of getting the document scores, you can also just retrieve the best documents withpythonbm25.get_top_n(tokenized_query, corpus, n1) [It is quite windy in London]And thats pretty much it</longdescription>
</pkgmetadata>