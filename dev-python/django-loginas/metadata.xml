<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-loginasAbout-----Login as user for the Django admin.[[PyPI version](httpsimg.shields.iopypivdjango-loginas.svg)](httpspypi.python.orgpypidjango-loginas)loginas supports Python 3 only, as of version 0.4. If youre on 2, use[0.3.6](httpspypi.orgprojectdjango-loginas0.3.6).Installing django-loginas------------------------- Add loginas to your Python path, or install using pip pip install django-loginas Add the loginas app to your INSTALLED_APPSpython settings.pyINSTALLED_APPS  [... loginas, ...] Add the loginas URL to your urls.pypython urls.pyurlpatterns  [     from Django 3.2 on, make sure to add loginas urls before the admin site urls, i.e.    path(admin, include(loginas.urls)),    path(admin, admin.site.urls),] If youre using a custom User model, youll need to add the template to it so the button shows uppython admin.pyclass YourUserAdmin(ModelAdmin)    change_form_template  loginaschange_form.htmlAt this point, you should be good to go. Just visit the Django admin, navigate to a user and you should see the Login as user button at the top right of the screen.Configuring-----------At this point, the only users who will be able to log in as other users are those with the is_superuser permission.If you use custom User models, and havent specified that permission, or if you want to change which users areauthorized to log in as others, you can define the CAN_LOGIN_AS setting, like sopython settings.py This will only allow admins to log in as other usersCAN_LOGIN_AS  lambda request, target_user request.user.is_superuser This will only allow admins to log in as other users, as long as those users are not admins themselvesCAN_LOGIN_AS  lambda request, target_user request.user.is_superuser and not target_user.is_superuser You can also define a string path to a moduleCAN_LOGIN_AS  utils.helpers.custom_loginasBy default, clicking Login as user will send the user to settings.LOGIN_REDIRECT_URL.You can override this behavior like sopython settings.pyLOGINAS_REDIRECT_URL  loginas-redirect-urlIn order to automatically restore the original user upon log out, replace the default log outwith a special log out that restores the original login session from a signed session.python settings.pyfrom django.core.urlresolvers import reverse_lazyLOGOUT_URL  reverse_lazy(loginas-logout)Additionally, you can specify the redirect url for logout (the default is settings.LOGIN_REDIRECT_URL).python settings.pyfrom django.core.urlresolvers import reverse_lazyLOGINAS_LOGOUT_REDIRECT_URL  reverse_lazy(adminindex)By default, clicking Login as user will not update user.last_login.You can override this behavior like sopython settings.pyLOGINAS_UPDATE_LAST_LOGIN  TrueBy default, the login switch message will generate [Django admin LogEntry](httpsdocs.djangoproject.comen3.2refcontribadminlogentry-objects) messages using the User modelsUSERNAME_FIELD like fUser {impersonator_user.getattr(USERNAME_FIELD)} logged in as {impersonated_user.getattr(USERNAME_FIELD)}. You can override this behavior by passing in a differentfield namepython settings.pyLOGINAS_USERNAME_FIELD  emailOther implementation suggestions-------------------------------- Existing logout viewIf you already have a logout view, you can modify to login the original user again after having had a login as session. Heres an examplepythonclass LogoutView(LogoutView)    template_name  myapplogged_out.html    method_decorator(never_cache)    def dispatch(self, request, args, kwargs)        from loginas.utils import restore_original_login        restore_original_login(request)        return redirect(myapplogin) Template awarenessYou can add the context processor loginas.context_processors.impersonated_session_statusin your settings.py file if youd like to be able to access a variable is_impersonated_sessionin all your template contextspython settings.pyTEMPLATES  [    {        ...        OPTIONS {            context_processors [                django.template.context_processors.debug,                django.template.context_processors.request,                django.contrib.auth.context_processors.auth,                ...                loginas.context_processors.impersonated_session_status,            ],        },    },]Note that django-loginas wont let you log in as other superusers, to preventprivilege escalation from staff users to superusers. If you want to log in asa superuser, first demote them to a non-superuser, and then log in.License-------This software is distributed under the BSD license.</longdescription>
</pkgmetadata>