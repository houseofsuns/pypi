<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>sqlalchemy-repr.. image httpstravis-ci.orgmanicmaniacsqlalchemy-repr.svgbranchmaster    target httpstravis-ci.orgmanicmaniacsqlalchemy-reprAutomatically generates pretty repr of a SQLAlchemy model.Install-------.. code sh    pip install sqlalchemy-reprUsage-----.. code python    from sqlalchemy.ext.declarative import declarative_base    from sqlalchemy_repr import RepresentableBase    Base  declarative_base(clsRepresentableBase)Example-------sqlalchemy_repr.RepresentableBase is mixin to add simple representation of columns... code python     from datetime import datetime     from sqlalchemy import Column, DateTime, Integer, Unicode, create_engine     from sqlalchemy.ext.declarative import declarative_base     from sqlalchemy.orm import sessionmaker     from sqlalchemy_repr import RepresentableBase     Base  declarative_base(clsRepresentableBase)     class User(Base)    ...    __tablename__  users    ...    id  Column(Integer, primary_keyTrue)    ...    name  Column(Unicode(255), nullableFalse, uniqueTrue)    ...    created  Column(DateTime, nullableFalse)     engine  create_engine(sqlite)     Base.metadata.create_all(engine)     Session  sessionmaker(bindengine)     session  Session()     user  User(namespam, createddatetime(2016, 6, 1))     session.add(user)     session.commit()     print(user)    User id1, namespam, created2016-06-01T000000sqlalchemy_repr.PrettyRepresentableBase brings pretty, indented multi-line representation... code python     from sqlalchemy_repr import PrettyRepresentableBase     Base  declarative_base(clsPrettyRepresentableBase)     class User(Base)    ...    __tablename__  users    ...    id  Column(Integer, primary_keyTrue)    ...    first_name  Column(Unicode(255), nullableFalse, uniqueTrue)    ...    last_name  Column(Unicode(255), nullableFalse, uniqueTrue)    ...    email  Column(Unicode(255), nullableFalse)    ...    created  Column(DateTime, nullableFalse)    ...    modified  Column(DateTime, nullableFalse)     engine  create_engine(sqlite)     Base.metadata.create_all(engine)     Session  sessionmaker(bindengine)     session  Session()     user  User(first_namespam, last_nameham,  emailspamexample.com, createddatetime(2016, 6, 1), modifieddatetime(2016, 6, 1))     session.add(user)     session.commit()     print(user)    User        id1,        first_namespam,        last_nameham,        emailspamexample.com,        created2016-06-01T000000,        modified2016-06-01T000000</longdescription>
</pkgmetadata>