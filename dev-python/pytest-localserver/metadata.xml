<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpsimg.shields.iopypivpytest-localserver.svgstyleflat    alt PyPI Version    target httpspypi.python.orgpypipytest-localserver.. image httpsimg.shields.iopypipyversionspytest-localserver.svg    alt Supported Python versions    target httpspypi.python.orgpypipytest-localserver.. image httpsimg.shields.iobadgecode20style-black-000000.svg    target httpsgithub.compsfblack.. image httpsresults.pre-commit.cibadgegithubpytest-devpytest-localservermaster.svg   target httpsresults.pre-commit.cilatestgithubpytest-devpytest-localservermaster   alt pre-commit.ci statuspytest-localserverpytest-localserver is a plugin for the pytest_ testing framework which enablesyou to test server connections locally.Sometimes monkeypatching_ urllib2.urlopen() just does not cut it, forinstance if you work with urllib2.Request, define your own openershandlersor work with httplib. In these cases it may come in handy to have an HTTPserver running locally which behaves just like the real thing [1]_. Well, lookno furtherQuickstartLets say you have a function to scrape HTML which only required to be pointedat a URL     import requests    def scrape(url)        html  requests.get(url).text         some parsing happens here         ...        return resultYou want to test this function in its entirety without having to rely on aremote server whose content you cannot control, neither do you want to wastetime setting up a complex mechanism to mock or patch the underlying Pythonmodules dealing with the actual HTTP request (of which there are more than oneBTW). So what do you doYou simply use pytests funcargs feature_ and simulate an entire serverlocally     def test_retrieve_some_content(httpserver)        httpserver.serve_content(open(cached-content.html).read())        assert scrape(httpserver.url)  Found itWhat happened here is that for the duration of your tests an HTTP server isstarted on a random port on localhost which will serve the content you tell itto and behaves just like the real thing.The added bonus is that you can test whether your code behaves gracefully ifthere is a network problem    def test_content_retrieval_fails_graciously(httpserver)        httpserver.serve_content(File not found, 404)        pytest.raises(ContentNotFoundException, scrape, httpserver.url)The same thing works for SMTP servers, too    def test_sending_some_message(smtpserver)        mailer  MyMailer(hostsmtpserver.addr[0], portsmtpserver.addr[1])        mailer.send(tobobexample.com, from_aliceexample.com,            subjectMyMailer v1.0, bodyCheck out my mailer)        assert len(smtpserver.outbox)1Here an SMTP server is started which accepts e-mails being sent to it. Thenice feature here is that you can actually check if the message was receivedand what was sent by looking into the smtpservers outbox.It is really that easyAvailable funcargsHere is a short overview of the available funcargs. For more details I suggestpoking around in the code itself.httpserver    provides a threaded HTTP server instance running on localhost. It has the    following attributes     code - HTTP response code (int)     content - content of next response (str, bytes, or iterable of either)     headers - response headers (dict)     chunked - whether to chunk-encode the response (enumeration)    Once these attributes are set, all subsequent requests will be answered with    these values until they are changed or the server is stopped. A more    convenient way to change these is         httpserver.serve_content(contentNone, code200, headersNone, chunkedpytest_localserver.http.Chunked.NO)    The chunked attribute or parameter can be set to     Chunked.YES, telling the server to always apply chunk encoding     Chunked.NO, telling the server to never apply chunk encoding     Chunked.AUTO, telling the server to apply chunk encoding only if      the Transfer-Encoding header includes chunked    If chunk encoding is applied, each str or bytes in content becomes one    chunk in the response.    The server address can be found in property     url    which is the string representation of tuple server_address (host as str,    port as int).    If you want to check which form fields have been POSTed, Try         httpserver.serve_content(..., show_post_varsTrue)    which will display them as parsable text.    If you need to inspect the requests sent to the server, a list of all    received requests can be found in property     requests    which is a list of werkzeug.wrappers.Request objects.httpsserver    is the same as httpserver only with SSL encryption.smtpserver    provides a threaded instance of smtpd.SMTPServer running on localhost.    It has the following attributes     addr - server address as tuple (host as str, port as int)     outbox - list of email.message.Message instances received.Using your a WSGI application as test serverAs of version 0.3 you can now use a WSGI application_ to run on the testserver     from pytest_localserver.http import WSGIServer    def simple_app(environ, start_response)        Simplest possible WSGI application        status  200 OK        response_headers  [(Content-type, textplain)]        start_response(status, response_headers)        return [Hello worldn]    pytest.fixture    def testserver(request)        Defines the testserver funcarg        server  WSGIServer(applicationsimple_app)        server.start()        request.addfinalizer(server.stop)        return server    def test_retrieve_some_content(testserver)        assert scrape(testserver.url)  Hello worldnHave a look at the following page for more information on WSGIhttpwsgi.readthedocs.orgenlatestlearn.htmlDownload and InstallationYou can install the plugin by running     pip install pytest-localserverAlternatively, get the latest stable version from PyPI_ or the latestbleeding-edge_ from Github.License and CreditsThis plugin is released under the MIT license. You can find the full text ofthe license in the LICENSE file.Copyright (C) 2010-2022 Sebastian Rahlf and others (see AUTHORS).Some parts of this package is based on ideas or code from other people- I borrowed some implementation ideas for the httpserver from linkchecker_.- The implementation for the SMTP server is based on the Mailsink recipe_ by  Adam Feuer, Matt Branthwaite and Troy Frever.- The HTTPS implementation is based on work by Sebastien Martini_.Thanks guysDevelopment and future plansFeel free to clone the repository and add your own changes. Pull requests arealways welcome    git clone httpsgithub.compytest-devpytest-localserverIf you find any bugs, please file a report_.Test can be run with tox.I already have a couple of ideas for future versions support for FTP, SSH (maybe base all on twisted) making the SMTP outbox as convenient to use as django.core.mail.outbox add your own herePreparing a release-------------------For package maintainers, here is how we release a new version. Ensure that the CHANGES file is up to date with the latest changes.. Make sure that all tests pass on the version you want to release.. Use the new release form on Github_ (or some other equivalent method) to   create a new release, following the pattern of previous releases.    Each release has to be based on a tag. You can either create the tag first     (e.g. using git tag) and then make a release from that tag, or you can     have Github create the tag as part of the process of making a release     either way works.    The tag name must be the PEP 440_-compliant version number prefixed     by v, making sure to include at least three version number components     (e.g. v0.6.0).    The Auto-generate release notes button will be useful in summarizing     the changes since the last release.. Using either the release workflows page_ or the link in the email you   received about a Deployment review, go to the workflow run created for   the new release and click Review deployments, then either approve or reject   the two deployments, one to Test PyPI and one to real PyPI. (It should not be   necessary to reject a deployment unless something really weird happens.)   Once the deployment is approved, Github will automatically upload the files.----.. [1] The idea for this project was born when I needed to check that a piece       of software_ behaved itself when receiving HTTP error codes 404 and 500.       Having unsuccessfully tried to mock a server, I stumbled across       linkchecker_ which uses a the same idea to test its internals... _monkeypatching httppytest.orglatestmonkeypatch.html.. _pytest httppytest.org.. _funcargs feature httppytest.orglatestfuncargs.html.. _linkchecker httplinkchecker.sourceforge.net.. _WSGI application httpwww.python.orgdevpepspep-0333.. _PyPI httppypi.python.orgpypipytest-localserver.. _bleeding-edge httpsgithub.compytest-devpytest-localserver.. _report httpsgithub.compytest-devpytest-localserverissues.. _tox httptestrun.orgtox.. _a piece of software httppypi.python.orgpypipython-amazon-product-api.. _Mailsink recipe httpcode.activestate.comrecipes440690.. _Sebastien Martini httpcode.activestate.comrecipes442473.. _PEP 440 httpspeps.python.orgpep-0440.. _build httpspypa-build.readthedocs.ioenlatest.. _twine httpstwine.readthedocs.ioenstable.. _new release form on Github httpsgithub.compytest-devpytest-localserverreleasesnew.. _release workflows page httpsgithub.compytest-devpytest-localserveractionsworkflowsrelease.yml</longdescription>
</pkgmetadata>