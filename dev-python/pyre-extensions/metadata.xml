<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Pyre ExtensionsThis module defines extensions to the standard typing module that are supported by the [Pyre typechecker](httpspypi.orgprojectpyre-check). none_throwsFunction to make assumptions about Optionals explicit. The function will raise anassertion error if passed None and return the value otherwise. ParameterSpecificationParameterSpecifications are a special kind of type variable that captures callable parameterspecifications (known as argspecs in the runtime and inspect library) instead of types, allowingthe typing of decorators which transform the return type of the given callable.For examplefrom typing import TypeVar, Callable, Listfrom pyre_extensions import ParameterSpecificationTParams  ParameterSpecification(TParams)TReturn  TypeVar(TReturn)def unwrap(f Callable[TParams, List[TReturn]]) - Callable[TParams, TReturn]    def inner(args TParams.args, kwargs TParams.kwargs) - TReturn        return f(args, kwargs)[0]    return innerunwrapdef foo(x int, y str, z bool  False) - List[int]    return [1, 2, 3]decorates foo into a callable that returns int, but still has the same parameters, including theirnames and whether they are required.These ParameterSpecification variables also have two special properties, args and kwargs,which correspond to the positional and keyword arguments for a specific call to theParameterSpecification function.  Because the division of parameters into these two argumentcollections can be different each invocation, these special annotations can only be used in onemanner together, in a function definition, as args and kwargs with no other parameterslisted. Safe JSONThe safe_json module provides a type-safe way to parse JSON. It is meant as a drop-in replacementfor the builtin json module but instead of returning an object of undefined shape (i.e. Any)allows you to specify the shape of the JSON youre expecting. The parser will validate whether theinput matches the expected type and raise an exception if it does not. ExamplesFor trivial JSON structures you can use builtin typespython from pyre_extensions import safe_json from typing import List, Dict safe_json.loads([1, 2, 3], List[int])[1, 2, 3] safe_json.loads([1, 2, 3], List[str]) Raises pyre_extensions.safe_json.InvalidJson safe_json.loads({key value}, Dict[str, str]){key value} safe_json.loads({key value}, Dict[str, int]) Raises pyre_extensions.safe_json.InvalidJsonFor more complicated, nested structures, typed dictionaries are the way to gopython from typing import TypedDict class Movie(TypedDict)...     name str...     year int... safe_json.loads({name Blade Runner, year 1982 }, Movie){name Blade Runner, year 1982} safe_json.loads({name Blade Runner, year 1982 }, Movie) Raises pyre_extensions.safe_json.InvalidJsonValidate if data is expected typepython from pyre_extensions import safe_json from typing import List, Dict data  {foo 23} safe_json.validate(data, Dict[str, str]) Raises pyre_extensions.safe_json.InvalidJson safe_json.validate(data, Dict[str, int]){foo 23}</longdescription>
</pkgmetadata>