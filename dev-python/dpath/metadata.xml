<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>dpath-pythonPyPIPython VersionBuild StatusGitterA python library for accessing and searching dictionaries viaslashedpaths ala xpathBasically it lets you glob over a dictionary as if it were a filesystem.It allows you to specify globs (ala the bash eglob syntax, through someadvanced fnmatch.fnmatch magic) to access dictionary elements, andprovides some facility for filtering those results.sdists are available on pypi httppypi.python.orgpypidpathInstallingThe best way to install dpath is via easy_install or pip.    easy_install dpath    pip install dpathUsing Dpath.. code-block python    import dpathSeparatorsAll of the functions in this library (except merge) accept aseparator argument, which is the character that should separate pathcomponents. The default is , but you can set it to whatever you want.SearchingSuppose we have a dictionary like this.. code-block python    x  {        a {            b {                3 2,                43 30,                c [],                d [red, buggy, bumpers],            }        }    }... And we want to ask a simple question, like Get me the value of thekey 43 in the b hash which is in the a hash. Thats easy... code-block pycon     help(dpath.get)    Help on function get in module dpath    get(obj, glob, separator)        Given an object which contains only one possible match for the given glob,        return the value for the leaf matching the given glob.        If more than one leaf matches the glob, ValueError is raised. If the glob is        not found, KeyError is raised.     dpath.get(x, ab43)    30Or you could say Give me a new dictionary with the values of allelements in x[a][b] where the key is equal to the glob [cd]. Okay... code-block pycon     help(dpath.search)    Help on function search in module dpath    search(obj, glob, yieldedFalse)    Given a path glob, return a dictionary containing all keys    that matched the given glob.    If yielded is true, then a dictionary will not be returned.    Instead tuples will be yielded in the form of (path, value) for    every element in the document that matched the glob.... Sounds easy.. code-block pycon     result  dpath.search(x, ab[cd])     print(json.dumps(result, indent4, sort_keysTrue))    {        a {            b {                c [],                d [                    red,                    buggy,                    bumpers                ]            }        }    }... Wow that was easy. What if I want to iterate over the results, andnot get a merged view.. code-block pycon     for x in dpath.search(x, ab[cd], yieldedTrue) print(x)    ...    (abc, [])    (abd, [red, buggy, bumpers])... Or what if I want to just get all the values back for the glob Idont care about the paths they were found at.. code-block pycon     help(dpath.values)    Help on function values in module dpath    values(obj, glob, separator, afilterNone, dirsTrue)    Given an object and a path glob, return an array of all values which match    the glob. The arguments to this function are identical to those of search(),    and it is primarily a shorthand for a list comprehension over a yielded    search call.     dpath.values(x, abd)    [red, buggy, bumpers]Example Setting existing keysLets use that same dictionary, and set keys like ab[cd] to thevalue Waffles... code-block pycon     help(dpath.set)    Help on function set in module dpath    set(obj, glob, value)    Given a path glob, set all existing elements in the document    to the given value. Returns the number of elements changed.     dpath.set(x, ab[cd], Waffles)    2     print(json.dumps(x, indent4, sort_keysTrue))    {        a {            b {                3 2,                43 30,                c Waffles,                d Waffles            }        }    }Example Adding new keysLets make a new key with the path abefg, set it to Roffle. Thisbehaves like mkdir -p in that it makes all the intermediate pathsnecessary to get to the terminus... code-block pycon     help(dpath.new)    Help on function new in module dpath    new(obj, path, value)    Set the element at the terminus of path to value, and create    it if it does not exist (as opposed to set that can only    change existing keys).    path will NOT be treated like a glob. If it has globbing    characters in it, they will become part of the resulting    keys     dpath.new(x, abefg, Roffle)     print(json.dumps(x, indent4, sort_keysTrue))    {        a {            b {                3 2,                43 30,                c Waffles,                d Waffles,                e {                    f {                        g Roffle                    }                }            }        }    }This works the way we expect with lists, as well. If you have a listobject and set index 10 of that list object, it will grow the listobject with None entries in order to make it big enough.. code-block pycon     dpath.new(x, abefh, [])     dpath.new(x, abefh13, Wow this is a big array, it sure is lonely in here by myself)     print(json.dumps(x, indent4, sort_keysTrue))    {        a {            b {                3 2,                43 30,                c Waffles,                d Waffles,                e {                    f {                        g Roffle,                        h [                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            Wow this is a big array, it sure is lonely in here by myself                        ]                    }                }            }        }    }HandyExample Deleting Existing KeysTo delete keys in an object, use dpath.delete, which accepts the same globbing syntax as the other methods... code-block pycon     help(dpath.delete)    delete(obj, glob, separator, afilterNone)        Given a path glob, delete all elements that match the glob.        Returns the number of deleted objects. Raises PathNotFound if        no paths are found to delete.Example MergingAlso, check out dpath.merge. The python dict update() method isgreat and all but doesnt handle merging dictionaries deeply. This onedoes... code-block pycon     help(dpath.merge)    Help on function merge in module dpath    merge(dst, src, afilterNone, flags4, _path)        Merge source into destination. Like dict.update() but performs        deep merging.        flags is an ORed combination of MergeType enum members.             ADDITIVE  List objects are combined onto one long              list (NOT a set). This is the default flag.             REPLACE  Instead of combining list objects, when              2 list objects are at an equal depth of merge, replace              the destination with the source.             TYPESAFE  When 2 keys at equal levels are of different              types, raise a TypeError exception. By default, the source              replaces the destination in this situation.     y  {a {b { e {f {h [None, 0, 1, None, 13, 14]}}}, c RoffleWaffles}}     print(json.dumps(y, indent4, sort_keysTrue))    {        a {            b {                e {                    f {                        h [                            null,                            0,                            1,                            null,                            13,                            14                        ]                    }                }            },            c RoffleWaffles        }    }     dpath.merge(x, y)     print(json.dumps(x, indent4, sort_keysTrue))    {        a {            b {                3 2,                43 30,                c Waffles,                d Waffles,                e {                    f {                        g Roffle,                        h [                            null,                            0,                            1,                            null,                            13,                            14,                            null,                            null,                            null,                            null,                            null,                            null,                            null,                            Wow this is a big array, it sure is lonely in here by myself                        ]                    }                }            },            c RoffleWaffles        }    }Now thats handy. You shouldnt try to use this as a replacement for thedeepcopy method, however - while merge does create new dict and listobjects inside the target, the terminus objects (strings and ints) arenot copied, they are just re-referenced in the merged object.FilteringAll of the methods in this library (except new()) support a afilterargument. This can be set to a function that will return True or Falseto say yes include that value in my result set or no dont includeit.Filtering functions receive every terminus node in a search - e.g.,anything that is not a dict or a list, at the very end of the path. Foreach value, they return True to include that value in the result set, orFalse to exclude it.Consider this example. Given the source dictionary, we want to find ALLkeys inside it, but we only really want the ones that contain ffle inthem.. code-block pycon     print(json.dumps(x, indent4, sort_keysTrue))    {        a {            b {                3 2,                43 30,                c Waffles,                d Waffles,                e {                    f {                        g Roffle                    }                }            }        }    }     def afilter(x)    ...     if ffle in str(x)    ...             return True    ...     return False    ...     result  dpath.search(x, , afilterafilter)     print(json.dumps(result, indent4, sort_keysTrue))    {        a {            b {                c Waffles,                d Waffles,                e {                    f {                      g Roffle                    }                }            }        }    }Obviously filtering functions can perform more advanced tests (regularexpressions, etc etc).Key NamesBy default, dpath only understands dictionary keys that are integers orstrings. String keys must be non-empty. You can change this behavior bysetting a library-wide dpath option.. code-block python    import dpath.options    dpath.options.ALLOW_EMPTY_STRING_KEYS  TrueAgain, by default, this behavior is OFF, and empty string keys willresult in dpath.exceptions.InvalidKeyName being thrown.Separator got you down Use lists as pathsThe default behavior in dpath is to assume that the path given is a string, which must be tokenized by splitting at the separator to yield a distinct set of path components against which dictionary keys can be individually glob tested. However, this presents a problem when you want to use paths that have a separator in their name the tokenizer cannot properly understand what you mean by abc if it is possible for  to exist as a valid character in a key name.To get around this, you can sidestep the whole filesystem path style, and abandon the separator entirely, by using lists as paths. All of the methods in dpath. support the use of a list instead of a string as a path. So for example.. code-block python    x  { a {bc 0}}    dpath.get([a, bc])   0dpath.segments  The Low-Level Backenddpath is where you want to spend your time this library has the friendlyfunctions that will understand simple string globs, afilter functions, etc.dpath.segments is the backend pathing library. It passes around tuples of pathcomponents instead of string globs... PyPI image httpsimg.shields.iopypivdpath.svgstyleflat    target httpspypi.python.orgpypidpath    alt PyPI Latest Version.. Python Version image httpsimg.shields.iopypipyversionsdpathstyleflat    target httpspypi.python.orgpypidpath    alt Supported Python Version.. Build Status image httpsgithub.comdpath-maintainersdpath-pythonactionsworkflowstests.ymlbadge.svg    target httpsgithub.comdpath-maintainersdpath-pythonactionsworkflowstests.yml   .. Gitter image httpsbadges.gitter.imdpath-pythonchat.svg    target httpsgitter.imdpath-pythonchatutm_sourcebadgeutm_mediumbadgeutm_campaignpr-badge    alt GitterContributorsWe would like to thank the community for their interest and involvement. Youhave all made this project significantly better than the sum of its parts, andyour continued feedback makes it better every day. Thank you so muchThe following authors have contributed to this project, in varying capacities+ Caleb Case calebcasegmail.com+ Andrew Kesterson andrewaklabs.net+ Marc Abramowitz marcmarc-abramowitz.com+ Richard Han xhh2aberkeley.edu+ Stanislav Ochotnicky sochotnickyredhat.com+ Misja Hoebe misjaconversify.com+ Gagandeep Singh gagandeep.2020gmail.com+ Alan Gibson alan.gibsongmail.comAnd many others If weve missed you please open an PR and add your name here.</longdescription>
</pkgmetadata>