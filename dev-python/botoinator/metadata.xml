<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Botoinator A python module that allows for declaring decorators to be added to boto3 methods Overview  Botoinator allows you to apply decorators to boto3 methods on either a class or object level. It works through boto3 sessions to allow you to apply decorators to either all clientsresources of a particular session, or to specific clientsresources of boto3.DEFAULT_SESSION. Generated documentationYou can see the pydoc generated documentation [HERE](httpsgithub.comQuiNovasbotoinatortreemasterdocumentationbotoinator.txt) Usage Decorate a method belonging to a client object to a single sessionpythonsession  boto3.session.Session()session.register_client_decorator(service_name, method_names, decorator)Arguments service_name -- the boto3 name (as a string) of the client to apply the decorator to. method_names -- one or more method names of the client to apply the decorator to. Single names can be a string, while multiple names can be a listtupleset. decorator -- the decorator function. Must be a function that takes a function and returns a function. The returned function must take (args, kwargs) as arguments. Decorate a method belonging to a resource object in a single sessionpythonsession  boto3.session.Session()session.register_resource_decorator(service_name, resource_name, method_names, decorator)Arguments service_name -- the boto3 name (as a string) of the service to apply the decorator to. resource_name -- the boto3 name of the resource of the service to apply the decorator to. method_names -- one or more method names of the resource to apply the decorator to. Single names can be a string, while multiple names should be a listtupleset. decorator -- the decorator function. Must be a function that takes a function and returns a function. The returned function must take (args, kwargs) as arguments Decorate a method for clients created in any sessionpythonboto3.session.Session.add_client_decorator(service_name, method_names, decorator)Arguments service_name -- the boto3 name (as a string) of the client to apply the decorator to. method_names -- one or more method names of the client to apply the decorator to. Single names can be a string, while multiple names should be a listtupleset. decorator -- the decorator function. Must be a function that takes a function and returns a function. The returned function must take (args, kwargs) as arguments. Decorate a method of a resource in all sessionspythonboto3.session.Session.add_resource_decorator(service_name, resource_name, method_names, decorator)Arguments service_name -- the boto3 name of the service to apply the decorator to. resource_name -- the boto3 name of the resource of the service to apply the decorator to. method_names -- one or more method names of the resource to apply the decorator to. Single names can be a string, while multiple names should be a listtupleset decorator -- the decorator function. Must be a function that takes a function and returns a function. The returned function must take (args, kwargs) as arguments Unregister a decorator so that future clients will not have their methods decorated. Clients that have already registered decorators to methods will retain their decoration.pythonsession  boto3.session.Session()session.unregister_client_decorator(service_name, method_names)Arguments service_name -- the boto3 name of the service to apply the decorator to. method_names -- one or more method names of the client to apply the decorator to. Single names can be a string, while multiple names should be a listtupleset Unregister a decorator so that future resources will not have their methods decorated. Resources that have already registered decorators to methods will retain their decoration.pythonsession  boto3.session.Session()session.unregister_resource_decorator(service_name, resource_name, method_names)Arguments service_name -- the boto3 name of the service to apply the decorator to. resource_name -- the boto3 name of the resource of the service to apply the decorator to. method_names -- one or more method names of the resource to apply the decorator to. Single names can be a string, while multiple names should be a listtupleset Undecorate a method for clients created in any session.pythonboto3.session.Session.remove_client_decorator(service_name, method_names)Arguments service_name -- the boto3 name (as a string) of the client to apply the decorator to. method_names -- one or more method names of the client to apply the decorator to. Single names can be a string, while multiple names should be a listtupleset. Undecorate a method of a resource in all sessionspythonboto3.session.Session.remove_resource_decorator(service_name, resource_name, method_names)Arguments service_name -- the boto3 name of the service to apply the decorator to. resource_name -- the boto3 name of the resource of the service to apply the decorator to. method_names -- one or more method names of the resource to apply the decorator to. Single names can be a string, while multiple names should be a listtupleset boto3 convienence methodsIf you use the boto3.client() or boto3.resource() methods, these create a default session object found at boto3.DEFAULT_SESSION.Changing the default sessions decorators requires using the register_xxx and unregister_xxx methods documented here.For example boto3.DEFAULT_SESSION.register_client_decorator(...). Example of decorating create_bucket() on a single boto3 sessionpythonimport boto3import botoinatorfrom moto import mock_s3, mock_sqs This is our decorator that we will apply to boto3 methods def myDecorator(func)  def test_decorator(args, kwargs)    setattr(test_decorator, testValue, True)  Add this attribute to the returned function for testing    return func(args, kwargs)  return test_decoratormock_s3def testRegisterToClient()    Test registering a decorator to a single boto3 session     Create a boto3 session  s  boto3.session.Session()   Register the create_bucket() method to use our decorator for this session  s.register_client_decorator(s3, create_bucket, myDecorator)   Now create our client as we normally would  client1  s.client(s3)   Now we can see that create_bucket() was decorated by testing the attribute we added  client1.create_bucket(Bucketfoo)  assert hasattr(client1.create_bucket, testValue)   We can also see that this only applies to calls made by the session we registered by creating a new session through boto3.client() and not registering a decorator  client2  boto3.client(s3)  client2.create_bucket(Bucketfoo)   Now we can see that client.create_bucket() is not decorated  assert not hasattr(client2.create_bucket, testValue)   Remove the decorator from the session  s.unregister_client_decorator(s3, create_bucket)   Now create a new client on the same session we created at first  client3  s.client(s3)  client3.create_bucket(Bucketbar)   The session should no longer be decorating methods for new clients  assert not hasattr(client3.create_bucket, testValue1) View [more examples in the project documentation directory](httpsgithub.comQuiNovasbotoinatortreemasterdocumentationexamples).</longdescription>
</pkgmetadata>