<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Redo - Utilities to retry Python callablesIntroductionRedo provides various means to add seamless ability to retry to any Python callable. Redo includes a plain function (redo.retry), a decorator (redo.retriable), and a context manager (redo.retrying) to enable you to integrate it in the best possible way for your project. As a bonus, a standalone interface is also included (retry).InstallationFor installing with pip, run following commands    pip install redoHow To UseBelow is the list of functions available retrier retry retriable retrying (contextmanager)retrier(attempts5, sleeptime10, max_sleeptime300, sleepscale1.5, jitter1)------------------------------------------------------------------------------A generator function that sleeps between retries, handles exponential back off and jitter. The action you are retrying is meant to run after retrier yields. At each iteration, we sleep for sleeptime + random.randint(-jitter, jitter). Afterwards sleeptime is multiplied by sleepscale for the next iteration.Arguments Detail    1. attempts (int) maximum number of times to try defaults to 52. sleeptime (float) how many seconds to sleep between tries defaults to 60s (one minute)3. max_sleeptime (float) the longest well sleep, in seconds defaults to 300s (five minutes)4. sleepscale (float) how much to multiply the sleep time by each iteration defaults to 1.55. jitter (int) random jitter to introduce to sleep time each iteration. the amount is chosen at random between [-jitter, +jitter] defaults to 1Output None, a maximum of attempts number of timesExample     n  0     for _ in retrier(sleeptime0, jitter0)    ...     if n  3    ...          We did the thing    ...         break    ...     n + 1     n    3     n  0     for _ in retrier(sleeptime0, jitter0)    ...     if n  6    ...          We did the thing    ...         break    ...     n + 1    ... else    ...     print(max tries hit)    max tries hitretry(action, attempts5, sleeptime60, max_sleeptime5  60, sleepscale1.5, jitter1, retry_exceptions(Exception,), cleanupNone, args(), kwargs{})  --------------------------------------------------------------------------------------------------------------------------------------------------------Calls an action function until it succeeds, or we give up.Arguments Detail  1. action (callable) the function to retry2. attempts (int) maximum number of times to try defaults to 53. sleeptime (float) how many seconds to sleep between tries defaults to 60s (one minute)4. max_sleeptime (float) the longest well sleep, in seconds defaults to 300s (five minutes)5. sleepscale (float) how much to multiply the sleep time by each iteration defaults to 1.56. jitter (int) random jitter to introduce to sleep time each iteration. The amount is chosen at random between [-jitter, +jitter] defaults to 17. retry_exceptions (tuple) tuple of exceptions to be caught. If other exceptions are raised by action(), then these are immediately re-raised to the caller.8. cleanup (callable) optional called if one of retry_exceptions is caught. No arguments are passed to the cleanup function if your cleanup requires arguments, consider using functools.partial or a lambda function.9. args (tuple) positional arguments to call action with10. kwargs (dict) keyword arguments to call action withOutput Whatever action(args, kwargs) returns Output Whatever action(args, kwargs) raises. retry_exceptions are caught up until the last attempt, in which case they are re-raised.Example     count  0     def foo()    ...     global count    ...     count + 1    ...     print(count)    ...     if count  3    ...         raise ValueError(count is too small)    ...     return success     retry(foo, sleeptime0, jitter0)    1    2    3    successretriable(retry_args, retry_kwargs)-----------------------------------------A decorator factory for retry(). Wrap your function in retriable(...) to give it retry powersArguments Detail Same as for retry, with the exception of action, args, and kwargs, which are left to the normal function definition.Output A function decoratorExample     count  0     retriable(sleeptime0, jitter0)    ... def foo()    ...     global count    ...     count + 1    ...     print(count)    ...     if count  3    ...         raise ValueError(count too small)    ...     return success     foo()    1    2    3    successretrying(func, retry_args, retry_kwargs)----------------------------------------------A context manager for wrapping functions with retry functionality.Arguments Detail   1. func (callable) the function to wrap other arguments as per retryOutput A context manager that returns retriable(func) on __enter__Example     count  0     def foo()    ...     global count    ...     count + 1    ...     print(count)    ...     if count  3    ...         raise ValueError(count too small)    ...     return success     with retrying(foo, sleeptime0, jitter0) as f    ...     f()    1    2    3    success</longdescription>
</pkgmetadata>