<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> django-test-plusUseful additions to Djangos default TestCase from [REVSYS](httpswww.revsys.com)[[pypi](httpsimg.shields.iopypivdjango-test-plus.svg)](httpspypi.orgprojectdjango-test-plus)[[build matrix demo](httpsgithub.comrevsysdjango-test-plusactionsworkflowsactions.ymlbadge.svg)](httpsgithub.comrevsysdjango-test-plusactionsworkflowsactions.yml) RationaleLets face it, writing tests isnt always fun. Part of the reason forthat is all of the boilerplate you end up writing. django-test-plus isan attempt to cut down on some of that when writing Django tests. Weguarantee it will increase the time before you get carpal tunnel by atleast 3 weeks SupportSupports Python 3.6, 3.7, 3.8, 3.9, and 3.10.Supports Django Versions 2.0, 2.1, 2.2, 3.0, 3.1. and 3.2. DocumentationFull documentation is available at httpdjango-test-plus.readthedocs.org Installationshell pip install django-test-plus UsageTo use django-test-plus, have your tests inherit from test_plus.test.TestCase rather than the normal django.test.TestCasepythonfrom test_plus.test import TestCaseclass MyViewTests(TestCase)    ...This is sufficient to get things rolling, but you are encouraged tocreate your own sub-classes for your projects. This will allow youto add your own project-specific helper methods.For example, if you have a django project named myproject, you mightcreate the following in myprojecttest.pypythonfrom test_plus.test import TestCase as PlusTestCaseclass TestCase(PlusTestCase)    passAnd then in your tests usepythonfrom myproject.test import TestCaseclass MyViewTests(TestCase)    ...This import, which is similar to the way you would import Djangos TestCase, is also validpythonfrom test_plus import TestCase pytest UsageYou can get a TestCase like object as a pytest fixture now by asking for tp. All of the methods below would then work in pytest functions. Forexamplepythondef test_url_reverse(tp)    expected_url  api    reversed_url  tp.reverse(api)    assert expected_url  reversed_urlThe tp_api fixture will provide a TestCase that uses django-rest-frameworks APIClient()pythondef test_url_reverse(tp_api)    response  tp_api.client.post(myapi, formatjson)    assert response.status_code  200 Methods reverse(url_name, args, kwargs)When testing views you often find yourself needing to reverse the URLs name. With django-test-plus there is no need for the from django.core.urlresolvers import reverse boilerplate. Instead, usepythondef test_something(self)    url  self.reverse(my-url-name)    slug_url  self.reverse(name-takes-a-slug, slugmy-slug)    pk_url  self.reverse(name-takes-a-pk, pk12)As you can see our reverse also passes along any args or kwargs you needto pass in. get(url_name, followTrue, args, kwargs)Another thing you do often is HTTP get urls. Our get() methodassumes you are passing in a named URL with any args or kwargs necessaryto reverse the url_name.If needed, place kwargs for TestClient.get() in an extra dictionary.pythondef test_get_named_url(self)    response  self.get(my-url-name)     Get XML data via AJAX request    xml_response  self.get(        my-url-name,        extra{HTTP_X_REQUESTED_WITH XMLHttpRequest})When using this get method two other things happen for you we store thelast response in self.last_response and the responses Context in self.context.So instead ofpythondef test_default_django(self)    response  self.client.get(reverse(my-url-name))    self.assertTrue(foo in response.context)    self.assertEqual(response.context[foo], 12)You can writepythondef test_testplus_get(self)    self.get(my-url-name)    self.assertInContext(foo)    self.assertEqual(self.context[foo], 12)Its also smart about already reversed URLs, so you can be lazy and dopythondef test_testplus_get(self)    url  self.reverse(my-url-name)    self.get(url)    self.response_200()If you need to pass query string parameters to your url name, you can do so like this. Assuming the name search maps to search thenpythondef test_testplus_get_query(self)    self.get(search, data{query testing})Would GET searchquerytesting. post(url_name, data, followTrue, args, kwargs)Our post() method takes a named URL, an optional dictionary of data you wishto post and any args or kwargs necessary to reverse the url_name.If needed, place kwargs for TestClient.post() in an extra dictionary.pythondef test_post_named_url(self)    response  self.post(my-url-name, data{coolness-factor 11.0},                         extra{HTTP_X_REQUESTED_WITH XMLHttpRequest})NOTE Along with the frequently used get and post, we support all of the HTTP verbs such as put, patch, head, trace, options, and delete in the same fashion. get_context(key)Often you need to get things out of the template contextpythondef test_context_data(self)    self.get(my-view-with-some-context)    slug  self.get_context(slug) assertInContext(key)You can ensure a specific key exists in the last responses context byusingpythondef test_in_context(self)    self.get(my-view-with-some-context)    self.assertInContext(some-key) assertContext(key, value)We can get context values and ensure they exist, but we can also testequality while were at it. This asserts that key  valuepythondef test_in_context(self)    self.get(my-view-with-some-context)    self.assertContext(some-key, expected value) assert_http__status_name(response, msgNone) - status code checkingAnother test you often need to do is check that a response has a certainHTTP status code. With Djangos default TestCase you would writepythonfrom django.core.urlresolvers import reversedef test_status(self)    response  self.client.get(reverse(my-url-name))    self.assertEqual(response.status_code, 200)With django-test-plus you can shorten that to bepythondef test_better_status(self)    response  self.get(my-url-name)    self.assert_http_200_ok(response)Django-test-plus provides a majority of the status codes assertions for you. The status assertionscan be found in their own [mixin](httpsgithub.comrevsysdjango-test-plusblobmaintest_plusstatus_codes.py)and should be searchable if youre using an IDE like pycharm. It should be noted that in previousversions, django-test-plus had assertion methods in the pattern of response_(), which are stillavailable but have since been deprecated. See below for a list of those methods. Each of the assertion methods takes an optional Django test client response and a string msg argumentthat, if specified, is used as the error message when a failure occurs. The methods,assert_http_301_moved_permanently and assert_http_302_found also take an optional url argument thatif passed, will check to make sure the response.url matches.If its available, the assert_http__status_name methods will use the last response. So youcan dopythondef test_status(self)    self.get(my-url-name)    self.assert_http_200_ok()Which is a bit shorter.The response_() methods that are deprecated, but still available for use, include - response_200()- response_201() - response_204() - response_301()- response_302() - response_400()- response_401() - response_403()- response_404() - response_405()- response_409()- response_410()All of which take an optional Django test client response and a str msg argument that, if specified, is used as the error message when a failure occurs. Just like the assert_http__status_name() methods, these methods will use the last response if its available.  get_check_200(url_name, args, kwargs)GETing and checking views return status 200 is a common test. This method makes it more convenientpythondef test_even_better_status(self)    response  self.get_check_200(my-url-name) make_user(usernametestuser, passwordpassword, permsNone)When testing out views you often need to create various users to ensureall of your logic is safe and sound. To make this process easier, thismethod will create a user for youpythondef test_user_stuff(self)    user1  self.make_user(u1)    user2  self.make_user(u2)If creating a User in your project is more complicated, say for exampleyou removed the username field from the default Django Auth model,you can provide a [FactoryBoy](httpsfactoryboy.readthedocs.orgenlatest) factory to createit or override this method on your own sub-class.To use a Factory Boy factory, create your class like thispythonfrom test_plus.test import TestCasefrom .factories import UserFactoryclass MySpecialTest(TestCase)    user_factory  UserFactory    def test_special_creation(self)        user1  self.make_user(u1)NOTE Users created by this method will have their passwordset to the string password by default, in order to ease testing.If you need a specific password, override the password parameter.You can also pass in user permissions by passing in a string ofapp_name.perm name or app_name..  For examplepythonuser2  self.make_user(perms[myapp.create_widget, otherapp.]) print_form_errors(response_or_formNone)When debugging a failing test for a view with a form, this method helps youquickly look at any form errors.Example usagepythonclass MyFormTest(TestCase)    self.post(my-url-name, data{})    self.print_form_errors()     or    resp  self.post(my-url-name, data{})    self.print_form_errors(resp)     or    form  MyForm(data{})    self.print_form_errors(form) Authentication Helpers assertLoginRequired(url_name, args, kwargs)This method helps you test that a given named URL requires authorizationpythondef test_auth(self)    self.assertLoginRequired(my-restricted-url)    self.assertLoginRequired(my-restricted-object, pk12)    self.assertLoginRequired(my-restricted-object, slugsomething) login contextAlong with ensuring a view requires login and creating users, the nextthing you end up doing is logging in as various users to test yourrestriction logicpythondef test_restrictions(self)    user1  self.make_user(u1)    user2  self.make_user(u2)    self.assertLoginRequired(my-protected-view)    with self.login(usernameuser1.username, passwordpassword)        response  self.get(my-protected-view)         Test user1 sees what they should be seeing    with self.login(usernameuser2.username, passwordpassword)        response  self.get(my-protected-view)         Test user2 see what they should be seeingSince were likely creating our users using make_user() from above,the login context assumes the password is password unless specifiedotherwise. Therefore you you can dopythondef test_restrictions(self)    user1  self.make_user(u1)    with self.login(usernameuser1.username)        response  self.get(my-protected-view)We can also derive the username if were using make_user() so we canshorten that up even further like thispythondef test_restrictions(self)    user1  self.make_user(u1)    with self.login(user1)        response  self.get(my-protected-view) Ensuring low query counts assertNumQueriesLessThan(number) - contextDjango provides[assertNumQueries](httpsdocs.djangoproject.comen1.8topicstestingtoolsdjango.test.TransactionTestCase.assertNumQueries)which is great when your code generates a specific number ofqueries. However, if this number varies due to the nature of your data, with this method you can still test to ensure the code doesnt start producing a tonmore queries than you expectpythondef test_something_out(self)    with self.assertNumQueriesLessThan(7)        self.get(some-view-with-6-queries) assertGoodView(url_name, args, kwargs)This method does a few things for you. It- Retrieves the name URL- Ensures the view does not generate more than 50 queries- Ensures the response has status code 200- Returns the responseOften a wide, sweeping test like this is better than no test at all. Youcan use it like thispythondef test_better_than_nothing(self)    response  self.assertGoodView(my-url-name) Testing DRF viewsTo take advantage of the convenience of DRFs test client, you can create a subclass of TestCase and set the client_class propertypythonfrom test_plus import TestCasefrom rest_framework.test import APIClientclass APITestCase(TestCase)    client_class  APIClientFor convenience, test_plus ships with APITestCase, which does just thatpythonfrom test_plus import APITestCaseclass MyAPITestCase(APITestCase)    def test_post(self)        data  {testing {prop value}}        self.post(view-json, datadata, extra{format json})        self.assert_http_200_ok()Note that using APITestCase requires Django  1.8 and having installed django-rest-framework. Testing class-based generic viewsThe TestCase methods get() and post() work for both function-basedand class-based views. However, in doing so they invoke DjangosURL resolution, middleware, template processing, and decorator systems.For integration testing this is desirable, as you want to ensure yourURLs resolve properly, view permissions are enforced, etc.For unit testing this is costly because all these Django requestresponsesystems are invoked in addition to your method, and they typically do notaffect the end result.Class-based views (derived from Djangos generic.models.View class)contain methods and mixins which makes granular unit testing (more) feasible.Quite often your usage of a generic view class comprises an overrideof an existing method. Invoking the entire view and the Django requestresponsestack is a waste of time when you really want to call the overriddenmethod directly and test the result.CBVTestCase to the rescueAs with TestCase above, have your tests inheritfrom test_plus.test.CBVTestCase rather than TestCase like sopythonfrom test_plus.test import CBVTestCaseclass MyViewTests(CBVTestCase) Methods get_instance(cls, initkwargsNone, requestNone, args, kwargs)This core method simplifies the instantiation of your class, giving youa way to invoke class methods directly.Returns an instance of cls, initialized with initkwargs.Sets request, args, and kwargs attributes on the class instance.args and kwargs are the same values you would pass to reverse().Sample usagepythonfrom django.views import genericfrom test_plus.test import CBVTestCaseclass MyClass(generic.DetailView)    def get_context_data(self, kwargs)        kwargs[answer]  42        return kwargsclass MyTests(CBVTestCase)    def test_context_data(self)        my_view  self.get_instance(MyClass, {object some_object})        context  my_view.get_context_data()        self.assertEqual(context[answer], 42) get(cls, initkwargsNone, args, kwargs)Invokes cls.get() and returns the response, rendering template if possible.Builds on the CBVTestCase.get_instance() foundation.All test_plus.test.TestCase methods are valid, so the following workspythonresponse  self.get(MyClass)self.assertContext(my_key, expected_value)All test_plus TestCase side-effects are honored and all test_plusTestCase assertion methods work with CBVTestCase.get().NOTE This method bypasses Djangos middleware, and therefore contextvariables created by middleware are not available. If this affects yourtemplatecontext testing, you should use TestCase instead of CBVTestCase. post(cls, dataNone, initkwargsNone, args, kwargs)Invokes cls.post() and returns the response, rendering template if possible.Builds on the CBVTestCase.get_instance() foundation.Examplepythonresponse  self.post(MyClass, data{search_term revsys})self.response_200(response)self.assertContext(company_name, RevSys)All test_plus TestCase side-effects are honored and all test_plusTestCase assertion methods work with CBVTestCase.post().NOTE This method bypasses Djangos middleware, and therefore contextvariables created by middleware are not available. If this affects yourtemplatecontext testing you should use TestCase instead of CBVTestCase. get_check_200(cls, initkwargsNone, args, kwargs)Works just like TestCase.get_check_200().Caller must provide a view class instead of a URL name or path parameter.All test_plus TestCase side-effects are honored and all test_plusTestCase assertion methods work with CBVTestCase.post(). assertGoodView(cls, initkwargsNone, args, kwargs)Works just like TestCase.assertGoodView().Caller must provide a view class instead of a URL name or path parameter.All test_plus TestCase side-effects are honored and all test_plusTestCase assertion methods work with CBVTestCase.post(). DevelopmentTo work on django-test-plus itself, clone this repository and run the following commandsshell pip install -r requirements.txt pip install -e .NOTE You will also need to ensure that the test_project directory, locatedat the root of this repo, is in your virtualenvs path. Keep in touchIf you have a question about this project, please open a GitHub issue. If you love us and want to keep track of our goings-on, heres where you can find us onlinea hrefhttpsrevsys.comutm_mediumgithubutm_sourcedjango-test-plusimg srchttpspbs.twimg.comprofile_images915928618840285185sUdRGIn1_400x400.jpg height50 aa hrefhttpstwitter.comrevsysimg srchttpscdn1.iconfinder.comdataiconsnew_twitter_icon256bird_twitter_new_simple.png height43 aa hrefhttpswww.facebook.comrevsysllcimg srchttpscdn3.iconfinder.comdataiconspicons-social5706-facebook-512.png height50 aa hrefhttpsgithub.comrevsysimg srchttpsassets-cdn.github.comimagesmoduleslogos_pageGitHub-Mark.png height53 aa hrefhttpsgitlab.comrevsysimg srchttpsupload.wikimedia.orgwikipediacommonsthumb118GitLab_Logo.svg2000px-GitLab_Logo.svg.png height44 a</longdescription>
</pkgmetadata>