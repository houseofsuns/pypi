<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Ansimarkup.. class no-web no-pdfpypi build licenseAnsimarkup is an XML-like markup for producing colored terminal text... code-block python  from ansimarkup import ansiprint as print  print(bbold textb))  print(redred textred, red,greenred text on a green backgroundred,green)  print(fg ffaf00orange textfg ffaf00)Installation------------The latest stable version of ansimarkup can be installed from pypi.. code-block bash   pip install ansimarkupUsage-----Basic.. code-block python  from ansimarkup import parse, ansiprint   parse() converts the tags to the corresponding ansi escape sequence.  parse(bboldb ddimd)   ansiprint() works exactly like print(), but first runs parse() on all arguments.  ansiprint(bboldb, ddimd)  ansiprint(bboldb, ddimd, sep, filesys.stderr)Colors and styles.. code-block python   Colors may be specified in one of several ways.  parse(redred foregroundred)  parse(REDred backgroundRED)  parse(fg redred foregroundfg red)  parse(bg redred backgroundbg red)   Xterm, hex and rgb colors are accepted by the fg and bg tags.  parse(fg 86aquamarine foregroundfg 86)  parse(bg 00005fdark blue backgroundbg 00005f)  parse(fg 0,95,0dark green foregroundfg 0,95,0)   Tags may be nested.  parse(rYred text on a yellow foregroundYr)   The above may be more concisely written as  parse(r,yred text on a yellow backgroundr,y)   This shorthand also supports style tags.  parse(b,r,ybold red text on a yellow backgroundb,r,y)  parse(b,r,bold red textb,r,)  parse(b,,ybold regular text on a yellow backgroundb,,y)   Unrecognized tags are left as-is.  parse(belement1element1b)For a list of markup tags, please refer to tags.py_.User-defined tagsCustom tags or overrides for existing tags may be defined by creating a newAnsiMarkup instance.. code-block python  from ansimarkup import AnsiMarkup, parse  user_tags  {       Add a new tag (e.g. we want info to expand to boldgreen).      info parse(bg)       The ansi escape sequence can be used directly.      info ex1b[32mx1b[1m,       Tag names may also be callables.      err  lambda parse(r)       Colors may also be given convenient tag names.      orange parse(fg d78700),       User-defined tags always take precedence over existing tags.      bold parse(dim)  }  am  AnsiMarkup(tagsuser_tags)  am.parse(infobold greeninfo)  am.ansiprint(errrederr)   Calling the instance is equivalent to calling its parse method.  am(bboldb)  am.parse(bboldb)Alignment and lengthAligning formatted strings can be challenging because the length of the renderedstring is different that the number of printable characters. Consider this example.. code-block python   a   {30} .format(abc)   b   {30} .format(parse(babcb))   print(a, b, sepn)   abc                       abc                            This can be addressed by using the ansistring function or theAnsiMarkup.string(markup) method, which has the following useful properties.. code-block python   s  ansistring(babcb)   print(repr(s), -, s)  babcb - abc   abc is printed in bold   len(s), len(am.parse(babcb), s.delta  3, 11, 8With the help of the delta property, it is easy to align the strings in theabove example.. code-block python   s  ansistring(babcb)   a   {{width}} .format(abc, width30)   b   {{width}} .format(s, width(30 + s.delta))   print(a, b, sepn)   abc                               abc                            Other featuresThe default tag separators can be changed by passing the tag_sep argument toAnsiMarkup.. code-block python  from ansimarkup import AnsiMarkup  am  AnsiMarkup(tag_sep{})  am.parse({b}{r}bold red{b}{r})Markup tags can be removed using the strip() method.. code-block python  from ansimarkup import AnsiMarkup  am  AnsiMarkup()  am.strip(brbold redbr)The strict option instructs the parser to raise MismatchedTag if openingtags dont have corresponding closing tags.. code-block python  from ansimarkup import AnsiMarkup  am  AnsiMarkup(strictTrue)  am.parse(rbbold red)   ansimarkup.MismatchedTag opening tag r has no corresponding closing tagCommand-lineAnsimarkup may also be used on the command-line. This works as if allarguments were passed to ansiprint()   python -m ansimarkup                                                                                                                                      [270121  429PM 0 0s]  Usage python -m ansimarkup [arg [arg ...]]  Example usage    python -m ansimarkup bBoldb rRedr    python -m ansimarkup brBold Redrb    python -m ansimarkup  input-with-markup.txt    echo bBoldb  python -m ansimarkupLogging formatterAnsimarkup also comes with a formatter for the standard library loggingmodule. It can be used as.. code-block python  import logging  from ansimarkup.logformatter import AnsiMarkupFormatter  log  logging.getLogger()  hdl  logging.StreamHandler()  fmt  AnsiMarkupFormatter()  hdl.setFormatter(fmt)  log.addHandler(hdl)  log.info(bbold textb)WindowsAnsimarkup uses the colorama_ library internally, which means that Windowssupport for ansi escape sequences is available by first running.. code-block python  import colorama  colorama.init()For more information on Windows support, consult the Usage section of thecolorama_ documentation.Performance-----------While the focus of ansimarkup is convenience, it does try to keep processing toa minimum. The benchmark.py_ script attempts to benchmark different ansiescape code libraries  Benchmark 1 rbred boldbr    colorama     0.2998 s    termcolor    3.2339 s    colr         3.6483 s    ansimarkup   6.8679 s    pastel       28.8538 s    plumbum      53.5004 s  Benchmark 2 rbred boldbredrbboldb    colorama     0.8269 s    termcolor    8.9296 s    ansimarkup   9.3099 s    colr         9.6244 s    pastel       62.2018 s    plumbum      120.8048 sLimitations-----------Ansimarkup is a simple wrapper around colorama. It does very little in the wayof validating that markup strings are well-formed. This is a conscious decisionwith the goal of keeping things simple and fast.Unbalanced nesting, such as in the following example, will produce incorrectoutput  rY1r2YTodo----- Many corner cases remain to be fixed.- More elaborate testing. The current test suite mostly covers the  happy paths.- Replace tag_list.index in sub_end with something more  efficient (i.e. something like an ordered MultiDict).Similar libraries------------------ pastel_ bring colors to your terminal- plumbum.colors_ small yet feature-rich library for shell script-like programs in Python- colr_ easy terminal colors, with chainable methodsLicense-------Ansimarkup is released under the terms of the Revised BSD License_... pypi image httpsimg.shields.iopypivansimarkup.svgstyleflat-squarelabellatest20stable20version    target httpspypi.python.orgpypiansimarkup    alt Latest version released on PyPi.. license image httpsimg.shields.iopypilansimarkup.svgstyleflat-squarelabellicense    target httpspypi.python.orgpypiansimarkup    alt BSD 3-Clause.. build image httpsimg.shields.iotravisgvalkovpython-ansimarkupmaster.svgstyleflat-squarelabelbuild    target httptravis-ci.orggvalkovpython-ansimarkup    alt Build status.. _tags.py        httpsgithub.comgvalkovpython-ansimarkupblobmasteransimarkuptags.py.. _benchmark.py   httpsgithub.comgvalkovpython-ansimarkupblobmastertestsbenchmark.py.. _colorama       httpspypi.python.orgpypicolorama.. _pastel         httpsgithub.comsdispaterpastel.. _plumbum.colors httpsplumbum.readthedocs.ioenlatestcli.htmlcolors.. _colr           httpspypi.python.orgpypiColr.. _Revised BSD License httpsraw.github.comgvalkovpython-ansimarkupmasterLICENSE</longdescription>
</pkgmetadata>