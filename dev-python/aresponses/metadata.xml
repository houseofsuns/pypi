<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> aresponses[[image](httpsimg.shields.iopypivaresponses.svg)](httpspypi.orgprojectaresponses)[[image](httpsimg.shields.iopypipyversionsaresponses.svg)](httpspypi.orgprojectaresponses)[[build status](httpsgithub.comCircleUparesponsesworkflowsPython20Checksbadge.svg)](httpsgithub.comCircleUparesponsesactionsquerybranch3Amaster)[[Code style black](httpsimg.shields.iobadgecode20style-black-000000.svg)](httpsgithub.comambvblack)an asyncio testing server for mocking external services Features - Fast mocks using actual network connections - allows mocking some types of network issues - use regular expression matching for domain, path, method, or body - works with https requests as well (by switching them to http requests) - works with callables UsageAdd routes and responses via the aresponses.add methodpythondef add(    host_patternANY,     path_patternANY,     method_patternANY,     response,     ,     routeNone,     body_patternANY, m    match_querystringFalse,     repeat1    )When a request is received the first matching response will be returnedand removed from the routing table.  The response argument can beeither a string, Response, dict, or list.  Use aresponses.Responsewhen you need to do something more complex.Note that version 2.0 requires explicit assertionspythonpytest.mark.asyncioasync def test_simple(aresponses)    aresponses.add(google.com, apiv1, GET, responseOK)    aresponses.add(foo.com, , get, aresponses.Response(texterror, status500))    async with aiohttp.ClientSession() as session        async with session.get(httpgoogle.comapiv1) as response            text  await response.text()            assert text  OK        async with session.get(httpsfoo.com) as response            text  await response.text()            assert text  error    aresponses.assert_plan_strictly_followed() AssertionsIn aresponses 1.x requests that didnt match a route stopped the eventloop and thus forced an exception.  In aresponses 2.x its required tomake assertions at the end of the test.There are three assertions functions provided- aresponses.assert_no_unused_routes Raises UnusedRouteError if allthe routes defined were not used up.- aresponses.assert_called_in_order - Raises UnorderedRouteCallErrorif the routes werent called in the order they were defined.- aresponses.assert_all_requests_matched - Raises NoRouteFoundErrorif any requests were made that didnt match to a route.  Its likelybut not guaranteed that your code will throw an exception in thissituation before the assertion is reached.Instead of calling these individually, its recommended to callaresponses.assert_plan_strictly_followed() at the end of each test asit runs all three of the above assertions. Regex and Repeathost_pattern, path_pattern, method_pattern and body_pattern maybe either strings (exact match) or regular expressions.The repeat argument permits a route to be used multiple times.If you want to just blanket mock a service, without concern for how manytimes its called you could set repeat to a large number and not callaresponses.assert_plan_strictly_followed orarespones.assert_no_unused_routes.pythonpytest.mark.asyncioasync def test_regex_repetition(aresponses)    aresponses.add(re.compile(r..google.com), responseOK, repeat2)    async with aiohttp.ClientSession() as session        async with session.get(httpgoogle.com) as response            text  await response.text()            assert text  OK        async with session.get(httpapi.google.com) as response            text  await response.text()            assert text  OK    aresponses.assert_plan_strictly_followed() Json ResponsesAs a convenience, if a dict or list is passed to response then it willcreate a json response. A aiohttp.web_response.json_response objectcan be used for more complex situations.pythonpytest.mark.asyncioasync def test_json(aresponses)    aresponses.add(google.com, apiv1, GET, response{status OK})    async with aiohttp.ClientSession() as session        async with session.get(httpgoogle.comapiv1) as response            assert {status OK}  await response.json()    aresponses.assert_plan_strictly_followed() Custom HandlerCustom functions can be used for whatever other complex logic isdesired. In example below the handler is set to repeat infinitelyand always return 500.pythonimport mathpytest.mark.asyncioasync def test_handler(aresponses)    def break_everything(request)        return aresponses.Response(status500, textstr(request.url))    aresponses.add(responsebreak_everything, repeatmath.inf)    async with aiohttp.ClientSession() as session        async with session.get(httpgoogle.comapiv1) as response            assert response.status  500 PassthroughPass aresponses.passthrough into the response argument to allow arequest to bypass mocking.python    aresponses.add(httpstat.us, 200, get, aresponses.passthrough) Inspecting historyHistory of calls can be inspected via aresponses.history which returnsthe namedTuple RoutingLog(request, route, response)pythonpytest.mark.asyncioasync def test_history(aresponses)    aresponses.add(responsearesponses.Response(texthi), repeat2)    async with aiohttp.ClientSession() as session        async with session.get(httpfoo.comb) as response            await response.text()        async with session.get(httpbar.coma) as response            await response.text()    assert len(aresponses.history)  2    assert aresponses.history[0].request.host  foo.com    assert aresponses.history[1].request.host  bar.com    assert Route( in repr(aresponses.history[0].route)    aresponses.assert_plan_strictly_followed() Context manager usagepythonimport aiohttpimport pytestimport aresponsespytest.mark.asyncioasync def test_foo(event_loop)    async with aresponses.ResponsesMockServer(loopevent_loop) as arsps        arsps.add(foo.com, , get, hi there)        arsps.add(arsps.ANY, , get, arsps.Response(texthey))        async with aiohttp.ClientSession(loopevent_loop) as session            async with session.get(httpfoo.com) as response                text  await response.text()                assert text  hi            async with session.get(httpsgoogle.com) as response                text  await response.text()                assert text  hey working with [pytest-aiohttp](httpsgithub.comaio-libspytest-aiohttp)If you need to use aresponses together with pytest-aiohttp, you should re-initialize main aresponses fixture with loop fixturepythonfrom aresponses import ResponsesMockServerpytest.fixtureasync def aresponses(loop)    async with ResponsesMockServer(looploop) as server        yield serverIf youre trying to use the aiohttp_client test fixture then youll need to mock out the aiohttp loop fixtureinsteadpythonpytest.fixturedef loop(event_loop)    replace aiohttp loop fixture with pytest-asyncio fixture    return event_loop Contributing Dev environment setup  - install pyenv and pyenv-virtualenv  - Makes it easy to install specific versions of python and switch between them. Make sure you install the virtualenv bash hook  - git clone the repo and cd into it.  - make init - installs proper version of python, creates the virtual environment, activates it and installs all the requirements Submitting a feature request    - git checkout -b my-feature-branch   - make some cool changes  - make autoformat  - make test  - make lint  - create pull request Updating package on pypi  - make deploy Changelog 2.1.6- fix incorrect pytest plugin entrypoint name (72) 2.1.5- support asyncio_mode  strict (68) 2.1.4- fix dont assume utf8 request contents 2.1.3- accidental no-op release 2.1.2- documentation add pypi documentation 2.1.1- bugfix RecursionError when aresponses is used in more than 1000 tests (63) 2.1.0- feature add convenience method add_local_passthrough- bugfix fix https subrequest mocks. support aiohttp_client compatibility 2.0.2- bugfix ensure request body is available in history 2.0.0Warning Breaking Changes- breaking change require explicit assertions for test failures- feature autocomplete works in intellijpycharm- feature can match on body of request- feature store calls made- feature repeated responses- bugfix no longer stops event loop- feature if dict or list is passed into response, a json responsewill be generated 1.1.2- make passthrough feature work with binary data 1.1.1- regex fix for Python 3.7.0 1.1.0- Added passthrough option to permit live network calls- Added example of using a callable as a response 1.0.0- Added an optional match_querystring argument that lets you match on querystring as well Contributors Bryce Drennan, CircleUp aresponsesbrycedrennan.com Marco Castelluccio, Mozilla mcastellucciomozilla.com Jesse Vogt, CircleUp jesse.vogtgmail.com Pavol Vargovcik, Kiwi.com pavol.vargovcikgmail.com</longdescription>
</pkgmetadata>