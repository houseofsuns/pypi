<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-multidb-router.. image httpsimg.shields.iogithubworkflowstatusjbaloghdjango-multidb-routerCIlabelCIlogogithubbranchmaster    alt Build Status    target httpsgithub.comjbaloghdjango-multidb-routeractionsqueryworkflow3ACI.. image httpsimg.shields.iopypivdjango-multidb-router.svg    target httpspypi.python.orgpypidjango-multidb-routermultidb provides two Django database routers useful in primary-replica databasedeployments.ReplicaRouter-----------------With multidb.ReplicaRouter all read queries will go to a replicadatabase  all inserts, updates, and deletes will go to the defaultdatabase.First, define REPLICA_DATABASES in your settings.  It should be a list ofdatabase aliases that can be found in DATABASES    DATABASES  {        default {...},        shadow-1 {...},        shadow-2 {...},    }    REPLICA_DATABASES  [shadow-1, shadow-2]Then put multidb.ReplicaRouter into DATABASE_ROUTERS    DATABASE_ROUTERS  (multidb.ReplicaRouter,)The replica databases will be chosen in round-robin fashion.If you want to get a connection to a replica in your app, usemultidb.get_replica    from django.db import connections    import multidb    connection  connections[multidb.get_replica()]PinningReplicaRouter------------------------In some applications, the lag between the primary database receiving a write and itsreplication to the replicas is enough to cause inconsistency for the end user.For example, imagine a scenario with 1 second of replication lag. If a usermakes a forum post (to the primary) and then is redirected to a fully-renderedview of it (from a replica) 500ms later, the view will fail. If this is a problemin your application, consider using multidb.PinningReplicaRouter. Thisrouter works in combination with multidb.middleware.PinningRouterMiddlewareto assure that, after writing to the default database, future reads fromthe same user agent are directed to the default database for a configurablelength of time.CaveatsPinningRouterMiddleware identifies database writes primarily by requesttype, assuming that requests with HTTP methods that are not GET, TRACE,HEAD, or OPTIONS are writes. You can indicate that any view writes tothe database by using the multidb.db_write decorator. This will cause thesame result as if the request were, e.g., a POST.You can also manually set response._db_write  True to indicate that awrite occurred. This will not result in using the default database in thisrequest, but only in the next request.ConfigurationTo use PinningReplicaRouter, put it into DATABASE_ROUTERS in yoursettings    DATABASE_ROUTERS  (multidb.PinningReplicaRouter,)Then, install the middleware. It must be listed before any other middlewarewhich performs database writes    MIDDLEWARE_CLASSES  (        multidb.middleware.PinningRouterMiddleware,        ...more middleware here...    )PinningRouterMiddleware attaches a cookie to any user agent who has justwritten. The cookie should be set to expire at a time longer than yourreplication lag. By default, its value is a conservative 15 seconds, but it canbe adjusted like so    MULTIDB_PINNING_SECONDS  5If you need to change the name of the cookie, use the MULTIDB_PINNING_COOKIEsetting    MULTIDB_PINNING_COOKIE  multidb_pin_writesYou may also set the Secure, HttpOnly, and SameSite cookie attributes byusing the following settings. These settings are based on Djangos settings forthe session and CSRF cookies    MULTIDB_PINNING_COOKIE_SECURE  False    MULTIDB_PINNING_COOKIE_HTTPONLY  False    MULTIDB_PINNING_COOKIE_SAMESITE  LaxNote the SameSite attribute is only available on django 2.1 and higherhttpsdocs.djangoproject.comen2.1releases2.1_.use_primary_dbmultidb.pinning.use_primary_db is both a context manager and a decorator forwrapping code to use the primary database. You can use it as a context manager    from multidb.pinning import use_primary_db    with use_primary_db        touch_the_database()    touch_another_database()or as a decorator    from multidb.pinning import use_primary_db    use_primary_db    def func(args, kw)        Touches the primary database.Running the Tests-----------------To run the tests, youll need to install the development requirements    pip install -r requirements.txt    .run.sh testAlternatively, you can run the tests with several versions of Djangoand Python using tox     pip install tox     tox</longdescription>
</pkgmetadata>