<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Yamale (yamal) warning Ensure that your schema definitions come from internal or trusted sources. Yamale does not protect against intentionally malicious schemas. ------------img srchttpsgithub.com23andMeYamaleblobmasteryamale.pngrawtrue altYamale width400A schema and validator for YAML.Whats YAML See the current spec [here](httpwww.yaml.orgspec1.2spec.html) and an introductionto the syntax [here](httpsgithub.comAnimosityCraftIRCwikiComplete-idiots-introduction-to-yaml).[[Build Status](httpsgithub.com23andMeYamaleactionsworkflowsrun-tests.ymlbadge.svg)](httpsgithub.com23andMeYamaleactionsworkflowsrun-tests.yml)[[PyPI](httpsimg.shields.iopypivyamale.svg)](httpspypi.python.orgpypiyamale)Requirements------------ Python 3.6+ PyYAML ruamel.yaml (optional)Install------- pipbash pip install yamaleNOTE Some platforms, e.g., Mac OS, may ship with only Python 2 and may not have pip installed.Installation of Python 3 should also install pip. To preserve any system dependencies on defaultsoftware, consider installing Python 3 as a local package. Please note replacing system-providedPython may disrupt other software. Mac OS users may wish to investigate MacPorts, homebrew, orbuilding Python 3 from source in all three cases, Apples Command Line Tools (CLT) for Xcodemay be required. See also [developers](developers), below. Manual1. Download Yamale from httpsgithub.com23andMeYamalearchivemaster.zip2. Unzip somewhere temporary3. Run python setup.py install (may have to prepend sudo)Usage----- Command lineYamale can be run from the command line to validate one or many YAML files. Yamale will search thedirectory you supply (current directory is default) for YAML files. Each YAML file it finds it willlook in the same directory as that file for its schema, if there is no schema Yamale will keeplooking up the directory tree until it finds one. If Yamale can not find a schema it will tell you.Usagebashusage yamale [-h] [-s SCHEMA] [-n CPU_NUM] [-p PARSER] [--no-strict] [PATH]Validate yaml files.positional arguments  PATH                  folder to validate. Default is current directory.optional arguments  -h, --help            show this help message and exit  -s SCHEMA, --schema SCHEMA                        filename of schema. Default is schema.yaml.  -n CPU_NUM, --cpu-num CPU_NUM                        number of CPUs to use. Default is 4.  -p PARSER, --parser PARSER                        YAML library to load files. Choices are ruamel or                        pyyaml (default).  --no-strict           Disable strict mode, unexpected elements in the data                        will be accepted. APIThere are several ways to feed Yamale schema and data files. The simplest way is to let Yamale takecare of reading and parsing your YAML files.All you need to do is supply the files pathpython Import Yamale and make a schema objectimport yamaleschema  yamale.make_schema(.schema.yaml) Create a Data objectdata  yamale.make_data(.data.yaml) Validate data against the schema. Throws a ValueError if data is invalid.yamale.validate(schema, data)You can pass a string of YAML to make_schema() and make_data() instead of passing a file pathby using the content parameterpythondata  yamale.make_data(contentname Billage 26height 6.2awesome True)If data is valid, nothing will happen. However, if data is invalid Yamale will throw aYamaleError with a message containing all the invalid nodespythontry    yamale.validate(schema, data)    print(Validation success )except ValueError as e    print(Validation failedns  str(e))    exit(1)and an array of ValidationResult.pythontry    yamale.validate(schema, data)    print(Validation success )except YamaleError as e    print(Validation failedn)    for result in e.results        print(Error validating data s with snt  (result.data, result.schema))        for error in result.errors            print(ts  error)    exit(1)You can also specify an optional parser if youd like to use the ruamel.yaml (YAML 1.2 support) insteadpython Import Yamale and make a schema object, make sure ruamel.yaml is installed already.import yamaleschema  yamale.make_schema(.schema.yaml, parserruamel) Create a Data objectdata  yamale.make_data(.data.yaml, parserruamel) Validate data against the schema same as before.yamale.validate(schema, data) Schema warning Ensure that your schema definitions come from internal or trusted sources. Yamale does not protect against intentionally malicious schemas. ------------To use Yamale you must make a schema. A schema is a valid YAML file with one or more documentsinside. Each node terminates in a string which contains valid Yamale syntax. For example, str()represents a [String validator](validators).A basic schemayamlname str()age int(max200)height num()awesome bool()And some YAML that validatesyamlname Billage 26height 6.2awesome TrueTake a look at the [Examples](examples) section for more complex schema ideas. IncludesSchema files may contain more than one YAML document (nodes separated by ---). The first documentfound will be the base schema. Any additional documents will be treated as Includes. Includes allowyou to define a valid structure once and use it several times. They also allow you to do recursion.A schema with an Include validatoryamlperson1 include(person)person2 include(person)---person    name str()    age int()Some valid YAMLyamlperson1    name Bill    age 70person2    name Jill    age 20Every root node not in the first YAML document will be treated like an includeyamlperson include(friend)group include(family)---friend    name str()family    name str()Is equivalent toyamlperson include(friend)group include(family)---friend    name str()---family    name str() RecursionYou can get recursion using the Include validator.This schemayamlperson include(human)---human    name str()    age int()    friend include(human, requiredFalse)Will validate this datayamlperson    name Bill    age 50    friend        name Jill        age 20        friend            name Will            age 10 Adding external includesAfter you construct a schema you can add extra, external include definitions by callingschema.add_include(dict). This method takes a dictionary and adds each key as another include. Strict modeBy default Yamale will provide errors for extra elements present in lists and maps that are notcovered by the schema. With strict mode disabled (using the --no-strict command line option),additional elements will not cause any errors. In the API, strict mode can be toggled by passingthe strictTrueFalse flag to the validate function.It is possible to mix strict and non-strict mode by setting the strictTrueFalse flag in theinclude validator, setting the option only for the included validators.Validators----------Here are all the validators Yamale knows about. Every validator takes a required keyword tellingYamale whether or not that node must exist. By default every node is required. Example str(requiredFalse)You can also require that an optional value is not None by using the none keyword. By defaultYamale will accept None as a valid value for a key thats not required. Reject None valueswith noneFalse in any validator. Example str(requiredFalse, noneFalse).Some validators take keywords and some take arguments, some take both. For instance the enum()validator takes one or more constants as arguments and the required keywordenum(a string, 1, False, requiredFalse) String - str(minint, maxint, equalsstring, starts_withstring, ends_withstring, matchesregex, excludestring, ignore_caseFalse, multilineFalse, dotallFalse)Validates strings.- keywords    - min len(string)  min    - max len(string)  max    - equals string  value (add ignore_caseTrue for case-insensitive checking)    - starts_with Accepts only strings starting with given value (add ignore_caseTrue for      case-insensitive checking)    - matches Validates the string against a given regex. Similar to the regex() validator,      you can use ignore_case, multiline and dotall)    - ends_with Accepts only strings ending with given value (add ignore_caseTrue for case-insensitive checking)    - exclude Rejects strings that contains any character in the excluded value    - ignore_case Validates strings in a case-insensitive manner.    - multiline  and  in a pattern match at the beginning and end of each line in a string       in addition to matching at the beginning and end of the entire string. (A pattern matches       at [the beginning of a string](httpsdocs.python.org3libraryre.htmlre.match) even in       multiline mode see below for a workaround.) only allowed in conjunction with a matches keyword.    - dotall . in a pattern matches newline characters in a validated string in addition to      matching every character that isnt a newline. only allowed in conjunction with a matches keyword.Examples- str(max10, exclude) Allows only strings less than 11 characters that dont contain  or . Regex - regex([patterns], namestring, ignore_caseFalse, multilineFalse, dotallFalse)Validates strings against one or more regular expressions.- arguments one or more Python regular expression patterns- keywords    - name A friendly description for the patterns.    - ignore_case Validates strings in a case-insensitive manner.    - multiline  and  in a pattern match at the beginning and end of each line in a string       in addition to matching at the beginning and end of the entire string. (A pattern matches       at [the beginning of a string](httpsdocs.python.org3libraryre.htmlre.match) even in       multiline mode see below for a workaround.)    - dotall . in a pattern matches newline characters in a validated string in addition to      matching every character that isnt a newline.Examples- regex([]{,10}) Allows only strings less than 11 characters that dont contain  or .- regex(r(d+)(s1)+, namerepeated natural) Allows only strings that contain two or  more identical digit sequences, each separated by a whitespace character. Non-matching strings  like sugar are rejected with a message like sugar is not a repeated natural.- regex(.apples, multilineTrue, dotallTrue) Allows the string apples as well  as multiline strings that contain the line apples. Integer - int(minint, maxint)Validates integers.- keywords    - min int  min    - max int  max Number - num(minfloat, maxfloat)Validates integers and floats.- keywords    - min num  min    - max num  max Boolean - bool()Validates booleans. Null - null()Validates null values. Enum - enum([primitives])Validates from a list of constants.- arguments constants to test equality withExamples- enum(a string, 1, False) a value can be either a string, 1 or False Day - day(mindate, maxdate)Validates a date in the form of YYYY-MM-DD.- keywords    - min date  min    - max date  maxExamples- day(min2001-01-01, max2100-01-01) Only allows dates between 2001-01-01 and 2100-01-01. Timestamp - timestamp(mintime, maxtime)Validates a timestamp in the form of YYYY-MM-DD HHMMSS.- keywords    - min time  min    - max time  maxExamples- timestamp(min2001-01-01 010000, max2100-01-01 230000) Only allows times between  2001-01-01 010000 and 2100-01-01 230000. List - list([validators], minint, maxint)Validates lists. If one or more validators are passed to list() only nodes that pass atleast one of those validators will be accepted.- arguments one or more validators to test values with- keywords    - min len(list)  min    - max len(list)  maxExamples- list() Validates any list- list(include(custom), int(), min4) Only validates lists that contain the custom include  or integers and contains a minimum of 4 items. Map - map([validators], keyvalidator, minint, maxint)Validates maps. Use when you want a node to contain freeform data. Similar to List, Map takesone or more validators to run against the values of its nodes, and only nodes that pass at leastone of those validators will be accepted. By default, only the values of nodes are validated andthe keys arent checked.- arguments one or more validators to test values with- keywords    - key A validator for the keys of the map.    - min len(map)  min    - max len(map)  maxExamples- map() Validates any map- map(str(), int()) Only validates maps whose values are strings or integers.- map(str(), keyint()) Only validates maps whose keys are integers and values are strings. 1 one would be valid but 1 one would not.- map(str(), min1) Only validates a non-empty map. IP Address - ip()Validates IPv4 and IPv6 addresses.- keywords    - version 4 or 6 explicitly force IPv4 or IPv6 validationExamples- ip() Allows any valid IPv4 or IPv6 address- ip(version4) Allows any valid IPv4 address- ip(version6) Allows any valid IPv6 address MAC Address - mac()Validates MAC addresses.Examples- mac() Allows any valid MAC address Any - any([validators])Validates against a union of types. Use when a node must contain one and only one of several types. It is validif at least one of the listed validators is valid. If no validators are given, accept any value.- arguments validators to test values with (if none is given, allow any value if one or more are given,one must be present)Examples- any(int(), null()) Validates either an integer or a null value.- any(num(), include(vector)) Validates either a number or an included vector type.- any(str(min3, max3),str(min5, max5),str(min7, max7)) validates to a string that is exactly 3, 5, or 7 characters long- any() Allows any value. Subset - subset([validators], allow_emptyFalse)Validates against a subset of types. Unlike the Any validator, this validators allows one or more of several types.As such, it automatically validates against a list. It is valid if all values can be validated against at least onevalidator.- arguments validators to test with (at least one if none is given, a ValueError exception will be raised)- keywords    - allow_empty Allow the subset to be empty (and is, therefore, also optional). This overrides the requiredflag.      Examples- subset(int(), str()) Validators against an integer, a string, or a list of either.- subset(int(), str(), allow_emptyTrue) Same as above, but allows the empty set and makes the subset optional. Include - include(include_name)Validates included structures. Must supply the name of a valid include.- arguments single name of a defined include, surrounded by quotes.Examples- include(person) Custom validatorsIt is also possible to add your own custom validators. This is an advanced topic, but here is anexample of adding a Date validator and using it in a schema as date()pythonimport yamaleimport datetimefrom yamale.validators import DefaultValidators, Validatorclass Date(Validator)     Custom Date validator     tag  date    def _is_valid(self, value)        return isinstance(value, datetime.date)validators  DefaultValidators.copy()   This is a dictionaryvalidators[Date.tag]  Dateschema  yamale.make_schema(.schema.yaml, validatorsvalidators) Then use schema as normalExamples-------- warning Ensure that your schema definitions come from internal or trusted sources. Yamale does not protect against intentionally malicious schemas. ------------ Using keywords Schemayamloptional str(requiredFalse)optional_min int(min1, requiredFalse)min num(min1.5)max int(max100) Valid Datayamloptional_min 10min 1.6max 100 Includes and recursion SchemayamlcustomerA include(customer)customerB include(customer)recursion include(recurse)---customer    name str()    age int()    custom include(custom_type)custom_type    integer int()recurse    level int()    again include(recurse, requiredFalse) Valid DatayamlcustomerA    name bob    age 900    custom        integer 1customerB    name jill    age 1    custom        integer 3recursion    level 1    again        level 2        again            level 3            again                level 4 Lists Schemayamllist_with_two_types list(str(), include(variant))questions list(include(question))---variant  rsid str()  name str()question  choices list(include(choices))  questions list(include(question), requiredFalse)choices  id str() Valid Datayamllist_with_two_types  - some  - rsid rs123    name some SNP  - thing  - rsid rs312    name another SNPquestions  - choices      - id id_str      - id id_str1    questions      - choices        - id id_str        - id id_str1 The data is a list of items without a keyword at the top level Schemayamllist(include(human), min2, max2)---human  name str()  age int(max200)  height num()  awesome bool() Valid Datayaml- name Bill  age 26  height 6.2  awesome True- name Adrian  age 23  height 6.3  awesome TrueWriting Tests-------------To validate YAML files when you run your programs tests use Yamales YamaleTestCaseExamplepythonclass TestYaml(YamaleTestCase)    base_dir  os.path.dirname(os.path.realpath(__file__))    schema  schema.yaml    yaml  data.yaml     or yaml  [data-.yaml, some_data.yaml]    def runTest(self)        self.assertTrue(self.validate())base_dir String path to prepend to all other paths. This is optional.schema String of path to the schema file to use. One schema file per test case.yaml String or list of yaml files to validate. Accepts globs.Developers---------- TestingYamale uses [Tox](httpstox.readthedocs.orgenlatest) to run its tests against multiple Pythonversions. To run tests, first checkout Yamale, install Tox, then run make test in Yamales rootdirectory. You may also have to install the correct Python versions to test with as well.NOTE on Python versions tox.ini specifies the lowest and highest versions of Python supported byYamale. Unless your development environment is configured to support testing against multiple Pythonversions, one or more of the test branches may fail. One method of enabling testing against multipleversions of Python is to install pyenv and tox-pyenv and to use pyenv install and pyenv localto ensure that tox is able to locate appropriate Pythons. ReleasingYamale uses Github Actions to upload new tags to PyPi.To release a new version1. Make a commit with the new version number in yamaleVERSION.1. Run tests for good luck.1. Run make release.Github Actions will take care of the rest.</longdescription>
</pkgmetadata>