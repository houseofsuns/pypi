<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpsimg.shields.iopypivkeyring.svg   target httpspypi.orgprojectkeyring.. image httpsimg.shields.iopypipyversionskeyring.svg.. image httpsgithub.comjaracokeyringworkflowstestsbadge.svg   target httpsgithub.comjaracokeyringactionsqueryworkflow3A22tests22   alt tests.. image httpsimg.shields.iobadgecode20style-black-000000.svg   target httpsgithub.compsfblack   alt Code style Black.. image httpsreadthedocs.orgprojectskeyringbadgeversionlatest   target httpskeyring.readthedocs.ioenlatestbadgelatest.. image httpsimg.shields.iobadgeskeleton-2022-informational   target httpsblog.jaraco.comskeleton.. image httpstidelift.combadgespackagepypikeyring   target httpstidelift.comsubscriptionpkgpypi-keyringutm_sourcepypi-keyringutm_mediumreadme.. image httpsbadges.gitter.imjaracokeyring.svg   alt Join the chat at httpsgitter.imjaracokeyring   target httpsgitter.imjaracokeyringutm_sourcebadgeutm_mediumbadgeutm_campaignpr-badgeutm_contentbadgeThe Python keyring library provides an easy way to access thesystem keyring service from python. It can be used in anyapplication that needs safe password storage.These recommended keyring backends are supported macOS Keychain  httpsen.wikipedia.orgwikiKeychain_28software29_ Freedesktop Secret Service  httpstandards.freedesktop.orgsecret-service_ supports many DE including  GNOME (requires secretstorage httpspypi.python.orgpypisecretstorage_) KDE4  KDE5 KWallet httpsen.wikipedia.orgwikiKWallet_  (requires dbus httpspypi.python.orgpypidbus-python_) Windows Credential Locker  httpsdocs.microsoft.comen-uswindowsuwpsecuritycredential-locker_Other keyring implementations are available through Third-Party Backends_.Installation - LinuxOn Linux, the KWallet backend relies on dbus-python_, which does not alwaysinstall correctly when using pip (compilation is needed). For best results,install dbus-python as a system package... _dbus-python httpsgitlab.freedesktop.orgdbusdbus-pythonCompatibility - macOSmacOS keychain supports macOS 11 (Big Sur) and later requires Python 3.8.7or later with the universal2 binary. See525 httpsgithub.comjaracokeyringissues525_ for details.Using KeyringThe basic usage of keyring is pretty simple just callkeyring.set_password and keyring.get_password     import keyring     keyring.set_password(system, username, password)     keyring.get_password(system, username)    passwordCommand-line Utility--------------------Keyring supplies a keyring command which is installed with thepackage. After installing keyring in most environments, thecommand should be available for setting, getting, and deletingpasswords. For more usage information, invoke with no argumentsor with --help as so     keyring --help     keyring set system username    Password for username in system     keyring get system username    passwordThe command-line functionality is also exposed as an executablepackage, suitable for invoking from Python like so     python -m keyring --help     python -m keyring set system username    Password for username in system     python -m keyring get system username    passwordTab Completion--------------If installed via a package manager (apt, pacman, nix, homebrew, etc),these shell completions may already have been distributed with the package(no action required).Keyring provides tab completion if the completion extra is installed     pip install keyring[completion]Then, generate shell completions, something like     keyring --print-completion bash  sudo tee usrsharebash-completioncompletionskeyring     keyring --print-completion zsh  sudo tee usrsharezshsite-functions_keyring     keyring --print-completion tcsh  sudo tee etcprofile.dkeyring.cshNote the path of usrshare is mainly for GNULinux. For other OSs,consider- macOS (Homebrew x86) usrlocalshare- macOS (Homebrew ARM) opthomebrewshare- Android (Termux) datadatacom.termuxfilesusrshare- Windows (mingw64 of msys2) mingw64share- ...After installing the shell completions, enable them following your shellsrecommended instructions. e.g.- bash install [bash-completion](httpsgithub.comscopbash-completion),  and ensure . usrsharebash-completionbash_completion in .bashrc.- zsh ensure autoload -Uz compinit  compinit appears in .zshrc,  then grep -w keyring .zcompdump to verify keyring appears, indicating  it was installed correctly.ConfiguringThe python keyring lib contains implementations for several backends. Thelibrary will attempt toautomatically choose the most suitable backend for the currentenvironment. Users may also specify the preferred keyring in aconfig file or by calling the set_keyring() function.Config file path----------------The configuration is stored in a file named keyringrc.cfgfound in a platform-specific location. To determinewhere the config file is stored, run the following    python -c import keyring.util.platform_ print(keyring.util.platform_.config_root())Some keyrings also store the keyring data in the file system.To determine where the data files are stored, run    python -c import keyring.util.platform_ print(keyring.util.platform_.data_root())Config file content-------------------To specify a keyring backend, set the default-keyring option to thefull path of the class for that backend, such askeyring.backends.OS_X.Keyring.If keyring-path is indicated, keyring will add that path to the Pythonmodule search path before loading the backend.For example, this config might be used to load theSimpleKeyring from the simplekeyring module inthe .demo directory (not implemented)    [backend]    default-keyringsimplekeyring.SimpleKeyring    keyring-pathdemoThird-Party BackendsIn addition to the backends provided by the core keyring package forthe most common and secure use cases, thereare additional keyring backend implementations available for otheruse cases. Simply install them to make them available- keyrings.cryptfile httpspypi.orgprojectkeyrings.cryptfile_  - Encrypted text file storage.- keyring_jeepney httpspypi.orgprojectkeyring_jeepney__ - a  pure Python backend using the secret service DBus API for desktop  Linux.- keyrings.alt httpspypi.orgprojectkeyrings.alt_ - alternate,  possibly-insecure backends, originally part of the core package, but  available for opt-in.- gsheet-keyring httpspypi.orgprojectgsheet-keyring_  - a backend that stores secrets in a Google Sheet. For use with  ipython-secrets httpspypi.orgprojectipython-secrets_.- bitwarden-keyring httpspypi.orgprojectbitwarden-keyring0.1.0_  - a backend that stores secrets in the BitWarden httpsbitwarden.com_  password manager.- sagecipher httpspypi.orgprojectsagecipher_ - an encryption  backend which uses the ssh agent protocols signature operation to  derive the cipher key.- keyrings.osx_keychain_keys httpspypi.orgprojectkeyrings.osx-keychain-keys_  - OSX keychain key-management, for private, public, and symmetric keys.Write your own keyring backendThe interface for the backend is defined by keyring.backend.KeyringBackend.Every backend should derive from that base class and define a priorityattribute and three functions get_password(), set_password(), anddelete_password(). The get_credential() function may be defined ifdesired.See the backend module for more detail on the interface of this class.Keyring employs entry points to allow any third-party package to implementbackends without any modification to the keyring itself. Those interested increating new backends are encouraged to create new, third-party packagesin the keyrings namespace, in a manner modeled by the keyrings.altpackage httpsgithub.comjaracokeyrings.alt_. See thesetup.cfg filein that project for hints on how to create the requisite entry points.Backends that prove essential may be considered for inclusion in the corelibrary, although the ease of installing these third-party packages shouldmean that extensions may be readily available.To create an extension for Keyring, please submit a pull request tohave your extension mentioned as an available extension.Runtime ConfigurationKeyring additionally allows programmatic configuration of thebackend calling the api set_keyring(). The indicated backendwill subsequently be used to store and retrieve passwords.To invoke set_keyring     define a new keyring class which extends the KeyringBackend    import keyring.backend    class TestKeyring(keyring.backend.KeyringBackend)        A test keyring which always outputs the same password                priority  1        def set_password(self, servicename, username, password)            pass        def get_password(self, servicename, username)            return password from TestKeyring        def delete_password(self, servicename, username)            pass     set the keyring for keyring lib    keyring.set_keyring(TestKeyring())     invoke the keyring lib    try        keyring.set_password(demo-service, tarek, passexample)        print(password stored successfully)    except keyring.errors.PasswordSetError        print(failed to store password)    print(password, keyring.get_password(demo-service, tarek))Disabling KeyringIn many cases, uninstalling keyring will never be necessary.Especially on Windows and macOS, the behavior of keyring isusually degenerate, meaning it will return empty values tothe caller, allowing the caller to fall back to some otherbehavior.In some cases, the default behavior of keyring is undesirable andit would be preferable to disable the keyring behavior altogether.There are several mechanisms to disable keyring- Uninstall keyring. Most applications are tolerant to keyring  not being installed. Uninstalling keyring should cause those  applications to fall back to the behavior without keyring.  This approach affects the Python environment where keyring  would otherwise have been installed.- Configure the Null keyring in the environment. Set  PYTHON_KEYRING_BACKENDkeyring.backends.null.Keyring  in the environment, and the Null (degenerate) backend  will be used. This approach affects all uses of Keyring where  that variable is set.- Permanently configure the Null keyring for the user by running  keyring --disable or python -m keyring --disable.  This approach affects all uses of keyring for that user.Altering Keyring BehaviorKeyring provides a mechanism to alter the keyrings behavior throughenvironment variables. Each backend implements aKeyringBackend.set_properties_from_env, whichwhen invoked will find all environment variables beginning withKEYRING_PROPERTY_{NAME} and will set a property for each{NAME.lower()} on the keyring. This method is invoked duringinitialization for the defaultconfigured keyring.This mechanism may be used to set some useful values on variouskeyrings, including- keychain macOS, path to an alternate keychain file- appid LinuxSecretService, alternate ID for the applicationUsing Keyring on Ubuntu 16.04The following is a complete transcript for installing keyring in avirtual environment on Ubuntu 16.04.  No config file was used   sudo apt install python3-venv libdbus-glib-1-dev   cd tmp   pyvenv py3   source py3binactivate   pip install -U pip   pip install secretstorage dbus-python   pip install keyring   python   import keyring   keyring.get_keyring()  keyring.backends.SecretService.Keyring object at 0x7f9b9c971ba8   keyring.set_password(system, username, password)   keyring.get_password(system, username)  passwordUsing Keyring on headless Linux systemsIt is possible to use the SecretService backend on Linux systems withoutX11 server available (only D-Bus is required). In this case Install the GNOME Keyring_ daemon. Start a D-Bus session, e.g. run dbus-run-session -- sh and run  the following commands inside that shell. Run gnome-keyring-daemon with --unlock option. The description of  that option says      Read a password from stdin, and use it to unlock the login keyring      or create it if the login keyring does not exist.  When that command is started, enter a password into stdin and  press Ctrl+D (end of data). After that, the daemon will fork into  the background (use --foreground option to block). Now you can use the SecretService backend of Keyring. Remember to  run your application in the same D-Bus session as the daemon... _GNOME Keyring httpswiki.gnome.orgProjectsGnomeKeyringUsing Keyring on headless Linux systems in a Docker containerIt is possible to use keyring with the SecretService backend in Docker containers as well.All you need to do is install the necessary dependencies and add the --privileged flagto avoid any Operation not permitted errors when attempting to unlock the systems keyring.The following is a complete transcript for installing keyring on a Ubuntu 1804 container  docker run -it -d --privileged ubuntu18.04   apt-get update   apt install -y gnome-keyring python3-venv python3-dev   python3 -m venv venv   source venvbinactivate  source a virtual environment to avoid polluting your system   pip3 install --upgrade pip   pip3 install keyring   dbus-run-session -- sh  this will drop you into a new D-bus shell   echo somecredstorepass  gnome-keyring-daemon --unlock  unlock the systems keyring   python   import keyring   keyring.get_keyring()  keyring.backends.SecretService.Keyring object at 0x7f9b9c971ba8   keyring.set_password(system, username, password)   keyring.get_password(system, username)  passwordIntegrationAPI---The keyring lib has a few functions get_keyring() Return the currently-loaded keyring implementation. get_password(service, username) Returns the password stored in the  active keyring. If the password does not exist, it will return None. get_credential(service, username) Return a credential object stored  in the active keyring. This object contains at least username and  password attributes for the specified service, where the returned  username may be different from the argument. set_password(service, username, password) Store the password in the  keyring. delete_password(service, username) Delete the password stored in  keyring. If the password does not exist, it will raise an exception.In all cases, the parameters (service, username, password)should be Unicode text.Exceptions----------The keyring lib raises the following exceptions keyring.errors.KeyringError Base Error class for all exceptions in keyring lib. keyring.errors.InitError Raised when the keyring cannot be initialized. keyring.errors.PasswordSetError Raised when the password cannot be set in the keyring. keyring.errors.PasswordDeleteError Raised when the password cannot be deleted in the keyring.Get InvolvedPython keyring lib is an open community project and eagerlywelcomes contributors. Repository httpsgithub.comjaracokeyring Bug Tracker httpsgithub.comjaracokeyringissues Mailing list httpgroups.google.comgrouppython-keyringFor EnterpriseAvailable as part of the Tidelift Subscription.This project and the maintainers of thousands of other packages are working with Tidelift to deliver one enterprise subscription that covers all of the open source you use.Learn more httpstidelift.comsubscriptionpkgpypi-keyringutm_sourcepypi-keyringutm_mediumreferralutm_campaigngithub_.Security ContactTo report a security vulnerability, please use theTidelift security contact httpstidelift.comsecurity_.Tidelift will coordinate the fix and disclosure.Security ConsiderationsEach built-in backend may have security considerations to understandbefore using this library. Authors of tools or libraries utilizingkeyring are encouraged to consider these concerns.As with any list of known security concerns, this list is not exhaustive.Additional issues can be added as needed.- macOS Keychain    - Any Python script or application can access secrets created by      keyring from that same Python executable without the operating      system prompting the user for a password. To cause any specific      secret to prompt for a password every time it is accessed, locate      the credential using the Keychain Access application, and in      the Access Control settings, remove Python from the list      of allowed applications.- Freedesktop Secret Service    - No analysis has been performed- KDE4  KDE5 KWallet    - No analysis has been performed- Windows Credential Locker    - No analysis has been performedMaking ReleasesThis project makes use of automated releases and continuousintegration. Thesimple workflow is to tag a commit and push it to Github. If itpasses tests in CI, it will be automatically deployed to PyPI.Other things to consider when making a release- Check that the changelog is current for the intended release.Running TestsTests are continuously run in Github Actions.To run the tests locally, install and invoketox httpspypi.orgprojecttox_.BackgroundThe project was based on Tarek Ziades idea in this post_. Kang Zhanginitially carried it out as a Google Summer of Code_ project, and Tarekmentored Kang on this project... _this post httptarekziade.wordpress.com20090327pycon-hallway-session-1-a-keyring-library-for-python.. _Google Summer of Code httpsocghop.appspot.com</longdescription>
</pkgmetadata>