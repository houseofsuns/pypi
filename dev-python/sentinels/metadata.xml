<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Overview--------The sentinels module is a small utility providing the Sentinel class, along with useful instances.What are Sentinels-------------------Sentinels are objects with special meanings. They can be thought of as singletons, but they service the need of having special values in your code, that have special meanings (see example below).Why Do I Need Sentinels------------------------Lets take NOTHING for example. This sentinel is automatically provided with the sentinels import   from sentinels import NOTHINGLets say youre writing a wrapper around a Python dictionary, which supports a special kind of method, get_default_or_raise. This method behaves like get, but when it does not receive a default and the key does not exist, it raises a KeyError. How would you implement such a thing The naive method is this   class MyDict(dict)  ...     def get_default_or_raise(self, key, defaultNone)  ...         if key not in self and default is None  ...             raise KeyError(key)  ...         return self.get(key, default)Or even this   class MyDict(dict)  ...     def get_default_or_raise(self, key, defaultNone)  ...         returned  self.get(key, default)  ...         if returned is None  ...             raise KeyError(key)  ...         return returnedBut the problem with the above two pieces of code is the same -- when writing a general utility class, we dont know how it will be used later on. More importantly, None might be a perfectly valid dictionary valueThis is where NOTHING comes in handy   class MyDict(dict)  ...     def get_default_or_raise(self, key, defaultNOTHING)  ...         returned  self.get(key, default)  ...         if returned is NOTHING  ...             raise KeyError(key)  ...         return returnedAnd TadaSemantics---------Sentinels are always equal to themselves   NOTHING  NOTHING  TrueBut never to another object   from sentinels import Sentinel   NOTHING  2  False   NOTHING  NOTHING  FalseCopying sentinels returns the same object   import copy   copy.deepcopy(NOTHING) is NOTHING  TrueAnd of course also picklingunpickling   import pickle   NOTHING is pickle.loads(pickle.dumps(NOTHING))  True</longdescription>
</pkgmetadata>