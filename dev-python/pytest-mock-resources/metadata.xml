<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>[CircleCI](httpsimg.shields.iocirclecibuildghschiresonpytest-mock-resourcesmaster)[[codecov](httpscodecov.ioghschiresonpytest-mock-resourcesbranchmastergraphbadge.svg)](httpscodecov.ioghschiresonpytest-mock-resources)[[DocumentationStatus](httpsreadthedocs.orgprojectspytest-mock-resourcesbadgeversionlatest)](httpspytest-mock-resources.readthedocs.ioenlatestbadgelatest) IntroductionCode which depends on external resources such a databases (postgres, redshift, etc) can be difficultto write automated tests for. Conventional wisdom might be to mock or stub out the actual databasecalls and assert that the code works correctly beforeafter the calls.However take the following, _simple_ examplepythondef serialize(users)    return [        {            user user.serialize(),            address user.address.serialize(),            purchases [p.serialize() for p in user.purchases],        }        for user in users    ]def view_function(session)    users  session.query(User).join(Address).options(selectinload(User.purchases)).all()    return serialize(users)Sure, you can test serialize, but whether the actual query did the correct thing _truly_requires that you execute the query. The PitchHaving tests depend upon a real postgres instance running somewhere is a pain, very fragile, andprone to issues across machines and test failures.Therefore pytest-mock-resources (primarily) works by managing the lifecycle of docker containersand providing access to them inside your tests.As such, this package makes 2 primary assumptions- Youre using pytest (hopefully thats appropriate, given the package name)- For many resources, docker is required to be available and running (or accessible through remote  docker).If you arent familiar with Pytest Fixtures, you can read up on them in the [Pytestdocumentation](httpsdocs.pytest.orgenlatestfixture.html).In the above example, your test file could look something likepythonfrom pytest_mock_resources import create_postgres_fixturefrom models import ModelBasepg  create_postgres_fixture(ModelBase, sessionTrue)def test_view_function_empty_db(pg)  response  view_function(pg)  assert response  ...def test_view_function_user_without_purchases(pg)  pg.add(User(...))  pg.flush()  response  view_function(pg)  assert response  ...def test_view_function_user_with_purchases(pg)  pg.add(User(..., purchases[Purchase(...)]))  pg.flush()  response  view_function(pg)  assert response  ... Existing Resources (many more possible)- SQLite  python  from pytest_mock_resources import create_sqlite_fixture  - Postgres  python  from pytest_mock_resources import create_postgres_fixture  - Redshift  note Uses postgres under the hood, but the fixture tries to support as much redshift  functionality as possible (including redshifts COPYUNLOAD commands).  python  from pytest_mock_resources import create_redshift_fixture  - Mongo  python  from pytest_mock_resources import create_mongo_fixture  - Redis  python  from pytest_mock_resources import create_redis_fixture  - MySQL  python  from pytest_mock_resources import create_mysql_fixture  - Moto  python  from pytest_mock_resources import create_moto_fixture   FeaturesGeneral features include- Support for actions which pre-populate the resource youre mocking before the test- [Async fixtures](httpspytest-mock-resources.readthedocs.ioenlatestasync.html)- Custom configuration for containerresource startup Installationbash Basic fixture support i.e. SQLitepip install pytest-mock-resources General, docker-based fixture supportpip install pytest-mock-resources[docker] Mongo fixture support, installs pymongopip install pytest-mock-resources[mongo] Moto fixture support, installs non-driver extras specific to moto supportpip install pytest-mock-resources[moto] Redis fixture support, Installs redis clientpip install pytest-mock-resources[redis] Redshift fixture support, installs non-driver extras specific to redshift supportpip install pytest-mock-resources[redshift]Additionally there are number of convenience extras currently providedfor installing driversclients of specific features. However in most cases,you should already be installing the driverclient used for that fixtureas as first-party dependency of your project.As such, we recommend against using these extras, and instead explcitly dependingon the package in question in your own projects 1st party dependencies.bash Installs psycopg2psycopg2-binary driverpip install pytest-mock-resources[postgres-binary]pip install pytest-mock-resources[postgres] Installs asyncpg driverpip install pytest-mock-resources[postgres-async] Installs pymysql driverpip install pytest-mock-resources[mysql] Possible Future Resources- Rabbit Broker- AWS PrestoFeel free to file an [issue](httpsgithub.comschiresonpytest-mock-resourcesissues) if you findany bugs or want to start a conversation around a mock resource you want implemented Python 2Releases in the 1.x series were supportive of python 2. However starting from 2.0.0, support forpython 2 was dropped. We may accept bugfix PRs for the 1.x series, however new development andfeatures will not be backported.</longdescription>
</pkgmetadata>