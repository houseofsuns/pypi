<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Digital Ocean API Python Wrapper================================Inspired by [dop](https://github.com/ahmontero/dop).Installation============.. code-block:: bash        # pip install dopyGetting Started===============To interact with Digital Ocean, you first need .. a digital ocean account with valid API keys.Keys can be set either as Env variables, or within the code.For API v.2... code-block:: bash    # export DO_API_VERSION='2'    # export DO_API_TOKEN='api_token'.. code-block:: pycon    &gt;&gt;&gt; from dopy.manager import DoManager    &gt;&gt;&gt; do = DoManager(None, 'api_token', api_version=2)For API v.1... code-block:: bash        # export DO_CLIENT_ID='client_id'    # export DO_API_KEY='long_api_key'.. code-block:: pycon    &gt;&gt;&gt; from dopy.manager import DoManager    &gt;&gt;&gt; do = DoManager('client_id', 'long_api_key')Methods=======The methods of the DoManager are self explanatory; ex... code-block:: pycon    &gt;&gt;&gt; do.all_active_droplets()    &gt;&gt;&gt; do.show_droplet('12345')    &gt;&gt;&gt; do.destroy_droplet('12345')    &gt;&gt;&gt; do.all_regions()    &gt;&gt;&gt; do.all_images()    &gt;&gt;&gt; do.all_ssh_keys()    &gt;&gt;&gt; do.sizes()    &gt;&gt;&gt; do.all_domains()    &gt;&gt;&gt; do.new_droplet('new_droplet', 66, 1601, 1)The methods for v.2 API are similar, the only differenceis using names instead of IDs for domains and slugs forsizes, images and datacenters; ex... code-block:: pycon    &gt;&gt;&gt; do.show_domain('exapmle.com')    &gt;&gt;&gt; do.new_droplet('new_droplet', '512mb', 'lamp', 'ams2')                                    TODO====See github issue list - post if any neededhttps://github.com/devo-ps/dopy/issues</longdescription>
</pkgmetadata>