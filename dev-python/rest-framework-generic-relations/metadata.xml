<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Rest Framework Generic Relations [[Build Status](httpsgithub.comIan-Footerest-framework-generic-relationsactionsworkflowstests.ymlbadge.svg)](httpsgithub.comIan-Footerest-framework-generic-relationsactionsworkflowstests.yml)This library implements [Django REST Framework](httpwww.django-rest-framework.org) serializers to handle generic foreign keys. RequirementsAny currently-supported combination of Django REST Framework, Python, and Django. InstallationInstall using pip...shpip install  rest-framework-generic-relationsAdd generic_relations to your INSTALLED_APPS setting.pythonINSTALLED_APPS  (    ...    generic_relations,) API Reference GenericRelatedFieldThis field serializes generic foreign keys. For a primer on generic foreign keys, first see httpsdocs.djangoproject.comendevrefcontribcontenttypesLets assume a TaggedItem model which has a generic relationship with other arbitrary modelspythonclass TaggedItem(models.Model)    tag_name  models.SlugField()    content_type  models.ForeignKey(ContentType, on_deletemodels.CASCADE)    object_id  models.PositiveIntegerField()    tagged_object  GenericForeignKey(content_type, object_id)And the following two models, which may have associated tagspythonclass Bookmark(models.Model)        A bookmark consists of a URL, and 0 or more descriptive tags.        url  models.URLField()    tags  GenericRelation(TaggedItem)class Note(models.Model)        A note consists of some text, and 0 or more descriptive tags.        text  models.CharField(max_length1000)    tags  GenericRelation(TaggedItem)Now we define serializers for each model that may get associated with tags.pythonclass BookmarkSerializer(serializers.ModelSerializer)    class Meta        model  Bookmark        fields  (url,)class NoteSerializer(serializers.ModelSerializer)    class Meta        model  Note        fields  (text,)The model serializer for the TaggedItem model could look like thispythonfrom generic_relations.relations import GenericRelatedFieldclass TagSerializer(serializers.ModelSerializer)        A TaggedItem serializer with a GenericRelatedField mapping all possible    models to their respective serializers.        tagged_object  GenericRelatedField({        Bookmark BookmarkSerializer(),        Note NoteSerializer()    })    class Meta        model  TaggedItem        fields  (tag_name, tagged_object)The JSON representation of a TaggedItem object with namedjango and its generic foreign key pointing at a Bookmark object with urlhttpswww.djangoproject.com would look like thisjson{    tagged_object {        url httpswww.djangoproject.com    },    tag_name django}If you want to have your generic foreign key represented as hyperlink, simply use HyperlinkedRelatedField objectspythonclass TagSerializer(serializers.ModelSerializer)        A Tag serializer with a GenericRelatedField mapping all possible    models to properly set up HyperlinkedRelatedFields.        tagged_object  GenericRelatedField({        Bookmark serializers.HyperlinkedRelatedField(            queryset  Bookmark.objects.all(),            view_namebookmark-detail,        ),        Note serializers.HyperlinkedRelatedField(            queryset  Note.objects.all(),            view_namenote-detail,        ),    })    class Meta        model  TaggedItem        fields  (tag_name, tagged_object)The JSON representation of the same TaggedItem example object could now look something like thisjson{    tagged_object bookmark1,    tag_name django} Writing to generic foreign keysThe above TagSerializer is also writable. By default, a GenericRelatedField iterates over its nested serializers and returns the value of the first serializer that is actually able to perform to_internal_value() without any errors.Note, that (at the moment) only HyperlinkedRelatedField is able to serialize model objects out of the box.The following operations would create a TaggedItem object with its tagged_object property pointing at the Bookmark object found at the given detail end point.pythontag_serializer  TagSerializer(data{    tag_name python,    tagged_object bookmark1})tag_serializer.is_valid()tag_serializer.save()If you feel that this default behavior doesnt suit your needs, you can subclass GenericRelatedField and override its get_serializer_for_instance or get_deserializer_for_data respectively to implement your own way of decision-making. GenericModelSerializerSometimes you may want to serialize a single list of different top-level things. For instance, suppose I have an API view that returns what items are on my bookshelf. Lets define some modelspythonfrom django.core.validators import MaxValueValidatorclass Book(models.Model)    title  models.CharField(max_length255)    author  models.CharField(max_length255)class Bluray(models.Model)    title  models.CharField(max_length255)    rating  models.PositiveSmallIntegerField(        validators[MaxValueValidator(5)],    )Then we could have a serializer for each type of objectpythonclass BookSerializer(serializers.ModelSerializer)    class Meta        model  Book        fields  (title, author)class BluraySerializer(serializers.ModelSerializer)    class Meta        model  Bluray        fields  (title, rating)Now we can create a generic list serializer, which delegates to the above serializers based on the type of model its serializingpythonbookshelf_item_serializer  GenericModelSerializer(    {        Book BookSerializer(),        Bluray BluraySerializer(),    },    manyTrue,)Then we can serialize a mixed list of itemspython bookshelf_item_serializer.to_representation([    Book.objects.get(titleWar and Peace),    Bluray.objects.get(titleDie Hard),    Bluray.objects.get(titleShawshank Redemption),    Book.objects.get(titleTo Kill a Mockingbird),])[    {title War and Peace, author Leo Tolstoy},    {title Die Hard, rating 5},    {title Shawshank Redemption, rating 5},    {title To Kill a Mockingbird, author Harper Lee}] A few things you should note Although GenericForeignKey fields can be set to any model object, the GenericRelatedField only handles models explicitly defined in its configuration dictionary. Reverse generic keys, expressed using the GenericRelation field, can be serialized using the regular relational field types, since the type of the target in the relationship is always known. The order in which you register serializers matters as far as write operations are concerned. Unless you provide a custom get_deserializer_for_data() method, only HyperlinkedRelatedField provides write access to generic model relations.</longdescription>
</pkgmetadata>