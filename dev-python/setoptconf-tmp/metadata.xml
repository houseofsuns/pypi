<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>IMPORTANTThis is a fork of setoptconf httpsgithub.comjayclasslesssetoptconf_ .It is a temporary solution to fix prospector httpsgithub.comPyCQAprospector_being unable to install due to setoptconf not working with setuptools  58 - see the issue here httpsgithub.comPyCQAprospectorissues438_.This package is only meant as a temporary solution to fix CI builds using prosectoruntil setoptconf can be removed from there. Therefore do not expect this fork to be maintained and it will be purged once prospector is updated.setoptconf-tmpsetoptconf is a Python library that can be used to retrieve program settingsfrom a variety of common sources Command Line Environment Variables INI Files JSON Files YAML Files Python ObjectsModulesThe goal of this project is to define your desired settings in a simple andconsistent way, and then point setoptconf at as many of the sources as youdlike to use, and let it comb them all, looking for your settings.This README is admittedly very light on details. Full documentation will comein time. For now, heres an example of its useImport the library    import setoptconf as socInstantiate the manager    manager  soc.ConfigurationManager(myprogram)Define the settings wed like to collect    manager.add(soc.StringSetting(foo))    manager.add(soc.IntegerSetting(bar, requiredTrue))    manager.add(soc.BooleanSetting(baz, defaultTrue))Retreive the settings from our desired sources, combining the settings andoverriding with the priority implied by the order of the sources we pass    config  manager.retrieve(         This source pulls from the command line using argparse.        soc.CommandLineSource,         This source pulls from environment variables that are prefixed         with MYPROGRAM_        soc.EnvironmentVariableSource,         This source pulls from the named INI files. It stops at the first         file it finds.        soc.ConfigFileSource((.myprogramrc, etcmyprogram.conf)),    )We now have a Configuration object named config that has three attributesfoo, bar, and baz.</longdescription>
</pkgmetadata>