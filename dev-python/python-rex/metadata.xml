<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Welcome to python-Rex.. image httpspypip.invpython-rexbadge.png   target httpscrate.iopackagespython-rex.. image httpspypip.indpython-rexbadge.png   target httpscrate.iopackagespython-rex.. image httpstravis-ci.orgcypreesspython-rex.pngbranchmaster   target httpstravis-ci.orgcypreesspython-rex.. image httpscoveralls.ioreposcypreesspython-rexbadge.pngbranchmaster   target httpscoveralls.iorcypreesspython-rexbranchmaster   Python Rex is regular expressions for humans. (Rex is also abbreviation from re X tended).Rex is for the re standard module httpdocs.python.org2libraryindex.html_ asrequests httpdocs.python-requests.orgenlatest_ is for urllib module httpdocs.python.org2libraryurllib.html_.Rex also is latin for king httpen.wikipedia.orgwikiRex_, and the king of regular expressions is Perl.So Rex API tries to mimic at least some Perls idioms.Supported Python versions 2.6, 2.7, 3.3Installation    pip install python-rexor      pip install -e git+httpsgithub.comcypreesspython-rex.giteggrex-devThere are no external dependencies.       from rex import rexQuickstartDo that   from rex import rex   print (Your ticket number XyZ-1047. Have fun  rex([a-z]{3}-(d{4})i))[1]    instead of doing that   import re   regex  re.compile([a-z]{3}-(d{4}), flagsre.IGNORECASE)   m  regex.search(Your ticket number XyZ-1047. Have fun)      if m is not None      print m.group(1)   else      print None       or in shorter way   print m.group(1) if m else None(both should print 1047).DocsSo far Rex supports simple matching (first match), substitution, all python re flags.Matching --------The most obvious usage - test condition by matching to string    if This is a dog  rex(dog)        print Oh yeahor    if My lucky 777 number  rex([0-9]+)        print Number foundYou can use Perl notation and prepend m character to your search    if My lucky 777 number  rex(m[0-9]+)        print Number foundbut you can also simply check your match    if (My lucky 777 number  rex(m[0-9]+))[0]  777        print Number foundor even groups    if (My lucky 777 number  rex(m(Pnumber[0-9]+)))[number]  777        print Number foundRemember a mess with re module when it does not match anything Rex wont let you down,it will kindly return None for whatever you ask     print (My lucky 777 number  rex(m(Pnumber[0-9]+)))[no_such_group]    None     print (I dont tell you my lucky number  rex(m(Pnumber[0-9]+)))[number]    NoneSubstituting------------Substitution can be made by prefixing pattern with s character (like in perl expression)     print This is a cat  rex(sCATdogi)    This is a dogFlags-----Every Rex pattern as in Perl patterns allows to suffix some flags, e.g. rex(patterniu) for enabling i and u flag. Rex supports all standard python re flags d - re.DEBUG i - re.IGNORECASE l - re.LOCALE m - re.MULTILINE s - re.DOTALL u - re.UNICODE x - re.VERBOSECaching-------Rex caches all patterns so reusing patterns is super fast. You can always clear Rex cache by calling rex_clear_cache() ordisable caching for specific patterns rex(pattern, cacheFalse).Rex for orthodox----------------If you are so orthodox pythonist that couldnt leave with overloaded  operator syntax in your codebase,you can use orthodox mode of rex. Just put the string to matchsubstitute against as a second argument     bool(rex(dog, This is a dog))    True     rex(scatdog, This is a cat)    This is a dogAdditionally Rex objects are callable. This is especially useful in situations where you need to process many valuesagainst the same regular expression     my_re  rex(foo)     for thing in [foobar, bar, barfoo]    ...     print bool(my_re(thing))    True    False    True</longdescription>
</pkgmetadata>