<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>smart_open  utils for streaming large files in PythonLicense_ GHA_ Coveralls_ Downloads_.. License image httpsimg.shields.iopypilsmart_open.svg.. GHA image httpsgithub.comRaRe-Technologiessmart_openworkflowsTestbadge.svg.. Coveralls image httpscoveralls.ioreposgithubRaRe-Technologiessmart_openbadge.svgbranchdevelop.. Downloads image httpspepy.techbadgesmart-openmonth.. _License httpsgithub.comRaRe-Technologiessmart_openblobmasterLICENSE.. _GHA httpsgithub.comRaRe-Technologiessmart_openactionsqueryworkflow3ATest.. _Coveralls httpscoveralls.iogithubRaRe-Technologiessmart_openbranchHEAD.. _Downloads httpspypi.orgprojectsmart-openWhatsmart_open is a Python 3 library for efficient streaming of very large files fromto storages such as S3, GCS, Azure Blob Storage, HDFS, WebHDFS, HTTP, HTTPS, SFTP, or local filesystem. It supports transparent, on-the-fly (de-)compression for a variety of different formats.smart_open is a drop-in replacement for Pythons built-in open() it can do anything open can (100 compatible, falls back to native open wherever possible), plus lots of nifty extra stuff on top.Python 2.7 is no longer supported. If you need Python 2.7, please use smart_open 1.10.1 httpsgithub.comRaRe-Technologiessmart_openreleasestag1.10.0_, the last version to support Python 2.WhyWorking with large remote files, for example using Amazons boto3 httpsboto3.amazonaws.comv1documentationapilatestindex.html_ Python library, is a pain.boto3s Object.upload_fileobj() and Object.download_fileobj() methods require gotcha-prone boilerplate to use successfully, such as constructing file-like object wrappers.smart_open shields you from that. It builds on boto3 and other remote storage libraries, but offers a clean unified Pythonic API. The result is less code for you to write and fewer bugs to make.Howsmart_open is well-tested, well-documented, and has a simple Pythonic API.. _doctools_before_examples.. code-block python   from smart_open import open      stream lines from an S3 object   for line in open(s3commoncrawlrobots.txt)  ...    print(repr(line))  ...    break  User-Agent n    stream fromto compressed files, with transparent (de)compression   for line in open(smart_openteststest_data1984.txt.gz, encodingutf-8)  ...    print(repr(line))  It was a bright cold day in April, and the clocks were striking thirteen.n  Winston Smith, his chin nuzzled into his breast in an effort to escape the vilen  wind, slipped quickly through the glass doors of Victory Mansions, though notn  quickly enough to prevent a swirl of gritty dust from entering along with him.n    can use context managers too   with open(smart_openteststest_data1984.txt.gz) as fin  ...    with open(smart_openteststest_data1984.txt.bz2, w) as fout  ...        for line in fin  ...           fout.write(line)  74  80  78  79    can use any IOBase operations, like seek   with open(s3commoncrawlrobots.txt, rb) as fin  ...     for line in fin  ...         print(repr(line.decode(utf-8)))  ...         break  ...     offset  fin.seek(0)   seek to the beginning  ...     print(fin.read(4))  User-Agent n  bUser    stream from HTTP   for line in open(httpexample.comindex.html)  ...     print(repr(line))  ...     break  doctype htmln.. _doctools_after_examplesOther examples of URLs that smart_open accepts    s3my_bucketmy_key    s3my_keymy_secretmy_bucketmy_key    s3my_keymy_secretmy_servermy_portmy_bucketmy_key    gsmy_bucketmy_blob    azuremy_bucketmy_blob    hdfspathfile    hdfspathfile    webhdfshostportpathfile    .localpathfile    localpathfile    localpathfile    .localpathfile.gz    filehomeuserfile    filehomeuserfile.bz2    [sshscpsftp]usernamehostpathfile    [sshscpsftp]usernamehostpathfile    [sshscpsftp]usernamepasswordhostpathfileDocumentationInstallation------------smart_open supports a wide range of storage solutions, including AWS S3, Google Cloud and Azure.Each individual solution has its own dependencies.By default, smart_open does not install any dependencies, in order to keep the installation size small.You can install these dependencies explicitly using    pip install smart_open[azure]  Install Azure deps    pip install smart_open[gcs]  Install GCS deps    pip install smart_open[s3]  Install S3 depsOr, if you dont mind installing a large number of third party libraries, you can install all dependencies using    pip install smart_open[all]Be warned that this option increases the installation size significantly, e.g. over 100MB.If youre upgrading from smart_open versions 2.x and below, please check out the Migration Guide MIGRATING_FROM_OLDER_VERSIONS.rst_.Built-in help-------------For detailed API info, see the online help.. code-block python    help(smart_open)or click here httpsgithub.comRaRe-Technologiessmart_openblobmasterhelp.txt__ to view the help in your browser.More examples-------------For the sake of simplicity, the examples below assume you have all the dependencies installed, i.e. you have done    pip install smart_open[all].. code-block python     import os, boto3          stream content into S3 (write mode) using a custom session     session  boto3.Session(    ...     aws_access_key_idos.environ[AWS_ACCESS_KEY_ID],    ...     aws_secret_access_keyos.environ[AWS_SECRET_ACCESS_KEY],    ... )     url  s3smart-open-py37-benchmark-resultstest.txt     with open(url, wb, transport_params{client session.client(s3)}) as fout    ...     bytes_written  fout.write(bhello world)    ...     print(bytes_written)    12.. code-block python     stream from HDFS    for line in open(hdfsuserhadoopmy_file.txt, encodingutf8)        print(line)     stream from WebHDFS    for line in open(webhdfshostportuserhadoopmy_file.txt)        print(line)     stream content into HDFS (write mode)    with open(hdfshostportuserhadoopmy_file.txt, wb) as fout        fout.write(bhello world)     stream content into WebHDFS (write mode)    with open(webhdfshostportuserhadoopmy_file.txt, wb) as fout        fout.write(bhello world)     stream from a completely custom s3 server, like s3proxy    for line in open(s3uusersecrethostportmybucketmykey.txt)        print(line)     Stream to Digital Ocean Spaces bucket providing credentials from boto3 profile    session  boto3.Session(profile_namedigitalocean)    client  session.client(s3, endpoint_urlhttpsams3.digitaloceanspaces.com)    transport_params  {client client}    with open(s3bucketkey.txt, wb, transport_paramstransport_params) as fout        fout.write(bhere we stand)     stream from GCS    for line in open(gsmy_bucketmy_file.txt)        print(line)     stream content into GCS (write mode)    with open(gsmy_bucketmy_file.txt, wb) as fout        fout.write(bhello world)     stream from Azure Blob Storage    connect_str  os.environ[AZURE_STORAGE_CONNECTION_STRING]    transport_params  {        client azure.storage.blob.BlobServiceClient.from_connection_string(connect_str),    }    for line in open(azuremycontainermyfile.txt, transport_paramstransport_params)        print(line)     stream content into Azure Blob Storage (write mode)    connect_str  os.environ[AZURE_STORAGE_CONNECTION_STRING]    transport_params  {        client azure.storage.blob.BlobServiceClient.from_connection_string(connect_str),    }    with open(azuremycontainermy_file.txt, wb, transport_paramstransport_params) as fout        fout.write(bhello world)Compression Handling--------------------The top-level compression parameter controls compressiondecompression behavior when reading and writing.The supported values for this parameter are- infer_from_extension (default behavior)- disable- .gz- .bz2By default, smart_open determines the compression algorithm to use based on the file extension... code-block python     from smart_open import open, register_compressor     with open(smart_openteststest_data1984.txt.gz) as fin    ...     print(fin.read(32))    It was a bright cold day in ApriYou can override this behavior to either disable compression, or explicitly specify the algorithm to use.To disable compression.. code-block python     from smart_open import open, register_compressor     with open(smart_openteststest_data1984.txt.gz, rb, compressiondisable) as fin    ...     print(fin.read(32))    bx1fx8bx08x08x85Fx94x00x031984.txtx005x8frxc3x08x85{x9dxe2x1dTo specify the algorithm explicitly (e.g. for non-standard file extensions).. code-block python     from smart_open import open, register_compressor     with open(smart_openteststest_data1984.txt.gzip, compression.gz) as fin    ...     print(fin.read(32))    It was a bright cold day in ApriYou can also easily add support for other file extensions and compression formats.For example, to open xz-compressed files.. code-block python     import lzma, os     from smart_open import open, register_compressor     def _handle_xz(file_obj, mode)    ...      return lzma.LZMAFile(filenamefile_obj, modemode, formatlzma.FORMAT_XZ)     register_compressor(.xz, _handle_xz)     with open(smart_openteststest_data1984.txt.xz) as fin    ...     print(fin.read(32))    It was a bright cold day in Aprilzma is in the standard library in Python 3.3 and greater.For 2.7, use backports.lzma_... _backports.lzma httpspypi.orgprojectbackports.lzmaTransport-specific Options--------------------------smart_open supports a wide range of transport options out of the box, including- S3- HTTP, HTTPS (read-only)- SSH, SCP and SFTP- WebHDFS- GCS- Azure Blob StorageEach option involves setting up its own set of parameters.For example, for accessing S3, you often need to set up authentication, like API keys or a profile name.smart_opens open function accepts a keyword argument transport_params which accepts additional parameters for the transport layer.Here are some examples of using this parameter.. code-block python   import boto3   fin  open(s3commoncrawlrobots.txt, transport_paramsdict(clientboto3.client(s3)))   fin  open(s3commoncrawlrobots.txt, transport_paramsdict(buffer_size1024))For the full list of keyword arguments supported by each transport option, see the documentation.. code-block python  help(smart_open.open)S3 Credentials--------------smart_open uses the boto3 library to talk to S3.boto3 has several mechanisms httpsboto3.amazonaws.comv1documentationapilatestguideconfiguration.html__ for determining the credentials to use.By default, smart_open will defer to boto3 and let the latter take care of the credentials.There are several ways to override this behavior.The first is to pass a boto3.Client object as a transport parameter to the open function.You can customize the credentials when constructing the session for the client.smart_open will then use the session when talking to S3... code-block python    session  boto3.Session(        aws_access_key_idACCESS_KEY,        aws_secret_access_keySECRET_KEY,        aws_session_tokenSESSION_TOKEN,    )    client  session.client(s3, endpoint_url..., config...)    fin  open(s3bucketkey, transport_paramsdict(clientclient))Your second option is to specify the credentials within the S3 URL itself.. code-block python    fin  open(s3aws_access_key_idaws_secret_access_keybucketkey, ...)Important The two methods above are mutually exclusive. If you pass an AWS client and the URL contains credentials, smart_open will ignore the latter.Important smart_open ignores configuration files from the older boto library.Port your old boto settings to boto3 in order to use them with smart_open.Iterating Over an S3 Buckets Contents--------------------------------------Since going over all (or select) keys in an S3 bucket is a very common operation, theres also an extra function smart_open.s3.iter_bucket() that does this efficiently, processing the bucket keys in parallel (using multiprocessing).. code-block python   from smart_open import s3    we use workers1 for reproducibility you should use as many workers as you have cores   bucket  silo-open-data   prefix  Officialannualmonthly_rain   for key, content in s3.iter_bucket(bucket, prefixprefix, accept_keylambda key 201 in key, workers1, key_limit3)  ...     print(key, round(len(content)  220))  Officialannualmonthly_rain2010.monthly_rain.nc 13  Officialannualmonthly_rain2011.monthly_rain.nc 13  Officialannualmonthly_rain2012.monthly_rain.nc 13GCS Credentials---------------smart_open uses the google-cloud-storage library to talk to GCS.google-cloud-storage uses the google-cloud package under the hood to handle authentication.There are several options httpsgoogleapis.devpythongoogle-api-corelatestauth.html__ to providecredentials.By default, smart_open will defer to google-cloud-storage and let it take care of the credentials.To override this behavior, pass a google.cloud.storage.Client object as a transport parameter to the open function.You can customize the credentials httpsgoogleapis.devpythonstoragelatestclient.html__when constructing the client. smart_open will then use the client when talking to GCS. To follow allow withthe example below, refer to Googles guide httpscloud.google.comstoragedocsreferencelibrariessetting_up_authentication__to setting up GCS authentication with a service account... code-block python    import os    from google.cloud.storage import Client    service_account_path  os.environ[GOOGLE_APPLICATION_CREDENTIALS]    client  Client.from_service_account_json(service_account_path)    fin  open(gsgcp-public-data-landsatindex.csv.gz, transport_paramsdict(clientclient))If you need more credential options, you can create an explicit google.auth.credentials.Credentials objectand pass it to the Client. To create an API token for use in the example below, refer to theGCS authentication guide httpscloud.google.comstoragedocsauthenticationapiauth__... code-block pythonimport osfrom google.auth.credentials import Credentialsfrom google.cloud.storage import Clienttoken  os.environ[GOOGLE_API_TOKEN]credentials  Credentials(tokentoken)client  Client(credentialscredentials)fin  open(gsgcp-public-data-landsatindex.csv.gz, transport_paramsdict(clientclient))Azure Credentials-----------------smart_open uses the azure-storage-blob library to talk to Azure Blob Storage.By default, smart_open will defer to azure-storage-blob and let it take care of the credentials.Azure Blob Storage does not have any ways of inferring credentials therefore, passing a azure.storage.blob.BlobServiceClientobject as a transport parameter to the open function is required.You can customize the credentials httpsdocs.microsoft.comen-usazurestoragecommonstorage-samples-pythonauthentication__when constructing the client. smart_open will then use the client when talking to. To follow allow withthe example below, refer to Azures guide httpsdocs.microsoft.comen-usazurestorageblobsstorage-quickstart-blobs-pythoncopy-your-credentials-from-the-azure-portal__to setting up authentication... code-block python    import os    from azure.storage.blob import BlobServiceClient    azure_storage_connection_string  os.environ[AZURE_STORAGE_CONNECTION_STRING]    client  BlobServiceClient.from_connection_string(azure_storage_connection_string)    fin  open(azuremy_containermy_blob.txt, transport_paramsdict(clientclient))If you need more credential options, refer to theAzure Storage authentication guide httpsdocs.microsoft.comen-usazurestoragecommonstorage-samples-pythonauthentication__.Drop-in replacement of pathlib.Path.open--------------------------------------------smart_open.open can also be used with Path objects.The built-in Path.open() is not able to read text from compressed files, so use patch_pathlib to replace it with smart_open.open() instead.This can be helpful when e.g. working with compressed files... code-block python     from pathlib import Path     from smart_open.smart_open_lib import patch_pathlib         _  patch_pathlib()   replace Path.open with smart_open.open         path  Path(smart_openteststest_datacrime-and-punishment.txt.gz)         with path.open(r) as infile    ...     print(infile.readline()[41])      ,    How do I ...See this document howto.md__.Extending smart_openSee this document extending.md__.Testing smart_opensmart_open comes with a comprehensive suite of unit tests.Before you can run the test suite, install the test dependencies    pip install -e .[test]Now, you can run the unit tests    pytest smart_openThe tests are also run automatically with Travis CI httpstravis-ci.orgRaRe-Technologiessmart_open_ on every commit push  pull request.Comments, bug reportssmart_open lives on Github httpsgithub.comRaRe-Technologiessmart_open_. You can fileissues or pull requests there. Suggestions, pull requests and improvements welcome----------------smart_open is open source software released under the MIT license httpsgithub.compiskvorkysmart_openblobmasterLICENSE_.Copyright (c) 2015-now Radim ehek httpsradimrehurek.com_.</longdescription>
</pkgmetadata>