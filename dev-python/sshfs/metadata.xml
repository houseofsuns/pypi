<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> sshfssshfs is an implementation of [fsspec](httpsgithub.comintakefilesystem_spec) forthe SFTP protocol using [asyncssh](httpsgithub.comronfasyncssh). Features- A complete implementation of the fsspec protocol through SFTP- Supports features outside of the SFTP (e.g server side copy through SSH command execution)- Quite fast (compared to alternatives like paramiko)- Builtin Channel Management- Async (thanks to asyncssh) TutorialInstall the sshfs from PyPI or the conda-forge, and import itpyfrom sshfs import SSHFileSystemTo connect with a password, you can simply specify usernamepasswordas keyword arguments and connect to the host of your choosingpy Connect with a passwordfs  SSHFileSystem(    127.0.0.1,    usernamesam,    passwordfishing)If you want to use a private key to authenticate, you can eitherpass a string pointing to the path of the key, or give a list ofthem to be triedpy or with a private keyfs  SSHFileSystem(    ssh.example.com,    client_keys[pathtosshkey])All operations and their descriptions are specified [here](httpsfilesystem-spec.readthedocs.ioenlatestapi.htmlfsspec.spec.AbstractFileSystem).Here are a few example calls you can make, starting with info() which allows you to retrieve the metadata about given pathpy details  fs.info(tmp) print(f{details[name]r} is a {details[type]})tmp is a directory crontab  fs.info(etccrontab) print(f{crontab[name]r} is a {crontab[type]})etccrontab is a fileYou can also create new files through either putting a local file with put_file or opening a file in write modepy with fs.open(tmpmessage.dat, wb) as stream...     stream.write(bsuper secret messsage)... And either download it through get_file or simply read it on the fly with opening itpy with fs.open(tmpmessage.dat) as stream...     print(stream.read())... bsuper secret messsageThere are also a lot of other basic filesystem operations, such as mkdir, touch and findpy fs.mkdir(tmpdir) fs.mkdir(tmpdireggs) fs.touch(tmpdirspam) fs.touch(tmpdireggsquux)  for file in fs.find(tmpdir)...     print(file)... tmpdireggsquuxtmpdirspamIf you want to list a directory but not its children, you can use ls()py [(detail[name], detail[type]) for detail in fs.ls(tmpdir, detailTrue)][(tmpdirspam, file), (tmpdireggs, directory)]</longdescription>
</pkgmetadata>