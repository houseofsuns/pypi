<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> PyGeth[[Build Status](httpscircleci.comghethereumpy-geth.svgstyleshield)](httpscircleci.comghethereumpy-geth)[[PyPi version](httpsimg.shields.iopypivpy-geth.svg)](httpspypi.python.orgpypipy-geth)Python wrapper around running geth as a subprocess System DependencyThis library requires the geth executable to be present. InstallationInstallationbashpip install py-geth QuickstartTo run geth connected to the mainnetpython from geth import LiveGethProcess geth  LiveGethProcess() geth.start()Or a private local chain for testing.  These require you to give them a name.python from geth import DevGethProcess geth  DevGethProcess(testing) geth.start()By default the DevGethProcess sets up test chains in the default datadirused by geth.  If you would like to change the location for these testchains, you can specify an alternative base_dir.python geth  DevGethProcess(testing, tmpsome-other-base-dir) geth.start()Each instance has a few convenient properties.python geth.data_dir.ethereum geth.rpc_port8545 geth.ipc_path.ethereumgeth.ipc geth.accounts[0xd3cda913deb6f67967b99d67acdfa1712c293601] geth.is_aliveFalse geth.is_runningFalse geth.is_stoppedFalse geth.start() geth.is_aliveTrue   indicates that the subprocess hasnt exited geth.is_runningTrue   indicates that start() has been called (but stop() hasnt) geth.is_stoppedFalse geth.stop() geth.is_aliveFalse geth.is_runningFalse geth.is_stoppedTrueWhen testing it can be nice to see the logging output produced by the gethprocess.  py-geth provides a mixin class that can be used to log the stdoutand stderr output to a logfile.python from geth import LoggingMixin, DevGethProcess class MyGeth(LoggingMixin, DevGethProcess)...     pass geth  MyGeth() geth.start()All logs will be written to logfiles in .logs in the current directory.The underlying geth process can take additional time to open the RPC or IPCconnections, as well as to start mining if it needs to generate the DAG.  Youcan use the following interfaces to query whether these are ready.python geth.is_rpc_readyTrue geth.wait_for_rpc(timeout30)   wait up to 30 seconds for the RPC connection to open geth.is_ipc_readyTrue geth.wait_for_ipc(timeout30)   wait up to 30 seconds for the IPC socket to open geth.is_dag_generatedTrue geth.is_miningTrue geth.wait_for_dag(timeout600)   wait up to 10 minutes for the DAG to generate. The DAG functionality currently only applies to the DAG for epoch 0. Installing specific versions of geth This feature is experimental and subject to breaking changes.Versions of geth dating back to v1.9.14 can be installed using py-geth.See [install.py](httpsgithub.comethereumpy-gethblobmastergethinstall.py) forthe current list of supported versions.Installation can be done via the command linebash python -m geth.install v1.11.2Or from python using the install_geth function.python from geth import install_geth install_geth(v1.11.2)The installed binary can be found in the HOME.py-geth directory, under yourhome directory.  The v1.11.2 binary would be located atHOME.py-gethgeth-v1.11.2bingeth. About DevGethProcessThe DevGethProcess is designed to facilitate testing.  In that regard, it ispreconfigured as follows. A single account is created and allocated 1 billion ether. All APIs are enabled on both rpc and ipc interfaces. Account 0 is unlocked Networking is configured to not look for or connect to any peers. The networkid of 1234 is used. Verbosity is set to 5 (DEBUG) Mining is enabled with a single thread. The RPC interface tries to bind to 8545 but will find an open port if this  port is not available. The DevP2P interface tries to bind to 30303 but will find an open port if this  port is not available. GotchasIf you are running with mining enabled, which is default for DevGethProcess,then you will likely need to generate the DAG manually.  If you do not, thenit will auto-generate the first time you run the process and this takes awhile.To generate it manuallysh geth makedag 0 .ethashThis is especially important in CI environments like Travis-CI where yourprocess will likely timeout during generation. DevelopmentClone the repositoryshell git clone gitgithub.comethereumpy-geth.gitNext, run the following from the newly-created py-geth directorysh pip install -e .[dev] Running the testsYou can run the tests withshpytest testsOr you can install tox to run the full test suite. ReleasingPandoc is required for transforming the markdown README to the proper format torender correctly on pypi.For Debian-like systemsapt install pandocOr on OSXshbrew install pandocTo release a new versionshmake release bumpVERSION_PART_TO_BUMPThe version format for this repo is {major}.{minor}.{patch} for stable, and{major}.{minor}.{patch}-{stage}.{devnum} for unstable (stage can be alpha or beta).To issue the next version in line, specify which part to bump,like make release bumpminor or make release bumpdevnum.If you are in a beta version, make release bumpstage will switch to a stable.To issue an unstable version when the current version is stable, specify thenew version explicitly, like make release bump--new-version 4.0.0-alpha.1 devnum Adding Support For New Geth VersionsThere is an automation script to facilitate adding support for new geth versions update_geth.pyTo add support for a geth version, run the following line from the py-geth directory, substitutingthe version for the one you wish to add support for. Note that the v in the versioning isoptional.shell python update_geth.py v1.10.9To introduce support for more than one version, pass in the versions in increasing order,ending with the latest version.shell python update_geth.py v1.10.7 v1.10.8 v1.10.9Always review your changes before committing as something may cause this existing pattern to change at some point. It is best to compare the git difference with a previous commit that introduced support for a new geth version to make sure everything looks good.</longdescription>
</pkgmetadata>