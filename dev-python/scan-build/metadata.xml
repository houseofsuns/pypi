<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. image httpsimg.shields.iopypivscan-build.svg       target httpspypi.python.orgpypiscan-build.. image httpsimg.shields.iopypilscan-build.svg       target httpspypi.python.orgpypiscan-build.. image httpsimg.shields.iopypidmscan-build.svg       target httpspypi.python.orgpypiscan-build.. image httpsimg.shields.iopypipyversionsscan-build.svg       target httpspypi.python.orgpypiscan-build.. image httpsbadges.gitter.imrizsottoscan-build.svg        target httpsgitter.imrizsottoscan-buildutm_sourcebadgeutm_mediumbadgeutm_campaignpr-badgeutm_contentbadgescan-buildA package designed to wrap a build so that all calls to gccclang areintercepted and logged into a compilation database_ andor piped tothe clang static analyzer. Includes intercept-build tool, which logsthe build, as well as scan-build tool, which logs the build and runsthe clang static analyzer on it.How to get----------Its available from the Python Package Index_      pip install scan-buildPortability-----------Should be working on UNIX operating systems.- It has been tested on FreeBSD, GNULinux, OS X and Windows.Prerequisites-------------1. clang compiler, to compile the sources and have the static analyzer.2. python interpreter (version 3.6, 3.7, 3.8, 3.9).How to use----------To run the Clang static analyzer against a project goes like this     scan-build your build commandTo generate a compilation database file goes like this     intercept-build your build commandTo run the Clang static analyzer against a project with compilation databasegoes like this     analyze-buildUse --help to know more about the commands.Limitations-----------Generally speaking, the intercept-build and analyze-build toolstogether does the same job as scan-build does. So, you can expect thesame output from this line as simple scan-build would do     intercept-build your build command  analyze-buildThe major difference is how and when the analyzer is run. The scan-buildtool has three distinct model to run the analyzer1.  Use compiler wrappers to make actions.    The compiler wrappers does run the real compiler and the analyzer.    This is the default behaviour, can be enforced with --override-compiler    flag.2.  Use special library to intercept compiler calls during the build process.    The analyzer run against each modules after the build finished.    Use --intercept-first flag to get this model.3.  Use compiler wrappers to intercept compiler calls during the build process.    The analyzer run against each modules after the build finished.    Use --intercept-first and --override-compiler flags together to get    this model.The 1. and 3. are using compiler wrappers, which works only if the buildprocess respects the CC and CXX environment variables. (Some buildprocess can override these variable as command line parameter only. This caseyou need to pass the compiler wrappers manually. eg. intercept-build--override-compiler make CCintercept-cc CXXintercept-c++ all where theoriginal build command would have been make all only.)The 1. runs the analyzer right after the real compilation. So, if the buildprocess removes removes intermediate modules (generated sources) the analyzeroutput still kept.The 2. and 3. generate the compilation database first, and filters out thosemodules which are not exists. So, its suitable for incremental analysis duringthe development.The 2. mode is available only on FreeBSD, Linux and OSX. Where library preloadis available from the dynamic loader. Security extensionmodes on differentoperating systems might disable library preload. This case the build behavesnormally, but the result compilation database will be empty. (Notable examplesfor enabled security modes are SIP on OS X Captain and SELinux on Fedora,RHEL and CentOS.) The program checks the security modes for SIP, and fallsback to 3. mode.intercept-build command uses only the 2. and 3. mode to generate thecompilation database. analyze-build does only run the analyzer against thecaptured compiler calls.Known problems--------------Because it uses LD_PRELOAD or DYLD_INSERT_LIBRARIES environment variables,it does not append to it, but overrides it. So builds which are using thesevariables might not work. (I dont know any build tool which does that, butplease let me know if you do.)Problem reports---------------If you find a bug in this documentation or elsewhere in the program or wouldlike to propose an improvement, please use the projects issue tracker_.Please describing the bug and where you found it. If you have a suggestionhow to fix it, include that as well. Patches are also welcome.License-------The project is licensed under University of IllinoisNCSA Open Source License.See LICENSE.TXT for details... _compilation database httpclang.llvm.orgdocsJSONCompilationDatabase.html.. _the Python Package Index httpspypi.python.orgpypiscan-build.. _issue tracker httpsgithub.comrizsottoscan-buildissues</longdescription>
</pkgmetadata>