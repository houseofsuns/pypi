<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>eradicate.. image httpsgithub.commyinteradicateactionsworkflowstest.ymlbadge.svg    target httpsgithub.commyinteradicateactionsworkflowstest.yml    alt Build status----eradicate removes commented-out code from Python files.IntroductionWith modern revision control available, there is no reason to savecommented-out code to your repository. eradicate helps cleans upexisting junk comments. It does this by detecting block comments thatcontain valid Python syntax that are likely to be commented out code.(It avoids false positives like the sentence this is not good,which is valid Python syntax, but is probably not code.)Example.. code-block bash     eradicate --in-place example.pyBefore running eradicate... code-block python    import os     from foo import junk    a  3    a  4    foo(1, 2, 3)    def foo(x, y, z)         print(hello)        print(x, y, z)         This is a real comment.        return True        return FalseAfter running eradicate... code-block python    a  4    def foo(x, y, z)        print(x, y, z)         This is a real comment.        return FalseWhitelistingFalse positives can happen so there is a whitelist feature to fix them shorthand.You can either add entries to the default whitelist with --whitelist-extend or overwrite the default with --whitelist.Both arguments expect a string of  separated regex strings (whitespaces are preserved). E.g. eradicate --whitelist foob a r filenameThose regex strings are matched case insensitive against the start of the comment itself.For the default whitelist please see eradicate.py.RelatedThere are different tools, plugins, and integrations for eradicate users- flake8-eradicate httpsgithub.comsobolevnflake8-eradicate_ - Flake8 plugin to find commented out or dead code.</longdescription>
</pkgmetadata>