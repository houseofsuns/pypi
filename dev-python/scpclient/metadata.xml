<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>---------scpclient---------A library that implements the client side of the scp (Secure Copy)protocol. It is designed to be used with paramiko(httpwww.lag.netparamiko).Using paramikoThere are many ways to use paramiko. For the purposes of theseexamples, you want to obtain an authenticated SSHClient object    import paramiko    ssh_client  paramiko.SSHClient()    ssh_client.connect(hostname, usernameusername, key_filenamekey_filename, passwordpassword)Writing filesThe scpclient.Write object is used to write files to an scpserver. Its required parameters are a paramiko.SSHClient instance anda remote directory name. It has 2 methods send_file andsend. send_file takes a filename to send, send takes a file-likeobject, a remote filename, a mode, and a length. You may call send andsend_file multiple times.Example    with closing(Write(ssh_client.get_transport(), .)) as scp        scp.send_file(file.txt, True)        scp.send_file(....test.log, remote_filenamebaz.log)        s  StringIO(this is a test)        scp.send(s, test, 0601, len(s.getvalue()))Writing directoriesExample    with closing(WriteDir(ssh_client.get_transport(), subdir)) as scp        scp.send_dir(....manuals, preserve_timesTrue, progressprogress)Reading filesExample    with closing(ReadDir(ssh_client.get_transport(), .)) as scp        scp.receive_dir(foo, preserve_timesTrue)Reading directoriesExample    with closing(Read(ssh_client.get_transport(), .)) as scp        scp.receive(file.txt)Change log0.7 2010-10-27 Eric V. Smith---------------------------- Remove hack for changing the RPM name (issue 10). No code changes.0.6 2016-10-02 Eric V. Smith---------------------------- Fixed issue 8, Support building wheels. No code changes.0.5 2014-04-27 Eric V. Smith---------------------------- Fixed issue 5, Not all files included in sdist. Fixed issue 6, Rename bdist RPM to python-scpclient. No code changes.0.4 2012-11-28 Eric V. Smith---------------------------- Fixed issue 2, Missing contextlib import. Thanks Olivier CARRERE.0.3 2011-10-31 Eric V. Smith---------------------------- Improved documentation, including how to create a paramiko  connection.0.2 2011-10-31 Eric V. Smith---------------------------- Removed useless tests. Fixed embarassing last minute typo which broke the code. Included trivial examples in README.txt. Needs much work. Forbid files that contain .. in the filename. This is an attempt  to prevent path traversal. This approach is simplistic, but its a  reasonable first step and gets the job done.0.1 2011-10-31 Eric V. Smith---------------------------- Initial release.</longdescription>
</pkgmetadata>