<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>phpserializea port of the serialize and unserialize functions ofphp to python.  This module implements the python serializationinterface (eg provides dumps, loads and similar functions).Usage from phpserialize import  obj  dumps(Hello World) loads(obj)Hello WorldDue to the fact that PHP doesnt know the concept of lists, listsare serialized like hash-maps in PHP.  As a matter of fact thereverse value of a serialized list is a dict loads(dumps(range(2))){0 0, 1 1}If you want to have a list again, you can use the dict_to_listhelper function dict_to_list(loads(dumps(range(2))))[0, 1]Its also possible to convert into a tuple by using the dict_to_tuplefunction dict_to_tuple(loads(dumps((1, 2, 3))))(1, 2, 3)Another problem are unicode strings.  By default unicode strings areencoded to utf-8 but not decoded on unserialize.  The reason forthis is that phpserialize cant guess if you have binary or text datain the strings loads(dumps(uHello Wxf6rld))Hello Wxc3xb6rldIf you know that you have only text data of a known charset in the resultyou can decode strings by setting decode_strings to True when callingloads loads(dumps(uHello Wxf6rld), decode_stringsTrue)uHello Wxf6rldDictionary keys are limited to strings and integers.  None is convertedinto an empty string and floats and booleans into integers for PHPcompatibility loads(dumps({None 14, 42.23 foo, True [1, 2, 3]})){ 14, 1 {0 1, 1 2, 2 3}, 42 foo}It also provides functions to read from file-like objects from StringIO import StringIO stream  StringIO(a2{i0i1i1i2}) dict_to_list(load(stream))[1, 2]And to write to those stream  StringIO() dump([1, 2], stream) stream.getvalue()a2{i0i1i1i2}Like pickle chaining of objects is supported stream  StringIO() dump([1, 2], stream) dump(foo, stream) stream.seek(0) load(stream){0 1, 1 2} load(stream)fooThis feature however is not supported in PHP.  PHP will only unserializethe first object.Array SerializationStarting with 1.2 you can provide an array hook to the unserializationfunctions that are invoked with a list of pairs to return a real arrayobject.  By default dict is used as array object which however meansthat the information about the order is lost for associative arrays.For example you can pass the ordered dictionary to the unserilizationfunctions from collections import OrderedDict loads(a2{s3fooi1s3bari2},...       array_hookOrderedDict)collections.OrderedDict([(foo, 1), (bar, 2)])Object SerializationPHP supports serialization of objects.  Starting with 1.2 of phpserializeit is possible to both serialize and unserialize objects.  Because classnames in PHP and Python usually do not map, there is a separateobject_hook parameter that is responsible for creating these classes.For a simple test example the phpserialize.phpobject class can be used data  O7WP_User1{s8usernames5admin} user  loads(data, object_hookphpobject) user.usernameadmin user.__name__WP_UserAn object hook is a function that takes the name of the class and a dictwith the instance data as arguments.  The instance data keys are in PHPformat which usually is not what you want.  To convert it into Pythonidentifiers you can use the convert_member_dict function.  For moreinformation about that, have a look at the next section.  Here anexample for a simple object hook class User(object)...     def __init__(self, username)...         self.username  username... def object_hook(name, d)...     cls  {WP_User User}[name]...     return cls(d)... user  loads(data, object_hookobject_hook) user.usernameadminTo serialize objects you can use the object_hook of the dump functionsand return instances of phpobject def object_hook(obj)...     if isinstance(obj, User)...         return phpobject(WP_User, {username obj.username})...     raise LookupError(unknown object)... dumps(user, object_hookobject_hook)O7WP_User1{s8usernames5admin}PHPs Object SystemThe PHP object system is derived from compiled languages such as Javaand C.  Attributes can be protected from external access by settingthem to protected or private.  This does not only serve the purposeto encapsulate internals but also to avoid name clashes.In PHP each class in the inheritance chain can have a private variablewith the same name, without causing clashes.  (This is similar to thePython __var name mangling system).This PHP class    class WP_UserBase {        protected username        public function __construct(username) {            this-username  username        }    }    class WP_User extends WP_UserBase {        private password        public flag        public function __construct(username, password) {            parent__construct(username)            this-password  password            this-flag  0        }    }Is serialized with a member data dict that looks like this data  {...       username          the username,...      WP_User password    the password,...     flag                 the flag... }Because this access system does not exist in Python, theconvert_member_dict can convert this dict d  convert_member_dict(data) d[username]the username d[password]the passwordThe phpobject class does this conversion on the fly.  What isserialized is the special __php_vars__ dict of the class user  phpobject(WP_User, data) user.usernamethe username user.username  admin user.__php_vars__[  username]adminAs you can see, reassigning attributes on a php object will tryto change a private or protected attribute with the same name.Setting an unknown one will create a new public attribute user.is_admin  True user.__php_vars__[is_admin]TrueTo convert the phpobject into a dict, you can use the _asdictmethod d  user._asdict() d[username]adminPython 3 NotesBecause the unicode support in Python 3 no longer transparentlyhandles bytes and unicode objects we had to change the way thedecoding works.  On Python 3 you most likely want to alwaysdecode strings.  Because this would totally fail on binary dataphpserialize uses the surrogateescape method to not fail oninvalid data.  See the documentation in Python 3 for moreinformation.Changelog1.3    -   added support for Python 31.2    -   added support for object serialization    -   added support for array hooks1.1    -   added dict_to_list and dict_to_tuple    -   added support for unicode    -   allowed chaining of objects like pickle does</longdescription>
</pkgmetadata>