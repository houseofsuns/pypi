<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Responses.. image httpsimg.shields.iopypivresponses.svg    target httpspypi.python.orgpypiresponses.. image httpsimg.shields.iopypipyversionsresponses.svg    target httpspypi.orgprojectresponses.. image httpsimg.shields.iopypidmresponses   target httpspypi.python.orgpypiresponses.. image httpscodecov.ioghgetsentryresponsesbranchmastergraphbadge.svg    target httpscodecov.ioghgetsentryresponsesA utility library for mocking out the requests Python library...  note    Responses requires Python 3.7 or newer, and requests  2.22.0Table of Contents-----------------.. contentsInstalling----------pip install responsesDeprecations and Migration Path-------------------------------Here you will find a list of deprecated functionality and a migration path for each.Please ensure to update your code according to the guidance... list-table Deprecation and Migration   widths 50 25 50   header-rows 1    - Deprecated Functionality     - Deprecated in Version     - Migration Path    - responses.json_params_matcher     - 0.14.0     - responses.matchers.json_params_matcher    - responses.urlencoded_params_matcher     - 0.14.0     - responses.matchers.urlencoded_params_matcher    - stream argument in Response and CallbackResponse     - 0.15.0     - Use stream argument in request directly.    - match_querystring argument in Response and CallbackResponse.     - 0.17.0     - Use responses.matchers.query_param_matcher or responses.matchers.query_string_matcher    - responses.assert_all_requests_are_fired, responses.passthru_prefixes, responses.target     - 0.20.0     - Use responses.mock.assert_all_requests_are_fired,       responses.mock.passthru_prefixes, responses.mock.target instead.BETA Features-------------Below you can find a list of BETA features. Although we will try to keep the API backwards compatiblewith released version, we reserve the right to change these APIs before they are considered stable. Please share your feedback viaGitHub Issues httpsgithub.comgetsentryresponsesissues_.Record Responses to filesYou can perform real requests to the server and responses will automatically record the output to thefile. Recorded data is stored in toml httpstoml.io_ format.Apply responses._recorder.record(file_pathout.toml) decorator to any function where you performrequests to record responses to out.toml file.Following code.. code-block python    import requests    from responses import _recorder    def another()        rsp  requests.get(httpshttpstat.us500)        rsp  requests.get(httpshttpstat.us202)    _recorder.record(file_pathout.toml)    def test_recorder()        rsp  requests.get(httpshttpstat.us404)        rsp  requests.get(httpshttpbin.orgstatuswrong)        another()will produce next output.. code-block toml    [[responses]]    [responses.response]    method  GET    url  httpshttpstat.us404    body  404 Not Found    status  404    content_type  textplain    auto_calculate_content_length  false    [[responses]]    [responses.response]    method  GET    url  httpshttpbin.orgstatuswrong    body  Invalid status code    status  400    content_type  textplain    auto_calculate_content_length  false    [[responses]]    [responses.response]    method  GET    url  httpshttpstat.us500    body  500 Internal Server Error    status  500    content_type  textplain    auto_calculate_content_length  false    [[responses]]    [responses.response]    method  GET    url  httpshttpstat.us202    body  202 Accepted    status  202    content_type  textplain    auto_calculate_content_length  falseReplay responses (populate registry) from filesYou can populate your active registry from a toml file with recorded responses.(See Record Responses to files_ to understand how to obtain a file).To do that you need to execute responses._add_from_file(file_pathout.toml) withinan activated decorator or a context manager.The following code example registers a patch response, then all responses present inout.toml file and a post response at the end... code-block python    import responses    responses.activate    def run()        responses.patch(httphttpbin.org)        responses._add_from_file(file_pathout.toml)        responses.post(httphttpbin.orgform)    run()Basics------The core of responses comes from registering mock responses and covering test functionwith responses.activate decorator. responses provides similar interface as requests.Main Interface responses.add(Response or Response args) - allows either to register Response object or directly  provide arguments of Response object. See Response Parameters_.. code-block python    import responses    import requests    responses.activate    def test_simple()         Register via Response object        rsp1  responses.Response(            methodPUT,            urlhttpexample.com,        )        responses.add(rsp1)         register via direct arguments        responses.add(            responses.GET,            httptwitter.comapi1foobar,            json{error not found},            status404,        )        resp  requests.get(httptwitter.comapi1foobar)        resp2  requests.put(httpexample.com)        assert resp.json()  {error not found}        assert resp.status_code  404        assert resp2.status_code  200        assert resp2.request.method  PUTIf you attempt to fetch a url which doesnt hit a match, responses will raisea ConnectionError.. code-block python    import responses    import requests    from requests.exceptions import ConnectionError    responses.activate    def test_simple()        with pytest.raises(ConnectionError)            requests.get(httptwitter.comapi1foobar)ShortcutsShortcuts provide a shorten version of responses.add() where method argument is prefilled responses.delete(Response args) - register DELETE response responses.get(Response args) - register GET response responses.head(Response args) - register HEAD response responses.options(Response args) - register OPTIONS response responses.patch(Response args) - register PATCH response responses.post(Response args) - register POST response responses.put(Response args) - register PUT response.. code-block python    import responses    import requests    responses.activate    def test_simple()        responses.get(            httptwitter.comapi1foobar,            json{type get},        )        responses.post(            httptwitter.comapi1foobar,            json{type post},        )        responses.patch(            httptwitter.comapi1foobar,            json{type patch},        )        resp_get  requests.get(httptwitter.comapi1foobar)        resp_post  requests.post(httptwitter.comapi1foobar)        resp_patch  requests.patch(httptwitter.comapi1foobar)        assert resp_get.json()  {type get}        assert resp_post.json()  {type post}        assert resp_patch.json()  {type patch}Responses as a context managerInstead of wrapping the whole function with decorator you can use a context manager... code-block python    import responses    import requests    def test_my_api()        with responses.RequestsMock() as rsps            rsps.add(                responses.GET,                httptwitter.comapi1foobar,                body{},                status200,                content_typeapplicationjson,            )            resp  requests.get(httptwitter.comapi1foobar)            assert resp.status_code  200         outside the context manager requests will hit the remote server        resp  requests.get(httptwitter.comapi1foobar)        resp.status_code  404Response Parameters-------------------The following attributes can be passed to a Response mockmethod (str)    The HTTP method (GET, POST, etc).url (str or compiled regular expression)    The full resource URL.match_querystring (bool)    DEPRECATED Use responses.matchers.query_param_matcher or    responses.matchers.query_string_matcher    Include the query string when matching requests.    Enabled by default if the response URL contains a query string,    disabled if it doesnt or the URL is a regular expression.body (str or BufferedReader or Exception)    The response body. Read more Exception as Response body_json    A Python object representing the JSON response body. Automatically configures    the appropriate Content-Type.status (int)    The HTTP status code.content_type (content_type)    Defaults to textplain.headers (dict)    Response headers.stream (bool)    DEPRECATED use stream argument in request directlyauto_calculate_content_length (bool)    Disabled by default. Automatically calculates the length of a supplied string or JSON body.match (tuple)    An iterable (tuple is recommended) of callbacks to match requests    based on request attributes.    Current module provides multiple matchers that you can use to match     body contents in JSON format     body contents in URL encoded data format     request query parameters     request query string (similar to query parameters but takes string as input)     kwargs provided to request e.g. stream, verify     multipartform-data content and headers in request     request headers     request fragment identifier    Alternatively user can create custom matcher.    Read more Matching Requests_Exception as Response body--------------------------You can pass an Exception as the body to trigger an error on the request.. code-block python    import responses    import requests    responses.activate    def test_simple()        responses.get(httptwitter.comapi1foobar, bodyException(...))        with pytest.raises(Exception)            requests.get(httptwitter.comapi1foobar)Matching Requests-----------------Matching Request Body ContentsWhen adding responses for endpoints that are sent request data you can addmatchers to ensure your code is sending the right parameters and providedifferent responses based on the request body contents. responses providesmatchers for JSON and URL-encoded request bodies.URL-encoded data.. code-block python    import responses    import requests    from responses import matchers    responses.activate    def test_calc_api()        responses.post(            urlhttpcalc.comsum,            body4,            match[matchers.urlencoded_params_matcher({left 1, right 3})],        )        requests.post(httpcalc.comsum, data{left 1, right 3})JSON encoded dataMatching JSON encoded data can be done with matchers.json_params_matcher()... code-block python    import responses    import requests    from responses import matchers    responses.activate    def test_calc_api()        responses.post(            urlhttpexample.com,            bodyone,            match[                matchers.json_params_matcher({page {name first, type json}})            ],        )        resp  requests.request(            POST,            httpexample.com,            headers{Content-Type applicationjson},            json{page {name first, type json}},        )Query Parameters MatcherQuery Parameters as a DictionaryYou can use the matchers.query_param_matcher function to matchagainst the params request parameter. Just use the same dictionary as youwill use in params argument in request.Note, do not use query parameters as part of the URL. Avoid using match_querystringdeprecated argument... code-block python    import responses    import requests    from responses import matchers    responses.activate    def test_calc_api()        url  httpexample.comtest        params  {hello world, I am a big test}        responses.get(            urlurl,            bodytest,            match[matchers.query_param_matcher(params)],        )        resp  requests.get(url, paramsparams)        constructed_url  rhttpexample.comtestI+ama+big+testhelloworld        assert resp.url  constructed_url        assert resp.request.url  constructed_url        assert resp.request.params  paramsBy default, matcher will validate that all parameters match strictly.To validate that only parameters specified in the matcher are present in original requestuse strict_matchFalse.Query Parameters as a StringAs alternative, you can use query string value in matchers.query_string_matcher to matchquery parameters in your request.. code-block python    import requests    import responses    from responses import matchers    responses.activate    def my_func()        responses.get(            httpshttpbin.orgget,            match[matchers.query_string_matcher(didiprotest1)],        )        resp  requests.get(httpshttpbin.orgget, params{test 1, didi pro})    my_func()Request Keyword Arguments MatcherTo validate request arguments use the matchers.request_kwargs_matcher function to matchagainst the request kwargs.Note, only arguments provided to matchers.request_kwargs_matcher will be validated... code-block python    import responses    import requests    from responses import matchers    with responses.RequestsMock(assert_all_requests_are_firedFalse) as rsps        req_kwargs  {            stream True,            verify False,        }        rsps.add(            GET,            http111.com,            match[matchers.request_kwargs_matcher(req_kwargs)],        )        requests.get(http111.com, streamTrue)           Arguments dont match {stream True, verify True} doesnt match {stream True, verify False}Request multipartform-data Data ValidationTo validate request body and headers for multipartform-data data you can usematchers.multipart_matcher. The data, and files parameters provided will be comparedto the request.. code-block python    import requests    import responses    from responses.matchers import multipart_matcher    responses.activate    def my_func()        req_data  {some other, data fields}        req_files  {file_name bOld World}        responses.post(            urlhttphttpbin.orgpost,            match[multipart_matcher(req_files, datareq_data)],        )        resp  requests.post(httphttpbin.orgpost, files{file_name bNew World})    my_func()      raises ConnectionError multipartform-data doesnt match. Request body differs.Request Fragment Identifier ValidationTo validate request URL fragment identifier you can use matchers.fragment_identifier_matcher.The matcher takes fragment string (everything after  sign) as input for comparison.. code-block python    import requests    import responses    from responses.matchers import fragment_identifier_matcher    responses.activate    def run()        url  httpexample.comabxyzedqwetest1foobar        responses.get(            url,            match[fragment_identifier_matcher(test1foobar)],            bodybtest,        )         two requests to check reversed order of fragment identifier        resp  requests.get(httpexample.comabxyzedqwetest1foobar)        resp  requests.get(httpexample.comzedqweabxyfoobartest1)    run()Request Headers ValidationWhen adding responses you can specify matchers to ensure that your code issending the right headers and provide different responses based on the requestheaders... code-block python    import responses    import requests    from responses import matchers    responses.activate    def test_content_type()        responses.get(            urlhttpexample.com,            bodyhello world,            match[matchers.header_matcher({Accept textplain})],        )        responses.get(            urlhttpexample.com,            json{content hello world},            match[matchers.header_matcher({Accept applicationjson})],        )         request in reverse order to how they were added        resp  requests.get(httpexample.com, headers{Accept applicationjson})        assert resp.json()  {content hello world}        resp  requests.get(httpexample.com, headers{Accept textplain})        assert resp.text  hello worldBecause requests will send several standard headers in addition to what wasspecified by your code, request headers that are additional to the onespassed to the matcher are ignored by default. You can change this behaviour bypassing strict_matchTrue to the matcher to ensure that only the headersthat youre expecting are sent and no others. Note that you will probably haveto use a PreparedRequest in your code to ensure that requests doesntinclude any additional headers... code-block python    import responses    import requests    from responses import matchers    responses.activate    def test_content_type()        responses.get(            urlhttpexample.com,            bodyhello world,            match[matchers.header_matcher({Accept textplain}, strict_matchTrue)],        )         this will fail because requests adds its own headers        with pytest.raises(ConnectionError)            requests.get(httpexample.com, headers{Accept textplain})         a prepared request where you overwrite the headers before sending will work        session  requests.Session()        prepped  session.prepare_request(            requests.Request(                methodGET,                urlhttpexample.com,            )        )        prepped.headers  {Accept textplain}        resp  session.send(prepped)        assert resp.text  hello worldCreating Custom MatcherIf your application requires other encodings or different data validation you can buildyour own matcher that returns Tuple[matches bool, reason str].Where boolean represents True or False if the request parameters match andthe string is a reason in case of match failure. Your matcher canexpect a PreparedRequest parameter to be provided by responses.Note, PreparedRequest is customized and has additional attributes params and req_kwargs.Response Registry---------------------------Default RegistryBy default, responses will search all registered Response objects andreturn a match. If only one Response is registered, the registry is kept unchanged.However, if multiple matches are found for the same request, then first match is returned andremoved from registry.Ordered RegistryIn some scenarios it is important to preserve the order of the requests and responses.You can use registries.OrderedRegistry to force all Response objects to be dependenton the insertion order and invocation index.In following example we add multiple Response objects that target the same URL. However,you can see, that status code will depend on the invocation order... code-block python    import requests    import responses    from responses.registries import OrderedRegistry    responses.activate(registryOrderedRegistry)    def test_invocation_index()        responses.get(            httptwitter.comapi1foobar,            json{msg not found},            status404,        )        responses.get(            httptwitter.comapi1foobar,            json{msg OK},            status200,        )        responses.get(            httptwitter.comapi1foobar,            json{msg OK},            status200,        )        responses.get(            httptwitter.comapi1foobar,            json{msg not found},            status404,        )        resp  requests.get(httptwitter.comapi1foobar)        assert resp.status_code  404        resp  requests.get(httptwitter.comapi1foobar)        assert resp.status_code  200        resp  requests.get(httptwitter.comapi1foobar)        assert resp.status_code  200        resp  requests.get(httptwitter.comapi1foobar)        assert resp.status_code  404Custom RegistryBuilt-in registries are suitable for most of use cases, but to handle special conditions, you canimplement custom registry which must follow interface of registries.FirstMatchRegistry.Redefining the find method will allow you to create custom search logic and returnappropriate ResponseExample that shows how to set custom registry.. code-block python    import responses    from responses import registries    class CustomRegistry(registries.FirstMatchRegistry)        pass    print(Before tests, responses.mock.get_registry())     Before tests responses.registries.FirstMatchRegistry object      using function decorator    responses.activate(registryCustomRegistry)    def run()        print(Within test, responses.mock.get_registry())         Within test __main__.CustomRegistry object     run()    print(After test, responses.mock.get_registry())     After test responses.registries.FirstMatchRegistry object      using context manager    with responses.RequestsMock(registryCustomRegistry) as rsps        print(In context manager, rsps.get_registry())         In context manager __main__.CustomRegistry object     print(After exit from context manager, responses.mock.get_registry())        After exit from context manager responses.registries.FirstMatchRegistry object    Dynamic Responses-----------------You can utilize callbacks to provide dynamic responses. The callback must returna tuple of (status, headers, body)... code-block python    import json    import responses    import requests    responses.activate    def test_calc_api()        def request_callback(request)            payload  json.loads(request.body)            resp_body  {value sum(payload[numbers])}            headers  {request-id 728d329e-0e86-11e4-a748-0c84dc037c13}            return (200, headers, json.dumps(resp_body))        responses.add_callback(            responses.POST,            httpcalc.comsum,            callbackrequest_callback,            content_typeapplicationjson,        )        resp  requests.post(            httpcalc.comsum,            json.dumps({numbers [1, 2, 3]}),            headers{content-type applicationjson},        )        assert resp.json()  {value 6}        assert len(responses.calls)  1        assert responses.calls[0].request.url  httpcalc.comsum        assert responses.calls[0].response.text  {value 6}        assert (            responses.calls[0].response.headers[request-id]             728d329e-0e86-11e4-a748-0c84dc037c13        )You can also pass a compiled regex to add_callback to match multiple urls.. code-block python    import re, json    from functools import reduce    import responses    import requests    operators  {        sum lambda x, y x + y,        prod lambda x, y x  y,        pow lambda x, y xy,    }    responses.activate    def test_regex_url()        def request_callback(request)            payload  json.loads(request.body)            operator_name  request.path_url[1]            operator  operators[operator_name]            resp_body  {value reduce(operator, payload[numbers])}            headers  {request-id 728d329e-0e86-11e4-a748-0c84dc037c13}            return (200, headers, json.dumps(resp_body))        responses.add_callback(            responses.POST,            re.compile(httpcalc.com(sumprodpowunsupported)),            callbackrequest_callback,            content_typeapplicationjson,        )        resp  requests.post(            httpcalc.comprod,            json.dumps({numbers [2, 3, 4]}),            headers{content-type applicationjson},        )        assert resp.json()  {value 24}    test_regex_url()If you want to pass extra keyword arguments to the callback function, for example when reusinga callback function to give a slightly different result, you can use functools.partial.. code-block python    from functools import partial    def request_callback(request, idNone)        payload  json.loads(request.body)        resp_body  {value sum(payload[numbers])}        headers  {request-id id}        return (200, headers, json.dumps(resp_body))    responses.add_callback(        responses.POST,        httpcalc.comsum,        callbackpartial(request_callback, id728d329e-0e86-11e4-a748-0c84dc037c13),        content_typeapplicationjson,    )Integration with unit test frameworks-------------------------------------Responses as a pytest fixture.. code-block python    pytest.fixture    def mocked_responses()        with responses.RequestsMock() as rsps            yield rsps    def test_api(mocked_responses)        mocked_responses.get(            httptwitter.comapi1foobar,            body{},            status200,            content_typeapplicationjson,        )        resp  requests.get(httptwitter.comapi1foobar)        assert resp.status_code  200Add default responses for each testWhen run with unittest tests, this can be used to set up somegeneric class-level responses, that may be complemented by each test.Similar interface could be applied in pytest framework... code-block python    class TestMyApi(unittest.TestCase)        def setUp(self)            responses.get(httpsexample.com, bodywithin setup)             here go other self.responses.add(...)        responses.activate        def test_my_func(self)            responses.get(                httpshttpbin.orgget,                match[matchers.query_param_matcher({test 1, didi pro})],                bodywithin test,            )            resp  requests.get(httpsexample.com)            resp2  requests.get(                httpshttpbin.orgget, params{test 1, didi pro}            )            print(resp.text)              within setup            print(resp2.text)              within testRequestMock methods start, stop, resetresponses has start, stop, reset methods very analogous tounittest.mock.patch httpsdocs.python.org3libraryunittest.mock.htmlpatch-methods-start-and-stop_.These make it simpler to do requests mocking in setup methods or whereyou want to do multiple patches without nesting decorators or with statements... code-block python    class TestUnitTestPatchSetup        def setup(self)            Creates RequestsMock instance and starts it.            self.r_mock  responses.RequestsMock(assert_all_requests_are_firedTrue)            self.r_mock.start()             optionally some default responses could be registered            self.r_mock.get(httpsexample.com, status505)            self.r_mock.put(httpsexample.com, status506)        def teardown(self)            Stops and resets RequestsMock instance.            If assert_all_requests_are_fired is set to True, will raise an error            if some requests were not processed.                        self.r_mock.stop()            self.r_mock.reset()        def test_function(self)            resp  requests.get(httpsexample.com)            assert resp.status_code  505            resp  requests.put(httpsexample.com)            assert resp.status_code  506Assertions on declared responses--------------------------------When used as a context manager, Responses will, by default, raise an assertionerror if a url was registered but not accessed. This can be disabled by passingthe assert_all_requests_are_fired value.. code-block python    import responses    import requests    def test_my_api()        with responses.RequestsMock(assert_all_requests_are_firedFalse) as rsps            rsps.add(                responses.GET,                httptwitter.comapi1foobar,                body{},                status200,                content_typeapplicationjson,            )Assert Request Call Count-------------------------Assert based on Response objectEach Response object has call_count attribute that could be inspectedto check how many times each request was matched... code-block python    responses.activate    def test_call_count_with_matcher()        rsp  responses.get(            httpwww.example.com,            match(matchers.query_param_matcher({}),),        )        rsp2  responses.get(            httpwww.example.com,            match(matchers.query_param_matcher({hello world}),),            status777,        )        requests.get(httpwww.example.com)        resp1  requests.get(httpwww.example.com)        requests.get(httpwww.example.comhelloworld)        resp2  requests.get(httpwww.example.comhelloworld)        assert resp1.status_code  200        assert resp2.status_code  777        assert rsp.call_count  2        assert rsp2.call_count  2Assert based on the exact URLAssert that the request was called exactly n times... code-block python    import responses    import requests    responses.activate    def test_assert_call_count()        responses.get(httpexample.com)        requests.get(httpexample.com)        assert responses.assert_call_count(httpexample.com, 1) is True        requests.get(httpexample.com)        with pytest.raises(AssertionError) as excinfo            responses.assert_call_count(httpexample.com, 1)        assert (            Expected URL httpexample.com to be called 1 times. Called 2 times.            in str(excinfo.value)        )    responses.activate    def test_assert_call_count_always_match_qs()        responses.get(httpwww.example.com)        requests.get(httpwww.example.com)        requests.get(httpwww.example.comhelloworld)         One call on each url, querystring is matched by default        responses.assert_call_count(httpwww.example.com, 1) is True        responses.assert_call_count(httpwww.example.comhelloworld, 1) is TrueMultiple Responses------------------You can also add multiple responses for the same url.. code-block python    import responses    import requests    responses.activate    def test_my_api()        responses.get(httptwitter.comapi1foobar, status500)        responses.get(            httptwitter.comapi1foobar,            body{},            status200,            content_typeapplicationjson,        )        resp  requests.get(httptwitter.comapi1foobar)        assert resp.status_code  500        resp  requests.get(httptwitter.comapi1foobar)        assert resp.status_code  200URL Redirection---------------In the following example you can see how to create a redirection chain and add custom exception that will be raisedin the execution chain and contain the history of redirects...  code-block    A - 301 redirect - B    B - 301 redirect - C    C - connection issue.. code-block python    import pytest    import requests    import responses    responses.activate    def test_redirect()         create multiple Response objects where first two contain redirect headers        rsp1  responses.Response(            responses.GET,            httpexample.com1,            status301,            headers{Location httpexample.com2},        )        rsp2  responses.Response(            responses.GET,            httpexample.com2,            status301,            headers{Location httpexample.com3},        )        rsp3  responses.Response(responses.GET, httpexample.com3, status200)         register above generated Responses in response module        responses.add(rsp1)        responses.add(rsp2)        responses.add(rsp3)         do the first request in order to generate genuine requests response         this object will contain genuine attributes of the response, like history        rsp  requests.get(httpexample.com1)        responses.calls.reset()         customize exception with response attribute        my_error  requests.ConnectionError(custom error)        my_error.response  rsp         update body of the 3rd response with Exception, this will be raised during execution        rsp3.body  my_error        with pytest.raises(requests.ConnectionError) as exc_info            requests.get(httpexample.com1)        assert exc_info.value.args[0]  custom error        assert rsp1.url in exc_info.value.response.history[0].url        assert rsp2.url in exc_info.value.response.history[1].urlValidate Retry mechanism----------------------------If you are using the Retry features of urllib3 and want to cover scenarios that test your retry limits, you can test those scenarios with responses as well. The best approach will be to use an Ordered Registry_.. code-block python    import requests    import responses    from responses import registries    responses.activate(registryregistries.OrderedRegistry)    def test_max_retries()        url  httpsexample.com        rsp1  responses.get(url, bodyError, status500)        rsp2  responses.get(url, bodyError, status500)        rsp3  responses.get(url, bodyError, status500)        rsp4  responses.get(url, bodyOK, status200)        session  requests.Session()        adapter  requests.adapters.HTTPAdapter(            max_retriesRetry(                total4,                backoff_factor0.1,                status_forcelist[500],                method_whitelist[GET, POST, PATCH],            )        )        session.mount(https, adapter)        resp  session.get(url)        assert resp.status_code  200        assert rsp1.call_count  1        assert rsp2.call_count  1        assert rsp3.call_count  1        assert rsp4.call_count  1Using a callback to modify the response---------------------------------------If you use customized processing in requests via subclassingmixins, or if youhave library tools that interact with requests at a low level, you may needto add extended processing to the mocked Response object to fully simulate theenvironment for your tests.  A response_callback can be used, which will bewrapped by the library before being returned to the caller.  The callbackaccepts a response as its single argument, and is expected to return asingle response object... code-block python    import responses    import requests    def response_callback(resp)        resp.callback_processed  True        return resp    with responses.RequestsMock(response_callbackresponse_callback) as m        m.add(responses.GET, httpexample.com, bodybtest)        resp  requests.get(httpexample.com)        assert resp.text  test        assert hasattr(resp, callback_processed)        assert resp.callback_processed is TruePassing through real requests-----------------------------In some cases you may wish to allow for certain requests to pass through responsesand hit a real server. This can be done with the add_passthru methods.. code-block python    import responses    responses.activate    def test_my_api()        responses.add_passthru(httpspercy.io)This will allow any requests matching that prefix, that is otherwise notregistered as a mock response, to passthru using the standard behavior.Pass through endpoints can be configured with regex patterns if youneed to allow an entire domain or path subtree to send requests.. code-block python    responses.add_passthru(re.compile(httpspercy.iow+))Lastly, you can use the passthrough argument of the Response objectto force a response to behave as a pass through... code-block python     Enable passthrough for a single response    response  Response(        responses.GET,        httpexample.com,        bodynot used,        passthroughTrue,    )    responses.add(response)     Use PassthroughResponse    response  PassthroughResponse(responses.GET, httpexample.com)    responses.add(response)ViewingModifying registered responses--------------------------------------Registered responses are available as a public method of the RequestMockinstance. It is sometimes useful for debugging purposes to view the stack ofregistered responses which can be accessed via responses.registered().The replace function allows a previously registered response to bechanged. The method signature is identical to add. response s areidentified using method and url. Only the first matched response isreplaced... code-block python    import responses    import requests    responses.activate    def test_replace()        responses.get(httpexample.org, json{data 1})        responses.replace(responses.GET, httpexample.org, json{data 2})        resp  requests.get(httpexample.org)        assert resp.json()  {data 2}The upsert function allows a previously registered response to bechanged like replace. If the response is registered, the upsert functionwill registered it like add.remove takes a method and url argument and will remove allmatched responses from the registered list.Finally, reset will reset all registered responses.Coroutines and Multithreading-----------------------------responses supports both Coroutines and Multithreading out of the box.Note, responses locks threading on RequestMock object allowing onlysingle thread to access it... code-block python    async def test_async_calls()        responses.activate        async def run()            responses.get(                httptwitter.comapi1foobar,                json{error not found},                status404,            )            resp  requests.get(httptwitter.comapi1foobar)            assert resp.json()  {error not found}            assert responses.calls[0].request.url  httptwitter.comapi1foobar        await run()Contributing------------Environment ConfigurationResponses uses several linting and autoformatting utilities, so its important that whensubmitting patches you use the appropriate toolchainClone the repository.. code-block shell    git clone httpsgithub.comgetsentryresponses.gitCreate an environment (e.g. with virtualenv).. code-block shell    virtualenv .env  source .envbinactivateConfigure development requirements.. code-block shell    make developTests and Code Quality ValidationThe easiest way to validate your code is to run tests via tox.Current tox configuration runs the same checks that are used inGitHub Actions CICD pipeline.Please execute the following command line from the project root to validateyour code against Unit tests in all Python versions that are supported by this project Type validation via mypy All pre-commit hooks.. code-block shell    toxAlternatively, you can always run a single test. See documentation below.Unit testsResponses uses Pytest httpsdocs.pytest.orgenlatest_ fortesting. You can run all tests by.. code-block shell    tox -e py37    tox -e py310OR manually activate required version of Python and run.. code-block shell    pytestAnd run a single test by.. code-block shell    pytest -k test_function_nameType ValidationTo verify type compliance, run mypy httpsgithub.compythonmypy_ linter.. code-block shell    tox -e mypyOR.. code-block shell    mypy --config-file.mypy.ini -p responsesCode Quality and StyleTo check code style and reformat it run.. code-block shell    tox -e precomOR.. code-block shell    pre-commit run --all-files</longdescription>
</pkgmetadata>