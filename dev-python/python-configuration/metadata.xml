<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> python-configuration A library to load configuration parameters hierarchically from multiple sources and formats[[version](httpsimg.shields.iopypivpython-configuration)](httpspypi.orgprojectpython-configuration)[python](httpsimg.shields.iopypipyversionspython-configuration)[wheel](httpsimg.shields.iopypiwheelpython-configuration)[license](httpsimg.shields.iopypilpython-configuration)[[build](httpsimg.shields.iotravistr11python-configuration)](httpstravis-ci.orgtr11python-configuration)[[codecov](httpscodecov.ioghtr11python-configurationbranchmastergraphbadge.svg)](httpscodecov.ioghtr11python-configuration)[[Documentation Status](httpsreadthedocs.orgprojectspython-configurationbadgeversionlatest)](httpspython-configuration.readthedocs.ioenlatestbadgelatest)This library is intended as a helper mechanism to load configuration fileshierarchically.  Current format types are Python files Dictionaries Environment variables Filesystem paths JSON files INI files dotenv type filesand optionally YAML files TOML files Azure Key Vault credentials AWS Secrets Manager credentials GCP Secret Manager credentials InstallingTo install the libraryshellpip install python-configurationTo include the optional TOML andor YAML loaders, install the optionaldependencies toml and  yaml. For example,shellpip install python-configuration[toml,yaml] Getting startedThis library converts the config types above into dictionaries with dotted-based keys. That is, given a config cfg from the structurepython{    a {        b value    }}we are able to refer to the parameter above as any of pythoncfg[a.b]cfg[a][b]cfg[a].bcfg.a.band extract specific data types such as dictionariespythoncfg[a].as_dict  {b value}This is particularly useful in order to isolate group parameters.For example, with the JSON configurationjson{  database.host something,  database.port 12345,  database.driver name,  app.debug true,  app.environment development,  app.secrets super secret,  logging {    service service,    token token,    tags tags  }}one can retrieve the dictionaries as pythoncfg.database.as_dict()cfg.app.as_dict()cfg.logging.as_dict()or simply as pythondict(cfg.database)dict(cfg.app)dict(cfg.logging) ConfigurationThere are two general types of objects in this library. The first one is the Configuration,which represents a single config source.  The second is a ConfigurationSet that allows formultiple Configuration objects to be specified. Single Config Python FilesTo load a configuration from a Python module, the config_from_python can be used.The first parameter must be a Python module and can be specified as an absolute path to the Python file or as an importable module.Optional parameters are the prefix and separator.  The following call pythonconfig_from_python(foo.bar, prefixCONFIG, separator__)will read every variable in the foo.bar module that starts with CONFIG__ and replaceevery occurrence of __ with a .. For example,python foo.barCONFIG__AA__BB_C  1CONFIG__AA__BB__D  2CONF__AA__BB__D  3would result in the configurationpython{    aa.bb_c 1,    aa.bb.d 2,}Note that the single underscore in BB_C is not replaced and the last line is notprefixed by CONFIG.  DictionariesDictionaries are loaded with config_from_dict and are converted internally to a flattened dict. python{    a {        b value    }}becomespython{    a.b value} Environment VariablesEnvironment variables starting with prefix can be read with config_from_envpythonconfig_from_env(prefix, separator_) Filesystem PathsFolders with files named as xxx.yyy.zzz can be loaded with the config_from_path function.  This format is useful to load mountedKubernetes [ConfigMaps](httpskubernetes.iodocstasksconfigure-pod-containerconfigure-pod-configmappopulate-a-volume-with-data-stored-in-a-configmap)or [Secrets](httpskubernetes.iodocstasksinject-data-applicationdistribute-credentials-securecreate-a-pod-that-has-access-to-the-secret-data-through-a-volume). JSON, INI, .env, YAML, TOMLJSON, INI, YAML, TOML files are loaded respectively withconfig_from_json,config_from_ini,config_from_dotenv,config_from_yaml, andconfig_from_toml.The parameter read_from_file controlswhether a string should be interpreted as a filename. CaveatsIn order for Configuration objects to act as dict and allow the syntaxdict(cfg), the keys() method is implemented as the typical dict keys.If keys is an element in the configuration cfg then the dict(cfg) call will fail.In that case, its necessary to use the cfg.as_dict() method to retrieve thedict representation for the Configuration object.The same applies to the methods values() and items().  Configuration SetsConfiguration sets are used to hierarchically load configurations and mergesettings. Sets can be loaded by constructing a ConfigurationSet object directly orusing the simplified config function.To construct a ConfigurationSet, pass in as many of the simple Configuration objects as neededpythoncfg  ConfigurationSet(    config_from_env(prefixPREFIX),    config_from_json(path, read_from_fileTrue),    config_from_dict(DICT),)The example above will read first from Environment variables prefixed with PREFIX, and fallback first to the JSON file at path, and finally use the dictionary DICT.The config function simplifies loading sets by assuming some defaults.The example above can also be obtained bypythoncfg  config(    (env, PREFIX),    (json, path, True),    (dict, DICT),)or, even simpler if path points to a file with a .json suffixpythoncfg  config(env, path, DICT, prefixPREFIX)The config function automatically detects the following extension .py for python modules dot-separated python identifiers as a python module (e.g. foo.bar) extension .json for JSON files extension .yaml for YAML files extension .toml for TOML files extension .ini for INI files extension .env for dotenv type files filesystem folders as Filesystem Paths the strings env or environment for Environment Variables Merging ValuesConfigurationSet instances are constructed by inspecting each configuration source, taking into account nested dictionaries, and merging at the most granular level.For example, the instance obtained from cfg  config(d1, d2) for the dictionaries belowpythond1  {sub {a 1, b 4}}d2  {sub {b 2, c 3}}is such that cfg[sub] equalspython{a 1, b 4, c 3}Note that the nested dictionaries of sub in each of d1 and d2 do not overwrite each other, but are merged into a singledictionary with keys from both d1 and d2, giving priority to the values of d1 over those from d2. CaveatsAs long as the data types are consistent across all the configurations that arepart of a ConfigurationSet, the behavior should be straightforward.  When differentconfiguration objects are specified with competing data types, the first configuration todefine the elements sets its datatype. For example, if in the example above element is interpreted as a dict from environment variables, but the JSON file specifies it as anything else besides a mapping, then the JSON value will bedropped automatically.  Other Features String InterpolationWhen setting the interpolate parameter in any Configuration instance, the library willperform a string interpolation step using the [str.format](httpsdocs.python.org3librarystring.htmlformatstrings)syntax.  In particular, this allows to format configuration values automaticallypythoncfg  config_from_dict({    percentage {val.3},    with_sign {val+f},    val 1.23456}, interpolateTrue)assert cfg.val  1.23456assert cfg.with_sign  +1.234560assert cfg.percentage  123.456 ExtrasThe config.contrib package contains extra implementations of the Configuration classused for special cases. Currently the following are implemented AzureKeyVaultConfiguration in config.contrib.azure, which takes Azure Key Vault  credentials into a Configuration-compatible instance. To install the needed dependencies  execute  shell  pip install python-configuration[azure]   AWSSecretsManagerConfiguration in config.contrib.aws, which takes AWS Secrets Manager  credentials into a Configuration-compatible instance. To install the needed dependencies  execute  shell  pip install python-configuration[aws]   GCPSecretManagerConfiguration in config.contrib.gcp, which takes GCP Secret Manager  credentials into a Configuration-compatible instance. To install the needed dependencies  execute  shell  pip install python-configuration[gcp]   DevelopingTo develop this library, download the source code and install a local version. Features Load multiple configuration types Hierarchical configuration Ability to override with environment variables Merge parameters from different configuration types ContributingIf youd like to contribute, please fork the repository and use a featurebranch. Pull requests are welcome. Links- Repository httpsgithub.comtr11python-configuration- Issue tracker httpsgithub.comtr11python-configurationissues- Documentation httpspython-configuration.readthedocs.io LicensingThe code in this project is licensed under MIT license.</longdescription>
</pkgmetadata>