<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>sqlitedict -- persistent dict, backed by SQLiteGithubActions_License_.. GithubActions image httpsgithub.comRaRe-Technologiessqlitedictactionsworkflowspython-package.ymlbadge.svg.. Downloads image httpsimg.shields.iopypidmsqlitedict.svg.. License image httpsimg.shields.iopypilsqlitedict.svg.. _GithubActions httpsgithub.comRaRe-Technologiessqlitedictactionsworkflowspython-package.yml.. _Downloads httpspypi.python.orgpypisqlitedict.. _License httpspypi.python.orgpypisqlitedictA lightweight wrapper around Pythons sqlite3 database with a simple, Pythonicdict-like interface and support for multi-thread accessUsageWrite-----.. code-block python     from sqlitedict import SqliteDict     db  SqliteDict(example.sqlite)         db[1]  {name first item}     db[2]  {name second item}     db[3]  {name yet another item}          Commit to save the objects.     db.commit()         db[4]  {name yet another item}      Oops, forgot to commit here, that object will never be saved.      Always remember to commit, or enable autocommit with SqliteDict(example.sqlite, autocommitTrue)      Autocommit is off by default for performance.         db.close()Read----.. code-block python     from sqlitedict import SqliteDict     db  SqliteDict(example.sqlite)         print(There are d items in the database  len(db))    There are 3 items in the database          Standard dict interface. items() values() keys() etc...     for key, item in db.items()    ...     print(ss  (key, item))    1{name first item}    2{name second item}    3{name yet another item}         db.close()Efficiency----------By default, sqlitedicts exception handling favors verbosity over efficiency.It extracts and outputs the outer exception stack to the error logs.If you favor efficiency, then initialize the DB with outer_stackFalse... code-block python     from sqlitedict import SqliteDict     db  SqliteDict(example.sqlite, outer_stackFalse)   True is the default     db[1]    {name first item}Context Manager---------------.. code-block python     from sqlitedict import SqliteDict          The database is automatically closed when leaving the with section.      Uncommitted objects are not saved on close. REMEMBER TO COMMIT         with SqliteDict(example.sqlite) as db    ...     print(There are d items in the database  len(db))    There are 3 items in the databaseTables------A database file can store multiple tables.A default table is used when no table name is specified.Note Writes are serialized, having multiple tables does not improve performance... code-block python     from sqlitedict import SqliteDict         products  SqliteDict(example.sqlite, tablenameproduct, autocommitTrue)     manufacturers  SqliteDict(example.sqlite, tablenamemanufacturer, autocommitTrue)         products[1]  {name first item,  manufacturer_id 1}     products[2]  {name second item, manufacturer_id 1}         manufacturers[1]  {manufacturer_name afactory, location US}     manufacturers[2]  {manufacturer_name anotherfactory, location UK}         tables  products.get_tablenames(example.sqlite)     print(tables)    [unnamed, product, manufacturer]         products.close()     manufacturers.close()In case youre wondering, the unnamed table comes from the previous examples,where we did not specify a table name.Serialization-------------Keys are strings. Values are any serializeable object.By default Pickle is used internally to (de)serialize the values.Its possible to use a custom (de)serializer, notably for JSON and for compression... code-block python      Use JSON instead of pickle     import json     with SqliteDict(example.sqlite, encodejson.dumps, decodejson.loads) as mydict    ...     pass          Apply zlib compression after pickling     import zlib, pickle, sqlite3         def my_encode(obj)    ...     return sqlite3.Binary(zlib.compress(pickle.dumps(obj, pickle.HIGHEST_PROTOCOL)))         def my_decode(obj)    ...     return pickle.loads(zlib.decompress(bytes(obj)))         with SqliteDict(example.sqlite, encodemy_encode, decodemy_decode) as mydict    ...     passIts also possible to use a custom (de)serializer for keys to allow non-string keys... code-block python      Use key encoding instead of default string keys only     from sqlitedict import encode_key, decode_key     with SqliteDict(example.sqlite, encode_keyencode_key, decode_keydecode_key) as mydict    ...     passMore----Functions are well documented, see docstrings directly in sqlitedict.py or call help(sqlitedict).Beware because of Python semantics, sqlitedict cannot know when a mutableSqliteDict-backed entry was modified in RAM. Youll need toexplicitly assign the mutated object back to SqliteDict.. code-block python     from sqlitedict import SqliteDict     db  SqliteDict(example.sqlite)     db[colors]  {red (255, 0, 0)}     db.commit()         colors  db[colors]     colors[blue]  (0, 0, 255)  sqlite DB not updated here     db[colors]  colors   now updated         db.commit()  remember to commit (or set autocommit)     db.close()Features Values can be any picklable objects (uses pickle with the highest protocol). Support for multiple tables (dicts) living in the same database file. Support for access from multiple threads to the same connection (needed by e.g. Pyro).  Vanilla sqlite3 gives you ProgrammingError SQLite objects created in a thread can  only be used in that same thread.  Concurrent requests are still serialized internally, so this multithreaded support  doesnt give you any performance benefits. It is a work-around for sqlite limitations in Python. Support for custom serialization or compression.. code-block python   use JSON instead of pickle   import json   mydict  SqliteDict(.my_db.sqlite, encodejson.dumps, decodejson.loads)   apply zlib compression after pickling   import zlib, pickle, sqlite3   def my_encode(obj)  ...     return sqlite3.Binary(zlib.compress(pickle.dumps(obj, pickle.HIGHEST_PROTOCOL)))   def my_decode(obj)  ...     return pickle.loads(zlib.decompress(bytes(obj)))   mydict  SqliteDict(.my_db.sqlite, encodemy_encode, decodemy_decode) sqlite is efficient and can work effectively with large databases (multi gigabytes), not limited by memory. sqlitedict is mostly a thin wrapper around sqlite. items() keys() values() are iterating one by one, the rows are loaded in a worker thread and queued in memory. len() is calling sqlite to count rows, that is scanning the whole table. For better performance, write objects in batch and commit() once.InstallationThe module has no dependencies beyond Python itself.The minimum supported Python version is 3.7, continuously tested on Python 3.7, 3.8, 3.9, and 3.10 on Travis httpstravis-ci.orgRaRe-Technologiessqlitedict_.Install or upgrade with    pip install -U sqlitedictor from the source tar.gz httppypi.python.orgpypisqlitedict_    python setup.py installContributionsTesting-------Install     pip install pytest coverage pytest-coverageTo perform all tests     mkdir -p testsdb     pytest tests     python -m doctest README.rstTo perform all tests with coverage     pytest tests --covsqlitedictComments, bug reports---------------------sqlitedict resides on github httpsgithub.comRaRe-Technologiessqlitedict_. You can fileissues or pull requests there.Licensesqlitedict is open source software released under the Apache 2.0 license httpopensource.orglicensesapache2.0.php_.Copyright (c) 2011-now Radim ehek httpradimrehurek.com_ and contributors.HousekeepingClean up the test database to keep each doctest run idempotent.. code-block python    import os    if __name__  __main__   ...     os.unlink(example.sqlite)</longdescription>
</pkgmetadata>