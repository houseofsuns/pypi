<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> PyTorch learning rate finder[](httpsgithub.comdavidtvspytorch-lr-finderworkflowsci-buildbadge.svgbranchmaster)[[codecov](httpscodecov.ioghdavidtvspytorch-lr-finderbranchmastergraphbadge.svg)](httpscodecov.ioghdavidtvspytorch-lr-finder)[[](httpsimg.shields.iopypivtorch-lr-finder)](httpspypi.orgprojecttorch-lr-finder)A PyTorch implementation of the learning rate range test detailed in [Cyclical Learning Rates for Training Neural Networks](httpsarxiv.orgabs1506.01186) by Leslie N. Smith and the tweaked version used by [fastai](httpsgithub.comfastaifastai).The learning rate range test is a test that provides valuable information about the optimal learning rate. During a pre-training run, the learning rate is increased linearly or exponentially between two boundaries. The low initial learning rate allows the network to start converging and as the learning rate is increased it will eventually be too large and the network will diverge.Typically, a good static learning rate can be found half-way on the descending loss curve. In the plot below that would be lr  0.002.For cyclical learning rates (also detailed in Leslie Smiths paper) where the learning rate is cycled between two boundaries (start_lr, end_lr), the author advises the point at which the loss starts descending and the point at which the loss stops descending or becomes ragged for start_lr and end_lr respectively.  In the plot below, start_lr  0.0002 and end_lr0.2.[Learning rate range test](imageslr_finder_cifar10.png) InstallationPython 3.5 and abovebashpip install torch-lr-finderInstall with the support of mixed precision training (see also [this section](Mixed-precision-training))bashpip install torch-lr-finder -v --global-optionapex Implementation details and usage Tweaked version from fastaiIncreases the learning rate in an exponential manner and computes the training loss for each learning rate. lr_finder.plot() plots the training loss versus logarithmic learning rate.pythonfrom torch_lr_finder import LRFindermodel  ...criterion  nn.CrossEntropyLoss()optimizer  optim.Adam(model.parameters(), lr1e-7, weight_decay1e-2)lr_finder  LRFinder(model, optimizer, criterion, devicecuda)lr_finder.range_test(trainloader, end_lr100, num_iter100)lr_finder.plot()  to inspect the loss-learning rate graphlr_finder.reset()  to reset the model and optimizer to their initial state Leslie Smiths approachIncreases the learning rate linearly and computes the evaluation loss for each learning rate. lr_finder.plot() plots the evaluation loss versus learning rate.This approach typically produces more precise curves because the evaluation loss is more susceptible to divergence but it takes significantly longer to perform the test, especially if the evaluation dataset is large.pythonfrom torch_lr_finder import LRFindermodel  ...criterion  nn.CrossEntropyLoss()optimizer  optim.Adam(model.parameters(), lr0.1, weight_decay1e-2)lr_finder  LRFinder(model, optimizer, criterion, devicecuda)lr_finder.range_test(trainloader, val_loaderval_loader, end_lr1, num_iter100, step_modelinear)lr_finder.plot(log_lrFalse)lr_finder.reset() Notes- Examples for CIFAR10 and MNIST can be found in the examples folder.- The optimizer passed to LRFinder should not have an LRScheduler attached to it.- LRFinder.range_test() will change the model weights and the optimizer parameters. Both can be restored to their initial state with LRFinder.reset().- The learning rate and loss history can be accessed through lr_finder.history. This will return a dictionary with lr and loss keys.- When using step_modelinear the learning rate range should be within the same order of magnitude.- LRFinder.range_test() expects a pair of input, label to be returned from the DataLoader objects passed to it. The input must be ready to be passed to the model and the label must be ready to be passed to the criterion without any further data processinghandlingconversion. If you find yourself needing a workaround you can make use of the classes TrainDataLoaderIter and ValDataLoaderIter to perform any data processinghandlingconversion inbetween the DataLoader and the trainingevaluation loop. You can find an example of how to use these classes in [exampleslrfinder_cifar10_dataloader_iter](exampleslrfinder_cifar10_dataloader_iter.ipynb). Additional support for training Gradient accumulationYou can set the accumulation_steps parameter in LRFinder.range_test() with a proper value to perform gradient accumulationpythonfrom torch.utils.data import DataLoaderfrom torch_lr_finder import LRFinderdesired_batch_size, real_batch_size  32, 4accumulation_steps  desired_batch_size  real_batch_sizedataset  ... Beware of the batch_size used by DataLoadertrainloader  DataLoader(dataset, batch_sizereal_batch_size, shuffleTrue)model  ...criterion  ...optimizer  ... (Optional) With this setting, amp.scale_loss() will be adopted automatically. model, optimizer  amp.initialize(model, optimizer, opt_levelO1)lr_finder  LRFinder(model, optimizer, criterion, devicecuda)lr_finder.range_test(trainloader, end_lr10, num_iter100, step_modeexp, accumulation_stepsaccumulation_steps)lr_finder.plot()lr_finder.reset() Mixed precision trainingCurrently, we use [apex](httpsgithub.comNVIDIAapex) as the dependency for mixed precision training.To enable mixed precision training, you just need to call amp.initialize() before running LRFinder. e.g.pythonfrom torch_lr_finder import LRFinderfrom apex import amp Add this line before running LRFindermodel, optimizer  amp.initialize(model, optimizer, opt_levelO1)lr_finder  LRFinder(model, optimizer, criterion, devicecuda)lr_finder.range_test(trainloader, end_lr10, num_iter100, step_modeexp)lr_finder.plot()lr_finder.reset()Note that the benefit of mixed precision training requires a nvidia GPU with tensor cores (see also [NVIDIAapex 297](httpsgithub.comNVIDIAapexissues297))Besides, you can try to set torch.backends.cudnn.benchmark  True to improve the training speed. (but it wont work for some cases, you should use it at your own risk) Contributing and pull requestsAll contributions are welcome but first, have a look at [CONTRIBUTING.md](CONTRIBUTING.md).</longdescription>
</pkgmetadata>