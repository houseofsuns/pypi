<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-discover-runner.. note    This runner has been added to Django 1.6 as the default test runner.    If you use Django 1.6 or above you dont need this app.An alternative Django TEST_RUNNER which uses the unittest2_ test discoveryfrom a base path specified in the settings, or any other module or packagespecified to the test management command -- including app tests.If you just run .manage.py test, itll discover and run all testsunderneath the current working directory. E.g. if you run.manage.py test full.dotted.path.to.test_module, itll run the tests inthat module (you can also pass multiple modules). If you give it a singledotted path to a package (like a Django app) like .manage.py test myappand that package does not itself directly contain any tests, itll dotest discovery in all submodules of that package... note    This code uses the default unittest2_ test discovery behavior, which    only searches for tests in files named test.py. To override this    see the TEST_DISCOVER_PATTERN setting or use the --pattern    option.Why----Djangos own test discovery is very much tied to the directory structureof Django apps, partly due to historic reasons (the unittest librarydidnt have its own discovery for a long time) and prevents Django appauthors from being good Python citizens. django-discover-runner uses theofficial test discovery feature of the new unittest2_ library which isincluded in Django.By default there is no way to put project specific tests in a separatefolder outside the Python package of the Django project, which is a greatway to organize your code, separating the tests and non-test code.django-discover-runner helps you clean up your project tests.There is also no way to specify fully dotted import paths to testmodules, functions, class or methods to the test management commandbut only Djangos odd standard appname.TestClassName.django-discover-runner allows you to specify any type of label to Djangostest management command.By default Djangos test runner will execute the tests of Djangos owncontrib apps, which doesnt make sense if you just want to run yourown apps or projects tests. django-discover-runner fixes this by allowingyou to specify which tests to run and organize your test code outside thereach of the Django test runner.More reasons can be found in Carl Meyers excellent talk aboutTesting and Django_ (slides_)... _Testing and Django httppyvideo.orgvideo699testing-and-django.. _slides httpcarljm.github.comdjango-testing-slidesInstallation------------Install it with your favorite installer, e.g.    pip install -U django-discover-runnerdjango-discover-runner requires at least Django 1.4 and also works on 1.5.x.Starting in Django 1.6 the discover runner is a built-in.Setup------ TEST_RUNNER (required) needs to point to the DiscoverRunner class  to enable it    TEST_RUNNER  discover_runner.DiscoverRunner- Add discover_runner to your INSTALLED_APPS setting to enable the  ability to override the discovery settings below when using the test  management command.- TEST_DISCOVER_TOP_LEVEL (optional) should be the directory containing  your top-level package(s) in other words, the directory that should be on  sys.path for your code to import. This is for example the directory  containing manage.py in the new Django 1.4 project layout.  The management command option is called --top-level.- TEST_DISCOVER_PATTERN (optional) is the pattern to use when discovering  tests and defaults to the unittest2_ standard test.py. The management  command option is called --pattern.Examples--------Django appTo test a reusable Django app its recommended to add a test_settings.pyfile to your app package to easily run the app tests with the testmanagement command. Simply set the TEST_RUNNER setting todiscover_runner.DiscoverRunner, configure the other settings necessaryto run your tests and call the test management command with the name ofthe app package, e.g.     django-admin.py test --settingsmyapp.test_settings myappDjango projectIf you want to test a project and want to store the projects tests outsidethe project main package (recommended), you can simply follow the appinstructions above, applying it to the project package, but set a fewadditional settings to tell the test runner to find the tests    from os import path    TEST_DISCOVER_TOP_LEVEL  path.dirname(path.dirname(__file__))This would find all the tests within a top-level tests package. Running thetests is as easy as calling    django-admin.py test --settingsmysite.test_settings testsAlternatively you can specify the --top-level-directory managementcommand option.Multiple Django versionsIn case you want to test your app on older Django versions as well asDjango  1.6 you can simply conditionally configure the test runner in yourtest settings, e.g.  import django  if django.VERSION[2]  (1, 6)    TEST_RUNNER  discover_runner.DiscoverRunnerChangelog---------1.0 06152013 GOOD NEWS This runner was added to Django 1.6 as the new default  This version backports that runner for Django 1.4.x and 1.5.x. Removed TEST_DISCOVER_ROOT setting in favor of unittest2s own way to  figure out the root. Dropped support for Django 1.3.x.0.4 04122013 Added ability to override the discover settings with a custom test management  command.0.3 01282013 Fixed setup.py to work on Python 3. This should make this app compatible  to Python 3.0.2.2 09042012 Stopped setting the top level variable in the case of using a module path  as the test label as it made the wrong assumption that the parent directory  is the top level.0.2.1 08202012 Fixed a rather esoteric bug with testing test case class methods  that was caused by a wrong import and the way Django wraps itself  around the unittest2 module (if availale) or unittest on Python  2.7.0.2 05262012 Added ability to use an optionally installed unittest2 library  for Django projects using Django  1.3 (which added unittest2 to the  django.utils.unittest package).0.1.1 05232012 Fixed a bug that prevented the project based feature to work correctly.0.1 05202012 Initial release with support for Django  1.3.Thanks------This test runner is a humble rip-off of Carl Meyers DiscoveryRunnerwhich he published as a gist_ a while ago. All praise should be directed athim. Thanks, CarlThis was also very much related to ticket 17365_ which eventually ledto the replacement of the default test runner in Django. Thanks again,Carl.. _unittest2 httppypi.python.orgpypiunittest2.. _gist httpsgist.github.com1450104.. _17365 httpscode.djangoproject.comticket17365</longdescription>
</pkgmetadata>