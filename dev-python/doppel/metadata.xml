<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>doppel======**doppel** copies files or directories to a destination (a file,directory, or archive). Think of it as the offspring of`install(1) &lt;http://linux.die.net/man/1/install&gt;`__ and`tar(1) &lt;http://linux.die.net/man/1/tar&gt;`__.Usage-----::   doppel [OPTION]... SOURCE... DESTTo copy files or directories, just list the sources as argumentsfollowed by the destination: ``doppel src1 src2 dst``. By default, ifonly one source is specified, it is copied *onto* the destination; ifmultiple sources are specified, they are copied *into* the destination.This default can be explicitly specified with ``-o/--onto`` or``-i/--into``, respectively.If the destination requires administrator access to write to, doppelwill try to detect this an automatically provide a ``sudo`` (orequivalent) prompt before copying.*Note:* Unlike *install(1)*, doppel copies relative symlinks as symlinksby default. To adjust this behavior, you can use the ``--symlink``option, described below.Options--------C, –directory=DIR~~~~~~~~~~~~~~~~~~Change to directory ``DIR`` before resolving paths of source files.-f, –format=FMT~~~~~~~~~~~~~~~Set the format of the output archive; one of: ``tar``, ``gzip``,``bzip2``, or ``zip``. If this option is specified, ``--into`` isimplied. If not set, source files will be copied normally, and noarchive will be created.-i, –into~~~~~~~~~Copy sources into directory ``DEST``.-m, –mode=MODE~~~~~~~~~~~~~~Set the file permissions to ``MODE`` (an octal) when copying. Note: thishas no effect when ``--format=zip``.-N, –full-name~~~~~~~~~~~~~~When copying, use the full name of each source file as specified on thecommand line instead of just the tail. This behavior is similar to`tar(1) &lt;http://linux.die.net/man/1/tar&gt;`__.-o, –onto~~~~~~~~~Copy a single source onto file or directory ``DEST``.-p, –parents~~~~~~~~~~~~Automatically create parent directories for ``DEST`` as needed.-P, –dest-prefix=DIR~~~~~~~~~~~~~~~~~~~~A prefix to add to the paths of destination files. This only applieswhen using ``--format``.-r, –recursive~~~~~~~~~~~~~~Recursively copy source directories to the destination.-S, –sudo=WHEN~~~~~~~~~~~~~~Before copying, elevate with ``sudo`` (or your platform’s equivalent).``WHEN`` is one of ``always``, ``auto`` (the default), or ``never``. If``auto``, doppel will attempt to determine automatically if ``sudo``would be needed before performing the copy.–symlink=WHEN~~~~~~~~~~~~~Copy symlinks as links. ``WHEN`` is one of ``always``, ``relative`` (thedefault), or ``never``. If ``relative``, only symlinks with relativetargets will be copied as symlinks; those with absolute targets will becopied as regular files.@file~~~~~Read options from ``file`` and use them when executing.–help~~~~~Display a help message and exit.–version~~~~~~~~Output version information and exit.License-------This project is licensed under the `BSD 3-clause license &lt;LICENSE&gt;`__.</longdescription>
</pkgmetadata>