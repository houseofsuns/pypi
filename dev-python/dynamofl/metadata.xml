<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># Run the samplesTry the samples to see the DynamoFL SDK in action.## Generate API key from UI## Setup the environment1. Create a venv and activate2. `cd` into the `client-py/samples` directory3. Run `cp .env.template .env` to create a `.env` file and set the `API_HOST` and `API_KEY` (generated from the UI)4. Run `pip install -r requirements.txt` to install the dependencies to the venv5. Run `python sample.py`# Development## Install requirements1. Create a venv and activate2. `cd` into the `client-py` directory3. Run `pip install -r requirements.txt` to install the dependencies to the venv## Tired of copy-pasting your latest changes into `site-packages` ?Follow the steps below to run the `samples` against your latest code1. Open `&lt;venv&gt;/bin/activate` 2. Paste the below code snippet to the end of file and set `CLIENT_PY_DIR` ```CLIENT_PY_DIR=&lt;absolute path to client-py repo&gt;SYSPATHS=$($VIRTUAL_ENV/bin/python -c &quot;import sys; print(':'.join(x for x in sys.path if x))&quot;)export PYTHONPATH=$SYSPATHS&quot;:&quot;$CLIENT_PY_DIR```3. Run `pip uninstall dynamofl` to delete the `dynamofl` package from `site-packages`&lt;br&gt;&gt; To test against a published `dynamofl` SDK, run `pip install dynamofl` before running the samples# Build and publish the packageNOTE: Building the package would delete the `dist` directory and `dynamofl.egg-info` file at the root of `client-py`1. Ensure the libraries listed in `client-py/requirements.txt` is installed in the venv2. Activate the venv3. Run `./build.sh`</longdescription>
</pkgmetadata>