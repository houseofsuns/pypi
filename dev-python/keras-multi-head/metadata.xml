<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Keras Multi-Head[[Version](httpsimg.shields.iopypivkeras-multi-head.svg)](httpspypi.orgprojectkeras-multi-head)[License](httpsimg.shields.iopypilkeras-multi-head.svg)A wrapper layer for stacking layers horizontally.[](httpsuser-images.githubusercontent.com85384245797517-867b8580-bcd8-11e8-9ec6-39d6508cf438.png) Installbashpip install keras-multi-head Usage Duplicate LayersThe layer will be duplicated if only a single layer is provided. The layer_num argument controls how many layers will be duplicated eventually.pythonfrom tensorflow import kerasfrom keras_multi_head import MultiHeadmodel  keras.models.Sequential()model.add(keras.layers.Embedding(input_dim100, output_dim20, nameEmbedding))model.add(MultiHead(keras.layers.LSTM(units32), layer_num5, nameMulti-LSTMs))model.add(keras.layers.Flatten(nameFlatten))model.add(keras.layers.Dense(units4, activationsoftmax, nameDense))model.build()model.summary() Use Multiple-LayersThe first argument could also be a list of layers with different configurations, however, they must have the same output shapes.pythonfrom tensorflow import kerasfrom keras_multi_head import MultiHeadmodel  keras.models.Sequential()model.add(keras.layers.Embedding(input_dim100, output_dim20, nameEmbedding))model.add(MultiHead([    keras.layers.Conv1D(filters32, kernel_size3, paddingsame),    keras.layers.Conv1D(filters32, kernel_size5, paddingsame),    keras.layers.Conv1D(filters32, kernel_size7, paddingsame),], nameMulti-CNNs))model.build()model.summary() Linear TransformationThe input data will be mapped to different values of the same shape for each layer when hidden_dim is given. Regularization[](httpsuser-images.githubusercontent.com85384245857922-8b4e4100-bd8d-11e8-905a-4eb07da31418.png)The regularization is used when you expect to extract different features from the parallel layers. You can customize the indices of weights in the layers, the intervals represent the parts of the weights and the factor of the regularization.For example, the bidirectional LSTM layer has 6 weights by default, and the first 3s belong to the forward layer. The 2nd weight (recurrent kernel) in the forward layer controls the computation of gates for recurrent connections. The kernel for computing cell states lays in units x 2 to units x 3 of the recurrent kernel. We can used the regularization for the kernelspythonfrom tensorflow import kerasfrom keras_multi_head import MultiHeadmodel  keras.models.Sequential()model.add(keras.layers.Embedding(input_dim5, output_dim3, nameEmbed))model.add(MultiHead(    layerkeras.layers.Bidirectional(keras.layers.LSTM(units16), nameLSTM),    layer_num5,    reg_index[1, 4],    reg_slice(slice(None, None), slice(32, 48)),    reg_factor0.1,    nameMulti-Head-Attention,))model.add(keras.layers.Flatten(nameFlatten))model.add(keras.layers.Dense(units2, activationsoftmax, nameDense))model.build() reg_index The indices of layer.get_weights(), a single integer or a list of integers. reg_slice slices or a tuple of slices or a list of the previous choices. If multiple indices are provided in reg_index and reg_slice is not a list, then reg_slice is assumed to be equal for all the indices. The whole array will be used if you leave this argument to None. reg_factor The factor of the regularization, a float or a list of floats. Multi-Head AttentionA more specific multi-head layer is provided (since the general one is harder to use). The layer uses scaled dot product attention layers as its sub-layers and only head_num is requiredpythonfrom tensorflow import kerasfrom keras_multi_head import MultiHeadAttentioninput_layer  keras.layers.Input(    shape(2, 3),    nameInput,)att_layer  MultiHeadAttention(    head_num3,    nameMulti-Head,)(input_layer)model  keras.models.Model(inputsinput_layer, outputsatt_layer)model.compile(    optimizeradam,    lossmse,    metrics{},)model.summary()The shapes of input and output tensors would be the same if only one layer is presented as input. The input layers will be considered as query, key and value when a list is givenpythonfrom tensorflow import kerasfrom keras_multi_head import MultiHeadAttentioninput_query  keras.layers.Input(    shape(2, 3),    nameInput-Q,)input_key  keras.layers.Input(    shape(4, 5),    nameInput-K,)input_value  keras.layers.Input(    shape(4, 6),    nameInput-V,)att_layer  MultiHeadAttention(    head_num3,    nameMulti-Head,)([input_query, input_key, input_value])model  keras.models.Model(inputs[input_query, input_key, input_value], outputsatt_layer)model.compile(    optimizeradam,    lossmse,    metrics{},)model.summary()</longdescription>
</pkgmetadata>