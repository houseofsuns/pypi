<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>## pyLookoutA simple Python program to check Linux system's resource utilization and service availability.  Currently it can:* Check CPU, RAM and Disk space* Send notifications via SendGrid and SimplepushPlanned functionality:* Send notifications via Telegram, WhatsApp and IRC.* Check container status.* Report new active SSH sessions.* Monitor logs to find suspicious activity.* Run continuously as a service in the background. ### InstallationTo install the app, clone the repository and install with pip:```bashgit clone https://github.com/Lab-Brat/pyLookout.gitcd pyLookoutpython -m pip install .```### UsageTo send notifications pyLookout reads API keys from the environment.  SendGrid requires to specify sending and destination email addresses, and also an API key:```SENDGRID_TOSENDGRID_FROMSENDGRID_API_KEY```  Simplepush requires only the API key and sends notifications to the app&quot;```SIMPLEPUSH```  To run the program, first install it with pip. It will create an executable in `/home/$USER/.local/bin/`, (which should be in $PATH), and launch it:```bashpylookout```It will gather server metrics and send a notificationa via preferred method if a certain total utilization percentageis reached (75% by default).Add it to crontab to run on a schedule.</longdescription>
</pkgmetadata>