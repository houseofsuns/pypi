<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Pluxp  a hrefhttpsgithub.comlocalstackpluxactionsworkflowsbuild.ymlimg altCI badge srchttpsgithub.comlocalstackpluxactionsworkflowsbuild.ymlbadge.svgimga  a hrefhttpspypi.orgprojectpluximg altPyPI Version srchttpsimg.shields.iopypivpluxcolorbluea  a hrefhttpsimg.shields.iopypilplux.svgimg altPyPI License srchttpsimg.shields.iopypilplux.svga  a hrefhttpsgithub.compsfblackimg altCode style black srchttpsimg.shields.iobadgecode20style-black-000000.svgapplux is the dynamic code loading framework used in [LocalStack](httpsgithub.comlocalstacklocalstack).Overview--------Plux builds a higher-level plugin mechanism around [Pythons entry point mechanism](httpspackaging.python.orgspecificationsentry-points).It provides tools to load plugins from entry points at run time, and to discover entry points from plugins at build time (so you dont have to declare entry points statically in your setup.py). Core concepts PluginSpec describes a Plugin. Each plugin has a namespace, a unique name in that namespace, and a PluginFactory (something that creates Plugin the spec is describing.  In the simplest case, that can just be the Plugins class). Plugin an object that exposes a should_load and load method.  Note that it does not function as a domain object (it does not hold the plugins lifecycle state, like initialized, loaded, etc..., or other metadata of the Plugin) PluginFinder finds plugins, either at build time (by scanning the modules using pkgutil and setuptools) or at run time (reading entrypoints of the distribution using [stevedore](httpsdocs.openstack.orgstevedorelatest)) PluginManager manages the run time lifecycle of a Plugin, which has three states   resolved the entrypoint pointing to the PluginSpec was imported and the PluginSpec instance was created   init the PluginFactory of the PluginSpec was successfully invoked   loaded the load method of the Plugin was successfully invoked[architecture](httpsraw.githubusercontent.comlocalstackpluxmaindocsplux-architecture.png) Loading PluginsAt run time, a PluginManager uses a PluginFinder that in turn uses stevedore to scan the available entrypoints for things that look like a PluginSpec.With PluginManager.load(name str) or PluginManager.load_all(), plugins within the namespace that are discoverable in entrypoints can be loaded.If an error occurs at any state of the lifecycle, the PluginManager informs the PluginLifecycleListener about it, but continues operating. Discovering entrypointsAt build time (e.g., with python setup.py developinstallsdist), a special PluginFinder collects anything that can be interpreted as a PluginSpec, and creates from it setuptools entrypoints.In the setup.py we can use the plugin.setuptools.load_entry_points method to collect a dictionary for the entry_points value of setup().pythonfrom plugin.setuptools import load_entry_pointssetup(    entry_pointsload_entry_points(exclude(tests, tests.,)))Note that load_entry_points will try to resolve a cached version of entry_points.txt from the .egg-info directory,to avoid resolving the entry points when building the package from a source distribution.Examples--------To build something using the plugin framework, you will first want to introduce a Plugin that does something when it is loaded.And then, at runtime, you need a component that uses the PluginManager to get those plugins. One class per pluginThis is the way we went with LocalstackCliPlugin. Every plugin class (e.g., ProCliPlugin) is essentially a singleton.This is easy, as the classes are discoverable as plugins.Simply create a Plugin class with a name and namespace and it will be discovered by the build time PluginFinder.python abstract case (not discovered at build time, missing name)class CliPlugin(Plugin)    namespace  my.plugins.cli    def load(self, cli)        self.attach(cli)    def attach(self, cli)        raise NotImplementedError discovered at build time (has a namespace, name, and is a Plugin)class MyCliPlugin(CliPlugin)    name  my    def attach(self, cli)         ... attach commands to cli objectnow we need a PluginManager (which has a generic type) to load the plugins for uspythoncli   ... needs to come from somewheremanager PluginManager[CliPlugin]  PluginManager(my.plugins.cli, load_args(cli,))plugins List[CliPlugin]  manager.load_all() todo do stuff with the plugins, if you wantneed  in this example, we simply use the plugin mechanism to run a one-shot function (attach) on a load argument Re-usable pluginsWhen you have lots of plugins that are structured in a similar way, we may not want to create a separate Plugin classfor each plugin. Instead we want to use the same Plugin class to do the same thing, but use several instances of it.The PluginFactory, and the fact that PluginSpec instances defined at module level are discoverable (inpiredby [pluggy](httpsgithub.compytest-devpluggy)), can be used to achieve that.pythonclass ServicePlugin(Plugin)    def __init__(self, service_name)        self.service_name  service_name        self.service  None    def should_load(self)        return self.service_name in config.SERVICES    def load(self)        module  importlib.import_module(localstack.services.s  self.service_name)         suppose we define a convention that each service module has a Service class, like motos Backend        self.service  module.Service()def service_plugin_factory(name) - PluginFactory    def create()        return ServicePlugin(name)    return create discoverables3  PluginSpec(localstack.plugins.services, s3, service_plugin_factory(s3)) discoverabledynamodb  PluginSpec(localstack.plugins.services, dynamodb, service_plugin_factory(dynamodb)) ... could be simplified with convenience framework code, but the principle will stay the sameThen we could use the PluginManager to build a Supervisorpythonclass Supervisor    manager PluginManager[ServicePlugin]    def start(self, service_name)        plugin  manager.load(service_name)        service  plugin.service        service.start() Functions as pluginswith the plugin decorator, you can expose functions as plugins. They will be wrapped by the frameworkinto FunctionPlugin instances, which satisfy both the contract of a Plugin, and that of the function.pythonfrom plugin import pluginplugin(namespacelocalstack.configurators)def configure_logging(runtime)    logging.basicConfig(levelruntime.config.loglevel)plugin(namespacelocalstack.configurators)def configure_somethingelse(runtime)     do other stuff with the runtime object    passWith a PluginManager via load_all, you receive the FunctionPlugin instances, that you can call like the functionspythonruntime  LocalstackRuntime()for configurator in PluginManager(localstack.configurators).load_all()    configurator(runtime)Install-------    pip install pluxDevelop-------Create the virtual environment, install dependencies, and run tests    make venv    make testRun the code formatter    make formatUpload the pypi package using twine    make upload</longdescription>
</pkgmetadata>