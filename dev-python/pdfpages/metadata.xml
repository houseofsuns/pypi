<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pdfpages========.. image:: https://img.shields.io/pypi/v/pdfpages.svg?style=flat-square   :https://pypi.python.org/pypi/pdfpages: .. image:: https://img.shields.io/github/license/philbooth/pdfpages.svg?style=flat-square   :target: https://opensource.org/licenses/MITExtract specific pages from PDF documents.* `What is it?`_* `How do I install it?`_* `How do I run it on the command line?`_* `How do I call it from python code?`_* `What license is it released under?`_What is it?-----------A python packagethat extracts pages from PDF documentsand writes them to a fresh PDF.How do I install it?--------------------Via pip::  pip install pdfpagesOr if you just want the git repo::  git clone git@github.com:philbooth/pdfpages.gitHow do I run it on the command line?------------------------------------::  pdfpages -o out.pdf in.pdfThe ``-o`` optionis used to specifythe output pathand the final argumentis the path to the input document.You can specifymultiple input documentsby listing further pathsat the end of the command::  pdfpages -o out.pdf in1.pdf in2.pdf  pdfpages -o out.pdf in/*.pdfWithout other arguments,the default behaviour isto extract the first pagefrom each input documentand write the resultto the output PDF.If you want to extract specific pages,you can use the ``-p`` option.For instance,to extract just the second pagefrom each input documentyou woud run::  pdfpages -p 2 -o out.pdf in/*.pdfOr to extract the second and third pagesfrom each document::  pdfpages -p 2 3 -o out.pdf in/*.pdfYou can also usethe ``-f`` and ``-c`` optionsto specify ranges of page numbers.For instance,to extract the first hundred pagesfrom each document::  pdfpages -f 1 -c 100 -o out.pdf in/*.pdfOr to extractthe second hundred pages::  pdfpages -f 101 -c 100 -o out.pdf in/*.pdfYou can exclude specific pagesfrom these rangeswith the ``-e`` option.For example,to exclude the third pagefrom the first five pagesof each input document::  pdfpages -f 1 -c 5 -e 3 -o out.pdf in/*.pdfFinally,you can see the usage informationat any timeusing the ``-h`` option::  python pdfpages.py -hHow do I call it from python code?----------------------------------::  import pdfpages  pdfpages.extract(in_files, out_file, pages, exclude_pages)* ``in_files``:  A tuple containing files  opened for binary reading  (mode ``&quot;rb&quot;``).* ``out_file``:  A file  opened for binary writing  (mode ``&quot;wb&quot;``).* ``pages``:  A tuple containing page numbers  to extract  (integers).* ``exclude_pages``:  An optional tuple containing page numbers  to exclude from extraction  (integers).  Defaults to an empty tuple.What license is it released under?----------------------------------`MIT &lt;LICENSE&gt;`_</longdescription>
</pkgmetadata>