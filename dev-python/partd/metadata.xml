<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>PartDBuild Status Version StatusKey-value byte store with appendable values    Partd stores key-value pairs.    Values are raw bytes.    We append on old values.Partd excels at shuffling operations.Operations----------PartD has two main operations, append and get.Example-------1.  Create a Partd backed by a directory         import partd         p  partd.File(pathtonewdataset)2.  Append key-byte pairs to dataset         p.append({x bHello , y b123})         p.append({x bworld, y b456})3.  Get bytes associated to keys         p.get(x)          One key        bHello world         p.get([y, x])   List of keys        [b123456, bHello world]4.  Destroy partd dataset         p.drop()Thats it.Implementations---------------We can back a partd by an in-memory dictionary     p  Dict()For larger amounts of data or to share data between processes we back a partdby a directory of files.  This uses file-based locks for consistency.     p  File(pathtodataset)However this can fail for many small writes.  In these cases you may wish to buffer one partd with another, keeping a fixed maximum of data in the buffering partd.  This writes the larger elements of the first partd to the second partd when space runs low     p  Buffer(Dict(), File(), available_memory2e9)   2GB memory bufferYou might also want to have many distributed process write to a single partdconsistently.  This can be done with a server   Server Process         p  Buffer(Dict(), File(), available_memory2e9)   2GB memory buffer         s  Server(p, addressipcserver)   Worker processes         p  Client(ipcserver)   Client machine talks to remote serverEncodings and Compression-------------------------Once we can robustly and efficiently append bytes to a partd we considercompression and encodings.  This is generally available with the Encodepartd, which accepts three functions, one to apply on bytes as they arewritten, one to apply to bytes as they are read, and one to join bytestreams.Common configurations already exist for common data and compression formats.We may wish to compress and decompress data transparently as we interact with apartd.  Objects like BZ2, Blosc, ZLib and Snappy exist and takeanother partd as an argument.     p  File(...)     p  ZLib(p)These work exactly as before, the (de)compression happens automatically.Common data formats like Python lists, numpy arrays, and pandasdataframes are also supported out of the box.     p  File(...)     p  NumPy(p)     p.append({x np.array([...])})This lets us forget about bytes and think instead in our normal data types.Composition-----------In principle we want to compose all of these choices together1.  Write policy  Dict, File, Buffer, Client2.  Encoding  Pickle, Numpy, Pandas, ...3.  Compression  Blosc, Snappy, ...Partd objects compose by nesting.  Here we make a partd that writes pickleencoded BZ2 compressed bytes directly to disk     p  Pickle(BZ2(File(foo)))We could construct more complex systems that include compression,serialization, buffering, and remote access.     server  Server(Buffer(Dict(), File(), available_memory2e0))     client  Pickle(Snappy(Client(server.address)))     client.append({x [1, 2, 3]}).. Build Status image httpsgithub.comdaskpartdworkflowsCIbadge.svg   target httpsgithub.comdaskpartdactionsqueryworkflow3ACI.. Version Status image httpsimg.shields.iopypivpartd.svg   target httpspypi.python.orgpypipartd</longdescription>
</pkgmetadata>