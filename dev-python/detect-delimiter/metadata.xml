<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># detect_delimiter## AboutDetects the delimiter used in CSV, TSV and other ad hoc file formats.## InstallationUse `pip install detect_delimiter`## Usage`detect_delimiter` exposes the `detect()` functinon, which takes a `str`as input and returns a delimiter.    &gt;&gt;&gt; from detect_delimiter import detect    &gt;&gt;&gt; detect(&quot;looks|like|the vertical bar\n is|the|delimiter\n&quot;)    '|'When `detect()` doesn't know, it returns `None`:    &gt;&gt;&gt; text = &quot;not really any delimiters in here.\nthis is just text.\n&quot;    &gt;&gt;&gt; detect()It's possible to provide a default, which will be used in that case:    &gt;&gt;&gt; detect(text, default=',')    ','By default, `detect()` will prevent avoid checking alpha-numeric charactersand the period/full stop character (&quot;.&quot;). This can be adjusted via the `blacklist` parameter.If you believe that you know the delimiter, it's possible to providea list of possible delimiters to check for via the `whitelist` parameter.If you don't provide a value, `[',', ';', ':', '|', '\t']` will be checked.## TestingYou can either use `pytest` or `tox` directly from the project's root directory.</longdescription>
</pkgmetadata>