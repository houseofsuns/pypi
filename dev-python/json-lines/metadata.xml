<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>json-lines.. image httpsimg.shields.iopypivjson-lines.svg   target httpspypi.python.orgpypijson-lines   alt PyPI Version.. image httpstravis-ci.orgTeamHG-Memexjson-lines.svgbranchmaster   target httptravis-ci.orgTeamHG-Memexjson-lines   alt Build Status.. image httpcodecov.iogithubTeamHG-Memexjson-linescoverage.svgbranchmaster   target httpcodecov.iogithubTeamHG-Memexjson-linesbranchmaster   alt Code CoverageThis is a tiny library for reading JSON lines (.jl) files,including gzipped and broken files.JSON lines httpjsonlines.org_ is a text file formatwhere each line is a single json encoded item.Why----Reading a well-formed JSON lines file is a one-liner in Python.But the file can be broken cut at some point(this happens when the process writing it is killed),or concatenated from several cut pieces(this happend when the process started appending to the same file again).Handling all this cases is not easy, especially if the file is compressed.json-lines handles all this cases for youInstallation------------    pip install json-linesIf ujson httpspypi.python.orgpypiujson_ is installed, it is usedto speed up json decoding (which is the main performance bottleneckeven for gzipped files).Usage-----In order to read a well-formed json lined file,pass an open file as the first argument to json_lines.reader.The file can be openedin text or binary mode, but if its opened in text mode, the encodingmust be set correctly    import json_lines    with open(file.jl, rb) as f        for item in json_lines.reader(f)            print(item[x])There is also a helper function json_lines.open that recognizes.gz and .gzip extensions and opens them with gzip    with json_lines.open(file.jl.gz) as f        for item in f            print(item[x])Handling broken (cut at some point) files is enabled by passing brokenTrueto json_lines.reader or json_lines.open.Broken lines are skipped (only logging a warning), and reading continuesfrom the next valid position.This works both for compressed and uncompressed files    with json_lines.open(file.jl.gz, brokenTrue) as f        for item in f            print(item[x])License-------License is MIT.</longdescription>
</pkgmetadata>