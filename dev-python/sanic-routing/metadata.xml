<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Sanic Routing BackgroundBeginning in v21.3, Sanic makes use of this new AST-style router in two use cases1. Routing paths and2. Routing signals.Therefore, this package comes with a BaseRouter that needs to be subclassed in order to be used for its specific needs. Most Sanic users should never need to concern themselves with the details here. Basic ExampleA simple implementationpythonimport loggingfrom sanic_routing import BaseRouterlogging.basicConfig(levellogging.DEBUG)class Router(BaseRouter)    def get(self, path, args, kwargs)        return self.resolve(path, args, kwargs)router  Router()router.add(foo, lambda ...)router.finalize()router.tree.display()logging.info(router.find_route_src)route, handler, params  router.get(matchme, methodBASE, extraNone)The above snippet uses router.tree.display() to show how the router has decided to arrange the routes into a tree. In this simple exampleNode level0    Node part__dynamic__str, level1, groups[RouteGroup pathfoostr len1], dynamicTrueWe can can see the code that the router has generated for us. It is available as a string at router.find_route_src.pythondef find_route(path, method, router, basket, extra)    parts  tuple(path[1].split(router.delimiter))    num  len(parts)         node1  part__dynamic__str    if num  1   CHECK 1        try            basket[__matches__][0]  str(parts[0])        except ValueError            pass        else             Return 1            return router.dynamic_routes[(__dynamic__str,)][0], basket    raise NotFound_FYI If you are on Python 3.9, you can see a representation of the source after compilation at router.find_route_src_compiled_ Whats it doingTherefore, in general implementation requires you to1. Define a router with a get method2. Add one or more routes3. Finalize the router (router.finalize()) and4. Call the routers get method._NOTE You can call router.finalize(False) if you do not want to compile the source code into executable form. This is useful if you only intend to review the generated output._Every time you call router.add you create one (1) new Route instance. Even if that one route is created with multiple methods, it generates a single instance. If you add() another Route that has a similar path structure (but, perhaps has differen methods) they will be grouped together into a RouteGroup. It is worth also noting that a RouteGroup is created the first time you call add(), but subsequent similar routes will reuse the existing grouping instance.When you call finalize(), it is taking the defined route groups and arranging them into nodes in a hierarchical tree. A single node is a path segment. A Node instance can have one or more RouteGroup on it where the Node is the termination point for that path.Perhaps an example is easierpythonrouter.add(pathtofoo, lambda ...)router.add(pathtofooint, lambda ...)router.add(pathtodifferentfoo, lambda ...)router.add(pathtodifferentfoo, lambda ..., methods[one, two])The generated RouteGroup instances (3)RouteGroup pathpathtofoostr len1RouteGroup pathpathtofooint len1RouteGroup pathpathtodifferentfoostr len2The generated Route instances (4)Route pathpathtofoostrRoute pathpathtofoointRoute pathpathtodifferentfoostrRoute pathpathtodifferentfoostrThe Node TreeNode level0    Node partpath, level1        Node partto, level2            Node partdifferent, level3                Node part__dynamic__str, level4, groups[RouteGroup pathpathtodifferentfoostr len2], dynamicTrue            Node part__dynamic__int, level3, groups[RouteGroup pathpathtofooint len1], dynamicTrue            Node part__dynamic__str, level3, groups[RouteGroup pathpathtofoostr len1], dynamicTrueAnd, the generated source codepythondef find_route(path, method, router, basket, extra)    parts  tuple(path[1].split(router.delimiter))    num  len(parts)         node1  partpath    if num  1   CHECK 1        if parts[0]  path   CHECK 4                         node1.1  partto            if num  2   CHECK 1                if parts[1]  to   CHECK 4                                         node1.1.1  partdifferent                    if num  3   CHECK 1                        if parts[2]  different   CHECK 4                                                         node1.1.1.1  part__dynamic__str                            if num  4   CHECK 1                                try                                    basket[__matches__][3]  str(parts[3])                                except ValueError                                    pass                                else                                    if method in frozenset({one, two})                                        route_idx  0                                    elif method in frozenset({BASE})                                        route_idx  1                                    else                                        raise NoMethod                                     Return 1.1.1.1                                    return router.dynamic_routes[(path, to, different, __dynamic__str)][route_idx], basket                                         node1.1.2  part__dynamic__int                    if num  3   CHECK 1                        try                            basket[__matches__][2]  int(parts[2])                        except ValueError                            pass                        else                            if num  3   CHECK 5                                 Return 1.1.2                                return router.dynamic_routes[(path, to, __dynamic__int)][0], basket                                         node1.1.3  part__dynamic__str                    if num  3   CHECK 1                        try                            basket[__matches__][2]  str(parts[2])                        except ValueError                            pass                        else                            if num  3   CHECK 5                                 Return 1.1.3                                return router.dynamic_routes[(path, to, __dynamic__str)][0], basket    raise NotFound Special casesThe above example only shows routes that have a dynamic path segment in them (example foo). But, there are other use cases that are covered differently1. fully static paths - These are paths with no parameters (example userlogin). These are basically matched against a keyvalue store.2. regex paths - If a route as a single regular expression match, then the whole route will be matched via regex. In general, this happens inline not too dissimilar than what we see in the above example.3. special regex paths - The router comes with a special path type (example foopath) that can match on an expanded delimiter. This is also true for any regex that uses the path delimiter in it. These cannot be matched in the normal course since they are of unknown length.</longdescription>
</pkgmetadata>