<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> FastAPI Cloud Auth[Tests](httpsgithub.comtokusumifastapi-cloudauthworkflowsTestsbadge.svg)[[codecov](httpscodecov.ioghtokusumifastapi-cloudauthbranchmastergraphbadge.svg)](httpscodecov.ioghtokusumifastapi-cloudauth)[[PyPI version](httpsbadge.fury.iopyfastapi-cloudauth.svg)](httpsbadge.fury.iopyfastapi-cloudauth)fastapi-cloudauth standardizes and simplifies the integration between FastAPI and cloud authentication services (AWS Cognito, Auth0, Firebase Authentication). Features [X] Verify accessid token standard JWT validation (signature, expiration), token audience claims, etc. [X] Verify permissions based on scope (or groups) within access token and extract user info  [X] Get the detail of login user info (name, email, etc.) within ID token [X] Dependency injection for verificationgetting user, powered by [FastAPI](httpsgithub.comtiangolofastapi) [X] Support for     [X] [AWS Cognito](httpsaws.amazon.comjpcognito)     [X] [Auth0](httpsauth0.comjp)     [x] [Firebase Auth](httpsfirebase.google.comdocsauth) (Only ID token) RequirementsPython 3.6+ Installconsole pip install fastapi-cloudauth Example (AWS Cognito) Pre-requirements Check region, userPoolID and AppClientID of AWS Cognito that you manage to Create a users assigned readusers permission in AWS Cognito  Get AccessID token for the created userNOTE access token is valid for verification, scope-based authentication, and getting user info (optional). ID token is valid for verification and getting full user info from claims. Create itCreate a main.py file with the following contentpython3import osfrom pydantic import BaseModelfrom fastapi import FastAPI, Dependsfrom fastapi_cloudauth.cognito import Cognito, CognitoCurrentUser, CognitoClaimsapp  FastAPI()auth  Cognito(    regionos.environ[REGION],     userPoolIdos.environ[USERPOOLID],    client_idos.environ[APPCLIENTID])app.get(, dependencies[Depends(auth.scope([readusers]))])def secure()     access token is valid    return Helloclass AccessUser(BaseModel)    sub strapp.get(access)def secure_access(current_user AccessUser  Depends(auth.claim(AccessUser)))     access token is valid and getting user info from access token    return fHello, {current_user.sub}get_current_user  CognitoCurrentUser(    regionos.environ[REGION],     userPoolIdos.environ[USERPOOLID],    client_idos.environ[APPCLIENTID])app.get(user)def secure_user(current_user CognitoClaims  Depends(get_current_user))     ID token is valid and getting user info from ID token    return fHello, {current_user.username}Run the server withconsole uvicorn mainappINFO     Started server process [15332]INFO     Waiting for application startup.INFO     Application startup complete.INFO     Uvicorn running on http127.0.0.18000 (Press CTRL+C to quit) Interactive API DocGo to http127.0.0.18000docs.You will see the automatic interactive API documentation (provided by Swagger UI).Authorize unlock button can be available at the endpoints injected dependency.You can supply a token and try the endpoint interactively.[Swagger UI](httpsraw.githubusercontent.comtokusumifastapi-cloudauthmasterdocssrcauthorize_in_doc.jpg) Example (Auth0) Pre-requirement Check domain, customAPI (Audience) and ClientID of Auth0 that you manage to Create a user assigned readusers permission in Auth0  Get AccessID token for the created user Create itCreate a file main.py withpython3import osfrom pydantic import BaseModelfrom fastapi import FastAPI, Dependsfrom fastapi_cloudauth.auth0 import Auth0, Auth0CurrentUser, Auth0Claimsapp  FastAPI()auth  Auth0(domainos.environ[DOMAIN], customAPIos.environ[CUSTOMAPI])app.get(, dependencies[Depends(auth.scope([readusers]))])def secure()     access token is valid    return Helloclass AccessUser(BaseModel)    sub strapp.get(access)def secure_access(current_user AccessUser  Depends(auth.claim(AccessUser)))     access token is valid and getting user info from access token    return fHello, {current_user.sub}get_current_user  Auth0CurrentUser(    domainos.environ[DOMAIN],    client_idos.environ[CLIENTID])app.get(user)def secure_user(current_user Auth0Claims  Depends(get_current_user))     ID token is valid and getting user info from ID token    return fHello, {current_user.username}Try to run the server and see interactive UI in the same way. Example (Firebase Authentication) Pre-requirement Create a user in Firebase Authentication and get project ID Get ID token for the created user Create itCreate a file main.py withpython3from fastapi import FastAPI, Dependsfrom fastapi_cloudauth.firebase import FirebaseCurrentUser, FirebaseClaimsapp  FastAPI()get_current_user  FirebaseCurrentUser(    project_idos.environ[PROJECT_ID])app.get(user)def secure_user(current_user FirebaseClaims  Depends(get_current_user))     ID token is valid and getting user info from ID token    return fHello, {current_user.user_id}Try to run the server and see the interactive UI in the same way. Additional User InformationWe can get values for the current user from accessID token by writing a few lines. Custom ClaimsFor Auth0, the ID token contains the following extra values (Ref at [Auth0 official doc](httpsauth0.comdocstokens))json{  iss httpYOUR_DOMAIN,  sub auth0123456,  aud YOUR_CLIENT_ID,  exp 1311281970,  iat 1311280970,  name Jane Doe,  given_name Jane,  family_name Doe,  gender female,  birthdate 0000-10-31,  email janedoeexample.com,  picture httpexample.comjanedoeme.jpg}By default, Auth0CurrentUser gives pydantic.BaseModel object, which has username (name) and email fields.Here is sample code for extracting extra user information (adding user_id) from ID tokenpython3from pydantic import Fieldfrom fastapi_cloudauth.auth0 import Auth0Claims   base current user info model (inheriting pydantic). extend current user info model by pydantic.class CustomAuth0Claims(Auth0Claims)    user_id str  Field(aliassub)get_current_user  Auth0CurrentUser(domainDOMAIN, client_idCLIENTID)get_current_user.user_info  CustomAuth0Claims   override user info model with a custom one.Or, we can set new custom claims as followspython3get_user_detail  get_current_user.claim(CustomAuth0Claims)app.get(new)async def detail(user CustomAuth0Claims  Depends(get_user_detail))    return fHello, {user.user_id} Raw payloadIf you dont require pydantic data serialization (validation), FastAPI-CloudAuth has an option to extract the raw payload.All you need ispython3get_raw_info  get_current_user.claim(None)app.get(new)async def raw_detail(user  Depends(get_raw_info))     user has all items (ex. iss, sub, aud, exp, ... it depends on passed token)     return fHello, {user.get(sub)} Additional scopesAdvanced user-SCOPE verification to protect your API.Supports- all (default) required all scopes you set- any At least one of the configured scopes is requiredUse as (auth is this instanse and app is fastapi.FastAPI instanse)python3from fastapi import Dependsfrom fastapi_cloudauth import Operatorapp.get(, dependencies[Depends(auth.scope([allowned, scopes]))])def api_all_scope()    return user has allowned and scopes scopesapp.get(, dependencies[Depends(auth.scope([allowned, scopes], opOperator._any))])def api_any_scope()    return user has at least one of scopes (allowned, scopes) Development - ContributingPlease read [CONTRIBUTING](.CONTRIBUTING.md) for how to set up the development environment and testing.</longdescription>
</pkgmetadata>