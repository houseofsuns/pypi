<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>UnidiffSimple Python library to parse and interact with unified diff data... image httpswww.travis-ci.commatiasbpython-unidiff.svgbranchmaster    target httpstravis-ci.commatiasbpython-unidiffInstalling unidiff------------------     pip install unidiffQuick start-----------     import urllib.request     from unidiff import PatchSet     diff  urllib.request.urlopen(httpsgithub.commatiasbpython-unidiffpull3.diff)     encoding  diff.headers.get_charsets()[0]     patch  PatchSet(diff, encodingencoding)     patch    PatchSet [PatchedFile .gitignore, PatchedFile unidiffpatch.py, PatchedFile unidiffutils.py]     patch[0]    PatchedFile .gitignore     patch[0].is_added_file    True     patch[0].added    6     patch[1]    PatchedFile unidiffpatch.py     patch[1].added, patch[1].removed    (20, 11)     len(patch[1])    6     patch[1][2]    Hunk  109,14 110,21  def __repr__(self)     patch[2]    PatchedFile unidiffutils.py     print(patch[2])    diff --git aunidiffutils.py bunidiffutils.py    index eae63e6..29c896a 100644    --- aunidiffutils.py    +++ bunidiffutils.py     -37,4 +37,3      - deleted line      No newline case (ignore)    RE_HUNK_BODY_LINE  re.compile(r([- +]))    -Load unified diff data by instantiating codePatchSet with a file-like object asargument, or using codePatchSet.from_filename class method to read diff from file.A codePatchSet is a list of files updated by the given patch. For each codePatchedFileyou can get stats (if it is a new, removed or modified file the sourcetargetlines etc), besides having access to each hunk (also like a list) and itsrespective info.At any point you can get the string representation of the current object, andthat will return the unified diff data of it.As a quick example of what can be done, check binunidiff file.Also, once installed, unidiff provides a command-line program that displaysinformation from diff data (a file, or stdin). For example     git diff  unidiff    Summary    -------    README.md +6 additions, -0 deletions    1 modified file(s), 0 added file(s), 0 removed file(s)    Total 6 addition(s), 0 deletion(s)Load a local diff file----------------------To instantiate codePatchSet from a local file, you can use     from unidiff import PatchSet     patch  PatchSet.from_filename(testssamplesbzr.diff, encodingutf-8)     patch    PatchSet [PatchedFile added_file, PatchedFile modified_file, PatchedFile removed_file]Notice the (optional) codeencoding parameter. If not specified, unicode input will be expected. Or alternatively     import codecs     from unidiff import PatchSet     with codecs.open(testssamplesbzr.diff, r, encodingutf-8) as diff    ...     patch  PatchSet(diff)    ...     patch    PatchSet [PatchedFile added_file, PatchedFile modified_file, PatchedFile removed_file]Finally, you can also instantiate codePatchSet passing any iterable (and encoding, if needed)     from unidiff import PatchSet     with open(testssamplesbzr.diff, r) as diff    ...     data  diff.readlines()    ...     patch  PatchSet(data)     patch    PatchSet [PatchedFile added_file, PatchedFile modified_file, PatchedFile removed_file]If you dont need to be able to rebuild the original unified diff input, you can passcodemetadata_onlyTrue (defaults to codeFalse), which should help making theparsing more efficient     from unidiff import PatchSet     patch  PatchSet.from_filename(testssamplesbzr.diff, encodingutf-8, metadata_onlyTrue)References---------- httpen.wikipedia.orgwikiDiff_utility httpwww.artima.comweblogsviewpost.jspthread164293</longdescription>
</pkgmetadata>