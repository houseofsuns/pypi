<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>jsonfield.. image httpscircleci.comghrpkilbyjsonfield.svgstyleshield  target httpscircleci.comghrpkilbyjsonfield.. image httpscodecov.ioghrpkilbyjsonfieldbranchmastergraphbadge.svg  target httpscodecov.ioghrpkilbyjsonfield.. image httpsimg.shields.iopypivjsonfield.svg  target httpspypi.orgprojectjsonfield.. image httpsimg.shields.iopypiljsonfield.svg  target httpspypi.orgprojectjsonfieldjsonfield is a reusable model field that allows you to store validated JSON, automatically handlingserialization to and from the database. To use, add jsonfield.JSONField to one of your models.Note django.contrib.postgres_ now supports PostgreSQLs jsonb type, which includes extended queryingcapabilities. If youre an end user of PostgreSQL and want full-featured JSON support, then it isrecommended that you use the built-in JSONField. However, jsonfield is still useful when your appneeds to be database-agnostic, or when the built-in JSONFields extended querying is not being leveraged.e.g., a configuration field... _django.contrib.postgres httpsdocs.djangoproject.comendevrefcontribpostgresfieldsjsonfieldRequirements------------jsonfield aims to support all current versions of Django_, however the explicity tested versions are Python 3.6, 3.7, 3.8 Django 2.2, 3.0.. _versions of Django httpswww.djangoproject.comdownloadsupported-versionsInstallation------------.. code-block python    pip install jsonfieldUsage-----.. code-block python    from django.db import models    from jsonfield import JSONField    class MyModel(models.Model)        json  JSONField()Querying--------As stated above, JSONField is not intended to provide extended querying capabilities.That said, you may perform the same basic lookups provided by regular text fields (e.g.,exact or regex lookups). Since values are stored as serialized JSON, it is highlyrecommended that you test your queries to ensure the expected results are returned.Handling null values--------------------A model fields null argument typically controls whether null values may be stored inits column by setting a not-null constraint. However, because JSONField serializes itsvalues (including nulls), this option instead controls how null values are persisted. IfnullTrue, then nulls are not serialized and are stored as a null value in thedatabase. If nullFalse, then the null is instead stored in its serialized form.This in turn affects how null values may be queried. Both fields support exact matching.. code-block python    MyModel.objects.filter(jsonNone)However, if you want to use the isnull lookup, you must set nullTrue... code-block python    class MyModel(models.Model)        json  JSONField(nullTrue)    MyModel.objects.filter(json__isnullTrue)Note that as JSONField.null does not prevent nulls from being stored, achieving thismust instead be handled with a validator.Advanced Usage--------------By default python deserializes json into dict objects. This behavior differs from the standard jsonbehavior  because python dicts do not have ordered keys. To overcome this limitation and keep thesort order of OrderedDict keys the deserialisation can be adjusted on model initialisation.. code-block python    import collections    class MyModel(models.Model)        json  JSONField(load_kwargs{object_pairs_hook collections.OrderedDict})Other Fields------------jsonfield.JSONCharFieldSubclasses models.CharField instead of models.TextField.Running the tests-----------------The test suite requires tox... code-block shell     pip install toxThen, run the tox command, which will run all test jobs... code-block shell     toxOr, to test just one job (for example Django 2.0 on Python 3.6).. code-block shell     tox -e py36-django20Release Process--------------- Update changelog Update package version in setup.py Check supported versions in setup.py and readme Create git tag for version Upload release to PyPI test server Upload release to official PyPI server.. code-block shell     pip install -U pip setuptools wheel twine     rm -rf dist build     python setup.py sdist bdist_wheel     twine upload -r test dist     twine upload distChanges-------Take a look at the changelog_... _changelog httpsgithub.comrpkilbyjsonfieldblobmasterCHANGES.rst</longdescription>
</pkgmetadata>