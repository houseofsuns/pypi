<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Tippo=====.. image:: https://github.com/brunonicko/tippo/workflows/MyPy/badge.svg   :target: https://github.com/brunonicko/tippo/actions?query=workflow%3AMyPy.. image:: https://github.com/brunonicko/tippo/workflows/Lint/badge.svg   :target: https://github.com/brunonicko/tippo/actions?query=workflow%3ALint.. image:: https://github.com/brunonicko/tippo/workflows/Tests/badge.svg   :target: https://github.com/brunonicko/tippo/actions?query=workflow%3ATests.. image:: https://readthedocs.org/projects/tippo/badge/?version=stable   :target: https://tippo.readthedocs.io/en/stable/.. image:: https://img.shields.io/github/license/brunonicko/tippo?color=light-green   :target: https://github.com/brunonicko/tippo/blob/main/LICENSE.. image:: https://static.pepy.tech/personalized-badge/tippo?period=total&amp;units=international_system&amp;left_color=grey&amp;right_color=brightgreen&amp;left_text=Downloads   :target: https://pepy.tech/project/tippo.. image:: https://img.shields.io/pypi/pyversions/tippo?color=light-green&amp;style=flat   :target: https://pypi.org/project/tippo/Overview--------Use the latest type annotation features in older versions of Python.Motivation----------When working with Python development for VFX pipelines we are often stuck with olderPython versions for the runtime.`Tippo` aims to bridge that gap since it allows us to use features such as static typechecking (which could be performed by newer Python versions with support for MyPy duringthe testing phase) even though the code might be designed to run in Python 2.7, forexample.Installation------------`Tippo` is available through `pip`:``pip install tippo``Usage-----Instead of importing from `typing` and/or `typing_extensions`..... code:: python    &gt;&gt;&gt; try:    ...     from typing import TypeAlias    ... except ImportError:    ...     from typing_extensions import TypeAlias    ...    &gt;&gt;&gt; try:    ...     from typing import final    ... except ImportError:    ...     from typing_extensions import final    ......just import directly from `tippo`:.. code:: python    &gt;&gt;&gt; from tippo import TypeAlias, finalGeneric Fixes-------------`Tippo` patches `GenericMeta` to fix known bugs for the Python 2.7 version of `typing`that were not addressed since it's not officially supported anymore.Generic class comparison:.. code:: python    &gt;&gt;&gt; from tippo import Mapping    &gt;&gt;&gt; assert Mapping[str, int] == Mapping[str, int]  # passes    &gt;&gt;&gt; assert not (Mapping[str, int] != Mapping[str, int])  # passesSubclassing a generic class with a `__weakref__` slot:.. code:: python    &gt;&gt;&gt; from weakref import ref    &gt;&gt;&gt; from tippo import Generic, TypeVar    &gt;&gt;&gt; T = TypeVar(&quot;T&quot;)    &gt;&gt;&gt; class MyGeneric(Generic[T]):    ...     __slots__ = (&quot;__weakref__&quot;,)    ...    &gt;&gt;&gt; class SubClass(MyGeneric[T]):  # does not error out    ...     __slots__ = ()    ...    &gt;&gt;&gt; instance = SubClass()    &gt;&gt;&gt; instance_ref = ref(instance)In order to maintain the same interface, `GenericMeta` points to `type` when importedfrom `tippo` in newer versions of Python.Backports---------Features from the latest versions of Python, such as `TypeAlias`, `ClassVar`, `NewType`,`get_origin`, and `get_args`... code:: python    &gt;&gt;&gt; from tippo import Mapping, get_args, get_name    &gt;&gt;&gt; mapping_type = Mapping[str, int]    &gt;&gt;&gt; [get_name(a) for a in get_args(mapping_type)]    ['str', 'int']Commonly Used Protocols-----------------------Such as:- `tippo.SupportsGetItem`- `tippo.SupportsGetSetItem`- `tippo.SupportsGetSetDeleteItem`- `tippo.SupportsKeysAndGetItem`.. code:: python    &gt;&gt;&gt; from tippo import SupportsGetItem    &gt;&gt;&gt; class Foo(object):    ...     def __getitem__(self, item):    ...         # type: (str) -&gt; int    ...         return 3    ...    &gt;&gt;&gt; def get_stuff(bar):    ...     # type: (SupportsGetItem[str, int]) -&gt; int    ...     return bar[&quot;stuff&quot;]    ...    &gt;&gt;&gt; assert get_stuff(Foo()) == 3  # passes static type checking    &gt;&gt;&gt; assert get_stuff({&quot;stuff&quot;: 3}) == 3  # passes static type checking</longdescription>
</pkgmetadata>