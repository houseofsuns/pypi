<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>aws-encryption-sdk-cli.. image httpsimg.shields.iopypivaws-encryption-sdk-cli.svg   target httpspypi.python.orgpypiaws-encryption-sdk-cli   alt Latest Version.. image httpsimg.shields.iopypipyversionsaws-encryption-sdk-cli.svg   target httpspypi.python.orgpypiaws-encryption-sdk-cli   alt Supported Python Versions.. image httpsimg.shields.iobadgecode_style-black-000000.svg   target httpsgithub.comambvblack   alt Code style black.. image httpsreadthedocs.orgprojectsaws-encryption-sdk-clibadge   target httpsaws-encryption-sdk-cli.readthedocs.ioenstable   alt Documentation Status.. image httpsgithub.comawsaws-encryption-sdk-cliworkflowstestsbadge.svg   target httpsgithub.comawsaws-encryption-sdk-cliactionsqueryworkflow3Atests   alt tests.. image httpsgithub.comawsaws-encryption-sdk-cliworkflowsstatic20analysisbadge.svg   target httpsgithub.comawsaws-encryption-sdk-cliactionsqueryworkflow3A22static+analysis22   alt static analysisThis command line tool can be used to encrypt and decrypt files and directories using the AWS Encryption SDK_.The latest full documentation can be found at Read the Docs_.Find us on GitHub_.Security issue notifications_See Support Policy_ for details on the current support status of all major versions of this library.Getting StartedRequired Prerequisites Python 3.6+ aws-encryption-sdk  3.1.0Installation.. note   If you have not already installed cryptography_, you might need to install additional prerequisites as   detailed in the cryptography installation guide_ for your operating system.   As a system package   .. code        pip install aws-encryption-sdk-cli   Using a virtual environment   Installation using a python virtual environment is recommended to avoid conflicts between system packages and user installed packages.   For the latest information on Python virtual environments, refer to the Python.org Virtual Environment Documentation_   MacOSUnix   .. code-block sh       cd my_project       python3 -m venv env       source envbinactivate       python -m pip install aws-encryption-sdk-cli   Windows (PowerShell)   .. code-block sh       cd my_project       python3 -m venv env       .envScriptsActivate.ps1      (env)  pip install aws-encryption-sdk-cliUsageInput and OutputFor the most part, the behavior of aws-encryption-cli in handling files is based on thatof GNU CLIs such as cp.  A qualifier to this is that when encrypting a file, if adirectory is provided as the destination, rather than creating the source filenamein the destination directory, a suffix is appended to the destination filename. Bydefault the suffix is .encrypted when encrypting and .decrypted when decrypting,but a custom suffix can be provided by the caller if desired.If a destination file already exists, the contents will be overwritten... table    +------------------------------+---------------------------------------+     Allowed inputoutput        output                                 pairings                   +------------+----------+---------------+                                   stdout  file  directory     +-----------+------------------+------------+----------+---------------+     input    stdin       Y           Y                                      +------------------+------------+----------+---------------+                 single file  Y           Y         Y                            +------------------+------------+----------+---------------+                pattern match                       Y                            +------------------+------------+----------+---------------+                  directory                         Y                 +-----------+------------------+------------+----------+---------------+If the source includes a directory and the --recursive flag is set, the entiretree of the source directory is replicated in the target directory.Parameter Values----------------Some arguments accept additional parameter values.  These values must be provided in theform of keyvalue as demonstrated below... code-block sh   --encryption-context key1value1 key2value2 key 3value with spaces   --master-keys provideraws-kms keyKEY_ID_1 keyKEY_ID_2   --caching capacity3 max_age80.0Encryption Context------------------EncryptThe encryption context_ is an optional, but recommended, set of key-value pairs that containarbitrary nonsecret data. The encryption context can contain any data you choose, but ittypically consists of data that is useful in logging and tracking, such as data about the filetype, purpose, or ownership.Parameters may be provided using Parameter Values_... code-block sh   --encryption-context key1value1 key2value2 key 3value with spacesDecryptIf an encryption context is provided on decrypt, it is instead used to require that the messagebeing decrypted was encrypted using an encryption context that matches the specified requirements.If keyvalue elements are provided, the decryption will only continue if the encryptioncontext found in the encrypted message contains matching pairs... code-block sh   --encryption-context required_keyrequired_value classificationsecretIf bare key elements are provided, the decryption will continue if those keys are found,regardless of the values. key and keyvalue elements can be mixed... code-block sh   --encryption-context required_key classificationsecret.. warning   If encryption context requirements are not satisfied by the ciphertext message, the   message will not be decrypted. One side effect of this is that if you chose to write   the plaintext output to a file and that file already exists, it will be deleted when   we stop the decryption.Output Metadata---------------In addition to the actual output of the operation, there is metadata about the operationthat can be useful. This metadata includes some information about the operation as well asthe complete header data from the ciphertext message.The metadata for each operation is written to the specified file as a single line containingformatted JSON, so if a single command performs multiple file operations, a separate linewill be written for each operation. There are three operating modes --metadata-output FILE  Writes the metadata output to FILE (can be - for stdout  as long as main output is not stdout). Default behavior is to append the metadata entry to  the end of FILE. --overwrite-metadata  Force overwriting the contents of FILE with the new metadata. -S--suppress-metadata  Output metadata is suppressed.Metadata ContentsThe metadata JSON contains the following fields mode  encryptdecryptdecrypt-unsigned input  Full path to input file (or stdin if stdin) output  Full path to output file (or stdout if stdout) header  JSON representation of message header data_ header_auth  JSON representation of message header authentication data_ (only on decrypt)Skipped FilesIf encryption context checks fail when attempting to decrypt a file, the metadata containsadditional fields skipped  true reason  Missing encryption context key or value missing_encryption_context_keys  List of required encryption context keys that were  missing from the message. missing_encryption_context_pairs  List of required encryption context key-value pairs  missing from the message.Master Key Provider-------------------Information for configuring a master key provider must be provided.Parameters may be provided using Parameter Values_.These parameters are common to all master key providers provider (default aws-encryption-sdk-cliaws-kms)  Indicator of the master key  provider to use.     See Advanced Configuration_ for more information on using other master key providers. key (on encrypt at least one required, many allowed on decrypt one of key or discovery is required)   Identifier for a wrapping key to be used in the operation. Must be an identifier understood by the specified master  key provider. The discovery attribute is only available if you are using an aws-kms provider.     If using aws-kms to decrypt, you must specify either a key or discovery with a value of true_.     If using aws-kms to decrypt and specifying a key, you must use a key ARN key ids, alias names, and alias      ARNs are not supported.Any additional parameters supplied are collected into lists by parameter name andpassed to the master key provider class when it is instantiated. Custom master key providersmust accept all arguments as prepared. See Advanced Configuration_ for more information.Multiple master keys can be defined using multiple instances of the key argument.Multiple master key providers can be defined using multiple --wrapping-keys groups.If multiple master key providers are defined, the first one is treated as the primary.If multiple master keys are defined in the primary master key provider, the first one is treatedas the primary. The primary master key is used to generate the data key.The following logic is used to construct all master key providers. We useStrictAwsKmsMasterKeyProvider as an example... code-block python    With parameters   --wrapping-keys provideraws-kms keyKEY_1 keyKEY_2    KMSMasterKeyProvider is called as   key_provider  StrictAwsKmsMasterKeyProvider(key_ids[KEY_1, KEY_2]).. code-block sh    Single KMS CMK   --wrapping-keys provideraws-kms keyKEY_ARN_1    Two KMS CMKs   --wrapping-keys provideraws-kms keyKEY_ARN_1 keyKEY_ARN_2    KMS Alias by name in default region   --wrapping-keys provideraws-kms keyALIAS_NAME    KMS Alias by name in two specific regions   --wrapping-keys provideraws-kms keyALIAS_NAME regionus-west-2   --wrapping-keys provideraws-kms keyALIAS_NAME regioneu-central-1AWS KMSIf you want to use the aws-kms master key provider, you can either specify thatas the provider or simply not specify a provider and allow the default value to be used.There are some configuration options which are unique to the aws-kms master key provider profile  Providing this configuration value will use the specified named profile_  credentials. discovery (default false one of key or discovery with a value of true is required)   Indicates whether this provider should be in discovery mode. If true (enabled), the AWS Encryption CLI will attempt  to decrypt ciphertexts encrypted with any AWS KMS CMK. If false (disabled), the AWS Encryption CLI will only attempt  to decrypt ciphertexts encrypted with the key ARNs specified in the key attribute.  Any key specified in the key attribute that is a KMS CMK Identier other than a key ARN will not  be used for decryption. discovery-account (optional available only when discoverytrue and discovery-partition is also provided)   If discovery is enabled, limits decryption to AWS KMS CMKs in the specified accounts. discovery-partition (optional available only when discoverytrue and discovery-account is also provided)   If discovery is enabled, limits decryption to AWS KMS CMKs in the specified partition, e.g. aws or aws-gov. region  This allows you to specify the target region.The logic for determining which region to use is shown in the pseudocode below.. code-block python   if key ID is an ARN      use region identified in ARN   else      if region is specified         use region      else if profile is specified and profile has a defined region         use region defined in profile      else         use system default regionAdvanced ConfigurationIf you want to use a different master key provider, that provider must register asetuptools entry point_. You can find an example of registering this entry point in thesetup.py for this package.When a provider name is specifed in a call to aws-encryption-cli, the appropriate entrypoint for that name is used.Handling Multiple Entry PointsIf multiple entry points are registered for a given name, you will need to specify the packagethat registered the entry point you want to use.In order to specify the package name, use the format PACKAGE_NAMEENTRY_POINT. provideraws-kms provideraws-encryption-sdk-cliaws-kmsIf you supply only an entry point name and there is only one entry point registered for thatname, that entry point will be used.If you supply only an entry point name and there is more than one entry point registeredfor that name, an error will be raised showing you all of the packages that have an entrypoint registered for that name.If you supply both a package and an entry point name, that exact entry point will be used.If it is not accessible, an error will be raised showing you all of the packages that havean entry point registered for that name.External Master Key ProvidersThe entry point name use must not contain the string . This is used as a namespaceseparator as descibed in Handling Multiple Entry Points_.When called, these entry points must return an instance of a master key provider. They mustaccept the parameters prepared by the CLI as described in Master Key Provider_.These entry points must be registered in the aws_encryption_sdk_cli.master_key_providersgroup.If the entry point raises a aws_encryption_sdk_cli.exceptions.BadUserArgumentError, theCLI will present the raised error message to the user to indicate bad user input.Commitment Policy-----------------The commitment policy controls which algorithms can be used in encryption and decryption.Versions 2.0.x and later of the AWS Encryption CLI use a default commitment policy ofrequire-encrypt-require-decrypt, which ensures that only algorithms which provide key commitment_ can be usedon both encryption and decryption. If you want to use a different commitment policy, you can do sowith the --commitment-policy parameter.For more details, see the Commitment Policy_ documentation... code-block sh    Use a commitment policy that requires an algorithm which provides key commitment    on both encryption and decryption   --commitment-policy require-encrypt-require-decryptData Key Caching----------------Data key caching is optional, but if used then the parameters noted as required mustbe provided.  For detailed information about using data key caching with the AWSEncryption SDK, see the data key caching documentation_.Parameters may be provided using Parameter Values_.Allowed parameters capacity (required)  Number of entries that the cache will hold. max_age (required)   Determines how long each entry can remain in the cache, beginning when it was added. max_messages_encrypted   Determines how long each entry can remain in the cache, beginning when it was added. max_bytes_encrypted  Specifies the maximum number of bytes that a cached data key can encrypt.Logging and Verbosity---------------------The -v argument allows you to tune the verbosity of the built-in logging to your desired level.In short, the more -v arguments you supply, the more verbose the output gets. unset  aws-encryption-cli logs all warnings, all dependencies only log critical messages -v   aws-encryption-cli performs moderate logging, all dependencies only log critical messages -vv   aws-encryption-cli performs detailed logging, all dependencies only log critical messages -vvv   aws-encryption-cli performs detailed logging, all dependencies perform moderate logging -vvvv   aws-encryption-cli performs detailed logging, all dependencies perform detailed logging.. table   +-----------------------------------------------+              python logging levels                  ++++    verbosity  aws-encryption-cli  dependencies     flag                                           +-----------+--------------------+--------------+    unset      WARNING             CRITICAL        +-----------+--------------------+--------------+    -v         INFO                CRITICAL        +-----------+--------------------+--------------+    -vv        DEBUG               CRITICAL        +-----------+--------------------+--------------+    -vvv       DEBUG               INFO            +-----------+--------------------+--------------+    -vvvv      DEBUG               DEBUG           +-----------+--------------------+--------------+Configuration Files-------------------As with any CLI where the configuration can get rather complex, you might want to use a configurationfile to define some or all of your desired behavior.Configuration files are supported using Pythons native argparse file support_, which allowsyou to write configuration files exactly as you would enter arguments in the shell. Configurationfile references passed to aws-encryption-cli are identified by the  prefix and thecontents are expanded as if you had included them in line. Configuration files can have anyname you desire... note   In PowerShell, you will need to escape the  symbol so that it is sent to aws-encryption-cli   rather than interpreted by PowerShell.For example, if I wanted to use a common master key configuration for all of my calls, I couldcreate a file master-key.conf with contents detailing my master key configuration.master-key.conf.. code-block sh   --master-key keyA_KEY keyANOTHER_KEYThen, when calling aws-encryption-cli, I can specify the rest of my arguments and referencemy new configuration file, and aws-encryption-cli will use the composite configuration... code-block sh   aws-encryption-cli -e -i INPUT_FILE -o OUTPUT_FILE master-key.confTo extend the example, if I wanted a common caching configuration for all of my calls, I couldsimilarly place my caching configuration in a configuration file caching.conf in this exampleand include both files in my call.caching.conf.. code-block sh   --caching capacity10 max_age60.0 max_messages_encrypted15.. code-block sh   aws-encryption-cli -e -i INPUT_FILE -o OUTPUT_FILE master-key.conf caching.confConfiguration files can be referenced anywhere in aws-encryption-cli parameters... code-block sh   aws-encryption-cli -e -i INPUT_DIR -o OUTPUT_DIR master-key.conf caching.conf --recursiveConfiguration files can have many lines, include comments using . Escape characters areplatform-specific  on Linux and MacOS and  on Windows. Configuration files mayalso include references to other configuration files.my-encrypt.config.. code-block sh   --encrypt   master-key.conf  Use existing master key config   caching.conf    Always recurse, but require interactive overwrite.   --recursive   --interactive.. code-block sh   aws-encryption-cli my-encrypt -i INPUT -o OUTPUTEncoding--------By default, aws-encryption-cli will always output raw binary data and expect raw binary dataas input. However, there are some cases where you might not want this to be the case.Sometimes this might be for convenience Accepting ciphertext through stdin from a human. Presenting ciphertext through stdout to a human.Sometimes it might be out of necessity Saving ciphertext output to a shell variable.    Most shells apply a system encoding to any data stored in a variable. As a result, this     often results in corrupted data if binary data is stored without additional encoding. Piping ciphertext in PowerShell.    Similar to the above, all data passed through a PowerShell pipe is encoded using the     system encoding.In order to address these scenarios, we provide two optional arguments --decode  Base64-decode input before processing. --encode  Base64-encode output after processing.These can be used independently or together, on any valid input or output.Be aware, however, that if you target multiple files either through a path expansion or bytargetting a directory, the requested decodingencoding will be applied to all files... _AWS Encryption SDK httpsdocs.aws.amazon.comencryption-sdklatestdeveloper-guideintroduction.html.. _message header data httpdocs.aws.amazon.comencryption-sdklatestdeveloper-guidemessage-format.htmlheader-structure.. _message header authentication data httpdocs.aws.amazon.comencryption-sdklatestdeveloper-guidemessage-format.htmlheader-authentication.. _Read the Docs httpaws-encryption-sdk-cli.readthedocs.ioenlatest.. _GitHub httpsgithub.comawsaws-encryption-sdk-cli.. _cryptography httpscryptography.ioenlatest.. _cryptography installation guide httpscryptography.ioenlatestinstallation.. _data key caching documentation httpdocs.aws.amazon.comencryption-sdklatestdeveloper-guidedata-key-caching.html.. _encryption context httpdocs.aws.amazon.comencryption-sdklatestdeveloper-guideconcepts.htmlencryption-context.. _KMSMasterKeyProvider httpaws-encryption-sdk-python.readthedocs.ioenlatestgeneratedaws_encryption_sdk.key_providers.kms.htmlaws_encryption_sdk.key_providers.kms.KMSMasterKeyProvider.. _argparse file support httpsdocs.python.org3libraryargparse.htmlfromfile-prefix-chars.. _named profile httpdocs.aws.amazon.comclilatestuserguidecli-multiple-profiles.html.. _setuptools entry point httpsetuptools.readthedocs.ioenlatestsetuptools.htmldynamic-discovery-of-services-and-plugins.. _you must specify either a key or discovery with a value of true httpsdocs.aws.amazon.comencryption-sdklatestdeveloper-guidecrypto-cli-how-to.htmlcrypto-cli-master-key.. _Security issue notifications httpsgithub.comawsaws-encryption-sdk-clitreemasterCONTRIBUTING.mdsecurity-issue-notifications.. _Support Policy .SUPPORT_POLICY.rst.. _key commitment httpsdocs.aws.amazon.comencryption-sdklatestdeveloper-guideconcepts.htmlkey-commitment.. _Commitment Policy httpsdocs.aws.amazon.comencryption-sdklatestdeveloper-guidemigrate-commitment-policy.html.. _Python.org Virtual Environment Documentation httpspackaging.python.orgguidesinstalling-using-pip-and-virtual-environments</longdescription>
</pkgmetadata>