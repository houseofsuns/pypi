<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> RethinkDB Python driver[[PyPI version](httpsbadge.fury.iopyrethinkdb.svg)](httpsbadge.fury.iopyrethinkdb) [[Build Status](httpstravis-ci.orgrethinkdbrethinkdb-python.svgbranchmaster)](httpstravis-ci.orgrethinkdbrethinkdb-python) [[Codacy Badge](httpsapi.codacy.comprojectbadgeGrade2b5231a6f90a4a1ba2fc795f8466bbe4)](httpswww.codacy.comapprethinkdbrethinkdb-pythonutm_sourcegithub.comamputm_mediumreferralamputm_contentrethinkdbrethinkdb-pythonamputm_campaignBadge_Grade) [[Codacy Badge](httpsapi.codacy.comprojectbadgeCoverage2b5231a6f90a4a1ba2fc795f8466bbe4)](httpswww.codacy.comapprethinkdbrethinkdb-pythonutm_sourcegithub.comutm_mediumreferralutm_contentrethinkdbrethinkdb-pythonutm_campaignBadge_Coverage) Overview What is RethinkDBRethinkDB is the first open-source scalable database built for realtime applications. It exposes a new database access model -- instead of polling for changes, the developer can tell the database to continuously push updated query results to applications in realtime. RethinkDB allows developers to build scalable realtime apps in a fraction of the time with less effort. Installationbash pip install rethinkdbNote this package is the extracted driver of RethinkDBs original python driver. QuickstartThe main difference with the previous driver (except the name of the package) is we are not importing RethinkDB as r. If you would like to use RethinkDBs python driver as a drop in replacement, you should do the followingpythonfrom rethinkdb import rconnection  r.connect(dbtest) Blocking and Non-blocking IOThis driver supports blocking IO (i.e. standard Python sockets) as well asnon-blocking IO through multiple async frameworks [Asyncio](httpsdocs.python.org3libraryasyncio.html) [Gevent](httpwww.gevent.org) [Tornado](httpswww.tornadoweb.orgenstable) [Trio](httpstrio.readthedocs.ioenlatest) [Twisted](httpstwistedmatrix.comtrac)The following examples demonstrate how to use the driver in each mode. Default mode (blocking IO)The drivers default mode of operation is to use blocking IO, i.e. standard Pythonsockets. This example shows how to create a table, populate with data, and get everydocument.pythonfrom rethinkdb import rconnection  r.connect(dbtest)r.table_create(marvel).run(connection)marvel_heroes  r.table(marvel)marvel_heroes.insert({    id 1,    name Iron Man,    first_appearance Tales of Suspense 39}).run(connection)for hero in marvel_heroes.run(connection)    print(hero[name]) Asyncio modeAsyncio mode is compatible with Python  3.4, which is when asyncio wasintroduced into the standard library.pythonimport asynciofrom rethinkdb import r Native coroutines are supported in Python  3.5. In Python 3.4, you should use the asyncio.couroutine decorator instead of async def, and yield from instead of await.async def main()    r.set_loop_type(asyncio)    connection  await r.connect(dbtest)    await r.table_create(marvel).run(connection)    marvel_heroes  r.table(marvel)    await marvel_heroes.insert({        id 1,        name Iron Man,        first_appearance Tales of Suspense 39    }).run(connection)     async for is supported in Python  3.6. In earlier versions, you should     call await cursor.next() in a loop.    cursor  await marvel_heroes.run(connection)    async for hero in cursor        print(hero[name])asyncio.get_event_loop().run_until_complete(main()) Gevent modepythonimport geventfrom rethinkdb import rdef main()    r.set_loop_type(gevent)    connection  r.connect(dbtest)    r.table_create(marvel).run(connection)    marvel_heroes  r.table(marvel)    marvel_heroes.insert({        id 1,        name Iron Man,        first_appearance Tales of Suspense 39    }).run(connection)    for hero in marvel_heroes.run(connection)        print(hero[name])gevent.joinall([gevent.spawn(main)]) Tornado modeTornado mode is compatible with Tornado  5.0.0. Tornado 5 is not supported.pythonfrom rethinkdb import rfrom tornado import genfrom tornado.ioloop import IOLoopgen.coroutinedef main()    r.set_loop_type(tornado)    connection  yield r.connect(dbtest)    yield r.table_create(marvel).run(connection)    marvel_heroes  r.table(marvel)    yield marvel_heroes.insert({        id 1,        name Iron Man,        first_appearance Tales of Suspense 39    }).run(connection)    cursor  yield marvel_heroes.run(connection)    while (yield cursor.fetch_next())        hero  yield cursor.next()        print(hero[name])IOLoop.current().run_sync(main) Trio modepythonfrom rethinkdb import rimport trioasync def main()    r.set_loop_type(trio)    async with trio.open_nursery() as nursery        async with r.open(dbtest, nurserynursery) as conn            await r.table_create(marvel).run(conn)            marvel_heroes  r.table(marvel)            await marvel_heroes.insert({                id 1,                name Iron Man,                first_appearance Tales of Suspense 39            }).run(conn)             async for is supported in Python  3.6. In earlier versions, you should             call await cursor.next() in a loop.            cursor  await marvel_heroes.run(conn)            async with cursor                async for hero in cursor                    print(hero[name])trio.run(main)The Trio mode also supports a database connection pool. You can modify the example aboveas followspythondb_pool  r.ConnectionPool(dbtest, nurserynursery)async with db_pool.connection() as conn    ...await db_pool.close() Twisted modepythonfrom rethinkdb import rfrom twisted.internet import reactor, deferdefer.inlineCallbacksdef main()    r.set_loop_type(twisted)    connection  yield r.connect(dbtest)    yield r.table_create(marvel).run(connection)    marvel_heroes  r.table(marvel)    yield marvel_heroes.insert({        id 1,        name Iron Man,        first_appearance Tales of Suspense 39    }).run(connection)    cursor  yield marvel_heroes.run(connection)    while (yield cursor.fetch_next())        hero  yield cursor.next()        print(hero[name])main().addCallback(lambda d print(stopping) or reactor.stop())reactor.run() MiscTo help the migration from rethinkdb2.4 we introduced a shortcut which can easily replace the old import rethinkdb as r import with from rethinkdb import r. Run testsIn the Makefile you can find three different test commands test-unit, test-integration and test-remote. As RethinkDB has dropped the support of Windows, we would like to ensure that those of us who are using Windows for development can still contribute. Because of this, we support running integration tests against Digital Ocean Droplets as well.Before you run any test, make sure that you install the requirements.bash pip install -r requirements.txt make prepare Running unit testsbash make test-unit Running integration testsTo run integration tests locally, make sure you intstalled RethinkDBbash make test-integration Running remote integration testsTo run the remote tests, you need to have a Digital Ocean account and an API key.Remote test will create a new temporary SSH key and a Droplet for you until the tests are finished.Available environment variables Variable name  Default value ------------------------------ DO_TOKEN       NA            DO_SIZE        512MB          DO_REGION      sfo2          bash pip install paramiko python-digitalocean export DO_TOKENYOUR_TOKEN make test-remote New featuresGithubs Issue tracker is ONLY used for reporting bugs. NO NEW FEATURE ACCEPTED Use [spectrum](httpsspectrum.chatrethinkdb) for supporting features. ContributingHurray You reached this section which means, that you would like to contribute. Please read our contributing guide lines and feel free to open a pull request.</longdescription>
</pkgmetadata>