<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>ground======[![](https://github.com/lycantropos/ground/workflows/CI/badge.svg)](https://github.com/lycantropos/ground/actions/workflows/ci.yml &quot;Github Actions&quot;)[![](https://readthedocs.org/projects/ground/badge/?version=latest)](https://ground.readthedocs.io/en/latest &quot;Documentation&quot;)[![](https://codecov.io/gh/lycantropos/ground/branch/master/graph/badge.svg)](https://codecov.io/gh/lycantropos/ground &quot;Codecov&quot;)[![](https://img.shields.io/github/license/lycantropos/ground.svg)](https://github.com/lycantropos/ground/blob/master/LICENSE &quot;License&quot;)[![](https://badge.fury.io/py/ground.svg)](https://badge.fury.io/py/ground &quot;PyPI&quot;)Summary-------`ground` is a pure Python library that provides protocolsfor planar computational geometry models &amp; operationsto unify interaction between different librariesand allow customization.---In what follows `python` is an alias for `python3.7` or `pypy3.7`or any later version (`python3.8`, `pypy3.8` and so on).Installation------------Install the latest `pip` &amp; `setuptools` packages versions```bashpython -m pip install --upgrade pip setuptools```### UserDownload and install the latest stable version from `PyPI` repository```bashpython -m pip install --upgrade ground```### DeveloperDownload the latest version from `GitHub` repository```bashgit clone https://github.com/lycantropos/ground.gitcd ground```Install dependencies```bashpython -m pip install -r requirements.txt```Install```bashpython setup.py install```Usage-----```python&gt;&gt;&gt; from ground.base import get_context&gt;&gt;&gt; context = get_context()&gt;&gt;&gt; Multipoint = context.multipoint_cls&gt;&gt;&gt; Point = context.point_cls&gt;&gt;&gt; Segment = context.segment_cls&gt;&gt;&gt; origin = Point(0, 0)&gt;&gt;&gt; x_unit = Point(1, 0)&gt;&gt;&gt; y_unit = Point(0, 1)&gt;&gt;&gt; from ground.base import Kind&gt;&gt;&gt; context.angle_kind(origin, x_unit, y_unit) is Kind.RIGHTTrue&gt;&gt;&gt; from ground.base import Orientation&gt;&gt;&gt; (context.angle_orientation(origin, x_unit, y_unit)...  is Orientation.COUNTERCLOCKWISE)True&gt;&gt;&gt; context.cross_product(origin, x_unit, origin, y_unit) == 1True&gt;&gt;&gt; context.dot_product(origin, x_unit, origin, y_unit) == 0True&gt;&gt;&gt; context.multipoint_centroid(Multipoint([origin])) == originTrue&gt;&gt;&gt; (context.points_convex_hull([origin, x_unit, y_unit])...  == [origin, x_unit, y_unit])True&gt;&gt;&gt; context.segment_contains_point(Segment(origin, x_unit), y_unit)False&gt;&gt;&gt; context.segment_contains_point(Segment(origin, x_unit), origin)True&gt;&gt;&gt; context.segments_intersection(Segment(origin, x_unit),...                               Segment(origin, y_unit)) == originTrue&gt;&gt;&gt; from ground.base import Relation&gt;&gt;&gt; context.segments_relation(Segment(origin, x_unit),...                           Segment(origin, y_unit)) is Relation.TOUCHTrue```Development-----------### Bumping version#### PreparationInstall[bump2version](https://github.com/c4urself/bump2version#installation).#### Pre-releaseChoose which version number category to bump following [semverspecification](http://semver.org/).Test bumping version```bashbump2version --dry-run --verbose $CATEGORY```where `$CATEGORY` is the target version number category name, possiblevalues are `patch`/`minor`/`major`.Bump version```bashbump2version --verbose $CATEGORY```This will set version to `major.minor.patch-alpha`. #### ReleaseTest bumping version```bashbump2version --dry-run --verbose release```Bump version```bashbump2version --verbose release```This will set version to `major.minor.patch`.### Running testsInstall dependencies```bashpython -m pip install -r requirements-tests.txt```Plain```bashpytest```Inside `Docker` container:- with `CPython`  ```bash  docker-compose --file docker-compose.cpython.yml up  ```- with `PyPy`  ```bash  docker-compose --file docker-compose.pypy.yml up  ````Bash` script:- with `CPython`  ```bash  ./run-tests.sh  ```  or  ```bash  ./run-tests.sh cpython  ```- with `PyPy`  ```bash  ./run-tests.sh pypy  ````PowerShell` script:- with `CPython`  ```powershell  .\run-tests.ps1  ```  or  ```powershell  .\run-tests.ps1 cpython  ```- with `PyPy`  ```powershell  .\run-tests.ps1 pypy  ```</longdescription>
</pkgmetadata>