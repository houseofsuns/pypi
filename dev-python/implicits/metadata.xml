<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> ImplicitsGlobal state can be hard to reason about, but piping dependencies from function to function is a pain. With implicits, you _explicitly_ which function parameters are _implicit_ dependencies. When you call the function, no need to explicilty provide these parameters instead, the parameters will be implicitly passed All thats required is that there exists local variables in scope that match the names of the parameters youre calling.python3implicits(current_user)def create_task(title, , current_user)    print(f{current_user} created a task titled {title})current_user  Jadencreate_task(Hooray, a task)  Jaden created a task titled Hooray, a taskcreate_task(Buy some trackpants)  Jaden created a task titled Buy some trackpants Usage1) Install via pip install implicits.2) Import with from implicits import implicits.3) Decorate using implicits(names, of, implicit, parameters). Examplepython3import loggingimport boto3from implicits import implicitsclass Giraffe    implicits(logger)    def __init__(self, name, , logger)        self.name  name        logger.info(fCreating a Giraffe named {name})    property    implicits(logger)    def full_name(self, , logger)        logger.info(fGetting {self.name}s full name)        return f{self.name} the Giraffe    property    implicits(food)    def is_hungry(self, , food)        return leaves in foodimplicits(logger)def main(, logger)    jeff  Giraffe(Jeff)  Creating a Giraffe named Jeff    name  jeff.full_name   Getting Jeffs full name    food  [rocks, dirt]    logger.info(jeff.is_hungry)  False    food.append(leaves)    logger.info(jeff.is_hungry)  Truelogger  logging.getLogger()logger.setLevel(logging.INFO)logger.addHandler(logging.StreamHandler())main() ReferencesI didnt invent this idea Quite a few other languages support implicit parameters. The most mainstream of these languages is Scala. [Check out how implicits work in Scala](httpsdocs.scala-lang.orgtourimplicit-parameters.html)</longdescription>
</pkgmetadata>