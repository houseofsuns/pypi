<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>urllib3-mockA utility library for mocking out the urllib3_ Python library.This is an adaptation of the responses_ library... image httpstravis-ci.orgflorentxurllib3-mock.pngbranchmastertarget httpstravis-ci.orgflorentxurllib3-mock.. _urllib3 httpsurllib3.readthedocs.org.. _responses httpsgithub.comgetsentryresponsesResponse body as string-----------------------.. code-block python    from urllib3_mock import Responses    import requests    responses  Responses(requests.packages.urllib3)    responses.activate    def test_my_api()        responses.add(GET, api1foobar,                      body{error not found}, status404,                      content_typeapplicationjson)        resp  requests.get(httptwitter.comapi1foobar)        assert resp.json()  {error not found}        assert len(responses.calls)  1        assert responses.calls[0].request.url  api1foobar        assert responses.calls[0].request.host  twitter.com        assert responses.calls[0].request.scheme  httpRequest callback----------------.. code-block python    import json    from urllib3_mock import Responses    import requests    responses  Responses(requests.packages.urllib3)    responses.activate    def test_calc_api()        def request_callback(request)            payload  json.loads(request.body)            resp_body  {value sum(payload[numbers])}            headers  {request-id 728d329e-0e86-11e4-a748-0c84dc037c13}            return (200, headers, json.dumps(resp_body))        responses.add_callback(POST, sum,                               callbackrequest_callback,                               content_typeapplicationjson)        resp  requests.post(            httpcalc.comsum,            json.dumps({numbers [1, 2, 3]}),            headers{content-type applicationjson},        )        assert resp.json()  {value 6}        assert len(responses.calls)  1        assert responses.calls[0].request.url  sum        assert responses.calls[0].request.host  calc.com        assert (            responses.calls[0].response.headers[request-id]             728d329e-0e86-11e4-a748-0c84dc037c13        )Instead of passing a string URL into responses.add or responses.add_callbackyou can also supply a compiled regular expression... code-block python    import re    from urllib3_mock import Responses    import requests    responses  Responses(requests.packages.urllib3)     Instead of    responses.add(GET, api1foobar,                  body{error not found}, status404,                  content_typeapplicationjson)     You can do the following    url_re  re.compile(rapid+foobar)    responses.add(GET, url_re,                  body{error not found}, status404,                  content_typeapplicationjson)A response can also throw an exception as follows... code-block python    from urllib3_mock import Responses    from requests.packages.urllib3.exceptions import HTTPError    exception  HTTPError(Something went wrong)    responses  Responses(requests.packages.urllib3)    responses.add(GET, api1foobar,                  bodyexception)     All calls to httptwitter.comapi1foobar will throw exception.</longdescription>
</pkgmetadata>