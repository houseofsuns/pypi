<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Poe the PoetA task runner that works well with poetry... role sh(code)   language sh.. role bash(code)   language bash.. role fish(code)   language fish.. role zsh(code)   language zsh.. role toml(code)   language toml.. role python(code)   language python..  unicode  0xA0 0xA0   trimFeatures Straight forward declaration of project tasks in your pyproject.toml (kind of like npm scripts) Task are run in poetrys virtualenv (or another env you specify) Shell completion of task names (and global options too for zsh) Can be used standalone or as a poetry plugin Tasks can be commands (with or without a shell) or references to python functions (like tool.poetry.scripts) Short and sweet commands with extra arguments passed to the task bashpoe [options] task [task_args], or you can define arguments explicitly. Tasks can specify and reference environment variables as if they were evaluated by a shell Tasks are self documenting, with optional help messages (just run poe without arguments) Tasks can be defined as a sequence of other tasks Works with .env filesInstallation1.  Install the CLI  .. code-block bash    pipx install poethepoet  Or use pip to install into any python environment  .. code-block bash    pip install poethepoet2.  Or into your project (so it works inside poetry shell)  .. code-block bash    poetry add --group dev poethepoet3.  Or into poetry as a plugin (requires poetry  1.2)  .. code-block bash    poetry self add poethepoet[poetry_plugin]Enable tab completion for your shell------------------------------------Poe comes with tab completion scripts for bash, zsh, and fish to save you keystrokes.How to install them will depend on your shell setup.Zsh.. code-block zsh   oh-my-zsh  mkdir -p .oh-my-zshcompletions  poe _zsh_completion  .oh-my-zshcompletions_poe   without oh-my-zsh  mkdir -p .zfunc  poe _zsh_completion  .zfunc_poetryNote that youll need to start a new shell for the new completion script to be loaded.If it still doesnt work try adding a call to bashcompinit to the end of your zshrcfile.Bash.. code-block bash   System bash  poe _bash_completion  etcbash_completion.dpoe.bash-completion   Homebrew bash  poe _bash_completion  (brew --prefix)etcbash_completion.dpoe.bash-completionHow to ensure installed bash completions are enabled may vary depending on your system.Fish.. code-block fish   Fish  poe _fish_completion  .configfishcompletionspoe.fish   Homebrew fish  poe _fish_completion  (brew --prefix)sharefishvendor_completions.dpoe.fishBasic UsageDefine tasks in your pyproject.toml-----------------------------------See a real example httpsgithub.comnat-npoethepoetblobmasterpyproject.toml_.. code-block toml  [tool.poe.tasks]  test    pytest --covpoethepoet                                 simple command based task  serve   { script  my_app.servicerun(debugTrue) }             python script based task  tunnel  { shell  ssh -N -L 0.0.0.08080PROD8080 PROD  }   (posix) shell based taskRun tasks with the poe cli--------------------------.. code-block bash  poe testBy default additional arguments are passed to the task so.. code-block bash  poe test -v testsfavorite_test.pyresults in the following being run inside poetrys virtualenv.. code-block bash  pytest --covpoethepoet -v testsfavorite_test.pyYou can also run it like so if you fancy.. code-block bash  python -m poethepoet [options] task [task_args]Or use it as a poetry plugin (for poetry  1.2) like so.. code-block bash  poetry self add poethepoet[poetry_plugin]  poetry poe [options] task [task_args]Or just install it as a dev dependency with poetry and run it like.. code-block bash  poetry add --group dev poethepoet  poetry run poe [options] task [task_args]Though in that case you might like to define bashalias poepoetry run poe.Types of taskThere are seven types of task- Command tasks (cmd) for simple commands that are executed as a subprocess without a shell- Script tasks (script) for python function calls- Shell tasks (shell) for scripts to be executed with via an external interpreter (such as sh).- Sequence tasks (sequence) for composing multiple tasks into a sequence- Expression tasks (expr) which consist of a python expression to evaluate- Switch tasks (switch) for running different tasks depending on a control value (such as the platform)- Ref tasks (ref) used for defining a task as an alias of another task, such as in a sequence task.The default task type is cmd.cmd tasks-----------Command tasks contain a single command that will be executed without a shell.This covers most basic use cases for example.. code-block toml  [tool.poe.tasks]  format  black .   strings are interpreted as commands by default  clean     Multiline commands including comments work too. Unescaped whitespace is ignored.  rm -rf .coverage         .mypy_cache         .pytest_cache         dist         .__pycache__    lint  { cmd pylint poethepoet }   Inline tables with a cmd key work too  greet  echo Hello USER   Environment variables work, even though theres no shellscript tasks--------------Script tasks consist of a reference to a python callable to import and execute, and optionally values or expressions to pass as arguments, for example.. code-block toml  [tool.poe.tasks]  fetch-assets  { script  my_package.assetsfetch }  fetch-images  { script  my_package.assetsfetch(onlyimages, logenviron[LOG_PATH]) }As in the second example, is it possible to hard code literal arguments to the targetcallable. In fact a subset of python syntax, operators, and globals can be used inlineto define the arguments to the function using normal python syntax, including environ(from the os package) to access environment variables that are available to the task.If extra arguments are passed to task on the command line (and no CLI args aredeclared), then they will be available within the called python function viapythonsys.argv.Calling standard library functionsAny python callable accessible via the python path can be referenced, including thestandard library. This can be useful for ensuring that tasks work across platforms.For example, the following task will not always work on windows.. code-block toml  [[tool.poe.tasks.build]]  cmd  mkdir -p buildassetswhereas the same behaviour can can be reliably achieved like so.. code-block toml  [[tool.poe.tasks.build]]  script  osmakedirs(buildassets, exist_okTrue)Output the return value from the python callableScript tasks can be configured to output the return value of a callable using thetomlprint_result option like so.. code-block toml  [tool.poe.tasks.create-secret]  script  django.core.management.utilsget_random_secret_key()  print_result  trueGiven the above configuration running the following command would output just thegenerated key... code-block bash  poe -q create-secretNote that if the return value is None then the tomlprint_result option has noeffect.shell tasks-------------Shell tasks are similar to simple command tasks except that they are executedinside a new shell, and can consist of multiple statements. This means they can leveragethe full syntax of the shell interpreter such as command substitution, pipes, backgroundprocesses, etc.An example use case for this might be opening some ssh tunnels in the background withone task and closing them with another like so.. code-block toml  [tool.poe.tasks]  pfwd  { shell  ssh -N -L 0.0.0.08080STAGING8080 STAGING  ssh -N -L 0.0.0.05432STAGINGDB5432 STAGINGDB  }  pfwdstop  { shell  kill (pgrep -f ssh -N -L .(80805432)) }By default poe attempts to find a posix shell (sh, bash, or zsh in that order) on thesystem and uses that. When running on windows, poe will first look forGit bash httpsgitforwindows.org_ at the usual location, and otherwise attempt tofind it via the PATH, though this might not always be possible.Using different types of shellinterpreterIt is also possible to specify an alternative interpreter (or list of compatibleinterpreters ordered by preference) to be invoked to execute shell task content. Forexample if you only expect the task to be executed on windows or other environmentswith powershell installed then you can specify a powershell based task like so.. code-block toml  [tool.poe.tasks.install-poetry]  shell    (Invoke-WebRequest -Uri httpsraw.githubusercontent.compython-poetrypoetrymasterget-poetry.py -UseBasicParsing).Content  python -    interpreter  pwshIf your task content is restricted to syntax that is valid for both posix shells andpowershell then you can maximise the likelihood of it working on any system byspecifying the interpreter as.. code-block toml  interpreter  [posix, pwsh]It is also possible to specify python code as the shell task code as in the followingexample. However it is recommended to use a script task rather than writing complexcode inline within your pyproject.toml... code-block toml  [tool.poe.tasks.time]  shell    from datetime import datetime  print(datetime.now())    interpreter  pythonThe following interpreter values may be usedposix    This is the default behavoir, equivalent to [sh, bash, zsh], meaning that    poe will try to find sh, and fallback to bash, then zsh.sh    Use the basic posix shell. This is often an alias for bash or dash depending on    the operating system.bash    Uses whatever version of bash can be found. This is usually the most portable option.zsh    Uses whatever version of zsh can be found.fish    Uses whatever version of fish can be found.pwsh    Uses powershell version 6 or higher.powershell    Uses the newest version of powershell that can be found.The default value can be changed with the global shell_interpreter option asdescribed below.sequence tasks----------------Sequence tasks are defined as a array of other tasks to be run one after the other.By default the contents of the array are interpreted as references to other tasks(actually a ref task type), though this behaviour can be altered by setting the globaltomldefault_array_item_task_type option to the name of another task type such ascmd, or by setting the tomldefault_item_type option locally on the sequence task.Sequence task with references.. code-block toml  [tool.poe.tasks]  test  pytest --covsrc  build  poetry build  _publish  poetry publish  release  [test, build, _publish]Note that tasks with names prefixed with code_ are not included in thedocumentation or directly executable, but can be useful for cases where a task is onlyneeded for referencing from another task.Sequence task with inline tasks expressed via inline tables.. code-block toml  release  [    { cmd  pytest --covsrc },    { script  devtasksbuild },    { ref  _publish },  ]Sequence task with inline tasks expressed via an array of tables.. code-block toml  [tool.poe.tasks]    [[tool.poe.tasks.release]]    cmd  pytest --covsrc    [[tool.poe.tasks.release]]    script  devtasksbuild    [[tool.poe.tasks.release]]    ref  _publishSequence task with inline script subtasks using default_item_type.. code-block toml  release.sequence  [    devtasksrun_tests(allTrue),    devtasksbuild,    devtaskspublish,  ]  release.default_item_type  scriptA failure (non-zero result) will result in the rest of the tasks in the sequence notbeing executed, unless the tomlignore_fail option is set on the task totomltrue or tomlreturn_zero like so.. code-block toml  [tool.poe.tasks]  attempts.sequence  [task1, task2, task3]  attempts.ignore_fail  return_zeroIf you want to run all the subtasks in the sequence but return non-zero result in theend of the sequence if any of the subtasks have failed you can set tomlignore_failoption to the tomlreturn_non_zero value like so.. code-block toml  [tool.poe.tasks]  attempts.sequence  [task1, task2, task3]  attempts.ignore_fail  return_non_zeroexpr tasks------------Expr tasks consist of a single python expression httpsdocs.python.org3referenceexpressions.html_. Running the task evaluates the expression and outputs the resultingvalue. Heres a trivial example of an expr task that will print 2 when run.. code-block toml  [tool.poe.tasks.trivial-example]  expr  1 + 1.. code-block bash   poe trivial-example  Poe  1 + 1  2Expressions can- use most python expression constructs with the exception of yield, await, or named  expressions- use most builtin functions including all members of  this collection httpsgithub.comnat-npoethepoetblobmainpoethepoethelperspython.pyL13_- reference the sys module without having to specify it as an import- reference sys.argv to get whatever arguments were passed to the task, just like in  script tasks- referene values of named args as python variables- include environment variables as string values that are injected into the expression  using the usual templating syntaxReferencing arguments and environment variablesThe expression can reference environment variables using templating syntax like in cmdtasks, and named arguments as python variables in scope like in script tasks... code-block toml  [tool.poe.tasks.venv-active]  expr  (    f{target_venv} is active    if {VIRTUAL_ENV}.endswith(target_venv)    else f{target_venv} is not active  )  args  [{ name  target-venv, default  .venv, positional  true }].. code-block   poe venv-active poethepoet-LCpCQf8S-py3.10  Poe  (    f{target_venv} is active    if {VIRTUAL_ENV}.endswith(target_venv)    else f{target_venv} is not active  )  poethepoet-LCpCQf8S-py3.10 is not activeIn this example the codeVIRTUAL_ENV environment variable is templated into theexpression using the usual templating syntax, and the codetarget_venv argument isreferenced directly as a variable.Notice that the expression may be formatted over multiple lines, as in normal pythoncode.Referencing imported modules in an expressionBy default the sys module is available to the expression which allows access to sys.argvor sys.platform amoung other useful values. However you can also reference any otherimportable module via the imports option as in the following example... code-block toml  [tool.poe.tasks.count-hidden]  help     Count hidden files or subdirectories  expr     len(list(pathlib.Path(.).glob(.)))  imports  [pathlib]Fail if the expression result is falseyThe expression can be made to behave like an assertion that fails if the result is not truthy by providing the assert option. The task defined in the following example willreturn non-zero if the result is False... code-block toml  [tool.poe.tasks.venv-active]  expr    {VIRTUAL_ENV}.endswith(target_venv)  assert  true  args    [{ name  target-venv, default  .venv, positional  true }]Referencing the result of other tasks in an expressionExpr tasks can reference the results of other tasks by leveraging the codeusesoption... code-block toml  [tool.poe.tasks._get_active_session]  cmd  read_session --format json  [tool.poe.tasks.show-user]  expr     (    fUser {json.loads({SESSION_JSON})[User]}    if len({SESSION_JSON})  2    else No active session.  )  uses     { SESSION_JSON  _get_active_session }  imports  [json]switch tasks--------------Much like a switch statement in many programming languages, a switch task consists of acontrol task and a array of tasks to switch between. The control task is run first, andits output is captured and matched against the case option of each of the items in theswitch array to determine which one to run.This can be used to define a task that runs a different subtask depending on whichplatform it is running on like so.. code-block toml  [tool.poe.tasks.build]  control.expr  sys.platform    [[tool.poe.tasks.platform_dependent.switch]]    case  win32    cmd   windows_build    [[tool.poe.tasks.platform_dependent.switch]]    cmd   posix_buildIn the above example the control task checks the value of sys.platform, and if runningon windows itll execute tomlwindows_build, otherwise itll fall back to the defaultcase (i.e. the switch item with no case option defined) and execute tomlposix_build.Multiple values per caseIt is also possible to define multiple values for a single case option by providing aarray of values like so.. code-block toml    [[tool.poe.tasks.platform_dependent.switch]]    case  [linux, darwin]    cmd   buildDont fail if theres no matchIf all tasks in the switch array include a case value, but none of them match the resultof the control task then by default the switch task will fail. You can instead configurethe switch task to pass and simply do nothing by providing the default option like so.. code-block toml  [tool.poe.tasks.build_on_windows]  control.expr  sys.platform  default  pass    [[tool.poe.tasks.platform_dependent.switch]]    case  win32    cmd   buildSwitching on an environment variable or named argumentIt is possible to run a different task depending on the value of an environment variableas in the following example... code-block toml  [tool.poe.tasks.check_number]  control.expr  int({BEST_NUMBER})  2    [[tool.poe.tasks.check_number.switch]]    case  0    expr  f{{BEST_NUMBER}} is even)    [[tool.poe.tasks.check_number.switch]]    case  1    expr  f{{BEST_NUMBER}} is oddUsing this task will look like the following.. code-block sh   BEST_NUMBER12 poe check_number  Poe  int({BEST_NUMBER})  2  Poe  f{{BEST_NUMBER}} is even)  12 is even   BEST_NUMBER17 poe check_number  Poe  int({BEST_NUMBER})  2  Poe  f{{BEST_NUMBER}} is odd  17 is oddYou can also run a different task depending on the value of a named argument as in the following example... code-block toml  [tool.poe.tasks.icecream]  control.expr  flavor  args  [flavor]    [[tool.poe.tasks.icecream.switch]]    case  chocolate    cmd   make_chocolate_icecream    [[tool.poe.tasks.icecream.switch]]    case  strawberry    cmd   make_strawberry_icecream    [[tool.poe.tasks.icecream.switch]]    cmd   make_vanilla_icecreamref tasks-----------A ref task is essentially a call to another task. It is the default task type within a sequence task, but is not often used otherwise.A ref task can set environment variables, and pass arguments to the referenced task asfollows.. code-block toml  [tool.poe.tasks]  do_things.cmd  do_cmd  do_things.args  [{ name  things, multiple  true, positional  true }]  do_specific_things.ref  do_things thing1 thing2  do_specific_things.env  { URGENCY  11 }In the above example calling.. code-block sh  poe do_specific_thingswould be equivalent to executing the following in the shell.. code-block sh  URGENCY11 do_cmd thing1 thing2Task level configurationTask help text--------------You can specify help text to be shown alongside the task name in the list of availabletasks (such as when executing poe with no arguments), by adding a help key like so.. code-block toml    [tool.poe.tasks]    style  {cmd  black . --check --diff, help  Check code style}Environment variables---------------------You can specify arbitrary environment variables to be set for a task by providing theenv key like so.. code-block toml    [tool.poe.tasks]    serve.script  myapprun    serve.env  { PORT  9001 }Notice this example uses deep keys which can be more convenient but arent as wellsupported by some older toml implementations.The above example can be modified to only set the PORT variable if it is not alreadyset by replacing the last line with the following.. code-block toml    serve.env.PORT.default  9001Loading env vars from an env fileYou can also specify one or more env files (with bash-like syntax) to load per task like so.. code-block bash     .env    STAGEdev    PASSWORD(.. code-block toml    [tool.poe.tasks]    serve.script   myapprun    serve.envfile  .envThe envfile option accepts the name (or relative path) to a single envfile as shownabove but can also by given a list of such paths like so.. code-block toml    serve.envfile  [.env, local.env]In this case the referenced files will be loaded in the given order.Defining env vars in terms of other env varsIt is also possible to reference existing env vars when defining a new env var for atask. This may be useful for aliasing or extending a variable already defined in thehost environment, globally in the config, or in a referenced envfile. In the followingexample the value from TF_VAR_service_port on the host environment is also madeavailable as FLASK_RUN_PORT within the task... code-block toml    [tool.poe.tasks.serve]    cmd  flask run    env  { FLASK_RUN_PORT  {TF_VAR_service_port} }Running a task with a specific working directoryBy default tasks are run from the project root  that is the parent directory of thepyproject.toml file. However if a task needs to be run in another directory within theproject then this can be accomplished by using the tomlcwd option like so.. code-block toml    [tool.poe.tasks.build-client]    cmd  npx ts-node -T .build.ts    cwd  .clientIn this example, the npx executable is executed inside the sh.client subdirectory ofthe project, and will use the nodejs package.json configuration from that location andevaluate paths relative to that location.Defining tasks that run via exec instead of a subprocessNormally tasks are executed as subprocesses of the poe cli. This makes it possible forpoe to run multiple tasks, for example within a sequence task or task graph.However in certain situations it can be desirable to define a task that is insteadexecuted within the same process via exec. Cmd and script tasks can be configured towork this way using the tomluse_exec option like so.. code-block toml    [tool.poe.tasks.serve]    cmd       gunicorn .my_apprun    use_exec  trueNote the following limitations with this feature- a task configured in this way may not be referenced by another task- this does not work on windows becuase of this issue httpsbugs.python.orgissue19066_. On windows a subprocess is always created.Declaring CLI arguments-----------------------By default extra arguments passed to the poe CLI following the task name are appended tothe end of a cmd task, or exposed as sys.argv in a script task (but will cause an errorfor shell or sequence tasks). Alternatively it is possible to define named argumentsthat a task should accept, which will be documented in the help for that task, andexposed to the task in a way the makes the most sense for that task type.In general named arguments can take one of the following three forms- positional arguments which are provided directly following the name of the task like   bashpoe task-name arg-value- option arguments which are provided like   bashpoe task-name --option-name arg-value- flags which are either provided or not, but dont accept a value like   bashpoe task-name --flagThe value for the named argument is then accessible by name within the task content,though exactly how will depend on the type of the task as detailed below.Configuration syntaxNamed arguments are configured by declaring the args task option as either an array ora subtable.Array configuration syntaxThe array form may contain string items which are interpreted as an option argument withthe given name... code-block toml    [tool.poe.tasks.serve]    cmd  myapprun    args  [host, port]This example can be invoked as.. code-block bash    poe serve --host 0.0.0.0 --port 8001Items in the array can also be inline tables to allow for more configuration to beprovided to the task like so.. code-block toml    [tool.poe.tasks.serve]    cmd  myapprun    args  [{ name  host, default  localhost }, { name  port, default  9000 }]You can also use the toml syntax for an array of tables like so.. code-block toml    [tool.poe.tasks.serve]    cmd  myapprun    help  Run the application server      [[tool.poe.tasks.serve.args]]      name  host      options  [-h, --host]      help  The host on which to expose the service      default  localhost      [[tool.poe.tasks.serve.args]]      name  port      options  [-p, --port]      help  The port on which to expose the service      default  8000Table configuration syntaxYou can also use the toml syntax for subtables like so.. code-block toml    [tool.poe.tasks.serve]    cmd  myapprun    help  Run the application server      [tool.poe.tasks.serve.args.host]      options  [-h, --host]      help  The host on which to expose the service      default  localhost      [tool.poe.tasks.serve.args.port]      options  [-p, --port]      help  The port on which to expose the service      default  8000When using this form the name option is no longer applicable because the key for theargument within the args table is taken as the name.Task argument optionsNamed arguments support the following configuration options- default  Union[str, int, float, bool]   The value to use if the argument is not provided. This option has no significance if   the required option is set to true.   For string values, environment variables can be referenced using the usual templating   syntax as in the following example.   .. code-block toml    [[tool.poe.tasks.deploy.args]]    name     region    help     The region to deploy to    default  {AWS_REGION}   This can be combined with setting an env values on the task with the default   specifier to get the following precendence of values for the arg   1. the value passed on the command line   2. the value of the variable set on the environment   3. the default value for the environment variable configured on the task- help  str   A short description of the argument to include in the documentation of the task.- name  str   The name of the task. Only applicable when args is an array.- options  List[str]   A list of options to accept for this argument, similar to   argsparse name or flags httpsdocs.python.org3libraryargparse.htmlname-or-flags_.   If not provided then the name of the argument is used. You can use this option to   expose a different name to the CLI vs the name that is used inside the task, or to   specify long and short forms of the CLI option, e.g. [-h, --help].- positional  bool   If set to true then the argument becomes a position argument instead of an option   argument. Note that positional arguments may not have type bool.- multiple  Union[bool, int]   If the multiple option is set to true on a positional or option argument then that   argument will accept multiple values.   If set to a number, then the argument will accept exactly that number of values.   For positional aguments, only the last positional argument may have the multiple   option set.   The multiple option is not compatible with arguments with type boolean since   these are interpreted as flags. However multiple ones or zeros can be passed to an   argument of type integer for similar effect.   The values provided to an argument with the multiple option set are available on   the environment as a string of whitespace separated values. For script tasks, the   values will be provided to your python function as a list of values. In a cmd task   the values can be passed as separate arugments to the task via templating as in the   following example.   .. code-block toml    [tool.poe.tasks.save]    cmd   echo {FILE_PATHS}    args  [{ name  FILE_PATHS, positional  true, multiple  true }]- required  bool   If true then not providing the argument will result in an error. Arguments are not   required by default.- type  str   The type that the provided value will be cast to. The set of acceptable options is   {string, float, integer, boolean}. If not provided then the default behaviour   is to keep values as strings. Setting the type to boolean makes the resulting   argument a flag that if provided will set the value to the boolean opposite of the   default value  i.e. true if no default value is given, or false if   tomldefault  true.Arguments for cmd and shell tasksFor cmd and shell tasks the values are exposed to the task as environment variables. Forexample given the following configuration.. code-block toml  [tool.poe.tasks.passby]  shell    echo hello planet  echo goodbye planet    help  Pass by a planet    [[tool.poe.tasks.passby.args]]    name  planet    help  Name of the planet to pass    default  earth    options  [-p, --planet]The resulting task can be run like.. code-block bash  poe passby --planet marsArguments for script tasksArguments can be defined for script tasks in the same way, but how they are exposed tothe underlying python function depends on how the script is defined.In the following example, since no parenthesis are included for the referenced function,all provided args will be passed to the function as kwargs.. code-block toml  [tool.poe.tasks]  build  { script  project.utilbuild, args  [dest, version] }You can also control exactly how values are passed to the python function asdemonstrated in the following example.. code-block toml  [tool.poe.tasks]  build  { script  project.utilbuild(dest, build_versionversion, verboseTrue), args  [dest, version]Arguments for sequence tasksArguments can be passed to the tasks referenced from a sequence task as in the followingexample... code-block toml  [tool.poe.tasks]  build  { script  utilbuild_app, args  [{ name  target, positional  true }] }  [tool.poe.tasks.check]  sequence  [build {target}, { script  utilrun_tests(environ[target]) }]  args  [target]This works by setting the argument values as environment variables for the subtasks,which can be read at runtime, but also referenced in the task definition asdemonstrated in the above example for a ref task and script task.Project-wide configuration optionsGlobal environment variables----------------------------You can configure environment variables to be set for all poe tasks in thepyproject.toml file by specifying tomltool.poe.env like so.. code-block toml  [tool.poe.env]  VAR1  FOO  VAR2  BAR BAR BLACK {FARM_ANIMAL}The example above also demonstrates how  as with env vars defined at the task level posix variable interpolation syntax may be used to define global env vars with referenceto variables already defined in the host environment or in a referenced env file.As with the task level option, you can indicated that a variable should only be set ifnot already set like so.. code-block toml  [tool.poe.env]  VAR1.default  FOOYou can also specify an env file (with bash-like syntax) to load for all tasks like so.. code-block bash     .env    STAGEdev    PASSWORD(.. code-block toml    [tool.poe]    envfile  .envThe envfile global option also accepts a list of env files.Default command verbosity-------------------------You can alter the verbosity level for poe commands by passing bash--quiet bash-q (which decreases verbosity) or bash--verbose  bash-v (whichincreases verbosity) on the CLI.If you want to change the default verbosity level for all commands, you can usethe tomltool.poe.verbose option in pyproject.toml like so.. code-block toml  [tool.poe]  verbosity  -1toml-1 is the quietest and toml1 is the most verbose. toml0 is thedefault.Note that the command line arguments are incremental bash-q subtracts onefrom the default verbosity, and bash-v adds one. So setting the defaultverbosity to toml-1 and passing bash-v -v on the command line isequivalent to setting the verbosity to toml0 and just passing bash-v.Run poe from anywhere---------------------By default poe will detect when youre inside a project with a pyproject.toml in theroot. However if you want to run it from elsewhere then that is supported by using thebash--root option to specify an alternate location for the toml file. The task willrun with the given location as the current working directory.In all cases the path to project root (where the pyproject.toml resides) will beavailable as bashPOE_ROOT within the command line and process.Change the default task type----------------------------By default tasks defined as strings are interpreted as shell commands, and script tasksrequire the more verbose table syntax to specify. For example.. code-block toml  my_cmd_task  cmd args  my_script_task  { script  my_package.my_modulerun }This behaviour can be reversed by setting the tomldefault_task_type option in yourpyproject.toml like so.. code-block toml  [tool.poe]  default_task_type  script  [tool.poe.tasks]  my_cmd_task  { cmd  cmd args }  my_script_task  my_package.my_modulerunChange the executor type------------------------You can configure poe to use a specific executor by settingtomltool.poe.executor.type. Valid values include- auto to automatically use the most appropriate of the following executors in order- poetry to run tasks in the poetry managed environment- virtualenv to run tasks in the indicated virtualenv (or else ..venv if present)- simple to run tasks without doing any specific environment setupThe default behaviour is auto.For example the following configuration will cause poe to ignore the poetry environment(if present), and instead use the virtualenv at the given location relative to theparent directory... code-block toml  [tool.poe.executor]  type  virtualenv  location  myvenvSee below for more details.Change the default shell interpreter------------------------------------Normally shell tasks are executed using a posix shell by default (see section for shelltasks above). This default can be overridden to something else by setting theshell_interpreter global option. In the following example we configure all shell tasksto use fish by default... code-block toml  tool.poe.shell_interpreter  fish  [tool.poe.tasks.fibonacci]  help  Output the fibonacci sequence up to 89  shell      function fib --argument-names max n0 n1      if test max -ge n0        echo n0        fib max n1 (math n0 + n1)      end    end    fib 89 1 1  Load tasks from another fileThere are some scenarios where one might wish to define tasks outside of pyproject.toml,or to collect tasks from multiple projects into one. For example, if you want to sharetasks between projects via git modules, generate tasks definitions dynamically, organiseyour code in a monorepo, or simply have a lot of tasks and dont want the pyproject.tomlto get too large. This can be achieved by creating a toml or json including the samestructure for tasks as used in pyproject.tomlFor example.. code-block toml   acme_commonshared_tasks.toml  [tool.poe.tasks.build-image]  cmd  docker build.. code-block toml  [tool.poe]   this references a file from a git submodule  include  modulesacme_commonshared_tasks.tomlImported files may also specify environment variables viacodetool.poe.envfile or entries for codetool.poe.env.Its also possible to include tasks from multiple files by providing a list likeso.. code-block toml  [tool.poe]  include  [modulesacme_commonshared_tasks.toml, generated_tasks.json]Files are loaded in the order specified. If an item already exists then the includedvalue it ignored.If an included task file itself includes other files, these second order includes arenot inherited, so circular includes dont cause any problems.When including files from another location, you can also specify that tasks from thatother file should be run from within a specific directory. For example with thefollowing configuration, when tasks imported from my_subproject are runfrom the root, the task will actually execute as if it had been run from themy_subproject subdirectory... code-block toml  [[tool.poe.include]]  path  my_subprojectpyproject.toml  cwd   my_subprojectThe cwd option still has the limitation that it cannot be used to specify a directoryoutside of parent directory of the pyproject.toml file that poe is running with.If a referenced file is missing then poe ignores it without error, thoughfailure to read the contents will result in failure.Usage as a poetry pluginDepending on how you manage your python environments you may also wish to use Poe thePoet in the form of a poetry plugin. This requires installing poethepoet[poetry_plugin]either into the same environment as poetry or into poetry itself.See the poetry docs httpspython-poetry.orgdocsmasterpluginsusing-plugins_for more details.Due to how the poetry CLI works (using cleo httpsgithub.comsdispatercleo_  afeatureful but highly opinionated  CLI framework) there exist a few minor limitationswhen used in this way.1.  Normally the poe CLI allows tasks to accept any arguments, either by defining the  expected options or by passing any command line tokens following the task name to the  task at runtime. This is not supported by cleo. The plugin implements a workaround  that mostly works, but still if the --no-plugins option is provided anywhere in  the command line then the poe plugin will never be invoked.2.  Poetry comes with its own  command line completion httpspython-poetry.orgdocsenable-tab-completion-for-bash-fish-or-zsh_,  but poes command line completion wont work.3.  If you declare named arguments for your poe tasks then these are included in the  documentation when poe is invoked without any arguments. However the inline  documentation for poetry commands contains only the task names and help text.Therefore it is recommended to use the poe CLI tool directly if you dont mind havingit installed onto your path.Configuring the plugin----------------------By default the poetry plugin will register poe as a command prefix so tasks can beinvoked like.. code-block sh  poetry poe [task_name] [task_args]And the poe documentation can be viewed via.. code-block bash  poetry poeIt is also possible to modify this behavoir, to either have a different command prefixor none at all by setting the tomlpoetry_command global option in yourpyproject.toml like so.. code-block toml  [tool.poe]  poetry_command  In this case poe tasks will be registered as top level commands on poetry and can beinvoked simply as.. code-block sh  poetry [task_name].. warning    Whatever tomltool.poe.poetry_command is set to must not already exist as a    poetry command    Additionally if setting it to the emtpy string then care must be taken to avoid    defining any poe tasks that conflict with any other built in or plugin provided    poetry command.Hooking into poetry commands----------------------------It is also possible to configure a task to be run before or after a specific poetrycommand by declaring the poetry_hooks global option like so.. code-block toml  [tool.poe.poetry_hooks]  pre_build   prep-assets --verbosity5  post_build  archive-build  [tool.poe.tasks.prep-assets]  script  scriptsprepare_assets  help    Optimise static assets for inclusion in the build  [tool.poe.tasks.archive-build]  script  scriptsarchive_build  help    Upload the latest build version to archive serverIn this example the codeprep-assets task will be run as the first step in callingbashpoetry build with an argument passed as if the task were being called via thepoe CLI. Weve also configured the codearchive-build task to be run after everysuccessful build.If a task fails when running as a hook, then the poetry command will exit with an error.If it is a pre hook then this will cause the actual poetry command not to execute.This behaviour may be useful for running checks before bashpoetry publishHooks can be disabled for a single invocation by passing the bash--no-plugins optionto poetry.Namespaced commands like bashpoetry env info can be specified with underscores like so.. code-block toml  [tool.poe.poetry_hooks]  post_env_info  infoUsage without poetryPoe the Poet was originally intended for use alongside poetry. But it works just aswell with any other kind of virtualenv, or simply as a general purpose way to definehandy tasks for use within a certain directory structure This behaviour is configurablevia the tomltool.poe.executor global option (see above).By default poe will run tasks in the poetry managed environment, if the pyproject.tomlcontains a tomltool.poetry section. If it doesnt then poe looks for a virtualenv touse from bash..venv or bash.venv relative to the pyproject.toml file.Otherwise it falls back to running tasks without any special environment management.Composing tasks into graphs (Experimental)You can define tasks that depend on other tasks, and optionally capture and reuse theoutput of those tasks, thus defining an execution graph of tasks. This is done by usingthe deps task option, or if you want to capture the output of the upstream task topass it to the present task then specify the uses option, as demonstrated below... code-block toml  [tool.poe.tasks]  _website_bucket_name.shell      aws cloudformation describe-stacks       --stack-name AWS_SAM_STACK_NAME       --query Stacks[0].Outputs[(.OutputKey  FrontendS3Bucket)].OutputValue      jq -cr select(0)[0]    [tool.poe.tasks.build-backend]  help  Build the backend  sequence  [    {cmd  poetry export -f requirements.txt --output srcrequirements.txt},    {cmd  sam build},  ]  [tool.poe.tasks.build-frontend]  help  Build the frontend  cmd  npm --prefix client run build  [tool.poe.tasks.shipit]  help  Build and deploy the app  sequence  [    sam deploy --config-env SAM_ENV_NAME,    aws s3 sync --delete .clientbuild s3{BUCKET_NAME}  ]  default_item_type  cmd  deps  [build-frontend, build-backend]  uses  { BUCKET_NAME  _website_bucket_name }In this example the shipit task depends on the build-frontend build-backend, whichmeans that these tasks get executed before the shipit task. It also declares that ituses the output of the hidden _website_bucket_name task, which means that this alsogets executed, but its output it captured and then made available to the shipit taskas the environment variable BUCKET_NAME.Note that captured output that is exposed as an environment variable via the usesis compacted to have new lines removed. This is similar to how interpolated commandoutput is treated by bash.This feature is experimental. There may be edge cases that arent handled well, sofeedback is requested. Some details of the implementation or API may be altered infuture versions.Supported python versionsPoe the Poet officially supports python 3.7, and is tested with python 3.7 to 3.11 onmacOS, linux and windows.ContributingTheres plenty to do, come say hi inthe issues httpsgithub.comnat-npoethepoetissues_ Also check out theCONTRIBUTING.MD httpsgithub.comnat-npoethepoetblobmain.githubCONTRIBUTING.md_ LicenceMIT.</longdescription>
</pkgmetadata>