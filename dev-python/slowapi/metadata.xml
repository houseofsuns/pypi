<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> SlowApiA rate limiting library for Starlette and FastAPI adapted from [flask-limiter](httpgithub.comalisaifeeflask-limiter).Note this is alpha quality code still, the API may change, and things may fall apart while you try it.The documentation is on [read the docs](httpsslowapi.readthedocs.ioenlatest). Quick start Installationslowapi is available from [pypi](httpspypi.orgprojectslowapi) so you can install it as usual pip install slowapi FeaturesMost feature are coming from FlaskLimiter and the underlying [limits](httpslimits.readthedocs.io).Supported now- Single and multiple limit decorator on endpoint functions to apply limits- redis, memcached and memory backends to track your limits (memory as a fallback)- support for sync and async HTTP endpoints- Support for shared limits across a set of routes Limitations and known issues   The request argument must be explicitly passed to your endpoint, or slowapi wont be able to hook into it. In other words, writepython    limiter.limit(5minute)    async def myendpoint(request Request)        passand notpython    limiter.limit(5minute)    async def myendpoint()        pass   websocket endpoints are not supported yet. Developing and contributingPRs are more than welcome Please include tests for your changes )The package uses [poetry](httpspython-poetry.org) to manage dependencies. To setup your dev envbash poetry installTo run the testsbash pytest CreditsCredits go to [flask-limiter](httpsgithub.comalisaifeeflask-limiter) of which SlowApi is a (still partial) adaptation to Starlette and FastAPI.Its also important to mention that the actual rate limiting work is done by [limits](httpsgithub.comalisaifeelimits), slowapi is just a wrapper around it.</longdescription>
</pkgmetadata>