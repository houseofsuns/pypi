<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>envtpl_Render jinja2 templates on the command line with shell environment variables_Installation------------    pip install envtplHow-to------Say you have a configuration file called whatever.conf that looks like this    foo  123    bar  abcYou can use envtpl to set foo and bar from the command line by creating a file called whatever.conf.tpl    foo  {{ FOO }}    bar  {{ BAR }}If you run    FOO123 BARabc envtpl  whatever.conf.tpl  whatever.confyoull get back the original whatever.conf.You can also specify default values    foo  {{ FOO  default(123) }}    bar  {{ BAR  default(abc) }}Running    FOO456 envtpl  whatever.conf.tpl  whatever.confwill generate    foo  456    bar  abcThis is all standard [Jinja2 syntax](httpjinja.pocoo.orgdocstemplates), so you can do things like    { if BAZ is defined }    foo  123    { else }    foo  456    { endif }    bar  abcIf an environment variable is missing, envtpl will throw an error     echo {{ FOO }} {{ BAR }}  FOO123 envtpl    Error BAR is undefinedYou can change this behaviour to insert empty strings instead by passing the --allow-missing flag.Instead of reading from stdin and writing to stdout, you can pass the input filename as an optional positional argument,and set the output filename with the --output-file (-o) argument.    envtpl -o whatever.conf  whatever.conf.tplAs a convenience, if you dont specify an output filename and the input filename ends with .tpl, the output filename will be the input filename without the .tpl extension, i.e.    envtpl whatever.conf.tpl     is equivalent to    envtpl -o whatever.conf whatever.conf.tplBy default, envtpl will delete the input template file. You can keep it by passing the --keep-template flag.Theres a special environment(prefix) function that you can use as a kind of wildcard variable. If you have hello.tpl    hello  {{ FOO }}    { for key, value in environment(MY_) }{{ key }}  {{ value }}    { endfor }and compile it using    FOOworld MY_bazqux MY_foobar envtpl hello.tplYou end up with    hello  world    baz  qux    foo  barWhats the point-----------------I use this script quite a lot in Docker images. Usually Ill have the CMD execute some file, like binstart_container, that sets up the runtime configuration for the container by inserting environment variables into config files before starting the main process. A redis example could look like this    binbash     start_container    envtpl etcredis.conf.tpl    redis-serverThis is the use case Ive optimised for, so thats why envtpl by default will delete the original template file.</longdescription>
</pkgmetadata>