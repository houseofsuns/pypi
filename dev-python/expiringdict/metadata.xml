<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Expiring Dict-------------.. image httpstravis-ci.orgmailgunexpiringdict.svgbranchmaster    target httpstravis-ci.orgmailgunexpiringdict.. image httpscoveralls.ioreposgithubmailgunexpiringdictbadge.svgbranchmaster    target httpscoveralls.iogithubmailgunexpiringdictbranchmasterChangeLog_expiringdict is a Python caching library. The core of the library is ExpiringDict class whichis an ordered dictionary with auto-expiring values for caching purposes. Expiration happens onany access, object is locked during cleanup from expired values. ExpiringDict can not storemore than max_len elements - the oldest will be deleted.Note Iteration over dict and also keys() do not remove expired values.. _ChangeLog .CHANGELOG.rstInstallation------------If you wish to install from PyPi.. code-block bash    pip install expiringdictIf you wish to download the source and install from GitHub.. code-block bash    git clone gitgithub.commailgunexpiringdict.git    python setup.py installor to install with test dependencies (Nose httpreadthedocs.orgdocsnoseenlatest_, Mock httpwww.voidspace.org.ukpythonmock_, coverage httpnedbatchelder.comcodecoverage_) run from the directory above.. code-block bash    pip install -e expiringdict[test]To run tests with coverage.. code-block bash    nosetests --with-coverage --cover-packageexpiringdictUsage-----Create a dictionary with capacity for 100 elements and elements expiring in 10 seconds.. code-block py    from expiringdict import ExpiringDict    cache  ExpiringDict(max_len100, max_age_seconds10)put and get a value there.. code-block py     cache[key]  value     cache.get(key)copy from dict or OrderedDict.. code-block py     from expiringdict import ExpiringDict     my_dictdict()     my_dict[test]  1     cache  ExpiringDict(max_len100, max_age_seconds10, itemsmy_dict)     assert cache[test]  1copy from another ExpiringDict, with or without new length and timeout.. code-block py     from expiringdict import ExpiringDict     cache_hour  ExpiringDict(max_len100, max_age_seconds3600)     cache_hour[test]  1     cache_hour_copy  ExpiringDict(max_lenNone, max_age_secondsNone, itemscache_hour)     cache_minute_copy  ExpiringDict(max_lenNone, max_age_seconds60, itemscache_hour)     assert cache_minute_copy[test]  1pickle .. code-block py    import dill    from expiringdict import ExpiringDict    cache  ExpiringDict(max_len100, max_age_seconds10)    cache[test]  1    pickled_cache  dill.dumps(cache)    unpickled_cache  dill.loads(cache)    assert unpickled_cache[test]  1</longdescription>
</pkgmetadata>