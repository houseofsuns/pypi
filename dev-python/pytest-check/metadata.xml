<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> pytest-checkA pytest plugin that allows multiple failures per test.----Normally, a test function will fail and stop running with the first failed assert.Thats totally fine for tons of kinds of software tests.However, there are times where youd like to check more than one thing, and youd really like to know the results of each check, even if one of them fails.pytest-check allows multiple failed checks per test function, so you can see the whole picture of whats going wrong. InstallationFrom PyPI pip install pytest-checkFrom conda (conda-forge) conda install -c conda-forge pytest-check ExampleQuick example of where you might want multiple checkspythonimport httpxfrom pytest_check import checkdef test_httpx_get()    r  httpx.get(httpswww.example.org)     bail if bad status code    assert r.status_code  200     but if we get to here     then check everything else without stopping    with check        assert r.is_redirect is False    with check        assert r.encoding  utf-8    with check        assert Example Domain in r.text Import vs fixtureThe example above used import from pytest_check import check.You can also grab check as a fixture with no importpythondef test_httpx_get(check)    r  httpx.get(httpswww.example.org)    ...    with check        assert r.is_redirect  False    ... Validation functionscheck also helper functions for common checks. These methods do NOT need to be inside of a with check block.- check.equal - a  b- check.not_equal - a  b- check.is_ - a is b- check.is_not - a is not b- check.is_true - bool(x) is True- check.is_false - bool(x) is False- check.is_none - x is None- check.is_not_none - x is not None- check.is_in - a in b- check.is_not_in - a not in b- check.is_instance - isinstance(a, b)- check.is_not_instance - not isinstance(a, b)- check.almost_equal - a  pytest.approx(b, rel, abs) see at [pytest.approx](httpsdocs.pytest.orgenlatestreference.htmlpytest-approx)- check.not_almost_equal - a  pytest.approx(b, rel, abs) see at [pytest.approx](httpsdocs.pytest.orgenlatestreference.htmlpytest-approx)- check.greater - a  b- check.greater_equal - a  b- check.less - a  b- check.less_equal - a  b- check.between - a  b  c- check.raises - func raises given exception similar to [pytest.raises](httpsdocs.pytest.orgenlatestreferencereference.htmlpytest-raises)The httpx example can be rewritten with helper functionspythondef test_httpx_get_with_helpers()    r  httpx.get(httpswww.example.org)    assert r.status_code  200    check.is_false(r.is_redirect)    check.equal(r.encoding, utf-8)    check.is_in(Example Domain, r.text)Which you use is personal preference. Defining your own check functionsThe check.check_func decorator allows you to wrap any test helper that has an assertstatement in it to be a non-blocking assert function.pythonfrom pytest_check import checkcheck.check_funcdef is_four(a)    assert a  4def test_all_four()    is_four(1)    is_four(2)    is_four(3)    is_four(4) Using raises as a context managerraises is used as context manager, much like pytest.raises. The main difference being that a failure to raise the right exception wont stop the execution of the test method.pythonfrom pytest_check import checkdef test_raises()    with check.raises(AssertionError)        x  3        assert 1  x  4 Pseudo-tracebacksWith check, tests can have multiple failures per test.This would possibly make for extensive output if we include the full traceback forevery failure.To make the output a little more concise, pytest-check implements a shorter version, which we call pseudo-tracebacks.For example, take this testpythondef test_example()    a  1    b  2    c  [2, 4, 6]    check.greater(a, b)    check.less_equal(b, a)    check.is_in(a, c, Is 1 in the list)    check.is_not_in(b, c, make sure 2 isnt in list)This will result in FAILURES _________________________________ test_example _________________________________FAILUREassert 1  2  test_check.py, line 14, in test_example() - check.greater(a, b)FAILUREassert 2  1  test_check.py, line 15, in test_example() - check.less_equal(b, a)FAILURE Is 1 in the listassert 1 in [2, 4, 6]  test_check.py, line 16, in test_example() - check.is_in(a, c, Is 1 in the list)FAILURE make sure 2 isnt in listassert 2 not in [2, 4, 6]  test_check.py, line 17, in test_example() - check.is_not_in(b, c, make sure 2 isnt in list)------------------------------------------------------------Failed Checks 4 1 failed in 0.11 seconds  Red outputThe failures will also be red, unless you turn that off with pytests --colorno. No outputYou can turn off the failure reports with pytests --tbno. Stop on Fail (maxfail behavior)Setting -x or --maxfail1 will cause this plugin to abort testing after the first failed check.Setting -maxfail2 or greater will turn off any handling of maxfail within this plugin and the behavior is controlled by pytest.In other words, the maxfail count is counting tests, not checks.The exception is the case of 1, where we want to stop on the very first failed check. any_failures()Use any_failures() to see if there are any failures.  One use case is to make a block of checks conditional on not failing in a previous set of checkspythonfrom pytest_check import checkdef test_with_groups_of_checks()     always check these    check.equal(1, 1)    check.equal(2, 3)    if not check.any_failures()         only check these if the above passed        check.equal(1, 2)        check.equal(2, 2) SpeedupsIf you have lots of check failures, your tests may not run as fast as you want.There are a few ways to speed things up. --check-max-tb5 - Only first 5 failures per test will include pseudo-tracebacks (rest without them).     The example shows 5 but any number can be used.     pytest-check uses custom traceback code Im calling a pseudo-traceback.     This is visually shorter than normal assert tracebacks.     Internally, it uses introspection, which can be slow.     Allowing a limited number of pseudo-tracebacks speeds things up quite a bit.     Default is 1.          Set a large number, e.g 1000, if you want pseudo-tracebacks for all failures --check-max-report10 - limit reported failures per test.     The example shows 10 but any number can be used.     The test will still have the total nuber of failures reported.     Default is no maximum. --check-max-fail20 - Stop the test after this many check failures.     This is useful if your code under test is slow-ish and you want to bail early.     Default is no maximum. Any of these can be used on their own, or combined. Recommendation     Leave the default, equivelant to --check-max-tb1.     If excessive output is annoying, set --check-max-report10 or some tolerable number. Local speedupsThe flags above are global settings, and apply to every test in the test run.  Locally, you can set these values per test.From examplestest_example_speedup_funcs.pypythondef test_max_tb()    check.set_max_tb(2)    for i in range(1, 11)        check.equal(i, 100)def test_max_report()    check.set_max_report(5)    for i in range(1, 11)        check.equal(i, 100)def test_max_fail()    check.set_max_fail(5)    for i in range(1, 11)        check.equal(i, 100) ContributingContributions are very welcome. Tests can be run with [tox](httpstox.readthedocs.ioenlatest).Test coverage is now 100. Please make sure to keep it at 100.If you have an awesome pull request and need help with getting coverage back up, let me know. LicenseDistributed under the terms of the [MIT](httpopensource.orglicensesMIT) license, pytest-check is free and open source software IssuesIf you encounter any problems, please [file an issue](httpsgithub.comokkenpytest-checkissues) along with a detailed description. ChangelogSee [changelog.md](httpsgithub.comokkenpytest-checkblobmainchangelog.md)</longdescription>
</pkgmetadata>