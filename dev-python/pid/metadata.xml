<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pid.. image httpstravis-ci.orgtrbspid.svgbranchmaster    target httpstravis-ci.orgtrbspid.. image httpscoveralls.iorepostrbspidbadge.png    target httpscoveralls.iortrbspid.. image httpsimg.shields.iopypivpid.svg    target httpspypi.python.orgpypipid    alt Latest PyPI version.. image httpsimg.shields.iopypidmpid.svg    target httpspypi.python.orgpypipid    alt Number of PyPI downloadsPidFile class featuring - stale detection - pidfile locking (fcntl) - chmod (default is 0o644) - chown - custom exceptionsContext Manager, Daemons and Logging------------------------------------PidFile can be used as a context manager  from pid import PidFile  import os  with PidFile(foo) as p    print(p.pidname)  - foo    print(p.piddir)  - varrun But you can modify it when initialize PidFile.    print(os.listdir(varrun))  - [foo.pid]   pid file will delete after with literal.Logging to file is also possible when using PidFile with a daemon context manager(e.g. python-daemon httpspypi.python.orgpypipython-daemon_). This requires some care inhandling the open files when the daemon starts to avoid closing them, which causes problems with thelogging. In particular, the open handlers should be preserved  import sys  import logging  import logging.config  import daemon  from pid impor PidFile  logging.config.fileConfig(fnamelogging.conf, disable_existing_loggersFalse)  log  logging.getLogger(__name__)  PIDNAME  tmpmydaemon.pid  def get_logging_handles(logger)      handles  []      for handler in logger.handlers          handles.append(handler.stream.fileno())      if logger.parent          handles + get_logging_handles(logger.parent)      return handles  def daemonize()    file_preserve  get_logging_handles(logging.root)    pid_file  PidFile(pidnamePIDNAME)    with daemon.DaemonContext(stdoutsys.stdout,                              stderrsys.stderr,                              stdinsys.stdin,                              pidfile_pid_file,                              files_preservefiles_preserve)      run_daemon_job()    print(DONE)  if __name__  __main__    daemonize()This assumes a logging.conf file has been created, see e.g. basic tutorialhttpsdocs.python.org3howtologging.htmllogging-basic-tutorial_ for logging.Decorator---------PidFile can also be used a a decorator  from pid.decorator import pidfile  pidfile()  def main()    pass  if __name__  __main__    main()Exception Order---------------In default mode PidFile will try to acquire a file lock before anything else.This means that normally you get a PidFileAlreadyLockedError instead of thePidFileAlreadyRunningError when running a program twice.If you just want to know if a program is already running its easiest to catchjust PidFileError since it will capture all possible PidFile exceptions.Behaviour---------Changes in version 2.0.0 and going forward pid is now friendly with daemon context managers such as  python-daemon httpspypi.python.orgpypipython-daemon_ where  the PidFile context manager is passed as a parameter. The  new corrected behaviour will ensure the process environment is  determined at the time of acquiringchecking the lock. Prior  behaviour would determine the process environment when  instancing the class which may result in incorrect determination  of the PID in the case of a process forking after instancing  PidFile. Cleanup of pidfile on termination is done using atexit module.  The default SIGTERM handler doesnt cleanly exit and therefore  the atexit registered functions will not execute. A custom  handler which triggers the atexit registered functions for cleanup  will override the default SIGTERM handler. If a prior signal handler  has been configured, then it will not be overridden.</longdescription>
</pkgmetadata>