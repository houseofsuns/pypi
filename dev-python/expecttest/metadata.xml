<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> expecttest [[PyPI version](httpsbadge.fury.iopyexpecttest.svg)](httpsbadge.fury.iopyexpecttest)This library implements expect tests (also known as golden tests). Expecttests are a method of writing tests where instead of hard-coding the expectedoutput of a test, you run the test to get the output, and the test frameworkautomatically populates the expected output.  If the output of the test changes,you can rerun the test with the environment variable EXPECTTEST_ACCEPT1 toautomatically update the expected output.Somewhat unusually, this library implements inline expect tests that is tosay, the expected output isnt saved to an external file, it is saved directlyin the Python file (and we modify your Python file when updating the expecttest.)The general recipe for how to use this is as follows  1. Write your test and use assertExpectedInline() instead of a normal     assertEqual.  Leave the expected argument blank with an empty string     py     self.assertExpectedInline(some_func(), )       2. Run your test.  It should fail, and you get an error message about     accepting the output with EXPECTTEST_ACCEPT1  3. Rerun the test with EXPECTTEST_ACCEPT1.  Now the previously blank string     literal will contain the expected value of the test.     py     self.assertExpectedInline(some_func(), my_value)     Some tips and tricks  - Often, you will want to expect test on a multiline string.  This framework    understands triple-quoted strings, so you can just write my_value    and it will turn into triple-quoted strings.  - Take some time thinking about how exactly you want to design the output    format of the expect test.  It is often profitable to design an output    representation specifically for expect tests.</longdescription>
</pkgmetadata>