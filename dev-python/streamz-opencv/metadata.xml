<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># streamz-opencvProvide an opencv capture source for [streamz](https://streamz.readthedocs.io/en/latest/).# QuickstartInstall `streamz` and `streamz-opencv`:```bashpython -m pip install streamz streamz-opencv```Build your pipeline:```pythonimport cv2from streamz import Streamimport streamz_opencvdef scale(img, scale=1.0):    scale_percent = 60 # percent of original size    width = int(img.shape[1] * scale)    height = int(img.shape[0] * scale)    dim = (width, height)    resized = cv2.resize(img, dim, interpolation = cv2.INTER_AREA)    return resizeddef save(img, path):    cv2.imwrite(path, img)stream = Stream.from_opencv(0, asynchronous=True)stream.map(scale, scale=0.5).sink(save, path='small.jpg')stream.map(scale, scale=1.5).sink(save, path='big.jpg')stream.start()```## good to know* testing via pytest* dep management via venv+setup.py+pip-tools* see example usage in test</longdescription>
</pkgmetadata>