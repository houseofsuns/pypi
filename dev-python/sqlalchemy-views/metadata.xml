<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>View Manipulation for SQLAlchemyAdds CreateView and DropView constructs to SQLAlchemy.Status------Current version is 0.3.2 releases are infrequent as the code here is small and based on stable pieces of the SQLAlchemy API, so there has been minimal maintenance required so far to keep compatibility with new SQLAlchemy or python versions.This package is working with latest Python and SQLAlchemy (1.0+ and 2.0) as of early 2023.Usage-----Examples     from sqlalchemy import Table, MetaData     from sqlalchemy.sql import text     from sqlalchemy_views import CreateView, DropView     view  Table(my_view, MetaData())     definition  text(SELECT  FROM my_table)     create_view  CreateView(view, definition)     print(str(create_view.compile()).strip())    CREATE VIEW my_view AS SELECT  FROM my_table     create_view  CreateView(view, definition, or_replaceTrue)     print(str(create_view.compile()).strip())    CREATE OR REPLACE VIEW my_view AS SELECT  FROM my_table     create_view  CreateView(view, definition, options{check_option local})     print(str(create_view.compile()).strip())    CREATE VIEW my_view WITH (check_optionlocal) AS SELECT  FROM my_table     drop_view  DropView(view)     print(str(drop_view.compile()).strip())    DROP VIEW my_view     drop_view  DropView(view, if_existsTrue, cascadeTrue)     print(str(drop_view.compile()).strip())    DROP VIEW IF EXISTS my_view CASCADENote that the SQLAlchemy Table object is used to representboth tables and views. To introspect a view, create a Tablewith autoloadTrue, and then use SQLAlchemysget_view_definition method to generate the secondargument to CreateView.Installation------------sqlalchemy-views is available on PyPI and can be installed via pip     pip install sqlalchemy-viewsLimitations-----------Various SQL dialects have developed customCREATE VIEW and DROP VIEW syntax.This project aims to provide the core set of functionalityshared by most database engines.Acknowledgements----------------Some design ideas taken from theSQLAlchemy Usage Recipe for views httpsbitbucket.orgzzzeeksqlalchemywikiUsageRecipesViews_.Package structure is based onpython-project-template httpsgithub.comseanfiskpython-project-template_.And thank you to the variouscontributors httpsgithub.comjklukassqlalchemy-viewsgraphscontributors_</longdescription>
</pkgmetadata>