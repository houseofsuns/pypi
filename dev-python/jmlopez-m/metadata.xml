<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># [m](https://jmlopez-rod.github.io/m/)## install```pip install jmlopez-m```To install from a branch run```pip install git+https://github.com/jmlopez-rod/m.git@[branch-name]```The cli supports `argcomplete`. To set it up follow the following depending onyour shell```shell# for fish shellregister-python-argcomplete --shell fish m &gt; /tmp/foosudo mv /tmp/foo /usr/share/fish/vendor_functions.d/m.fish# for bash shellregister-python-argcomplete --shell bash m &gt; /tmp/foosudo mv /tmp/foo /usr/share/bash-completion/completions/m# for zshregister-python-argcomplete --shell zsh m &gt; /tmp/foosudo mv /tmp/foo /usr/share/zsh/site-functions/_m```## developmentIf you are working on a non-intel machine (M1/M2 Macs) you can run`make buildPy311DevContainer` and update `devcontainer.json` to use the`m-devcontainer` image.Open the devcontainer in VSCode. Once there open a terminal and run```poetry install```and```pnpm install```to install the dependencies.There are several make targets to run. The most important one is`make ci-checks`. Run that since it is the closest to what the CI will run.</longdescription>
</pkgmetadata>