# automatically generated by g-sorcery
# please do not edit this file

EAPI=8

REALNAME="${PN}"
LITERALNAME="${PN}"
REALVERSION="${PV}"
DIGEST_SOURCES="yes"
PYTHON_COMPAT=( python{3_10,3_11,3_12} )
DISTUTILS_USE_PEP517=standalone

inherit python-r1 gs-pypi

DESCRIPTION="Apache Sedona is a cluster computing system for processing large-scale spatial data"

HOMEPAGE="https://sedona.apache.org"
LICENSE="Apache License v2.0"
SRC_URI="https://files.pythonhosted.org/packages/source/${REALNAME::1}/${REALNAME}/${REALNAME//-/_}-${REALVERSION}.tar.gz"
SOURCEFILE="${REALNAME//-/_}-${REALVERSION}.tar.gz"
RESTRICT="test"

SLOT="0"
KEYWORDS="~amd64 ~x86"

IUSE="all kepler-map pydeck-map spark"
DEPENDENCIES="dev-python/attrs[${PYTHON_USEDEP}]
	>=dev-python/shapely-1.7.0[${PYTHON_USEDEP}]
	all? ( dev-python/pyspark[${PYTHON_USEDEP}] )
	all? ( <=dev-python/pandas-1.3.5[${PYTHON_USEDEP}] )
	all? ( dev-python/geopandas[${PYTHON_USEDEP}] )
	all? ( dev-python/pydeck[${PYTHON_USEDEP}] )
	all? ( dev-python/keplergl[${PYTHON_USEDEP}] )
	kepler-map? ( <=dev-python/pandas-1.3.5[${PYTHON_USEDEP}] )
	kepler-map? ( dev-python/geopandas[${PYTHON_USEDEP}] )
	kepler-map? ( dev-python/keplergl[${PYTHON_USEDEP}] )
	pydeck-map? ( <=dev-python/pandas-1.3.5[${PYTHON_USEDEP}] )
	pydeck-map? ( dev-python/geopandas[${PYTHON_USEDEP}] )
	pydeck-map? ( dev-python/pydeck[${PYTHON_USEDEP}] )
	spark? ( dev-python/pyspark[${PYTHON_USEDEP}] )"
BDEPEND="${DEPENDENCIES}"
RDEPEND="${DEPENDENCIES}"
