<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-money.. image httpsgithub.comdjango-moneydjango-moneyworkflowsCIbadge.svg   target httpsgithub.comdjango-moneydjango-moneyactions   alt Build Status.. image httpcodecov.iogithubdjango-moneydjango-moneycoverage.svgbranchmain   target httpcodecov.iogithubdjango-moneydjango-moneybranchmain   alt Coverage Status.. image httpsreadthedocs.orgprojectsdjango-moneybadgeversionlatest   target httpdjango-money.readthedocs.ioenlatest   alt Documentation Status.. image httpsimg.shields.iopypivdjango-money.svg   target httpspypi.python.orgpypidjango-money   alt PyPIA little Django app that uses py-moneyed httpsgithub.compy-moneyedpy-moneyed__ to add support for Moneyfields in your models and forms. Django versions supported 2.2, 3.2, 4.0, 4.1 Python versions supported 3.7, 3.8, 3.9, 3.10 PyPy versions supported PyPy3If you need support for older versions of Django and Python, please refer to older releases mentioned in the release notes httpsdjango-money.readthedocs.ioenlatestchanges.html__.Through the dependency py-moneyed httpsgithub.compy-moneyedpy-moneyed__, django-money gets Support for proper Money value handling (using the standard Money  design pattern) A currency class and definitions for all currencies in circulation Formatting of most currencies with correct currency signInstallation------------Using pip.. code bash    pip install django-moneyThis automatically installs py-moneyed v1.2 (or later).Add djmoney to your INSTALLED_APPS. This is required so that money field are displayed correctly in the admin... code python   INSTALLED_APPS  [      ...,      djmoney,      ...   ]Model usage-----------Use as normal model fields.. code python        from djmoney.models.fields import MoneyField        from django.db import models        class BankAccount(models.Model)            balance  MoneyField(max_digits14, decimal_places2, default_currencyUSD)To comply with certain strict accounting or financial regulations, you may consider using max_digits19 and decimal_places4, see more in this StackOverflow answer httpsstackoverflow.coma224866405682__It is also possible to have a nullable MoneyField.. code python        class BankAccount(models.Model)            money  MoneyField(max_digits10, decimal_places2, nullTrue, default_currencyNone)        account  BankAccount.objects.create()        assert account.money is None        assert account.money_currency is NoneSearching for models with money fields.. code python        from djmoney.money import Money        account  BankAccount.objects.create(balanceMoney(10, USD))        swissAccount  BankAccount.objects.create(balanceMoney(10, CHF))        BankAccount.objects.filter(balance__gtMoney(1, USD))         Returns the account objectThe default currency code length is 3 but you can change it with the CURRENCY_CODE_MAX_LENGTH setting.Caution this setting also affects the initial migration of the exchange plugin, so changing it after runningthe initial migration has no effect. (Youd need to manage migrate exchange zero and migrate again if you wantto change it).Field validation----------------There are 3 different possibilities for field validation by numeric part of money despite on currency by single money amount by multiple money amounts.All of them could be used in a combination as is shown below.. code python        from django.db import models        from djmoney.models.fields import MoneyField        from djmoney.money import Money        from djmoney.models.validators import MaxMoneyValidator, MinMoneyValidator        class BankAccount(models.Model)            balance  MoneyField(                max_digits10,                decimal_places2,                validators[                    MinMoneyValidator(10),                    MaxMoneyValidator(1500),                    MinMoneyValidator(Money(500, NOK)),                    MaxMoneyValidator(Money(900, NOK)),                    MinMoneyValidator({EUR 100, USD 50}),                    MaxMoneyValidator({EUR 1000, USD 500}),                ]            )The balance field from the model above has the following validation All input values should be between 10 and 1500 despite on currency Norwegian Crowns amount (NOK) should be between 500 and 900 Euros should be between 100 and 1000 US Dollars should be between 50 and 500Adding a new Currency---------------------Currencies are listed on moneyed, and this modules use this to provide achoice list on the admin, also for validation.To add a new currency available on all the project, you can simply addthese few lines to your settings.py file.. code python        import moneyed        BOB  moneyed.add_currency(            codeBOB,            numeric068,            namePeso boliviano,            countries(BOLIVIA, )        )To restrict the currencies listed on the project set a CURRENCIESvariable with a list of Currency codes on settings.py.. code python        CURRENCIES  (USD, BOB)The list has to contain valid Currency codesAdditionally there is an ability to specify currency choices directly.. code python        CURRENCIES  (USD, EUR)        CURRENCY_CHOICES  [(USD, USD ), (EUR, EUR )]Important note on model managers--------------------------------Django-money leaves you to use any custom model managers you like foryour models, but it needs to wrap some of the methods to allow searchingfor models with money values.This is done automatically for the objects attribute in any model thatuses MoneyField. However, if you assign managers to some otherattribute, you have to wrap your manager manually, like so.. code python        from djmoney.models.managers import money_manager        class BankAccount(models.Model)            balance  MoneyField(max_digits10, decimal_places2, default_currencyUSD)            accounts  money_manager(MyCustomManager())Also, the money_manager wrapper only wraps the standard QuerySetmethods. If you define custom QuerySet methods, that do not end up usingany of the standard ones (like get, filter and so on), then you alsoneed to manually decorate those custom methods, like so.. code python        from djmoney.models.managers import understands_money        class MyCustomQuerySet(QuerySet)           understands_money           def my_custom_method(args, kwargs)                Awesome stuffFormat localization-------------------The formatting is turned on if you have set USE_L10N  True in theyour settings file.If formatting is disabled in the configuration, then in the templateswill be used default formatting.In the templates you can use a special tag to format the money.In the file settings.py add to INSTALLED_APPS entry from thelibrary djmoney.. code python        INSTALLED_APPS + (djmoney, )In the template, add        { load djmoney }        ...        { money_localize money }and that is all.Instructions to the tag money_localize            { money_localize money_object [ on(default)  off ] [as var_name] }            { money_localize amount currency [ on(default)  off ] [as var_name] }ExamplesThe same effect            { money_localize money_object }            { money_localize money_object on }Assignment to a variable            { money_localize money_object on as NEW_MONEY_OBJECT }Formatting the number with currency            { money_localize 4.5 USD }    Return        Money objectTesting-------Install the required packages    git clone httpsgithub.comdjango-moneydjango-money    cd .django-money    pip install -e .[test]  installation with required packages for testingRecommended way to run the tests.. code bash    toxTesting the application in the current environment python.. code bash    make testWorking with Exchange Rates---------------------------To work with exchange rates, add the following to your INSTALLED_APPS... code python    INSTALLED_APPS  [        ...,        djmoney.contrib.exchange,    ]Also, it is required to have certifi installed.It could be done via installing djmoney with exchange extra.. code bash    pip install django-money[exchange]To create required relations run python manage.py migrate. To fill these relations with data you need to choose adata source. Currently, 2 data sources are supported - httpsopenexchangerates.org (default) and httpsfixer.io.To choose another data source set EXCHANGE_BACKEND settings with importable string to the backend you need.. code python    EXCHANGE_BACKEND  djmoney.contrib.exchange.backends.FixerBackendIf you want to implement your own backend, you need to extend djmoney.contrib.exchange.backends.base.BaseExchangeBackend.Two data sources mentioned above are not open, so you have to specify access keys in order to use themOPEN_EXCHANGE_RATES_APP_ID - your actual key from openexchangerates.orgFIXER_ACCESS_KEY - your actual key from fixer.ioBackends return rates for a base currency, by default it is USD, but could be changed via BASE_CURRENCY setting.Open Exchanger Rates  Fixer supports some extra stuff, like historical data or restricting currenciesin responses to the certain list. In order to use these features you could change default URLs for these backends.. code python    OPEN_EXCHANGE_RATES_URL  httpsopenexchangerates.orgapihistorical2017-01-01.jsonsymbolsEUR,NOK,SEK,CZK    FIXER_URL  httpdata.fixer.ioapi2013-12-24symbolsEUR,NOK,SEK,CZKOr, you could pass it directly to update_rates method.. code python     from djmoney.contrib.exchange.backends import OpenExchangeRatesBackend     backend  OpenExchangeRatesBackend(urlhttpsopenexchangerates.orgapihistorical2017-01-01.json)     backend.update_rates(symbolsEUR,NOK,SEK,CZK)There is a possibility to use multiple backends in the same time.. code python     from djmoney.contrib.exchange.backends import FixerBackend, OpenExchangeRatesBackend     from djmoney.contrib.exchange.models import get_rate     OpenExchangeRatesBackend().update_rates()     FixerBackend().update_rates()     get_rate(USD, EUR, backendOpenExchangeRatesBackend.name)     get_rate(USD, EUR, backendFixerBackend.name)Regular operations with Money will use EXCHANGE_BACKEND backend to get the rates.Also, there are two management commands for updating rates and removing them.. code bash     python manage.py update_rates    Successfully updated rates from openexchangerates.org     python manage.py clear_rates    Successfully cleared rates for openexchangerates.orgBoth of them accept -b--backend option, that will updateclear data only for this backend.And clear_rates accepts -a--all option, that will clear data for all backends.To set up a periodic rates update you could use Celery task.. code python    CELERYBEAT_SCHEDULE  {        update_rates {            task path.to.your.task,            schedule crontab(minute0, hour0),            kwargs {}   For custom arguments        }    }Example task implementation.. code python    from django.utils.module_loading import import_string    from celery import Celery    from djmoney import settings    app  Celery(tasks, brokerpyamqpguestlocalhost)    app.task    def update_rates(backendsettings.EXCHANGE_BACKEND, kwargs)        backend  import_string(backend)()        backend.update_rates(kwargs)To convert one currency to another.. code python     from djmoney.money import Money     from djmoney.contrib.exchange.models import convert_money     convert_money(Money(100, EUR), USD)    Money 122.8184375038380800 USDExchange rates are integrated with Django Admin.django-money can be configured to automatically use this app for currencyconversions by settings AUTO_CONVERT_MONEY  True in your Djangosettings. Note that currency conversion is a lossy process, so automaticconversion is usually a good strategy only for very simple use cases. For mostuse cases you will need to be clear about exactly when currency conversionoccurs, and automatic conversion can hide bugs. Also, with automatic conversionyou lose some properties like commutativity (A + B  B + A) due toconversions happening in different directions.Usage with Django REST Framework--------------------------------Make sure that djmoney and is in the INSTALLED_APPS of yoursettings.py and that rest_framework has been installed. MoneyField willautomatically register a serializer for Django REST Framework throughdjmoney.apps.MoneyConfig.ready().You can add a serializable field the following way.. code python    from djmoney.contrib.django_rest_framework import MoneyField    class Serializers(serializers.Serializer)        my_computed_prop  MoneyField(max_digits10, decimal_places2)Built-in serializer works in the following way.. code python    class Expenses(models.Model)        amount  MoneyField(max_digits10, decimal_places2)    class Serializer(serializers.ModelSerializer)        class Meta            model  Expenses            fields  __all__     instance  Expenses.objects.create(amountMoney(10, EUR))     serializer  Serializer(instanceinstance)     serializer.data    ReturnDict([        (id, 1),        (amount_currency, EUR),        (amount, 10.000),    ])Note that when specifying individual fields on your serializer, the amount and currency fields are treated separately.To achieve the same behaviour as above you would include both field names.. code python    class Serializer(serializers.ModelSerializer)        class Meta            model  Expenses            fields  (id, amount, amount_currency)Customization-------------If there is a need to customize the process deconstructing Money instances onto Django Fields and the other way around,then it is possible to use a custom descriptor like this one.. code python    class MyMoneyDescriptor        def __get__(self, obj, typeNone)            amount  obj.__dict__[self.field.name]            return Money(amount, EUR)It will always use EUR for all Money instances when obj.money is called. Then it should be passed to MoneyField.. code python    class Expenses(models.Model)        amount  MoneyField(max_digits10, decimal_places2, money_descriptor_classMyMoneyDescriptor)Background----------This project is a fork of the Django support that was inhttpcode.google.comppython-moneyThis version adds tests, and comes with several critical bugfixes.</longdescription>
</pkgmetadata>