<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-google-tag-manager=========================Template tag to install your Google Tag Manager account in yourtemplates (http://www.google.com/tagmanager/)## Installation and Usage1. run `pip install django-google-tag-manager`2. add `'gtm'` to your `INSTALLED_APPS` setting.3. set `GOOGLE_TAG_ID` to your Google Tag Container Id. It   should look something like `GTM_XXXXXX`4. In your templates (probably in your base template) you `{% load   gtm_tags %}` and then add `{% gtm_head %}` to your `&lt;head&gt;` and   add `{% gtm_body %}` just below your `&lt;body&gt;` opening tag.5. ProfitThat's it for the most part. If for any reason you want to overridethe templates used to render the tags, they are called`gtm/gtm_head.html` and `gtm/gtm_body.html`.Optionally, you can pass the google tag id as a parameter to thetemplatetag like this:    {% gtm_head &quot;GTM-ABC123&quot; %}and    {% gtm_body &quot;GTM-ABC123&quot; %}For backwards compatibility you can still use the single `{% gtm %}`tag just below your `&lt;body&gt;` opening tag. This will output both thehead and body code.</longdescription>
</pkgmetadata>