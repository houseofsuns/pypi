<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Generalized Package Asset LoaderLoads resources and symbols from a python package, whether installedas a directory, an egg, or in source form. Also provides some otherpackage-related helper methods, including asset.version(),asset.caller(), and asset.chunks().TLDRInstall.. code bash   pip install assetLoad symbols (e.g. functions, classes, or variables) from a package byname.. code python  import asset   load the mypackage.foo.myfunc function and call it with some parameter  retval  asset.symbol(mypackage.foo.myfunc)(paramvalue)Load data files from a package.. code python   load the file mypackagetemplatesdata.txt into string  data  asset.load(mypackagetemplatesdata.txt).read()   or as a file-like stream  stream  asset.load(mypackagetemplatesdata.txt).stream()  data    stream.read()Multiple files can be operated on at once by using globrehttpspypi.python.orgpypiglobre_ style wildcards.. code python   concatenate all css files into one string  css  asset.load(mypackagestaticstyle.css).read()   load all .txt files, XML-escaping the data and wrapping   each file in an node name......node element.  import xml.etree.ElementTree as ET  data  ET.Element(nodes)  for item in asset.load(asset.txt)    cur  ET.SubElement(data, node, nameitem.name)    cur.text  item.read()  data  ET.tostring(data)Query the installed version of a package.. code python  asset.version(asset)    0.0.5  asset.version(python)    2.7  asset.version(no-such-package)    NoneFind out what package is calling the current function.. code python   assuming the call stack is     in package zig a function x, which calls     in package bar a function y, which calls     in package foo a function callfoo defined as  def callfoo()    asset.caller()      bar    asset.caller(ignorebar)      zig    asset.caller(ignore[bar, zig])      NoneCall all the plugins for a given group.. code python  for plugin in asset.plugins(mypackage.plugins)    plugin.handle()Filter an object through all the plugins for a given group (if thereare no plugins, this will simply return thing).. code python  result  asset.plugins(mypackage.plugins).filter(thing)Load all registered plugins, select the ones named foo and invokethem (this will fail if there is no foo plugin).. code python  result  asset.plugins(mypackage.plugins).select(foo).handle(thing)Chunk a file (or any file-like object) into 1 KiB chunks.. code python  with open(varbinarydata, rb) as fp    for chunk in asset.chunks(fp, 1024)       ... do something with chunk ...Chunk an Asset stream (here using the .chunks alias method).. code python  for chunk in asset.load(mypackagedata.bin).chunks()     ... using the default chunk size (usually 8 KiB) ...TestingIn order to run the unit tests correctly, the pxml package needs tobe installed as a zipped package (i.e. an egg) and the globrepackage needs to be installed unzipped. To accomplish that, do.. code bash   easy_install --zip-ok pxml   easy_install --always-unzip globreThe reason is that the unit tests confirm that asset can load assetsfrom both zipped and unzipped packages, and can also identify in whichmode it is operating.DetailsTODO add detailed docs... Asset.filename  If the asset represents a file on the filesystem, is the absolute  path to the specified file. Otherwise is None. AssetGroupStream.readline()  Returns the next line from the aggregate asset group stream, as if  the assets had been concatenate into a single asset.  IMPORTANT if an asset ends with content that is not terminated  by an EOL token, it is returned as-is, i.e. it does NOT append the  first line from the next asset.Note because asset.load() does lazy-loading, it only throws aNoSuchAsset exception when you actually attempt to use theAssetGroup If you need an immediate error, use the peek() method.Note that it returns itself, so you can do something like.. code python  import asset  def my_function_that_returns_an_iterable()    return asset.load(my_spec).peek()     this returns exactly the same thing as the following           return asset.load(my_spec)         but throws an exception early if there are no matching assets.</longdescription>
</pkgmetadata>