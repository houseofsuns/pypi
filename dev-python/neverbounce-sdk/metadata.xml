<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>NeverBounceApi-Python.. image httpsneverbounce-marketing.s3.amazonaws.comneverbounce_color_600px.png    target httpsneverbounce.com    width 600    align center    alt NeverBounce Logotravisci codeclimateWelcome to NeverBounces Python SDK  We hope that it will aid you in consumingour service.  Please report any bugs to the github issue tracker and make sureyou read the documentation before use.Installation------------The preferred method of installation is with pip in a virtual environment    pip install neverbounce_sdkIf you must use easy_install, you may.  If youd like to install for localdevelopment    git clone gitgithub.comNeverBounceNeverBounceApi-Python.git    cd NeverBounceApi-Python    pip install -e .This will install neverbounce_sdk into the active environment in editablemode.Usage-----    The API username and secret key used to authenticate V3 API requests will not work to authenticate V4 API requests. If you are attempting to authenticate your request with the 8 character username or 12-16 character secret key the request will return an auth_failure error. The API key used for the V4 API will look like the following secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx. To create new V4 API credentials please go [here](httpsapp.neverbounce.comappscustom-integrationnew).The NeverBounce Python SDK provides a simple interface by which to interactwith NeverBounces email verification API version 4.  To get up and running, make sureyou have your API token on hand    import neverbounce_sdk    api_key  secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx    client  neverbounce_sdk.client(api_keyapi_key,timeout30)And now youre ready to use the client.  You can check your accountinformation    info  client.account_info()     info is {billing_type default, credits 12345, ... }You can verify single emails    resp  client.single_check(testexample.com)    resp[result]               invalid    resp[execution_time]       285And you can create, query the status of, and control email verification bulkjobs    emails  [        {email tomatoveggies.com},   must have an email key        {email cucumberveggies.com,        best_when cold},              may contain metadata    ]    job  client.jobs_create(emails)     all state-changing methods return a status object    resp  client.jobs_parse(job[id], auto_startFalse)    assert resp[status]  success    client.jobs_start(job[id])    progress  client.jobs_status(job[id])    print(progress)   dict with keys job_status, started, percent_complete, etcWhen creating a job, you may attach metadata in the form of additional keysto each object (python dict) included in the job input listing.  Note thatthese additional keys will be broadcasted i.e., every row of the result setfor the job will contain an entry for every key - if the value of the key wasnot specified in the input, it will be the empty string in the job processingsoutput.All API operations return dictionaries with information about the execution ofthe operation or the results of the operation, whichever is more appropriate.The only exceptions are the client.search and client.results functions.The response generated by these API endpoints is paginated therefore thesefunctions return custom iterators that allow you to iterate across the APIspagination    all_my_jobs  client.jobs_search()    type(all_my_jobs)        neverbounce_sdk.bulk.ResultIter    for job in all_my_jobs         process job         this loop will make API calls behind the scenes, so be careful        if all_my_jobs.page  10            breakThe ResultIter will pull down pages behind the scenes, so be careful  AResultIter will expose the raw API response as a data attribute, thecurrent page number as page, and the total number of pages as total_pages,so you can use these attributes to implement finer-grained control over resultiteration.  Additionally, the methods raw_search and raw_results of theclient object will return the raw API response (this is the same as the dataattribute of the ResultIter object).Behind the scenes the client uses requests, and if you would like toexplicitly provide a requests.Session, you may do so    from requests import Session    api_key  secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx    session  Session()    client  neverbounce_sdk.client(api_keyapi_key, sessionsession)And all outgoing HTTP requests will be routed through the session objectsrequest method, taking advantage of requests.Sessions connection pooling.You may provide any custom object that provides a request interface with thesame signature as that provided by requests.Session and a close method.Finally, the client may be used a context manager.  If a session is provided,it will be used for all connections in the with block if not, a session willbe created.  Either way, a session associated with a client is alwaysclosed at the end of the context block.     with neverbounce_sdk.client() as client        client.api_key  secret_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx         the client creates a session behind the scenes        assert client.session is not None         do other stuff with the client     and then removes it at the end of the block    assert client.session is NoneSee Also--------Documentation for each function of the client object is available throughPythons built-in help function, e.g.     help(client.create)   brings up a ton of information about the create    ...                       functions arguments and optionsMany of the inputs and outputs of the client objects functions map fairlyclosely to NeverBounces raw v4 API, reading through the official APIdocshttpsdevelopers.neverbounce.comv4.0referenceaccount will bevaluable in conjunction with using the built-in online help... travisci image httpstravis-ci.orgNeverBounceNeverBounceApi-Python.svgbranchmaster    target httpstravis-ci.orgNeverBounceNeverBounceApi-Python    alt Build Status.. codeclimate image httpscodeclimate.comgithubNeverBounceNeverBounceApi-Pythonbadgesgpa.svg    target httpscodeclimate.comgithubNeverBounceNeverBounceApi-Python    alt Code Climate</longdescription>
</pkgmetadata>