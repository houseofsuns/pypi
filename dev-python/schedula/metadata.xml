<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>About schedulaschedula is a dynamic flow-based programming environment forpython, that handles automatically the control flow of the program.The control flow generally is represented by a Directed Acyclic Graph(DAG), where nodes are the operationsfunctions to be executed andedges are the dependencies between them.The algorithm of schedula dates back to 2014, when a colleagueasked for a method to automatically populate the missing data of adatabase. The imputation method chosen to complete the database was asystem of interdependent physical formulas - i.e., the inputs of aformula are the outputs of other formulas. The current library hasbeen developed in 2015 to support the design of the COsub2MPAStool httpsgithub.comJRCSTUCO2MPAS-TA_ - a COsub2 vehiclesimulatorhttpsjrcstu.github.ioco2mpasmodelurlhttpsjrcstu.github.ioco2mpasmodelcoreCO2MPAS_modelcalibrate_with_wltp_h.html_.During the developing phase, the physical formulas (more than 700)were known on the contrary of the software inputs and outputs.Why schedulaThe design of flow-based programs begins with the definition of thecontrol flow graph, and implicitly of its inputs and outputs. If theprogram accepts multiple combinations of inputs and outputs, you haveto design and code all control flow graphs. With normal schedulers, itcan be very demanding.While with schedula, giving whatever set of inputs, itautomatically calculates any of the desired computable outputs,choosing the most appropriate DAG from the dataflow execution model.Note The DAG is determined at runtime and it is extracted using the   shortest path from the provided inputs. The path is calculated   based on a weighted directed graph (dataflow execution model) with   a modified Dijkstra algorithm.schedula makes the code easy to debug, to optimize, and to presentit to a non-IT audience through its interactive graphs and charts. Itprovides the option to run a model asynchronously or in parallelmanaging automatically the Global Interpreter Lock (GIL), and toconvert a model into a web API service... _start-install-coreInstallationTo install it use (with root privileges).. code console    pip install schedulaor download the last git version and use (with root privileges).. code console    python setup.py install.. _end-install-coreInstall extrasSome additional functionality is enabled installing the followingextras  io enables to readwrite functions.  plot enables the plot of the Dispatcher model and workflow   (see plot()).  web enables to build a dispatcher Flask app (see web()).  sphinx enables the sphinx extension directives (i.e.,   autosummary and dispatcher).  parallel enables the parallel execution of Dispatcher model.To install schedula and all extras, do.. code console    pip install schedula[all]Note plot extra requires Graphviz. Make sure that the directory   containing the dot executable is on your systems path. If you   have not you can install it from its download page   httpswww.graphviz.orgdownload_.TutorialLets assume that we want develop a tool to automatically manage thesymmetric cryptography. The base idea is to open a file, read itscontent, encrypt or decrypt the data and then write them out to a newfile. This tutorial shows how to   1. define model-definition_ and execute dispatching_ a      dataflow execution model,   2. extract sub-model-extraction_ a sub-model, and   3. deploy api-server_ a web API service.Note You can find more examples, on how to use the schedula library,   into the folder examples   httpsgithub.comvinci1it2000schedulatreemasterexamples_.Model definitionFirst of all we start defining an empty Dispatcher namedsymmetric_cryptography that defines the dataflow execution model    import schedula as sh    dsp  sh.Dispatcher(namesymmetric_cryptography)There are two main ways to get a key, we can either generate a new oneor use one that has previously been generated. Hence, we can definethree functions to simply generate, save, and load the key. Toautomatically populate the model inheriting the arguments names, wecan use the decorator add_function() as follow    import os.path as osp    from cryptography.fernet import Fernet    sh.add_function(dsp, outputs[key], weight2)   ... def generate_key()   ...     return Fernet.generate_key().decode()    sh.add_function(dsp)   ... def write_key(key_fpath, key)   ...     with open(key_fpath, w) as f   ...         f.write(key)    sh.add_function(dsp, outputs[key], input_domainosp.isfile)   ... def read_key(key_fpath)   ...     with open(key_fpath) as f   ...         return f.read()Note Since Python does not come with anything that can encryptdecrypt   files, in this tutorial, we use a third party module named   cryptography. To install it execute pip install   cryptography.To encryptdecrypt a message, you will need a key as previouslydefined and your data encrypted or decrypted. Therefore, we candefine two functions and add them, as before, to the model    sh.add_function(dsp, outputs[encrypted])   ... def encrypt_message(key, decrypted)   ...     return Fernet(key.encode()).encrypt(decrypted.encode()).decode()    sh.add_function(dsp, outputs[decrypted])   ... def decrypt_message(key, encrypted)   ...     return Fernet(key.encode()).decrypt(encrypted.encode()).decode()Finally, to read and write the encrypted or decrypted message,according to the functional programming philosophy, we can reuse thepreviously defined functions read_key and write_key changingthe model mapping (i.e., function_id, inputs, and outputs). Toadd to the model, we can simply use the add_function method asfollow    dsp.add_function(   ...     function_idread_decrypted,   ...     functionread_key,   ...     inputs[decrypted_fpath],   ...     outputs[decrypted]   ... )   read_decrypted    dsp.add_function(   ...     read_encrypted, read_key, [encrypted_fpath], [encrypted],   ...     input_domainosp.isfile   ... )   read_encrypted    dsp.add_function(   ...     write_decrypted, write_key, [decrypted_fpath, decrypted],   ...     input_domainosp.isfile   ... )   write_decrypted    dsp.add_function(   ...     write_encrypted, write_key, [encrypted_fpath, encrypted]   ... )   write_encryptedNote For more details on how to create a Dispatcher see   add_data(), add_func(), add_function(),   add_dispatcher(), SubDispatch, MapDispatch,   SubDispatchFunction, SubDispatchPipe, and DispatchPipe.To inspect and visualize the dataflow execution model, you can simplyplot the graph as follow    dsp.plot()  [graph]Tip You can explore the diagram by clicking on it.DispatchingTo see the dataflow execution model in action and its workflow togenerate a key, to encrypt a message, and to write the encrypt data,you can simply invoke dispatch() or __call__() methods of thedsp import tempfile tempdir  tempfile.mkdtemp() message  secret message sol  dsp(inputsdict(...     decryptedmessage,...     encrypted_fpathosp.join(tempdir, data.secret),...     key_fpathosp.join(tempdir,key.key)... )) sol.plot(indexTrue)  [graph]Note As you can see from the workflow graph (orange nodes), when some   functions inputs does not respect its domain, the Dispatcher   automatically finds an alternative path to estimate all computable   outputs. The same logic applies when there is a function failure.Now to decrypt the data and verify the message without saving thedecrypted message, you just need to execute again the dsp changingthe inputs and setting the desired outputs. In this way, thedispatcher automatically selects and executes only a sub-part of thedataflow execution model. dsp(...     inputssh.selector((encrypted_fpath, key_fpath), sol),...     outputs[decrypted]... )[decrypted]  messageTrueIf you want to visualize the latest workflow of the dispatcher, youcan use the plot() method with the keyword workflowTrue dsp.plot(workflowTrue, indexTrue)  [graph]</longdescription>
</pkgmetadata>