<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>This is the Zephyr RTOS meta tool, west.httpsdocs.zephyrproject.orglatestguideswestindex.htmlInstallation------------Using pip  pip3 install west(Use pip3 uninstall west to uninstall it.)Basic Usage-----------West lets you manage multiple Git repositories under a single directory using asingle file, called the west manifest file, or manifest for short.By default the manifest file is named west.yml.You use west init to set up this directory, then west update to fetchandor update the repositories named in the manifest.By default, west uses upstream Zephyrs manifest filehttpsgithub.comzephyrproject-rtoszephyrblobmainwest.yml_, but westdoesnt care if the manifest repository is a Zephyr tree or not.For more details, see Multiple Repository Managementhttpsdocs.zephyrproject.orglatestguideswestrepo-tool.html_ in thewest documentation.Example usage using the upstream manifest file  mkdir zephyrproject  cd zephyrproject  west init  west updateWhat just happened- west init clones the upstream west manifest repository, which in this  case is the zephyr repository. The manifest repository contains west.yml,  a YAML description of the Zephyr installation, including Git repositories and  other metadata.- west update clones the other repositories named in the manifest file,  creating working trees in the installation directory zephyrproject.Use west init -m to specify another manifest repository. Use --mr touse a revision to inialize from if not given, the remotes default branch is used.Use --mf to use a manifest file other than west.yml.Additional Commands-------------------West has multiple sub-commands. After running west init, you canrun them from anywhere under zephyrproject.For a list of available commands, run west -h. Get help on acommand with west command -h.West is extensible you can add new commands to west without modifying itssource code. See Extensionshttpsdocs.zephyrproject.orglatestguideswestextensions.html_ in thedocumentation for details.Running the Tests-----------------First, install tox   macOS, Windows  pip3 install tox   Linux  pip3 install --user toxThen, run the test suite locally from the top level directory  toxSee the tox configuration file, tox.ini, for more details.Hacking on West---------------Installing from SourceThe wheel_ package is required to install west from source. See InstallingWheel below if you dont have wheel installed.To build the west wheel file   macOS, Linux  python3 setup.py bdist_wheel   Windows  py -3 setup.py bdist_wheelThis will create a file named distwest-x.y.z-py3-none-any.whl,where x.y.z is the current version in setup.py.To install the wheel  pip3 install -U distwest-x.y.z-py3-none-any.whlYou can pip3 uninstall west to remove this wheel before re-installing theversion from PyPI, etc.Editable InstallTo run west live from the current source code tree, run this command from thetop level directory in the west repository  pip3 install -e .This is useful if you are actively working on west and dont want to re-packageand install a wheel each time you run it.Installing WheelOn macOS and Windows, you can install wheel with  pip3 install wheelThat also works on Linux, but you may want to install wheel from yoursystem package manager instead -- e.g. if you installed pip from yoursystem package manager. The wheel package is likely named somethinglike python3-wheel in that case... _wheel httpswheel.readthedocs.ioenlatest</longdescription>
</pkgmetadata>