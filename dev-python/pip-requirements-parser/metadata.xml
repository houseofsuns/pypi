<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pip-requirements-parser - a mostly correct pip requirements parsing libraryCopyright (c) nexB Inc. and others.Copyright (c) The pip developers (see AUTHORS.rst file)SPDX-License-Identifier MITHomepage httpsgithub.comnexBpip-requirements and httpswww.aboutcode.orgpip-requirements-parser is a mostly correct pip requirements parsinglibrary ... because it uses pips own codepip is the package installer for Python that is using requirements textfiles listing the packages to install.Per httpspip.pypa.ioenstablereferencerequirements-file-format     The requirements file format is closely tied to a number of internal    details of pip (e.g., pips command line options). The basic format is    relatively stable and portable but the full syntax, as described here,    is only intended for consumption by pip, and other tools should take    that into account before using it for their own purposes.And per httpspip.pypa.ioenstableuser_guideusing-pip-from-your-program     [..] pip is a command line program. While it is implemented in Python, and    so is available from your Python code via import pip, you must not use pips    internal APIs in this way.        What this means in practice is that everything inside of pip is considered    an implementation detail. Even the fact that the import name is pip is    subject to change without notice. While we do try not to break things as    much as possible, all the internal APIs can change at any time, for any    reason. It also means that we generally wont fix issues that are a result    of using pip in an unsupported way.Because of all this, pip requirements are notoriously difficult to parse rightin all their diversity because- pip does not have a public API and therefore cannot be reliably used as a  stable library. Some libraries attempt to do this though. (See Alternative)- The pip requirements file syntax is closely aligned with pips command line  interface and command line options. In some ways a pip requirements file is a  list of pip command line options and arguments. Therefore, it is hard to parse  these short of reproducing the pip command line options parsing. At least one  other library is using a command line option parser to parse options correctly.This pip-requirements-parser Python library is yet another pip requirementsfiles parser, but this time doing it hopefully correctly and doing as well aspip does it, because this is using pips own code.The pip-requirements-parser library offers these key advantages- Other requirements parsers typically do not work in all the cases that pip  supports parsing any requirement as seen in the wild will fail parsing some  valid pip requirements. Since the pip-requirements-parser library is based  on pips own code, it works exactly like pip and will parse all the  requirements files that pip can parse.- The pip-requirements-parser library offers a simple and stable code API  that will not change without notice.- The pip-requirements-parser library is designed to work offline without  making any external network call, while the original pip code needs network  access.- The pip-requirements-parser library is a single file that can easily be  copied around as needed for easy vendoring. This is useful as requirements  parsing is often needed to bootstrap in a constrained environment.- The pip-requirements-parser library has only one external dependency on  the common packaging package. Otherwise it uses only the standard library.  The benefits are the same as being a single file fewer moving parts helps with  using it in more cases.- The pip-requirements-parser library reuses and passes the full subset of  the pip test suite that deals with requirements. This is a not really  surprising since this is pips own code. The suite suite has been carefully  ported and adjusted to work with the updated code subset.- The standard pip requirements parser depends on the requests HTTP library  and makes network connection to PyPI and other referenced repositories when  parsing. The pip-requirements-parser library works entirely offline and the  requests dependency and calling has been entirely removed.- The pip-requirements-parser library has preserved the complete pip git  history for the subset of the code we kept. The original pip code was merged  from multiple modules keeping all the git history at the lineblame level using  some git fu and git filter repo. The benefit is that we will be able to more  easily track and merge future pip updates.- The pip-requirements-parser library has an extensive test suite  made of  - pips own tests  - new unit tests  - new requirements test files (over 40 new test files)  - the tests suite of some of the main other requirement parsers including     - httpgithub.comdipip-api     - httpsgithub.compyupiodparse     - httpsgithub.comlandscapeiorequirements-detector     - httpsgithub.commadpahrequirements-parserAs a result, it has likely the most comprehensive requiremente parsing testsuite around.UsageThe entry point is the RequirementsFile object     from pip_requirements_parser import RequirementsFile     rf  RequirementsFile.from_file(requirements.txt)From there, you can dump to a dict     rf.to_dict()Or access the requirements (either InstallRequirement or EditableRequirementobjects)     for req in rf.requirements    ...    print(req.to_dict())    ...    print(req.dumps())And the various other parsed elements such as options, commenst and invalid linesthat have a parsing error     rf.options     rf.comment_lines     rf.invalid_linesEach of these and the requirements hahve a requirement_line attributewith the original text.Finally you can get a requirements file back as a string     rf.dumps()Alternative------------------There are several other parsers that either- Implement their own parsing and can therefore miss some subtle differences- Or wrap and import pip as a library, working around the lack of pip APINone of these use the approach of reusing and forking the subset of pip that isneeded to parse requirements.  The ones that wrap pip require network accesslike pip does. They potentially need updating each time there is a new piprelease. The ones that reimplement pip parsing may not support all pipspecifics.Implement a new pip parser- pip-api httpsgithub.comdipip-api does not support hashes and certain pip options.  It does however use argparse for parsing options and is therefore correctly  handling most options. The parser is a single script that only depends on  packaging (that is vendored). It is not designed to be used as a single script  though and pip is a dependency.- requirements-parser httpsgithub.commadpahrequirements-parse does not  support hashes and certain pip options- dparse httpsgithub.compyupiodparse- httpsgithub.comGoogleCloudPlatformdjango-cloud-deployblobd316b1e45357761e2b124143e6e12ce34ef6f975django_cloud_deployskeletonrequirements_parser.pyReuse and wrap pips own parser- requirementslib httpsgithub.comsarugakurequirementslib uses pip-shim  httpsgithub.comsarugakupip-shims which is a set of shims around each  pip versions in an attempt to offer an API to pip. Comes with 20+ dependencies,- micropipenv httpsgithub.comthoth-stationmicropipenvblobd0c37c1bf0aadf5149aebe2df0bf1cb12ded4c40micropipenv.pyL53- pip-tools httpsgithub.comjazzbandpip-toolsblob9e1be05375104c56e07cdb0904e1b50b86f8b550piptools_compatpip_compat.py</longdescription>
</pkgmetadata>