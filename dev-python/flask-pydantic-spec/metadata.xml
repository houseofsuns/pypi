<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> Flask Pydantic SpecA library to make it easy to add OpenAPI documentation to your Flask app, and validate the requests using [Pydantic](httpsgithub.comsamuelcolvinpydantic).This library began as a fork of [Spectree](httpsgithub.com0b01001001spectree), but as we made changes we thought other people might be interested in our approach. Features Less boilerplate code, only annotations, no need for YAML sparkles Generate API document with [Redoc UI](httpsgithub.comRedoclyredoc) or [Swagger UI](httpsgithub.comswagger-apiswagger-ui) yum Validate query, JSON data, response data with [pydantic](httpsgithub.comsamuelcolvinpydantic) wink Has support for requestresponse types other than JSON. Quick Startinstall with pip pip install flask-pydantic-spec ExamplesCheck the [examples](examples) folder. [flask example](examplesflask_demo.py) Step by Step1. Define your data structure used in (query, json, headers, cookies, resp) with pydantic.BaseModel2. create flask_pydantic_spec.Validator instance with the web framework name you are using, like api  Validator(flask)3. api.validate decorate the route with    query    body    headers    cookies    resp    tags4. access this data with context(query, body, headers, cookies) (of course, you can access these from the original place where the framework offered)    flask request.context5. register to the web application api.register(app)6. check the document at URL location apidocredoc or apidocswaggerIf the request doesnt pass the validation, it will return a 422 with JSON error message(ctx, loc, msg, type). How To How to add summary and description to endpointsJust add docs to the endpoint function. The 1st line is the summary, and the rest is the description for this endpoint. How to add description to parametersCheck the [pydantic](httpspydantic-docs.helpmanual.iousageschema) docs about description in Field. Any config I can changeOf course. Check the [config](httpsflask-pydantic-spec.readthedocs.ioenlatestconfig.html) document.You can update the config when you init the validator like pyfrom flask_pydantic_spec import FlaskPydanticSpecFlaskPydanticSpec(flask, titleDemo API, versionv1.0, pathdoc) What is a Response and how to use itTo build a response for the endpoint, you need to declare the status code with format HTTP_{code} and corresponding data (optional).pyfrom flask_pydantic_spec import ResponseResponse(HTTP_200None, HTTP_403ForbidModel)Response(HTTP_200)  equals to Response(HTTP_200None) What should I return when Im using the libraryNo need to change anything. Just return what the framework required. How to logging when the validation failedValidation errors are logged with INFO level. Details are passed into extra. How can I change the response when there is a validation error Can I record some metricsThis library provides before and after hooks to do these. Check the [doc](httpsflask-pydantic-spec.readthedocs.ioenlatest) or the [test case](teststest_plugin_flask.py). You can change the handlers for Flask-Pydantic-Spec or for a specific endpoint validation. DemoTry it with http post 8000apiuser namealice age18. (if you are using httpie) Flaskpyfrom flask import Flask, request, jsonifyfrom pydantic import BaseModel, Field, constrfrom flask_pydantic_spec import FlaskPydanticSpec, Response, Requestclass Profile(BaseModel)    name constr(min_length2, max_length40)  Constrained Str    age int  Field(        ...,        gt0,        lt150,        descriptionuser age(Human)    )    class Config        schema_extra  {             provide an example            example {                name very_important_user,                age 42,            }        }class Message(BaseModel)    text strapp  Flask(__name__)api  FlaskPydanticSpec(flask)app.route(apiuser, methods[POST])api.validate(bodyRequest(Profile), respResponse(HTTP_200Message, HTTP_403None), tags[api])def user_profile()        verify user profile (summary of this endpoint)    users name, users age, ... (long description)        print(request.context.json)  or request.json    return jsonify(textit works)if __name__  __main__    api.register(app)  if you dont register in api init step    app.run(port8000) FAQ ValidationError missing field for headersThe HTTP headers keys in Flask are capitalized.You can use [pydantic.root_validators(preTrue)](httpspydantic-docs.helpmanual.iousagevalidatorsroot-validators) to change all the keys into lower cases or upper cases. ValidationError value is not a valid list for querySince there is no standard for HTTP query with multiple values, its hard to find the way to handle this for different web frameworks. So I suggest not to use list type in query until I find a suitable way to fix it.</longdescription>
</pkgmetadata>