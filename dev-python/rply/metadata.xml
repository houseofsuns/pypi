<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>RPLY.. image httpssecure.travis-ci.orgalexrply.png    target httpstravis-ci.orgalexrplyWelcome to RPLY A pure Python parser generator, that also works with RPython.It is a more-or-less direct port of David Beazleys awesome PLY, with a newpublic API, and RPython support.You can find the documentation online_.Basic API.. code python    from rply import ParserGenerator, LexerGenerator    from rply.token import BaseBox    lg  LexerGenerator()     Add takes a rule name, and a regular expression that defines the rule.    lg.add(PLUS, r+)    lg.add(MINUS, r-)    lg.add(NUMBER, rd+)    lg.ignore(rs+)     This is a list of the token names. precedence is an optional list of     tuples which specifies order of operation for avoiding ambiguity.     precedence must be one of left, right, nonassoc.     cache_id is an optional string which specifies an ID to use for     caching. It should always be safe to use caching,     RPly will automatically detect when your grammar is     changed and refresh the cache for you.    pg  ParserGenerator([NUMBER, PLUS, MINUS],            precedence[(left, [PLUS, MINUS])], cache_idmyparser)    pg.production(main  expr)    def main(p)         p is a list, of each of the pieces on the right hand side of the         grammar rule        return p[0]    pg.production(expr  expr PLUS expr)    pg.production(expr  expr MINUS expr)    def expr_op(p)        lhs  p[0].getint()        rhs  p[2].getint()        if p[1].gettokentype()  PLUS            return BoxInt(lhs + rhs)        elif p[1].gettokentype()  MINUS            return BoxInt(lhs - rhs)        else            raise AssertionError(This is impossible, abort the time machine)    pg.production(expr  NUMBER)    def expr_num(p)        return BoxInt(int(p[0].getstr()))    lexer  lg.build()    parser  pg.build()    class BoxInt(BaseBox)        def __init__(self, value)            self.value  value        def getint(self)            return self.valueThen you can do.. code python    parser.parse(lexer.lex(1 + 3 - 2+12-32))You can also substitute your own lexer. A lexer is an object with a next()method that returns either the next token in sequence, or None if the tokenstream has been exhausted.Why do we have the boxes-------------------------In RPython, like other statically typed languages, a variable must have aspecific type, we take advantage of polymorphism to keep values in a box sothat everything is statically typed. You can write whatever boxes you need foryour project.If you dont intend to use your parser from RPython, and just want a cool purePython parser you can ignore all the box stuff and just return whatever youlike from each production method.Error handling--------------By default, when a parsing error is encountered, an rply.ParsingError israised, it has a method getsourcepos(), which returns anrply.token.SourcePosition object.You may also provide an error handler, which, at the moment, must raise anexception. It receives the Token object that the parser errored on... code python    pg  ParserGenerator(...)    pg.error    def error_handler(token)        raise ValueError(Ran into a s where it wasnt expected  token.gettokentype())Python compatibility--------------------RPly is tested and known to work under Python 2.7, 3.4+, and PyPy. It isalso valid RPython for PyPy checkouts from 6c642ae7a0ea onwards.Links----- Source code and issue tracker httpsgithub.comalexrply_ PyPI releases httpspypi.python.orgpypirply_ Talk at PyCon US 2013 So you want to write an interpreter httppyvideo.orgvideo1694so-you-want-to-write-an-interpreter_.. _online httpsrply.readthedocs.io</longdescription>
</pkgmetadata>