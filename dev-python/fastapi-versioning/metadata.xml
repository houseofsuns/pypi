<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> fastapi-versioningapi versioning for fastapi web applications Installationpip install fastapi-versioning Examplespythonfrom fastapi import FastAPIfrom fastapi_versioning import VersionedFastAPI, versionapp  FastAPI(titleMy App)app.get(greet)version(1, 0)def greet_with_hello()    return Helloapp.get(greet)version(1, 1)def greet_with_hi()    return Hiapp  VersionedFastAPI(app)this will generate two endpointsv1_0greetv1_1greetas well asdocsv1_0docsv1_1docsv1_0openapi.jsonv1_1openapi.jsonTheres also the possibility of adding a set of additional endpoints thatredirect the most recent API version. To do that make the argumentenable_latest truepythonapp  VersionedFastAPI(app, enable_latestTrue)this will generate the following additional endpointslatestgreetlatestdocslatestopenapi.jsonIn this example, latest endpoints will reflect the same data as v1.1.Try it outshpip install pipenvpipenv install --devpipenv run uvicorn example.annotation.appapp pipenv run uvicorn example.folder_name.appapp Usage without minor versionpythonfrom fastapi import FastAPIfrom fastapi_versioning import VersionedFastAPI, versionapp  FastAPI(titleMy App)app.get(greet)version(1)def greet()  return Helloapp.get(greet)version(2)def greet()  return Hiapp  VersionedFastAPI(app,    version_format{major},    prefix_formatv{major})this will generate two endpointsv1greetv2greetas well asdocsv1docsv2docsv1openapi.jsonv2openapi.json Extra FastAPI constructor argumentsIts important to note that only the title from the original FastAPI will beprovided to the VersionedAPI app. If you have any middleware, event handlersetc these arguments will also need to be provided to the VersionedAPI functioncall, as in the example belowpythonfrom fastapi import FastAPI, Requestfrom fastapi_versioning import VersionedFastAPI, versionfrom starlette.middleware import Middlewarefrom starlette.middleware.sessions import SessionMiddlewareapp  FastAPI(    titleMy App,    descriptionGreet uses with a nice message,    middleware[        Middleware(SessionMiddleware, secret_keymysecretkey)    ])app.get(greet)version(1)def greet(request Request)    request.session[last_version_used]  1    return Helloapp.get(greet)version(2)def greet(request Request)    request.session[last_version_used]  2    return Hiapp.get(version)def last_version(request Request)    return fYour last greeting was sent from version {request.session[last_version_used]}app  VersionedFastAPI(app,    version_format{major},    prefix_formatv{major},    descriptionGreet users with a nice message,    middleware[        Middleware(SessionMiddleware, secret_keymysecretkey)    ])</longdescription>
</pkgmetadata>