<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>RQ SchedulerRQ Scheduler httpsgithub.comrqrq-scheduler_ is a small package thatadds job scheduling capabilities to RQ httpsgithub.comnvierq_,a Redis httpredis.io_ based Python queuing library... image httpstravis-ci.orgrqrq-scheduler.svgbranchmaster    target httpstravis-ci.orgrqrq-schedulerSupport RQ SchedulerIf you find rq-scheduler useful, please consider supporting its development via Tidelift httpstidelift.comsubscriptionpkgpypi-rq_schedulerutm_sourcepypi-rq-schedulerutm_mediumreferralutm_campaignreadme_.Requirements RQ_InstallationYou can install RQ Scheduler_ via pip    pip install rq-schedulerOr you can download the latest stable package from PyPI httppypi.python.orgpypirq-scheduler_.UsageSchedule a job involves doing two different things1. Putting a job in the scheduler2. Running a scheduler that will move scheduled jobs into queues when the time comes----------------Scheduling a Job----------------There are two ways you can schedule a job. The first is using RQ Schedulers enqueue_at.. code-block python    from redis import Redis    from rq import Queue    from rq_scheduler import Scheduler    from datetime import datetime    scheduler  Scheduler(connectionRedis())  Get a scheduler for the default queue    scheduler  Scheduler(foo, connectionRedis())  Get a scheduler for the foo queue     You can also instantiate a Scheduler using an RQ Queue    queue  Queue(bar, connectionRedis())    scheduler  Scheduler(queuequeue)     Puts a job into the scheduler. The API is similar to RQ except that it     takes a datetime object as first argument. So for example to schedule a     job to run on Jan 1st 2020 we do    scheduler.enqueue_at(datetime(2020, 1, 1), func)  Date time should be in UTC     Heres another example scheduling a job to run at a specific date and time (in UTC),     complete with args and kwargs.    scheduler.enqueue_at(datetime(2020, 1, 1, 3, 4), func, foo, barbaz)     You can choose the queue type where jobs will be enqueued by passing the name of the type to the scheduler     used to enqueue    scheduler  Scheduler(foo, queue_classrq.Queue)    scheduler.enqueue_at(datetime(2020, 1, 1), func)  The job will be enqueued at the queue named foo using the queue type rq.QueueThe second way is using enqueue_in. Instead of taking a datetime object,this method expects a timedelta and schedules the job to run atX secondsminuteshoursdaysweeks later. For example, if we want to monitor howpopular a tweet is a few times during the course of the day, we could do something like.. code-block python    from datetime import timedelta     Schedule a job to run 10 minutes, 1 hour and 1 day later    scheduler.enqueue_in(timedelta(minutes10), count_retweets, tweet_id)    scheduler.enqueue_in(timedelta(hours1), count_retweets, tweet_id)    scheduler.enqueue_in(timedelta(days1), count_retweets, tweet_id)IMPORTANT You should always use UTC datetime when working with RQ Scheduler_.------------------------Periodic  Repeated Jobs------------------------As of version 0.3, RQ Scheduler_ also supports creating periodic and repeated jobs.You can do this via the schedule method. Note that this feature needsRQ_  0.3.1.This is how you do it.. code-block python    scheduler.schedule(        scheduled_timedatetime.utcnow(),  Time for first execution, in UTC timezone        funcfunc,                      Function to be queued        args[arg1, arg2],              Arguments passed into function when executed        kwargs{foo bar},          Keyword arguments passed into function when executed        interval60,                    Time before the function is called again, in seconds        repeat10,                      Repeat this number of times (None means repeat forever)        meta{foo bar}             Arbitrary pickleable data on the job itself    )IMPORTANT NOTE If you set up a repeated job, you must make sure that youeither do not set a result_ttl value or you set a value larger than the interval.Otherwise, the entry with the job details will expire and the job will not get re-scheduled.------------------------Cron Jobs------------------------As of version 0.6.0, RQ Scheduler_ also supports creating Cron Jobs, which you can use forrepeated jobs to run periodically at fixed times, dates or intervals, for more info checkhttpsen.wikipedia.orgwikiCron. You can do this via the cron method.This is how you do it.. code-block python    scheduler.cron(        cron_string,                 A cron string (e.g. 0 0   0)        funcfunc,                   Function to be queued        args[arg1, arg2],           Arguments passed into function when executed        kwargs{foo bar},       Keyword arguments passed into function when executed        repeat10,                   Repeat this number of times (None means repeat forever)        result_ttl300               Specify how long (in seconds) successful jobs and their results are kept. Defaults to -1 (forever)        ttl200                      Specifies the maximum queued time (in seconds) before its discarded. Defaults to None (infinite TTL).        queue_namequeue_name,       In which queue the job should be put in        meta{foo bar},         Arbitrary pickleable data on the job itself        use_local_timezoneFalse     Interpret hours in the local timezone    )-------------------------Retrieving scheduled jobs-------------------------Sometimes you need to know which jobs have already been scheduled. You can get alist of enqueued jobs with the get_jobs method.. code-block python    list_of_job_instances  scheduler.get_jobs()In its simplest form (as seen in the above example) this method returns a listof all job instances that are currently scheduled for execution.Additionally the method takes two optional keyword arguments until andwith_times. The first one specifies up to which point in time scheduled jobsshould be returned. It can be given as either a datetime  timedelta instanceor an integer denoting the number of seconds since epoch (1970-01-01 000000).The second argument is a boolean that determines whether the scheduled executiontime should be returned along with the job instances.Example.. code-block python     get all jobs until 2012-11-30 100000    list_of_job_instances  scheduler.get_jobs(untildatetime(2012, 10, 30, 10))     get all jobs for the next hour    list_of_job_instances  scheduler.get_jobs(untiltimedelta(hours1))     get all jobs with execution times    jobs_and_times  scheduler.get_jobs(with_timesTrue)     returns a list of tuples     [(rq.job.Job object at 0x123456789, datetime.datetime(2012, 11, 25, 12, 30)), ...]------------------------------Checking if a job is scheduled------------------------------You can check whether a specific job instance or job id is scheduled forexecution using the familiar python in operator.. code-block python    if job_instance in scheduler         Do something     or    if job_id in scheduler         Do something---------------Canceling a job---------------To cancel a job, simply pass a Job or a job id to scheduler.cancel.. code-block python    scheduler.cancel(job)Note that this method returns None whether the specified job was found or not.---------------------Running the scheduler---------------------RQ Scheduler_ comes with a script rqscheduler that runs a schedulerprocess that polls Redis once every minute and move scheduled jobs to therelevant queues when they need to be executed.. code-block bash     This runs a scheduler process using the default Redis connection    rqschedulerIf you want to use a different Redis server you could also do.. code-block bash    rqscheduler --host localhost --port 6379 --db 0The script accepts these arguments -H or --host Redis server to connect to -p or --port port to connect to -d or --db Redis db to use -P or --password password to connect to Redis -b or --burst runs in burst mode (enqueue scheduled jobs whose execution time is in the past and quit) -i INTERVAL or --interval INTERVAL How often the scheduler checks for new jobs to add to the queue (in seconds, can be floating-point for more precision). -j or --job-class specify custom job class for rq to use (python module.Class) -q or --queue-class specify custom queue class for rq to use (python module.Class)The arguments pull default values from environment variables with thesame names but with a prefix of RQ_REDIS_.Running the Scheduler as a Service on Ubuntu--------------------------------------------sudo etcsystemdsystemrqscheduler.service.. code-block bash    [Unit]    DescriptionRQScheduler    Afternetwork.target    [Service]    ExecStarthomeUser.virtualenvsYourVirtualEnvbinpython         homeUser.virtualenvsYourVirtualEnvlibYourPythonVersionsite-packagesrq_schedulerscriptsrqscheduler.py    [Install]    WantedBymulti-user.targetYou will also want to add any command line parameters if your configuration is not localhost or not set in the environment variables.Start, check Status and Enable the service.. code-block bash    sudo systemctl start rqscheduler.service    sudo systemctl status rqscheduler.service    sudo systemctl enable rqscheduler.service---------------------------Running Multiple Schedulers---------------------------Multiple instances of the rq-scheduler can be run simultaneously. It allows for Reliability (no single point of failure) Failover (scheduler instances automatically retry to attain lock and schedule jobs) Running scheduler on multiple server instances to make deployment identical and easierMultiple schedulers can be run in any way you want. Typically youll only want to run one scheduler per serverinstance... code-block bash   rqscheduler -i 5    another shellsystemd service or ideally another server   rqscheduler -i 5    different parameters can be provided to different schedulers   rqscheduler -i 10Practical example- scheduler_a is running on ec2_instance_a- If scheduler_a crashes or ec2_instance_a goes down, then our tasks wont be scheduled at all- Instead we can simply run 2 schedulers. Another scheduler called scheduler_b can be run on ec2_instance_b- Now both scheduler_a and scheduler_b will periodically check and schedule the jobs- If one fails, the other still worksYou can read more about multiple schedulers in 212 httpsgithub.comrqrq-schedulerpull212_ and 195 httpsgithub.comrqrq-schedulerissues195_</longdescription>
</pkgmetadata>