<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription># RequestsToCurlConvert python requests object to cURL command.This repository is improved based on [curlify](https://github.com/ofw/curlify). Since curlify does not seem to be updated anymore, no one responded to the PR I provided, so this repository was created.## Installation```shpip install requests_to_curl```## Usage```py&gt;&gt;&gt; import curl&gt;&gt;&gt; import requests&gt;&gt;&gt;&gt;&gt;&gt; response = requests.get(&quot;https://lulaolu.com&quot;)&gt;&gt;&gt;&gt;&gt;&gt; curl.parse(response)  # stdoutcurl -X GET -H 'Accept: */*' -H 'Accept-Encoding: gzip, deflate' -H 'Connection: keep-alive' -H 'User-Agent: python-requests/2.27.1' https://lulaolu.com:443/```If you want to get the curl string in the code project, use `return_it````python3&gt;&gt;&gt; c = curl.parse(response, return_it=True)&gt;&gt;&gt; print(c)```If you want to hide stdout, use `print_it=False````python3&gt;&gt;&gt; c = curl.parse(response, print_it=False)```You can also use `r2c` or `requests_to_curl` instead of `curl````python3&gt;&gt;&gt; import r2c&gt;&gt;&gt; r2c.parse(response)  # ok!&gt;&gt;&gt;&gt;&gt;&gt; import requests_to_curl&gt;&gt;&gt; requests_to_curl.parse(response)  # ok!```</longdescription>
</pkgmetadata>