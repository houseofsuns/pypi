<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>Python Tokens.. image httpstravis-ci.orgzalando-stupspython-tokens.svgbranchmaster   target httpstravis-ci.orgzalando-stupspython-tokens   alt Build Status.. image httpscoveralls.ioreposzalando-stupspython-tokensbadge.svg   target httpscoveralls.iorzalando-stupspython-tokens   alt Code Coverage.. image httpsimg.shields.iopypidwstups-tokens.svg   target httpspypi.python.orgpypistups-tokens   alt PyPI Downloads.. image httpsimg.shields.iopypivstups-tokens.svg   target httpspypi.python.orgpypistups-tokens   alt Latest PyPI version.. image httpsimg.shields.iopypilstups-tokens.svg   target httpspypi.python.orgpypistups-tokens   alt LicenseA Python library that keeps OAuth 2.0 service access tokens in memory for your usage.Installation.. code-block bash     sudo pip3 install --upgrade stups-tokensUsage.. code-block python    import requests    import time    import tokens     will use OAUTH2_ACCESS_TOKEN_URL environment variable by default     will try to read application credentials from CREDENTIALS_DIR    tokens.configure(urlhttpsexample.comaccess_tokens)    tokens.manage(example, [read, write])    tokens.start()    tok  tokens.get(example)    requests.get(httpsexample.org, headers{Authorization Bearer {}.format(tok)})    time.sleep(3600)  make the token expire    tok  tokens.get(example)  will refresh the expired token    requests.get(httpsexample.org, headers{Authorization Bearer {}.format(tok)})This library also allows reading tokens directly from a file. The token needs to be in a file name {CREDENTIALS_DIR}{TOKEN_NAME}-secret.. code-block python    import tokens     the environment variable CREDENTIALS_DIR must be set correctly    tokens.configure(from_file_onlyTrue)    tokens.manage(full-access)    tok  tokens.get(full-access)    requests.get(httpsexample.org, headers{Authorization Bearer {}.format(tok)})Local testingThe tokens library allows injecting fixed OAuth2 access tokens via the OAUTH2_ACCESS_TOKENS environment variable.This allows testing applications using the library locally with personal OAuth2 tokens (e.g. generated by zign).. code-block bash     MY_TOKEN(zign token -n mytok)     export OAUTH2_ACCESS_TOKENSmytokMY_TOKEN     .myapp.py  start my local Python app using the tokens libraryReleasingUploading a new version to PyPI.. code-block bash     .release.sh NEW-VERSION</longdescription>
</pkgmetadata>