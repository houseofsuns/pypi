<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>django-relativedelta.. image httpstravis-ci.orgCodeYellowBVdjango-relativedelta.svgbranchmaster   target httpstravis-ci.orgCodeYellowBVdjango-relativedeltaA Django field for the dateutil.relativedelta.relativedelta httpdateutil.readthedocs.ioenstablerelativedelta.html_ class,which conveniently maps to the PostgreSQL INTERVAL type httpswww.postgresql.orgdocscurrentstaticdatatype-datetime.htmlDATATYPE-INTERVAL-INPUT_.The standard Django DurationField httpsdocs.djangoproject.comen1.10refmodelsfieldsdurationfield_maps to Pythons datetime.timedelta httpsdocs.python.org3librarydatetime.htmldatetime.timedelta_, whichhas support for days and weeks, but not for years and months.  And if you try to read an INTERVAL that containsmonths anyway, information is lost because each month gets converted to 30 days.For compatibility, a VARCHAR field is used on other databases.  Thisuses a custom relativedelta representation.  However, this means thattrue in-database interval operations are not supported in thesedatabases.  Sorting and comparing between two relativedelta fields ora relativedelta field and a fixed relativedelta value is supported,however.You should use this package when you need to store payment intervals(which tend to be monthly or quarterly), publication intervals (whichcan be weekly but also monthly) and so on, or when you simply dontknow what the intervals are going to be and want to offer someflexibility.If you want to use more advanced recurring dates, you should considerusing django-recurrence httpsgithub.comdjango-recurrencedjango-recurrence_instead.  This maps to the dateutil.rrule.rrule httpdateutil.readthedocs.ioenstablerrule.html_class, but it doesnt use native database field types, so you cantperform arithmetic on them within the database.Usage-----Using the field is straightforward.  You can add the field to yourmodel like so.. code python    from django.db import models    from relativedeltafield import RelativeDeltaField    class MyModel(models.Model)      rdfieldRelativeDeltaField()Then later, you can use it.. code python    from dateutil.relativedelta import relativedelta    rd  relativedelta(months2,days1,hours6)    my_model  MyModel(rdfieldrd)    my_model.save()Or, alternatively, you can use a string with theISO8601 format with designators time interval syntax httpswww.postgresql.orgdocscurrentstaticdatatype-datetime.htmlDATATYPE-INTERVAL-INPUT_.. code python    from dateutil.relativedelta import relativedelta    my_model  MyModel(rdfieldP2M1DT6H)    my_model.save()For convenience, a standard Python datetime.timedelta object isalso accepted.. code python    from datetime import timedelta    td  timedelta(days62,hours6)    my_model  MyModel(rdfieldtd)    my_model.save()After a full_clean(), the object will always be converted to a_normalized_ relativedelta instance.  It is highly recommendedyou use the django-fullclean httpsgithub.comfish-balldjango-fullclean_app to always force full_clean() on save(), so you can besure that after a save(), your fields are both normalizedand validated.Limitations and pitfalls------------------------Because this field is backed by an INTERVAL column, it neithersupports the relative weekday, leapdays, yearday andnlyearday arguments, nor the absolute arguments year,month, day, hour, second and microsecond.The microseconds field is converted to a fractional secondsvalue, which might lead to some precision loss due to floating-pointrepresentation.The weeks field is virtual, being derived from the multiple of 7days.  Thus, any week value in the input interval specification isconverted to days and added to the days field of the interval.When serializing back to a string, weeks will never be written.Similarly, if the interval contains a multiple of 7 days, you can readthis back out from the weeks property.Support for databases other than PostgreSQL is limited.For consistency reasons, when a relativedelta object is assigned to aRelativeDeltaField, it automatically calls normalized() onfull_clean.  This ensures that the database representation is assimilar to the relativedelta as possible (for instance, fractionaldays are always converted to hours).</longdescription>
</pkgmetadata>