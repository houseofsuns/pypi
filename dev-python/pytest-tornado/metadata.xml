<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>pytest-tornado.. image httpstravis-ci.orgeugeniypytest-tornado.svgbranchmaster    target httpstravis-ci.orgeugeniypytest-tornado.. image httpscoveralls.ioreposeugeniypytest-tornadobadge.svg    target httpscoveralls.ioreugeniypytest-tornadoA py.test_ plugin providing fixtures and markers to simplify testingof asynchronous tornado applications.Installation------------    pip install pytest-tornadoExample-------.. code-block python    import pytest    import tornado.web    class MainHandler(tornado.web.RequestHandler)        def get(self)            self.write(Hello, world)    application  tornado.web.Application([        (r, MainHandler),    ])    pytest.fixture    def app()        return application    pytest.mark.gen_test    def test_hello_world(http_client, base_url)        response  yield http_client.fetch(base_url)        assert response.code  200Running tests-------------    py.testFixtures--------io_loop    creates an instance of the tornado.ioloop.IOLoop_ for each test casehttp_port    get a port used by the test serverbase_url    Get an absolute base url for the test server,    for example httplocalhost59828.    Can also be used in a test with HTTPS fixture and will then return    a corresponding url, for example httplocalhost48372.http_server    start a tornado HTTP server, you must create an app fixture,    which returns the tornado.web.Application_ to be testedhttp_client    get an asynchronous HTTP clientThere is also the possibility to test applications with HTTPS.For running a server with HTTPS you need a certificate.https_port    Get a port used by the test server.https_server    Start a tornado HTTPS server. You must create an app fixture,    which returns the tornado.web.Application_ to be tested, and    an ssl_options fixture which returns the SSL options for the    tornado.httpserver.HTTPServer_.https_client    Get an asynchronous HTTP client.    In case your test uses an self-signed certificate you can set    verifyFalse on the fetch method.Show fixtures provided by the plugin    py.test --fixturesMarkers-------A gen_test marker lets you write a coroutine-style tests used with thetornado.gen_ module.. code-block python    pytest.mark.gen_test    def test_tornado(http_client)        response  yield http_client.fetch(httpwww.tornadoweb.org)        assert response.code  200This marker supports writing tests with asyncawait syntax as well.. code-block python    pytest.mark.gen_test    async def test_tornado(http_client)        response  await http_client.fetch(httpwww.tornadoweb.org)        assert response.code  200Marked tests will time out after 5 seconds. The timeout can be modified bysetting an ASYNC_TEST_TIMEOUT environment variable,--async-test-timeout command line argument or a marker argument... code-block python    pytest.mark.gen_test(timeout5)    def test_tornado(http_client)        yield http_client.fetch(httpwww.tornadoweb.org)The mark can also receive a run_sync flag, which if turned off will, instead of running the test synchronously, will add it as a coroutine and run the IOLoop (until the timeout). For instance, this allows to test things on both a client and a server at the same time... code-block python    pytest.mark.gen_test(run_syncFalse)    def test_tornado(http_server, http_client)        response  yield http_client.fetch(httplocalhost5555my_local_server_test)        assert response.body  Run on the same IOLoopShow markers provided by the plugin    py.test --markers.. _py.test httppytest.org.. _tornado.httpserver.HTTPServer httpswww.tornadoweb.orgenlatesthttpserver.htmlhttp-server.. _tornado.ioloop.IOLoop httptornado.readthedocs.orgenlatestioloop.htmlioloop-objects.. _tornado.web.Application httptornado.readthedocs.orgenlatestweb.htmlapplication-configuration.. _tornado.gen httptornado.readthedocs.orgenlatestgen.html</longdescription>
</pkgmetadata>