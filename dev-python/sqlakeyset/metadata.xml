<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>sqlakeyset offset-free paging for sqlalchemy.. image httpsimg.shields.iocirclecibuildghdjrobstepsqlakeysetlabeltests    alt Tests    target httpscircleci.comghdjrobstepsqlakeyset    .. image httpsimg.shields.iopypivsqlakeyset    alt PyPI    target httpspypi.orgprojectsqlakeyset    .. image httpsimg.shields.iocondavnconda-forgesqlakeyset.svg    alt conda-forge    target httpsanaconda.orgconda-forgesqlakeysetsqlakeyset implements keyset-based paging for SQLAlchemy (both ORM and core).This library is tested with PostgreSQL, MariaDBMySQL and SQLite. It should work with other SQLAlchemy-supported databases, too but you should verify the results are correct.Notice In accordance with Python 2s end-of-life, weve stopped supporting Python versions earlier than 3.4. If you really need it, the latest version to support Python 2 is 0.1.1559103842, but youll miss out on all the latest features and bugfixes from the latest version. You should be upgrading anywayBackground----------A lot of people use SQLs OFFSET syntax to implement paging of query results. The trouble with that is, the more pages you get through, the slower your query gets. Also, if the results youre paging through change frequently, its possible to skip over or repeat results between pages. Keyset paging avoids these problems Selecting even the millionth page is as fast as selecting the first.Getting Started---------------Heres how it works with a typical ORM query.. code-block python    from sqlakeyset import get_page    from sqlbag import S    from models import Book    with S(postgresqlbooks) as s   create a session        q  s.query(Book).order_by(Book.author, Book.title, Book.id)           gets the first page        page1  get_page(q, per_page20)         gets the key for the next page        next_page  page1.paging.next         gets the second page        page2  get_page(q, per_page20, pagenext_page)         returning to the first page, getting the key        previous_page  page2.paging.previous         the first page again, backwards from the previous page        page1  get_page(q, per_page20, pageprevious_page)         what if new items were added at the start        if page1.paging.has_previous             go back even further            previous_page  page1.paging.previous            page1  get_page(q, per_page20, pageprevious_page)Under the Hood--------------sqlakeyset does the following to your query in order to get the paged contents- adds a where clause, to get only rows after the specified row key.- if getting the previous page, reverses the order by direction in order the get the rows before the specified bookmark.- adds a limit clause, to fetch only enough items to fill the page, plus one additional (this additional row is used only to test for the existence of further pages after the current one, and is discarded from the results).- returns the page contents as an ordinary list that has an attached .paging attribute with the paging information for this and related pages.Page objects------------Paged itemsrows are returned in a Page object, which is a vanilla python list extended by an attached Paging object containing paging information.Properties such as next and previous return a pair containing the ordering key for the row, and a boolean to specify if the direction is forwards or backwards. We refer to such a pair (keyset, backwards) as a marker.In our above example, the marker specifying the second page might look like.. code-block python    (Joseph Heller, Catch 22, 123), FalseThe False means the query will fetch the page after the row containing Catch 22. This tuple contains two elements, title and id, to match the order by clause of the query.The page before this row would be specified as.. code-block python    (Joseph Heller, Catch 22, 123), TrueThe first and last pages are fetched with None instead of a tuple, so for the first page (this is also the default if the page parameter is not specified).. code-block python    None, FalseAnd the last page.. code-block python    None, TrueKeyset Serialization--------------------You will probably want to turn these markers into strings for passing around. sqlakeyset includes code to do this, and calls the resulting strings bookmarks. To get a serialized bookmark, just add bookmark_ to the name of the property that holds the keyset you want.Most commonly youll want next and previous, so.. code-block python     page.paging.bookmark_previous    i1i2015sBad Bloodi34     page.paging.bookmark_next    i1i2014sShake It Offi31sqlakeyset uses the python csv row serializer to serialize the bookmark values (using  instead of a , as the separator). Direction is indicated by  (forwardsnext), or  (backwardsprevious) at the start of the string.Limitations------------ Golden Rule Always ensure your keysets are unique per row. If you violate this condition you risk skipped rows and other nasty problems. The simplest way to do this is to always include your primary key column(s) at the end of your ordering columns.- Any rows containing null values in their keysets will be omitted from the results, so your ordering columns should be NOT NULL. (This is a consequence of the fact that comparisons against NULL are always false in SQL.) This may change in the future if we work out an alternative implementation but for now we recommend using coalesce as a workaround if you need to sort by nullable columns.. code-block python    from sqlakeyset import get_page    from sqlalchemy import func    from sqlbag import S    from models import Book    with S(postgresqlbooks) as s         If Book.cost can be NULL        q  s.query(Book).order_by(func.coalesce(Book.cost, 0), Book.id)         Assuming cost is non-negative, page1 will start with books where cost is null        page1  get_page(q, per_page20)- If youre using the in-built keyset serialization, this only handles basic datacolumn types so far (strings, ints, floats, datetimes, dates, booleans, and a few others). The serialization can be extended to serialize more advanced types as necessary (documentation on this is forthcoming).Documentation-------------Other than this README, there is some basic sphinx documentation, which you can build yourself with e.g. make -C doc html. Hopefully this will be available more conveniently soon - watch this space.Installation------------Assuming you have pip httpspip.pypa.io_ installed, all you need to do is install as follows.. code-block shell     pip install sqlakeysetThis will install sqlakeyset and also sqlalchemy if not already installed. Obviously youll need the necessary database driver for your chosen database to be installed also.</longdescription>
</pkgmetadata>