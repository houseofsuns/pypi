<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription> dbt-duckdb[DuckDB](httpduckdb.org) is an embedded database, similar to SQLite, but designed for OLAP-style analytics.It is crazy fast and allows you to read and write data stored in CSV and Parquet files directly, without requiring you to loadthem into the database first.[dbt](httpgetdbt.com) is the best way to manage a collection of data transformations written in SQL or Python for analyticsand data science. dbt-duckdb is the project that ties DuckDB and dbt together, allowing you to create a [Modern Data Stack InA Box](httpsduckdb.org20221012modern-data-stack-in-a-box.html) or a simple and powerful data lakehouse- no Java or Scalarequired. InstallationThis project is hosted on PyPI, so you should be able to install it and the necessary dependencies viapip3 install dbt-duckdbThe latest supported version targets dbt-core 1.3.x and duckdb version 0.5.x, but we work hard to ensure that newerversions of DuckDB will continue to work with the adapter as they are released. If you would like to use our new (and experimental)support for persisting the tables that DuckDB creates to the [AWS Glue Catalog](httpsaws.amazon.comglue), you should installdbt-duckdb[glue] in order to get the AWS dependencies as well. Configuring Your ProfileA minimal dbt-duckdb profile only needs two settings, type and pathdefault  outputs   dev     type duckdb     path tmpdbt.duckdb  target devThe path field should normally be the path to a local DuckDB file on your filesystem, but it can also be set equal to memory if youwould like to run an in-memory only version of dbt-duckdb. Keep in mind that if you are using the in-memory mode,any models that you want to keep from the dbt run will need to be persisted using one of the external materialization strategies described below.The database property is special prior to DuckDB 0.7.0 and dbt-duckdb 1.4.0, it was always set to main,because DuckDB did not have the concept of multiple databases accessible from a single connection.As of dbt-duckdb 1.4.0 and DuckDB 0.7.0, the value of the database property is automatically set to the basename of thefile in the path argument with the suffix removed (so for example if the path is tmpadbfile.duckdb the database argument will beautomatically set to dbfile). If you are running with the path equal to memory, then the name of the database will be memory. DuckDB Extensions and SettingsYou can load any supported [DuckDB extensions](httpsduckdb.orgdocsextensionsoverview) by listing them inthe extensions field in your profile. You can also set any additional [DuckDB configuration options](httpsduckdb.orgdocssqlconfiguration)via the settings field, including options that are supported in any loaded extensions. For example, to be able to connect to S3 and readwriteParquet files using an AWS access key and secret, your profile would look something like thisdefault  outputs    dev      type duckdb      path tmpdbt.duckdb      extensions        - httpfs        - parquet      settings        s3_region my-aws-region        s3_access_key_id {{ env_var(S3_ACCESS_KEY_ID) }}        s3_secret_access_key {{ env_var(S3_SECRET_ACCESS_KEY) }}  target dev Fetching credentials from contextInstead of specifying the credentials through the settings block, you can also use the use_credential_provider property. If you set this to aws (currently the only supported implementation) and you have boto3 installed in your python environment, we will fetch your AWS credentials using the credential provider chain as described [here](httpsboto3.amazonaws.comv1documentationapilatestguidecredentials.html). This means that you can use any supported mechanism from AWS to obtain credentials (e.g., web identity tokens). Attaching Additional DatabasesDuckDB version 0.7.0 and dbt-duckdb version 1.4.0 support [attaching additional databases](httpsduckdb.orgdocssqlstatementsattach.html) to your dbt-duckdb run so that you can readand write from multiple databases. Additional databases may be configured using [dbt run hooks](httpsdocs.getdbt.comdocsbuildhooks-operations) or via the attach argumentin your profiledefault  outputs    dev      type duckdb      path tmpdbt.duckdb      attach        - path tmpother.duckdb        - path .yetanother.duckdb          alias yet_another        - path s3yepeventhisworks.duckdb          read_only true        - path sqlite.db          type sqliteThe attached databases may be referred to in your dbt sources and models by either the basename of the database file minus its suffix (e.g., tmpother.duckdb is the other databaseand s3yepeventhisworks.duckdb is the works database) or by an alias that you specify (so the .yetanother.duckdb database in the above configuration is referred toas yet_another instead of another.) Note that these additional databases do not necessarily have to be DuckDB files DuckDBs storage and catalog engines are pluggable, andDuckDB 0.7.0 ships with support for reading and writing from attached SQLite databases. You can indicate the type of the database you are connecting to via the type argument,which currently supports duckdb and sqlite. External Materializations and SourcesOne of DuckDBs most powerful features is its ability to read and write CSV and Parquet files directly, without needing to importexportthem from the database first. In dbt-duckdb, we support creating models that are backed by external files via the external materializationstrategy{{ config(materializedexternal, locationlocaldirectoryfile.parquet) }}SELECT m., s.id IS NOT NULL as has_source_idFROM {{ ref(upstream_model) }} mLEFT JOIN {{ source(upstream, source) }} s USING (id) Option  Default  Description ---      ---     --- location  {{ name }}.{{ format }}  The path to write the external materialization to. See below for more details. format  parquet  The format of the external file, either parquet or csv. delimiter  ,     For CSV files, the delimiter to use for fields. glue_register  false  If true, try to register the file created by this model with the AWS Glue Catalog. glue_database  default  The name of the AWS Glue database to register the model with.If no location argument is specified, then the external file will be named after the model.sql (or model.py) file that defined itwith an extension that matches the file format (either .parquet or .csv). By default, external materializations are createdrelative to the current working directory, but you can change the default directory (or S3 bucketprefix) by specifying theexternal_root setting in your DuckDB profiledefault  outputs    dev      type duckdb      path tmpdbt.duckdb      extensions        - httpfs        - parquet      settings        s3_region my-aws-region        s3_access_key_id {{ env_var(S3_ACCESS_KEY_ID) }}        s3_secret_access_key {{ env_var(S3_SECRET_ACCESS_KEY) }}      external_root s3my-bucketmy-prefix-path  target dev Re-running external models with an in-memory version of dbt-duckdbWhen using memory as the DuckDB database, subsequent dbt runs can fail when selecting a subset of models that depend on external tables. This is because external Parquet or CSV files are only registered as  DuckDB views when they are created, not when they are referenced. To overcome this issue we have provided the register_upstream_external_models macro that can be triggered at the beginning of a run. To enable this automatic registration, place the following in your dbt_project.yml fileyamlon-run-start  - {{ register_upstream_external_models() }} External sourcesdbt-duckdb also includes support for referencing external CSV and Parquet files as dbt sources via the external_locationmeta optionsources  - name external_source    meta      external_location s3my-bucketmy-sources{name}.parquet    tables      - name source1      - name source2Here, the meta options on external_source defines external_location as an [f-string](httpspeps.python.orgpep-0498) thatallows us to express a pattern that indicates the location of any of the tables defined for that source. So a dbt model likeSELECT FROM {{ source(external_source, source1) }}will be compiled asSELECT FROM s3my-bucketmy-sourcessource1.parquetIf one of the source tables deviates from the pattern or needs some other special handling, then the external_location can also be set on the metaoptions for the table itself, for examplesources  - name external_source    meta      external_location s3my-bucketmy-sources{name}.parquet    tables      - name source1      - name source2        meta          external_location read_parquet([s3my-bucketmy-sourcessource2a.parquet, s3my-bucketmy-sourcessource2b.parquet]) Python Supportdbt added support for [Python models in version 1.3.0](httpsdocs.getdbt.comdocsbuildpython-models). For most data platforms,dbt will package up the Python code defined in a .py file and ship it off to be executed in whatever Python environment thatdata platform supports. However, in dbt-duckdb, the local machine is the data platform, and so we support executing any Pythoncode that will run on your machine via an [exec](httpsrealpython.compython-exec) call. The value of the dbt.ref and dbt.sourcefunctions will be a [DuckDB Relation](httpsduckdb.orgdocsapipythonreference) object that can be easily converted into aPandas DataFrame or Arrow table, and the return value of the def models function can be either a DuckDB Relation, a Pandas DataFrame,or an Arrow Table. RoadmapThings that we would like to add in the near future Support for Delta and Iceberg external table formats (both as sources and destinations) Make dbts incremental models and snapshots work with external materializations Make AWS Glue registration a first-class concept and add support for SnowflakeBigQuery registrations</longdescription>
</pkgmetadata>