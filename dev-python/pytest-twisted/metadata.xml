<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE pkgmetadata SYSTEM "http://www.gentoo.org/dtd/metadata.dtd">
<pkgmetadata>
	<maintainer type="person">
		<email>gentoo@houseofsuns.org</email>
		<name>Markus Walter</name>
	</maintainer>
	<longdescription>.. -- mode rst coding utf-8 --pytest-twisted - test twisted code with pytestPyPI Pythons Travis AppVeyor Actions BlackAuthors Ralf Schmitt, Kyle Altendorf, Victor TitorVersion 1.14.0Date    2022-10-16Download httpspypi.python.orgpypipytest-twisteddownloadsCode httpsgithub.compytest-devpytest-twistedpytest-twisted is a plugin for pytest, which allows to test code,which uses the twisted framework. test functions can return Deferredobjects and pytest will wait for their completion with this plugin.NOTICE Python 3.8 with asyncio supportIn Python 3.8, asyncio changed the default loop implementation to usetheir proactor.  The proactor does not implement some methods used byTwisteds asyncio support.  The result is a NotImplementedErrorexception such as below... code-block pytb    snip      File cprojectspytest-twisted.toxpy38-asyncioreactorlibsite-packagestwistedinternetasyncioreactor.py, line 320, in install        reactor  AsyncioSelectorReactor(eventloop)      File cprojectspytest-twisted.toxpy38-asyncioreactorlibsite-packagestwistedinternetasyncioreactor.py, line 69, in __init__        super().__init__()      File cprojectspytest-twisted.toxpy38-asyncioreactorlibsite-packagestwistedinternetbase.py, line 571, in __init__        self.installWaker()      File cprojectspytest-twisted.toxpy38-asyncioreactorlibsite-packagestwistedinternetposixbase.py, line 286, in installWaker        self.addReader(self.waker)      File cprojectspytest-twisted.toxpy38-asyncioreactorlibsite-packagestwistedinternetasyncioreactor.py, line 151, in addReader        self._asyncioEventloop.add_reader(fd, callWithLogger, reader,      File CPython38-x64Libasyncioevents.py, line 501, in add_reader        raise NotImplementedError    NotImplementedErrorThe previous default, the selector loop, still works but you have toexplicitly set it and do so early. The following conftest.py is providedfor reference... code-block python3    import sys    import pytest    import pytest_twisted    pytest.hookimpl(tryfirstTrue)    def pytest_configure(config)         httpstwistedmatrix.comtracticket9766         httpsgithub.compytest-devpytest-twistedissues80        if (            config.getoption(reactor, default)  asyncio            and sys.platform  win32            and sys.version_info  (3, 8)        )            import asyncio            selector_policy  asyncio.WindowsSelectorEventLoopPolicy()            asyncio.set_event_loop_policy(selector_policy)Python 2 support plansAt some point it may become impractical to retain Python 2 support.Given the small size and very low amount of development it seemslikely that this will not be a near term issue.  While I personallyhave no need for Python 2 support I try to err on the side of beinghelpful so support will not be explicitly removed just to not have tothink about it.  If major issues are reported and neither myself northe community have time to resolve them then options will beconsidered.InstallationInstall the plugin as below... code-block sh    pip install pytest-twistedUsing the pluginThe plugin is available after installation and can be disabled using-p notwisted.By default twisted.internet.default is used to install the reactor.This creates the same reactor that import twisted.internet.reactorwould.  Alternative reactors can be specified using the --reactoroption.  This presently supports qt5reactor for use with pyqt5and pytest-qt as well as asyncio. This guide_ describes how to addsupport for a new reactor.The reactor is automatically created prior to the first test but canbe explicitly installed earlier by callingpytest_twisted.init_default_reactor() or the corresponding functionfor the desired alternate reactor.inlineCallbacksUsing twisted.internet.defer.inlineCallbacks as a decorator for testfunctions, which use fixtures, does not work. Please usepytest_twisted.inlineCallbacks instead... code-block python  pytest_twisted.inlineCallbacks  def test_some_stuff(tmpdir)      res  yield threads.deferToThread(os.listdir, tmpdir.strpath)      assert res  []ensureDeferredUsing twisted.internet.defer.ensureDeferred as a decorator for testfunctions, which use fixtures, does not work. Please usepytest_twisted.ensureDeferred instead... code-block python  pytest_twisted.ensureDeferred  async def test_some_stuff(tmpdir)      res  await threads.deferToThread(os.listdir, tmpdir.strpath)      assert res  []Waiting for deferreds in fixturespytest_twisted.blockon allows fixtures to wait for deferreds... code-block python  pytest.fixture  def val()      d  defer.Deferred()      reactor.callLater(1.0, d.callback, 10)      return pytest_twisted.blockon(d)asyncawait fixturesasyncawait fixtures can be used along with yield for normalpytest fixture semantics of setup, value, and teardown.  At present onlyfunction and module scope are supported... code-block python   No yield (coroutine function)     - use pytest_twisted.async_fixture()  pytest_twisted.async_fixture()  async def foo()      d  defer.Deferred()      reactor.callLater(0.01, d.callback, 42)      value  await d      return value   With yield (asynchronous generator)     - use pytest_twisted.async_yield_fixture()  pytest_twisted.async_yield_fixture()  async def foo_with_teardown()      d1, d2  defer.Deferred(), defer.Deferred()      reactor.callLater(0.01, d1.callback, 42)      reactor.callLater(0.02, d2.callback, 37)      value  await d1      yield value      await d2Hypothesispytest-twisted can be used with Hypothesis... code-block python   hypothesis.given(xhypothesis.strategies.integers())   pytest_twisted.ensureDeferred   async def test_async(x)       assert isinstance(x, int)The twisted greenletSome libraries (e.g. corotwine) need to know the greenlet, which isrunning the twisted reactor. Its available from thetwisted_greenlet fixture. The following code can be used to makecorotwine work with pytest-twisted... code-block python  pytest.fixture(scopesession, autouseTrue)  def set_MAIN(request, twisted_greenlet)      from corotwine import protocol      protocol.MAIN  twisted_greenletThats (almost) all.Deprecations----v1.9----pytest.blockon    Use pytest_twisted.blockonpytest.inlineCallbacks    Use pytest_twisted.inlineCallbacks.. PyPI image httpsimg.shields.iopypivpytest-twisted.svg   alt PyPI version   target httpspypi.python.orgpypipytest-twisted.. Pythons image httpsimg.shields.iopypipyversionspytest-twisted.svg   alt Supported Python versions   target httpspypi.python.orgpypipytest-twisted.. Travis image httpstravis-ci.orgpytest-devpytest-twisted.svgbranchmaster   alt Travis build status   target httpstravis-ci.orgpytest-devpytest-twisted.. AppVeyor image httpsci.appveyor.comapiprojectsstatuseb1vp9hysp463c66branchmastersvgtrue   alt AppVeyor build status   target httpsci.appveyor.comprojectpytestbotpytest-twisted.. Actions image httpsimg.shields.iogithubworkflowstatuspytest-devpytest-twistedCImasterlogoGitHub-Actions   alt GitHub Actions build status   target httpsgithub.compytest-devpytest-twistedactionsquerybranch3Amaster.. Black image httpsimg.shields.iobadgecode20style-black-000000.svg   alt Black code style   target httpsgithub.comambvblack.. _guide CONTRIBUTING.rst</longdescription>
</pkgmetadata>